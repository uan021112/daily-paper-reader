---
title: "UniMesh: Unifying 3D Mesh Understanding and Generation"
title_zh: UniMesh：统一 3D 网格理解与生成
authors: "Peng Huang, Yifeng Chen, Zeyu Zhang, Hao Tang"
date: 2026-04-19
pdf: "https://arxiv.org/pdf/2604.17472v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 统一的3D网格理解与生成框架
tldr: 针对3D视觉中理解与生成任务长期割裂的问题，本文提出UniMesh统一框架。该框架通过创新的Mesh Head连接扩散生成与隐式解码，并引入Chain of Mesh机制实现迭代式语义编辑，同时结合自反思机制优化高层理解任务。UniMesh不仅在标准基准上表现优异，还实现了生成与理解的相互增强，为全方位3D场景建模提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.17472v1/fig-001.webp\", \"caption\": \"Table 1: Comparison on 3D Object Captioning. The experiment is conducted on a subset of Cap3D including 3186 3D objects.\", \"page\": 11, \"index\": 1, \"width\": 716, \"height\": 202}, {\"url\": \"assets/figures/arxiv/2604.17472v1/fig-002.webp\", \"caption\": \"Fig. 4: Pipeline of Self-Reflection. The pipeline progresses from a 3D object, through rendering, view selection, to model captioning. The Reflexion agent continuously corrects errors through iterative loops, proposes improvements, and eventually provides the final answer.\", \"page\": 8, \"index\": 2, \"width\": 669, \"height\": 268}, {\"url\": \"assets/figures/arxiv/2604.17472v1/fig-003.webp\", \"caption\": \"Fig. 2: Framework of UniMesh. Given a text prompt or modification instruction, BAGEL with Qwen generates an image latent, which is transformed by the Mesh Head into a conditioning latent for Hunyuan3D to produce a 3D mesh. The reference image latent of the generated mesh can be fed back into BAGEL for iterative refinement via Chain-of-Mesh, while self-reflection enables semantic feedback loops for understanding tasks.\", \"page\": 4, \"index\": 3, \"width\": 633, \"height\": 213}]"
motivation: 旨在解决3D理解与生成任务之间架构碎片化、知识迁移困难以及缺乏整体场景建模能力的问题。
method: 提出包含Mesh Head接口、Chain of Mesh迭代推理机制及“行动者-评估者-自反思”三元组的统一框架。
result: 在标准基准测试中达到竞争性水平，并展现出卓越的迭代编辑能力和理解与生成的互促效应。
conclusion: UniMesh成功统一了3D网格的理解与生成任务，通过闭环反馈和自反思机制显著提升了模型的综合性能。
---

## 摘要
3D 视觉的最新进展催生了专门用于 3D 理解（如形状分类、分割、重建）或 3D 生成（如合成、补全和编辑）的专用模型。然而，这些任务通常被孤立地处理，导致架构和表示碎片化，阻碍了知识迁移和整体场景建模。为了应对这些挑战，我们提出了 UniMesh，这是一个在单一架构中联合学习 3D 生成与理解的统一框架。首先，我们引入了一种新型的 Mesh Head，作为跨模型接口，连接了基于扩散的图像生成与隐式形状解码器。其次，我们开发了 Chain of Mesh (CoM)，这是迭代推理的一种几何实例化，通过闭环潜空间、提示和再生成的循环，实现了用户驱动的语义网格编辑。第三，我们结合了一种基于“执行者-评估者-自我反思”三元组（Actor-Evaluator-Self-reflection triad）的自我反思机制，用于诊断和纠正 3D 描述（3D captioning）等高级任务中的错误。实验结果表明，UniMesh 不仅在标准基准测试中取得了具有竞争力的性能，还开启了迭代编辑以及生成与理解之间相互增强的新功能。

## Abstract
Recent advances in 3D vision have led to specialized models for either 3D understanding (e.g., shape classification, segmentation, reconstruction) or 3D generation (e.g., synthesis, completion, and editing). However, these tasks are often tackled in isolation, resulting in fragmented architectures and representations that hinder knowledge transfer and holistic scene modeling. To address these challenges, we propose UniMesh, a unified framework that jointly learns 3D generation and understanding within a single architecture. First, we introduce a novel Mesh Head that acts as a cross model interface, bridging diffusion based image generation with implicit shape decoders. Second, we develop Chain of Mesh (CoM), a geometric instantiation of iterative reasoning that enables user driven semantic mesh editing through a closed loop latent, prompting, and re generation cycle. Third, we incorporate a self reflection mechanism based on an Actor Evaluator Self reflection triad to diagnose and correct failures in high level tasks like 3D captioning. Experimental results demonstrate that UniMesh not only achieves competitive performance on standard benchmarks but also unlocks novel capabilities in iterative editing and mutual enhancement between generation and understanding. Code: https://github.com/AIGeeksGroup/UniMesh. Website: https://aigeeksgroup.github.io/UniMesh.

---

## 论文详细总结（自动生成）

这篇论文提出了 **UniMesh**，旨在打破 3D 视觉领域中“理解”与“生成”任务长期孤立的局面。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前的 3D 视觉研究被分割为两个独立阵营：**3D 理解**（如分类、分割、描述）和 **3D 生成**（如合成、编辑）。这种碎片化导致了架构不兼容、知识无法迁移，且模型无法像人类一样通过理解自己的创作来进一步改进。
*   **研究动机**：作者希望构建一个统一的框架，使模型能够同时具备感知和创造能力，并支持类似大语言模型（LLM）那样的迭代推理过程，从而实现闭环的 3D 场景建模。

### 2. 方法论
UniMesh 的核心思想是通过一个统一的架构实现生成与理解的相互增强，关键技术包括：
*   **Mesh Head（跨模型接口）**：这是一个创新的模块，直接将 BAGEL（基于扩散的图像生成模型）的图像潜空间（Latent Space）映射到 Hunyuan3D 的隐式形状解码器条件空间。这种设计绕过了损耗严重的 RGB 图像重建步骤，保留了更多的几何细节。
*   **Chain-of-Mesh (CoM，网格链推理)**：受 Chain-of-Thought 启发，CoM 是一种几何迭代推理机制。用户输入修改指令后，模型利用当前网格的图像潜空间和新提示词，在潜空间内进行更新并重新生成，实现无需重训练的零样本语义编辑（如“给狮子加个皇冠”）。
*   **自我反思机制（Self-reflection）**：针对 3D 描述等理解任务，引入“行动者（Actor）- 评估者（Evaluator）- 自我反思（Self-reflection）”三元组。模型先生成描述，再自我评估，若发现错误则通过文字反思诊断原因并重新生成，从而提升理解的准确性。

### 3. 实验设计
*   **数据集**：
    *   **3D 生成**：使用 DreamFusion 的 404 个去重提示词进行测试。
    *   **3D 理解**：使用 Cap3D 数据集的子集（3186 个 3D 对象）进行描述任务评估。
*   **Benchmark 与对比方法**：
    *   **生成对比**：与 InstantMesh、LGM、Flex3D、OpenLRM 等主流 3D 生成模型对比，使用 CLIP 图像-文本相似度和 ViCLIP 视频相似度作为指标。
    *   **理解对比**：与 Cap3D、LLaVA-3D、Qwen2.5-VL、InternVL3.5、Kimi-VL 等多模态大模型对比，指标包括 FID、R@k 检索率和词汇相似度。

### 4. 资源与算力
*   **技术细节**：训练过程中使用了 **LoRA**（秩 r=4，alpha=8）对 Mesh Head 进行高效微调。
*   **模型变体**：训练时采用了 Hunyuan3D-2 Mini Turbo 版本并开启 FlashVDM 以节省显存，推理时切换回全量模型以保证质量。
*   **算力说明**：文中**未明确提及**具体的 GPU 型号、数量及总训练时长，但提到了通过参数高效微调（LoRA）和轻量化组件来降低资源消耗。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3D 生成、3D 描述、迭代编辑三大任务。描述任务使用了超过 3000 个样本，消融实验针对 200 个对象验证了视图选择（DiffuRank）、CoT 和反思机制的有效性。
*   **充分性与客观性**：实验对比了从专门的 3D 模型到通用的多模态大模型（VLM），覆盖面较广。使用了多种自动化指标（CLIP, FID, R@k），并提供了丰富的定性可视化结果（如图 1 的迭代编辑过程），实验设计较为客观、公平。

### 6. 主要结论与发现
*   **性能领先**：UniMesh 在文本转 3D 任务中取得了 0.296 的 CLIP 分数，优于所有对比的开源模型。
*   **理解增强**：通过自反思机制，UniMesh 在 3D 描述任务中取得了最低的 FID 分数（0.113），证明了其生成的描述更符合真实分布。
*   **涌现能力**：统一架构解锁了“零样本迭代编辑”能力，模型能理解复杂的语义修改指令并保持几何一致性。

### 7. 优点
*   **架构统一**：首次真正意义上在单一流水线中融合了高质量 3D 生成与深度语义理解。
*   **潜空间桥接**：Mesh Head 的设计避免了中间图像渲染带来的信息损失，提高了几何保真度。
*   **闭环推理**：引入 CoM 和自反思机制，使 3D 模型具备了类似人类的“创作-评估-修改”能力。

### 8. 不足与局限
*   **维度依赖**：3D 理解仍然高度依赖于 2D 渲染视图（6 个视角），而非直接处理原生 3D 几何表示（如点云或体素）。
*   **反思瓶颈**：自反思机制的效果受限于评估者（Evaluator）的能力，如果评估者判断错误，可能会导致错误的自我修正。
*   **应用限制**：虽然支持语义编辑，但对于极细微的局部几何变形（非语义层面的修改）可能仍有挑战。

（完）
