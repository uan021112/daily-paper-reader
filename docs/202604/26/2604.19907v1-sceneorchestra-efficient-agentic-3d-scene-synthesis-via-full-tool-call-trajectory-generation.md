---
title: "SceneOrchestra: Efficient Agentic 3D Scene Synthesis via Full Tool-Call Trajectory Generation"
title_zh: SceneOrchestra：通过全工具调用轨迹生成实现高效的智能体化 3D 场景合成
authors: "Yun He, Kelin Yu, Matthias Zwicker"
date: 2026-04-21
pdf: "https://arxiv.org/pdf/2604.19907v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 用于3D场景合成与生成的智能体框架
tldr: 针对现有3D场景合成框架中“执行-评审-反思”循环导致的效率低下和工具调用不佳问题，本文提出SceneOrchestra。该框架通过可训练的编排器直接生成完整的工具调用轨迹，无需每步渲染评审。利用两阶段训练策略，结合判别器优化轨迹质量并将其能力蒸馏至编排器。实验证明，该方法在提升场景合成质量的同时，显著降低了运行时间，实现了高效且高质量的3D场景生成。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.19907v1/fig-001.webp\", \"caption\": \"Fig. 2: Training Paradigm. Our training consists of two phases. In the independent training phase, we train the orchestrator and discriminator separately to develop their initial capabilities. The orchestrator undergoes four stages: 1) stepwise SFT, 2) trajectory-level SFT, 3) stepwise DPO, and 4) trajectory-level DPO, learning contextaware tool selection and complete trajectory generation. Meanwhile, the discriminator learns to select the optimal trajectory from multiple candidates. In the interleaved training phase, we adapt the discriminator to the orchestrator’s evolving trajectory distribution, then distill this discriminative capability back into the orchestrator. This enables inference with only the orchestrator, eliminating the need for the discriminator.\", \"page\": 6, \"index\": 1, \"width\": 718, \"height\": 244}, {\"url\": \"assets/figures/arxiv/2604.19907v1/fig-002.webp\", \"caption\": \"Table 1: Quantitative comparison with baselines on seen and unseen room types. We use the template based instruction \\\"Design me a <room_type>\\\". For each room type, all methods are executed five times and the results are averaged. Overall, our method achieves the best performance on nearly all metrics.\", \"page\": 11, \"index\": 2, \"width\": 723, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2604.19907v1/fig-003.webp\", \"caption\": \"Fig. 7: Example of user study.\", \"page\": 21, \"index\": 3, \"width\": 617, \"height\": 793}, {\"url\": \"assets/figures/arxiv/2604.19907v1/fig-004.webp\", \"caption\": \"Fig. 3: Qualitative comparison with baselines on both seen (bathroom, meeting room) and unseen (bedroom, gym) room types. Our method achieves higher visual realism, more plausible layouts, and richer details.\", \"page\": 10, \"index\": 4, \"width\": 723, \"height\": 455}, {\"url\": \"assets/figures/arxiv/2604.19907v1/fig-005.webp\", \"caption\": \"Fig. 5: Extra qualitative comparison with baselines on both seen (waiting room) and unseen (living room, restaurant) room types. “Ours 1” and “Ours 2” denote two different scenes generated by our method from the same room type. Our method not only produces more realistic scenes, but also generates diverse results.\", \"page\": 19, \"index\": 5, \"width\": 717, \"height\": 899}, {\"url\": \"assets/figures/arxiv/2604.19907v1/fig-006.webp\", \"caption\": \"Table 2: Generation time comparison (minutes). We report the average generation time for each room type. All methods run on a single A6000 GPU. Monolithic methods are much faster than agentic approaches, since agentic methods always invoke multiple tools. By optimizing the execution flow and removing the step-by-step review process, our method substantially reduces the runtime of agentic generation.\", \"page\": 12, \"index\": 6, \"width\": 689, \"height\": 259}, {\"url\": \"assets/figures/arxiv/2604.19907v1/fig-007.webp\", \"caption\": \"Fig. 6: Extra qualitative results on complex instructions. Our method follows instructions more faithfully and produces more realistic layouts.\", \"page\": 20, \"index\": 7, \"width\": 719, \"height\": 531}]"
motivation: 现有的3D场景合成代理依赖启发式规则和逐步评审循环，导致执行效率低、工具调用冗余且推理延迟高。
method: 提出一种包含编排器和判别器的训练框架，通过两阶段策略学习生成完整的工具调用轨迹，并将判别能力蒸馏回编排器。
result: 实验结果显示，SceneOrchestra在保持或提升场景合成质量的同时，大幅减少了推理过程中的运行时间。
conclusion: 该研究证明了通过全轨迹生成和模型蒸馏可以有效优化3D场景合成的编排效率，为构建高效的智能体框架提供了新思路。
---

## 摘要
最近用于 3D 场景合成的智能体框架通过集成异构生成和编辑工具，提升了场景的真实感和多样性。这些工具被组织成由现成的大语言模型（LLM）编排的工作流。目前的方法通常采用“执行-评审-反思”循环：在每一步中，编排器执行一个工具，渲染中间结果以供评审，然后决定下一步的工具及其参数。然而，这种设计存在两个关键局限性。首先，下一步的工具选择和参数配置是由启发式规则驱动的，这可能导致次优的执行流、不必要的工具调用、输出质量下降以及运行时间增加。其次，在每一步之后渲染和评审中间结果会引入额外的延迟。为了解决这些问题，我们提出了 SceneOrchestra，这是一个可训练的编排框架，它优化了工具调用执行流并消除了逐步评审循环，从而同时提高了效率和输出质量。SceneOrchestra 由一个编排器和一个判别器组成，我们通过两阶段训练策略对其进行微调。在第一阶段，编排器学习上下文感知的工具选择和完整的工具调用轨迹生成，而判别器则被训练用于评估完整轨迹的质量，使其能够从多个候选轨迹中选择最佳轨迹。在第二阶段，我们进行交替训练，判别器适应编排器不断演变的轨迹分布，并将其判别能力蒸馏回编排器。在推理时，我们仅使用编排器根据指令生成并执行完整的工具调用轨迹，而无需判别器。大量实验表明，与以往的工作相比，我们的方法在减少运行时间的同时，实现了最先进的场景质量。

## Abstract
Recent agentic frameworks for 3D scene synthesis have advanced realism and diversity by integrating heterogeneous generation and editing tools. These tools are organized into workflows orchestrated by an off-the-shelf LLM. Current approaches typically adopt an execute-review-reflect loop: at each step, the orchestrator executes a tool, renders intermediate results for review, and then decides on the tool and its parameters for the next step. However, this design has two key limitations. First, next-step tool selection and parameter configuration are driven by heuristic rules, which can lead to suboptimal execution flows, unnecessary tool invocations, degraded output quality, and increased runtime. Second, rendering and reviewing intermediate results after each step introduces additional latency. To address these issues, we propose SceneOrchestra, a trainable orchestration framework that optimizes the tool-call execution flow and eliminates the step-by-step review loop, improving both efficiency and output quality. SceneOrchestra consists of an orchestrator and a discriminator, which we fine-tune with a two-phase training strategy. In the first phase, the orchestrator learns context-aware tool selection and complete tool-call trajectory generation, while the discriminator is trained to assess the quality of full trajectories, enabling it to select the best trajectory from multiple candidates. In the second phase, we perform interleaved training, where the discriminator adapts to the orchestrator's evolving trajectory distribution and distills its discriminative capability back into the orchestrator. At inference, we only use the orchestrator to generate and execute full tool-call trajectories from instructions, without requiring the discriminator. Extensive experiments show that our method achieves state-of-the-art scene quality while reducing runtime compared to previous work.

---

## 论文详细总结（自动生成）

这是一份关于论文《SceneOrchestra: Efficient Agentic 3D Scene Synthesis via Full Tool-Call Trajectory Generation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **研究背景**：3D 场景合成在虚拟现实、具身 AI 和机器人仿真中至关重要。现有的方法（如 SceneWeaver）开始采用“智能体（Agentic）”框架，通过 LLM 编排多种异构工具（如布局生成、物体放置、物理优化等）来提升场景质量。
*   **核心问题**：现有的智能体框架通常采用“执行-评审-反思（Execute-Review-Reflect）”的逐步循环。这种模式存在两大缺陷：
    1.  **效率低下**：每一步都需要渲染中间结果并由 LLM 评审，导致极高的推理延迟（生成一个场景平均需 1.5 小时）。
    2.  **决策次优**：依赖现成 LLM 的启发式规则进行工具选择，往往导致冗余调用或生成质量不佳。
*   **研究目标**：开发一个可训练的编排框架，能够“一阶段”生成完整的工具调用轨迹，在保证高质量的同时大幅提升合成效率。

### 2. 方法论：核心思想与技术细节
SceneOrchestra 由**编排器（Orchestrator）**和**判别器（Discriminator）**两个微调后的 LLM 组成，核心思想是**全轨迹生成**与**能力蒸馏**。

*   **两阶段训练策略**：
    *   **第一阶段：独立训练（Independent Training）**
        *   **编排器训练**：分为四个子阶段：1) 逐步监督微调（Stepwise SFT）；2) 轨迹级监督微调（Trajectory-level SFT）；3) 逐步直接偏好优化（Stepwise DPO）；4) 轨迹级直接偏好优化（Trajectory-level DPO）。通过这种课程学习，模型从学习“下一步怎么选”进化到“全局规划整条路径”。
        *   **判别器训练**：学习根据“组合得分”（Composition Score，综合考虑物理质量、视觉语义和时间成本）对不同轨迹进行排序。
    *   **第二阶段：交替训练（Interleaved Training）**
        *   判别器适应编排器演变后的轨迹分布，并将其判别能力（即对高质量轨迹的偏好）通过 DPO 蒸馏回编排器。
*   **核心公式**：
    *   **质量得分 ($Q$)**：结合物理指标（物体数量、越界、碰撞）和视觉语义指标（真实感、功能性、布局、完整性）。
    *   **组合得分 ($C$)**：$C = Q - \gamma T$，其中 $T$ 是运行时间，$\gamma$ 是平衡质量与效率的系数。
*   **推理流程**：推理时仅需编排器，根据用户指令直接输出完整的工具调用序列并一次性执行，无需中间评审。

### 3. 实验设计
*   **场景与数据集**：评估了 10 种房间类型，分为**已见（Seen）**类型（训练过但指令不同）和**未见（Unseen）**类型（如卧室、健身房、餐厅等）。
*   **Benchmark 与对比方法**：
    *   **单体方法**：LayoutGPT, Holodeck, I-Design。
    *   **智能体方法**：SceneWeaver（SOTA 基准）。
*   **评估指标**：使用 GPT-4 作为裁判，评估物理有效性（物体数、碰撞数等）和视觉语义质量（真实感、功能性等），并统计运行时间。

### 4. 资源与算力
*   **硬件环境**：所有训练和推理实验均在**单台 NVIDIA RTX A6000 GPU** 上完成。
*   **模型基础**：使用 Qwen3-4B-Instruct 作为编排器和判别器的基础模型。
*   **训练时长**：文中未明确给出总训练小时数，但提到 S1 训练集包含 100 条指令，S2/S3 各 50 条，且单次轨迹执行耗时较长，暗示训练数据收集成本较高。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对 10 种房间类型进行了定量对比，每种类型执行 5 次取平均值。
    *   针对 10 条复杂的长指令进行了专门的细粒度控制能力测试。
    *   进行了包含 18 名参与者的用户主观研究（User Study）。
*   **消融实验**：非常充分，分别验证了 DPO、逐步训练、判别器以及交替训练对最终性能的贡献。
*   **客观性**：对比了多种不同架构的方法，并使用了自动化指标与人工主观评价相结合的方式，实验设计较为公平。

### 6. 主要结论与发现
*   **质量提升**：SceneOrchestra 在几乎所有物理和视觉指标上均优于 SceneWeaver 和其他单体方法，生成的布局更合理、细节更丰富。
*   **效率飞跃**：相比 SceneWeaver，推理时间**减少了约 70%**（例如从平均 91 分钟降至 27.7 分钟）。
*   **泛化能力**：在未见过的房间类型和复杂指令下表现稳健，证明了微调后的编排器具备强大的语义理解和规划能力。

### 7. 优点与亮点
*   **效率优化**：成功打破了智能体框架中“渲染-评审”的瓶颈，实现了端到端的轨迹生成。
*   **训练方案**：提出的两阶段、四步走训练法（SFT+DPO+蒸馏）为提升 LLM 在特定任务中的工具编排能力提供了有效范式。
*   **综合考量**：在损失函数中引入时间惩罚，使模型自发学习更简洁、高效的工具调用路径。

### 8. 不足与局限
*   **幻觉风险**：编排器偶尔会产生幻觉，输出错误的工具名称或格式，导致调用失败（需通过重试机制缓解）。
*   **性能上限受限**：其表现受限于底层工具箱（Toolbox）的能力。如果底层工具（如 SceneWeaver 的工具）无法处理特定物体（如关节衔接物体），该框架也无法生成。
*   **数据依赖**：训练过程依赖于 SceneWeaver 预先生成的轨迹，这可能导致模型继承了原始框架的一些固有偏差。

（完）
