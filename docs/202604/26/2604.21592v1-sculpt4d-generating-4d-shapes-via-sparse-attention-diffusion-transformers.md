---
title: "Sculpt4D: Generating 4D Shapes via Sparse-Attention Diffusion Transformers"
title_zh: Sculpt4D：基于稀疏注意力扩散 Transformer 的 4D 形状生成
authors: "Minghao Yin, Wenbo Hu, Jiale Xu, Ying Shan, Kai Han"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21592v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 将时间建模集成到3D扩散Transformer中的原生4D生成框架
tldr: "针对4D形状生成中存在的时序伪影、计算开销大及数据稀缺等挑战，本文提出Sculpt4D框架。该框架基于预训练的Hunyuan3D 2.1模型，引入块稀疏注意力机制，通过锚定首帧保持物体一致性，并利用时间衰减掩码捕捉动态变化。Sculpt4D在降低56%计算量的同时，实现了高保真且时序连贯的4D合成，达到了当前最先进水平。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21592v1/fig-001.webp\", \"caption\": \"Figure 3. Qualitative comparison of 4D mesh generation. We compare Sculpt4D against V2M4 [5] and L4GM [29]. Given an input image (left), we show two generated views per method. Top and bottom rows correspond to time frames Time 1 and Time 2, respectively.\", \"page\": 6, \"index\": 1, \"width\": 923, \"height\": 831}, {\"url\": \"assets/figures/arxiv/2604.21592v1/fig-002.webp\", \"caption\": \"Figure 2. An overview of our 4D generation framework. Conditioned on an image sequence, we use Consistent Surface Sampling (Sec. 3.1) to acquire both sharp edge points and random surface points, which a vector set VAE [13, 58] encodes into shape latents. These latents are processed by 4D DiT blocks, which use cross-attention for image conditioning and our novel Block Sparse Attention (Sec. 3.3). This sparse attention, guided by a composite mask (Temporal Sparse and First-Frame Anchor), efficiently captures motion while ensuring identity consistency. Finally, a decoder produces the final mesh sequence from the denoised latents.\", \"page\": 3, \"index\": 2, \"width\": 1031, \"height\": 412}, {\"url\": \"assets/figures/arxiv/2604.21592v1/fig-003.webp\", \"caption\": \"Figure 5. Mesh sequences generated from in the wild data.\", \"page\": 8, \"index\": 3, \"width\": 496, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2604.21592v1/fig-004.webp\", \"caption\": \"Figure 6. Qualitative results of textured mesh sequences.\", \"page\": 8, \"index\": 4, \"width\": 446, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2604.21592v1/fig-005.webp\", \"caption\": \"Figure 4. Additional qualitative results of our method. Each row displays one of six diverse 4D results across six time frames. For each frame, the main image shows View 1, with View 2 (top-left) and the input image (bottom-left) as insets.\", \"page\": 7, \"index\": 5, \"width\": 958, \"height\": 637}]"
motivation: 现有的4D生成模型面临时序不一致、计算成本极高以及高质量4D训练数据匮乏的问题。
method: 在预训练3D扩散Transformer基础上，采用块稀疏注意力机制，结合首帧锚定和时间衰减掩码来建模时空依赖。
result: "该方法显著减少了56%的网络总计算量，并有效消除了时序伪影，实现了高保真的动态形状生成。"
conclusion: Sculpt4D为高效、可扩展的4D形状生成提供了新路径，并在时序连贯性方面树立了新的行业标杆。
---

## 摘要
近期 3D 生成建模的突破在静态形状合成方面取得了显著进展，但高保真动态 4D 生成仍然难以实现，受限于时间伪影和极高的计算需求。我们提出了 Sculpt4D，这是一个原生的 4D 生成框架，它将高效的时间建模无缝集成到预训练的 3D 扩散 Transformer（Hunyuan3D 2.1）中，从而缓解了 4D 训练数据的稀缺问题。其核心是一种块稀疏注意力（Block Sparse Attention）机制，通过锚定初始帧来保持物体的一致性，同时通过时间衰减稀疏掩码捕捉丰富的运动动力学。该设计以高保真度忠实地建模了复杂的时空依赖关系，同时避开了全注意力机制的二次方开销，并将网络总计算量减少了 56%。因此，Sculpt4D 在时间连贯的 4D 合成方面达到了新的最先进水平，并为高效且可扩展的 4D 生成开辟了道路。

## Abstract
Recent breakthroughs in 3D generative modeling have yielded remarkable progress in static shape synthesis, yet high-fidelity dynamic 4D generation remains elusive, hindered by temporal artifacts and prohibitive computational demand. We present Sculpt4D, a native 4D generative framework that seamlessly integrates efficient temporal modeling into a pretrained 3D Diffusion Transformer (Hunyuan3D 2.1), thereby mitigating the scarcity of 4D training data. At its core lies a Block Sparse Attention mechanism that preserves object identity by anchoring to the initial frame while capturing rich motion dynamics via a time-decaying sparse mask. This design faithfully models complex spatiotemporal dependencies with high fidelity, while sidestepping the quadratic overhead of full attention and reducing network total computation by 56%. Consequently, Sculpt4D establishes a new state-of-the-art in temporally coherent 4D synthesis and charts a path toward efficient and scalable 4D generation.

---

## 论文详细总结（自动生成）

以下是对论文《Sculpt4D: Generating 4D Shapes via Sparse-Attention Diffusion Transformers》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文旨在解决**高保真动态 4D 形状生成**的核心难题。当前的 4D 生成面临三大挑战：
*   **数据稀缺**：高质量的 4D 扫描数据远少于静态 3D 数据。
*   **计算开销巨大**：传统的全时空注意力机制随序列长度呈二次方增长，难以扩展到长视频或高分辨率模型。
*   **时序不一致性**：现有方法（如逐帧生成或得分蒸馏采样 SDS）常导致几何抖动、身份漂移（Identity Drift）或运动不连贯。
Sculpt4D 通过在预训练的 3D 扩散 Transformer（DiT）中嵌入高效的时空建模，试图建立一个原生、高效且可扩展的 4D 生成框架。

### 2. 方法论：核心思想与关键技术
Sculpt4D 的核心是将静态 3D 生成能力扩展为动态序列生成，主要包含以下技术细节：
*   **时空一致性潜空间编码**：
    *   为了防止 VAE 编码阶段引入抖动，提出了**一致性表面采样**。通过重心坐标传播（Barycentric Propagation）将首帧采样点投影到后续帧，确保点云在时间轴上具有严格的对应关系。
    *   在 VAE 推理时采用**共享噪声（Shared Noise）**，消除随机重参数化带来的不一致性。
*   **4D-DiT 架构**：
    *   在预训练的 Hunyuan3D 2.1 块中插入时间自注意力层，并使用零初始化（Zero-initialization）以保护预训练的 3D 空间先验。
    *   引入 **1D 旋转位置编码（RoPE）** 来捕捉时间顺序。
*   **块稀疏注意力机制（Block Sparse Attention）**：
    *   **首帧锚定（First-Frame Anchor）**：所有时间步的 Token 都会关注第一帧的 Token，作为全局参考，防止物体特征随时间漂移。
    *   **时间衰减稀疏掩码**：基于“距离越远，相关性越低”的原则，设计了随时间距离增加而增大步长（Stride）的稀疏掩码。这种设计保留了对角线附近的密集注意力（捕捉精细运动），同时对远距离帧进行稀疏采样（捕捉语义一致性），将计算量降低了 56%。

### 3. 实验设计
*   **数据集**：从 Objaverse 中筛选出 13k 个 4D 动画对象进行训练；测试集包含 50个 4D 模型。此外，还使用了 DAVIS 数据集进行野外（In-the-wild）视频测试。
*   **Benchmark 指标**：倒角距离（CD）、交并比（IoU）、F-Score，以及针对视频质量的 LPIPS、CLIP 分数和 FVD（弗雷歇视频距离）。
*   **对比方法**：包括 L4GM（基于高斯泼溅）、V2M4（基于变形）、GVFD 以及基准模型 Hunyuan3D 的逐帧生成版本。

### 4. 资源与算力
*   **硬件**：使用了 **8 台拥有 96GB 显存的 GPU**（具体型号推测为 A100 或 H100 级别）。
*   **训练时长**：在 32 的 Batch Size 下训练了约 **2.4 万次迭代**，耗时约 **3 天**。
*   **推理效率**：生成一个完整序列仅需 **7 分钟**，远快于对比方法（通常需 24-45 分钟）。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了定量对比、定性视觉展示、野外数据泛化测试以及纹理映射展示。
*   **消融实验**：设计了 7 组变体实验，分别验证了一致性采样、共享噪声、首帧锚定、稀疏掩码策略（固定步长 vs 衰减步长）以及全注意力对比。
*   **充分性评价**：实验设计较为全面，不仅验证了生成质量，还重点分析了计算复杂度的缩放（Scaling）特性，证明了稀疏注意力在长序列（如 32、64 帧）下的巨大优势，实验结果客观且具有说服力。

### 6. 主要结论与发现
*   **性能领先**：Sculpt4D 在几何精度（CD, IoU）和时序连贯性（FVD）上均显著优于现有 SOTA 方法。
*   **计算高效**：通过块稀疏注意力，模型在不损失质量的前提下，将注意力层的计算开销降低至全注意力的 21.5%（32 帧情况下）。
*   **泛化能力**：尽管只在 16 帧序列上训练，但模型能够零样本（Zero-shot）泛化到 64 帧的长序列生成，且指标保持稳定。

### 7. 优点（亮点）
*   **原生 4D 建模**：不同于后处理优化方法，Sculpt4D 是端到端的生成，避免了误差累积。
*   **巧妙的稀疏设计**：结合了 LLM 中的“注意力锚点”和视频生成的“能量衰减”思想，解决了 4D 生成的算力瓶颈。
*   **极强的实用性**：推理速度快，且能通过拓扑一致性轻松集成现有的纹理生成管线。

### 8. 不足与局限
*   **依赖输入质量**：作为视频到 4D 的模型，其上限受限于输入视频的清晰度和视角覆盖范围。
*   **拓扑限制**：虽然能处理复杂的运动，但对于剧烈的拓扑改变（如物体撕裂或融合），其基于 SDF 的表示可能仍存在平滑化风险。
*   **算力门槛**：尽管降低了开销，但训练此类大规模 DiT 模型仍需要高性能计算集群，普通开发者难以复现。

（完）
