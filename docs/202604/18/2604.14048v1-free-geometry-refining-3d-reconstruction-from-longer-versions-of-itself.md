---
title: "Free Geometry: Refining 3D Reconstruction from Longer Versions of Itself"
title_zh: Free Geometry：通过自身的更长版本优化三维重建
authors: "Yuhang Dai, Xingyi Yang"
date: 2026-04-15
pdf: "https://arxiv.org/pdf/2604.14048v1"
tags: ["query:ff3dr"]
score: 10.0
evidence: 在测试时优化前馈3D重建模型
tldr: 针对前馈3D重建模型在处理遮挡和反光等复杂场景时缺乏适应性的问题，本文提出Free Geometry框架。该框架利用“更多视图带来更一致重建”的特性，在测试阶段通过掩码部分帧构建自监督任务。通过轻量化LoRA更新，模型能在两分钟内实现快速重校准，显著提升了现有基础模型在相机位姿和点图预测上的精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.14048v1/fig-001.webp\", \"caption\": \"Fig. 6: Qualitative Results on 3D Reconstruction. Free Geometry consistently improves geometry quality and reduce errors compared to baseline models. Red pixels mark regions where the model’s prediction deviates significantly from the ground truth, and gray pixels represent correctly reconstructed surfaces within threshold.\", \"page\": 11, \"index\": 1, \"width\": 723, \"height\": 671}, {\"url\": \"assets/figures/arxiv/2604.14048v1/fig-002.webp\", \"caption\": \"Fig. 4: Self-Supervised Geometric Losses of Free Geometry: Left illustrates the intra-frame consistency loss for aligning teacher and student features at corresponding image token locations. Right illustrates the cross-frame relational loss for preserving geometric relations between unmasked tokens and masked-frame anchors. The two losses jointly recalibrate the student representation to stronger full-observation features.\", \"page\": 7, \"index\": 2, \"width\": 617, \"height\": 332}, {\"url\": \"assets/figures/arxiv/2604.14048v1/fig-003.webp\", \"caption\": \"Fig. 5: Qualitative Results On Multi-view Depth. We extract the key frames from multi-view reconstruction depth outputs. In the error maps, red pixels mark regions where the model’s depth prediction deviates significantly from the ground truth, and gray pixels represent correctly reconstructed surfaces within threshold.\", \"page\": 10, \"index\": 3, \"width\": 723, \"height\": 675}, {\"url\": \"assets/figures/arxiv/2604.14048v1/fig-004.webp\", \"caption\": \"Fig. 2: Long Sequence Provides Better Reconstruction Geometry. The left example shows a HiRoom bedroom scene reconstructed by using 8 input views in VGGT while forwarding only the corresponding 4-view features to the decoder. The right example shows the same scene reconstructed using only 4 input views.\", \"page\": 3, \"index\": 4, \"width\": 723, \"height\": 1010}, {\"url\": \"assets/figures/arxiv/2604.14048v1/fig-005.webp\", \"caption\": \"Table 2: Cross-frame Relational Loss Selection Strategy Comparison: We report the results on ETH3D with default training settings, where only the cross-frame patch selection strategy differs. Top selection uses top K most similar patches from masked frames. Mixed selection uses top-K/2 most similar and top-K/2 least similar patches from masked frames. he best results for each metrics are bold.\", \"page\": 23, \"index\": 5, \"width\": 714, \"height\": 273}, {\"url\": \"assets/figures/arxiv/2604.14048v1/fig-006.webp\", \"caption\": \"Table 4: Ablations for Loss Components. We run the identical test-time adaptation settings over same scenes on ETH3D dataset and selected images with loss difference only. Benchmark results illustrate both loss components are critical for optimizing feature representations.\", \"page\": 13, \"index\": 6, \"width\": 713, \"height\": 681}, {\"url\": \"assets/figures/arxiv/2604.14048v1/fig-007.webp\", \"caption\": \"Fig. 1: Qualitative Results on 3D Reconstruction. In the error maps, red pixels denote regions whose reconstructed geometry deviates significantly from the ground truth, while gray pixels indicate regions that fall within the evaluation threshold.\", \"page\": 27, \"index\": 7, \"width\": 723, \"height\": 1023}, {\"url\": \"assets/figures/arxiv/2604.14048v1/fig-008.webp\", \"caption\": \"Fig. 3: Architecture of Free Geometry. The test sequence is processed in two configurations. Top: the full observation (all views, e.g. 8 views) passes through the Image Patch Embedding (e.g. DINOv2 [9]), the Multi-view Transformer, a randomized camera token, and encodes the views into feature representations. All encoders are frozen (gray). Bottom: the partial observation (half of views masked, e.g. 4 views) passes through the same frozen backbone (gray) with LoRA applied to multi-view transformer and the camera token is trainable (orange). All feature tokens of decoder input are extracted from both branches.\", \"page\": 5, \"index\": 8, \"width\": 723, \"height\": 442}]"
motivation: 现有的前馈3D重建模型在推理时过于僵化，无法根据特定测试场景进行自我调整以修正遮挡或歧义导致的错误。
method: 通过掩码部分视频帧，强制模型在完整观测和部分观测之间保持特征一致性及成对关系，并利用LoRA进行快速测试时微调。
result: "在四个基准数据集上，该方法使相机位姿准确率平均提升3.73%，点图预测精度提升2.88%。"
conclusion: Free Geometry证明了通过测试时自监督进化，可以有效提升前馈3D重建模型的鲁棒性和准确性。
---

## 摘要
前馈三维重建模型虽然高效但较为僵化：一旦训练完成，它们以零样本方式进行推理，无法适应测试场景。因此，视觉上合理的重建结果往往包含错误，特别是在遮挡、镜面反射和歧义线索的情况下。为了解决这一问题，我们引入了 Free Geometry，这是一个使前馈三维重建模型能够在测试时无需任何三维真值即可自我进化的框架。我们的核心见解是，当模型接收到更多视角时，它会产生更可靠且视角一致的重建结果。利用这一特性，给定一个测试序列，我们掩蔽一部分帧以构建一个自监督任务。Free Geometry 强制要求完整观测和部分观测的表示之间保持跨视角特征一致性，同时维持由留出帧所隐含的两两关系。这种自监督机制允许通过轻量级的 LoRA 更新进行快速重新校准，在单张 GPU 上处理每个数据集耗时不到 2 分钟。我们的方法在 4 个基准数据集上持续改进了包括 Depth Anything 3 和 VGGT 在内的最先进基础模型，使相机位姿准确度平均提升了 3.73%，点图预测准确度提升了 2.88%。代码可在 https://github.com/hiteacherIamhumble/Free-Geometry 获取。

## Abstract
Feed-forward 3D reconstruction models are efficient but rigid: once trained, they perform inference in a zero-shot manner and cannot adapt to the test scene. As a result, visually plausible reconstructions often contain errors, particularly under occlusions, specularities, and ambiguous cues. To address this, we introduce Free Geometry, a framework that enables feed-forward 3D reconstruction models to self-evolve at test time without any 3D ground truth. Our key insight is that, when the model receives more views, it produces more reliable and view-consistent reconstructions. Leveraging this property, given a testing sequence, we mask a subset of frames to construct a self-supervised task. Free Geometry enforces cross-view feature consistency between representations from full and partial observations, while maintaining the pairwise relations implied by the held-out frames. This self-supervision allows for fast recalibration via lightweight LoRA updates, taking less than 2 minutes per dataset on a single GPU. Our approach consistently improves state-of-the-art foundation models, including Depth Anything 3 and VGGT, across 4 benchmark datasets, yielding an average improvement of 3.73% in camera pose accuracy and 2.88% in point map prediction. Code is available at https://github.com/hiteacherIamhumble/Free-Geometry .

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **Free Geometry** 的测试时自适应（Test-Time Adaptation, TTA）框架，旨在提升前馈三维重建模型在未知场景下的表现。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：当前最先进的前馈三维重建模型（如 Depth Anything 3, VGGT）虽然推理速度快且具备零样本能力，但其遵循“训练后冻结”的范式。在面对具有遮挡、镜面反射或视觉歧义的复杂测试场景时，模型无法根据当前场景进行调整，导致重建结果虽然视觉上合理但几何精度存在误差。
*   **研究动机**：作者观察到一个直观现象——**“视角越多，重建越准”**。更多的视角能提供更强的几何约束。
*   **整体含义**：利用模型自身在“长序列（多视角）”下的优异表现作为伪标签，去指导模型在“短序列（部分视角）”下的学习，从而在无需三维真值（Ground Truth）的情况下实现模型的自我进化。

### 2. 方法论
*   **核心思想**：构建一个教师-学生蒸馏架构。教师分支输入完整序列（全观测），学生分支输入掩码后的子序列（部分观测）。
*   **关键技术细节**：
    *   **特征级蒸馏**：由于这些模型的解码器通常是逐帧独立运行的，瓶颈在于编码器。因此，Free Geometry 在编码器特征层进行自监督，而非输出层。
    *   **轻量化更新（LoRA）**：为了保证测试时的效率，冻结原始骨干网络，仅在多视角 Transformer 块中插入 LoRA 模块，并微调相机 Token。
*   **损失函数**：
    *   **帧内一致性损失 ($L_{intra}$)**：强制学生分支在未掩码帧上的特征在数值和方向上与教师分支对齐（结合 Huber 损失和余弦相似度）。
    *   **跨帧关系损失 ($L_{cross}$)**：为了利用被掩码帧的信息，在特征空间构建“虚拟三角形”，通过 KL 散度和角度约束来保持不同帧 Token 之间的几何拓扑关系。

### 3. 实验设计
*   **数据集/场景**：
    *   **ETH3D**：包含室内外场景，具有高精度激光扫描真值。
    *   **ScanNet++**：大规模室内场景，环境复杂。
    *   **7-Scenes**：用于相机重定位的 RGB-D 数据集。
    *   **HiRoom**：高分辨率房间尺度重建，包含挑战性的光照和反射表面。
*   **Benchmark 与对比方法**：
    *   以 **Depth Anything 3 (DA3)** 和 **VGGT** 作为基础模型（Baseline）。
    *   对比“冻结模型”与“经过 Free Geometry 优化后的模型”在不同输入视角数量（4, 8, 16, 32, 64, 100 帧）下的表现。
*   **评价指标**：相机位姿准确率（AUC@3, AUC@30）、重建质量（F1 分数、Chamfer Distance）。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX Pro 6000 GPU**。
*   **耗时**：每个数据集的测试时优化耗时**不到 2 分钟**。
*   **训练配置**：优化运行 5 个 Epoch，Batch Size 为 4，使用 FP16 混合精度。LoRA 的秩（Rank）设为 32。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个主流基准数据集上进行了广泛测试，涵盖了从 4 帧到 100 帧的不同稀疏/稠密场景。
*   **消融实验**：
    *   验证了两种损失函数（一致性损失和关系损失）的必要性。
    *   测试了不同的 LoRA 秩（8, 16, 32, 64）对结果的影响。
    *   对比了不同的掩码帧锚点选择策略（随机、最相似、混合选择）。
*   **客观性**：实验采用了 3 个随机种子取平均值，确保了结果的稳定性和公平性。

### 6. 主要结论与发现
*   **性能提升**：该方法使相机位姿准确率平均提升了 **3.73%**，点图预测精度提升了 **2.88%**。
*   **跨视角泛化**：虽然优化时使用的是“8 帧指导 4 帧”的设置，但优化后的模型在 16、32 甚至 100 帧的推理中依然表现出一致的性能提升。
*   **特征校准**：定量分析证明，优化后的学生特征在特征空间中确实更接近全观测下的教师特征。

### 7. 优点
*   **无需标签**：完全自监督，不需要任何 3D 真值，非常适合实际部署。
*   **即插即用**：可以适配任何基于 Transformer 的前馈多视角重建模型。
*   **高效性**：通过 LoRA 和特征级蒸馏，极大地降低了测试时训练的显存占用和时间成本。
*   **鲁棒性**：在处理反射、低纹理等前馈模型易出错的区域时，改进效果显著。

### 8. 不足与局限
*   **超参数敏感性**：学习率和每个场景的采样数量需要根据数据集进行调整（如 ScanNet++ 和 HiRoom 使用了不同的配置）。
*   **架构依赖**：该方法目前主要针对具有全局注意力机制的多视角 Transformer 架构，对于其他类型的架构（如纯 CNN）可能需要重新设计。
*   **提升上限**：在基础模型已经表现非常出色的数据集（如 ScanNet++）上，性能提升幅度相对较小。

（完）
