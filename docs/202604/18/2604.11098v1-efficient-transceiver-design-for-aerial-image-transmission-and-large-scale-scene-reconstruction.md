---
title: Efficient Transceiver Design for Aerial Image Transmission and Large-scale Scene Reconstruction
title_zh: 面向航空图像传输与大规模场景重建的高效收发机设计
authors: "Zeyi Ren, Jialin Dong, Wei Zuo, Yikun Wang, Bingyang Cheng, Sheng Zhou, Zhisheng Niu"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11098v1"
tags: ["query:ff3dr"]
score: 9.0
evidence: 使用 3D 高斯泼溅进行大规模三维场景重建
tldr: 本研究针对低空智能网络中的大规模三维场景重建，提出了一种高效的端到端收发机设计。该方案将3D高斯泼溅（3DGS）技术直接集成到深度学习训练过程中，通过联合优化通信模块与渲染损失，显著提升了场景恢复质量。此外，该任务驱动框架采用稀疏导频方案，在降低传输开销的同时，确保了低空信道下图像恢复的鲁棒性，实现了传输效率与重建精度的平衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11098v1/fig-001.webp\", \"caption\": \"Fig. 2. Architecture of the proposed task-oriented end-to-end transceiver. The transmitter utilizes a Transformer-based Moduformer for resilient constellation mapping, while the receiver employs a Deep-Rx network. Both modules are jointly optimized driven by the 3DGS rendering loss.\", \"page\": 3, \"index\": 1, \"width\": 1070, \"height\": 335}, {\"url\": \"assets/figures/arxiv/2604.11098v1/fig-002.webp\", \"caption\": \"Fig. 1. Illustration of the low-altitude intelligent network imagery scenario. Traditional full pilot schemes suffer from prohibitive signaling overhead, whereas the proposed AI-driven sparse pilot transmission improves spectral efficiency for downstream 3D scene reconstruction.\", \"page\": 1, \"index\": 2, \"width\": 529, \"height\": 235}]"
motivation: 现有的无线图像传输方案难以在巨大的导频开销与维持三维重建保真度所需的高传输精度之间取得平衡。
method: 提出一种将3D高斯泼溅渲染损失集成到训练中的端到端收发机，并结合任务驱动的稀疏导频方案进行联合优化。
result: 在真实航拍数据集上的实验表明，该设计在传输性能和三维场景重建精度上均显著优于现有基准方案。
conclusion: 该任务驱动的端到端框架有效解决了低空网络中高效传输与高质量三维重建的协同优化问题。
---

## 摘要
低空智能网络（LAIN）中的大规模三维（3D）场景重建对高效无线图像传输提出了极高要求。然而，现有方案难以在沉重的导频开销与维持重建保真度所需的传输准确性之间取得平衡。为了在效率与可靠性之间寻求平衡，本文提出了一种新型的基于深度学习的端到端（E2E）收发机设计，将 3D 高斯泼溅（3DGS）直接集成到训练过程中。通过结合 3DGS 渲染损失对通信模块进行联合优化，该方法显著提升了场景恢复质量。此外，这一任务驱动的框架允许使用稀疏导频方案，在显著降低传输开销的同时，在低空信道条件下保持稳健的图像恢复。在真实航空图像数据集上的广泛实验表明，所提出的端到端设计显著优于现有基准方案，提供了卓越的传输性能和精确的三维场景重建。

## Abstract
Large-scale three-dimensional (3D) scene reconstruction in low-altitude intelligent networks (LAIN) demands highly efficient wireless image transmission. However, existing schemes struggle to balance severe pilot overhead with the transmission accuracy required to maintain reconstruction fidelity. To strike a balance between efficiency and reliability, this paper proposes a novel deep learning-based end-to-end (E2E) transceiver design that integrates 3D Gaussian Splatting (3DGS) directly into the training process. By jointly optimizing the communication modules via the combined 3DGS rendering loss, our approach explicitly improves scene recovery quality. Furthermore, this task-driven framework enables the use of a sparse pilot scheme, significantly reducing transmission overhead while maintaining robust image recovery under low-altitude channel conditions. Extensive experiments on real-world aerial image datasets demonstrate that the proposed E2E design significantly outperforms existing baselines, delivering superior transmission performance and accurate 3D scene reconstructions.

---

## 论文详细总结（自动生成）

这篇论文提出了一种面向低空智能网络（LAIN）的端到端无线图像传输方案，旨在通过跨层优化提升大规模三维（3D）场景重建的效率与精度。

### 1. 核心问题与研究动机
*   **核心问题**：在无人机（UAV）执行大规模3D场景重建任务时，如何在有限的无线资源下，平衡**传输效率**（减少导频开销）与**重建保真度**（保证图像恢复精度）。
*   **研究背景**：
    *   传统无线通信依赖大量导频（Pilot）进行信道估计，在动态的低空信道中开销巨大，挤占了图像数据的传输带宽。
    *   现有的端到端（E2E）深度学习收发机在发送端（Tx）和接收端（Rx）之间存在严重的参数量不对称（Tx参数极少，Rx参数极多），导致训练不稳定，难以应对复杂的空地信道。
    *   传统传输优化目标（如MSE）与下游的3D重建任务（如3DGS渲染质量）之间存在脱节。

### 2. 方法论
论文提出了一种任务驱动的端到端收发机架构，核心组件包括：
*   **Moduformer (发送端)**：引入基于Transformer的调制架构。它不直接学习星座点坐标，而是通过自注意力机制捕捉符号间的长程依赖，生成针对信道衰落的“残差扰动”并叠加在标准QAM符号上。这增加了发送端的学习能力，解决了参数不对称导致的梯度消失问题。
*   **Deep-Rx (接收端)**：采用基于ResNet的神经接收机，利用其强大的非线性映射能力，将信道估计、均衡和解调整合为一个统一的网络处理过程。
*   **稀疏导频方案**：仅在数据流中插入极少量的导频符号作为信道锚点，相比传统方案减少了90%的导频开销，显著提升了频谱效率。
*   **3DGS 联合优化**：将 **3D高斯泼溅（3DGS）** 渲染过程直接嵌入训练循环。
*   **损失函数设计**：采用多任务损失函数 $L_{total} = L_{BCE} + \psi [ (1-\alpha)L_1 + \alpha L_{SSIM} ]$。其中 $L_{BCE}$ 负责比特恢复，而 $L_1$ 和 $L_{SSIM}$ 负责优化下游渲染图像的像素精度和结构保真度。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：包含20,000张高分辨率航空图像，来源于 GauU-Scene V2 和 Mega-NeRF (Mill 19) 数据集。
    *   **测试集**：使用 GauU-Scene 中的 HAV（何奥村）子集，该场景具有复杂的城市拓扑结构。
*   **Benchmark（对比方法）**：
    1.  **16QAM & Perfect CSI**：理想信道估计下的性能上限。
    2.  **16QAM & EZF+WMMSE**：传统的模块化通信方案（全导频）。
    3.  **Cons-Learning & Deep-Rx**：现有的星座学习端到端方案。
    4.  **16QAM & Deep-Rx**：基础的神经接收机方案。
*   **评估指标**：误块率（BLER）、有效吞吐量、峰值信噪比（PSNR）和结构相似性（SSIM）。

### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的硬件配置（如GPU型号、数量）及具体的训练时长。
*   **规模推断**：考虑到训练集包含20,000张图像，且训练循环中集成了计算密集型的3DGS渲染过程，可以推断该方案的离线训练对算力有较高要求。

### 5. 实验数量与充分性
*   **实验规模**：论文在-4dB到4dB的SNR范围内进行了多组对比实验，涵盖了通信链路性能和下游视觉重建质量两个维度。
*   **充分性**：实验对比了传统方法与前沿的深度学习方法，并使用了多个真实世界的航拍数据集。通过消融式的对比（如稀疏导频 vs 全导频，Moduformer vs 星座学习），验证了各组件的有效性。实验设计较为客观，能够支撑其结论。

### 6. 主要结论与发现
*   **传输性能**：在相同信噪比下，Moduformer 显著降低了 BLER，且在 0 dB SNR 下将有效吞吐量提升了约 **78%**。
*   **重建质量**：相比现有基准，该方案将 3D 重建的 PSNR 提升了 **6.2%**，SSIM 提升了 **5.3%**。
*   **鲁棒性**：即使在导频开销减少 90% 的情况下，该系统仍能保持稳健的图像恢复能力，性能接近完美信道估计（Perfect CSI）的水平。

### 7. 优点
*   **任务导向**：打破了通信与视觉任务的壁垒，通过渲染损失引导物理层编码，使无线资源优先保障对3D几何重建关键的特征。
*   **架构创新**：Moduformer 巧妙地利用 Transformer 解决了端到端通信中发送端“轻量化”导致的训练难点。
*   **高效率**：稀疏导频设计极大地释放了被信令占用的频谱资源。

### 8. 不足与局限
*   **训练成本**：将 3DGS 渲染引入训练闭环虽然提升了性能，但极大地增加了训练时的计算开销和内存占用。
*   **实时性疑虑**：Transformer 结构虽然增强了性能，但在算力受限的无人机终端上进行实时推理可能存在功耗和延迟挑战。
*   **信道泛化性**：实验主要基于几何随机信道模型（GBSM），在面对极其复杂的非视距（NLoS）或强干扰环境时的表现仍需进一步验证。

（完）
