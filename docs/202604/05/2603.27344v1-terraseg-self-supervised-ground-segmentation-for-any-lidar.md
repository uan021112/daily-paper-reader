---
title: "TerraSeg: Self-Supervised Ground Segmentation for Any LiDAR"
title_zh: TerraSeg：适用于任意激光雷达的自监督地面分割
authors: "Ted Lentsch, Santiago Montiel-Marín, Holger Caesar, Dariu M. Gavrila"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27344v1"
tags: ["query:partseg"]
score: 8.0
evidence: 利用大规模数据集对 LiDAR 点云进行自监督地面分割
tldr: TerraSeg是首个自监督、跨域通用的LiDAR地面分割模型。针对现有方法依赖特定传感器配置或昂贵人工标注的问题，研究者构建了包含2200万扫描的大规模数据集OmniLiDAR，并提出PseudoLabeler模块通过自监督优化生成伪标签。该模型在多个主流数据集上达到了SOTA水平，且具备实时处理能力，显著提升了LiDAR感知的泛化性。
selection_source: fresh_fetch
motivation: 现有的地面分割方法受限于特定传感器配置或高昂的人工标注成本，难以在不同设备和场景间泛化。
method: 提出了首个自监督通用模型TerraSeg，利用包含15种传感器的OmniLiDAR数据集进行训练，并通过PseudoLabeler模块实现逐帧自监督伪标签生成。
result: 在nuScenes、SemanticKITTI和Waymo等基准测试中，TerraSeg在无人工标注的情况下达到了最先进的性能，并支持实时运行。
conclusion: TerraSeg证明了通过大规模多样化数据和自监督学习可以构建出高性能、高泛化性的通用LiDAR地面分割模型。
---

## 摘要
激光雷达（LiDAR）感知是机器人技术的基础，使机器能够以 3D 方式理解其环境。基于激光雷达的场景理解和导航的一项关键任务是地面分割。然而，现有方法要么是针对特定传感器配置手工设计的，要么依赖于昂贵的逐点人工标注，这严重限制了它们的泛化能力和可扩展性。为了克服这一挑战，我们提出了 TerraSeg，这是首个用于激光雷达地面分割的自监督、领域无关模型。我们在 OmniLiDAR 上训练 TerraSeg，这是一个统一的大规模数据集，汇集并标准化了来自 12 个主要公共基准的数据。OmniLiDAR 涵盖了 15 种不同传感器型号的近 2200 万帧原始扫描，为学习高度泛化的地面模型提供了前所未有的多样性。为了在无需人工标注的情况下监督训练，我们提出了 PseudoLabeler，这是一个通过自监督的逐帧运行时优化生成高质量地面和非地面标签的新颖模块。广泛的评估表明，尽管没有使用人工标签，TerraSeg 在 nuScenes、SemanticKITTI 和 Waymo Perception 上仍取得了最先进的性能，同时实现了实时运行。我们的代码和模型权重已公开。

## Abstract
LiDAR perception is fundamental to robotics, enabling machines to understand their environment in 3D. A crucial task for LiDAR-based scene understanding and navigation is ground segmentation. However, existing methods are either handcrafted for specific sensor configurations or rely on costly per-point manual labels, severely limiting their generalization and scalability. To overcome this, we introduce TerraSeg, the first self-supervised, domain-agnostic model for LiDAR ground segmentation. We train TerraSeg on OmniLiDAR, a unified large-scale dataset that aggregates and standardizes data from 12 major public benchmarks. Spanning almost 22 million raw scans across 15 distinct sensor models, OmniLiDAR provides unprecedented diversity for learning a highly generalizable ground model. To supervise training without human annotations, we propose PseudoLabeler, a novel module that generates high-quality ground and non-ground labels through self-supervised per-scan runtime optimization. Extensive evaluations demonstrate that, despite using no manual labels, TerraSeg achieves state-of-the-art results on nuScenes, SemanticKITTI, and Waymo Perception while delivering real-time performance. Our code and model weights are publicly available.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **TerraSeg** 的自监督、领域无关（Domain-agnostic）的激光雷达（LiDAR）地面分割模型。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的地面分割方法存在两大瓶颈：一是**手工设计方法**（如 PatchWork++）通常针对特定传感器配置，换环境或换设备需重新调参；二是**监督学习方法**依赖昂贵的逐点人工标注，难以扩展到多种传感器和复杂场景。
*   **研究动机**：随着自动驾驶和机器人技术的发展，需要一种能够适配“任何激光雷达”、无需人工标注且具备实时处理能力的通用地面分割模型。

### 2. 方法论
TerraSeg 的核心由三个相互关联的部分组成：
*   **OmniLiDAR 数据集**：研究者整合了 12 个主流公共数据集（如 nuScenes, Waymo, SemanticKITTI 等），涵盖 15 种不同的 LiDAR 硬件型号，包含近 2200 万帧原始扫描。通过标准化坐标系和去除非地面点，构建了一个极具多样性的训练库。
*   **PseudoLabeler（伪标签生成器）**：
    *   **核心思想**：利用“地面通常是场景中最低的连续表面”这一几何先验，通过逐帧运行时优化来拟合高程图。
    *   **技术细节**：使用多层感知机（MLP）参数化高程函数 $g_\theta(x, y)$。采用**非对称损失函数**：对预测表面下方的点进行平方惩罚（处理负噪声），对上方点使用 Huber 损失（忽略障碍物）。
    *   **流程优化**：包含预处理（去除多路径反射噪声）和后处理（基于柱状体 Pillar 的精细化，防止将轮胎等物体底部误判为地面）。
*   **TerraSeg 模型架构**：
    *   基于 **Point Transformer v3 (PTv3)** 骨干网络。
    *   **领域无关设计**：禁用特定数据集的归一化，使用组归一化（Group Norm），且仅输入坐标、高度和水平范围等通用几何特征，不依赖强度等传感器相关信息。
    *   提供两个版本：**TerraSeg-B（基础版）**追求精度，**TerraSeg-S（轻量版）**追求极速。

### 3. 实验设计
*   **评估数据集**：在 nuScenes、SemanticKITTI 和 Waymo Perception 的验证集上进行零样本（Zero-shot）测试。
*   **对比基准（Benchmark）**：
    *   **手工方法**：RANSAC, PatchWork++, TRAVEL 等。
    *   **自监督方法**：Chodosh 等人的运行时优化方法。
    *   **监督方法**：GndNet（在特定数据集上训练）。
    *   **Oracle（上限）**：使用人工标注训练的 TerraSeg-B。
*   **评价指标**：Recall（召回率）、Precision（精确率）、IoU（交并比）以及 mIoU。

### 4. 资源与算力
*   **硬件环境**：论文提到在 **NVIDIA A100 GPU** 上测试吞吐量。
*   **训练细节**：使用 AdamW 优化器，有效 Batch Size 为 256（通过梯度累积实现），每个 Dataloader 步骤处理 4 帧扫描。
*   **运行效率**：TerraSeg-S 在 A100 上可达 17Hz 至 50Hz，TerraSeg-B 可达 10Hz 至 28Hz，均满足实时性要求。

### 5. 实验数量与充分性
*   **实验规模**：使用了 12 个数据集的 2200 万帧数据进行训练，这是目前该领域最大规模的尝试。
*   **消融实验**：详细分析了 PseudoLabeler 的预处理、后处理模块对 mIoU 的提升（提升幅度达 4-8%），并测试了不同 Pillar 尺寸和阈值的敏感度。
*   **地形适应性**：专门针对平坦和非平坦地形进行了性能拆解分析。
*   **客观性**：通过在多个未见过的传感器数据集上进行零样本测试，充分证明了模型的泛化能力，实验设计客观且公平。

### 6. 主要结论与发现
*   **性能领先**：TerraSeg 在无人工标注的情况下，在所有主流基准测试中均达到了 SOTA（最先进）水平，甚至在某些指标上接近或超过了有监督模型。
*   **跨传感器泛化**：通过大规模多样化数据的自监督学习，模型成功学习到了通用的地面几何先验，而非特定传感器的扫描模式。
*   **实时性**：模型推理速度远快于伪标签生成速度（PseudoLabeler 仅 0.3Hz），适合部署于移动机器人。

### 7. 优点
*   **首创性**：提出了首个自监督、领域无关的通用 LiDAR 地面分割模型。
*   **数据规模**：构建了 OmniLiDAR 这一庞大的统一数据集，对社区具有重要价值。
*   **鲁棒性**：PseudoLabeler 的非对称损失和后处理机制有效解决了传统几何方法容易过分割（Over-segmentation）的问题。
*   **实用性**：模型开源，且提供了不同规格的架构以适配不同算力的平台。

### 8. 不足与局限
*   **极端场景挑战**：在定性分析中发现，模型在面对**茂密且低矮的植被**或**非典型的道路碎片**时，仍可能出现误判（将其识别为地面）。
*   **稀疏区域依赖**：在点云极度稀疏的远距离区域，伪标签的质量可能下降，未来可能需要引入时间序列上下文来增强。
*   **训练成本**：虽然推理快，但 PseudoLabeler 的逐帧优化过程非常耗时，大规模生成伪标签需要巨大的离线算力。

（完）
