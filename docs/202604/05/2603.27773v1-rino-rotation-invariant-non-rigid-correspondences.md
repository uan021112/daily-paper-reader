---
title: "RINO: Rotation-Invariant Non-Rigid Correspondences"
title_zh: RINO：旋转不变的非刚性对应关系
authors: "Maolin Gao, Shao Jie Hu-Chen, Congyue Deng, Riccardo Marin, Leonidas Guibas, Daniel Cremers"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27773v1"
tags: ["query:partseg"]
score: 6.0
evidence: 稠密3D形状对应及从原始几何中提取特征
tldr: 针对3D形状密集对应任务中对预对齐和手工特征的依赖问题，本文提出RINO，一个无监督且旋转不变的密集对应框架。核心是RINONet，它结合了向量式SO(3)不变性学习与方向感知复数函数映射，能直接从原始几何中提取鲁棒特征。该方法实现了端到端学习，无需预对齐，在处理非等距变形、部分数据、非流形输入及噪声等复杂场景下表现卓越。
selection_source: fresh_fetch
motivation: 现有的3D形状对应方法过度依赖手工特征或预先对齐，难以应对非等距变形、部分数据和非流形输入等挑战。
method: 提出RINONet，通过集成向量式SO(3)不变性学习与方向感知复数函数映射，直接从原始几何中提取旋转不变的鲁棒特征。
result: 在涉及任意姿态、非等距、部分重叠、非流形及噪声的多种挑战性非刚性匹配任务中，RINO均取得了前所未有的性能表现。
conclusion: RINO通过统一刚性与非刚性匹配，提供了一种无需预处理、鲁棒性极强的端到端3D形状对应解决方案。
---

## 摘要
稠密 3D 形状对应仍然是计算机视觉和图形学中的一个核心挑战，因为许多深度学习方法仍然依赖于中间几何特征或手工设计的描述符，限制了它们在非等距形变、部分数据和非流形输入下的有效性。为了克服这些问题，我们提出了 RINO，这是一个无监督的、旋转不变的稠密对应框架，它有效地统一了刚性和非刚性形状匹配。我们方法的核心是新型的 RINONet，这是一种特征提取器，它将基于向量的 SO(3) 不变性学习与方向感知的复数泛函映射（complex functional maps）相结合，直接从原始几何中提取鲁棒特征。这实现了一种完全端到端的、数据驱动的方法，绕过了对形状预对齐或手工特征的需求。大量实验表明，RINO 在具有挑战性的非刚性匹配任务中表现出了前所未有的性能，包括任意姿态、非等距性、部分性、非流形性和噪声。

## Abstract
Dense 3D shape correspondence remains a central challenge in computer vision and graphics as many deep learning approaches still rely on intermediate geometric features or handcrafted descriptors, limiting their effectiveness under non-isometric deformations, partial data, and non-manifold inputs. To overcome these issues, we introduce RINO, an unsupervised, rotation-invariant dense correspondence framework that effectively unifies rigid and non-rigid shape matching. The core of our method is the novel RINONet, a feature extractor that integrates vector-based SO(3)-invariant learning with orientation-aware complex functional maps to extract robust features directly from raw geometry. This allows for a fully end-to-end, data-driven approach that bypasses the need for shape pre-alignment or handcrafted features. Extensive experiments show unprecedented performance of RINO across challenging non-rigid matching tasks, including arbitrary poses, non-isometry, partiality, non-manifoldness, and noise.