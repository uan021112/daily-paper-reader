---
title: "Hi-LOAM: Hierarchical Implicit Neural Fields for LiDAR Odometry and Mapping"
title_zh: Hi-LOAM：用于激光雷达里程计与建图的分层隐式神经场
authors: "Zhiliu Yang, Jianyuan Zhang, Lianhui Zhao, Jinyu Dai, Zhu Yang"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01720v1"
tags: ["query:ff3dr"]
score: 8.0
evidence: 用于大规模3D重建的分层隐式神经场
tldr: Hi-LOAM 是一种针对自动驾驶和机器人导航提出的多尺度隐式神经定位与建图框架。针对现有 LOAM 方法在复杂大场景中重建精度不足及依赖监督信号的问题，该方法利用基于八叉树结构的哈希表存储多层级潜在特征，并通过浅层 MLP 解码为符号距离值（SDF）。它采用无对应关系的扫描到隐式匹配进行位姿估计，且全过程自监督，无需预训练，在多个数据集上展现了卓越的重建精度和泛化能力。
selection_source: fresh_fetch
motivation: 现有的 LOAM 框架在处理大规模复杂场景时，往往面临重建精度不足或过度依赖监督信号的问题。
method: 提出一种基于八叉树结构的多尺度哈希表来存储特征，并结合浅层 MLP 和无对应关系的扫描到隐式匹配实现定位与建图。
result: 在多个真实世界和合成数据集上的实验表明，Hi-LOAM 在有效性和泛化能力方面均优于现有的最先进方法。
conclusion: Hi-LOAM 通过自监督的层次化隐式神经表示，实现了高精度的 LiDAR 定位与大规模场景重建。
---

## 摘要
激光雷达里程计与建图（LOAM）是自动驾驶和机器人导航等具身智能应用中的关键技术。大多数现有的 LOAM 框架要么依赖于监督信号，要么缺乏重建保真度，在描绘大规模复杂场景的细节方面存在不足。为了克服这些局限性，我们提出了一种使用激光雷达传感器的多尺度隐式神经定位与建图框架，称为 Hi-LOAM。Hi-LOAM 接收激光雷达点云作为输入数据模态，在基于八叉树结构的多级哈希表中学习并存储分层潜在特征，随后在建图过程中，通过浅层多层感知机（MLP）将这些多尺度潜在特征解码为符号距离值。在位姿估计过程中，我们依靠一种无对应关系的“扫描到隐式”匹配范式来估计最优位姿，并将当前扫描注册到子图中。整个训练过程以自监督方式进行，无需模型预训练，并在应用于不同环境时展现出良好的泛化能力。在多个真实世界和合成数据集上的广泛实验表明，与现有的最先进方法相比，我们的 Hi-LOAM 在有效性和泛化能力方面具有优越的性能。

## Abstract
LiDAR Odometry and Mapping (LOAM) is a pivotal technique for embodied-AI applications such as autonomous driving and robot navigation. Most existing LOAM frameworks are either contingent on the supervision signal, or lack of the reconstruction fidelity, which are deficient in depicting details of large-scale complex scenes. To overcome these limitations, we propose a multi-scale implicit neural localization and mapping framework using LiDAR sensor, called Hi-LOAM. Hi-LOAM receives LiDAR point cloud as the input data modality, learns and stores hierarchical latent features in multiple levels of hash tables based on an octree structure, then these multi-scale latent features are decoded into signed distance value through shallow Multilayer Perceptrons (MLPs) in the mapping procedure. For pose estimation procedure, we rely on a correspondence-free, scan-to-implicit matching paradigm to estimate optimal pose and register current scan into the submap. The entire training process is conducted in a self-supervised manner, which waives the model pre-training and manifests its generalizability when applied to diverse environments. Extensive experiments on multiple real-world and synthetic datasets demonstrate the superior performance, in terms of the effectiveness and generalization capabilities, of our Hi-LOAM compared to existing state-of-the-art methods.

---

## 论文详细总结（自动生成）

### Hi-LOAM：用于激光雷达里程计与建图的分层隐式神经场论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：现有的激光雷达里程计与建图（LOAM）框架存在两个主要瓶颈：一是许多高性能方法依赖于有监督学习（需要地面真值位姿进行预训练）；二是现有的隐式表示方法在大规模复杂场景中缺乏足够的重建保真度和细节刻画能力。
*   **研究动机**：为了在无需预训练的情况下实现高精度的定位与高保真度的三维重建，作者提出了一种基于分层隐式神经表示的自监督框架，旨在通过多尺度特征嵌入来捕捉环境的细微几何特征，并解决大规模场景下的扩展性问题。

#### 2. 方法论
*   **核心思想**：利用八叉树（Octree）结构管理多尺度哈希表，存储分层的潜在特征（Latent Features），并通过浅层多层感知机（MLP）将这些特征解码为符号距离函数（SDF）值。
*   **关键技术细节**：
    *   **分层特征嵌入**：采用八叉树结构，将空间划分为不同层级的体素。每个层级的节点关联哈希表中的特征向量。通过三线性插值融合最后三个层级的特征，生成增强的特征表示。
    *   **扫描到隐式地图匹配（Scan-to-Implicit Matching）**：定位不依赖显式的点对点匹配，而是将当前帧点云直接与隐式神经场（nSDF）进行配准。
    *   **优化算法**：使用 Levenberg-Marquardt (LM) 算法最小化 SDF 预测误差，从而估计最优的 6-DoF 位姿。
    *   **自监督训练**：利用激光雷达的测距信息作为天然的监督信号（点到传感器的距离即为 SDF 参考值），并引入重放策略（Replay Strategy）解决增量学习中的“灾难性遗忘”问题。
    *   **网格化重建**：应用 Marching Cubes 算法从隐式 SDF 场中提取等值面，生成三角网格地图。

#### 3. 实验设计
*   **数据集与场景**：
    *   **真实世界**：KITTI（大规模室外）、SemanticPOSS（高动态场景）、Newer College（手持设备、复杂结构）、Hilti-21/23（室内外及建筑工地）、MulRAN（长距离城市漫游）。
    *   **合成数据**：Mai City（用于评估重建精度）。
*   **Benchmark 与对比方法**：
    *   **定位指标**：平均相对平移误差（%）、绝对轨迹误差（ATE RMSE）。
    *   **建图指标**：准确度（Accuracy）、完整度（Completion）、Chamfer-L1 距离、F-score。
    *   **对比对象**：包括传统方法（LeGO-LOAM, KISS-ICP, MULLS）、有监督学习方法（PWCLO, LO-Net）以及最新的隐式神经方法（NeRF-LOAM, PIN-SLAM）。

#### 4. 资源与算力
*   **硬件环境**：单张 **NVIDIA GeForce RTX 4090** GPU。
*   **运行效率**：
    *   **位姿估计**：平均每帧耗时约 0.6 秒（约 1.7 FPS）。
    *   **地图优化**：每帧耗时约 2.8 到 5.5 秒（取决于数据集）。
    *   **参数量**：MLP 包含 2 层隐藏层，每层 32 个神经元；特征向量长度为 12。

#### 5. 实验数量与充分性
*   **实验规模**：论文在超过 7 个主流数据集上进行了测试，涵盖了从室外大规模街道到室内狭窄走廊、从手持设备到车载平台的多种工况。
*   **充分性与公平性**：
    *   进行了详细的**消融实验**，验证了多尺度特征层级（LOF）对定位精度的提升作用。
    *   进行了**敏感性分析**，探讨了叶节点大小、八叉树深度、子图规模对性能的影响。
    *   对比实验均采用了 SOTA 方法的公开结果或在相同硬件下重新运行，确保了客观性。

#### 6. 主要结论与发现
*   **定位精度**：Hi-LOAM 的定位精度优于现有的所有学习型 LiDAR 里程计，并与最先进的基于 ICP 的传统方法持平甚至更优。
*   **建图质量**：在重建保真度上显著超过了 NeRF-LOAM 和 PIN-SLAM，尤其是在处理复杂几何结构和减少伪影方面表现突出。
*   **泛化能力**：自监督机制使其能够直接适应新环境（如建筑工地或地下空间），无需针对特定场景进行预训练。

#### 7. 优点与亮点
*   **多尺度融合**：通过八叉树分层特征，既能捕捉宏观结构也能保留微观细节，提高了定位的鲁棒性。
*   **无对应关系匹配**：避免了传统 ICP 在稀疏点云下寻找对应点的困难，对动态物体有一定的自然过滤效果。
*   **端到端自监督**：摆脱了对地面真值位姿的依赖，极大地扩展了实际应用场景。

#### 8. 不足与局限
*   **实时性挑战**：目前 1.7 FPS 的定位速度尚无法满足高速移动机器人的实时需求（通常需要 10Hz 以上），地图优化过程耗时较长。
*   **回环检测缺失**：论文目前主要关注里程计和增量建图，尚未集成全局回环检测模块，在超长距离轨迹下仍会存在累计漂移（如 MulRAN 实验所示）。
*   **计算开销**：相比于单层特征方法（如 PIN-SLAM），多层特征带来了额外的内存和计算负担。

（完）
