---
title: "M2H-MX: Multi-Task Dense Visual Perception for Real-Time Monocular Spatial Understanding"
title_zh: M2H-MX：用于实时单目空间理解的多任务稠密视觉感知
authors: "U. V. B. L. Udugama, George Vosselman, Francesco Nex"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29236v1"
tags: ["query:ff3dr"]
score: 8.0
evidence: 从像素到深度和语义预测的直接映射以实现空间理解
tldr: 本文提出M2H-MX，一种用于实时单目空间感知的多任务稠密感知模型。针对单目相机在机器人感知中难以实现稳定空间理解的挑战，该模型通过轻量级解码器引入寄存器门控全局上下文和受控的任务间交互，使深度与语义预测相互增强。M2H-MX可直接集成至单目SLAM流程，在NYUDv2和ScanNet数据集上均取得显著提升，不仅刷新了稠密预测的SOTA，还大幅降低了SLAM轨迹误差，为机器人实时单目感知提供了可靠方案。
selection_source: fresh_fetch
motivation: 旨在解决单目相机在机器人实时空间感知中，难以在严格延迟约束下同时获得高精度深度和语义估计的问题。
method: 设计了一种包含寄存器门控全局上下文和受控跨任务交互的轻量级多任务解码器，并构建了紧凑的感知到建图接口。
result: "在NYUDv2上显著提升了语义和深度预测精度，并在ScanNet实测中将单目SLAM的平均轨迹误差降低了60.7%。"
conclusion: 证明了现代多任务稠密预测技术能够有效提升机器人实时单目空间感知的稳定性和建图质量。
---

## 摘要
单目相机因其低成本和易于部署而在机器人感知领域极具吸引力，但从单一图像流中实现可靠的实时空间理解仍然具有挑战性。尽管近期的多任务稠密预测模型提高了逐像素深度和语义估计的性能，但将这些进展转化为稳定的单目建图系统仍然并非易事。本文提出了 M2H-MX，一种用于单目空间理解的实时多任务感知模型。该模型在保留多尺度特征表示的同时，在轻量级解码器中引入了寄存器门控全局上下文和受控的跨任务交互，使得深度和语义预测在严格的延迟限制下能够相互增强。其输出通过一个紧凑的感知到建图接口，直接集成到未经修改的单目 SLAM 流水线中。我们评估了稠密预测的准确性和在环系统性能。在 NYUDv2 数据集上，M2H-MX-L 达到了最先进的结果，与具有代表性的多任务基准模型相比，语义 mIoU 提高了 6.6%，深度 RMSE 降低了 9.4%。当部署在 ScanNet 上的实时单目建图系统中时，与强单目 SLAM 基准相比，M2H-MX 将平均轨迹误差降低了 60.7%，同时生成了更清晰的度量-语义地图。这些结果表明，现代多任务稠密预测可以可靠地部署在机器人系统中，用于实时单目空间感知。

## Abstract
Monocular cameras are attractive for robotic perception due to their low cost and ease of deployment, yet achieving reliable real-time spatial understanding from a single image stream remains challenging. While recent multi-task dense prediction models have improved per-pixel depth and semantic estimation, translating these advances into stable monocular mapping systems is still non-trivial.   This paper presents M2H-MX, a real-time multi-task perception model for monocular spatial understanding. The model preserves multi-scale feature representations while introducing register-gated global context and controlled cross-task interaction in a lightweight decoder, enabling depth and semantic predictions to reinforce each other under strict latency constraints. Its outputs integrate directly into an unmodified monocular SLAM pipeline through a compact perception-to-mapping interface.   We evaluate both dense prediction accuracy and in-the-loop system performance. On NYUDv2, M2H-MX-L achieves state-of-the-art results, improving semantic mIoU by 6.6% and reducing depth RMSE by 9.4% over representative multi-task baselines. When deployed in a real-time monocular mapping system on ScanNet, M2H-MX reduces average trajectory error by 60.7% compared to a strong monocular SLAM baseline while producing cleaner metric-semantic maps. These results demonstrate that modern multi-task dense prediction can be reliably deployed for real-time monocular spatial perception in robotic systems.

---

## 论文详细总结（自动生成）

### M2H-MX：用于实时单目空间理解的多任务稠密视觉感知论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：如何在严格的实时性约束下，利用单目相机实现可靠的、具备几何与语义一致性的空间理解。
*   **背景与动机**：
    *   单目相机成本低、易部署，但存在几何歧义性，且难以在保证低延迟的同时提供稳定的深度和语义估计。
    *   现有的多任务稠密预测模型虽然在精度上有所提升，但往往过于沉重，难以直接集成到现有的 SLAM（即时定位与建图）系统中。
    *   实际应用中，感知模块必须满足低延迟、帧间稳定性和“无损集成”（不修改 SLAM 后端）三大约束。

#### 2. 方法论
M2H-MX 采用了一种高效的多任务学习架构，核心思想是利用强大的预训练基础模型特征，通过轻量级解码器实现任务间的互补增强。
*   **骨干网络（Backbone）**：采用冻结的 **DINOv3**，并引入 **LoRA（低秩自适应）** 技术对最后 12 层 Transformer 块进行微调，以极小的参数代价实现任务适配。
*   **寄存器门控多尺度解码器（RGM Decoder）**：
    *   引入 **寄存器令牌（Register Tokens）**：从 DINOv3 提取全局上下文向量，通过门控机制注入到每个尺度的解码特征中。
    *   **Mamba 块**：利用 Mamba 架构实现高效的长程特征传播，确保在低延迟下建模全局依赖。
*   **跨任务交互与细化**：
    *   **跨任务混合器（CTM）**：通过门控聚合机制，允许辅助任务（如法线、边缘）调节目标任务（深度、语义）的特征。
    *   **多尺度卷积注意力（MSCA）**：利用深度可分离卷积实现空间细化。
*   **感知到建图接口**：设计了一个紧凑接口，将预测的深度和语义直接输入到未经修改的 **Mono-Hydra** SLAM 流水线中。

#### 3. 实验设计
*   **数据集**：
    *   **NYUDv2**：室内场景，评估深度、语义、法线和边缘预测。
    *   **Cityscapes**：室外城场景，评估语义和视差预测。
    *   **ScanNet**：用于评估在 SLAM 系统在环（In-the-loop）中的实际表现。
*   **基准方法（Benchmarks）**：
    *   **多任务模型**：TaskPrompter, MTMamba, InvPT, M2H 等。
    *   **SLAM 系统**：DROID-SLAM, Go-SLAM, iMAP, NICE-SLAM。
*   **评价指标**：语义 mIoU、深度 RMSE、绝对轨迹误差（ATE）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的训练硬件（如 GPU 型号、数量）及训练总时长。
*   **推理性能**：提到在异步 SLAM 循环中，感知模块在 GPU 上运行，系统整体能维持 **15–20 Hz** 的稳定帧率，证明了其轻量化设计的有效性。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个主流稠密预测数据集（NYUDv2, Cityscapes）上进行了精度对比。
    *   在 ScanNet 上进行了系统级的 SLAM 轨迹误差测试。
    *   进行了详细的**消融实验**，验证了 CTM、MSCA、RGM 模块以及不同骨干网络（DINOv2, ConvNeXt）对结果的影响。
*   **充分性评价**：实验设计较为全面，不仅关注模型本身的指标，更强调了下游任务（建图与定位）的实际增益，具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **精度提升**：在 NYUDv2 上，M2H-MX-L 将语义 mIoU 提升了 6.6%，深度 RMSE 降低了 9.4%，刷新了 SOTA。
*   **系统增益**：集成到单目 SLAM 系统后，相比于强基准 Go-SLAM，平均轨迹误差（ATE）降低了 **60.7%**。
*   **稳定性**：证明了高质量的单目感知前端可以显著弥补单目 SLAM 在几何稳定性上的先天不足，生成更清晰的度量-语义地图。

#### 7. 优点
*   **系统集成导向**：不同于纯视觉模型，该研究从机器人部署的实际约束出发，解决了感知与建图的接口问题。
*   **架构高效**：结合了 DINOv3 的强大表征、LoRA 的参数效率以及 Mamba 的线性复杂度，实现了精度与速度的平衡。
*   **全局上下文建模**：通过寄存器令牌（Register Tokens）以极低成本引入全局信息，有效缓解了单目视觉的歧义性。

#### 8. 不足与局限
*   **骨干网络依赖**：消融实验显示模型性能高度依赖于 DINOv3，若更换为较弱的骨干网络，性能下降剧烈，这限制了其在极低功耗嵌入式设备上的应用。
*   **训练细节缺失**：未披露训练所需的计算资源和时间成本，难以评估其训练效率。
*   **泛化性验证**：虽然测试了室内外场景，但对于极端光照、动态物体密集等复杂环境的鲁棒性讨论较少。

（完）
