---
title: "To View Transform or Not to View Transform: NeRF-based Pre-training Perspective"
title_zh: 视角转换还是不转换：基于 NeRF 预训练的视角
authors: "Hyeonjun Jeong, Juyeb Shin, Dongsuk Kum"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28090v1"
tags: ["query:ff3dr"]
score: 8.0
evidence: 基于NeRF的3D几何与外观理解预训练
tldr: 针对自动驾驶中NeRF预训练与视图转换之间存在的离散与连续表示冲突问题，本文提出了一种名为NeRP3D的新型点云式3D检测器。该方法摒弃了传统的视图转换步骤，通过学习连续的3D表示来避免先验冲突，并在下游任务中保留预训练的NeRF网络。在nuScenes数据集上的实验证明，NeRP3D在场景重建和目标检测任务上均显著优于现有SOTA方法。
selection_source: fresh_fetch
motivation: 现有的NeRF预训练方法将离散的视图转换与连续的辐射场结合，导致3D表示模糊且预训练模型在下游任务中利用率低。
method: 提出NeRP3D框架，通过基于点的连续3D表示学习取代传统的视图转换，并在检测任务中完整保留预训练的NeRF网络。
result: 在nuScenes数据集上的实验表明，该方法在场景重建和3D目标检测任务中均刷新了SOTA性能。
conclusion: 通过消除视图转换的先验冲突并持续利用NeRF网络，可以显著增强自动驾驶感知模型对3D场景的理解能力。
---

## 摘要
神经辐射场 (NeRF) 已成为以视觉为中心的自动驾驶领域中一种重要的预训练范式，它以完全自监督的方式增强了对 3D 几何和外观的理解。为了将基于 NeRF 的预训练应用于 3D 感知模型，近期的方法简单地将 NeRF 应用于通过视角转换获得的体素特征。然而，将 NeRF 与视角转换耦合会继承相互冲突的先验；视角转换施加了离散且僵化的表示，而辐射场则假设了连续且自适应的函数。当这些相反的假设被强行纳入同一个流水线时，这种不匹配会表现为模糊且不明确的 3D 表示，最终限制了 3D 场景理解。此外，用于预训练的 NeRF 网络在下游任务中会被丢弃，导致通过 NeRF 增强的 3D 表示利用效率低下。在本文中，我们提出了一种新型的类 NeRF 点云 3D 检测器 (NeRP3D)，它可以学习连续的 3D 表示，从而避免视角转换带来的先验不匹配。NeRP3D 无论在何种任务中都保留了预训练的 NeRF 网络，继承了连续 3D 表示学习的原则，从而在场景重建和检测任务中都展现出更大的潜力。在 nuScenes 数据集上的实验表明，我们提出的方法显著改进了之前的最先进方法，不仅在代理场景重建任务中表现优异，在下游检测任务中也同样出色。

## Abstract
Neural radiance fields (NeRFs) have emerged as a prominent pre-training paradigm for vision-centric autonomous driving, which enhances 3D geometry and appearance understanding in a fully self-supervised manner. To apply NeRF-based pretraining to 3D perception models, recent approaches have simply applied NeRFs to volumetric features obtained from view transformation. However, coupling NeRFs with view transformation inherits conflicting priors; view transformation imposes discrete and rigid representations, whereas radiance fields assume continuous and adaptive functions. When these opposing assumptions are forced into a single pipeline, the misalignment surfaces as blurry and ambiguous 3D representations that ultimately limit 3D scene understanding. Moreover, the NeRF network for pre-training is discarded during downstream tasks, resulting in inefficient utilization of enhanced 3D representations through NeRF. In this paper, we propose a novel NeRF-Resembled Point-based 3D detector that can learn continuous 3D representation and thus avoid the misaligned priors from view transformation. NeRP3D preserves the pre-trained NeRF network regardless of the tasks, inheriting the principle of continuous 3D representation learning and leading to greater potentials for both scene reconstruction and detection tasks. Experiments on nuScenes dataset demonstrate that our proposed approach significantly improves previous state-of-the-art methods, outperforming not only pretext scene reconstruction tasks but also downstream detection tasks.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：To View Transform or Not to View Transform

#### 1. 核心问题与整体含义（研究动机和背景）
在以视觉为中心的自动驾驶领域，利用神经辐射场（NeRF）进行自监督预训练已成为增强 3D 几何理解的主流范式。然而，现有方法（如 UniPAD, SelfOcc）通常将 NeRF 挂载在**视角转换（View Transformation, VT）**生成的体素特征上。
*   **研究动机**：作者发现视角转换与 NeRF 之间存在本质的**先验冲突**——视角转换依赖于离散、僵化的网格表示，而 NeRF 假设的是连续、自适应的函数。这种冲突导致生成的 3D 表示模糊且存在歧义。
*   **核心问题**：此外，预训练后的 NeRF 网络在下游任务中往往被丢弃，导致预训练学到的精细 3D 知识无法有效迁移。

#### 2. 方法论：NeRP3D 架构
论文提出了一种名为 **NeRP3D** 的类 NeRF 点云 3D 检测器，旨在统一场景重建与感知任务。
*   **核心思想**：摒弃离散的体素网格，直接在连续 3D 空间中对点进行建模，使模型完全继承 NeRF 的连续表示能力。
*   **关键技术细节**：
    *   **自适应采样**：针对不同任务采用不同采样策略。渲染任务使用**射线采样（Ray-wise Sampling）**；感知任务（如检测、占据预测）使用**空间均匀采样（Spatial Sampling）**。
    *   **点特征提取**：利用**可变形交叉注意力（Deformable Cross Attention）**，将 3D 查询点与多视图 2D 图像特征直接关联，通过学习偏移量来捕捉相关区域的上下文。
    *   **统一表示**：所有采样点在同一坐标系下参数化，并使用符号距离函数（SDF）和 RGB 渲染头进行自监督优化。
    *   **损失函数**：结合了 RGB 重建损失、LiDAR 深度监督损失以及多视图一致性重投影损失。

#### 3. 实验设计
*   **数据集**：主要使用 **nuScenes**（包含 1000 个驾驶场景，涵盖 360° 环视图像和 LiDAR）。为了验证泛化性，还使用了 **Argoverse 2 (AV2)** 进行跨域实验。
*   **Benchmark 与任务**：
    *   **代理任务（Pretext）**：场景重建（PSNR, SSIM, LPIPS）和深度估计（AbsRel, RMSE）。
    *   **下游任务（Downstream）**：3D 目标检测（mAP, NDS）、占据预测（mIoU）、HD 地图构建（mAP）。
*   **对比方法**：包括 UniPAD, SelfOcc, BEVFormer, UVTR-C, GaussianPretrain 等 SOTA 预训练或感知模型。

#### 4. 资源与算力
*   **硬件**：使用了 **4 张 NVIDIA A6000 GPU**。
*   **训练配置**：
    *   预训练阶段：24 个 epoch。
    *   微调阶段：24 个 epoch（消融实验中部分为 12 个 epoch）。
    *   优化器：AdamW，初始学习率为 2e-4。
    *   分辨率：渲染与地图任务使用 800x450，目标检测使用 1600x900。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括三个主要的下游感知任务和两个重建任务。
*   **充分性评价**：
    *   **跨数据集验证**：通过 AV2 到 nuScenes 的零样本（Zero-shot）迁移实验，证明了点云架构比体素架构更具传感器无关性（Sensor-agnostic）。
    *   **消融实验**：探讨了分辨率影响、任务间特征保留（Cross-Task Generalization）、采样策略等，实验设计较为全面。
    *   **客观性**：在相同检测头和骨干网络下进行对比，确保了公平性。

#### 6. 主要结论与发现
*   **性能突破**：NeRP3D 在 nuScenes 3D 检测上达到 47.3 NDS，显著优于 UniPAD（45.5）和基于高斯散射的 GaussianPretrain（47.2）。
*   **重建质量**：在 RGB 重建上，NeRP3D 的 PSNR 达到 33.42，远超 UniPAD 的 21.14，证明了连续表示在捕捉高频几何细节方面的优势。
*   **知识保留**：实验发现，即使在感知任务微调后，NeRP3D 依然能保持良好的渲染能力，而基于视角转换的方法会出现“灾难性遗忘”，导致渲染结果崩溃。

#### 7. 优点（亮点）
*   **架构创新**：成功解决了 NeRF 预训练与离散视角转换之间的先验冲突。
*   **高保真度**：生成的 3D 特征图极其清晰，甚至在不进行额外蒸馏的情况下，其特征分割效果可媲美 DINO 等 2D 大模型。
*   **强泛化性**：由于不依赖固定的体素网格，模型对相机内参和外参的变化（如传感器布局改变）表现出极强的鲁棒性。

#### 8. 不足与局限
*   **计算开销**：点云架构在将输出适配到现有检测头（通常需要网格化输入）时，可能会带来额外的计算负担。
*   **远程依赖**：在感兴趣区域（ROI）之外的深度估计仍高度依赖 LiDAR 监督，纯视觉的远程重建仍有挑战。
*   **时序缺失**：目前主要基于单帧图像处理，尚未充分利用多帧时序信息进行 RGB 重建的一致性约束。

（完）
