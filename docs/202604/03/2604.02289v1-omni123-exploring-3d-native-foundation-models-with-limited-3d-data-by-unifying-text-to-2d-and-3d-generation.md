---
title: "Omni123: Exploring 3D Native Foundation Models with Limited 3D Data by Unifying Text to 2D and 3D Generation"
title_zh: Omni123：通过统一文本到 2D 与 3D 生成，探索有限 3D 数据下的 3D 原生基础模型
authors: "Chongjie Ye, Cheng Cao, Chuanyu Pan, Yiming Hao, Yihao Zhi, Yuanming Hu, Xiaoguang Han"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02289v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 统一文本到2D和文本到3D生成
tldr: 针对3D数据稀缺导致的几何一致性问题，本文提出Omni123，一个统一文本、图像和3D生成的原生3D大模型。该模型通过自回归框架将多模态数据Token化，利用丰富的2D数据作为几何先验，并通过交替式训练和跨模态循环约束，在无需完全对齐数据的情况下实现了高保真、几何一致的3D生成与编辑，为构建3D世界模型提供了可扩展路径。
selection_source: fresh_fetch
motivation: 现有的3D生成方法因缺乏高质量数据而依赖间接流水线，导致几何一致性不足且难以实现原生的多模态统一。
method: 提出一种自回归框架，将文本、图像和3D数据统一为离散Token，并通过交替式X-to-X训练和跨模态循环约束来增强几何先验。
result: 实验证明Omni123在文本引导的3D生成和编辑任务中显著提升了语义对齐度、外观保真度和多视角几何一致性。
conclusion: 该研究展示了通过统一2D与3D生成任务，利用跨模态一致性克服3D数据短缺并构建原生3D基础模型的可行性。
---

## 摘要
最近的多模态大语言模型在统一的文本和图像理解与生成方面取得了强劲性能，但由于数据有限，将这种原生能力扩展到 3D 领域仍具挑战性。与丰富的 2D 图像相比，高质量 3D 资产稀缺，导致 3D 合成面临约束不足的问题。现有方法通常依赖于间接流水线，即在 2D 空间进行编辑并通过优化将结果提升至 3D，这牺牲了几何一致性。我们提出了 Omni123，这是一个在单一自回归框架内统一了文本到 2D 和文本到 3D 生成的 3D 原生基础模型。我们的核心见解是，图像与 3D 之间的跨模态一致性可以作为一种隐式的结构约束。通过将文本、图像和 3D 表示为共享序列空间中的离散 token，该模型利用丰富的 2D 数据作为几何先验，以改进 3D 表示。我们引入了一种交错式 X-to-X 训练范式，在异构配对数据集上协调多种跨模态任务，而无需完全对齐的“文本-图像-3D”三元组。通过在自回归序列中遍历语义-视觉-几何循环（例如，从文本到图像到 3D 再到图像），该模型共同强化了语义对齐、外观保真度和多视图几何一致性。实验表明，Omni123 显著提升了文本引导的 3D 生成与编辑效果，展示了通往多模态 3D 世界模型的可扩展路径。

## Abstract
Recent multimodal large language models have achieved strong performance in unified text and image understanding and generation, yet extending such native capability to 3D remains challenging due to limited data. Compared to abundant 2D imagery, high-quality 3D assets are scarce, making 3D synthesis under-constrained. Existing methods often rely on indirect pipelines that edit in 2D and lift results into 3D via optimization, sacrificing geometric consistency. We present Omni123, a 3D-native foundation model that unifies text-to-2D and text-to-3D generation within a single autoregressive framework. Our key insight is that cross-modal consistency between images and 3D can serve as an implicit structural constraint. By representing text, images, and 3D as discrete tokens in a shared sequence space, the model leverages abundant 2D data as a geometric prior to improve 3D representations. We introduce an interleaved X-to-X training paradigm that coordinates diverse cross-modal tasks over heterogeneous paired datasets without requiring fully aligned text-image-3D triplets. By traversing semantic-visual-geometric cycles (e.g., text to image to 3D to image) within autoregressive sequences, the model jointly enforces semantic alignment, appearance fidelity, and multi-view geometric consistency. Experiments show that Omni123 significantly improves text-guided 3D generation and editing, demonstrating a scalable path toward multimodal 3D world models.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **Omni123** 的原生 3D 基础模型，旨在通过统一文本、图像和 3D 生成任务，解决高质量 3D 数据稀缺带来的生成不一致问题。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：高质量 3D 资产（如网格模型）的数量远少于互联网上的 2D 图像，导致 3D 生成模型面临严重的“欠约束”问题。
*   **现有方案局限**：目前的 3D 生成多采用“间接流水线”（如先生成 2D 图像再通过迭代优化提升至 3D），这往往牺牲了几何一致性，且流程复杂，非原生生成。
*   **研究动机**：2D 图像中隐含了丰富的几何、形状和外观先验。论文提出，如果能在一个统一的框架内同时学习 2D 和 3D 生成，就可以利用海量的 2D 数据来强化 3D 表示，实现“原生”且一致的 3D 生成。

### 2. 方法论
Omni123 采用了一个统一的自回归（Autoregressive）Transformer 框架，将所有模态转化为离散 Token。
*   **核心思想**：通过“语义-视觉-几何”循环（如：文本 $\to$ 图像 $\to$ 3D $\to$ 图像）建立跨模态生成一致性，将其作为隐式结构约束。
*   **关键技术细节**：
    *   **多模态 Token 化**：
        *   **文本**：使用 CLIP 和 Qwen3-0.6B 双编码器提取语义特征。
        *   **图像**：提出一种**两阶段 1D 图像 Tokenizer**。第一阶段训练高质量连续 VAE（基于 DINOv3），第二阶段训练 1D Q-Former 将特征离散化为 1024 个 Token。
        *   **3D 几何**：采用 Cube3D（基于 3DShape2VecSet）将 3D 网格压缩为 1024 个离散 Token。
    *   **三阶段训练范式**：
        1.  **预训练 (PT)**：执行 X-to-X 任务（文本 $\leftrightarrow$ 图像，图像 $\leftrightarrow$ 3D，文本 $\leftrightarrow$ 3D），在异构数据集上学习跨模态对齐。
        2.  **持续训练 (CT)**：引入可学习的**视角 Token (View Tokens)**，使模型具备视角感知能力，支持多视角图像生成。
        3.  **监督微调 (SFT)**：使用交错的序列（如文本 $\to$ 6 个视角图 $\to$ 3D）进行训练，闭合跨模态循环。
    *   **3D 编辑**：将编辑指令作为文本输入，源 3D 模型作为前缀，自回归生成修改后的 3D Token。

### 3. 实验设计
*   **数据集**：
    *   **2D**：63.7M 文本-图像对（含 Z-Image 合成数据）。
    *   **3D**：120M 图像-3D 对（来自 Objaverse 等），9M 文本-3D 对。
    *   **编辑**：3DEditVerse（116K 训练对）。
    *   **合成数据**：利用 Meshy 4 管道合成了 2M 高质量文本-图像-3D 三元组。
*   **Benchmark 与对比方法**：
    *   **文本转 3D 形状**：对比了“两阶段方法”（Sana/Show-o/LlamaGen + Hunyuan3D 2.1）和“原生方法”（ShapeLLM-Omni, Cube3D）。使用 ULIP-T 和 Uni3D-T 评估语义对齐。
    *   **3D 编辑**：在 Edit3D-Bench 上对比了 Steer3D, 3DEditFormer, Tailor3D 等。使用 Chamfer Distance (CD) 和 F1 分数评估几何准确性。
    *   **图像重建**：在 ImageNet-1K 上对比了 SDXL, Flux.1, LlamaGen 等。

### 4. 资源与算力
论文明确列出了算力消耗：
*   **硬件**：使用了 **256 张 NVIDIA H100 GPU** 进行预训练。
*   **训练量**：
    *   **预训练**：约 1.16 万亿 (T) Token。
    *   **持续训练**：约 0.2T Token。
    *   **微调 (SFT)**：约 0.15T Token。
*   **编辑微调**：使用 32 张 H100 训练了 10,000 步。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3D 形状生成、指令引导编辑、图像 Tokenizer 性能评估三大板块。
*   **充分性**：
    *   对比了当前最先进的开源和商业级 3D 生成模型。
    *   进行了定性和定量分析，并提供了大量的可视化结果（包括复杂组合提示词的生成）。
    *   **客观性**：通过第三方多模态评测模型（ULIP/Uni3D）进行打分，减少了主观偏差。但 3D 编辑实验中提到部分 Benchmark 与对比方法（Steer3D）存在数据分布重合，论文对此做了客观说明。

### 6. 主要结论与发现
*   **2D 辅助 3D 的有效性**：统一 2D 和 3D 生成任务能显著提升 3D 模型的几何一致性和语义对齐度，证明 2D 数据是极佳的几何先验。
*   **交错训练的优势**：通过交错任务（Interleaved tasks）可以缓解不同模态间的梯度干扰，促进知识的正向迁移。
*   **参数效率**：2.2B 参数的 Omni123 在 3D 任务上的表现优于某些 7B 规模的模型（如 ShapeLLM-Omni），说明架构设计和训练策略比单纯堆参数更重要。

### 7. 优点（亮点）
*   **原生统一**：真正实现了在一个自回归框架下处理文本、图像和 3D，无需中间代理。
*   **高效 Tokenizer**：提出的两阶段 1D 图像 Tokenizer 在重建质量（FID/PSNR）上显著优于现有的离散 Tokenizer（如 LlamaGen）。
*   **数据增强策略**：通过视觉思维链（Visual CoT）和多粒度标注提升了 3D 数据的描述质量。

### 8. 不足与局限
*   **分辨率限制**：3D Tokenizer 的网格分辨率是固定的，可能无法捕捉极细微的几何特征。
*   **视角依赖**：目前主要依赖 6 个标准视角，对于任意视角的处理能力仍有提升空间。
*   **场景级生成**：目前主要集中在单个物体（Object-centric）的生成与编辑，尚未扩展到复杂的 3D 场景生成。
*   **物理属性**：模型目前不涉及材质（PBR）的精细建模和物理仿真属性。

（完）
