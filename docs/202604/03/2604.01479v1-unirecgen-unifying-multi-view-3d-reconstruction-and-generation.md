---
title: "UniRecGen: Unifying Multi-View 3D Reconstruction and Generation"
title_zh: UniRecGen：统一多视角3D重建与生成
authors: "Zhisheng Huang, Jiahao Chen, Cheng Lin, Chenyu Hu, Hanzhuo Huang, Zhengming Yu, Mengfei Li, Yuheng Liu, Zekai Gu, Zibo Zhao, Yuan Liu, Xin Li, Wenping Wang"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01479v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 统一多视图3D重建与生成
tldr: 针对稀疏视图3D建模中重建保真度与生成合理性之间的矛盾，本文提出UniRecGen统一框架。该框架将前馈重建的高效性与扩散生成的丰富先验相结合，通过在共享规范空间内进行解耦协作学习，解决了坐标空间和训练目标的冲突。重建模块提供几何锚点，扩散生成器则负责细化和补全结构，最终实现了高保真、一致且完整的3D模型生成。
selection_source: fresh_fetch
motivation: 旨在解决稀疏视图3D建模中，传统重建方法缺乏全局先验而扩散生成方法难以保持多视角一致性的固有矛盾。
method: 提出一种解耦协作学习框架，在共享规范空间内将重建模块提供的几何锚点与扩散生成器的潜变量增强条件相结合。
result: 实验证明UniRecGen在处理稀疏观测时，其生成的3D模型在保真度、结构完整性和多视角一致性方面均优于现有方法。
conclusion: UniRecGen成功统一了重建与生成范式，为高效、高质量的稀疏视图3D建模提供了一种稳健的新方案。
---

## 摘要
稀疏视角3D建模体现了重建保真度与生成合理性之间的基本矛盾。虽然前馈重建在效率和输入对齐方面表现出色，但往往缺乏结构完整性所需的全局先验。相反，基于扩散的生成提供了丰富的几何细节，但在多视角一致性方面面临挑战。我们提出了 UniRecGen，这是一个将这两种范式整合到单个协作系统中的统一框架。为了克服坐标空间、3D表示和训练目标中的固有冲突，我们将两个模型对齐在共享的规范空间内。我们采用了解耦协作学习，在保持训练稳定的同时，实现了推理过程中的无缝协作。具体而言，重建模块经过调整以提供规范几何锚点，而扩散生成器则利用潜变量增强条件来细化并补全几何结构。实验结果表明，UniRecGen 实现了卓越的保真度和鲁棒性，在从稀疏观测中创建完整且一致的3D模型方面优于现有方法。

## Abstract
Sparse-view 3D modeling represents a fundamental tension between reconstruction fidelity and generative plausibility. While feed-forward reconstruction excels in efficiency and input alignment, it often lacks the global priors needed for structural completeness. Conversely, diffusion-based generation provides rich geometric details but struggles with multi-view consistency. We present UniRecGen, a unified framework that integrates these two paradigms into a single cooperative system. To overcome inherent conflicts in coordinate spaces, 3D representations, and training objectives, we align both models within a shared canonical space. We employ disentangled cooperative learning, which maintains stable training while enabling seamless collaboration during inference. Specifically, the reconstruction module is adapted to provide canonical geometric anchors, while the diffusion generator leverages latent-augmented conditioning to refine and complete the geometric structure. Experimental results demonstrate that UniRecGen achieves superior fidelity and robustness, outperforming existing methods in creating complete and consistent 3D models from sparse observations.

---

## 论文详细总结（自动生成）

以下是对论文《UniRecGen: Unifying Multi-View 3D Reconstruction and Generation》的结构化总结：

### 1. 论文的核心问题与整体含义
该论文旨在解决**稀疏视角（Sparse-view）3D建模**中长期存在的矛盾：
*   **重建范式（Reconstruction）**：如前馈回归模型，擅长与输入图像对齐（保真度高），但由于缺乏全局先验，在处理遮挡或未观测区域时会导致几何不完整。
*   **生成范式（Generation）**：如扩散模型，拥有强大的形状先验，能生成合理的细节，但难以保持严格的多视角一致性，且容易产生偏离输入的“幻觉”。
**UniRecGen** 的核心思想是将两者统一：利用重建模块建立确定性的“几何锚点”，并引导生成模块进行结构补全和细节细化，从而在保持高保真度的同时实现几何完整性。

### 2. 方法论
论文提出了一个两阶段的解耦协作学习框架：
*   **核心思想**：在共享的**规范坐标空间（Canonical Space）**中对齐重建与生成模型，解决两者在坐标系和表示法上的不兼容。
*   **关键技术细节**：
    1.  **生成兼容的重建（Stage I）**：基于 VGGT 架构，采用**分支改用（Branch Repurposing）**策略。仅让点图（Pointmap）分支输出规范空间坐标，而保持深度和相机分支在参考帧中以保留预训练先验。通过**相似性对齐（Procrustes Analysis）**将精确的深度预测转换至规范空间。
    2.  **重建引导的受控生成（Stage II）**：基于 Hunyuan3D-Omni 架构，引入**潜变量增强的多视角条件（Latent-Augmented View Conditioning）**。
    3.  **算法流程**：输入 $N$ 张无位姿图像 $\rightarrow$ 重建模块预测规范化点云（几何锚点） $\rightarrow$ 提取图像 DINO 特征、重建模块的中间潜变量（VGGT tokens）和相机嵌入 $\rightarrow$ 扩散生成器在几何锚点引导下合成最终的高保真网格（Mesh）。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：从 Objaverse-XL 中筛选出的 40K 个高质量 3D 模型。
    *   **测试集**：Google Scanned Objects (GSO) 和 Toys4K（各 100 个对象）。
*   **Benchmark 设置**：从 24 个随机视角中抽取 4 个稀疏视角作为输入，模拟无位姿、稀疏观测的挑战场景。
*   **对比方法**：
    *   **生成类**：Hunyuan3D-MV、TRELLIS-S/M（随机单视角/多视角融合）。
    *   **重建类**：LucidFusion、SAM 3D（单视角基础模型）。
    *   **统一类**：ReconViaGen（同期最相关的统一范式工作）。

### 4. 资源与算力
论文明确说明了训练所需的算力资源：
*   **硬件**：使用了 **4 台 NVIDIA H800 GPU**。
*   **第一阶段（重建适配）**：训练 80K 步，耗时约 **5 天**。
*   **第二阶段（生成器训练）**：训练 80K 步，耗时约 **8 天**。
*   **总计**：约 13 天的 4-GPU 算力投入。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流基准数据集（GSO, Toys4K）上进行了详尽的定量评估，涵盖了 Chamfer-L2、F-Score、Normal Consistency、IoU 等 6 项几何指标，以及位姿和深度预测指标。
*   **消融实验**：针对“规范化对齐策略”和“多视角条件注入方式”进行了专门的消融研究，验证了分支改用策略和潜变量增强条件的有效性。
*   **充分性评价**：实验设计较为充分且客观。通过与同期最先进的 ReconViaGen 对比，证明了其直接几何引导优于隐式特征注入。此外，还展示了在真实世界图像上的泛化能力。

### 6. 主要结论与发现
*   **统一的优越性**：UniRecGen 在所有几何指标上均显著优于纯重建或纯生成方法。
*   **坐标对齐是关键**：简单的坐标监督会破坏预训练先验，而“分支改用”策略能完美平衡坐标对齐与几何精度。
*   **密集特征的重要性**：相比于稀疏点引导的特征采样，保留完整的密集图像特征并注入几何上下文（Latent-Augmented）能产生更好的输入对齐效果。

### 7. 优点
*   **模块化设计**：重建和生成模块相对独立，未来可以灵活更换更强大的子模型。
*   **高保真与完整性并存**：成功解决了稀疏视角下“看得见的对不准，看不见的补不全”的问题。
*   **无需已知位姿**：能够处理完全无位姿的输入，具有极强的实用性。

### 8. 不足与局限
*   **计算开销**：两阶段训练耗时较长（共 13 天），且推理时需要运行两个大型模型，效率仍有提升空间。
*   **场景限制**：目前主要集中在**物体级（Object-level）**建模，尚未扩展到复杂的场景级（Scene-level）生成。
*   **属性缺失**：论文目前侧重于几何形状（Mesh）的恢复，尚未整合纹理合成（Texture Synthesis）和 PBR 材质生成。

（完）
