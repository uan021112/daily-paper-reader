---
title: Rendering Multi-Human and Multi-Object with 3D Gaussian Splatting
title_zh: 基于 3D 高斯泼溅的多人多物渲染
authors: "Weiquan Wang, Jun Xiao, Feifei Shao, Yi Yang, Yueting Zhuang, Long Chen"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02996v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 使用3D高斯泼溅重建包含多个人物和物体的动态场景
tldr: 针对稀疏视角下多人物与多物体（MHMO）动态场景重建的挑战，本文提出MM-GS框架。该方法基于3D高斯泼溅，通过单实例多视图融合模块解决严重遮挡下的表示一致性问题，并利用场景级实例交互模块建模复杂的组合依赖关系。实验证明，MM-GS在处理复杂交互和细节还原方面达到了SOTA水平，为机器人和VR/AR应用提供了高保真数字孪生方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02996v1/fig-001.webp\", \"caption\": \"Fig. 2. Overview of MM-GS pipeline. Our method refines initial 3D Gaussian representations through three main stages. (a) Human-Object Deformation: We initialize the scene by deforming canonical human and object models to their target poses and representing them as collections of 3D Gaussians. (b) Per-Instance Multi-View Fusion: A Cross-View Fusion network refines each instance’s appearance and local geometry by aggregating visual features from all its visible viewpoints, ensuring a view-consistent representation. (c) Scene-Level Instance Interaction: Finally, an Instance Interaction network operates on a global scene graph to model the dependencies between all participants, enabling a final refinement to capture interaction-driven effects.\", \"page\": 4, \"index\": 1, \"width\": 1052, \"height\": 476}, {\"url\": \"assets/figures/arxiv/2604.02996v1/fig-002.webp\", \"caption\": \"Fig. 1. Core challenges in Multi-Human Multi-Object (MHMO) rendering. From sparse views, rendering complex interactions involves overcoming two key challenges: ensuring cross-view consistency under severe occlusion (top) and modeling the mutual influence between instances at contact regions (bottom). Our MM-GS is designed to address both.\", \"page\": 1, \"index\": 2, \"width\": 512, \"height\": 487}]"
motivation: 在多实例交互场景中，严重的相互遮挡和复杂的组合依赖关系使得实现视图一致的表示和精确的交互建模变得极其困难。
method: 提出一种基于3DGS的分层框架，通过多视图融合建立单实例稳健表示，并利用全局场景图推理实例间的交互关系以优化属性。
result: 实验结果显示，该方法在多个挑战性数据集上显著超越了强基线模型，能够生成具有高保真细节和合理接触关系的渲染效果。
conclusion: MM-GS通过分层架构和显式交互建模，有效解决了MHMO场景的重建难题，为构建高保真数字孪生提供了先进的技术路径。
---

## 摘要
从稀疏视角输入中重建包含多个交互人物和物体的动态场景是一项关键且具有挑战性的任务，对于为机器人技术和 VR/AR 创建高保真数字孪生至关重要。我们将这一问题称为多人多物（MHMO）渲染，它面临两个主要障碍：在严重的相互遮挡下实现单个实例的视角一致性表示，以及显式建模由交互产生的复杂且组合的依赖关系。为了克服这些挑战，我们提出了 MM-GS，这是一个基于 3D 高斯泼溅（3D Gaussian Splatting）构建的新型分层框架。我们的方法首先采用单实例多视角融合模块，通过聚合所有可用视角的视觉信息，为每个实例建立鲁棒且一致的表示。随后，场景级实例交互模块在全局场景图上运行，以推理所有参与者之间的关系，并优化其属性以捕捉细微的交互效果。在具有挑战性的数据集上进行的广泛实验表明，我们的方法显著优于强基准模型，在产生高保真细节和合理的实例间接触方面达到了最先进的结果。

## Abstract
Reconstructing dynamic scenes with multiple interacting humans and objects from sparse-view inputs is a critical yet challenging task, essential for creating high-fidelity digital twins for robotics and VR/AR. This problem, which we term Multi-Human Multi-Object (MHMO) rendering, presents two significant obstacles: achieving view-consistent representations for individual instances under severe mutual occlusion, and explicitly modeling the complex and combinatorial dependencies that arise from their interactions. To overcome these challenges, we propose MM-GS, a novel hierarchical framework built upon 3D Gaussian Splatting. Our method first employs a Per-Instance Multi-View Fusion module to establish a robust and consistent representation for each instance by aggregating visual information across all available views. Subsequently, a Scene-Level Instance Interaction module operates on a global scene graph to reason about relationships between all participants, refining their attributes to capture subtle interaction effects. Extensive experiments on challenging datasets demonstrate that our method significantly outperforms strong baselines, producing state-of-the-art results with high-fidelity details and plausible inter-instance contacts.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **MM-GS** 的新型分层框架，旨在解决稀疏视角下多人与多物体（MHMO）交互场景的高保真动态渲染问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在仅有稀疏视角（Sparse-view）输入的情况下，重建并渲染包含多个相互作用的人物和物体的复杂动态场景。
*   **研究背景**：该任务（MHMO 渲染）是构建机器人导航、VR/AR 数字孪生的基础。
*   **主要挑战**：
    1.  **视角一致性难题**：在多人多物场景中，实例间存在严重的相互遮挡，导致单视角观察不完整，传统方法难以在不同视角间保持几何和外观的一致性。
    2.  **复杂的交互依赖**：实例间的接触（如手抓物体、脚踩地面）会产生细微的阴影、颜色渗透和边界模糊，现有的单人-单物模型无法处理多人多物组合产生的复杂关系图。

### 2. 方法论
MM-GS 基于 **3D 高斯泼溅（3DGS）**，采用分层精细化策略：
*   **阶段一：人体与物体变形（Initialization）**：
    *   **人体**：利用 SMPL 参数化模型，通过调制线性混合蒙皮（Modulated LBS）将高斯点从标准姿态变形到目标姿态。
    *   **物体**：基于刚体变换将物体模板对齐到场景中。
*   **阶段二：单实例多视图融合（Per-Instance Multi-View Fusion）**：
    *   **核心思想**：让“看得见”的视角去指导“被遮挡”的视角。
    *   **技术细节**：使用 2D CNN 提取图像特征，通过特征投影（Feature Lifting）将多视角视觉信息聚合到 3D 高斯点上。利用交叉视图融合机制建立信息流，确保每个实例的表示在全局视角下是完整且一致的。
*   **阶段三：场景级实例交互（Scene-Level Instance Interaction）**：
    *   **核心思想**：通过全局场景图建模实例间的物理和视觉关联。
    *   **技术细节**：根据实例包围盒（Bounding Box）的重叠情况构建动态场景图。使用**图注意力网络（GAT）**在节点（实例）间传递信息，捕捉接触区域的细节，并预测高斯属性的残差更新（如位置微调、接触阴影处理）。

### 3. 实验设计
*   **数据集**：
    1.  **HOI-M³**：大型多人多物交互数据集，涵盖客厅、健身房、办公室等复杂室内场景。
    2.  **CORE4D-Real**：双人协作搬运物体的真实场景数据集。
*   **基准测试（Benchmark）**：
    *   **NeuralHOIFVV-MM**：将领先的 NeRF 类人机交互方法扩展到多实例设置。
    *   **GTU-MM**：将最先进的 3DGS 多人重建方法扩展到包含物体的场景。
*   **对比指标**：PSNR（峰值信噪比）、SSIM（结构相似性）、LPIPS（感知相似度）。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA A100 GPU** 进行训练。
*   **训练时长**：完整模型的训练时间约为 **40 分钟**。
*   **推理性能**：渲染速度超过 **160 FPS**，满足实时性要求。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同性质的数据集上进行了测试，涵盖了从 3 视角到 4 视角的极端稀疏情况。
*   **消融实验**：对比了“无融合”、“无交互”、“两者皆无”以及“完整模型”四种变体，验证了每个模块对提升清晰度和处理接触边界的贡献。
*   **评价**：实验设计较为充分，对比了 NeRF 和 3DGS 两种主流技术路线的变体，客观展示了该方法在处理复杂遮挡和接触细节上的优势。

### 6. 主要结论与发现
*   **分层设计的必要性**：先解决单实例的视角一致性，再解决实例间的交互依赖，是处理 MHMO 场景的有效路径。
*   **交互建模的作用**：显式的场景图推理能显著减少接触区域的伪影（如穿模、模糊边界），使渲染结果更符合物理真实感。
*   **性能优势**：MM-GS 在保持 3DGS 实时渲染优势的同时，在稀疏视角下的重建质量显著优于基于 NeRF 的方法。

### 7. 优点与亮点
*   **首创性**：首次系统性地探讨了多人多物（MHMO）这一更具通用性的渲染任务。
*   **分层架构**：解耦了单实例表示学习与场景级交互建模，降低了优化难度。
*   **高质量细节**：在处理接触阴影和清晰边界方面表现出色，生成的数字孪生模型更具保真度。

### 8. 不足与局限
*   **依赖先验**：模型高度依赖准确的物体姿态估计和 2D 实例分割掩码（Mask），在“野外”无标注场景下的部署受限。
*   **物理范围限制**：目前主要针对刚性物体和关节化人体，对于流体、布料等更复杂的物理现象尚未建模。
*   **泛化性风险**：虽然在两个数据集上表现良好，但对于极端复杂的群体交互（如 10 人以上拥挤场景）的扩展性仍待验证。

（完）
