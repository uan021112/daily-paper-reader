---
title: "Think, Act, Build: An Agentic Framework with Vision Language Models for Zero-Shot 3D Visual Grounding"
title_zh: 思考、行动、构建：一种基于视觉语言模型的零样本 3D 视觉定位智能体框架
authors: "Haibo Wang, Zihao Lin, Zhiyang Xu, Lifu Huang"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00528v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 利用VLM从多视图2D图像重建3D结构
tldr: "本文提出Think, Act, Build (TAB)框架，旨在解决零样本3D视觉定位中过度依赖预处理点云的问题。该框架将任务重构为生成式2D到3D重建过程，利用VLM智能体在原始RGB-D流中动态追踪目标，并通过语义锚定几何扩展机制克服多视角覆盖不足。实验证明，该方法在ScanRefer和Nr3D上显著优于现有零样本方法，甚至超越了部分全监督基准。"
selection_source: fresh_fetch
motivation: 现有方法通常将3D视觉定位简化为预处理点云的提议匹配，忽略了2D VLM强大的语义解析能力与多视角几何在构建3D结构中的潜力。
method: 提出一种动态智能体框架，通过VLM调用视觉工具在2D帧中追踪目标，并利用语义锚定几何扩展机制将多视角特征聚合为精确的3D坐标。
result: 在ScanRefer和Nr3D数据集上的实验显示，该框架仅使用开源模型即大幅领先于此前的零样本方法，并展现出超越全监督基准的性能。
conclusion: 通过解耦空间语义理解与几何结构实例化，TAB框架为实现高效、鲁棒的零样本3D视觉定位提供了一种全新的生成式重建范式。
---

## 摘要
3D 视觉定位 (3D-VG) 旨在通过自然语言描述在 3D 场景中定位物体。虽然利用视觉语言模型 (VLMs) 的最新进展已经探索了零样本的可能性，但它们通常受限于依赖预处理 3D 点云的静态工作流，本质上将定位退化为了候选框匹配。为了摆脱这种依赖，我们的核心动机是将任务解耦：利用 2D VLMs 解析复杂的空间语义，同时依靠确定性的多视图几何来实例化 3D 结构。受此启发，我们提出了“思考、行动、构建 (TAB)”，这是一个动态智能体框架，它将 3D-VG 任务重新表述为直接在原始 RGB-D 流上运行的生成式 2D 到 3D 重建范式。具体而言，在专门的 3D-VG 技能引导下，我们的 VLM 智能体动态调用视觉工具，在 2D 帧中跟踪并重建目标。关键在于，为了克服由严格的 VLM 语义跟踪引起的多视图覆盖不足问题，我们引入了语义锚定几何扩展 (Semantic-Anchored Geometric Expansion) 机制，该机制首先在参考视频剪辑中锚定目标，然后利用多视图几何将其空间位置传播到未观察到的帧中。这使得智能体能够通过相机参数聚合这些多视图特征，从而“构建”目标的 3D 表示，将 2D 视觉线索直接映射到 3D 坐标。此外，为了确保评估的严谨性，我们识别了现有基准测试中存在的指代歧义和类别错误等缺陷，并手动修正了错误的查询。在 ScanRefer 和 Nr3D 上的大量实验表明，我们的框架完全依赖开源模型，其性能显著优于之前的零样本方法，甚至超过了全监督基准。

## Abstract
3D Visual Grounding (3D-VG) aims to localize objects in 3D scenes via natural language descriptions. While recent advancements leveraging Vision-Language Models (VLMs) have explored zero-shot possibilities, they typically suffer from a static workflow relying on preprocessed 3D point clouds, essentially degrading grounding into proposal matching. To bypass this reliance, our core motivation is to decouple the task: leveraging 2D VLMs to resolve complex spatial semantics, while relying on deterministic multi-view geometry to instantiate the 3D structure. Driven by this insight, we propose "Think, Act, Build (TAB)", a dynamic agentic framework that reformulates 3D-VG tasks as a generative 2D-to-3D reconstruction paradigm operating directly on raw RGB-D streams. Specifically, guided by a specialized 3D-VG skill, our VLM agent dynamically invokes visual tools to track and reconstruct the target across 2D frames. Crucially, to overcome the multi-view coverage deficit caused by strict VLM semantic tracking, we introduce the Semantic-Anchored Geometric Expansion, a mechanism that first anchors the target in a reference video clip and then leverages multi-view geometry to propagate its spatial location across unobserved frames. This enables the agent to "Build" the target's 3D representation by aggregating these multi-view features via camera parameters, directly mapping 2D visual cues to 3D coordinates. Furthermore, to ensure rigorous assessment, we identify flaws such as reference ambiguity and category errors in existing benchmarks and manually refine the incorrect queries. Extensive experiments on ScanRefer and Nr3D demonstrate that our framework, relying entirely on open-source models, significantly outperforms previous zero-shot methods and even surpasses fully supervised baselines.

---

## 论文详细总结（自动生成）

以下是对论文《Think, Act, Build: An Agentic Framework with Vision Language Models for Zero-Shot 3D Visual Grounding》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 3D 视觉定位（3D-VG）方法高度依赖预处理的 3D 点云数据，将定位任务简化为“提议匹配（Proposal Matching）”的分类任务。这限制了模型在缺乏预扫描 3D 地图的真实环境中的应用。
*   **研究动机**：
    *   **解耦语义与几何**：利用 2D 视觉语言模型（VLM）强大的空间语义推理能力，同时利用确定性的多视图几何（Multi-view Geometry）来构建 3D 结构。
    *   **生成式范式**：将 3D-VG 从静态匹配转变为动态的“推理-行动-构建”过程，直接处理原始的 RGB-D 视频流。
    *   **克服语义脆弱性**：纯语义跟踪在视角剧烈变化或遮挡时容易失效，需要几何约束来增强鲁棒性。

### 2. 方法论
TAB 框架采用 **ReAct（推理-行动）** 范式，由一个具备“3D-VG 技能”蓝图的 VLM 智能体驱动：
*   **Think（思考）**：智能体根据当前视觉上下文和任务目标，规划下一步行动（如：解析查询、过滤帧、识别目标）。
*   **Act（行动）**：调用视觉工具库（如 Grounding DINO 进行检测、SAM3 进行分割、Query Analysis 进行语义解析）。
*   **Build（构建）—— 核心技术：语义锚定几何扩展（SAGE）**：
    1.  **语义时间扩展（STE）**：在参考帧附近进行双向语义跟踪，获取目标的初步 2D 掩码序列。
    2.  **质心提取**：通过逆投影（Inverse Projection）将 2D 像素提升至 3D 空间，计算目标的物理几何质心。
    3.  **多视图几何扩展（MGE）**：将 3D 质心重投影回视频的所有帧，通过可见性检查（FoV、深度验证、Z-buffer 遮挡检查）自动获取 VLM 语义跟踪遗漏的视角。
    4.  **3D 重建**：聚合所有获取的多视图掩码，通过相机内外参重建完整的 3D 点云，并计算轴向对齐包围盒（AABB）。

### 3. 实验设计
*   **数据集**：使用了 **ScanRefer** 和 **Nr3D**（均基于 ScanNet 室内场景）。
*   **Benchmark**：
    *   **ScanRefer**：评估指标为 Acc@0.25 和 Acc@0.5（预测框与真值的 IoU 阈值）。
    *   **Nr3D**：评估指标为 Top-1 选择准确率。
*   **对比方法**：
    *   **全监督方法**：如 3DVG-T, EDA, G3-LQ, LLaVA-3D, Video-3D-LLM 等。
    *   **零样本方法**：如 LLM-Grounder, ZSVG3D, SeeGround, SeqVLM, SPAZER 等。
*   **基准修正**：作者手动修正了 ScanRefer 和 Nr3D 中存在的指代歧义、类别错误和空间位置描述错误，建立了更严谨的评估集。

### 4. 资源与算力
*   **核心模型**：使用开源的 **Qwen3-VL-32B** 作为 VLM 智能体，**Grounding DINO** 用于粗筛，**SAM3** 用于实例分割。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或总训练/推理时长。但提到视频采样为每段 300 帧，且扩展过程限制在 32 帧以内，这暗示了该方法对实时性有一定要求，但计算开销主要集中在 VLM 的推理上。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流 3D-VG 数据集上进行了全面测试，涵盖了“唯一目标”、“多目标”、“简单/困难”、“视角依赖/独立”等多个子维度。
*   **消融实验**：针对 STE（语义时间扩展）和 MGE（多视图几何扩展）模块进行了详细的消融研究，验证了各组件对提升定位精度的贡献。
*   **充分性评价**：实验设计较为充分且客观。通过与全监督方法的对比，证明了零样本框架的竞争力；通过基准修正，排除了数据噪声对结果的影响，增强了实验的公平性。

### 6. 主要结论与发现
*   **性能领先**：TAB 在零样本设置下显著优于之前的 SOTA 方法（如 SPAZER），在 ScanRefer 上的 Acc@0.25 达到 71.2%。
*   **超越监督模型**：在不使用任何 3D 标注训练的情况下，TAB 的表现甚至超过了一些早期的全监督模型（如 3DVG-T）。
*   **几何的重要性**：消融实验显示，仅靠 2D 语义跟踪（无 MGE）会导致严重的视角覆盖不足，而引入几何扩展能大幅提升重建的完整性和定位精度。

### 7. 优点
*   **无需预处理点云**：直接作用于 RGB-D 视频流，更符合机器人导航等实际应用场景。
*   **鲁棒性强**：具备动态调整和容错机制（如：若几何扩展失败，可回退至语义跟踪结果）。
*   **生成式重建**：通过“构建”物体而非“选择”提议，实现了更精细的 3D 空间理解。
*   **开源生态**：完全基于开源模型构建，具有良好的可复现性。

### 8. 不足与局限
*   **依赖深度信息**：该方法需要准确的 RGB-D 流，在缺乏深度传感器或深度图质量极差的环境中可能失效。
*   **计算延迟**：作为一个多步骤的智能体框架，调用多个基础模型（VLM, SAM3, DINO）的串行过程可能导致推理延迟较高，难以满足极高帧率的实时需求。
*   **静态场景假设**：目前的几何投影基于静态场景假设，对于动态变化的物体或场景可能存在投影偏差。

（完）
