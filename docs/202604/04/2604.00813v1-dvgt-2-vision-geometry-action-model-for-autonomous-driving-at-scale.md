---
title: "DVGT-2: Vision-Geometry-Action Model for Autonomous Driving at Scale"
title_zh: DVGT-2：面向大规模自动驾驶的视觉-几何-动作模型
authors: "Sicheng Zuo, Zixun Xie, Wenzhao Zheng, Shaoqing Xu, Fang Li, Hanbing Li, Long Chen, Zhi-Xin Yang, Jiwen Lu"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00813v1"
tags: ["query:ff3dr"]
score: 9.0
evidence: 用于高效大规模3D重建的流式驾驶视觉几何
tldr: 本文提出DVGT-2，一种视觉-几何-动作（VGA）范式的端到端自动驾驶模型。针对现有几何重建方法计算开销大、无法在线规划的问题，DVGT-2采用流式Transformer架构，通过时间因果注意力和历史特征缓存实现高效的在线推理。该模型能同时输出稠密3D几何与行驶轨迹，在提升速度的同时，在多个数据集上实现了卓越的重建与规划性能，且具备跨相机配置的泛化能力。
selection_source: fresh_fetch
motivation: 现有VLA模型侧重语言描述而忽视了对决策至关重要的稠密3D几何信息，且传统几何重建方法难以满足在线规划的实时性需求。
method: 提出流式VGA模型，利用时间因果注意力、历史特征缓存及滑动窗口策略，实现稠密几何重建与轨迹规划的同步在线输出。
result: DVGT-2在保持高效推理的同时，在NAVSIM和nuScenes等基准测试中展现出优异的几何重建精度和跨场景规划泛化性。
conclusion: 稠密3D几何是比语言更有效的自动驾驶辅助任务，DVGT-2证明了VGA范式在端到端自动驾驶中的巨大潜力。
---

## 摘要
端到端自动驾驶已从基于稀疏感知的传统范式演变为视觉-语言-动作（VLA）模型，后者侧重于将学习语言描述作为辅助任务以促进规划。在本文中，我们提出了一种替代的视觉-几何-动作（VGA）范式，主张将稠密 3D 几何作为自动驾驶的关键线索。由于车辆在 3D 世界中运行，我们认为稠密 3D 几何为决策提供了最全面的信息。然而，大多数现有的几何重建方法（如 DVGT）依赖于计算开销巨大的多帧输入批处理，无法应用于在线规划。为了解决这一问题，我们引入了流式驾驶视觉几何 Transformer（DVGT-2），它以在线方式处理输入，并同时输出当前帧的稠密几何和轨迹规划。我们采用时间因果注意力并缓存历史特征以支持即时推理。为了进一步提高效率，我们提出了一种滑窗流式策略，并利用一定间隔内的历史缓存来避免重复计算。尽管速度更快，DVGT-2 在各种数据集上仍实现了卓越的几何重建性能。同一训练好的 DVGT-2 无需微调即可直接应用于不同相机配置下的规划，包括闭环 NAVSIM 和开环 nuScenes 基准测试。

## Abstract
End-to-end autonomous driving has evolved from the conventional paradigm based on sparse perception into vision-language-action (VLA) models, which focus on learning language descriptions as an auxiliary task to facilitate planning. In this paper, we propose an alternative Vision-Geometry-Action (VGA) paradigm that advocates dense 3D geometry as the critical cue for autonomous driving. As vehicles operate in a 3D world, we think dense 3D geometry provides the most comprehensive information for decision-making. However, most existing geometry reconstruction methods (e.g., DVGT) rely on computationally expensive batch processing of multi-frame inputs and cannot be applied to online planning. To address this, we introduce a streaming Driving Visual Geometry Transformer (DVGT-2), which processes inputs in an online manner and jointly outputs dense geometry and trajectory planning for the current frame. We employ temporal causal attention and cache historical features to support on-the-fly inference. To further enhance efficiency, we propose a sliding-window streaming strategy and use historical caches within a certain interval to avoid repetitive computations. Despite the faster speed, DVGT-2 achieves superior geometry reconstruction performance on various datasets. The same trained DVGT-2 can be directly applied to planning across diverse camera configurations without fine-tuning, including closed-loop NAVSIM and open-loop nuScenes benchmarks.

---

## 论文详细总结（自动生成）

以下是对论文《DVGT-2: Vision-Geometry-Action Model for Autonomous Driving at Scale》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：端到端自动驾驶正从传统的“稀疏感知”转向“视觉-语言-动作（VLA）”范式。然而，作者认为自然语言描述过于粗糙且具有歧义，难以捕捉精确的 3D 空间细节。
*   **核心问题**：现有的稠密几何重建模型（如 DVGT）通常采用批处理（Batch Processing）方式，计算复杂度随帧数增加呈平方级增长（$O(T^2)$），且在处理在线流式数据时存在严重的计算冗余，无法满足实时自动驾驶的需求。
*   **整体含义**：本文提出了 **VGA（Vision-Geometry-Action，视觉-几何-动作）** 范式，主张将稠密 3D 几何作为驾驶决策的核心线索，并开发了 **DVGT-2** 模型，实现了高效的在线流式几何重建与轨迹规划。

### 2. 方法论
*   **核心思想**：通过流式 Transformer 架构，利用历史特征缓存和滑动窗口策略，将计算复杂度降低至常数级，同时输出像素级对齐的 3D 点图、相对位姿和未来行驶轨迹。
*   **关键技术细节**：
    *   **流式架构**：采用时间因果注意力（Temporal Causal Attention），仅让当前帧查询历史缓存，避免重复计算。
    *   **滑动窗口策略**：维持一个固定大小（$W$）的特征缓存，实现 $O(W)$ 的常数级每帧计算开销，支持无限长度的推理。
    *   **相对坐标系解耦**：不再依赖第一帧作为全局参考，而是预测当前帧相对于前一帧的位姿（Ego-pose）以及当前坐标系下的局部几何。
    *   **位置编码**：使用 **MRoPE-I**（相对时间位置编码），确保缓存特征在时间轴上具有平移不变性。
    *   **预测头**：使用 DPT 头恢复点图，并借鉴 DiffusionDrive 采用基于锚点的截断扩散模型（Truncated Diffusion）来预测位姿和轨迹。

### 3. 实验设计
*   **数据集**：在五个主流驾驶数据集上进行大规模混合训练：**nuScenes、OpenScene、Waymo、KITTI 和 DDAD**。
*   **Benchmark（基准测试）**：
    *   **几何重建**：对比了 VGGT、MapAnything、DVGT、StreamVGGT 等 SOTA 模型，评估指标包括准确率（Acc）、完整性（Comp）、绝对相对误差（Abs Rel）等。
    *   **规划性能**：在闭环基准 **NAVSIM (v1 & v2)** 和开环基准 **nuScenes** 上进行评估。
*   **对比方法**：涵盖了传统的端到端模型（UniAD、VAD）、VLA 模型（AutoVLA、DriveVLA）以及最新的生成式模型（GenAD、GaussianAD）。

### 4. 资源与算力
*   **算力投入**：使用了 **64 张 H20 GPU**。
*   **训练时长**：整个训练过程（包含几何预训练、VGA 联合训练及特定任务微调）耗时约 **10 天**。
*   **模型规模**：模型参数量约为 **18 亿（1.8B）**，图像编码器采用 DINOv3 (ViT-L)。

### 5. 实验数量与充分性
*   **实验规模**：论文在 5 个不同分布的数据集上验证了几何重建能力，并在闭环和开环两个维度验证了规划能力，实验覆盖面极广。
*   **消融实验**：针对滑动窗口大小（Window Size）对精度和效率的影响做了详细分析。
*   **充分性评价**：实验设计非常充分且客观。通过跨数据集的零样本（Zero-shot）规划测试，证明了 VGA 范式具有极强的泛化能力，不依赖于特定的相机配置。

### 6. 主要结论与发现
*   **性能领先**：DVGT-2 在射线深度估计（Ray Depth Estimation）上达到了 SOTA 水平，且在 NAVSIM 闭环测试中刷新了纪录（PDMS 90.3, EPDMS 89.6）。
*   **效率飞跃**：相比于之前的流式模型，DVGT-2 实现了常数级的内存占用和推理延迟（约 **260ms/帧**），能够支持长时、实时的自动驾驶。
*   **VGA 优越性**：证明了稠密几何比稀疏感知或语言描述更能提供安全的规划保障，尤其是在降低碰撞率方面表现突出。

### 7. 优点
*   **高效性**：滑动窗口和缓存机制彻底解决了长序列处理的计算瓶颈。
*   **高精度**：像素级对齐的点图避免了占据栅格（Occupancy）带来的量化误差。
*   **泛化强**：同一套模型参数可直接适配不同的传感器布局和数据集，展现了“大模型”的底座潜力。
*   **标注友好**：相比于需要复杂人工标注的语义或语言标签，几何监督（深度）可以通过现有的基础模型或 LiDAR 自动生成。

### 8. 不足与局限
*   **全局漂移风险**：由于采用相对位姿累加来构建全局地图，在极长序列下不可避免地会出现轨迹漂移（Drift）。
*   **特定场景偏差**：在 KITTI 和 DDAD 数据集上规划误差较大，主因是这些数据集的行驶速度分布与主训练集（OpenScene）存在显著领域差异（Domain Gap）。
*   **算力门槛**：尽管推理高效，但 1.8B 参数量的训练仍需极高的算力资源支持。

（完）
