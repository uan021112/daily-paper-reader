---
title: "Think, Act, Build: An Agentic Framework with Vision Language Models for Zero-Shot 3D Visual Grounding"
title_zh: 思考、行动、构建：一种基于视觉语言模型的零样本 3D 视觉定位智能体框架
authors: "Haibo Wang, Zihao Lin, Zhiyang Xu, Lifu Huang"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00528v2"
tags: ["query:3dgen"]
score: 8.0
evidence: 用于视觉定位的生成式 2D 到 3D 重建范式
tldr: 针对3D视觉定位（3D-VG）中现有零样本方法过度依赖预处理点云的问题，本文提出TAB框架。该框架将3D-VG重构为生成式2D到3D重建过程，利用VLM智能体在原始RGB-D流中动态调用工具进行目标追踪。通过语义锚定几何扩展机制解决多视角覆盖不足，直接从2D视觉线索构建3D坐标。实验表明，该方法在ScanRefer和Nr3D上显著优于现有零样本方法，甚至超过部分全监督基准。
selection_source: fresh_fetch
motivation: 现有零样本3D-VG方法受限于静态工作流和预处理点云，无法充分利用2D VLM的语义能力和多视角几何的结构优势。
method: 提出TAB框架，通过VLM智能体动态调用视觉工具，并结合语义锚定几何扩展机制，将2D追踪结果聚合为3D表示。
result: 在ScanRefer和Nr3D基准上，该框架仅使用开源模型便显著超越了以往的零样本方法，并优于部分全监督模型。
conclusion: 本研究证明了通过解耦语义理解与几何重建，并利用智能体驱动的动态工作流，可以实现高效且精准的零样本3D视觉定位。
---

## 摘要
3D 视觉定位 (3D-VG) 旨在通过自然语言描述在 3D 场景中定位物体。虽然利用视觉语言模型 (VLM) 的最新进展已经探索了零样本的可能性，但它们通常受限于依赖预处理 3D 点云的静态工作流，本质上将定位退化为了候选框匹配 (proposal matching)。为了摆脱这种依赖，我们的核心动机是将任务解耦：利用 2D VLM 来解析复杂的空间语义，同时依靠确定性的多视图几何来实例化 3D 结构。基于这一见解，我们提出了“思考、行动、构建 (TAB)”，这是一个动态智能体框架，它将 3D-VG 任务重新表述为直接在原始 RGB-D 流上运行的生成式 2D 到 3D 重建范式。具体而言，在专门的 3D-VG 技能引导下，我们的 VLM 智能体动态调用视觉工具，在 2D 帧中跟踪并重建目标。至关重要的是，为了克服由严格的 VLM 语义跟踪引起的多视图覆盖不足问题，我们引入了语义锚定几何扩展 (Semantic-Anchored Geometric Expansion)，该机制首先在参考视频剪辑中锚定目标，然后利用多视图几何将其空间位置传播到未观察到的帧中。这使得智能体能够通过相机参数聚合这些多视图特征，从而“构建”目标的 3D 表示，将 2D 视觉线索直接映射到 3D 坐标。此外，为了确保评估的严谨性，我们识别了现有基准测试中存在的指代歧义和类别错误等缺陷，并手动修正了错误的查询。在 ScanRefer 和 Nr3D 上的大量实验表明，我们的框架完全依赖开源模型，其性能显著优于之前的零样本方法，甚至超过了全监督基线。

## Abstract
3D Visual Grounding (3D-VG) aims to localize objects in 3D scenes via natural language descriptions. While recent advancements leveraging Vision-Language Models (VLMs) have explored zero-shot possibilities, they typically suffer from a static workflow relying on preprocessed 3D point clouds, essentially degrading grounding into proposal matching. To bypass this reliance, our core motivation is to decouple the task: leveraging 2D VLMs to resolve complex spatial semantics, while relying on deterministic multi-view geometry to instantiate the 3D structure. Driven by this insight, we propose "Think, Act, Build (TAB)", a dynamic agentic framework that reformulates 3D-VG tasks as a generative 2D-to-3D reconstruction paradigm operating directly on raw RGB-D streams. Specifically, guided by a specialized 3D-VG skill, our VLM agent dynamically invokes visual tools to track and reconstruct the target across 2D frames. Crucially, to overcome the multi-view coverage deficit caused by strict VLM semantic tracking, we introduce the Semantic-Anchored Geometric Expansion, a mechanism that first anchors the target in a reference video clip and then leverages multi-view geometry to propagate its spatial location across unobserved frames. This enables the agent to "Build" the target's 3D representation by aggregating these multi-view features via camera parameters, directly mapping 2D visual cues to 3D coordinates. Furthermore, to ensure rigorous assessment, we identify flaws such as reference ambiguity and category errors in existing benchmarks and manually refine the incorrect queries. Extensive experiments on ScanRefer and Nr3D demonstrate that our framework, relying entirely on open-source models, significantly outperforms previous zero-shot methods and even surpasses fully supervised baselines.

---

## 论文详细总结（自动生成）

### 论文总结：Think, Act, Build (TAB) 框架

#### 1. 核心问题与整体含义（研究动机和背景）
3D 视觉定位（3D-VG）的核心挑战在于如何根据自然语言描述在复杂的 3D 空间中精准定位物体。
*   **现有问题：** 现有的零样本（Zero-shot）方法大多依赖预处理好的 3D 点云，将定位任务简化为“候选框匹配”（Proposal Matching），这限制了其在无预建地图环境中的应用。此外，仅依赖 2D 语义匹配的方法在视角剧烈变化或遮挡时表现脆弱。
*   **研究动机：** 论文提出将任务解耦——利用 2D 视觉语言模型（VLM）处理复杂的语义推理，利用确定性的多视图几何（Multi-view Geometry）处理 3D 结构重建。
*   **核心含义：** 提出了 **TAB (Think, Act, Build)** 框架，将 3D-VG 从静态匹配转变为动态的“语义推理+几何重建”过程，直接运行在原始 RGB-D 视频流上。

#### 2. 方法论：核心思想与技术细节
TAB 框架遵循 **ReAct** 范式，通过一个预定义的“3D-VG 技能蓝图”引导 VLM 智能体执行以下三个阶段：
*   **Think & Act（参考目标定位）：**
    *   **查询解析：** 将复杂指令分解为目标类别、属性、空间条件和场景特征。
    *   **粗到细过滤：** 使用检测器（Grounding DINO）筛选含目标类别的帧，再由 VLM 验证场景语义。
    *   **目标隔离：** 在参考帧中利用 SAM 进行分割并编号，由 VLM 选定唯一的 Target ID。
*   **Build（语义锚定几何扩展 - SAGE）：**
    *   **语义时间扩展（STE）：** 沿时间轴双向追踪目标，构建初步的 3D 点云并提取 **3D 几何质心**。
    *   **多视图几何扩展（MGE）：** 将 3D 质心反向投影到所有视频帧。通过 FoV 检查和 Z-buffer 遮挡检查，在语义特征不明显的帧中也能精准找回目标掩码。
*   **2D 到 3D 重建：**
    *   利用相机内参和外参，将所有获取的 2D 掩码像素投影回 3D 空间，通过离群点去除（SOR）和聚类（DBSCAN）生成最终的 3D 边界框。

#### 3. 实验设计
*   **数据集：** 使用了 **ScanRefer** 和 **Nr3D**（均基于 ScanNet 室内场景）。
*   **基准测试（Benchmark）：** 针对现有基准中的标注噪声（指代歧义、类别错误、空间位置错误），作者手动修正了测试集，建立了更严谨的评估标准。
*   **对比方法：**
    *   **零样本方法：** LLM-Grounder, ZSVG3D, SeeGround, SeqVLM, SPAZER 等。
    *   **全监督方法：** 3DVG-T, BUTD-DETR, EDA, LLaVA-3D, Video-3D-LLM 等。

#### 4. 资源与算力
*   **模型配置：** 核心智能体使用 **Qwen3-VL-32B**（开源模型），视觉工具包括 Grounding DINO 和 SAM3。
*   **算力说明：** 论文**未明确说明**具体的 GPU 型号、数量或推理总时长。由于该框架是零样本（无需训练），算力消耗主要集中在 VLM 的多轮推理和点云处理上。

#### 5. 实验数量与充分性
*   **实验规模：** 在两个主流数据集上进行了全面测试，涵盖了“唯一物体”、“多同类物体”、“简单/困难”、“视角依赖/独立”等多个子集。
*   **消融实验：** 针对 STE（语义扩展）和 MGE（几何扩展）模块进行了详细的消融研究，验证了每个组件对提升定位精度的贡献。
*   **客观性：** 通过手动修正基准测试中的错误，并对比了“不依赖点云”和“辅助点云”两种设置，实验设计较为客观、公平。

#### 6. 主要结论与发现
*   **性能卓越：** TAB 在不依赖预处理点云的情况下，在 ScanRefer 上的 Acc@0.25 达到 71.2%，显著超过了之前的零样本 SOTA（如 SPAZER 的 57.2%），甚至在某些指标上优于全监督模型。
*   **几何互补性：** 实验证明，单纯的语义追踪在视角变化时易丢失目标，而引入几何投影扩展（MGE）能大幅提升多视图覆盖率和重建质量。
*   **容错性：** 智能体能够通过“动态调整”策略（如放宽阈值）从中间步骤的失败中恢复。

#### 7. 优点
*   **解耦设计：** 巧妙地结合了 VLM 的强语义推理能力和多视图几何的物理确定性。
*   **无需 3D 先验：** 摆脱了对预处理点云的依赖，更符合具身智能在未知环境中的实际需求。
*   **基准优化：** 对 ScanRefer 和 Nr3D 的手动修正为后续研究提供了更高质量的评估参考。
*   **高度鲁棒：** 引入了 Z-buffer 检查和双向追踪，有效应对遮挡和传感器噪声。

#### 8. 不足与局限
*   **实时性挑战：** 作为一个基于智能体的多步迭代框架，调用大型 VLM 和多次图像处理工具会导致较高的推理延迟，难以满足实时机器人任务。
*   **深度图依赖：** 框架高度依赖高质量的深度图（RGB-D），在深度信息缺失或噪声极大的场景（如室外或透明物体）中表现可能受限。
*   **视频流长度限制：** 实验中对扩展帧数设置了上限（32帧），在极长视频或大场景漫游中的扩展效率尚待验证。

（完）
