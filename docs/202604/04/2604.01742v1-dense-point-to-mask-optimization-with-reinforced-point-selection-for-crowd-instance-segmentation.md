---
title: Dense Point-to-Mask Optimization with Reinforced Point Selection for Crowd Instance Segmentation
title_zh: 结合强化点选择的密集点到掩码优化用于人群实例分割
authors: "Hongru Chen, Jiyang Huang, Jia Wan, Antoni B. Chan"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01742v1"
tags: ["query:partseg"]
score: 9.0
evidence: 使用点标签的人群实例分割
tldr: 针对密集人群实例分割中点标注丰富但区域标注匮乏的问题，本文提出DPMO方法，结合SAM与NNEC约束将点标注转化为高质量掩码。同时，引入基于强化学习GRPO的RPS框架优化点选择。该研究不仅在多个主流数据集上达到SOTA，还通过掩码监督显著提升了人群计数精度，为密集场景下的精细化分析提供了新思路。
selection_source: fresh_fetch
motivation: 现有人群数据集多为点标注且缺乏准确区域标注，且现有大模型如SAM在密集场景下难以直接获得理想的分割结果。
method: 提出DPMO优化策略将点标注转化为掩码，并设计基于GRPO强化学习的RPS框架来从采样点中选择最佳预测结果。
result: 在ShanghaiTech、UCF-QNRF、JHU-CROWD++和NWPU-Crowd等多个权威数据集上均取得了最先进的实例分割性能。
conclusion: 通过掩码标注和强化学习优化，不仅提升了密集人群的分割精度，还显著增强了不同模型的人群计数能力。
---

## 摘要
人群实例分割是一项关键任务，在监控和交通等领域具有广泛的应用。目前，点标注在人群数据集中十分常见，而区域标注（如边界框）则较为稀缺且不够准确。通过分割获得的掩码有助于提高区域标注的准确性，并解决个体位置坐标与人群密度图之间的对应关系。然而，直接应用当前流行的大型基础模型（如 SAM）在密集人群中无法获得理想的结果。为此，我们首先提出了密集点到掩码优化（DPMO），该方法将 SAM 与最近邻排他圆（NNEC）约束相结合，从而根据点标注生成密集的实例分割。利用 DPMO 和人工修正，我们从传统人群数据集的现有点标注中获取了掩码标注。随后，为了预测密集人群中的实例分割，我们提出了一个采用群体相对策略优化（GRPO）训练的强化点选择（RPS）框架，该框架能从初始点预测的采样中选出最佳预测点。通过广泛的实验，我们在 ShanghaiTech、UCF-QNRF、JHU-CROWD++ 和 NWPU-Crowd 数据集上实现了最先进的人群实例分割性能。此外，我们还设计了由掩码监督的新损失函数，提升了不同模型的计数性能，证明了掩码标注在增强计数准确性方面的显著作用。

## Abstract
Crowd instance segmentation is a crucial task with a wide range of applications, including surveillance and transportation. Currently, point labels are common in crowd datasets, while region labels (e.g., boxes) are rare and inaccurate. The masks obtained through segmentation help to improve the accuracy of region labels and resolve the correspondence between individual location coordinates and crowd density maps. However, directly applying currently popular large foundation models such as SAM does not yield ideal results in dense crowds. To this end, we first propose Dense Point-to-Mask Optimization (DPMO), which integrates SAM with the Nearest Neighbor Exclusive Circle (NNEC) constraint to generate dense instance segmentation from point annotations. With DPMO and manual correction, we obtain mask annotations from the existing point annotations for traditional crowd datasets. Then, to predict instance segmentation in dense crowds, we propose a Reinforced Point Selection (RPS) framework trained with Group Relative Policy Optimization (GRPO), which selects the best predicted point from a sampling of the initial point prediction. Through extensive experiments, we achieve state-of-the-art crowd instance segmentation performance on ShanghaiTech, UCF-QNRF, JHU-CROWD++, and NWPU-Crowd datasets. Furthermore, we design new loss functions supervised by masks that boost counting performance across different models, demonstrating the significant role of mask annotations in enhancing counting accuracy.

---

## 论文详细总结（自动生成）

### 结合强化点选择的密集点到掩码优化用于人群实例分割

#### 1. 核心问题与研究动机
人群分析（如人群计数）传统上依赖于**点坐标**或**密度图**。然而，点坐标无法捕捉个体的形状和大小，而密度图则难以建立个体位置与像素聚合之间的直接对应关系。现有的通用分割模型（如 SAM）在极端密集或严重遮挡的场景下表现不佳，难以准确分离个体。
**核心问题**：如何在仅有点标注的情况下，生成高质量的人群实例分割掩码，并解决密集场景下的点预测不准导致分割失败的问题。

#### 2. 方法论
论文提出了 **DPMO-RPS** 框架，包含两个核心组件：
*   **密集点到掩码优化 (DPMO)**：
    *   利用 SAM 作为基础分割器，并引入**最近邻排他圆 (NNEC)** 约束。
    *   **核心思想**：为每个点标注计算一个最大不重叠圆半径，限制分割掩码的范围，确保每个掩码仅包含其对应的点，从而解决密集区域掩码重叠和边界模糊问题。
*   **强化点选择 (RPS)**：
    *   基于 **GRPO (群体相对策略优化)** 算法。
    *   **流程**：首先通过优化后的 PET 网络预测初始点，然后在初始点周围进行高斯采样生成一组候选点。利用 PSNet（点选择网络）对这组点进行评分，选出最适合作为 SAM 提示词（Prompt）的点，以获得最高的分割 IoU。
*   **掩码监督损失函数**：
    *   设计了针对密度图预测和点预测的掩码损失。对于点预测，利用掩码约束匹配过程，确保预测点落在对应的掩码区域内。

#### 3. 实验设计
*   **数据集**：在四个主流人群计数数据集上进行了验证：ShanghaiTech (Part A & B)、UCF-QNRF、JHU-CROWD++ 和 NWPU-Crowd。
*   **基准模型 (Benchmark)**：
    *   **分割对比**：SAM + 类别过滤、FastSAM、CrowdSAM。
    *   **计数对比**：CSRNet、P2PNet、PET、CLIP-EBC、ZIP 等。
*   **评价指标**：IoU、Precision、Recall、F1 分数（用于分割）；MAE、RMSE（用于计数）。

#### 4. 资源与算力
*   **训练细节**：使用了 Adam 优化器，学习率为 1e-5，权重衰减为 1e-4。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。仅提到对图像进行了缩放处理（如最大边长限制在 1536 或 2048 像素）以适应显存。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的实验，包括在 4 个数据集上的 SOTA 对比、针对 3 种不同架构计数模型的掩码监督增益实验。
*   **消融实验**：详细探讨了 DPMO 组件、RPS 组件、不同点预测模型、以及 NNEC 算法中半径参数的影响。
*   **充分性评价**：实验设计较为全面，涵盖了从分割精度到下游计数任务的迁移能力，对比方法包含了最新的大模型变体，实验结果具有较强的说服力和客观性。

#### 6. 主要结论与发现
*   **分割性能**：DPMO-RPS 在所有数据集上均显著优于现有的 SAM 变体，尤其在密集区域的 F1 分数提升巨大。
*   **计数增益**：通过 DPMO 生成的掩码进行监督，可以显著提升传统人群计数模型（如 ZIP, CLIP-EBC）的精度，证明了掩码比单纯的点标注含有更丰富的监督信息。
*   **强化学习有效性**：GRPO 训练的 RPS 能够有效纠正点预测的微小偏移，从而大幅提高分割的成功率。

#### 7. 亮点
*   **创新性引入 GRPO**：将大语言模型中常用的强化学习策略（GRPO）引入视觉任务，用于优化点提示词的选择。
*   **解决对应关系**：通过掩码建立了点坐标与密度图之间的确定性联系，为人群分析提供了更精细的表示。
*   **实用性强**：提出了一套从点标注自动/半自动生成高质量掩码标注的流程，降低了人群分割数据的获取成本。

#### 8. 不足与局限
*   **人工干预**：虽然 DPMO 能自动生成掩码，但为了获得最终的“真值”掩码，仍提到了需要“人工修正”（Manual Correction），这在大规模数据集上仍有一定人力成本。
*   **两阶段流程**：整个框架涉及点预测、采样、评分选择和最终分割，推理链路较长，可能影响实时处理性能。
*   **泛化风险**：对于非人头目标（如极小物体或非圆形物体）的泛化能力未在文中深入讨论。

（完）
