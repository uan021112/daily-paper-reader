---
title: "Diff3R: Feed-forward 3D Gaussian Splatting with Uncertainty-aware Differentiable Optimization"
title_zh: Diff3R：具有不确定性感知可微优化的前馈 3D 高斯泼溅
authors: "Yueh-Cheng Liu, Jozef Hladký, Matthias Nießner, Angela Dai"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01030v1"
tags: ["query:ff3dr"]
score: 10.0
evidence: 桥接前馈预测与测试时优化以进行3D重建
tldr: 本研究提出Diff3R框架，旨在结合前馈3D高斯泼溅（3DGS）的快速推理与逐场景优化的渲染质量。通过在训练中引入可微优化层，网络能预测出更适合测试时优化的初始参数。利用隐函数定理和矩阵无关的PCG求解器解决计算开销，并结合不确定性模型增强鲁棒性。该方法可无缝集成至现有架构，显著提升稀疏视图下的重建效果。
selection_source: fresh_fetch
motivation: 旨在解决前馈3DGS模型在稀疏视图下精度不足，以及逐场景优化计算成本高昂且易过拟合的问题。
method: 提出一种包含可微优化层的框架，利用隐函数定理计算梯度，并引入数据驱动的不确定性模型来引导测试时优化。
result: 该方法能有效缓解欠约束区域的过拟合，提高对输入离群值的鲁棒性，并在多种前馈3DGS架构上实现性能提升。
conclusion: Diff3R成功桥接了前馈预测与测试时优化，为高质量、高效率的3D场景重建提供了一种通用的优化方案。
---

## 摘要
3D 高斯泼溅 (3DGS) 的最新进展呈现出两个主要方向：前馈模型在稀疏视图设置下提供快速推理，而逐场景优化可产生高质量渲染，但计算成本高昂。为了结合两者的优点，我们引入了 Diff3R，这是一个显式桥接前馈预测与测试时优化的新颖框架。通过将可微 3DGS 优化层直接整合到训练循环中，我们的网络学习预测测试时优化的最佳初始化，而非传统的零样本结果。为了克服通过优化步骤进行反向传播的计算成本，我们提出通过隐函数定理以及专为 3DGS 优化定制的可扩展、无矩阵 PCG 求解器来计算梯度。此外，我们通过自适应地控制优化过程中允许参数变化的幅度，将数据驱动的不确定性模型整合到优化过程中。这种方法有效地缓解了欠约束区域的过拟合问题，并增强了对输入离群值的鲁棒性。由于我们提出的优化层与模型无关，我们展示了它可以无缝集成到现有的前馈 3DGS 架构中（包括已知位姿和无位姿的方法），从而为测试时优化提供改进。

## Abstract
Recent advances in 3D Gaussian Splatting (3DGS) present two main directions: feed-forward models offer fast inference in sparse-view settings, while per-scene optimization yields high-quality renderings but is computationally expensive. To combine the benefits of both, we introduce Diff3R, a novel framework that explicitly bridges feed-forward prediction and test-time optimization. By incorporating a differentiable 3DGS optimization layer directly into the training loop, our network learns to predict an optimal initialization for test-time optimization rather than a conventional zero-shot result. To overcome the computational cost of backpropagating through the optimization steps, we propose computing gradients via the Implicit Function Theorem and a scalable, matrix-free PCG solver tailored for 3DGS optimization. Additionally, we incorporate a data-driven uncertainty model into the optimization process by adaptively controlling how much the parameters are allowed to change during optimization. This approach effectively mitigates overfitting in under-constrained regions and increases robustness against input outliers. Since our proposed optimization layer is model-agnostic, we show that it can be seamlessly integrated into existing feed-forward 3DGS architectures for both pose-given and pose-free methods, providing improvements for test-time optimization.

---

## 论文详细总结（自动生成）

### Diff3R：具有不确定性感知可微优化的前馈 3D 高斯泼溅论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
论文针对 **3D 高斯泼溅 (3DGS)** 领域中“前馈模型”与“逐场景优化”之间的鸿沟展开研究。
*   **研究动机**：前馈模型（Feed-forward）推理快但缺乏细节，且在稀疏视图下进行测试时优化（TTO）时，极易陷入**灾难性过拟合**（即为了拟合输入视图而破坏几何结构，产生浮点伪影）。
*   **核心问题**：如何训练一个前馈网络，使其预测的不是最终结果，而是**最适合后续优化的初始状态**，同时在优化过程中自动识别并保护不确定的区域，防止过拟合。

#### 2. 方法论：核心思想与关键技术
Diff3R 引入了一个**双层优化（Bilevel Optimization）**框架，将测试时优化过程嵌入到训练循环中：
*   **双层优化架构**：
    *   **内层（Inner Loop）**：执行简短的测试时优化，利用光度损失和近端正则化项（Proximal Term）调整高斯参数。
    *   **外层（Outer Loop）**：根据优化后高斯在**新视角（Novel View）**上的渲染质量来更新网络权重。
*   **隐式梯度计算（Implicit Gradients）**：
    *   为了避免显式展开优化步骤带来的巨大内存开销，利用**隐函数定理（Implicit Function Theorem, IFT）**在收敛点处直接计算解析梯度。
    *   使用 **Gauss-Newton 近似** Hessian 矩阵，并采用**无矩阵预处理共轭梯度法（PCG）**求解大规模线性方程组，从而支持数百万个高斯参数的端到端训练。
*   **不确定性感知正则化**：
    *   网络不仅预测高斯参数 $\Theta_0$，还预测每个参数的**不确定性权重 $\Lambda$**。
    *   在优化过程中，$\Lambda$ 作为自适应惩罚项：对于置信度高的区域，严格限制其偏离初始值；对于不确定区域，允许其自由优化以捕捉细节。

#### 3. 实验设计
*   **数据集**：
    *   **已知位姿（Pose-given）**：在 RealEstate10K (RE10K) 上训练和评估。
    *   **无位姿（Pose-free）**：在 RE10K、ScanNet、ScanNet++、DL3DV、Hypersim 和 ArkitScenes 的混合数据集上微调，并在 RE10K 和 ScanNet++ 验证集上评估。
*   **Benchmark 与对比方法**：
    *   **已知位姿对比**：PixelSplat, MVSplat, DepthSplat。
    *   **无位姿对比**：AnySplat, Depth Anything v3 (DA3)。
*   **评估指标**：PSNR, SSIM, LPIPS（分别在优化前和优化后进行测量）。

#### 4. 资源与算力
*   **训练开销**：论文明确指出，由于引入了双层优化，训练时间比原始前馈模型增加了 **4 到 5 倍**（前向传播中的梯度下降步骤占用了约 65% 的额外时间）。
*   **硬件支持**：实验得到了德国 Gauss 超算中心（JUWELS 集群）和西班牙 MareNostrum 5 超算的支持。
*   **测试开销**：推理阶段（前馈预测）的时间没有增加，仅在需要执行 TTO 时产生优化耗时。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从已知位姿到完全无位姿的多种复杂场景，并在室内（ScanNet++）和室外/通用（RE10K）场景下进行了验证。
*   **消融实验**：针对“近端正则化项”和“可学习不确定性权重 $\Lambda$”分别做了对比实验，证明了两者对提升性能和抑制过拟合的必要性。
*   **客观性**：通过可视化学习到的不确定性图（图 5），直观展示了模型如何自动识别非重叠区域并施加更强的约束，实验逻辑严密且对比公平。

#### 6. 主要结论与发现
*   **优化感知初始化更优**：训练网络去预测“易于优化的起点”比直接预测“最终结果”能获得更高的渲染上限。
*   **性能提升显著**：在 RE10K 上比基准提升了约 0.27 dB PSNR，在 ScanNet++ DSLR 场景下比 DA3 提升了 0.35 dB。
*   **鲁棒性增强**：通过自适应正则化，模型在面对输入视图中的曝光差异或离群值时表现出更强的鲁棒性，有效减少了伪影。

#### 7. 优点（亮点）
*   **模型无关性（Model-agnostic）**：该优化层可以无缝集成到任何基于高斯泼溅的前馈架构中。
*   **数学严谨性**：利用 IFT 和 PCG 解决了 3DGS 参数量巨大导致的二阶优化不可行问题，实现了内存高效的端到端训练。
*   **自动学习不确定性**：无需真值标签，仅通过新视角监督即可学习到合理的参数级不确定性分布。

#### 8. 不足与局限
*   **训练成本高**：4-5 倍的训练时长增加限制了其在大规模快速迭代中的应用。
*   **对初始化的依赖**：虽然旨在优化初始化，但如果前馈网络生成的初始几何结构过于糟糕，后续优化仍可能失败。
*   **实时性权衡**：虽然前馈是实时的，但为了达到最佳效果，仍需进行数十步到数百步的测试时优化，这在极高性能要求的实时场景中可能存在瓶颈。

（完）
