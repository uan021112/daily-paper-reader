---
title: "WildSplatter: Feed-forward 3D Gaussian Splatting with Appearance Control from Unconstrained Images"
title_zh: WildSplatter：基于非约束图像且具备外观控制的前馈 3D 高斯泼溅
authors: "Yuki Fujimura, Takahiro Kushida, Kazuya Kitano, Takuya Funatomi, Yasuhiro Mukaigawa"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.21182v1"
tags: ["query:ff3dr"]
score: 10.0
evidence: 用于从图像重建的前馈3D高斯泼溅模型
tldr: WildSplatter 是一种前馈式 3D 高斯泼溅模型，旨在处理相机参数未知且光照多变的非约束图像。它通过联合学习 3D 高斯和外观嵌入，实现了对光照变化的灵活调制，能在不到一秒内从稀疏视图完成重建。该方法解决了传统 3DGS 依赖迭代优化和一致光照的局限，在真实世界数据集上取得了领先的性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.21182v1/fig-001.webp\", \"caption\": \"Table 1: Quantitative comparison for novel-view synthesis on the NeRF-OSR dataset [22]. We report PSNR (↑) and LPIPS (↓). The best results are highlighted in red, and the second-best in orange.\", \"page\": 6, \"index\": 1, \"width\": 826, \"height\": 419}, {\"url\": \"assets/figures/arxiv/2604.21182v1/fig-002.webp\", \"caption\": \"Figure 8: From left to right: context images, estimated opacity maps for the context views, target image, rendered target image, and the visibility mask at the target view. The model suppresses transient objects by assigning low opacity values.\", \"page\": 15, \"index\": 2, \"width\": 650, \"height\": 927}, {\"url\": \"assets/figures/arxiv/2604.21182v1/fig-003.webp\", \"caption\": \"Figure 4: Qualitative comparison for novel-view synthesis on the NeRF-OSR dataset [22].\", \"page\": 7, \"index\": 3, \"width\": 706, \"height\": 816}, {\"url\": \"assets/figures/arxiv/2604.21182v1/fig-004.webp\", \"caption\": \"Figure 5: Appearance interpolation. The first and third rows show rendered images from novel views with fixed appearance embeddings from the two input views, respectively. The second row shows rendered images with linearly interpolated appearance embeddings.\", \"page\": 8, \"index\": 4, \"width\": 748, \"height\": 224}, {\"url\": \"assets/figures/arxiv/2604.21182v1/fig-005.webp\", \"caption\": \"Figure 2: Overview of the proposed method. Given sparse-view context images captured under inconsistent lighting, the model first estimates colorless 3D Gaussian geometry and local scene features. During training, target images are additionally used to extract global appearance embeddings, which are combined with local features to predict Gaussian colors. This design enables disentangled modeling of geometry and appearance for robust rendering under varying lighting conditions.\", \"page\": 4, \"index\": 5, \"width\": 811, \"height\": 318}, {\"url\": \"assets/figures/arxiv/2604.21182v1/fig-006.webp\", \"caption\": \"Figure 6: (a) t-SNE visualization [28] of learned appearance embeddings, where each point corresponds to an image and colors indicate K-means clusters. (b) Representative images for each cluster, showing that images within the same cluster share similar appearance characteristics.\", \"page\": 9, \"index\": 6, \"width\": 656, \"height\": 241}, {\"url\": \"assets/figures/arxiv/2604.21182v1/fig-007.webp\", \"caption\": \"Figure 3: Examples of training samples. The first two rows show context images captured under different lighting conditions, while the third row shows the corresponding target images. The last row shows visibility masks computed by warping depth maps from the target views to the context views.\", \"page\": 5, \"index\": 7, \"width\": 826, \"height\": 331}]"
motivation: 传统 3DGS 重建需要已知的相机位姿、一致的光照以及耗时的迭代优化，限制了其在非约束现实场景中的应用。
method: 提出一种前馈架构，通过输入图像联合预测 3D 高斯参数和外观嵌入，以实现对场景颜色和光照的动态控制。
result: 实验表明该方法能在秒级时间内完成高质量重建，并在处理光照多变的真实图像时优于现有的无位姿 3DGS 技术。
conclusion: WildSplatter 为从非约束图像集合中进行快速、可控的 3D 场景重建提供了一种高效且鲁棒的解决方案。
---

## 摘要
我们提出了 WildSplatter，这是一种针对具有未知相机参数和多变光照条件的非约束图像的前馈 3D 高斯泼溅（3DGS）模型。3DGS 是一种有效的场景表示，能够实现高质量、实时的渲染；然而，它通常需要迭代优化，以及在一致光照和已知相机参数下捕获的多视图图像。WildSplatter 在非约束照片集上进行训练，并联合学习以输入图像为条件的 3D 高斯和外观嵌入。这种设计能够灵活地调节高斯颜色，以表示光照和外观的显著变化。我们的方法可以在不到一秒的时间内从稀疏的输入视图中重建 3D 高斯，同时还能在不同的光照条件下实现外观控制。实验结果表明，在具有多变照明的挑战性真实世界数据集上，我们的方法优于现有的无位姿 3DGS 方法。

## Abstract
We propose WildSplatter, a feed-forward 3D Gaussian Splatting (3DGS) model for unconstrained images with unknown camera parameters and varying lighting conditions. 3DGS is an effective scene representation that enables high-quality, real-time rendering; however, it typically requires iterative optimization and multi-view images captured under consistent lighting with known camera parameters. WildSplatter is trained on unconstrained photo collections and jointly learns 3D Gaussians and appearance embeddings conditioned on input images. This design enables flexible modulation of Gaussian colors to represent significant variations in lighting and appearance. Our method reconstructs 3D Gaussians from sparse input views in under one second, while also enabling appearance control under diverse lighting conditions. Experimental results demonstrate that our approach outperforms existing pose-free 3DGS methods on challenging real-world datasets with varying illumination.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《WildSplatter: Feed-forward 3D Gaussian Splatting with Appearance Control from Unconstrained Images》** 的结构化深度总结：

### 1. 核心问题与整体含义（研究动机与背景）
传统的 3D 高斯泼溅（3DGS）技术虽然在实时渲染和重建质量上表现出色，但存在三个核心局限：
*   **依赖迭代优化：** 每个场景都需要数分钟甚至数小时的训练。
*   **光照一致性假设：** 要求输入图像在相同光照下拍摄，难以处理互联网照片集（非约束图像）。
*   **已知相机参数：** 通常需要预先通过 COLMAP 等工具计算位姿。

**WildSplatter 的目标**是开发一种**前馈式（Feed-forward）**模型，能够从**相机位姿未知**且**光照条件多变**的稀疏图像（如 2-4 张照片）中，在**一秒内**重建出具有外观控制能力的 3D 场景。

### 2. 方法论：核心思想与技术细节
WildSplatter 的核心思想是**几何与外观的解耦建模**。

*   **架构基础：** 采用 **Depth Anything 3** 作为骨干网络，利用其强大的几何先验。模型由 Vision Transformer (ViT) 编码器和多个 DPT 解码头组成。
*   **几何估计：** 
    *   模型首先从上下文图像（Context Images）中提取特征。
    *   通过 `dual-DPT` 头预测深度图和射线图，计算高斯中心（$\mu$）。
    *   通过 `DPT` 头预测不透明度（$\alpha$）、旋转（$r$）和缩放（$s$）。这些几何属性被假设在不同光照下保持不变。
*   **外观控制（核心创新）：**
    *   **全局外观嵌入（Appearance Embedding）：** 引入一个可学习的外观 Token，通过浅层 Transformer 和 MLP 从目标图像中提取低维向量 $e_j$（默认 32 维）。
    *   **颜色调制：** 将全局外观嵌入与局部场景特征图拼接，通过卷积层预测球谐函数（SH）系数。这使得模型能根据不同的嵌入向量动态调整高斯点的颜色。
*   **训练策略：** 在 **MegaScenes** 大规模数据集上训练，引入**可见性掩码（Visibility Mask）**来处理多视图不一致性（如行人等瞬态物体）和遮挡问题。

### 3. 实验设计
*   **数据集：** 
    *   **训练集：** MegaScenes（包含 3,634 个场景的 14,816 组视图，源自互联网地标照片）。
    *   **评估集：** **NeRF-OSR**（专门用于户外场景重光照的数据集，包含多变光照的真实序列）。
*   **Benchmark 与对比方法：**
    *   **优化类方法：** WildGaussians（带外观建模的 3DGS 优化版）。
    *   **前馈类方法：** SPFSplat、AnySplat、Depth Anything 3。
*   **评估指标：** PSNR（峰值信噪比）和 LPIPS（感知相似度）。

### 4. 资源与算力
*   **训练资源：** 使用了 **4 张 NVIDIA A100 GPU**。
*   **训练时长：** 训练 15,000 次迭代，耗时约 **2 天**。
*   **推理效率：** 在单张 **NVIDIA RTX 6000 Ada GPU** 上，处理两张输入图像仅需 **0.375 秒**，远快于优化类方法（约 1.5 分钟）。

### 5. 实验数量与充分性
*   **实验规模：** 论文在 NeRF-OSR 的 6 个代表性场景（europa, lk2 等）上进行了 2、3、4 张输入视图的对比测试。
*   **消融实验：** 针对外观嵌入的维度（32 vs 256）进行了对比，证明了低维空间足以捕捉全局光照变化。
*   **定性分析：** 提供了外观插值（Appearance Interpolation）、t-SNE 嵌入空间可视化以及**跨数据集外观控制**（将 A 场景的光照应用到 B 场景的几何上）的实验。
*   **评价：** 实验设计较为充分，涵盖了定量指标、定性视觉效果、运行效率及泛化能力测试，对比基准选择合理。

### 6. 主要结论与发现
*   **性能领先：** WildSplatter 在处理非约束图像的新视角合成任务上，PSNR 和 LPIPS 指标均显著优于现有的无位姿前馈模型。
*   **解耦有效：** 证明了几何与外观的解耦能够让模型在光照剧烈变化时依然保持稳定的几何重建。
*   **泛化能力：** 学习到的外观嵌入空间具有良好的平滑性，支持外观插值和跨场景的光照迁移。
*   **瞬态物体抑制：** 模型在训练过程中能够自动为行人等瞬态物体分配低不透明度，从而实现“去噪”重建。

### 7. 优点（亮点）
*   **极速重建：** 实现了亚秒级（<1s）的 3D 重建，极大地扩展了 3DGS 的应用场景。
*   **无需位姿：** 摆脱了对相机内参和外参的依赖，直接从原始图像输入。
*   **灵活控制：** 提供了显式的外观控制手段，不仅能合成视角，还能改变光照。
*   **鲁棒性：** 能够处理互联网照片中常见的行人干扰和光照不一致问题。

### 8. 不足与局限
*   **全局建模限制：** 使用单一全局嵌入向量可能导致局部颜色漂移，难以精细刻画复杂的局部阴影（Shadows）或高光。
*   **分辨率限制：** 实验主要在 504x504 分辨率下进行，对于极高分辨率的细节还原可能存在瓶颈。
*   **几何依赖：** 几何质量高度依赖于预训练的深度估计模型（Depth Anything 3），若基础模型在某些极端场景失效，重建质量会受损。

（完）
