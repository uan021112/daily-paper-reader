---
title: "OccDirector: Language-Guided Behavior and Interaction Generation in 4D Occupancy Space"
title_zh: OccDirector：4D 占用空间中语言引导的行为与交互生成
authors: "Zhuding Liang, Tianyi Yan, Dubing Chen, Jiasen Zheng, Huan Zheng, Cheng-zhong Xu, Yida Wang, Kun Zhan, Jianbing Shen"
date: 2026-04-24
pdf: "https://arxiv.org/pdf/2604.22240v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 仅以自然语言为条件生成4D占据动态
tldr: 本文提出OccDirector，一个仅通过自然语言引导4D占用空间生成的框架，旨在解决自动驾驶模拟中复杂多智能体交互难以通过简单文本或几何条件描述的问题。该框架采用VLM驱动的时空MMDiT架构，结合历史前缀锚定策略确保长时交互一致性。此外，研究者发布了包含8.5万条多级标注的OccInteract数据集。实验表明，OccDirector在生成质量和指令遵循方面表现卓越，实现了从外观合成到语言驱动行为编排的范式转变。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.22240v1/fig-001.webp\", \"caption\": \"Fig. 1: Script-driven behavior and interaction generation with OccDirector.\", \"page\": 2, \"index\": 1, \"width\": 718, \"height\": 325}, {\"url\": \"assets/figures/arxiv/2604.22240v1/fig-002.webp\", \"caption\": \"Table 2: Automated Data Validation. Mean rubric scores (1-5) assessing the quality of the generated dataset.\", \"page\": 10, \"index\": 2, \"width\": 725, \"height\": 214}, {\"url\": \"assets/figures/arxiv/2604.22240v1/fig-003.webp\", \"caption\": \"Fig. 5: Qualitative comparison of text-to-occupancy alignment. We evaluate adherence to fine-grained instructions: (Left) Negation and cardinality; (Middle) Dynamic attribute binding; (Right) Complex “cut-in” trajectory.\", \"page\": 14, \"index\": 3, \"width\": 690, \"height\": 317}, {\"url\": \"assets/figures/arxiv/2604.22240v1/fig-004.webp\", \"caption\": \"Fig. 4: Long-horizon narrative generation. We visualize multi-stage rollouts for a stop-and-go scenario (top) and an emergency braking event (bottom).\", \"page\": 13, \"index\": 4, \"width\": 690, \"height\": 390}, {\"url\": \"assets/figures/arxiv/2604.22240v1/fig-005.webp\", \"caption\": \"Fig. 2: Model Architecture of OccDirector. (a) Scalable 4D Generation Backbone: A Spatio-Temporal MMDiT processes text and occupancy tokens via dual streams, utilizing separated spatial and temporal attention. (b) Semantic Extraction: A frozen VLM and a Token Refiner bridge the semantic-spatial gap. (c) Interaction-Consistent Forecasting: The history-anchoring strategy ensures the generated future strictly adheres to the context during both training and sampling.\", \"page\": 5, \"index\": 5, \"width\": 690, \"height\": 381}]"
motivation: 现有4D占用生成模型依赖刚性几何先验，无法有效处理复杂的自然语言指令和多智能体交互。
method: 采用VLM驱动的时空MMDiT架构，并引入历史前缀锚定策略，实现从语言脚本到物理一致体素动态的直接映射。
result: 在新提出的OccInteract-85k数据集上，该方法在生成质量和指令遵循能力上均达到了当前最先进水平。
conclusion: OccDirector成功实现了由自然语言驱动的4D行为编排，为自动驾驶世界模型的交互式模拟提供了新路径。
---

## 摘要
生成式世界模型日益依赖 4D 占用（4D occupancy）来实现逼真的自动驾驶模拟。然而，现有的生成框架依赖于僵化的几何条件（如显式轨迹）或简单的属性级文本，无法编排复杂的、序列化的多智能体交互。为了弥补这一语义-时空鸿沟，我们提出了 OccDirector，这是一个仅以自然语言为条件生成 4D 占用动态的开创性框架。作为“场景导演”，OccDirector 在不需要几何先验的情况下，将自然语言脚本映射为符合物理规律的体素动态。在技术上，它采用了一种由视觉语言模型（VLM）驱动的时空 MMDiT，并配备了历史前缀锚定策略，以确保长时程交互的一致性。此外，我们推出了 OccInteract-85k，这是一个独特的数据集，带有从静态布局到复杂多智能体行为的多级语言指令标注，并提供了一个全新的基于 VLM 的评估基准。大量实验表明，OccDirector 实现了最先进的生成质量和前所未有的指令遵循能力，成功地将范式从外观合成转向了语言驱动的行为编排。

## Abstract
Generative world models increasingly rely on 4D occupancy for realistic autonomous driving simulation. However, existing generation frameworks depend on rigid geometric conditions (e.g., explicit trajectories) or simplistic attribute-level text, failing to orchestrate complex, sequential multi-agent interactions. To address this semantic-spatiotemporal gap, we propose OccDirector, a pioneering framework that generates 4D occupancy dynamics conditioned solely on natural language. Operating as a ``scenario director'', OccDirector maps natural language scripts into physically plausible voxel dynamics without requiring geometric priors. Technically, it employs a VLM-driven Spatio-Temporal MMDiT equipped with a history-prefix anchoring strategy to ensure long-horizon interaction consistency. Furthermore, we introduce OccInteract-85k, a novel dataset uniquely annotated with multi-level language instructions: ranging from static layouts to intricate multi-agent behaviors, alongside a novel VLM-based evaluation benchmark. Extensive experiments demonstrate that OccDirector achieves state-of-the-art generation quality and unprecedented instruction-following capabilities, successfully shifting the paradigm from appearance synthesis to language-driven behavior orchestration.

---

## 论文详细总结（自动生成）

这是一份关于论文《OccDirector: Language-Guided Behavior and Interaction Generation in 4D Occupancy Space》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的自动驾驶生成式世界模型（如 4D 占用生成）高度依赖于**刚性的几何条件**（如预定义的轨迹、HD 地图或 BEV 布局）。这种“轨迹工程”方式不仅繁琐，且难以表达高层语义意图（如“紧急避让”或“超车”）。
*   **语义-时空鸿沟**：自然语言具有逻辑性和时序性，而 4D 占用（体素流）是底层的几何表示。现有的文本编码器（如 CLIP）往往表现得像“词袋”，缺乏解析复杂交互所需的组合推理能力，导致生成的场景常出现物理冲突或逻辑断裂。
*   **研究背景**：为了实现更直观、可扩展的自动驾驶仿真，需要一种能像“导演”一样理解脚本并直接生成符合物理规律的 4D 动态场景的框架。

### 2. 方法论：核心思想与关键技术
OccDirector 的核心思想是将自然语言脚本直接映射为 4D 占用空间中的体素演化，无需任何几何先验。
*   **VLM 驱动的语义提取**：放弃传统的 CLIP，采用冻结的视觉语言模型（VLM，如 Qwen3-VL）作为文本编码器，以捕获复杂的程序化语义。通过一个双向 Transformer（Token Refiner）进一步对齐文本特征与空间布局。
*   **时空 MMDiT 架构**：
    *   采用双流 Transformer 结构，允许文本和占用 Token 进行深度交互。
    *   **时空分离注意力（STSA）**：将计算分解为“帧内 3D 空间联合注意力”和“跨帧时间自注意力”，解决了 4D 数据立方级计算复杂度的扩展性问题。
*   **历史前缀锚定（History-Prefix Anchoring）**：
    *   在训练和推理中引入随机 Token 替换策略。
    *   **核心逻辑**：在生成未来帧时，强制将历史帧替换为真实的（或已生成的）干净 Token。这确保了长时程生成的一致性，防止交互漂移或碰撞。
*   **流匹配（Flow Matching）**：基于连续归一化流（CNF）框架进行训练，比传统的扩散模型更高效且稳定。

### 3. 实验设计
*   **数据集：OccInteract-85k**（论文自研）：
    *   包含 8.5 万个实例，涵盖静态布局（Static）、环境交互（Env）和多智能体交互（Agent）三个层级。
    *   数据源自真实日志（UniOcc）、大规模仿真（CarlaSC）和安全关键场景合成（TTSG）。
*   **评估基准（Benchmark）**：
    *   **视觉质量**：IS、FID、KID（单帧）；FVD、Precision、Recall（视频级）。
    *   **指令遵循**：提出一种基于 VLM（如 Gemini-3.0-pro）的自动评分机制，从场景完整性、结构正确性和语义对齐三个维度打分。
*   **对比方法**：
    *   几何引导方法：DOME、COME、DynamicCity。
    *   文本引导基准：将 DynamicCity 的控制端替换为 CLIP 或 T5 编码器作为 Baseline。

### 4. 资源与算力
*   **硬件**：实验中提到了在 **NVIDIA A100 (80GB)** 上进行推理资源测试。
*   **训练细节**：
    *   Batch Size 为 32，学习率为 1e-4。
    *   训练迭代次数为 **100k 次**。
    *   推理时使用 Euler 解算器，采样步数为 20 步。
*   **效率**：由于采用了 STSA 机制，显存占用随帧数线性增长，且对文本长度不敏感（L 从 77 增加到 2048，显存增加小于 0.1%）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了大规模的定量对比（表 3、表 4）。
    *   针对架构组件（STSA、Token Refiner）、历史锚定比例、CFG 缩放等进行了详尽的消融实验。
    *   引入了跨 VLM（GPT-5.2, Gemini, Qwen）的鲁棒性测试和 10 人参与的人类用户研究。
*   **充分性与客观性**：实验设计较为全面，通过引入多种 VLM 评委和人类研究，有效降低了单一自动指标可能带来的偏差。对比实验涵盖了当前最先进的 4D 占用生成模型，具有较高的客观性。

### 6. 主要结论与发现
*   **性能领先**：OccDirector 在视频生成质量（FVD 275）上显著优于文本适配的基线模型（FVD 388），提升约 29.1%。
*   **语义理解力**：相比 CLIP/T5，VLM 驱动的架构能更好地处理否定词（如“没有其他车辆”）、属性绑定和复杂的侧向切入（Cut-in）逻辑。
*   **一致性**：历史前缀锚定策略是实现长时程、逻辑自洽的多阶段叙事生成的关键。

### 7. 优点（亮点）
*   **范式创新**：首次实现了纯文本驱动的 4D 占用生成，摆脱了对轨迹和地图的依赖。
*   **数据贡献**：构建了首个专门针对自动驾驶交互语义的大规模 4D 占用数据集 OccInteract-85k。
*   **架构设计**：STSA 机制在保持高性能的同时，极大地提升了处理长视频和长文本的扩展性。
*   **评估创新**：建立了一套完善的基于 VLM 的自动评估协议，解决了生成式世界模型难以量化评价的痛点。

### 8. 不足与局限
*   **数据伪影**：受限于原始传感器数据，生成的场景可能继承盲区或 ego 车辆体素缺失等缺陷。
*   **长时程极限**：虽然有锚定策略，但在极长序列（如超过 8 秒）生成中，物理一致性的累积误差仍是一个挑战。
*   **语义粒度**：目前仅支持 11 类语义，尚未涵盖车辆转向灯、刹车灯或行人手势等更细微的交互信号。
*   **VLM 偏见**：自动评估依赖于特定 VLM 的判断，不同模型之间的评分标准存在系统性偏移。

（完）
