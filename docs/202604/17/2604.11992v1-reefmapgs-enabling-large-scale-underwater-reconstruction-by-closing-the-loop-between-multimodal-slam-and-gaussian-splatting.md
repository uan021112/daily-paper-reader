---
title: "ReefMapGS: Enabling Large-Scale Underwater Reconstruction by Closing the Loop Between Multimodal SLAM and Gaussian Splatting"
title_zh: ReefMapGS：通过多模态 SLAM 与高斯泼溅之间的闭环实现大规模水下重建
authors: "Daniel Yang, Jungseok Hong, John J. Leonard, Yogesh Girdhar"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11992v1"
tags: ["query:ff3dr"]
score: 9.0
evidence: 利用高斯泼溅和SLAM实现大规模水下重建
tldr: 针对水下机器人大规模场景重建中3D高斯泼溅（3DGS）依赖高精度位姿且传统SfM计算开销大的问题，本文提出ReefMapGS框架。该框架融合多模态传感器SLAM与3DGS增量式重建，通过交替进行局部跟踪与场景优化，并将精炼位姿反馈至全局位姿图，实现了无需COLMAP的高效建模。在长达700米的水下珊瑚礁测绘中，该方法显著提升了重建质量与轨迹估计精度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11992v1/fig-001.webp\", \"caption\": \"Fig. 2: System overview ReefMapGStakes as input RGB images and inertial, acoustic, and pressure sensor data from an AUV. Sensor data is fused into odometry while the known, central landmark is detected from RGB frames. Together, this information is integrated into a factor graph to estimate the whole AUV trajectory, shown in the center. We incrementally build a 3DGS model of the scene, starting from the region of highest certainty near the center. The solid blue line shows observations incorporated into the current map. From this current map, we expand and incorporate new observations at the frontier, the solid red line. The observations are aligned with the current map starting from the pose estimate from the factor graph. Together with depth information from a metric depth estimation model, we initialize new Gaussians and then optimize the model, yielding an expanded map. We repeat this process to incorporate the remaining observations, shown in the dashed blue line.\", \"page\": 4, \"index\": 1, \"width\": 1052, \"height\": 279}, {\"url\": \"assets/figures/arxiv/2604.11992v1/fig-002.webp\", \"caption\": \"Fig. 3: Incremental, radially-expanding 3DGS optimization framework. Local camera pose refinement occurs as we radially and incrementally expand our 3DGS-based map representation, incorporating new cameras and their visual data into the scene. Given a current 3DGS map, target image, and initial camera pose estimate, we can refine the camera pose with gradient descent (as shown from the light to dark frustums). Sample target, initial pose render, and final pose render images are shown on the left for the blue frustum. As camera poses are refined, we add these refined camera pose estimates as factors into our pose-graph to globally update the whole robot trajectory, as shown between the light (before) and dark (after) overlayed trajectories. Local pose refinement aligns new observations before expanding the scene, while global trajectory optimization propagates these changes to poses far away.\", \"page\": 4, \"index\": 2, \"width\": 1047, \"height\": 290}, {\"url\": \"assets/figures/arxiv/2604.11992v1/fig-003.webp\", \"caption\": \"TABLE II: Reconstruction and tracking metrics across different methods. We ablate across different configurations of input data and 3DGS optimization and with WildGS-SLAM. Initial poses can come from GTSAM or Metashape, Gaussians are initialized either from the SfM point cloud, sfm, or depth estimator outputs, md, and the 3DGS can be optimized either in batch, batch, or incrementally following our method, inc. Input data configurations that\", \"page\": 7, \"index\": 3, \"width\": 1052, \"height\": 219}, {\"url\": \"assets/figures/arxiv/2604.11992v1/fig-004.webp\", \"caption\": \"Fig. 4: Qualitative trajectory evaluation Top-down view of robot trajectories estimated by ReefMapGSand other baselines methods are plotted against the reference trajectory estimated by Metashape, dashed green, at both reef sites, Tektite (above) and Yawzi (below).\", \"page\": 6, \"index\": 4, \"width\": 491, \"height\": 772}]"
motivation: 3DGS依赖的传统SfM位姿获取方式计算量巨大，无法直接应用于依赖多模态传感器进行实时作业的水下机器人领域。
method: 提出一种增量式框架，通过交替执行局部图像跟踪与3DGS优化，并将优化后的位姿回传至位姿图以实现全局轨迹校准。
result: 在复杂水下珊瑚礁场景中实现了无需COLMAP的高质量重建，并在长达700米的AUV作业轨迹上验证了更优的位姿估计精度。
conclusion: 该研究通过闭合多模态SLAM与3DGS之间的反馈环路，为大规模水下环境的自主三维重建提供了高效且鲁棒的解决方案。
---

## 摘要
3D 高斯泼溅（3DGS）是一种强大的视觉表示方法，能够提供高质量且高效的 3D 场景重建，但它关键性地依赖于精确的相机位姿。这些位姿通常通过运动恢复结构（SfM）等计算密集型过程获得，而这类过程并不适用于野外机器人应用。然而，在这些领域中，来自声学、惯性、压力和视觉传感器的多模态传感器数据是可用的，并且适用于基于位姿图优化的 SLAM 方法，这些方法可以在提供不确定性的同时估计载具轨迹，从而获得所需的相机位姿。我们提出了一种基于 3DGS 的增量式重建框架 ReefMapGS，该框架从高确定性区域构建初始模型，并逐步扩展以纳入整个场景。我们通过交替进行新图像观测的局部跟踪与底层 3DGS 场景的优化，来实现场景的增量式重建。这些优化后的位姿被整合回位姿图中，以对整个轨迹进行全局优化。我们展示了两个具有复杂几何形状的水下珊瑚礁遗址的无需 COLMAP 的 3D 重建，以及在长达 700 米的调查轨迹上对我们的 AUV 进行的更精确的全局位姿估计。

## Abstract
3D Gaussian Splatting is a powerful visual representation, providing high-quality and efficient 3D scene reconstruction, but it is crucially dependent on accurate camera poses typically obtained from computationally intensive processes like structure-from-motion that are unsuitable for field robot applications. However, in these domains, multimodal sensor data from acoustic, inertial, pressure, and visual sensors are available and suitable for pose-graph optimization-based SLAM methods that can estimate the vehicle's trajectory and thus our needed camera poses while providing uncertainty. We propose a 3DGS-based incremental reconstruction framework, ReefMapGS, that builds an initial model from a high certainty region and progressively expands to incorporate the whole scene. We reconstruct the scene incrementally by interleaving local tracking of new image observations with optimization of the underlying 3DGS scene. These refined poses are integrated back into the pose-graph to globally optimize the whole trajectory. We show COLMAP-free 3D reconstruction of two underwater reef sites with complex geometry as well as more accurate global pose estimation of our AUV over survey trajectories spanning up to 700 m.

---

## 论文详细总结（自动生成）

### 论文总结：ReefMapGS —— 闭环多模态 SLAM 与高斯泼溅实现大规模水下重建

#### 1. 核心问题与研究动机
*   **核心问题**：如何在不依赖计算密集型、离线运动恢复结构（SfM，如 COLMAP）的情况下，利用自主水下航行器（AUV）实现大规模（百米级）水下环境的高质量、高效率 3D 重建。
*   **研究动机**：
    *   **3DGS 的局限性**：3D 高斯泼溅（3DGS）虽能提供高质量重建，但极度依赖精确的相机位姿，通常需通过耗时的 SfM 获取，不适合野外机器人作业。
    *   **水下环境挑战**：光照衰减、浑浊、低纹理导致纯视觉 SLAM 极易失效。
    *   **传感器冗余**：水下机器人通常配备惯性（IMU）、声学（DVL）和压力传感器，这些多模态数据可提供位姿估计，但精度不足以直接用于高质量重建。

#### 2. 方法论
ReefMapGS 的核心思想是**将多模态位姿图 SLAM 与增量式 3DGS 紧密耦合，形成双向优化的闭环系统**。
*   **多模态因子图优化**：利用 EKF 融合 IMU、DVL 和压力传感器数据生成里程计，并结合预设的中心地标（如 AprilTag）构建因子图，初步估计 AUV 轨迹。
*   **增量式 3DGS 构建**：
    *   **径向扩展**：从地标附近（位姿不确定性最低的区域）开始构建初始模型，按距离地标的远近分层（环状）增量式加入新观测。
    *   **局部位姿精炼**：在加入新图像前，通过微分渲染最小化重建误差，对相机位姿进行梯度下降优化。
*   **全局轨迹校准（闭环）**：将 3DGS 精炼后的位姿作为“3DGS 因子”反馈回因子图，重新进行全局轨迹优化，修正长距离漂移。
*   **伪深度初始化**：由于没有 SfM 点云，使用微调后的 DepthAnythingV2 模型生成度量深度，用于初始化新增的高斯点。

#### 3. 实验设计
*   **场景与数据集**：在美属维尔京群岛的两个真实珊瑚礁遗址（Tektite 和 Yawzi）采集数据。
    *   **Tektite**：轨迹长 347 米，覆盖 20m x 10m。
    *   **Yawzi**：轨迹长 696 米，覆盖 20m x 20m。
*   **Benchmark（基准）**：以商业软件 Metashape 生成的 SfM 位姿作为轨迹真值参考。
*   **对比方法**：
    *   **视觉 SLAM**：ORB-SLAM3, DROID-SLAM, MASt3R-SLAM, VGGT-SLAM。
    *   **3DGS SLAM**：MonoGS, WildGS-SLAM。
    *   **消融实验**：对比了 Batch（批处理）vs Incremental（增量）、SfM 初始化 vs 深度估计初始化、有无全局重优化（+reopt）。

#### 4. 资源与算力
*   **硬件配置**：Ryzen 9 7900X CPU 和 **NVIDIA RTX 6000 Ada GPU**。
*   **运行耗时**：
    *   ReefMapGS 在两个场景下分别耗时约 **2.5 小时和 3.3 小时**。
    *   相比之下，COLMAP 需 10-17 小时，Metashape 需 7-8 小时。
    *   虽然比纯学习方法慢，但 ReefMapGS 是唯一能在这些大规模水下场景生成相干结果的方法。

#### 5. 实验数量与充分性
*   **实验规模**：针对两个大型真实水下遗址进行了详尽测试，轨迹长度达数百米，远超一般 3DGS SLAM 论文中的桌面级或房间级场景。
*   **充分性**：实验涵盖了轨迹精度（ATE RMSE）和重建质量（PSNR, SSIM, LPIPS）两个维度。消融实验设计合理，客观地展示了增量优化和全局重优化对提升精度的贡献。
*   **公平性**：对比了当前最先进的多种学习型和传统 SLAM 框架，证明了在水下特定分布数据下，现有通用方法大多会失效。

#### 6. 主要结论与发现
*   **精度提升**：ReefMapGS 的轨迹误差比仅使用多模态 SLAM（GTSAM）降低了 **19% 至 58.8%**。
*   **重建质量**：增量式优化显著优于批处理优化，即使在没有 SfM 初始点云的情况下，其重建质量也接近甚至超过了使用“先验真值”的方法。
*   **效率优势**：处理速度比传统 SfM 流程快 **2-5 倍**，且无需人工干预。

#### 7. 优点
*   **无需 SfM**：彻底摆脱了对 COLMAP 等昂贵预处理步骤的依赖，适合野外快速部署。
*   **鲁棒性强**：通过多模态传感器弥补了视觉在水下的不可靠性，通过 3DGS 反哺提升了 SLAM 的全局一致性。
*   **规模化**：成功将 3DGS 应用于近 700 米长的真实水下调查轨迹，展示了极佳的扩展性。

#### 8. 不足与局限
*   **地标依赖**：目前仍依赖一个已知的中心地标（AprilTag）来锚定位姿图并降低初始不确定性。
*   **轨迹限制**：实验主要基于“玫瑰花结（Rosette）”形状的调查轨迹，对于更随机或直线型的轨迹，其径向扩展策略可能需要调整。
*   **非实时性**：虽然比 SfM 快，但数小时的处理时间仍无法达到实时在线避障或导航的要求。
*   **计算资源**：依赖高端 GPU（RTX 6000 Ada），在嵌入式 AUV 平台上直接运行仍有挑战。

（完）
