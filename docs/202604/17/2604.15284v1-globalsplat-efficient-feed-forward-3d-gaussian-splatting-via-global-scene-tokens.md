---
title: "GlobalSplat: Efficient Feed-Forward 3D Gaussian Splatting via Global Scene Tokens"
title_zh: GlobalSplat：通过全局场景 Token 实现的高效前馈 3D 高斯泼溅
authors: "Roni Itkin, Noam Issachar, Yehonatan Keypur, Yehonatan Keypur, Anpei Chen, Sagie Benaim"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15284v1"
tags: ["query:ff3dr"]
score: 10.0
evidence: 通过全局场景令牌实现高效的前馈3D高斯泼溅
tldr: GlobalSplat 针对 3D 高斯泼溅（3DGS）中基元分配冗余和全局一致性差的问题，提出了一种“先对齐后解码”的前馈框架。通过学习全局场景 Token 来编码多视图输入并解决跨视图对应关系，该方法避免了像素对齐带来的冗余。结合由粗到细的训练策略，GlobalSplat 在保持高渲染质量的同时，显著减少了高斯数量（仅需 16K）和存储空间（4MB），并实现了极速推理。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15284v1/fig-001.webp\", \"caption\": \"Fig. 2: GlobalSplat Architecture Overview. Given a sparse set of input views, image features are extracted via a View Encoder. A fixed set of learnable latent scene tokens is iteratively refined through a dual-branch encoder block (repeated B times) designed to explicitly disentangle geometry and appearance. Within each branch, queries (QG, QA) cross-attend to multi-view features (KI , VI) and self-attend to global context. The streams are fused via a Mixer MLP to update the tokens for the subsequent block. Specialized Geometry and Appearance Decoders then transform these globallyaware tokens into explicit 3D Gaussians. As depicted on the right, the network employs a Coarse-to-Fine Training Curriculum strategy to progressively increase the decoded Gaussian capacity, supervised jointly by rendering, self-supervised consistency, and regularization losses.\", \"page\": 6, \"index\": 1, \"width\": 707, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2604.15284v1/fig-002.webp\", \"caption\": \"Fig. 1: Align First, Decode Later. Top: Existing feed-forward 3D Gaussian Splatting pipelines rely on view-centric, per-pixel primitive allocation. As the number of input views increases, these approaches bake massive redundancy into the 3D representation, scaling to hundreds of thousands or millions of Gaussians. In contrast, GlobalSplat aggregates multi-view inputs into a fixed set of global latent scene tokens before decoding geometry. This achieves an optimal balance: for instance, for 24 input views on RealEstate10K, GlobalSplat delivers highly competitive novel-view synthesis quality (28.5 PSNR), utilizing only 16K Gaussians and offering ultra-low GPU memory usage (1.79 GB), minimal disk size (<4 MB), and extremely fast inference times (<78 ms). Bottom Right: This scene-centric approach translates to a significantly stronger practical operating point in comparison to state-of-the art approaches, as demonstrated in the radar chart (evaluated on 24 input views on RealEstate10K). Bottom Left: By decoding from a global scene context (without a fixed grid), GlobalSplat stays sparse and places primitives at occupied 3D locations. This yields an adaptive allocation where low-frequency regions are covered by fewer Gaussians with larger spatial support (higher coverage), enabling complex scenes with far fewer primitives. We visualize the Gaussians as disks corresponding to their scale, as well as the Gaussian centers using a point cloud. As can be seen, this allows the model to effectively capture complex environments with drastically fewer primitives.\", \"page\": 2, \"index\": 2, \"width\": 719, \"height\": 568}, {\"url\": \"assets/figures/arxiv/2604.15284v1/fig-003.webp\", \"caption\": \"Table 6: Implementation details and training hyperparameters.\", \"page\": 28, \"index\": 3, \"width\": 520, \"height\": 856}, {\"url\": \"assets/figures/arxiv/2604.15284v1/fig-004.webp\", \"caption\": \"Fig. 4: Qualitative comparison on ACID. We compare GlobalSplat against baselines (Zpressor, DepthSplat, GGN, C3G) and the ground truth (GT) across 6 different ACID scenes (rows).\", \"page\": 19, \"index\": 4, \"width\": 716, \"height\": 742}, {\"url\": \"assets/figures/arxiv/2604.15284v1/fig-005.webp\", \"caption\": \"Fig. 3: Qualitative comparison. We compare GlobalSplat against baselines (Zpressor, DepthSplat, GGN, C3G) and the ground truth (GT) across 6 different scenes (rows).\", \"page\": 12, \"index\": 5, \"width\": 716, \"height\": 742}]"
motivation: 现有的前馈 3DGS 方法多采用像素或体素对齐，导致基元分配冗余、存储开销大且难以维持多视图全局一致性。
method: 提出一种基于全局场景 Token 的框架，通过先学习紧凑的全局潜空间表示再解码为 3D 几何，并配合由粗到细的训练课程来控制模型容量。
result: 在 RealEstate10K 和 ACID 数据集上，仅需 1.6 万个高斯基元（约 4MB 存储）即可达到竞争性性能，且单次前馈推理时间低于 78 毫秒。
conclusion: GlobalSplat 证明了通过全局场景感知和紧凑的潜空间编码，可以在极低存储和计算开销下实现高质量、全局一致的 3D 场景重建。
---

## 摘要
基元的有效空间分配是 3D 高斯泼溅（3D Gaussian Splatting）的基础，因为它直接决定了表示紧凑性、重建速度和渲染保真度之间的协同作用。之前的解决方案，无论是基于迭代优化还是前馈推理，都在这些目标之间面临显著的权衡，这主要是由于其依赖于缺乏全局场景感知、由启发式驱动的局部分配策略。具体而言，当前的前馈方法在很大程度上是像素对齐或体素对齐的。通过将像素反投影为密集的、视点对齐的基元，它们在 3D 资产中引入了冗余。随着输入视图的增加，表示规模随之增大，且全局一致性变得脆弱。为此，我们引入了 GlobalSplat，这是一个建立在“先对齐，后解码”原则之上的框架。我们的方法学习一种紧凑的全局潜场景表示，在解码任何显式 3D 几何之前，该表示会对多视图输入进行编码并解决跨视图对应关系。至关重要的是，这种形式化方法实现了紧凑且全局一致的重建，而无需依赖预训练的像素预测骨干网络，也不需要重用来自密集基准模型的潜特征。通过利用一种逐渐增加解码能力的由粗到精的训练课程，GlobalSplat 从根本上防止了表示膨胀。在 RealEstate10K 和 ACID 数据集上，我们的模型在仅使用 1.6 万个高斯函数的情况下，实现了具有竞争力的创新视角合成性能，这显著少于密集流水线所需的高斯数量，并获得了仅 4MB 的轻量化占用空间。此外，GlobalSplat 的推理速度显著快于基准模型，单次前馈运行时间低于 78 毫秒。项目页面见 https://r-itk.github.io/globalsplat/。

## Abstract
The efficient spatial allocation of primitives serves as the foundation of 3D Gaussian Splatting, as it directly dictates the synergy between representation compactness, reconstruction speed, and rendering fidelity. Previous solutions, whether based on iterative optimization or feed-forward inference, suffer from significant trade-offs between these goals, mainly due to the reliance on local, heuristic-driven allocation strategies that lack global scene awareness. Specifically, current feed-forward methods are largely pixel-aligned or voxel-aligned. By unprojecting pixels into dense, view-aligned primitives, they bake redundancy into the 3D asset. As more input views are added, the representation size increases and global consistency becomes fragile. To this end, we introduce GlobalSplat, a framework built on the principle of align first, decode later. Our approach learns a compact, global, latent scene representation that encodes multi-view input and resolves cross-view correspondences before decoding any explicit 3D geometry. Crucially, this formulation enables compact, globally consistent reconstructions without relying on pretrained pixel-prediction backbones or reusing latent features from dense baselines. Utilizing a coarse-to-fine training curriculum that gradually increases decoded capacity, GlobalSplat natively prevents representation bloat. On RealEstate10K and ACID, our model achieves competitive novel-view synthesis performance while utilizing as few as 16K Gaussians, significantly less than required by dense pipelines, obtaining a light 4MB footprint. Further, GlobalSplat enables significantly faster inference than the baselines, operating under 78 milliseconds in a single forward pass. Project page is available at https://r-itk.github.io/globalsplat/

---

## 论文详细总结（自动生成）

# 论文总结：GlobalSplat —— 基于全局场景 Token 的高效前馈 3D 高斯泼溅

### 1. 论文的核心问题与整体含义
*   **研究背景**：3D 高斯泼溅（3DGS）在前馈新视角合成（NVS）领域取得了显著进展，但现有方法（如 pixelSplat, MVSplat）大多采用**像素对齐**或**视点对齐**的策略。
*   **核心问题**：这种局部对齐方式导致随着输入视图数量增加，生成的高斯基元数量剧增（冗余严重），造成存储空间巨大、推理速度变慢，且难以维持长序列下的全局一致性。
*   **研究动机**：提出一种“先对齐，后解码”的架构，通过固定的全局场景 Token 聚合多视图信息，在保证重建质量的同时，实现极高的压缩率和推理效率。

### 2. 方法论
*   **核心思想**：**Align First, Decode Later**。不再从单个像素“反投影”生成高斯，而是先将所有视图特征融合进一组固定的潜场景 Token 中，再由这些 Token 解码出显式的 3D 高斯参数。
*   **关键技术细节**：
    *   **场景归一化**：将不同场景的相机位姿映射到统一的规范坐标系（Canonical frame），并进行尺度缩放。
    *   **输入准备**：结合 Plücker 射线特征与显式的相机元数据（中心点、内参），增强模型对全局几何的感知。
    *   **双分支编码器**：设计了显式分离**几何（Geometry）**与**外观（Appearance）**的迭代注意力架构。通过交叉注意力（Cross-Attention）从多视图图像中提取信息，通过自注意力（Self-Attention）进行全局建模。
    *   **由粗到细（Coarse-to-Fine）训练课程**：初始阶段将每个 Token 解码的多个高斯候选合并为一个，随着训练进行逐渐增加解码容量（从 1 增加到 8 个高斯/Token），确保模型先学习宏观结构再优化局部细节。
    *   **自监督一致性损失**：通过将输入视图分成两个子集并要求其生成的几何/深度图保持一致，增强了重建的鲁棒性。

### 3. 实验设计
*   **数据集**：
    *   **RealEstate10K**：主要训练和评估基准，包含大量室内外建筑漫游视频。
    *   **ACID**：用于测试模型的**零样本（Zero-shot）跨数据集泛化能力**，包含航拍景观。
*   **Benchmark 设置**：在 12、24、36 个输入视图的大上下文设置下评估新视角合成质量。
*   **对比方法**：
    *   非高斯方法：LVSM。
    *   密集/视图对齐方法：NoPoSplat, AnySplat, DepthSplat, GGN。
    *   压缩/紧凑型方法：Zpressor, C3G, EcoSplat。

### 4. 资源与算力
*   **硬件环境**：所有效率基准测试均在单张 **NVIDIA A100 (64GB VRAM)** GPU 上完成。
*   **训练细节**：
    *   总训练步数：220,000 步。
    *   全局 Batch Size：16。
    *   优化器：AdamW，学习率 $5 \times 10^{-4}$。
*   **推理效率**：单次前馈推理时间约为 **77.88 毫秒**，峰值显存占用仅 **1.79 GB**。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个大规模数据集上进行了详尽的定量对比，并提供了丰富的定性视觉结果。
*   **消融实验**：针对双分支架构、Coarse-to-Fine 策略、一致性损失、相机元数据注入以及 Token 数量与高斯密度的权衡进行了多组消融实验。
*   **客观性**：作者在相同硬件环境下重新运行了多个基准模型（如 DepthSplat, Zpressor, C3G）以确保效率对比的公平性。实验设置涵盖了不同数量的输入视图，验证了模型在处理长序列输入时的稳定性。

### 6. 主要结论与发现
*   **极高的压缩比**：GlobalSplat 仅需 **16K** 个高斯基元即可达到 28.5 PSNR，相比密集型基准减少了 99% 以上的基元数量。
*   **存储与速度优势**：生成的 3D 资产仅占 **4MB** 磁盘空间，推理速度比同类方法快数倍。
*   **关键发现**：实验证明，**增加潜空间 Token 的数量**比增加每个 Token 解码出的高斯数量对提升质量更有效。
*   **泛化性**：在未见过的 ACID 数据集上表现稳健，证明了全局 Token 机制捕捉到了通用的 3D 结构特征。

### 7. 优点
*   **架构创新**：打破了像素对齐的固有思维，提出的“先对齐后解码”有效解决了前馈 3DGS 的扩展性难题。
*   **实用性极强**：极低的显存占用和极快的推理速度使其非常适合移动端或实时应用。
*   **训练稳定**：通过 Coarse-to-Fine 课程和双分支设计，有效避免了复杂场景下的梯度冲突和表示膨胀。

### 8. 不足与局限
*   **固定容量限制**：使用固定数量的 Token（如 2048 个），在面对超大规模（如城市级）或极度复杂的场景时，可能会遇到表示能力瓶颈。
*   **静态场景假设**：目前仅针对静态场景设计，尚未扩展到动态 4D 重建。
*   **极稀疏输入挑战**：在极少数视图（如 2-3 张）输入时，由于缺乏足够的视差信息来构建全局潜空间，表现可能不如专门针对极稀疏视角优化的模型。

（完）
