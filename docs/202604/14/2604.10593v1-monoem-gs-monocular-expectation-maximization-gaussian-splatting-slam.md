---
title: "MonoEM-GS: Monocular Expectation-Maximization Gaussian Splatting SLAM"
title_zh: MonoEM-GS：单目期望最大化高斯泼溅 SLAM
authors: "Evgenii Kruzhkov, Sven Behnke"
date: 2026-04-12
pdf: "https://arxiv.org/pdf/2604.10593v1"
tags: ["query:ff3dr"]
score: 9.0
evidence: 用于稠密点云推理的前馈几何基础模型
tldr: 针对单目SLAM中几何基础模型预测存在噪声和视图不一致性的问题，本文提出MonoEM-GS。该方法将几何预测集成到全局高斯泼溅（GS）表示中，通过期望最大化（EM）框架稳定几何结构，并利用ICP进行位姿估计。此外，它通过多模态特征参数化支持开放集分割，在多个数据集上展现了优异的重建与定位性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10593v1/fig-001.webp\", \"caption\": \"Fig. 1. MonoEM-GS constructs a map in which each Gaussian aggregates measurements over time. Despite contradictory point-cloud predictions at timestamps t and t+1, the Gaussians remain consistent, with only a single update step needed to incorporate new observations.\", \"page\": 1, \"index\": 1, \"width\": 514, \"height\": 337}, {\"url\": \"assets/figures/arxiv/2604.10593v1/fig-002.webp\", \"caption\": \"Fig. 6. Reconstructed Gaussians (upper row) and corresponding meshes (bottom row) created from the Gaussian centers and normals.\", \"page\": 7, \"index\": 2, \"width\": 1054, \"height\": 475}, {\"url\": \"assets/figures/arxiv/2604.10593v1/fig-003.webp\", \"caption\": \"Fig. 7. MonoEM-GS Gaussian centers colored by their predicted classes (right) and the nearest ground-truth points colored by their GT classes (left).\", \"page\": 7, \"index\": 3, \"width\": 512, \"height\": 365}, {\"url\": \"assets/figures/arxiv/2604.10593v1/fig-004.webp\", \"caption\": \"Fig. 2. Single iteration of processing a new input image by MonoEM-GS. For each new RGB input, MapAnything [27] produces a dense point cloud prediction oNt . We align it with the new prediction for frame N-1: oN−1\", \"page\": 3, \"index\": 4, \"width\": 1052, \"height\": 563}, {\"url\": \"assets/figures/arxiv/2604.10593v1/fig-005.webp\", \"caption\": \"Fig. 3. Views of the current prediction alignment with the map. View 1: misaligned current prediction oNt ; View 2: predictions oN−1\", \"page\": 4, \"index\": 5, \"width\": 512, \"height\": 458}, {\"url\": \"assets/figures/arxiv/2604.10593v1/fig-006.webp\", \"caption\": \"Fig. 4. Multi-domain scene reconstructions produced by MonoEM-GS. A: Gaussian Splatting created by the proposed approach; B: Mesh reconstructed from Gaussian centers and normals; C: DINOv3 [29] feature map; D: Open-set 3D semantic map.\", \"page\": 5, \"index\": 6, \"width\": 1052, \"height\": 221}]"
motivation: 现有的几何基础模型在单目SLAM中预测的几何信息存在视图依赖性、噪声以及局部尺度漂移等不一致性问题。
method: 提出一种结合EM算法与高斯泼溅的制图管线，利用ICP进行位姿对齐，并引入多模态特征以支持下游语义查询。
result: 在7-Scenes、TUM RGB-D和Replica等数据集上的实验表明，该方法在处理几何不一致性方面优于现有基准。
conclusion: MonoEM-GS通过EM框架有效融合了带噪几何先验，实现了稳健的单目高斯SLAM及多功能地图构建。
---

## 摘要
前馈几何基础模型可以直接从 RGB 流中推断出稠密点云和相机运动，为单目 SLAM 提供先验。然而，它们的预测通常具有视角依赖性且包含噪声：几何结构可能随视角和图像变换而变化，且局部度量属性可能在帧间发生漂移。我们提出了 MonoEM-GS，这是一个单目建图流水线，它将此类几何预测集成到全局高斯泼溅（Gaussian Splatting）表示中，同时显式地处理了这些不一致性。MonoEM-GS 将高斯泼溅与期望最大化（Expectation-Maximization）公式相结合以稳定几何结构，并采用基于 ICP 的对齐进行单目位姿估计。除了几何结构外，MonoEM-GS 还利用多模态特征对高斯进行参数化，从而能够直接在重建的地图上进行原位开放集分割和其他下游查询。我们在 7-Scenes、TUM RGB-D 和 Replica 数据集上评估了 MonoEM-GS，并与最近的基准方法进行了比较。

## Abstract
Feed-forward geometric foundation models can infer dense point clouds and camera motion directly from RGB streams, providing priors for monocular SLAM. However, their predictions are often view-dependent and noisy: geometry can vary across viewpoints and under image transformations, and local metric properties may drift between frames. We present MonoEM-GS, a monocular mapping pipeline that integrates such geometric predictions into a global Gaussian Splatting representation while explicitly addressing these inconsistencies. MonoEM-GS couples Gaussian Splatting with an Expectation--Maximization formulation to stabilize geometry, and employs ICP-based alignment for monocular pose estimation. Beyond geometry, MonoEM-GS parameterizes Gaussians with multi-modal features, enabling in-place open-set segmentation and other downstream queries directly on the reconstructed map. We evaluate MonoEM-GS on 7-Scenes, TUM RGB-D and Replica, and compare against recent baselines.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **MonoEM-GS** 的单目 SLAM 系统，旨在通过结合期望最大化（EM）算法与高斯泼溅（Gaussian Splatting）技术，解决前馈几何基础模型在单目重建中的不一致性问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前的前馈几何基础模型（如 DUSt3R, MapAnything）虽然能从单目 RGB 流中推断出稠密点云和相机运动，但其预测结果具有**视角依赖性**且包含大量**噪声**。具体表现为：同一物体在不同视角下的几何形状不一致、图像变换（如翻转）会导致预测改变、局部度量属性（如尺度）在帧间发生漂移。
*   **研究动机**：现有的单目 SLAM 系统在处理这些不一致的几何先验时容易积累误差，导致全局地图破碎或不平滑。作者希望建立一个能显式处理这些不一致性、并能支持下游语义任务的稳健单目建图管线。

### 2. 方法论
MonoEM-GS 将地图表示为高斯混合模型（GMM），并利用高斯泼溅（GS）进行渲染和数据关联。
*   **模型推理**：使用 MapAnything 模型从连续 RGB 帧中提取度量尺度的点云、颜色、法线及 DINOv3 特征。
*   **定位（Localization）**：
    *   利用 ICP 算法将当前帧的预测点云与地图进行对齐。
    *   通过缓冲区（FIFO）中重复出现的帧建立点对点对应关系，实现从粗到精的位姿估计。
*   **建图（Mapping）**：
    *   **增量式 EM 更新**：将地图视为概率混合模型。在 E 步计算观测点对高斯的响应度（考虑位置、法线一致性和 DINO 特征相似度）；在 M 步通过加权运行平均值更新高斯的均值、协方差和颜色。
    *   **基于渲染的细化**：利用 GS 渲染生成的深度图来识别未探索区域。对于未被 EM 融合的点，如果它们位于新区域，则作为新高斯加入地图；如果是已有区域的噪声，则进行合并或剔除。
*   **多模态参数化**：每个高斯球不仅存储几何信息，还存储高维 DINOv3 特征，支持直接在地图上进行开放集语义分割。

### 3. 实验设计
*   **数据集**：
    *   **7-Scenes**：小型室内场景，用于评估轨迹和重建。
    *   **TUM RGB-D**：房间级环境，用于评估轨迹和重建。
    *   **Replica**：光照真实的仿真环境，用于评估 3D 语义分割性能。
*   **基准方法（Benchmarks）**：
    *   **SLAM 基准**：VGGT-SLAM, VGGT-SLAM2, MASt3R-SLAM。
    *   **语义基准**：ConceptGraphs, RayFronts, OVO, OMCL。
*   **对比指标**：ATE RMSE（绝对轨迹误差）、尺度误差、重建准确度（Acc）、完整度（Comp）、法线一致性、F1 分数、mIoU（语义分割）。

### 4. 资源与算力
*   **硬件**：实验在一台配备 **NVIDIA RTX 3090 GPU (24 GB)** 的工作站上运行。
*   **处理细节**：在所有数据集上每隔 10 帧处理一次。MapAnything 推理时采用 25 帧为一个批次（受限于显存）。
*   **训练/运行时间**：文中未明确给出完整的运行耗时，但提到 EM 更新是单步完成的，且 GS 渲染用于加速数据关联。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 3 个主流数据集，对比了 5-6 种最新的单目 SLAM 和语义建图方法。
*   **充分性与客观性**：
    *   实验设计较为全面，不仅对比了定位精度，还重点评估了重建的几何质量（如法线一致性）。
    *   **公平性**：所有基准方法均使用默认参数，并在支持的情况下使用校准后的相机内参。
    *   **消融/对比**：通过可视化对比（图 5）直观展示了该方法在消除几何歧义和生成平滑网格方面的优势。

### 6. 主要结论与发现
*   **几何一致性**：MonoEM-GS 在法线一致性和 F1 分数上表现优异，生成的网格比 VGGT-SLAM2 等方法更干净、噪声更少。
*   **尺度稳定性**：该方法能保持较好的度量尺度，与 MASt3R-SLAM 处于同一梯队。
*   **语义功能**：通过集成 DINOv3 特征，在 Replica 数据集上实现了领先的频率加权 mIoU（f-mIoU），证明了其在开放集语义理解方面的潜力。
*   **定位精度**：虽然在某些场景下定位精度略逊于专门优化位姿图的基准方法，但其重建质量弥补了这一不足。

### 7. 优点
*   **创新的融合机制**：首次将 EM 概率框架与 GS 渲染结合用于单目 SLAM，有效平滑了带噪的几何预测。
*   **稀疏且高效**：相比于存储数百万个点的稠密 SLAM，MonoEM-GS 的高斯表示更稀疏，允许存储高维特征而不产生显著开销。
*   **多功能性**：一个系统同时输出高质量点云、网格、GS 渲染图和语义地图。

### 8. 不足与局限
*   **场景规模限制**：目前仅限于房间级（Room-scale）场景，由于**缺乏回环检测（Loop Closure）**，在长距离大场景下会产生漂移。
*   **误匹配风险**：如果预测的几何体显著位于实际表面前方，系统可能会错误地将其集成。
*   **实时性挑战**：虽然 EM 更新很快，但依赖于重型前馈模型（如 MapAnything 和 DINOv3），整体管线的实时运行可能需要极高性能的硬件支持。

（完）
