---
title: "MorphoFlow: Sparse-Supervised Generative Shape Modeling with Adaptive Latent Relevance"
title_zh: MorphoFlow：具有自适应潜变量相关性的稀疏监督生成式形状建模
authors: "Mokshagna Sai Teja Karanam, Tushar Kataria, Shireen Elhabian"
date: 2026-04-13
pdf: "https://arxiv.org/pdf/2604.11636v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 使用神经隐式表示的生成式形状建模
tldr: 针对传统统计形状建模依赖密集标注和固定潜空间的问题，本文提出MorphoFlow框架。该框架结合神经隐式表示与自动解码器，支持从稀疏表面标注中学习概率形状分布。通过引入自回归正态化流和自适应潜变量相关性权重，模型能自动调节潜维度贡献并实现高分辨率重建。该方法在腰椎和股骨数据集上表现优异，实现了无需手动调参的解剖形状合成与不确定性量化，显著提升了建模的灵活性与可扩展性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.11636v1/fig-001.webp\", \"caption\": \"Fig. 1. MorphoFlow Architecture. For only generative shape modelling α−1 = 1, making the distribution a standard normal. For compact shape latent α−1 values are learned during training with ARD regularization loss.\", \"page\": 4, \"index\": 1, \"width\": 617, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2604.11636v1/fig-002.webp\", \"caption\": \"Table 1. Quantitative Results on VERSE. Average Surface Distance (ASD), Hausdorff Distance (HSD, HSD95), and Dice Score (DSC) are reported for all models. Var denotes the number of latent dimensions explaining 95–97–99% variance after ARD regularization. R2 and Corr (Pearson r) evaluate predictive performance.\", \"page\": 6, \"index\": 2, \"width\": 715, \"height\": 248}, {\"url\": \"assets/figures/arxiv/2604.11636v1/fig-003.webp\", \"caption\": \"Table 2. Quantitative Results for Femur.\", \"page\": 7, \"index\": 3, \"width\": 721, \"height\": 245}, {\"url\": \"assets/figures/arxiv/2604.11636v1/fig-004.webp\", \"caption\": \"Fig. 2. Results. (A) Best, median, and worst surface reconstructions obtained from models trained on thin, thick, and orthogonal slices(latent dimension=64). The right column shows models trained with MorphoFlow, while the left column shows models trained without Flow and ARD regularization. (B) Estimated σ values across different latent dimensionalities when using MorphoFlow showing the impact of ARD regularization. (C) Correlation plots for 1500 generated samples, showing the relationship between the distance in z0 space to the nearest training sample and the corresponding surface distance to the closest training surface. (D) Uncertainty visualization for one sample from the VERSE and OAI datasets using the best-performing thin-slice model(64 latent dimention), including a 3D rendering of uncertainty and cross-sectional views in the axial, sagittal, and coronal planes.(E) Generated Shapes from VERSE and OAI models trained with latent dimension 64.\", \"page\": 8, \"index\": 4, \"width\": 647, \"height\": 362}]"
motivation: 传统统计形状建模方法过度依赖密集标注且缺乏灵活的潜空间表示，限制了其在复杂解剖变异建模中的应用。
method: 集成神经隐式表示、自动解码器与自回归正态化流，并利用稀疏诱导先验实现潜维度的自适应相关性加权。
result: 在腰椎和股骨数据集上的评估显示，该模型能从稀疏输入中实现高精度重建，并准确捕捉群体水平的解剖变异趋势。
conclusion: MorphoFlow通过稀疏监督和自适应潜空间学习，为解剖形状的生成建模提供了一种高效、无需手动调参且具备不确定性量化能力的新方案。
---

## 摘要
统计形状建模 (SSM) 是解剖变异群体级分析的核心，但现有大多数方法依赖于密集标注的分割和固定的潜表示。这些要求限制了可扩展性，并在建模复杂的解剖变异时降低了灵活性。我们提出了 MorphoFlow，这是一个稀疏监督的生成式形状建模框架，可直接从稀疏表面标注中学习紧凑的概率形状表示。MorphoFlow 将神经隐式形状表示与自动解码器架构以及自回归归一化流相结合，以在潜形状空间上学习具有表现力的概率密度。神经隐式表示实现了与分辨率无关的三维解剖建模，而自动解码器架构支持在稀疏监督下直接优化每个实例的潜代码。自回归流捕捉了潜在解剖变异的分布，提供了一个可处理的、基于似然的形状生成模型。为了促进紧凑且结构化的潜表示，我们通过稀疏诱导先验引入了自适应潜变量相关性权重，使模型能够根据各个潜维度与底层解剖变异的相关性来调节其贡献，同时保持生成表现力。由此产生的潜空间支持不确定性量化和符合解剖学原理的形状合成，且无需手动调节潜维度。在公开的腰椎和股骨数据集上的评估表明，该方法能够从稀疏输入中实现精确的高分辨率重建，并恢复出与群体水平趋势一致的结构化解剖变异模式。

## Abstract
Statistical shape modeling (SSM) is central to population level analysis of anatomical variability, yet most existing approaches rely on densely annotated segmentations and fixed latent representations. These requirements limit scalability and reduce flexibility when modeling complex anatomical variation. We introduce MorphoFlow, a sparse supervised generative shape modeling framework that learns compact probabilistic shape representations directly from sparse surface annotations. MorphoFlow integrates neural implicit shape representations with an autodecoder formulation and autoregressive normalizing flows to learn an expressive probabilistic density over the latent shape space. The neural implicit representation enables resolution-agnostic modeling of 3D anatomy, while the autodecoder formulation supports direct optimization of per-instance latent codes under sparse supervision. The autoregressive flow captures the distribution of latent anatomical variability providing a tractable, likelihood-based generative model of shapes. To promote compact and structured latent representations, we incorporate adaptive latent relevance weighting through sparsity-inducing priors, enabling the model to regulate the contribution of individual latent dimensions according to their relevance to the underlying anatomical variation while preserving generative expressivity. The resulting latent space supports uncertainty quantification and anatomically plausible shape synthesis without manual latent dimensionality tuning. Evaluation on publicly available lumbar vertebrae and femur datasets demonstrates accurate high-resolution reconstruction from sparse inputs and recovery of structured modes of anatomical variation consistent with population level trends.

---

## 论文详细总结（自动生成）

### 论文总结：MorphoFlow - 具有自适应潜变量相关性的稀疏监督生成式形状建模

#### 1. 核心问题与研究动机
统计形状建模（SSM）在分析解剖变异（如骨骼形态）中至关重要。然而，传统方法面临两大挑战：
*   **标注成本高昂**：现有模型通常依赖密集的 3D 分割掩码，但在临床实践中，获取全量体积标注既耗时又昂贵，数据往往是稀疏的（如仅有少量切片或低分辨率图像）。
*   **潜空间缺乏结构**：现有的神经隐式表示方法虽能重建形状，但其潜空间（Latent Space）通常缺乏概率解释性，且需要手动调节维度，难以直接用于群体水平的生成式分析。

**研究动机**：开发一种能够直接从稀疏标注中学习、具备生成能力、且能自动优化潜空间紧凑性的形状建模框架。

#### 2. 方法论核心
MorphoFlow 结合了隐式表示、概率建模和稀疏诱导先验：
*   **神经隐式自动解码器**：使用多层感知机（MLP）作为占用预测器 $f_\theta(z, x)$，将空间坐标 $x$ 和潜代码 $z$ 映射为占用概率。通过自动解码器架构，在训练期间直接优化每个样本的潜代码。
*   **自回归归一化流（Normalizing Flows）**：引入掩码自回归有理二次样条变换（MARQS），将复杂的潜分布映射到简单的基础分布。这使得模型能够进行精确的似然评估和高质量的形状采样。
*   **自适应相关性判定（ARD）**：在归一化流的基础分布上施加 Gamma 先验，通过正则化项 $L_{ARD}$ 自动调节每个潜维度的精度（方差的倒数）。这会促使不相关的维度发生方差塌陷，从而实现潜空间的自动压缩和去冗余。
*   **联合损失函数**：$L = L_{rec}（重建） + \lambda_{flow}（似然） + \lambda_{ARD}（紧凑性）$。

#### 3. 实验设计
*   **数据集**：
    *   **VERSE**：230 个训练样本，57 个测试样本（腰椎）。
    *   **OAI-ZIB**：254 个训练样本，100 个测试样本（远端股骨）。
*   **稀疏监督场景**：
    *   **轴向薄切片/厚切片**：8 倍下采样。
    *   **正交切片**：仅使用轴向、冠状、矢状面各一张切片进行训练。
*   **对比基准**：将 MorphoFlow 与不含归一化流和 ARD 正则化的消融版本进行对比。
*   **评估指标**：平均表面距离 (ASD)、豪斯多夫距离 (HSD/HSD95)、Dice 系数 (DSC) 以及潜空间的相关性分析。

#### 4. 资源与算力
*   **硬件**：使用了 **NVIDIA H200 GPU**。
*   **参数设置**：Batch size 为 6。
*   **训练时长**：文中未明确给出具体的训练总时长，但提到了在推理阶段对测试形状进行潜向量优化。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了两个不同的解剖部位（脊柱和股骨），并设置了从 16 到 256 不等的多种潜维度测试。
*   **充分性**：实验设计较为全面，不仅验证了重建精度，还通过 1500 个采样样本验证了生成性能，并进行了不确定性量化分析。对比了不同稀疏程度下的表现，证明了模型在极度稀疏（正交切片）情况下的鲁棒性。

#### 6. 主要结论与发现
*   **性能保持**：引入复杂的归一化流和 ARD 先验并不会降低重建精度，在低维度（如 16 维）下甚至能提升性能。
*   **潜空间紧凑性**：ARD 正则化成功使约 40% 的潜维度趋于零方差，实现了无需手动干预的维度选择。
*   **生成一致性**：潜空间距离与实际几何表面距离高度相关（相关性 > 0.80），说明生成的形状在解剖学上是合理的。
*   **不确定性**：模型在几何高曲率区域（如椎骨突起）表现出更高的预测不确定性。

#### 7. 优点
*   **分辨率无关性**：基于隐式表示，支持任意分辨率的连续重建。
*   **自动调优**：通过 ARD 解决了潜空间维度难以确定的痛点。
*   **弱监督友好**：在极稀疏的切片数据下仍能保持亚像素级的几何准确度。
*   **概率属性**：提供了似然评估和不确定性量化，增强了临床应用的可解释性。

#### 8. 不足与局限
*   **高曲率挑战**：在几何极其复杂的区域，重建的不确定性仍然较高，可能影响精细解剖结构的分析。
*   **计算开销**：自动解码器架构在推理时需要针对每个新样本进行潜代码优化（Optimization-based inference），这比直接前向推理的编码器架构更耗时。
*   **基准对比局限**：主要与自身的消融版本对比，较少涉及与其他最先进（SOTA）的弱监督 3D 重建方法的横向对比。

（完）
