---
title: Efficient Camera Pose Augmentation for View Generalization in Robotic Policy Learning
title_zh: 机器人策略学习中视角泛化的高效相机位姿增强
authors: "Sen Wang, Huaiyi Dong, Jingyi Tian, Jiayi Li, Zhuo Yang, Tongtong Cao, Anlin Chen, Shuang Wu, Le Wang, Sanping Zhou"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29192v1"
tags: ["query:ff3dr"]
score: 10.0
evidence: 用于场景重建的前馈 3D 高斯泼溅
tldr: 针对2D视觉运动策略在未知视角下泛化能力差的问题，本文提出GenSplat框架。该框架利用前馈式3D高斯泼溅（3DGS）技术，从稀疏且未校准的输入中快速重建高质量3D场景。通过引入3D先验蒸馏策略确保几何稳定性，并利用渲染的多样化合成视角进行数据增强，使机器人策略能够基于底层3D结构进行决策，显著提升了在剧烈空间扰动下的执行鲁棒性。
selection_source: fresh_fetch
motivation: 现有的2D视觉策略过度依赖静态观测，导致其在面对未见过的新视角时难以保持动作的一致性。
method: 采用置换等变架构的前馈3DGS框架，并结合3D先验蒸馏策略来生成高质量的合成视角进行训练增强。
result: 实验表明，该方法能有效增强观测流形，使机器人在严重空间扰动下的表现优于基准模型。
conclusion: GenSplat通过将3D场景表示引入策略学习，显著提升了机器人视觉运动策略的视角泛化能力和鲁棒性。
---

## 摘要
现有的以2D为中心的视觉运动策略在面对新视角泛化时表现出明显的不足，因为它们对静态观测的依赖阻碍了在未见视角下的一致动作映射。为此，我们引入了 GenSplat，这是一个前馈 3D 高斯泼溅（3D Gaussian Splatting）框架，通过新视角渲染促进具有视角泛化能力的策略学习。GenSplat 采用置换等变架构，通过单次前馈过程从稀疏、未校准的输入中重建高保真 3D 场景。为了确保结构完整性，我们设计了一种 3D 先验蒸馏策略来正则化 3DGS 优化，防止纯光度监督中常见的几何坍缩。通过从这些稳定的 3D 表示中渲染多样化的合成视角，我们在训练过程中系统地增强了观测流形。这种增强迫使策略将其决策建立在底层 3D 结构之上，从而确保在基准模型性能严重下降的剧烈空间扰动下仍能稳健执行。

## Abstract
Prevailing 2D-centric visuomotor policies exhibit a pronounced deficiency in novel view generalization, as their reliance on static observations hinders consistent action mapping across unseen views. In response, we introduce GenSplat, a feed-forward 3D Gaussian Splatting framework that facilitates view-generalized policy learning through novel view rendering. GenSplat employs a permutation-equivariant architecture to reconstruct high-fidelity 3D scenes from sparse, uncalibrated inputs in a single forward pass. To ensure structural integrity, we design a 3D-prior distillation strategy that regularizes the 3DGS optimization, preventing the geometric collapse typical of purely photometric supervision. By rendering diverse synthetic views from these stable 3D representations, we systematically augment the observational manifold during training. This augmentation forces the policy to ground its decisions in underlying 3D structures, thereby ensuring robust execution under severe spatial perturbations where baselines severely degrade.

---

## 论文详细总结（自动生成）

这是一份关于论文《Efficient Camera Pose Augmentation for View Generalization in Robotic Policy Learning》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：当前的机器人视觉运动策略（Visuomotor Policies）高度依赖于固定的相机视角。当相机位姿发生微小偏移（SE(3) 扰动）时，策略的成功率会剧烈下降。
*   **研究动机**：为了提升视角的泛化能力，传统方法需要耗费大量人力重新采集不同视角的演示数据。本文旨在通过**前馈式 3D 高斯泼溅（3DGS）**技术，从极少数且未校准的观测中合成高质量、几何一致的新视角图像，从而在不增加人工采集成本的前提下，通过增强“观测流形”来训练鲁棒的机器人策略。

### 2. 方法论
*   **核心思想**：构建一个名为 **GenSplat** 的框架，利用前馈神经网络直接预测 3D 高斯参数，并引入 3D 基础模型的知识进行蒸馏，确保生成的虚拟视角在物理和几何上是连贯的。
*   **关键技术细节**：
    *   **置换等变架构（Permutation-Equivariant）**：采用 Transformer 架构处理稀疏输入，确保对输入图像的顺序不敏感，增强了对未校准相机设置的鲁棒性。
    *   **解耦预测**：模型包含相机位姿解码器（预测相对位姿）、点云图解码器（预测局部几何）和高斯参数头（预测不透明度、旋转、缩放和球谐系数）。
    *   **3D 先验蒸馏（3D-Prior Distillation）**：从预训练的 3D 基础模型（如 $\pi^3$）中提取几何知识，监督点云图（Point Map）和相机位姿的生成。相比单纯的深度图，点云图能更好地保持度量结构和空间平滑性。
    *   **损失函数**：结合了光度损失（L1 + LPIPS）、深度对齐损失以及 3D 先验蒸馏损失（点云欧氏距离 + 位姿测地距离）。

### 3. 实验设计
*   **实验场景**：使用 Franka Research 3 机械臂，配备 Robotiq 2F-85 夹持器。
*   **任务设置**：包含 6 个真实世界任务（如捡香蕉、堆叠方块、插环、放罐子到架子等），每个任务采集 100 份专家演示。
*   **Benchmark 与对比方法**：
    *   **策略模型**：$\pi_0$ (VLA 模型) 和 Diffusion Policy。
    *   **对比 NVS 方法**：扩散模型类（ZeroNVS, VISTA, SEVA）和高斯类（InstantSplat, NoPoSplat, AnySplat）。
*   **扰动等级**：分为无扰动、小、中、大四个等级（最大旋转 30°，平移 6cm）。

### 4. 资源与算力
*   **预训练数据**：在 DROID 数据集（约 271k 张图像）上进行预训练。
*   **训练细节**：训练 30k 次迭代，Batch Size 为 16，使用余弦退火学习率（峰值 2e-4）。
*   **算力说明**：文中提到了使用 FlashAttention、梯度检查点（Gradient Checkpointing）和 bfloat16 精度来优化内存，但**未明确给出具体的 GPU 型号和数量**。
*   **推理效率**：GenSplat 生成单帧新视角仅需 **0.14 秒**，远快于扩散模型（如 SEVA 需 50 秒）。

### 5. 实验数量与充分性
*   **实验规模**：在 6 个不同的操作任务上进行了测试，每个任务在不同扰动等级下评估 30 个 Episode。
*   **消融实验**：针对置换等变架构、3D 先验蒸馏、以及渲染视角数量（N=1 到 4）对性能的影响做了详细分析。
*   **充分性评价**：实验设计较为全面，涵盖了从底层渲染质量（PSNR/LPIPS）到高层策略成功率的端到端评估，对比了当前主流的多种生成式增强方案，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能提升**：GenSplat 显著增强了策略的视角泛化能力。在剧烈扰动下，相比仅用原始数据训练的策略，成功率提升显著（如 Diffusion Policy 提升了 56% 的相对性能）。
*   **几何一致性至关重要**：扩散模型生成的图像虽然视觉逼真，但常违反极线几何，导致策略性能下降；而 GenSplat 通过 3D 约束确保了动作映射的准确性。
*   **数据效率饱和点**：研究发现，为每条轨迹合成约 3 个新视角即可使性能趋于饱和，过多的合成视角边际收益递减。

### 7. 优点
*   **高效性**：前馈式架构避免了传统 3DGS 繁重的逐场景优化过程，适合大规模数据增强。
*   **无需校准**：能够处理未校准的相机输入，极大降低了在实际机器人部署中的应用门槛。
*   **高质量生成**：引入 3D 先验蒸馏有效解决了稀疏视角下 3DGS 容易出现的“漂浮物”和几何坍缩问题。

### 8. 不足与局限
*   **手眼相机集成**：目前主要针对外部固定视角进行增强，尚未充分整合动态的手腕相机（Wrist Camera）观测。
*   **依赖先验模型**：GenSplat 的性能上限受限于所使用的 3D 基础模型的准确性。
*   **静态假设**：虽然是逐帧处理，但对于操作过程中物体剧烈形变或极高速运动的场景，其几何一致性的保持能力仍有待进一步验证。

（完）
