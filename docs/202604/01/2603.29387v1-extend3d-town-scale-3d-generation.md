---
title: "Extend3D: Town-Scale 3D Generation"
title_zh: Extend3D：城镇规模的 3D 生成
authors: "Seungwoo Yoon, Jinmo Kim, Jaesik Park"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29387v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 从单张图像生成3D场景
tldr: Extend3D是一个无需训练的单图生成城镇级3D场景的框架。它通过在x和y方向扩展物体中心生成模型的潜空间，并将其划分为重叠块进行协同生成，解决了大场景表示难题。该方法利用单目深度估计的点云先验进行初始化，并通过SDEdit技术（under-noising）修复遮挡区域，同时引入3D感知优化目标以提升几何与纹理质量，实现了高质量的大尺度3D场景重建。
selection_source: fresh_fetch
motivation: 现有的物体中心3D生成模型由于潜空间大小固定，难以直接应用于表示和生成城镇规模的广阔3D场景。
method: 通过扩展潜空间并划分为重叠块进行协同生成，结合点云先验初始化和基于SDEdit的迭代细化及3D感知优化。
result: 定量实验和人类偏好调查均表明，该方法在生成大尺度场景的几何结构和纹理保真度方面显著优于现有方法。
conclusion: Extend3D证明了通过扩展和优化物体中心模型的潜空间，可以实现高质量、无需训练的城镇级3D场景生成。
---

## 摘要
在本文中，我们提出了 Extend3D，这是一个基于以物体为中心的 3D 生成模型、无需训练的单图 3D 场景生成流水线。为了克服以物体为中心的模型在表示宽阔场景时固定大小潜空间的局限性，我们在 x 和 y 方向上扩展了潜空间。随后，通过将扩展的潜空间划分为重叠的块（patches），我们在每个时间步将以物体为中心的 3D 生成模型应用于每个块并进行耦合。由于带有图像条件的块级 3D 生成需要图像与潜空间块之间严格的空间对齐，我们利用单目深度估计器的点云先验初始化场景，并通过 SDEdit 迭代细化遮挡区域。我们发现，在 3D 细化过程中将 3D 结构的不完整性视为噪声，可以通过一个我们称之为“欠去噪”（under-noising）的概念实现 3D 补全。此外，为了解决以物体为中心的模型在子场景生成中的次优性，我们在去噪过程中优化扩展的潜变量，确保去噪轨迹与子场景动力学保持一致。为此，我们引入了 3D 感知优化目标，以提升几何结构和纹理保真度。人类偏好和定量实验证明，我们的方法比以往的方法取得了更好的结果。

## Abstract
In this paper, we propose Extend3D, a training-free pipeline for 3D scene generation from a single image, built upon an object-centric 3D generative model. To overcome the limitations of fixed-size latent spaces in object-centric models for representing wide scenes, we extend the latent space in the $x$ and $y$ directions. Then, by dividing the extended latent space into overlapping patches, we apply the object-centric 3D generative model to each patch and couple them at each time step. Since patch-wise 3D generation with image conditioning requires strict spatial alignment between image and latent patches, we initialize the scene using a point cloud prior from a monocular depth estimator and iteratively refine occluded regions through SDEdit. We discovered that treating the incompleteness of 3D structure as noise during 3D refinement enables 3D completion via a concept, which we term under-noising. Furthermore, to address the sub-optimality of object-centric models for sub-scene generation, we optimize the extended latent during denoising, ensuring that the denoising trajectories remain consistent with the sub-scene dynamics. To this end, we introduce 3D-aware optimization objectives for improved geometric structure and texture fidelity. We demonstrate that our method yields better results than prior methods, as evidenced by human preference and quantitative experiments.

---

## 论文详细总结（自动生成）

以下是对论文《Extend3D: Town-Scale 3D Generation》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 3D 生成模型（如 Trellis, Hunyuan3D）大多是“以物体为中心”（Object-centric）的，受限于固定的潜空间（Latent Space）尺寸。当尝试生成大规模场景（如城镇）时，固定维度的潜空间会导致分辨率不足、结果模糊，且缺乏处理复杂多物体排列的能力。
*   **研究动机**：
    *   **数据稀缺**：高质量的大规模 3D 场景数据集极少，难以直接训练场景级生成模型。
    *   **扩展性差**：现有方法（如外扩生成 Outpainting）在处理大尺度场景时容易出现接缝不连续和几何畸变。
    *   **对齐难题**：单图生成 3D 场景需要严格的 2D-3D 空间对齐，而物体中心模型往往会忽略全局场景的动力学特征（如地面消失、物体随机旋转）。

### 2. 方法论
Extend3D 提出了一种**无需训练（Training-free）**的流水线，核心思想是将物体中心模型的能力扩展到无限的场景空间：
*   **潜空间扩展与重叠块流（Overlapping Patch-wise Flow）**：
    *   在 X 和 Y 方向扩展潜空间，并将其划分为多个重叠的块（Patches）。
    *   在每个去噪步，对每个块并行应用物体中心生成模型，并在重叠区域进行平均耦合，确保块与块之间的平滑过渡。
*   **基于先验的初始化与迭代 SDEdit**：
    *   利用单目深度估计器（MoGe-2）从输入图像提取点云作为初始结构。
    *   **欠去噪（Under-noising）策略**：在 SDEdit 细化过程中，设置去噪步长大于加噪步长，使模型将点云中的空缺（遮挡区域）视为噪声并进行补全。
    *   通过迭代应用该过程，逐步修复遮挡区域并完善场景几何。
*   **3D 感知优化目标（3D-aware Optimization）**：
    *   **稀疏结构优化**：在去噪过程中优化向量场，确保初始化的点云特征不消失。
    *   **结构化潜变量优化**：引入可微分渲染，通过计算渲染图与原图的感知损失（LPIPS）和结构相似性（SSIM）来实时修正潜变量，提升纹理保真度并消除接缝。

### 3. 实验设计
*   **数据集/场景**：
    *   使用来自 Google Earth、UrbanScene3D、CarlaSC 的真实场景图像。
    *   使用 Flux.1 和 ChatGPT 生成的各种风格（如赛博朋克、中世纪、绿洲）的合成图像。
*   **基准测试（Benchmark）**：
    *   **外观评估**：LPIPS、SSIM、PSNR（针对 100 张多样化场景图像）。
    *   **几何评估**：倒角距离（CD）、F-score（针对 UrbanScene3D 的 45 对图像与真值模型）。
    *   **文本一致性**：CLIP Score、HPSv3、Intra-LPIPS（衡量块间一致性）。
*   **对比方法**：
    *   物体中心模型：Trellis, Hunyuan3D-2.1。
    *   场景级模型：EvoScene, SynCity。

### 4. 资源与算力
*   **硬件**：使用 NVIDIA RTX Pro 6000 GPU。
*   **显存占用**：
    *   在扩展因子 $a=b=2$（4倍面积）且开启所有优化时，峰值显存为 **28GB**。
    *   若关闭结构化潜变量优化，显存可降至 14GB。
    *   对于更大规模场景（$a=b=6$），不开启优化时需 61GB 显存。
*   **耗时**：生成一个典型场景（$a=b=2, d=4$）约需 **14.1 分钟**，显著快于 EvoScene（35.3 min）和 SynCity（52.0 min）。

### 5. 实验数量与充分性
*   **实验规模**：进行了 100 组外观定量实验、45 组几何定量实验，以及针对 14 个场景的 10 人人类偏好调查。
*   **消融实验**：详细测试了重叠块划分因子 $d$、初始化先验、欠去噪参数、以及两阶段优化目标的有效性。
*   **充分性评价**：实验覆盖了从室内桌面到城镇规模的多种尺度，对比了当前最先进的开源模型，数据量适中且评估维度全面（几何+纹理+人类主观），论证较为客观公平。

### 6. 主要结论与发现
*   **扩展性**：通过扩展潜空间和重叠块耦合，物体中心模型可以成功生成比原始训练尺度大 36 倍甚至更多的 3D 场景。
*   **欠去噪的有效性**：证明了将 3D 结构的不完整性视为噪声是解决单目深度估计遮挡问题的有效手段。
*   **优化必要性**：实时优化向量场能有效防止物体中心模型在生成子场景时出现的“地面消失”或“物体漂移”现象。
*   **性能领先**：在几何保真度、图像忠实度和视觉美感上均显著优于现有的训练和非训练流水线。

### 7. 优点
*   **无需训练**：直接利用现有的高性能物体生成模型，避开了大规模 3D 场景数据匮乏的问题。
*   **高质量补全**：提出的“欠去噪”概念为 3D 补全提供了一个简洁而有效的视角。
*   **无缝衔接**：重叠块流结合实时渲染优化，解决了大场景生成中常见的接缝和不连续问题。

### 8. 不足与局限
*   **遮挡补全不彻底**：在某些极端复杂的室内场景中，遮挡区域的几何补全仍可能不完整。
*   **计算开销**：虽然比同类场景模型快，但相比物体中心模型仍需较长的推理时间和较高的显存（尤其是开启渲染优化时）。
*   **视角限制**：在处理街景级别（Street-level）图像时表现有限，主要是因为透视导致的 X/Y 坐标尺度严重失配（消失点问题）。

（完）
