---
title: "Few TensoRF: Enhance the Few-shot on Tensorial Radiance Fields"
title_zh: Few TensoRF：增强张量辐射场上的少样本性能
authors: "Thanh-Hai Le, Hoang-Hau Tran, Trong-Nghia Vu"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25008v3"
tags: ["query:3dgen"]
score: 9.0
evidence: 针对稀疏输入视图的3D重建框架
tldr: Few TensoRF 结合了 TensoRF 的高效张量表示与 FreeNeRF 的频率驱动正则化，旨在解决稀疏视角下的 3D 重建难题。通过引入频率掩码和遮挡掩码，该方法在保持 10-15 分钟快速训练的同时，显著提升了重建质量和稳定性。在 NeRF 合成数据集和 THuman 2.0 人体数据集上的实验证明，其在少样本场景下具有卓越的性能和效率。
selection_source: fresh_fetch
motivation: 针对 TensoRF 在稀疏输入视角下重建质量下降和不稳定的问题，寻求一种高效且稳健的少样本 3D 重建方案。
method: 将 TensoRF 的张量分解表示与 FreeNeRF 的频率正则化及遮挡掩码技术相结合，优化稀疏视角下的辐射场学习。
result: 在 NeRF 合成数据集上将平均 PSNR 从 21.45 dB 提升至最高 24.52 dB，且在人体重建任务中仅需 8 张图像即可达到高精度。
conclusion: Few TensoRF 是一种兼顾训练效率与重建精度的少样本 3D 重建框架，适用于多种复杂场景的实时应用。
---

## 摘要
本文提出了 Few TensoRF，这是一个结合了 TensoRF 高效的基于张量的表示与 FreeNeRF 频率驱动的少样本正则化的三维重建框架。该方法利用 TensoRF 显著加快了渲染速度，并通过引入频率掩码和遮挡掩码，提高了在稀疏输入视角下的稳定性和重建质量。在 Synthesis NeRF 基准测试上的实验表明，Few TensoRF 方法将平均峰值信噪比（PSNR）从 21.45 dB（TensoRF）提升至 23.70 dB，微调版本更是达到了 24.52 dB，同时保持了 TensoRF 约 10-15 分钟的快速训练时间。在 THuman 2.0 数据集上的实验进一步证明了其在人体重建方面的竞争性能，仅需 8 张输入图像即可达到 27.37 - 34.00 dB。这些结果凸显了 Few TensoRF 是一个针对多种场景的实时三维重建的高效且数据利用率高的解决方案。

## Abstract
This paper presents Few TensoRF, a 3D reconstruction framework that combines TensorRF's efficient tensor based representation with FreeNeRF's frequency driven few shot regularization. Using TensorRF to significantly accelerate rendering speed and introducing frequency and occlusion masks, the method improves stability and reconstruction quality under sparse input views. Experiments on the Synthesis NeRF benchmark show that Few TensoRF method improves the average PSNR from 21.45 dB (TensorRF) to 23.70 dB, with the fine tuned version reaching 24.52 dB, while maintaining TensorRF's fast \(\approx10-15\) minute training time. Experiments on the THuman 2.0 dataset further demonstrate competitive performance in human body reconstruction, achieving 27.37 - 34.00 dB with only eight input images. These results highlight Few TensoRF as an efficient and data effective solution for real-time 3D reconstruction across diverse scenes.

---

## 论文详细总结（自动生成）

以下是对论文《Few TensoRF: Enhance the Few-shot on Tensorial Radiance Fields》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的神经辐射场（NeRF）在处理**稀疏输入视图（Few-shot）**时表现不佳，容易产生高频伪影和不稳定的几何结构；而现有的改进方法（如 FreeNeRF）虽然解决了少样本问题，但训练速度较慢。
*   **研究动机**：旨在结合 **TensoRF**（基于张量分解的高效表示，训练快、内存省）与 **FreeNeRF**（基于频率驱动的正则化，擅长处理少样本）的优势，开发一种既能快速训练又能高效处理稀疏视角的三维重建框架。

### 2. 方法论
Few TensoRF 在 TensoRF 的基础上引入了三种关键的正则化技术：
*   **核心思想**：通过控制模型在训练初期对高频信息的敏感度，引导模型先学习低频的粗糙结构，再逐步细化高频细节，从而避免在稀疏视角下的过拟合。
*   **关键技术细节**：
    1.  **张量组件频率掩码（Frequency Masking Tensor Components）**：对密度组件（$A_\sigma$）和外观颜色组件（$A_c$）应用动态频率掩码。在训练初期屏蔽高频部分，随迭代次数增加逐渐释放高频位。
    2.  **外观网格频率掩码（Frequency Masking Appearance Grid）**：对外观特征网格 $G_c$ 和视角方向 $d$ 的位置编码应用类似的频率过滤，减轻 MLP 神经网络对高频信号的过拟合。
    3.  **遮挡正则化（Occlusion Regularization）**：引入遮挡损失，将靠近摄像机区域的“浮空物（floaters）”或“墙”状伪影的密度推向零，强制模型在更远、更合理的空间解释场景。

### 3. 实验设计
*   **数据集**：
    *   **Synthesis NeRF (Blender)**：包含 Lego、Chair、Drums 等 8 个标准场景，用于评估少样本下的合成质量。
    *   **THuman 2.0**：复杂的人体扫描数据集，用于测试模型在处理复杂几何形状（如衣服褶皱、人体姿态）时的泛化能力。
*   **Benchmark 与对比方法**：
    *   对比了原始的 **TensoRF**（基准）和复现的 **FreeNeRF**（少样本 SOTA）。
    *   评估指标主要采用 **PSNR**（峰值信噪比）。

### 4. 资源与算力
*   **硬件平台**：使用 Google Colab 上的 **NVIDIA T4 Tesla GPU**。
*   **训练参数**：Ray batch size 为 1024，总迭代次数为 15,000 次。
*   **训练时长**：每个场景仅需 **10-15 分钟**，相比 FreeNeRF（50k 迭代需约 5 小时）显著缩短。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 Synthesis NeRF 的 8 个场景上进行了完整对比。
    *   在 THuman 2.0 数据集上选择了 2 个代表性对象（0300 和 0525）进行 8 视图输入测试。
*   **充分性评价**：实验在标准数据集上表现充分，对比了不同迭代次数（15k vs 50k）和不同输入视角数量（3、8、10、50 图）。但在人体数据集上仅测试了 2 个对象（总数 500 个），样本量略显不足，且缺乏消融实验来量化三个改进点各自的贡献。

### 6. 主要结论与发现
*   **性能提升**：在 Synthesis NeRF 上，Few TensoRF 将平均 PSNR 从 TensoRF 的 21.45 dB 提升至 23.70 dB（微调后达 24.52 dB）。
*   **效率优势**：在保持与 TensoRF 相当的极速训练时间（约 15 分钟）的同时，渲染质量达到了甚至超过了训练数小时的 FreeNeRF。
*   **人体建模**：在仅有 8 张输入图像的情况下，能够重建出具有竞争力的 3D 人体模型（PSNR 达 27-34 dB），尽管在细节处仍有噪声。

### 7. 优点
*   **高效性**：成功实现了“快速训练”与“少样本鲁棒性”的平衡，是实时 3D 重建的理想方案。
*   **即插即用**：频率掩码和遮挡正则化逻辑简单，易于集成到现有的基于张量的辐射场框架中。
*   **视觉质量**：相比 FreeNeRF，该方法在保留细节方面表现更好，减少了过度平滑（模糊）的现象。

### 8. 不足与局限
*   **特定场景失效**：在极其复杂的场景（如 Drums）中，模型仍会产生奇怪的伪影，说明对复杂遮挡关系的理解仍有提升空间。
*   **人体细节噪声**：在 THuman 2.0 实验中，虽然 PSNR 较高，但生成的 3D 渲染图存在明显噪声，且 3D 建模（Mesh）的稳定性不如多视图输入。
*   **评估范围有限**：人体数据集的测试覆盖面较窄，未能全面验证在不同体型、服装和姿态下的稳健性。

（完）
