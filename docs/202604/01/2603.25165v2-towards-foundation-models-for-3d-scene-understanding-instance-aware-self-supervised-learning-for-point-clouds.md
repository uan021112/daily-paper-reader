---
title: "Towards Foundation Models for 3D Scene Understanding: Instance-Aware Self-Supervised Learning for Point Clouds"
title_zh: 迈向3D场景理解的基础模型：点云的实例感知自监督学习
authors: "Bin Yang, Mohamed Abdelsamad, Miao Zhang, Alexandru Paul Condurache"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25165v2"
tags: ["query:partseg"]
score: 9.0
evidence: 点云的实例感知自监督学习
tldr: 针对现有3D点云自监督学习在实例定位任务上表现不佳的问题，本文提出PointINS框架。该框架通过引入正交偏移分支，结合几何感知学习，增强了点云表示的实例感知能力。通过偏移分布正则化（ODR）和空间聚类正则化（SCR）策略，PointINS在室内外多个数据集上显著提升了实例分割和全景分割性能，为构建通用的3D基础模型奠定了基础。
selection_source: fresh_fetch
motivation: 现有的3D自监督学习方法侧重于语义一致性，缺乏对实例定位至关重要的几何感知能力。
method: 提出PointINS框架，通过正交偏移分支学习几何推理，并利用ODR和SCR两种正则化策略强化偏移预测的准确性与局部一致性。
result: "在五个数据集上的实验表明，该方法使室内实例分割mAP平均提升3.5%，室外全景分割PQ平均提升4.1%。"
conclusion: PointINS成功弥补了3D自监督学习中实例感知的短板，是迈向支持全任务3D基础模型的重要一步。
---

## 摘要
近期点云自监督学习（SSL）的进展在无需人工标注的情况下显著提升了3D场景理解能力。现有方法通过强制增强视图间的特征一致性或通过掩码场景建模来强调语义感知。然而，由此产生的表示在实例定位方面的迁移效果较差，且通常需要全量微调才能获得较强的性能。实例感知是3D感知的基本组成部分，因此弥合这一差距对于迈向支持3D数据所有下游任务的真正3D基础模型至关重要。在这项工作中，我们介绍了 PointINS，这是一个面向实例的自监督框架，通过几何感知学习丰富了点云表示。PointINS 采用正交偏移分支来共同学习高层语义理解和几何推理，从而实现实例感知。我们确定了对于鲁棒实例定位至关重要的两个一致属性，并将它们制定为互补的正则化策略：偏移分布正则化（ODR），将预测的偏移与经验观察到的几何先验对齐；以及空间聚类正则化（SCR），通过使用伪实例掩码正则化偏移来强制局部连贯性。通过在五个数据集上的广泛实验，PointINS 在室内实例分割方面平均实现了 +3.5% 的 mAP 提升，在室外全景分割方面实现了 +4.1% 的 PQ 增益，为可扩展的 3D 基础模型铺平了道路。

## Abstract
Recent advances in self-supervised learning (SSL) for point clouds have substantially improved 3D scene understanding without human annotations. Existing approaches emphasize semantic awareness by enforcing feature consistency across augmented views or by masked scene modeling. However, the resulting representations transfer poorly to instance localization, and often require full finetuning for strong performance. Instance awareness is a fundamental component of 3D perception, thus bridging this gap is crucial for progressing toward true 3D foundation models that support all downstream tasks on 3D data. In this work, we introduce PointINS, an instance-oriented self-supervised framework that enriches point cloud representations through geometry-aware learning. PointINS employs an orthogonal offset branch to jointly learn high-level semantic understanding and geometric reasoning, yielding instance awareness. We identify two consistent properties essential for robust instance localization and formulate them as complementary regularization strategies, Offset Distribution Regularization (ODR), which aligns predicted offsets with empirically observed geometric priors, and Spatial Clustering Regularization (SCR), which enforces local coherence by regularizing offsets with pseudo-instance masks. Through extensive experiments across five datasets, PointINS achieves on average +3.5% mAP improvement for indoor instance segmentation and +4.1% PQ gain for outdoor panoptic segmentation, paving the way for scalable 3D foundation models.

---

## 论文详细总结（自动生成）

### 论文总结：Towards Foundation Models for 3D Scene Understanding (PointINS)

#### 1. 核心问题与整体含义（研究动机和背景）
现有的3D点云自监督学习（SSL）方法（如对比学习或掩码建模）主要侧重于学习**语义一致性**，即通过增强视图间的特征对齐来捕捉类别信息。然而，这些方法往往忽略了**实例感知（Instance Awareness）**，导致模型在实例分割和全景分割等需要精确几何定位的任务中表现不佳，通常需要全量微调才能达到理想效果。
本文的研究动机是弥合语义不变性与几何敏感性之间的鸿沟，提出一个能够同时学习语义和实例特征的框架，从而推动开发支持全任务的3D基础模型。

#### 2. 方法论：核心思想与关键技术
论文提出了 **PointINS** 框架，其核心思想是在传统的教师-学生自监督架构中引入一个**正交偏移（Offset）分支**，专门用于预测点到其所属实例几何中心的向量。

*   **双分支架构**：
    *   **语义分支**：采用基于原型的自蒸馏机制，学习类别级的语义特征。
    *   **偏移分支**：预测每个点的3D偏移向量，捕捉点与实例中心的关系。
*   **关键正则化策略**：
    *   **偏移分布正则化 (ODR)**：针对无标签预测易崩溃的问题，利用概率积分变换（PIT）将预测的偏移量强制对齐到经验几何先验（如偏移幅值呈长尾分布，方向在球面上均匀分布）。
    *   **空间聚类正则化 (SCR)**：利用模型早期学习到的语义特征进行K-means聚类，并结合空间邻域（BFS算法）生成“伪实例掩码”，强制局部点云向共同的质心收敛。
*   **自蒸馏损失**：学生网络通过 $\ell_1$ 损失和余弦相似度损失，学习教师网络经过 ODR 和 SCR 优化后的偏移目标。

#### 3. 实验设计
*   **数据集/场景**：
    *   **室内**：ScanNet (v2), ScanNet200, S3DIS (Area 5)。
    *   **室外**：nuScenes, SemanticKITTI。
*   **评估协议（Benchmark）**：
    *   **线性探测 (Linear Probing)**：冻结主干网络，仅训练分类/偏移头。
    *   **解码器探测 (Decoder Probing)**：训练标准解码器。
    *   **全量微调 (Full Finetuning)**。
*   **对比方法**：包括 SegContrast, PSA, NOMAE, Sonata, DOS 等最先进的3D SSL方法。

#### 4. 资源与算力
*   **硬件**：使用了 NVIDIA **H200 GPU**。
*   **配置**：单数据集预训练使用1张 H200；多数据集大规模预训练使用2张 H200（DDP模式）。
*   **时长**：PointINS 相比基准方法（DOS）增加了约 25% 的计算开销。例如，在 ScanNet 上预训练约需 25 小时，在 nuScenes 上约需 29 小时。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了5个主流数据集，进行了语义、实例、全景分割三种任务的评估。
*   **消融实验**：详细分析了 ODR、SCR 的各自贡献、损失权重 $\lambda_{off}$、聚类数 $K$、预热比例等超参数的影响。
*   **充分性与公平性**：实验对比了不同标注比例（0.1%, 1%, 10%）下的性能，并验证了框架在不同 SSL 基础模型（如 PSA, Sonata）上的通用性。对比实验均采用相同的 PTv3 主干网络，确保了比较的客观性。

#### 6. 主要结论与发现
*   **性能领先**：PointINS 在室内实例分割 mAP 上平均提升 3.5%，在室外全景分割 PQ 上平均提升 4.1%，显著优于现有 SSL 方法。
*   **标签效率**：在仅有 0.1% 标注的情况下，PointINS 的全景分割性能（PQ）比全监督方法高出 10% 以上。
*   **几何推理能力**：可视化结果证明，经过 ODR 和 SCR 正则化后，模型预测的质心更加集中且符合物理逻辑，证明了其具备了初步的几何推理能力。

#### 7. 优点
*   **创新性**：首次在 3D SSL 中显式引入实例偏移学习，打破了以往 SSL 只关注语义的局限。
*   **鲁棒性**：ODR 策略对先验分布的选择具有很强的鲁棒性，即使使用粗略的聚类估计（如 HDBSCAN）也能获得良好效果。
*   **兼容性**：作为一个模块化设计，PointINS 可以轻松集成到现有的教师-学生自监督框架中。

#### 8. 不足与局限
*   **性能差距**：尽管在线性探测下表现优异，但与全监督（Fully Supervised）性能之间仍存在一定差距。
*   **计算开销**：引入偏移分支和复杂的正则化计算（如 K-means 和 BFS）增加了预训练的时间成本。
*   **维度局限**：目前主要针对静态 3D 场景，尚未扩展到包含时序信息的 4D 几何推理。

（完）
