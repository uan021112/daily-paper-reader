---
title: "Agentic Video Generation: From Text to Executable Event Graphs via Tool-Constrained LLM Planning"
title_zh: 代理式视频生成：通过工具约束的大语言模型规划实现从文本到可执行事件图的转换
authors: "Nicolae Cudlenco, Mihai Masala, Marius Leordeanu"
date: 2026-04-11
pdf: "https://arxiv.org/pdf/2604.10383v1"
tags: ["query:3dgen"]
score: 7.0
evidence: 通过3D游戏引擎执行的程序化3D内容生成
tldr: 本研究提出一种基于智能体的视频生成系统，通过LLM构建时空事件图（GEST）并由3D引擎执行，解决了传统神经视频生成语义不可靠的问题。该系统采用分层架构，将叙事规划与物理约束分离，通过工具约束的LLM规划确保生成规格的可执行性，在物理真实性和语义对齐方面显著优于现有神经生成模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.10383v1/fig-001.webp\", \"caption\": \"Figure 2. Example agentic story. Top: GEST subgraph for Marcus (red) and Sarah (blue) across two scenes. Director’s narrative: “Two old friends reunite [. . . ] Marcus grabs drinks, they chat and laugh, Marcus smokes while Sarah calls a friend, they share a hug.” Middle: Scene 1 frames. Bottom: Scene 2 frames. Post-simulation VDG: “A man answers a call [. . . ] chats with a woman [. . . ] grabs a drink [. . . ] they share a hug and a laugh.”\", \"page\": 7, \"index\": 1, \"width\": 1032, \"height\": 610}, {\"url\": \"assets/figures/arxiv/2604.10383v1/fig-002.webp\", \"caption\": \"Table 1. Comparison with LLM-guided video generation systems. Adapted and extended from [17]. MA = multi-agent, HD = hierarchical delegation, EG = environment grounding, SR = structured representation, ET = execution target, DT = deterministic, GT = ground truth, MN = multi-actor narratives.\", \"page\": 3, \"index\": 2, \"width\": 513, \"height\": 353}, {\"url\": \"assets/figures/arxiv/2604.10383v1/fig-003.webp\", \"caption\": \"Figure 1. System architecture. The Director Agent explores the simulation world and plans the story, delegating individual scenes to the Scene Builder Subagent. Both agents interact through Exploration Tools (read-only) and Building Tools (constraintenforcing), which operate on the State Backend—the procedural generator repurposed as a tool layer.\", \"page\": 4, \"index\": 3, \"width\": 496, \"height\": 466}]"
motivation: 现有的神经视频生成系统虽然视觉效果出色，但往往存在语义不可靠、物理逻辑错误以及缺乏底层事实标注等问题。
method: 提出一种分层双智能体架构，利用LLM进行叙事规划，并通过程序化后端工具调用强制执行模拟器约束，生成可由3D引擎执行的结构化事件图。
result: "该系统在物理有效性（58%）和语义对齐评分（3.75/5）上显著超越了VEO 3.1和WAN 2.2等主流神经视频生成模型。"
conclusion: 通过将LLM的自然语言推理与3D引擎的确定性执行相结合，可以实现比纯神经生成方法更具语义一致性和物理真实性的视频创作。
---

## 摘要
现有的多智能体视频生成系统利用大语言模型（LLM）智能体编排神经视频生成器，虽然能产生视觉效果出众的输出，但在语义上并不可靠，且缺乏真值标注。我们提出了一种反转该范式的代理系统：LLM 不再生成像素，而是构建一个正式的时空事件图（GEST）——这是一种包含角色、动作、物体和时间约束的结构化规范——随后在 3D 游戏引擎中确定性地执行。传统的阶段式 LLM 细化流水线在这一任务上完全失败（50 次尝试中无一能生成可执行规范），这促使我们采用一种基于关注点分离的根本不同的架构：LLM 通过自然语言推理处理叙事规划，而程序化状态后端通过经过验证的工具调用强制执行所有模拟器约束，从而保证生成的每个规范在构造上都是可执行的。该系统采用分层双智能体架构——负责规划故事的“导演”和通过基于回合的状态机构建单个场景的“场景构建器”——并配备专门的“关系子代理”来填充 GEST 形式化中程序化生成留空的逻辑和语义边类型，使其成为首个充分发挥该表示法表达能力的方法。我们分两个阶段进行评估：首先是针对程序化基准的自主生成评估，通过 3 模型 LLM 评审团，代理式叙事在文本对比中胜出 79%，在视频对比中胜出 74%；其次是种子生成评估，将相同的文本输入我们的系统、VEO 3.1 和 WAN 2.2，人工标注显示引擎生成的视频在物理有效性（58% 对 25% 和 20%）和语义一致性（3.75/5 对 2.33 和 1.50）上显著优于神经生成器。

## Abstract
Existing multi-agent video generation systems use LLM agents to orchestrate neural video generators, producing visually impressive but semantically unreliable outputs with no ground truth annotations. We present an agentic system that inverts this paradigm: instead of generating pixels, the LLM constructs a formal Graph of Events in Space and Time (GEST) -- a structured specification of actors, actions, objects, and temporal constraints -- which is then executed deterministically in a 3D game engine. A staged LLM refinement pipeline fails entirely at this task (0 of 50 attempts produce an executable specification), motivating a fundamentally different architecture based on a separation of concerns: the LLM handles narrative planning through natural language reasoning, while a programmatic state backend enforces all simulator constraints through validated tool calls, guaranteeing that every generated specification is executable by construction. The system uses a hierarchical two-agent architecture -- a Director that plans the story and a Scene Builder that constructs individual scenes through a round-based state machine -- with dedicated Relation Subagents that populate the logical and semantic edge types of the GEST formalism that procedural generation leaves empty, making this the first approach to exercise the full expressive capacity of the representation. We evaluate in two stages: autonomous generation against procedural baselines via a 3-model LLM jury, where agentic narratives win 79% of text and 74% of video comparisons; and seeded generation where the same text is given to our system, VEO 3.1, and WAN 2.2, with human annotations showing engine-generated videos substantially outperform neural generators on physical validity (58% vs 25% and 20%) and semantic alignment (3.75/5 vs 2.33 and 1.50).