---
title: "TreeGaussian: Tree-Guided Cascaded Contrastive Learning for Hierarchical Consistent 3D Gaussian Scene Segmentation and Understanding"
title_zh: TreeGaussian：用于层次一致性 3D 高斯场景分割与理解的树引导级联对比学习
authors: "Jingbin You, Zehao Li, Hao Jiang, Xinzhu Ma, Shuqin Gao, Honglong Zhao, Congcong Zheng, Tianlu Mao, Feng Dai, Yucheng Zhang, Zhaoqi Wang"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.03309v1"
tags: ["query:partseg"]
score: 9.0
evidence: 分层3D高斯场景分割与整体-部分关系
tldr: 针对3D高斯泼溅（3DGS）在处理复杂场景层级语义结构和整体-局部关系方面的不足，本文提出了TreeGaussian框架。该框架通过构建多级对象树来显式建模层级关系，并采用两阶段级联对比学习策略，从全局到局部逐步优化特征表示。结合一致性分割检测和图去噪模块，TreeGaussian显著提升了3D场景分割的层级一致性和质量，在开放词汇目标选择等任务中表现优异。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.03309v1/fig-001.webp\", \"caption\": \"Fig. 2: Overview of our method. (a) Constructing object-tree from multi-view images using SAM to capture structured relationships between object parts and wholes. (b) Two-stage cascaded contrastive learning strategy to progressively optimize the instance feature of each Gaussian point. (c) Graph-based denoising is applied to each languagemapped instance cluster for improving the multi-view rendering quality.\", \"page\": 6, \"index\": 1, \"width\": 714, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2604.03309v1/fig-002.webp\", \"caption\": \"Fig. 5: Qualitative comparison of rendered local objects. Our method produces cleaner and more accurate segmentation results compared to baselines, effectively reducing noise and preserving fine-grained structures.\", \"page\": 11, \"index\": 2, \"width\": 716, \"height\": 288}, {\"url\": \"assets/figures/arxiv/2604.03309v1/fig-003.webp\", \"caption\": \"Fig. 1: Motivation illustration. (a) Flat contrastive learning isolates feature spaces for object wholes and parts, limiting their hierarchical interaction. (b) Fused contrastive learning merges feature spaces but suffers from oversaturation and instability due to dense pairwise comparisons. (c) Cascaded contrastive learning (our method) preserves semantic hierarchy while minimizing contrastive redundancy, enabling efficient and stable optimization. Solid arrows indicate implicit contrastive interactions among clusters; dashed arrows denote explicit hierarchical indexing relationships.\", \"page\": 2, \"index\": 3, \"width\": 637, \"height\": 302}, {\"url\": \"assets/figures/arxiv/2604.03309v1/fig-004.webp\", \"caption\": \"Table 1: Quantitative comparison of open-vocabulary 3D object selection on the Lerf_ovs dataset. We report mIoU(%) and mAcc@0.25(%) scores.\", \"page\": 10, \"index\": 4, \"width\": 723, \"height\": 286}, {\"url\": \"assets/figures/arxiv/2604.03309v1/fig-005.webp\", \"caption\": \"Fig. 3: Consistent Segmentation Detection (CSD) for local contrastive learning. The blue curve shows the raw Split Number and the red curve shows the smoothed reference across views. Views where the blue curve lies above the red reference are treated as over segmentation (apply only L2 pull), while views where it lies below are treated as under segmentation (apply only L2 push). When the blue curve is close to the reference, we treat it as optimal segmentation (apply L2 pull + L2 push).\", \"page\": 8, \"index\": 5, \"width\": 712, \"height\": 200}]"
motivation: 现有的3DGS方法难以捕捉复杂场景中的层级语义结构和整体与局部的关系，且面临对比学习冗余和标签不一致的问题。
method: 提出一种树引导的级联对比学习框架，通过多级对象树建模层级关系，并利用两阶段级联策略、一致性分割检测及图去噪模块优化特征。
result: 实验证明该方法在开放词汇3D对象选择和点云理解任务中取得了显著的性能提升，并增强了分割的鲁棒性。
conclusion: TreeGaussian通过结构化学习和级联优化，有效解决了3DGS在层级场景理解中的一致性与精度问题。
---

## 摘要
3D 高斯泼溅（3DGS）已成为神经场景理解的一种实时、可微表示。然而，现有的基于 3DGS 的方法难以表示层次化的 3D 语义结构，并捕捉复杂场景中的整体与部分关系。此外，密集的成对比较和来自 2D 先验的不一致层次标签阻碍了特征学习，导致分割效果不佳。为了解决这些局限性，我们引入了 TreeGaussian，这是一个树引导的级联对比学习框架，它显式地建模了层次语义关系并减少了对比监督中的冗余。通过构建多级对象树，TreeGaussian 实现了跨对象-部分层次结构的结构化学习。此外，我们提出了一种两阶段级联对比学习策略，从全局到局部逐步细化特征表示，从而缓解饱和并稳定训练。进一步引入了一致性分割检测（CSD）机制和基于图的去噪模块，以对齐跨视图的分割模式，同时抑制不稳定的高斯点，增强分割的一致性和质量。大量的实验，包括开放词汇 3D 对象选择、3D 点云理解和消融研究，证明了我们方法的有效性和鲁棒性。

## Abstract
3D Gaussian Splatting (3DGS) has emerged as a real-time, differentiable representation for neural scene understanding. However, existing 3DGS-based methods struggle to represent hierarchical 3D semantic structures and capture whole-part relationships in complex scenes. Moreover, dense pairwise comparisons and inconsistent hierarchical labels from 2D priors hinder feature learning, resulting in suboptimal segmentation. To address these limitations, we introduce TreeGaussian, a tree-guided cascaded contrastive learning framework that explicitly models hierarchical semantic relationships and reduces redundancy in contrastive supervision. By constructing a multi-level object tree, TreeGaussian enables structured learning across object-part hierarchies. In addition, we propose a two-stage cascaded contrastive learning strategy that progressively refines feature representations from global to local, mitigating saturation and stabilizing training. A Consistent Segmentation Detection (CSD) mechanism and a graph-based denoising module are further introduced to align segmentation modes across views while suppressing unstable Gaussian points, enhancing segmentation consistency and quality. Extensive experiments, including open-vocabulary 3D object selection, 3D point cloud understanding, and ablation studies, demonstrate the effectiveness and robustness of our approach.

---

## 论文详细总结（自动生成）

以下是对论文《TreeGaussian: Tree-Guided Cascaded Contrastive Learning for Hierarchical Consistent 3D Gaussian Scene Segmentation and Understanding》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的基于 3D 高斯泼溅（3DGS）的场景分割方法在处理复杂场景时，难以有效表示**层级语义结构**（如“整体-部分”关系）。
*   **研究动机**：
    *   **结构缺失**：现有的对比学习方法要么将实例视为孤立节点（Flat），要么简单融合特征空间（Fused），缺乏显式的层级建模。
    *   **特征饱和**：在复杂场景中，密集的成对对比会导致低维特征空间饱和，训练不稳定。
    *   **视图不一致**：来自 2D 分割先验（如 SAM）的标签在不同视角下存在尺度不一致（过分割或欠分割），导致 3D 监督信号冲突。

### 2. 方法论
TreeGaussian 提出了一套树引导的级联对比学习框架，核心技术包括：
*   **多级对象树构建**：利用 SAM 生成多尺度掩码，通过填充空隙、边缘清零、掩码裁剪等预处理，利用深度优先搜索（DFS）构建 2D 实例树，确保子节点完全包含在父节点内。
*   **两阶段级联对比学习**：
    *   **全局阶段**：在粗粒度尺度上学习判别性特征，通过 K-means 将场景划分为粗略对象节点。
    *   **局部阶段**：在每个粗略节点内部进行局部对比学习，仅在局部区域内区分细粒度零件，减少了对比冗余，缓解了特征空间饱和。
*   **一致性分割检测（CSD）**：
    *   计算各视图的“分裂数”（Split Number），并通过滑动窗口平滑处理。
    *   动态调整损失函数：若当前视图过分割，仅应用 `pull` 损失；若欠分割，仅应用 `push` 损失；若处于最佳状态，则同时应用两者。
*   **图基高斯去噪**：构建空间与特征感知的近邻图，剔除连接强度低的噪声点，并利用有向包围盒（OBB）恢复误删的有效点。

### 3. 实验设计
*   **数据集与场景**：
    *   **Lerf_ovs**：包含 figurines, teatime, ramen, waldo_kitchen 四个场景，具有像素级语义标注。
    *   **ScanNet-v2**：选取 10 个室内场景，涵盖 19/15/10 个语义类别。
*   **任务与 Benchmark**：
    *   开放词汇 3D 对象选择（Open-vocabulary 3D Object Selection）。
    *   3D 点云理解（3D Point Cloud Understanding）。
*   **对比方法**：包括 LangSplat, LEGaussians（语言蒸馏类）、OpenGaussian, InstanceGaussian（平坦对比学习类）、OmniSeg3D-GS（融合对比学习类）。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA A100 GPU**。
*   **训练时长**：
    *   标准 3DGS 预训练：30,000 步。
    *   Lerf_ovs：全局尺度训练 50,000 步，局部尺度训练 100,000 步。
    *   ScanNet-v2：各尺度训练 60,000 至 80,000 步不等。
*   **内存**：显存占用在 7.6GB 至 16.8GB 之间（随语义尺度增加而增加）。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流数据集上进行了定量评估，并提供了大量的定性可视化结果。
*   **消融实验**：针对局部损失（Llocal）、去噪模块、CSD 机制、L2-pull/push 损失分别做了详细的消融研究。
*   **鲁棒性测试**：额外进行了特征扰动实验，验证了位置感知聚类在减少误差累积方面的作用。
*   **评价**：实验设计全面，涵盖了从 2D 渲染质量到 3D 点云分类的多个维度，对比基准选择合理，实验结果具有较高的说服力和客观性。

### 6. 主要结论与发现
*   **性能领先**：TreeGaussian 在全尺度（Whole）和零件尺度（Part）的 mIoU 和 mAcc 指标上均显著优于现有方法。
*   **层级优势**：显式的层级建模能有效解决复杂场景中细小零件的特征塌陷问题。
*   **一致性提升**：CSD 机制有效缓解了多视图掩码不一致带来的训练震荡，使分割结果在空间上更连续。
*   **去噪效果**：图基去噪显著提升了实例簇的纯度，减少了渲染时的伪影和漂浮物。

### 7. 优点（亮点）
*   **结构化思维**：将复杂的场景分割问题转化为树状结构的级联优化，符合现实世界物体的物理构成。
*   **动态调整**：CSD 机制不强求所有视图标签完全一致，而是通过动态选择损失函数来“包容”2D 先验的缺陷。
*   **训练稳定**：通过局部化对比学习，解决了低维特征空间在处理大量实例时的饱和难题。

### 8. 不足与局限
*   **语义先验限制**：严重依赖 CLIP 模型，对于极小或语义模糊的零件，CLIP 提取的特征可能不够准确，导致分割失败。
*   **场景限制**：目前仅适用于**静态场景**，未考虑动态环境中的时序一致性。
*   **计算开销**：虽然推理是实时的，但构建对象树和多阶段训练增加了预处理和训练的时间成本。

（完）
