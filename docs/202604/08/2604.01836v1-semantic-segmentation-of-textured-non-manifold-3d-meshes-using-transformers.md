---
title: Semantic Segmentation of Textured Non-manifold 3D Meshes using Transformers
title_zh: 基于 Transformer 的带纹理非流形 3D 网格语义分割
authors: "Mohammadreza Heidarianbaei, Max Mehltretter, Franz Rottensteiner"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.01836v1"
tags: ["query:partseg"]
score: 9.0
evidence: 使用纹理感知 Transformer 对 3D 网格进行语义分割
tldr: 本研究针对纹理3D网格语义分割中几何与纹理信息融合不足的问题，提出了一种纹理感知Transformer模型。该模型直接从网格面的原始像素中学习，结合分层学习方案和两阶段Transformer块（TSTB），实现了局部与全局信息的有效聚合。在SUM城市网格基准和新创建的文化遗产数据集上，该方法显著优于现有技术，展示了在处理非流形复杂网格方面的卓越性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.01836v1/fig-001.webp\", \"caption\": \"Figure 3. Qualitative results achieved by our method and by the best-performing baseline, NoMeFormer. From left to right: Input, reference, our method, and NoMeFormer. Top: an example from SUM. Bottom: an example from CH. Colour code — SUM: T (blue), V (dark green), Bld (green), W (yellow), C (orange), B (red). Colour code — CH: ND (greenish blue), J (red), St (green), D (chartreuse green), LI (gold), BC (yellow), SE (orange), unlabelled (blue).\", \"page\": 8, \"index\": 1, \"width\": 486, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2604.01836v1/fig-002.webp\", \"caption\": \"Figure 1. Overview of the network architecture. The feature extraction branch generates one feature vector per face. The faces are clustered using K-means clustering. The tensor containing the feature vectors is reshaped so that there is one sequence of feature vectors per cluster, and a learnable cluster token is prepended to each sequence. These feature vectors are processed by a sequence of NBl TSTB. The output of the final block is used to predict the class scores and labels. Numbers in brackets denote tensor dimensions.\", \"page\": 4, \"index\": 2, \"width\": 808, \"height\": 383}, {\"url\": \"assets/figures/arxiv/2604.01836v1/fig-003.webp\", \"caption\": \"Figure 2. Architecture of a TSTB. It consists of two sub-blocks: a local block in which the feature vectors within each face cluster can interact with each other, and a cross-cluster block designed to capture global context by allowing the cluster tokens of different clusters to interact. Numbers in brackets are dimensions of tensors.\", \"page\": 4, \"index\": 3, \"width\": 456, \"height\": 266}]"
motivation: 现有的3D网格分割方法往往忽略了丰富的纹理信息，且难以处理非流形结构的几何复杂性。
method: 提出一种纹理感知Transformer，通过纹理分支提取面级像素特征，并利用两阶段Transformer块（TSTB）融合几何描述符进行多尺度特征聚合。
result: "在SUM基准上达到81.9% mF1，在文化遗产数据集上达到49.7% mF1，性能大幅超越现有方法。"
conclusion: 该研究证明了结合纹理与几何信息的Transformer架构在处理复杂、非流形3D网格语义分割任务中的有效性。
---

## 摘要
带纹理的 3D 网格共同表示几何、拓扑和外观，但其不规则结构给基于深度学习的语义分割带来了重大挑战。虽然近期的一些方法直接在网格上操作而不施加几何约束，但它们通常忽略了此类网格提供的丰富纹理信息。我们引入了一种纹理感知 Transformer，它直接从与每个网格面相关的原始像素中学习，并结合了一种用于多尺度特征聚合的新型分层学习方案。纹理分支将所有面级像素总结为一个可学习的 token，该 token 与几何描述符融合，并由一堆两阶段 Transformer 块 (TSTB) 处理，从而实现局部和全局信息流。我们在语义城市网格 (SUM) 基准测试和一个新策划的文化遗产数据集上评估了我们的模型，该数据集包含具有三角形级损伤类型标注的纹理屋顶瓦片。我们的方法在 SUM 上实现了 81.9% 的 mF1 和 94.3% 的 OA，在新的数据集上实现了 49.7% 的 mF1 和 72.8% 的 OA，显著优于现有方法。

## Abstract
Textured 3D meshes jointly represent geometry, topology, and appearance, yet their irregular structure poses significant challenges for deep-learning-based semantic segmentation. While a few recent methods operate directly on meshes without imposing geometric constraints, they typically overlook the rich textural information also provided by such meshes. We introduce a texture-aware transformer that learns directly from raw pixels associated with each mesh face, coupled with a new hierarchical learning scheme for multi-scale feature aggregation. A texture branch summarizes all face-level pixels into a learnable token, which is fused with geometrical descriptors and processed by a stack of Two-Stage Transformer Blocks (TSTB), which allow for both a local and a global information flow. We evaluate our model on the Semantic Urban Meshes (SUM) benchmark and a newly curated cultural-heritage dataset comprising textured roof tiles with triangle-level annotations for damage types. Our method achieves 81.9\% mF1 and 94.3\% OA on SUM and 49.7\% mF1 and 72.8\% OA on the new dataset, substantially outperforming existing approaches.

---

## 论文详细总结（自动生成）

这篇论文针对带纹理的非流形 3D 网格语义分割问题，提出了一种结合原始像素信息与几何特征的 Transformer 架构。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在处理具有不规则、非流形（Non-manifold）结构的 3D 网格时，充分利用其高分辨率的纹理信息进行精确的语义分割。
*   **研究动机**：
    *   现有的网格处理方法（如 MeshCNN）通常要求网格是流形的，但现实中通过摄影测量生成的网格往往是非流形的。
    *   大多数方法忽略了纹理信息，或仅使用简单的颜色统计值（如平均 RGB），丢失了纹理中的高频细节和空间模式。
    *   传统的 Transformer 在处理大规模网格面片时计算复杂度过高，且现有的局部-全局注意力机制容易导致分类结果过度平滑。

### 2. 提出的方法论
核心思想是构建一个**纹理感知 Transformer**，通过分层学习方案聚合多尺度特征。
*   **特征提取分支**：
    *   **几何分支**：提取每个面片的 16 维手工特征（坐标、法向量、面积、邻接角），通过线性层映射。
    *   **纹理分支**：直接从纹理图中提取每个面片对应的原始像素（固定采样为 $P$ 个像素），利用一个专门的 Transformer 块将这些像素总结为一个可学习的“纹理 Token”。
    *   **融合**：将几何特征与纹理 Token 拼接，通过多层感知机（MLP）生成初始面片特征。
*   **两阶段 Transformer 块 (TSTB)**：
    *   **空间聚类**：使用 K-means 将网格面片划分为 $K$ 个局部簇。
    *   **第一阶段（局部块）**：在簇内部进行自注意力计算，面片 Token 与簇 Token（Cluster Token）交互，捕捉局部细节。
    *   **第二阶段（跨簇块）**：仅在所有簇 Token 之间进行自注意力计算，实现全局信息交换。
    *   **信息回流**：更新后的全局簇 Token 在下一层 TSTB 中将上下文信息带回局部面片，避免了直接交叉注意力带来的过度平滑。

### 3. 实验设计
*   **数据集**：
    *   **SUM (Semantic Urban Meshes)**：城市规模基准数据集（赫尔辛基），包含 6 类语义标签（建筑、车辆、水体等）。
    *   **CH (Cultural Heritage)**：新创建的文化遗产数据集（西班牙瓦伦西亚 La Nau 中心屋顶），包含 7 类损伤类型标签（生物定殖、盐渍、结构缺失等），具有极高的空间分辨率（1mm GSD）。
*   **对比方法**：
    *   非流形基准：DiffusionNet、NoMeFormer。
    *   SUM 榜单领先方法：RF-MRF（基于网格）、KPConv（基于点云）。

### 4. 资源与算力
*   **硬件**：使用单块 **NVIDIA A100 GPU (40 GB 显存)**。
*   **超参数**：特征维度 $d=64$，TSTB 堆叠层数 $N_{Bl}=6$，聚类数 $K=300$，每个面片采样像素数 $P=128$。
*   **训练细节**：使用 AdamW 优化器，采用余弦退火学习率调度，并进行了旋转、缩放和高斯噪声等数据增强。

### 5. 实验数量与充分性
*   **实验规模**：在两个不同领域（城市建模与文物保护）的数据集上进行了验证，总计处理了数千万个三角形面片。
*   **消融实验**：
    1.  **特征维度**：测试了 $N_F$ 从 32 到 256 的影响。
    2.  **模型深度**：测试了 TSTB 层数的影响。
    3.  **输入模态**：对比了“仅几何”、“仅纹理”与“融合”的效果。
    4.  **注意力机制**：对比了提出的“自注意力跨簇块”与前人“交叉注意力机制”的优劣。
*   **评价**：实验设计较为充分，涵盖了性能对比、模块有效性验证及跨领域适用性分析，对比基准选择合理且公平。

### 6. 主要结论与发现
*   **性能提升**：在 SUM 数据集上达到 81.9% mF1，在 CH 数据集上达到 49.7% mF1，显著超越了 NoMeFormer 和 DiffusionNet。
*   **纹理的重要性**：实验证明纹理特征对分类的贡献大于几何特征，且直接学习原始像素远优于使用颜色统计值。
*   **架构优势**：提出的 TSTB 结构能有效平衡局部细节与全局上下文，生成的预测图边缘更锐利，减少了模糊效应。

### 7. 优点
*   **端到端纹理学习**：首次在非流形网格分割中直接集成原始像素级的 Transformer 学习。
*   **非流形兼容性**：不依赖网格的拓扑流形约束，适用于摄影测量生成的真实世界复杂模型。
*   **高效的全局建模**：通过簇 Token 的自注意力机制，以较低的计算成本实现了长程依赖建模。

### 8. 不足与局限
*   **纹理分支缺乏位置编码**：纹理 Transformer 分支目前未显式加入像素位置编码，可能损失了面片内的微观空间结构。
*   **计算资源限制**：受限于显存，目前无法处理极大规模的单次输入面片数或更高维度的特征。
*   **类别不平衡**：在 CH 数据集上，对于极少数样本类（如“沉积物”类）的识别效果依然较差（F1 为 0）。

（完）
