---
title: Learning 3D Reconstruction with Priors in Test Time
title_zh: 在测试阶段利用先验学习三维重建
authors: "Lei Zhou, Haoyu Wu, Akshat Dave, Dimitris Samaras"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03878v1"
tags: ["query:ff3dr"]
score: 9.0
evidence: 将前馈多视图Transformer与测试时优化相结合用于3D任务
tldr: "本研究针对多视图Transformer（MVT）提出了一种测试时约束优化（TCO）框架，旨在不重新训练或修改预训练模型的情况下，有效整合相机位姿、内参和深度等先验信息。该方法将先验视为预测约束，在推理阶段通过结合自监督目标（光度或几何损失）与先验惩罚项来优化网络。实验表明，该方法在ETH3D等多个3D视觉基准上显著提升了性能，将点图估计误差降低了50%以上，证明了其在3D重建任务中的优越性和通用性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.03878v1/fig-001.webp\", \"caption\": \"Table 4. Point Map Estimation on NRGBD (sparse view). Our method outperforms both the base image-only models and the prior-aware feed-forward methods.\", \"page\": 6, \"index\": 1, \"width\": 487, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2604.03878v1/fig-002.webp\", \"caption\": \"Table 3. Point Map Estimation on 7-Scenes (sparse view). Our method outperforms both the base image-only models and the prior-aware feed-forward methods.\", \"page\": 6, \"index\": 2, \"width\": 487, \"height\": 251}, {\"url\": \"assets/figures/arxiv/2604.03878v1/fig-003.webp\", \"caption\": \"Table 1. Point Map Estimation on ETH3D. TCO outperforms both base image-only MVTs and prior-aware feed-forward methods, achieving the best overall performance. Camera poses are the most important prior for improving 3D reconstruction on ETH3D, and combining them with intrinsics yields further gains.\", \"page\": 6, \"index\": 3, \"width\": 487, \"height\": 335}, {\"url\": \"assets/figures/arxiv/2604.03878v1/fig-004.webp\", \"caption\": \"Table 2. Point Map Estimation on DTU. TCO outperforms both the base image-only models and the prior-aware feed-forward methods. The proposed prediction compatibility loss is essential for incorporating priors as a regularization signal from distillation priors into the network.\", \"page\": 6, \"index\": 4, \"width\": 487, \"height\": 288}, {\"url\": \"assets/figures/arxiv/2604.03878v1/fig-005.webp\", \"caption\": \"Table 6. Ablations on ETH3D. We study the fine-tuning strategy and the 2DGS heuristics used in the compatibility objective.\", \"page\": 7, \"index\": 5, \"width\": 487, \"height\": 256}, {\"url\": \"assets/figures/arxiv/2604.03878v1/fig-006.webp\", \"caption\": \"Figure 5. 2DGS Rendering Visualization. We visualize the 2DGS rendering process for one scene from 7-Scenes. As shown in the Rendered Image row, our 2DGS heuristic parameterization produces rendered images that closely match the ground-truth images. We also compare the depth maps and normal maps rendered from 2DGS with the corresponding ground-truth depth and normal maps, i.e., those directly predicted from the MVT views.\", \"page\": 14, \"index\": 6, \"width\": 1019, \"height\": 1052}, {\"url\": \"assets/figures/arxiv/2604.03878v1/fig-007.webp\", \"caption\": \"Figure 1. Method Overview. (left) Multi-view Transformers (MVTs) take a set of RGB images as input and output depth maps, camera poses, and intrinsics. (middle) Given camera priors, MapAnything [22] and Pow3R [19] feed them into the network as additional input modalities, which requires retraining a modified MVT. (right) Our method, Test-time Constrained Optimization (TCO), treats the priors as constraints on the MVT’s predictions and optimizes the network with LoRA using both prior penalty terms and a self-supervised objective, namely MVT prediction compatibility, at inference time.\", \"page\": 3, \"index\": 7, \"width\": 986, \"height\": 538}, {\"url\": \"assets/figures/arxiv/2604.03878v1/fig-008.webp\", \"caption\": \"Figure 4. Fine-grained Qualitative Results. We compare TCO-VGGT with prior-aware feed-forward methods, including Pow3R and MapAnything. In each grid cell, the predicted geometry is overlaid with the ground truth geometry, whose points are shown in green. Discrepancies between the predicted and ground-truth geometries are highlighted by red double arrows, whose lengths indicate the magnitude of the errors. TCO-VGGT exhibits much smaller discrepancies than MapAnything and Pow3R in both scene structure and boundary regions.\", \"page\": 13, \"index\": 8, \"width\": 1036, \"height\": 561}, {\"url\": \"assets/figures/arxiv/2604.03878v1/fig-009.webp\", \"caption\": \"Figure 2. Qualitative Comparison. We compare TCO-VGGT with the base image-only model VGGT and the prior-aware feed-forward methods Pow3R and MapAnything. Overall, TCO-VGGT effectively corrects structural errors in image-only reconstructions by incorporating camera priors. Red, orange, and green circles highlight regions that are wrongly reconstructed, partially corrected, and correctly reconstructed, respectively. In the first row, TCO-VGGT corrects the inaccurate relative positions of two walls in the VGGT reconstruction. The same phenomenon is observed in the second and third rows, where the inaccurate scene structures are corrected. In the last row, TCOVGGT reconstructs the hand as a whole, whereas VGGT reconstructs it as separate parts. MapAnything reconstructs the hand with blurry boundaries. Additional fine-grained qualitative comparisons with MapAnything and Pow3R are provided in the Supplementary Material.\", \"page\": 8, \"index\": 9, \"width\": 1036, \"height\": 640}, {\"url\": \"assets/figures/arxiv/2604.03878v1/fig-010.webp\", \"caption\": \"Figure 3. Test-time scaling curve of our method on ETH3D.\", \"page\": 8, \"index\": 10, \"width\": 492, \"height\": 337}, {\"url\": \"assets/figures/arxiv/2604.03878v1/fig-011.webp\", \"caption\": \"Table 7. Ablation: Target view rendering loss and LoRA rank (ETH3D). Acc., Comp., and N.C. are reported (lower is better for Acc./Comp., higher is better for N.C.).\", \"page\": 12, \"index\": 11, \"width\": 487, \"height\": 305}]"
motivation: 旨在解决现有3D重建模型在引入相机位姿等先验信息时，往往需要复杂的架构修改或耗时的重新训练问题。
method: 提出测试时约束优化框架，通过在推理阶段最小化自监督一致性损失和先验约束惩罚项来动态调整模型输出。
result: 在多个主流数据集上，该方法显著降低了点图和位姿估计误差，性能远超基础模型及经过重新训练的先验感知方法。
conclusion: 该框架为3D视觉任务提供了一种无需重新训练即可灵活利用多源先验信息的有效途径，具有极强的实用价值。
---

## 摘要
我们为多视图 Transformer (MVT) 引入了一个测试阶段框架，该框架结合了先验信息（如相机位姿、内参和深度），在不重新训练或修改预训练的纯图像网络的情况下提升三维任务的性能。我们并非将先验输入到架构中，而是将其视为对预测结果的约束，并在推理阶段对网络进行优化。优化损失由自监督目标和先验惩罚项组成。自监督目标捕捉多视图预测之间的一致性，并通过其他视图的渲染图与各视图自身之间的光度或几何损失来实现。任何可用的先验都会被转化为对应输出模态的惩罚项。在包括点图估计和相机位姿估计在内的一系列三维视觉基准测试中，我们的方法相比基础 MVT 始终能大幅提升性能。在 ETH3D、7-Scenes 和 NRGBD 数据集上，与基础纯图像模型相比，我们的方法将点图距离误差降低了一半以上。我们的方法还优于经过重新训练的先验感知前馈方法，证明了我们的测试阶段约束优化 (TCO) 框架在将先验融入三维视觉任务中的有效性。

## Abstract
We introduce a test-time framework for multiview Transformers (MVTs) that incorporates priors (e.g., camera poses, intrinsics, and depth) to improve 3D tasks without retraining or modifying pre-trained image-only networks. Rather than feeding priors into the architecture, we cast them as constraints on the predictions and optimize the network at inference time. The optimization loss consists of a self-supervised objective and prior penalty terms. The self-supervised objective captures the compatibility among multi-view predictions and is implemented using photometric or geometric loss between renderings from other views and each view itself. Any available priors are converted into penalty terms on the corresponding output modalities. Across a series of 3D vision benchmarks, including point map estimation and camera pose estimation, our method consistently improves performance over base MVTs by a large margin. On the ETH3D, 7-Scenes, and NRGBD datasets, our method reduces the point-map distance error by more than half compared with the base image-only models. Our method also outperforms retrained prior-aware feed-forward methods, demonstrating the effectiveness of our test-time constrained optimization (TCO) framework for incorporating priors into 3D vision tasks.

---

## 论文详细总结（自动生成）

以下是对论文《Learning 3D Reconstruction with Priors in Test Time》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：当前基于多视图 Transformer (MVT) 的三维重建模型（如 DUSt3R, VGGT）虽然推理速度快，但通常仅设计为接收 RGB 图像。在实际应用中，往往能获得相机位姿（来自 SfM 或传感器）、内参或深度（来自 LiDAR）等先验信息。
*   **核心矛盾**：现有的先验感知方法（如 Pow3R, MapAnything）需要修改网络架构并针对特定先验进行重新训练，这导致模型缺乏灵活性，且计算成本高昂。
*   **研究目标**：提出一种“即插即用”的框架，在不改变预训练模型权重、不修改架构的前提下，在推理阶段（Test-time）动态地整合各种先验信息。

### 2. 方法论：测试时约束优化 (TCO)
*   **核心思想**：将先验信息视为对模型输出的**约束**，而非输入特征。在推理时，通过梯度下降微调模型参数，使输出既符合多视图几何一致性，又满足给定的先验约束。
*   **关键技术细节**：
    *   **损失函数设计**：总损失 = 自监督目标 ($L_{SSL}$) + 先验惩罚项 ($\sum \mu \|g\|$)。
    *   **自监督一致性 ($L_{SSL}$)**：利用 **2D Gaussian Splatting (2DGS)** 作为可微渲染器。通过模型预测的深度、位姿和置信度，启发式地初始化高斯参数，将源视图渲染到目标视图，计算光度损失（重建任务）或几何损失（位姿任务）。
    *   **先验约束 ($g$)**：针对位姿（旋转角度与平移距离）、内参（焦距差异）和深度（尺度对齐后的 $L_1$ 距离）分别定义惩罚项。
    *   **微调策略**：采用 **LoRA (Low-Rank Adaptation)** 仅微调共享的 Decoder 网络，同时**冻结** Encoder 和所有预测 Head。这种策略能利用不同任务间的协同效应，防止过拟合到单一先验。

### 3. 实验设计
*   **数据集**：
    *   **3D 重建/点图估计**：ETH3D（高分辨率室外）、DTU（物体中心）、7-Scenes（室内）、NRGBD（室内）。
    *   **相机位姿估计**：ScanNet。
*   **Benchmark 与对比方法**：
    *   **基础模型**：VGGT 和 $\pi^3$（作为 TCO 的底座）。
    *   **对比对象**：纯图像模型（Fast3R, CUT3R, FLARE 等）以及先验感知的前馈模型（Pow3R, MapAnything）。
*   **评估指标**：准确度 (Acc)、完整度 (Comp)、法线一致性 (N.C.)、绝对轨迹误差 (ATE) 等。

### 4. 资源与算力
*   **硬件环境**：论文提到使用了 **NVIDIA TITAN RTX** GPU 进行测试。
*   **时间开销**：
    *   以 10 个视图（分辨率 392x518）为例，原始前馈推理仅需约 **1.3 秒**。
    *   进行 40 步 TCO 优化后，总耗时增加至约 **161.5 秒**。
*   **参数配置**：LoRA 的秩 (rank) 设置为 4。

### 5. 实验数量与充分性
*   **实验规模**：论文在 5 个主流数据集上进行了广泛测试，涵盖了从稀疏视图到不同基线长度的场景。
*   **消融实验**：非常充分。作者研究了：
    1.  不同先验组合（位姿、内参、深度）的影响。
    2.  微调策略（是否冻结 Head、LoRA 秩的大小）。
    3.  2DGS 渲染器的启发式规则（半径缩放、不透明度定义等）。
    4.  测试时缩放曲线（性能随迭代次数的变化）。
    5.  对先验噪声的鲁棒性分析。
*   **客观性**：通过在两个不同的基础架构（VGGT 和 $\pi^3$）上应用 TCO，证明了该方法的通用性和非偶然性。

### 6. 主要结论与发现
*   **性能飞跃**：TCO 显著提升了基础模型的表现，在 ETH3D 等数据集上将重建误差降低了 **50% 以上**。
*   **超越重训模型**：尽管 TCO 不进行离线训练，其效果甚至优于那些专门针对先验信息训练的前馈模型（如 MapAnything）。
*   **协同效应**：冻结预测 Head 仅优化 Decoder 是关键，这迫使网络学习更一致的底层场景表示，从而让先验信息（如位姿）能反哺非约束输出（如深度）。
*   **测试时缩放**：性能随测试时计算量（迭代次数）的增加而提升，符合“System-2”思维的推理逻辑。

### 7. 优点
*   **即插即用**：无需修改原始模型架构，无需重新训练，具有极高的工程实用性。
*   **架构无关**：可以适配任何输出深度和位姿的多视图 Transformer。
*   **高精度**：通过引入可微渲染和测试时优化，弥补了前馈模型在几何细节上的不足。

### 8. 不足与局限
*   **推理延迟**：由于涉及多次反向传播优化，推理速度比纯前馈模型慢两个数量级，无法满足实时性要求。
*   **显存压力**：在处理大量视图或高分辨率图像时，维护可微渲染梯度和多视图特征会消耗大量显存。
*   **依赖初始质量**：如果基础模型的初始预测与真实场景偏差过大，优化过程可能会陷入局部最优，难以收敛到正确解。

（完）
