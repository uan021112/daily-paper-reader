---
title: "EfficientMonoHair: Fast Strand-Level Reconstruction from Monocular Video via Multi-View Direction Fusion"
title_zh: EfficientMonoHair：通过多视图方向融合实现单目视频的快速发丝级重建
authors: "Da Li, Dominik Engel, Deng Luo, Ivan Viola"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05794v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 单目视频的发丝级重建
tldr: 针对单目视频发丝级重建中精度与效率难以兼得的问题，本文提出EfficientMonoHair框架。该方法结合隐式神经网络与多视图几何融合，引入基于融合块的多视图优化以加速点云方向场计算，并采用新型并行发丝生长策略以增强对噪声的鲁棒性。实验表明，该方法在保持高保真重建质量的同时，运行效率较现有最先进方法提升了近一个数量级。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05794v1/fig-001.webp\", \"caption\": \"Figure 3. Visual comparison of the reconstruction of curly hair by multiple methods.Table 1 Quantitative Results on Hair20K. Our method is competitive with MonoHair at patch size 𝑃 = 5, while being 6× faster. MonoHair’s fast variant (𝑃 = 1) is still considerably slower than Ours, while singificanlty degrading the accuracy. Diffusion based methods like DiffLocks are much faster, but produce comparatively inaccurate results.\", \"page\": 6, \"index\": 1, \"width\": 1023, \"height\": 602}, {\"url\": \"assets/figures/arxiv/2604.05794v1/fig-002.webp\", \"caption\": \"Figure 2. Our framework reconstructs strand-level hair geometry from a monocular video in three stages: (a) Outer Direction Optimization, where coarse hair geometry is reconstructed via Instant-NGP, followed by our FPMVO (detailed illustration in appendix) to obtain a stable outer point cloud with directions. (b) Inner Direction Inference, which leverages a View-Aware Transformer to infer invisible inner orientations from unidirectional strand maps; and (c) Hair Growing, where both inner and outer direction fields are fused into an Occupancy/Orientation Volume, from which our parallel strand growing (PHG) generates the final high-fidelity 3D hair model.\", \"page\": 4, \"index\": 2, \"width\": 1040, \"height\": 416}, {\"url\": \"assets/figures/arxiv/2604.05794v1/fig-003.webp\", \"caption\": \"Figure 5. Visual comparison of the reconstruction of big wavy, asymmetric, short and medium curly hair styles by multiple methods. At the third row, we failed in reproducing this short hair using the GaussianHaircut codebase, therefore we present the results of Figure 7 in their paper Zakharov et al., 2024 next to our reproduced result. For the last row, GaussianHaircut also failed in the reconstruction. This shows the low robustness of their method compared to ours.\", \"page\": 7, \"index\": 3, \"width\": 504, \"height\": 416}]"
motivation: 现有的发丝重建方法在隐式表示的细节缺失与显式优化的计算开销之间存在显著的权衡问题。
method: 提出一种结合隐式网络与多视图几何融合的框架，利用融合块优化方向场并配合并行发丝生长策略。
result: 在真实和合成数据集上实现了与最先进方法相当的重建质量，且运行速度提升了近十倍。
conclusion: EfficientMonoHair通过创新的优化和生长策略，为高效、高保真的单目视频发丝重建提供了有效的解决方案。
---

## 摘要
发丝级头发几何重建是虚拟人建模和发型数字化中的一个基础问题。然而，现有方法在准确性和效率之间仍面临显著的权衡。隐式神经表示可以捕捉全局头发形状，但往往无法保留细粒度的发丝细节；而基于显式优化的方法虽然能实现高保真重建，但代价是计算量大且可扩展性差。为解决这一问题，我们提出了 EfficientMonoHair，这是一个快速且准确的框架，它结合了隐式神经网络与多视图几何融合，用于从单目视频中进行发丝级重建。我们的方法引入了基于融合补丁（fusion-patch）的多视图优化，减少了点云方向的优化迭代次数，并提出了一种新型并行头发生长策略，该策略放宽了体素占用约束，使得大规模发丝追踪在不准确或有噪声的方向场下仍能保持稳定和鲁棒。在代表性真实世界发型上的大量实验表明，我们的方法能够稳健且准确地重建高保真发丝几何结构。在合成基准测试中，我们的方法达到了与最先进方法相当的重建质量，同时将运行效率提高了近一个数量级。

## Abstract
Strand-level hair geometry reconstruction is a fundamental problem in virtual human modeling and the digitization of hairstyles. However, existing methods still suffer from a significant trade-off between accuracy and efficiency. Implicit neural representations can capture the global hair shape but often fail to preserve fine-grained strand details, while explicit optimization-based approaches achieve high-fidelity reconstructions at the cost of heavy computation and poor scalability. To address this issue, we propose EfficientMonoHair, a fast and accurate framework that combines the implicit neural network with multi-view geometric fusion for strand-level reconstruction from monocular video. Our method introduces a fusion-patch-based multi-view optimization that reduces the number of optimization iterations for point cloud direction, as well as a novel parallel hair-growing strategy that relaxes voxel occupancy constraints, allowing large-scale strand tracing to remain stable and robust even under inaccurate or noisy orientation fields. Extensive experiments on representative real-world hairstyles demonstrate that our method can robustly reconstruct high-fidelity strand geometries with accuracy. On synthetic benchmarks, our method achieves reconstruction quality comparable to state-of-the-art methods, while improving runtime efficiency by nearly an order of magnitude.

---

## 论文详细总结（自动生成）

### EfficientMonoHair：通过多视图方向融合实现单目视频的快速发丝级重建

#### 1. 核心问题与整体含义（研究动机和背景）
头发重建是数字化虚拟人的核心挑战，因其具有非刚性行为和复杂的拓扑结构。现有的技术路径存在明显权衡：
*   **隐式神经表示（如NeRF/3DGS）：** 擅长捕捉全局形状，但难以恢复物理仿真所需的细粒度发丝细节。
*   **显式几何优化：** 虽然保真度高，但计算开销巨大且扩展性差。
*   **现有SOTA（如MonoHair）：** 虽然实现了单目视频重建，但处理单个发型仍需4-9小时，无法满足高效制作的需求。

**EfficientMonoHair** 的研究动机在于打破“高精度”与“高效率”的互斥，旨在通过混合显式-隐式框架，在保持发丝级精度的前提下，将重建速度提升一个数量级。

#### 2. 论文提出的方法论
该框架分为三个主要阶段，核心思想是利用多视图几何融合加速方向场优化，并利用并行化策略加速发丝生长。

*   **基于融合补丁的多视图优化 (FPMVO)：**
    *   **多视图采样：** 从单目视频中提取2D方向图和置信度图，通过反向投影生成3D候选方向。
    *   **中值导向融合 (Medoid-based Fusion)：** 针对每个空间点，从多个视图中聚合候选方向，通过计算加权一致性得分选择“中值方向”，有效避免了直接平均导致的模糊。
    *   **补丁一致性优化：** 在图像域执行补丁级的匹配响应，进一步精细化外部发丝的方向场。
*   **内部方向推理：** 沿用前作的View-Aware Transformer，根据单向发丝图推断视频中不可见的内部头发结构。
*   **并行发丝生长 (PHG)：**
    *   **并行初始化：** 在GPU上同时从数万个头皮种子点开始追踪。
    *   **放宽约束：** 不同于传统方法严格的体素占用检查（导致串行），PHG允许初期生长重叠，通过后期段连接解决冲突，这显著增强了对噪声方向场的鲁棒性。
    *   **快速空间链接：** 利用KD-tree进行批量最近邻查询，将短发丝段高效连接成连续长发丝。

#### 3. 实验设计
*   **数据集：**
    *   **真实数据：** 采用MonoHair提供的多视图头发视频序列，涵盖短发、长发、卷发等代表性发型。
    *   **合成数据：** 使用Hair20K数据集（源自USC-HairSalon），提供精确的发丝级Ground Truth用于定量评估。
*   **对比方法 (Baselines)：**
    *   **MonoHair (2024)：** 当前显式优化的SOTA。
    *   **DiffLocks (2025)：** 基于扩散模型的单视图隐式生成方法。
    *   **GaussianHaircut (2024)：** 基于3D高斯投影的重建方法。
*   **评估指标：** 占用率（Precision/Recall/F1）和方向准确度（位置+角度的综合指标）。

#### 4. 资源与算力
*   **硬件：** 实验主要在 **NVIDIA RTX 4090 GPU** 上运行。对比方法中的DiffLocks在A100上运行。
*   **耗时：** 
    *   本方法总耗时约 **23-50分钟**（视发型复杂度而定）。
    *   相比之下，MonoHair需要约136分钟（其高质量设置下），本方法在方向优化阶段实现了 **28倍** 的加速，整体提速约 **6-7倍**。

#### 5. 实验数量与充分性
*   **实验规模：** 论文展示了对多种复杂发型（如大波浪、不对称发型、极卷发）的视觉对比。
*   **消融实验：** 专门设计了“仅FPMVO”和“仅PHG”的对比实验，验证了两个核心模块分别对效率和鲁棒性的贡献。
*   **客观性：** 通过合成数据集的定量指标证明了在大幅提速的同时，精度损失极小（保持了约88%的方向F1得分，且在占用率指标上甚至有所超越）。实验覆盖了从算法效率到下游应用（如Houdini物理仿真）的完整链路，较为充分。

#### 6. 主要结论与发现
*   **效率飞跃：** 成功将单目发丝重建从“小时级”降低到“分钟级”。
*   **鲁棒性增强：** 并行生长策略（PHG）不仅快，而且由于放宽了硬性占用约束，对方向场中的噪声和不一致性具有更强的容忍度。
*   **质量对标：** 在处理极具挑战性的卷发时，本方法在捕捉发丝流向和空间层次感方面优于基于扩散模型和高斯投影的方法。

#### 7. 优点
*   **算法创新：** FPMVO通过中值融合解决了多视图方向冲突问题，PHG通过并行化打破了发丝生长的计算瓶颈。
*   **实用性强：** 重建结果可直接导入Blender或Houdini进行照片级渲染和物理仿真，具有极高的工业应用价值。
*   **平衡性极佳：** 在速度、精度和发型多样性之间找到了目前最优的平衡点。

#### 8. 不足与局限
*   **复杂拓扑限制：** 继承了MonoHair的局限，对于高度缠结的结构（如发辫、发髻、紧凑的扎发）重建效果仍不理想。
*   **串行瓶颈：** 尽管大部分流程已并行化，但最后将发丝连接到头皮（Scalp Attachment）的步骤仍依赖顺序处理，在大规模发丝场景下存在优化空间。
*   **先验依赖：** 内部结构的恢复仍依赖于预训练的Transformer模型，其泛化能力受限于训练数据的分布。

（完）
