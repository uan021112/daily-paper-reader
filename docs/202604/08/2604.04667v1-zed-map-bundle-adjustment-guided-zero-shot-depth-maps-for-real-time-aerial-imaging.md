---
title: "ZeD-MAP: Bundle Adjustment Guided Zero-Shot Depth Maps for Real-Time Aerial Imaging"
title_zh: ZeD-MAP：用于实时航空成像的光束法平差引导零样本深度图
authors: "Selim Ahmet Iz, Francesco Nex, Norman Kerle, Henry Meissner, Ralf Berger"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04667v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 利用扩散模型从航空图像中进行零样本深度重建
tldr: 针对无人机高分辨率影像实时深度重建中存在的尺度不一致和计算效率问题，本文提出ZeD-MAP框架。该方法将零样本扩散深度模型与增量式集群平差（BA）相结合，利用BA生成的度量一致位姿和稀疏3D点作为引导，使扩散模型生成的深度图具备度量准确性和时空一致性。实验表明，该方法在保持实时处理速度的同时，实现了亚米级的重建精度，为灾害响应等时效性任务提供了高效的3D制图方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04667v1/fig-001.webp\", \"caption\": \"Figure 1. Integration of cluster-based bundle adjustment and diffusion model.\", \"page\": 2, \"index\": 1, \"width\": 1023, \"height\": 505}, {\"url\": \"assets/figures/arxiv/2604.04667v1/fig-002.webp\", \"caption\": \"Figure 4. Point clouds generated by (a) VGGT, (b) Map Anything, (c) COLMAP, (d) proposed method. (i) Enlarged views of target regions from corresponding point clouds (a* to d*). (ii) The perspective view of selected regions in the point cloud generated by the proposed method.\", \"page\": 7, \"index\": 2, \"width\": 1019, \"height\": 863}, {\"url\": \"assets/figures/arxiv/2604.04667v1/fig-003.webp\", \"caption\": \"Figure 2 . Ground-marker locations on test scene. 9m\", \"page\": 5, \"index\": 3, \"width\": 514, \"height\": 564}]"
motivation: 传统的立体匹配对拍摄几何要求严苛，而现有的零样本扩散模型虽快但缺乏度量准确性和帧间一致性。
method: 提出一种集群级框架，通过增量式平差获取度量位姿和稀疏点云，并将其重投影至影像中作为扩散深度估计的度量引导。
result: 在50米航高影像上实现了0.87米水平误差和0.12米垂直误差，单幅影像处理时间仅需1.47至4.91秒。
conclusion: 该方法证明了平差引导可使扩散模型达到与传统摄影测量相当的一致性，并显著提升了实时3D地图生成的效率。
---

## 摘要
从超高分辨率无人机（UAV）图像中进行实时深度重建对于灾难响应等时间敏感型地理空间任务至关重要，但由于宽基线视差、大图像尺寸、低纹理或镜面反射表面、遮挡以及严格的计算限制，这仍然具有挑战性。最近的零样本扩散模型无需针对特定任务进行重新训练即可提供快速的单图稠密预测，且与基于 Transformer 的预测器相比所需的标注数据集更少，同时避免了经典多视图立体（MVS）对采集几何形状的严格要求。然而，它们的概率推理阻碍了在连续帧和重叠瓦片之间实现可靠的度量精度和时间一致性。我们提出了 ZeD-MAP，这是一个集群级框架，通过集成增量式基于集群的光束法平差（BA），将测试时扩散深度模型转换为具有度量一致性的、类 SLAM 的制图流水线。流式传输的无人机帧被分组为重叠的集群；周期性的 BA 产生具有度量一致性的位姿和稀疏 3D 连接点，这些点被重投影到选定帧中，并作为基于扩散的深度估计的度量引导。使用 DLR 模块化航空相机系统（MACS）在约 50 米高度拍摄的地面标记飞行数据（地面采样距离 GSD 约为 0.85 厘米/像素，对应每帧 2,650 平方米的地面覆盖范围）进行的验证表明，我们的方法实现了亚米级精度，水平（XY）平面误差约为 0.87 米，垂直（Z）方向误差约为 0.12 米，同时保持单幅图像运行时间在 1.47 至 4.91 秒之间。结果受手动点云标注产生的轻微噪声影响。这些研究结果表明，基于 BA 的度量引导提供了与经典摄影测量方法相当的一致性，同时显著加快了处理速度，从而实现了实时 3D 地图生成。

## Abstract
Real-time depth reconstruction from ultra-high-resolution UAV imagery is essential for time-critical geospatial tasks such as disaster response, yet remains challenging due to wide-baseline parallax, large image sizes, low-texture or specular surfaces, occlusions, and strict computational constraints. Recent zero-shot diffusion models offer fast per-image dense predictions without task-specific retraining, and require fewer labelled datasets than transformer-based predictors while avoiding the rigid capture geometry requirement of classical multi-view stereo. However, their probabilistic inference prevents reliable metric accuracy and temporal consistency across sequential frames and overlapping tiles. We present ZeD-MAP, a cluster-level framework that converts a test-time diffusion depth model into a metrically consistent, SLAM-like mapping pipeline by integrating incremental cluster-based bundle adjustment (BA). Streamed UAV frames are grouped into overlapping clusters; periodic BA produces metrically consistent poses and sparse 3D tie-points, which are reprojected into selected frames and used as metric guidance for diffusion-based depth estimation. Validation on ground-marker flights captured at approximately 50 m altitude (GSD is approximately 0.85 cm/px, corresponding to 2,650 square meters ground coverage per frame) with the DLR Modular Aerial Camera System (MACS) shows that our method achieves sub-meter accuracy, with approximately 0.87 m error in the horizontal (XY) plane and 0.12 m in the vertical (Z) direction, while maintaining per-image runtimes between 1.47 and 4.91 seconds. Results are subject to minor noise from manual point-cloud annotation. These findings show that BA-based metric guidance provides consistency comparable to classical photogrammetric methods while significantly accelerating processing, enabling real-time 3D map generation.

---

## 论文详细总结（自动生成）

这是一份关于论文《ZeD-MAP: Bundle Adjustment Guided Zero-Shot Depth Maps for Real-Time Aerial Imaging》的结构化深度分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何在无人机（UAV）超高分辨率影像中，实现**实时**且具有**度量一致性**（Metric Consistency）的稠密深度重建。
*   **研究背景**：在灾难响应等时效性极强的任务中，传统的摄影测量（如 SGM）计算量巨大，难以满足实时性；而新兴的零样本（Zero-shot）扩散深度模型虽然泛化性强，但存在概率推理导致的尺度不确定、帧间不一致以及缺乏绝对度量精度的问题。
*   **整体含义**：本文旨在结合几何约束（光束法平差 BA）的严谨性和生成式模型（扩散模型）的鲁棒性，构建一个类 SLAM 的实时制图流水线。

### 2. 方法论：核心思想与技术细节
ZeD-MAP 框架的核心思想是利用轻量化的局部几何优化来“引导”深度学习模型的推理过程。
*   **核心流程**：
    1.  **集群化 N 帧选择**：将流式影像按重叠度分为集群。若有 GNSS 数据，则动态选择重叠度降至 10% 以前的帧；若无，则固定为 3 帧窗口。
    2.  **集群级增量 BA**：对集群内的 3 帧影像进行 BA 优化，获取精确的相机位姿和稀疏 3D 连接点。为了保持全局一致性，采用“位姿锚定”协议，固定前一窗口的最后 M 帧。
    3.  **稀疏点引导的扩散模型**：将 BA 生成的稀疏 3D 点重投影到集群中心帧，作为度量引导信号（Conditioning）。模型基于 **Murre** 架构，在潜空间内通过迭代去噪生成稠密深度图。
    4.  **增量融合**：利用截断符号距离函数（TSDF）将各帧深度图实时融合，生成全局点云、深度图和正射影像。

### 3. 实验设计
*   **数据集/场景**：
    1.  **地面标记数据集**：在 50 米航高拍摄，包含手动布设的地面标记，用于定量评估绝对度量精度。
    2.  **大规模地震灾区数据集**：2023 年土耳其地震灾区影像（航高 325 米，60 帧），用于评估复杂环境下的鲁棒性和多航带一致性。
*   **对比方法（Benchmark）**：
    *   **经典方法**：COLMAP（离线摄影测量标杆）。
    *   **前馈几何模型**：VGGT、MapAnything（基于 Transformer 的度量重建）。
    *   **单图深度模型**：DINOv2、DepthPro、Marigold、DepthAnything v2。

### 4. 资源与算力
*   **硬件配置**：
    *   **扩散推理**：NVIDIA RTX A6000 GPU（Python 环境）。
    *   **几何处理（BA）**：Intel Core i7-13700K (20核) CPU，32GB RAM（MATLAB 环境）。
*   **推理参数**：扩散模型采用 10 次迭代，集成规模（Ensemble size）为 5，图像下采样至 1024 像素宽度进行处理。
*   **训练时长**：论文使用的是预训练的零样本模型（Murre/Marigold 变体），**无需针对特定场景重新训练**。

### 5. 实验数量与充分性
*   **实验规模**：主要针对两个典型航空场景进行了深入分析。
*   **充分性评价**：
    *   **定量分析**：通过地面标记计算了 XY 平面和 Z 轴的绝对误差，实验设计较为客观。
    *   **定性分析**：对比了多种主流深度估计模型在连续帧间的稳定性（通过箭头标示不一致处），视觉对比直观。
    *   **局限性**：实验样本量（60 帧及 22 帧）相对较小，尚未在大规模城市级航测任务中进行长时间漂移测试。

### 6. 主要结论与发现
*   **精度达标**：在 50 米航高下，实现了 **0.87 米（水平）**和 **0.12 米（垂直）**的平均误差，相对精度优于 2.2%。
*   **实时性突破**：单对影像处理耗时在 **1.47 至 4.91 秒**之间（取决于集群大小），满足无人机典型飞行速度下的实时处理需求。
*   **一致性提升**：BA 引导显著消除了扩散模型原有的随机波动，使生成的 3D 点云在多航带交汇处保持对齐，表现优于纯前馈模型（如 VGGT）。

### 7. 优点（亮点）
*   **混合驱动**：巧妙结合了 BA 的度量确定性和扩散模型的细节生成能力，解决了零样本模型“有形无量”的痛点。
*   **无需先验**：不依赖外部第三方 SfM 工具（如 COLMAP）预先生成的深度图，实现了端到端的增量处理。
*   **灵活调度**：提出的 N 帧选择策略在保证 3D 点覆盖范围的同时，极大地降低了 BA 的计算负担。

### 8. 不足与局限
*   **计算开销**：尽管达到了实时性，但扩散模型的迭代去噪过程仍比纯 CNN 或 Transformer 模型慢，且对高端 GPU 有依赖。
*   **分辨率瓶颈**：为了保证速度，扩散推理时对图像进行了大幅下采样（至 1024px），可能损失超高分辨率影像中的微小细节。
*   **高度敏感性**：在缺乏精确偏航角（Yaw）的情况下，实验观察到了一定的高度膨胀现象，说明模型对初始几何参数仍有一定依赖。

（完）
