---
title: "AA-Splat: Anti-Aliased Feed-forward Gaussian Splatting"
title_zh: AA-Splat：抗锯齿前馈高斯泼溅
authors: "Taewoo Suh, Sungpyo Kim, Jongmin Park, Munchurl Kim"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29394v1"
tags: ["query:ff3dr"]
score: 10.0
evidence: 用于重建的抗锯齿前馈3D高斯泼溅
tldr: AA-Splat 针对前馈 3D 高斯泼溅（FF-3DGS）在不同分辨率下渲染时出现的伪影问题，提出了一种抗锯齿解决方案。该方法引入了不透明度平衡带限（OBBL）设计，通过 3D 带限后滤波器集成多视图频率边界，并结合不透明度平衡技术处理重叠的高斯原语。实验表明，AA-Splat 在各种分辨率下均显著提升了新视角合成的质量，相比 SOTA 基准 DepthSplat 提升了 5.4~7.5dB PSNR。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2603.29394v1/fig-001.webp\", \"caption\": \"Fig. 2: Overview of AA-Splat. After unprojecting per-view depth maps to obtain the 3D Gaussian centers, the maximal frequency bound of each 3D Gaussian primitive is computed from context view camera poses and focal lengths, which is then converted to its 3D band-limiting Gaussian post-filter. The GS head predicts the residual Gaussian parameters which are then post-filtered by the 3D filters to obtain the bandlimited 3D Gaussians. During rendering, opacity balancing (OB) compensates for the increase in overlap between pixel-aligned Gaussian primitives for improved photometric supervision and rendering fidelity.\", \"page\": 6, \"index\": 1, \"width\": 712, \"height\": 929}, {\"url\": \"assets/figures/arxiv/2603.29394v1/fig-002.webp\", \"caption\": \"Fig. 3: Multi-scale visual comparison on RE10K [45]. ×4 indicates rendering target views at higher resolution to simulate zoom-in effects, while ×1/4 indicates rendering target views at lower resolution to simulate zoom-out effects. Additional visualizations will be available in supplementary materials.\", \"page\": 11, \"index\": 2, \"width\": 646, \"height\": 967}, {\"url\": \"assets/figures/arxiv/2603.29394v1/fig-003.webp\", \"caption\": \"Fig. 1: Ablation results on the DL3DV [24] dataset. All variants perform feedforward reconstruction with 2 context views of 256 × 448 resolution and are rendered at 4× resolution to mimic zoom-in effects. Removing either OB or 3D-BLPF causes unnatural grid-like artifacts or sharp jagged edges. Using only the 2D Mip filter also causes grid-like artifacts. Our method (AA-Splat) effectively suppresses these rendering artifacts, enabling smooth interpolation. Best viewed zoomed-in.\", \"page\": 18, \"index\": 3, \"width\": 718, \"height\": 736}, {\"url\": \"assets/figures/arxiv/2603.29394v1/fig-004.webp\", \"caption\": \"Fig. 4: Multi-scale visual comparison of cross-dataset generalization on ACID [25]. Models trained on RE10K are directly used to test on scenes from ACID. Additional visualizations will be available in supplementary materials.\", \"page\": 12, \"index\": 4, \"width\": 723, \"height\": 346}]"
motivation: 现有的前馈 3D 高斯泼溅方法因使用错误的屏幕空间扩张滤波器，导致在非分布采样率下渲染时产生严重的锯齿伪影。
method: 提出 OBBL 设计，结合 3D 带限后滤波器以消除退化高斯，并利用不透明度平衡技术补偿扩展高斯原语间的重叠。
result: 在所有分辨率（4倍到1/4倍）下，AA-Splat 相比 DepthSplat 在 PSNR 指标上实现了 5.4 到 7.5dB 的大幅提升。
conclusion: AA-Splat 实现了在任意分辨率下稳健的抗锯齿渲染，显著增强了前馈 3D 高斯泼溅在稀疏视图重建中的性能。
---

## 摘要
前馈 3D 高斯泼溅 (FF-3DGS) 已成为稀疏视图 3D 重建和新视图合成 (NVS) 的一种快速且鲁棒的解决方案。然而，现有的 FF-3DGS 方法建立在不正确的屏幕空间膨胀滤波器之上，导致在以分布外采样率进行渲染时出现严重的渲染伪影。我们首次提出了一种名为 AA-Splat 的 FF-3DGS 模型，旨在实现任何分辨率下的鲁棒抗锯齿渲染。AA-Splat 采用了一种不透明度平衡带限 (OBBL) 设计，该设计结合了两个组件：一个 3D 带限后置滤波器，将多视图最大频率边界集成到前馈重建流水线中，从而有效地对生成的 3D 场景表示进行带限并消除退化高斯；以及一种不透明度平衡 (OB) 机制，将所有像素对齐的高斯基元无缝集成到渲染过程中，以补偿扩展高斯基元之间增加的重叠。AA-Splat 在所有分辨率（从 4 倍到 1/4 倍）下，相较于最先进的 (SOTA) 基准模型 DepthSplat，在新视图合成 (NVS) 性能上表现出显著提升，平均 PSNR 增益达 5.4~7.5dB。代码将会开源。

## Abstract
Feed-forward 3D Gaussian Splatting (FF-3DGS) emerges as a fast and robust solution for sparse-view 3D reconstruction and novel view synthesis (NVS). However, existing FF-3DGS methods are built on incorrect screen-space dilation filters, causing severe rendering artifacts when rendering at out-of-distribution sampling rates. We firstly propose an FF-3DGS model, called AA-Splat, to enable robust anti-aliased rendering at any resolution. AA-Splat utilizes an opacity-balanced band-limiting (OBBL) design, which combines two components: a 3D band-limiting post-filter integrates multi-view maximal frequency bounds into the feed-forward reconstruction pipeline, effectively band-limiting the resulting 3D scene representations and eliminating degenerate Gaussians; an Opacity Balancing (OB) to seamlessly integrate all pixel-aligned Gaussian primitives into the rendering process, compensating for the increased overlap between expanded Gaussian primitives. AA-Splat demonstrates drastic improvements with average 5.4$\sim$7.5dB PSNR gains on NVS performance over a state-of-the-art (SOTA) baseline, DepthSplat, at all resolutions, between $4\times$ and $1/4\times$. Code will be made available.

---

## 论文详细总结（自动生成）

这是一份关于论文《AA-Splat: Anti-Aliased Feed-forward Gaussian Splatting》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的前馈 3D 高斯泼溅（FF-3DGS）模型在改变采样率（如缩放、改变焦距或分辨率）时，会产生严重的渲染伪影。
*   **研究背景**：
    *   **侵蚀伪影（Erosion）**：放大（Zoom-in）时，由于高斯原语过于细长（退化），像素间会出现透明缝隙或黑洞。
    *   **扩张伪影（Dilation）**：缩小（Zoom-out）时，结构会显得异常粗厚。
*   **动机**：传统的 3DGS 依赖物理上不正确的屏幕空间扩张滤波器。虽然针对“单场景优化”的抗锯齿已有研究（如 Mip-Splatting），但在“前馈（Feed-forward）”模型中，直接应用这些技术会导致严重的网格状伪影和遮挡问题。

### 2. 方法论
AA-Splat 提出了**不透明度平衡带限（OBBL）**设计，包含两个核心组件：
*   **3D 带限后滤波器（3D-BLPF）**：
    *   **原理**：基于奈奎斯特采样定律，计算每个高斯原语在所有上下文视图中的最大频率边界。
    *   **实现**：将预测的高斯原语与一个各向同性的 3D 高斯低通滤波器进行卷积，确保高斯原语的最小尺度不会小于像素足迹，从而消除“退化”的高斯。
*   **不透明度平衡（OB）**：
    *   **原理**：3D-BLPF 会扩张高斯原语，导致像素对齐的高斯之间重叠增加，遮挡了背景信息，产生网格伪影。
    *   **技术细节**：
        1.  **不透明度裁剪**：将高斯不透明度限制在一个阈值（如 0.6）以下，防止任何原语完全遮挡后方。
        2.  **颜色归一化**：在渲染时同时计算 Alpha 通道，并用总 Alpha 对颜色进行归一化，补偿裁剪带来的透明度影响，确保所有相关高斯都能贡献颜色。

### 3. 实验设计
*   **数据集**：
    *   **训练/测试**：RealEstate10K (RE10K)。
    *   **零样本泛化（Zero-shot）**：DL3DV-10K 和 ACID 数据集。
*   **对比方法**：包括 MVSplat, DepthSplat (基准), NoPoSplat, SPFSplat, SPFSplatV2 等 SOTA 前馈模型。
*   **评价指标**：PSNR, SSIM, LPIPS，以及针对高分辨率一致性的 HRRC 指标。
*   **实验场景**：在 1/4× 到 4× 的多种分辨率下进行渲染，模拟从剧烈缩小到剧烈放大的过程。

### 4. 资源与算力
*   **硬件**：使用了 **2 台 NVIDIA RTX 4090 GPU**。
*   **训练时长**：大约需要 **3 天**。
*   **训练细节**：在 RE10K 上以 256x256 分辨率训练了 60 万步（600K steps），Batch Size 为 8。

### 5. 实验数量与充分性
*   **实验规模**：
    *   涵盖了 3 个主流数据集。
    *   进行了 5 种不同分辨率（1/4x, 1/2x, 1x, 2x, 4x）的详尽对比。
    *   **消融实验**：分别验证了 3D-BLPF、OB 以及 2D Mip 滤波器的各自贡献。
*   **充分性评价**：实验设计非常充分且客观。通过跨数据集的零样本测试，证明了该方法不仅解决了锯齿问题，还增强了模型的泛化能力。对比实验涵盖了目前该领域几乎所有的主流前馈模型。

### 6. 主要结论与发现
*   **性能飞跃**：在所有分辨率下，AA-Splat 相比基准模型 DepthSplat 实现了平均 **5.4 ~ 7.5dB 的 PSNR 提升**。
*   **抗锯齿效果**：成功消除了放大时的缝隙和缩小时的结构肥大，实现了平滑的插值效果。
*   **泛化增强**：即使在原始分辨率（1x）下，AA-Splat 的表现也优于基准模型，说明 OBBL 是一种强大的尺度不变先验。

### 7. 优点
*   **首创性**：第一个解决前馈 3DGS 抗锯齿问题的框架。
*   **鲁棒性**：在极端的采样率变化（4倍缩放）下依然能保持高保真度。
*   **即插即用**：其 OBBL 设计可以集成到任何基于深度估计的前馈 3DGS 架构中。
*   **视觉质量**：显著减少了前馈模型中常见的网格状伪影和边缘锯齿。

### 8. 不足与局限
*   **计算开销**：虽然是前馈模型，但引入 3D 卷积和额外的不透明度平衡计算可能会略微增加推理延迟（文中未详细对比推理耗时）。
*   **依赖深度质量**：作为基于 DepthSplat 的改进，其最终效果仍受限于底层深度估计模型的准确性。
*   **极端遮挡**：在极少数完全没有上下文视图覆盖的区域，颜色归一化可能会引入轻微的不稳定性。

（完）
