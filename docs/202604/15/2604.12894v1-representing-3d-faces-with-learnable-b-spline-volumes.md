---
title: Representing 3D Faces with Learnable B-Spline Volumes
title_zh: 基于可学习 B 样条体的三维人脸表示
authors: "Prashanth Chandran, Daoye Wang, Timo Bolkart"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12894v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 使用可学习B样条体进行单目3D人脸重建
tldr: 本文提出CUBE，一种结合B样条体与学习特征的新型人脸几何表示方法。该方法通过高维控制特征晶格参数化，利用B样条的局部支持特性实现精细的局部编辑。CUBE采用两阶段映射：先通过B样条混合生成基础形状，再利用轻量级MLP预测残差位移。实验表明，该方法在3D扫描配准和单目人脸重建任务中表现优异，实现了最先进的性能，同时保持了语义一致性和局部可编辑性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12894v1/fig-001.webp\", \"caption\": \"Figure 5. For a test scan shown in the top left, we evaluate CUBE models with encoders of different sizes and a varying number of control points. For each model, we show the predicted mesh and a corresponding error map visualizing the point-to-scan distance. The first columns contain shapes without applying the residuals predicted by the CUBE residual MLP, while the third and fourth columns are of shapes with the residuals applied. The last column visualizes the magnitude of the residuals across the face. The first three rows show models with 16x16x16 control points with encoders of different sizes (small, medium and large), while the last two rows are from a CUBE-Large model with 8 × 8 × 8 and 4× 4× 4 control points respectively.\", \"page\": 6, \"index\": 1, \"width\": 446, \"height\": 526}, {\"url\": \"assets/figures/arxiv/2604.12894v1/fig-002.webp\", \"caption\": \"Table 1. Impact of model sizes, number of control points mc, and residual offsets. Ablation study showing the performance of our different model configurations on our test set of synthetic scans. We report point-to-scan (PTS) and vertex-to-vertex (V2V) distances.\", \"page\": 6, \"index\": 2, \"width\": 494, \"height\": 511}, {\"url\": \"assets/figures/arxiv/2604.12894v1/fig-003.webp\", \"caption\": \"Figure 6. We show qualitative comparisons of BPS [40], TEMPEH [6] and CUBE for predicting registered meshes from three different real scans in various expressions. BPS struggles with scans of varying orientations and collapses to predicting an average face shape that only rigidly matches the location of scan vertices. While TEMPEH-Refined improves on the predictions of its Global stage, it produces noisy reconstructions with geometric artifacts (last example). In contrast, CUBE produces reconstructions with the lowest errors, while matching the identity and expression of the scan closely even for challenging expressions (ex: a person having their tongue out in the first row).\", \"page\": 7, \"index\": 3, \"width\": 496, \"height\": 821}, {\"url\": \"assets/figures/arxiv/2604.12894v1/fig-004.webp\", \"caption\": \"Table 2. Comparison with State-of-the-Art Methods. Quantitative results for BPS [40], TEMPEH [6], and CUBE across different model sizes and residual configurations. We report the point-toscan (PTS) and vertex-to-vertex (V2V) distances.\", \"page\": 7, \"index\": 4, \"width\": 469, \"height\": 462}, {\"url\": \"assets/figures/arxiv/2604.12894v1/fig-005.webp\", \"caption\": \"Figure 8. We compute the difference between the control features of a neutral and expression scan of a source subject (first column). This feature difference is then added to control features derived from neutral scans obtained of different target subjects (columns 2-6). In this case, CUBE control arithmetic transfers facial expressions from the source to the target subjects.\", \"page\": 8, \"index\": 5, \"width\": 496, \"height\": 244}, {\"url\": \"assets/figures/arxiv/2604.12894v1/fig-006.webp\", \"caption\": \"Figure 9. The CUBE scan encoder generalizes to scans captured by different setups. Here we show scans from two different datasets CoMA [45] (columns 1-3) and FaMoS [6] (columns 4-6). The input scans are shown in the first row, the predictions from our CUBE-L model are shown in the second row, and an overlay of the predictions with the input scan is shown in the third row.\", \"page\": 8, \"index\": 6, \"width\": 496, \"height\": 340}, {\"url\": \"assets/figures/arxiv/2604.12894v1/fig-007.webp\", \"caption\": \"Figure 4. Starting from a randomly sampled mesh, we accessorize it with skin appearance, clothing, and hair and render it from 16 cameras using Blender. The synthetic images along with the cameras are then used to reconstruct a scan [44] to provide paired training data. We show some examples of mesh-scan pairs generated using this approach in the last two rows.\", \"page\": 5, \"index\": 7, \"width\": 446, \"height\": 344}]"
motivation: 旨在解决现有3D人脸表示在表达能力、局部编辑灵活性以及语义一致性之间难以兼顾的问题。
method: 提出CUBE表示法，通过B样条基函数对高维控制特征进行局部混合，并结合MLP预测残差位移以精细化3D坐标。
result: 在3D扫描配准和单目重建任务中，基于Transformer的编码器配合CUBE表示法达到了当前最先进的精度。
conclusion: CUBE成功结合了传统B样条的局部控制优势与深度学习的强大表达能力，为3D人脸建模提供了一种高效且灵活的新方案。
---

## 摘要
我们提出了 CUBE（基于控制的统一 B 样条编码），这是一种将 B 样条体与学习特征相结合的新型人脸几何表示方法，并展示了其作为解码器在三维扫描配准和单目三维人脸重建中的应用。与现有的使用三维控制点的 B 样条表示不同，CUBE 由高维控制特征的点阵（例如 8 x 8 x 8）进行参数化，从而增强了模型的表达能力。这些特征定义了一个从三维参数域通过中间特征空间到三维欧几里得空间的连续两阶段映射。首先，利用 B 样条基函数对高维控制特征进行局部混合，生成一个高维特征向量，其前三个值定义了一个三维基础网格。随后，一个小型多层感知机（MLP）处理该特征向量，以预测相对于基础形状的残差位移，从而得到最终细化的三维坐标。为了重建具有密集语义对应关系的三维表面，CUBE 在从固定模板网格采样的三维坐标处进行查询。至关重要的是，CUBE 保留了传统 B 样条表示的局部支持特性，能够通过更新单个控制特征来实现局部表面编辑。我们通过训练基于 Transformer 的编码器，从无结构点云和单目图像中预测 CUBE 的控制特征，展示了该表示方法的优势，并与近期的基准模型相比，实现了最先进的扫描配准结果。

## Abstract
We present CUBE (Control-based Unified B-spline Encoding), a new geometric representation for human faces that combines B-spline volumes with learned features, and demonstrate its use as a decoder for 3D scan registration and monocular 3D face reconstruction. Unlike existing B-spline representations with 3D control points, CUBE is parametrized by a lattice (e.g., 8 x 8 x 8) of high-dimensional control features, increasing the model's expressivity. These features define a continuous, two-stage mapping from a 3D parametric domain to 3D Euclidean space via an intermediate feature space. First, high-dimensional control features are locally blended using the B-spline bases, yielding a high-dimensional feature vector whose first three values define a 3D base mesh. A small MLP then processes this feature vector to predict a residual displacement from the base shape, yielding the final refined 3D coordinates. To reconstruct 3D surfaces in dense semantic correspondence, CUBE is queried at 3D coordinates sampled from a fixed template mesh. Crucially, CUBE retains the local support property of traditional B-spline representations, enabling local surface editing by updating individual control features. We demonstrate the strengths of this representation by training transformer-based encoders to predict CUBE's control features from unstructured point clouds and monocular images, achieving state-of-the-art scan registration results compared to recent baselines.

---

## 论文详细总结（自动生成）

这是一份关于论文《Representing 3D Faces with Learnable B-Spline Volumes》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保持三维人脸表示的**高表达能力**（捕捉细节）的同时，兼顾**局部可编辑性**、**计算效率**以及**密集的语义对应关系**。
*   **研究背景**：
    *   传统的**3DMM（三维可变形模型）**受限于线性空间和固定拓扑，难以捕捉高频细节。
    *   **非线性神经模型**虽然灵活，但缺乏可解释性和局部控制手段。
    *   **隐式表示（如 SDF）**虽细节丰富，但缺乏固有的语义对应，且渲染时需要昂贵的等值面提取（如 Marching Cubes）。
*   **动机**：结合传统 B 样条（B-Spline）的局部控制优势与深度学习的非线性表达能力，提出一种混合表示法。

### 2. 方法论
CUBE（Control-based Unified B-spline Encoding）的核心思想是将 B 样条体参数化为高维可学习特征。
*   **核心架构**：
    1.  **高维控制晶格**：定义一个 $m \times m \times m$ 的晶格，每个节点是一个 $d$ 维的可学习特征向量（而非传统的 3D 坐标）。
    2.  **两阶段映射**：
        *   **第一阶段（基础形状）**：通过 B 样条基函数对高维特征进行局部混合，得到插值后的特征向量。该向量的前三维直接作为 3D 基础网格（Base Shape）的坐标。
        *   **第二阶段（残差细化）**：将完整的高维特征向量输入一个轻量级 MLP（4 层），预测相对于基础形状的 3D 残差位移（Residuals），叠加后得到最终坐标。
*   **连续查询与对应**：通过在固定模板网格的参数空间内采样，CUBE 可以生成具有一致拓扑和语义对应关系的结果，同时也支持连续空间查询。
*   **编码器设计**：采用基于 Transformer（如 ViT 架构）的编码器，将无结构点云或单目图像映射为 CUBE 的控制特征晶格。

### 3. 实验设计
*   **数据集**：
    *   **合成数据**：利用 Blender 渲染 30 万对“网格-扫描图”数据，通过多视图重建（MVS）模拟真实扫描噪声。
    *   **真实数据**：使用 1.8 万个真实人类扫描样本进行测试，涵盖多种身份和表情。
    *   **外部数据集**：在 CoMA 和 FaMoS 数据集上验证泛化能力。
*   **Benchmark 与对比方法**：
    *   **对比方法**：BPS（Basis Point Sets，基于点云）、TEMPEH（基于多视图图像）。
    *   **评估指标**：点到扫描距离（PTS）、顶点到顶点距离（V2V）。
*   **消融实验**：研究了编码器规模（S/M/L）、控制点数量（$4^3, 8^3, 16^3$）以及残差 MLP 对精度的贡献。

### 4. 资源与算力
*   **训练算力**：使用了 **16 个 TPU** 核心进行并行训练。
*   **训练时长**：最大的模型（CUBE-L，带 $16^3$ 控制点）训练耗时约 **1 天**。
*   **推理性能**：在 V100 GPU 上，推理时间根据配置不同在 **0.20s 到 2.92s** 之间。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量评估（1.8 万个真实样本测试），这在同类研究中属于较高水平。
*   **消融实验**：非常充分，清晰地展示了增加控制点维度和引入 MLP 残差如何逐步降低误差。
*   **客观性**：通过在未见过的真实扫描数据和第三方公开数据集（CoMA, FaMoS）上进行测试，证明了模型的泛化能力，实验设计较为公平、客观。

### 6. 主要结论与发现
*   **性能领先**：CUBE 在前馈式扫描配准任务中达到了 **SOTA（最先进）** 水平，精度显著优于 BPS 和 TEMPEH。
*   **局部控制**：由于 B 样条的局部支持特性，用户可以通过修改单个控制特征来实现精确的局部几何编辑（如只改变下唇形状），且无需专门的局部性损失函数。
*   **平滑插值**：高维控制特征具有良好的几何意义，支持在不同身份和表情之间进行平滑的线性插值和算术运算（如表情迁移）。
*   **拓扑灵活性**：通过改变采样策略，同一个模型可以输出不同拓扑结构的网格。

### 7. 优点
*   **混合优势**：成功将经典几何建模（B 样条）的数学严谨性与现代深度学习的表达力结合。
*   **局部编辑性**：这是许多神经表示法（如全局隐式函数）所欠缺的关键特性，对图形学应用至关重要。
*   **鲁棒性**：对输入点云的采样密度和旋转具有较强的鲁棒性。
*   **多功能性**：一套表示法同时适用于扫描配准、单目重建、局部编辑和表情迁移。

### 8. 不足与局限
*   **模板依赖**：虽然支持不同拓扑，但建立语义对应仍依赖于初始的模板网格参数化。
*   **计算开销**：随着控制晶格分辨率增加（如从 $8^3$ 到 $16^3$），计算量和内存占用显著上升，存在性能瓶颈。
*   **极端细节限制**：虽然有残差 MLP，但对于极高频的皮肤纹理（如毛孔级细节），目前的表示精度可能仍有提升空间。
*   **算力门槛**：训练过程依赖 TPU 等高性能计算资源，普通开发者复现成本较高。

（完）
