---
title: Physically Grounded 3D Generative Reconstruction under Hand Occlusion using Proprioception and Multi-Contact Touch
title_zh: 基于本体感受与多点触觉的遮挡下物理约束3D生成式重建
authors: "Gabriele Mario Caddeo, Pasquale Marra, Lorenzo Natale"
date: 2026-04-10
pdf: "https://arxiv.org/pdf/2604.09100v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 使用潜空间和扩散模型的3D生成式重建
tldr: 本研究提出一种结合视觉、本体感受和多点触觉的多模态生成式方法，旨在解决手部严重遮挡下的3D物体重建难题。通过将手部几何与触觉约束引入扩散模型，并结合物理引导的推理过程，实现了具有真实物理尺度且无穿透的物体形状与位姿估计。该方法在仿真和真实机器人平台上均验证了其在处理复杂操纵场景时的优越性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.09100v1/fig-001.webp\", \"caption\": \"Table 1: 3D reconstruction results in simulation.\", \"page\": 11, \"index\": 1, \"width\": 502, \"height\": 383}, {\"url\": \"assets/figures/arxiv/2604.09100v1/fig-002.webp\", \"caption\": \"Fig. 3: Qualitative results in simulation. Vision-only baselines often exhibit artifacts (e.g ., holes or inconsistent relative dimensions) under occlusion. By leveraging contact cues, our method produces more physically plausible reconstructions.\", \"page\": 11, \"index\": 2, \"width\": 662, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2604.09100v1/fig-003.webp\", \"caption\": \"Table 2: Pose estimation results in simulation.\", \"page\": 12, \"index\": 3, \"width\": 502, \"height\": 264}, {\"url\": \"assets/figures/arxiv/2604.09100v1/fig-004.webp\", \"caption\": \"Fig. 2: Training procedure and Architecture. a1): We train a Structure-VAE autoencoder that reconstructs pose-aware object SDFs. a2): Using the frozen VAE encoder, we build latent datasets and train a conditional flow transformer from scratch using pose-consistent, unoccluded object images. a3): We finetune the Structureflow on occluded manipulation scenes, conditioning on visible RGB evidence, occluder/visibility masks, hand latent, and tactile features. b) The architecture of the Flow Transformer. Figure design inspired by [66].\", \"page\": 5, \"index\": 4, \"width\": 704, \"height\": 395}]"
motivation: 传统的纯视觉3D重建方法在手部遮挡严重的操纵场景中，难以准确恢复物体的完整几何结构和真实的物理尺度。
method: 利用Structure-VAE学习SDF潜空间，并训练条件流匹配扩散模型，通过本体感受和触觉信号进行物理一致性的差分引导推理。
result: 实验证明，引入物理交互信号显著提升了遮挡区域的补全精度，并有效减少了手物穿透，生成了物理上更合理的重建结果。
conclusion: 融合本体感受与触觉的多模态物理信号是实现鲁棒、物理一致的机器人感知和物体重建的关键途径。
---

## 摘要
我们提出了一种多模态、基于物理约束的方法，用于在严重手部遮挡下进行米制尺度的非全貌（amodal）物体重建和位姿估计。与以往仅依赖视觉的遮挡感知3D生成方法不同，我们利用了物理交互信号：本体感受提供了手部的姿态几何信息，而多点触觉则约束了物体表面的位置，从而减少了遮挡区域的歧义性。我们将物体结构表示为位姿感知且相机对齐的有符号距离场（SDF），并利用 Structure-VAE 学习一个紧凑的隐空间。在该隐空间中，我们训练了一个条件流匹配（flow-matching）扩散模型，先在纯视觉图像上进行预训练，然后在遮挡操作场景中进行微调，同时以可见的 RGB 证据、遮挡物/可见性掩码、手部隐式表示以及触觉信息为条件。关键在于，我们在微调和推理过程中引入了基于物理的目标和可微解码器引导，以减少手物穿透，并使重建表面与接触观测保持一致。由于我们的方法能够产生米制尺度且物理一致的结构估计，它可以自然地集成到现有的两阶段重建流水线中，由下游模块进一步优化几何形状并预测外观。仿真实验表明，与仅依赖视觉的基准方法相比，加入本体感受和触觉显著改善了遮挡下的补全效果，并生成了具有正确真实世界尺度的物理合理重建；我们通过在真实人形机器人上部署该模型进一步验证了其迁移能力，该机器人的末端执行器与训练时使用的不同。

## Abstract
We propose a multimodal, physically grounded approach for metric-scale amodal object reconstruction and pose estimation under severe hand occlusion. Unlike prior occlusion-aware 3D generation methods that rely only on vision, we leverage physical interaction signals: proprioception provides the posed hand geometry, and multi-contact touch constrains where the object surface must lie, reducing ambiguity in occluded regions. We represent object structure as a pose-aware, camera-aligned signed distance field (SDF) and learn a compact latent space with a Structure-VAE. In this latent space, we train a conditional flow-matching diffusion model, pretraining on vision-only images and finetuning on occluded manipulation scenes while conditioning on visible RGB evidence, occluder/visibility masks, the hand latent representation, and tactile information. Crucially, we incorporate physics-based objectives and differentiable decoder-guidance during finetuning and inference to reduce hand--object interpenetration and to align the reconstructed surface with contact observations. Because our method produces a metric, physically consistent structure estimate, it integrates naturally into existing two-stage reconstruction pipelines, where a downstream module refines geometry and predicts appearance. Experiments in simulation show that adding proprioception and touch substantially improves completion under occlusion and yields physically plausible reconstructions at correct real-world scale compared to vision-only baselines; we further validate transfer by deploying the model on a real humanoid robot with an end-effector different from those used during training.

---

## 论文详细总结（自动生成）

以下是对论文《Physically Grounded 3D Generative Reconstruction under Hand Occlusion using Proprioception and Multi-Contact Touch》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在机器人抓取和操纵过程中，手部会对物体造成严重遮挡，导致单视角RGB重建变得极度欠定（Underconstrained）。
*   **研究动机**：
    *   **视觉局限性**：纯视觉重建在遮挡下虽然看起来“合理”，但往往违反物理定律（如手物穿透）、缺失真实尺度或在遮挡区域产生几何漂移。
    *   **物理信号的价值**：操纵过程天然产生了物理证据——**本体感受**（提供手部几何）和**多点触觉**（约束表面位置）。这些信号能直接填补视觉缺失的几何空白。
    *   **物理落地**：旨在将生成式3D模型与物理约束（非穿透性、接触一致性）结合，实现米制尺度（Metric-scale）且物理可行的重建。

### 2. 方法论
该方法分为两个阶段，核心在于**阶段 A（结构生成）**：
*   **核心思想**：将物体几何表示为相机对齐的有符号距离场（SDF），利用隐空间流匹配（Flow-matching）模型进行生成，并在推理时引入物理引导。
*   **关键技术细节**：
    1.  **Structure-VAE**：学习一个紧凑的SDF隐空间，通过$L_1$损失、Eikonal正则化（梯度模长为1）和法线一致性损失进行训练。
    2.  **条件流变换器（Flow Transformer）**：在VAE隐空间内建模。输入条件包括：可见RGB图像、物体/手部掩码、手部几何的隐式表示（本体感受）、以及一个包含接触占用和距离信息的3D张量（触觉）。
    3.  **物理感知损失函数**：
        *   **非穿透损失 ($L_{NI}$)**：惩罚物体SDF在手部内部的质量，防止几何重叠。
        *   **接触一致性损失 ($L_C$)**：惩罚接触点处的SDF绝对值，强制物体表面经过触觉观测点。
    4.  **推理阶段的物理引导**：在采样过程中，利用上述物理能量函数的梯度来修正流场方向，使生成的形状向物理可行的解空间靠拢。
*   **阶段 B（精细化）**：将阶段A生成的粗糙结构输入到类似SLat的模块中，恢复纹理和高频几何细节。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：3D-FUTURE, HSSD, ABO, ObjaverseXL（约10万个物体）。
    *   **微调**：Google Scanned Objects (GSO)，通过抓取合成模型生成了约46万张遮挡操纵图像。
    *   **测试**：YCB数据集（36个物体），生成了2.2万张测试图像。
*   **Benchmark 与对比方法**：
    *   **Amodal3R**：纯视觉的非全貌3D重建基准。
    *   **SAM3D**：当前最强的单图3D重建模型之一。
*   **评估指标**：倒角距离 (CD)、法线一致性 (NC)、F-score、体素 IoU、位姿估计指标 (3D IoU, ADD-S)。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的GPU型号、数量及训练总时长。但考虑到其使用了基于Transformer的流匹配模型和大规模3D数据集（10万+物体），预计需要多张高性能显卡（如 A100 或 H100）进行数周的训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **仿真实验**：涵盖了5个不同遮挡等级（B1-B5）的详尽对比，样本量超过2万。
    *   **消融实验**：分别验证了视觉、本体感受、触觉各模态的贡献，以及对触觉噪声（3mm, 5mm偏移）的鲁棒性。
    *   **真实世界验证**：在人形机器人上进行了跨具身（Cross-embodiment）测试，即测试时的机械手与训练时完全不同。
*   **充分性评价**：实验设计较为充分，通过分层遮挡等级展示了物理信号在极端情况下的关键作用。对比实验选择了领域内具有代表性的基准，且包含了从仿真到真实的迁移验证，客观性较强。

### 6. 主要结论与发现
*   **多模态优势**：引入本体感受和触觉显著提升了严重遮挡下的重建精度，尤其在体素IoU和位姿估计上大幅领先纯视觉方法。
*   **物理引导的有效性**：物理引导不仅减少了手物穿透，还纠正了物体的米制尺度，使其更符合真实物理环境。
*   **泛化能力**：模型能够泛化到训练期间未见过的机械手结构，证明了在共享3D空间中表示交互信号的有效性。

### 7. 优点与亮点
*   **物理接地（Physical Grounding）**：不同于只追求视觉美观的生成模型，该方法将物理可行性作为生成的核心约束。
*   **可微引导**：将不可微的物理冲突转化为可微的SDF梯度引导，实现了端到端的推理优化。
*   **尺度准确性**：通过相机对齐的SDF表示，直接解决了单目重建中难以确定的绝对尺度问题。

### 8. 不足与局限
*   **分辨率限制**：受限于体素网格（64³），对于细长结构或微小凹陷的刻画能力有限。
*   **依赖校准**：系统高度依赖相机与手部之间的外参校准以及正向运动学的准确性，校准误差会直接导致重建偏差。
*   **推理开销**：作为生成式模型，其采样过程（多次迭代解码）可能难以满足极高频率的实时控制需求。
*   **应用限制**：目前主要针对孤立物体的抓取场景，尚未探讨复杂堆叠环境下的多物体交互重建。

（完）
