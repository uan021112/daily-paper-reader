---
title: "VVGT: Visual Volume-Grounded Transformer"
title_zh: VVGT：视觉体素锚定 Transformer
authors: "Yuxuan Wang, Qibiao Li, Youcheng Cai"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12217v1"
tags: ["query:ff3dr"]
score: 10.0
evidence: 将体积数据直接映射到3D高斯泼溅表示的前馈框架
tldr: 针对传统体绘制（DVR）扩展性差及现有3D高斯泼溅（3DGS）需逐场景优化的局限，本文提出VVGT框架。这是一种前馈式、以表示为中心的模型，通过双Transformer网络和体几何强制机制，将体积数据直接映射为3DGS表示。该方法无需逐场景优化，在保证高质量可视化的同时，实现了极速转换和强大的零样本泛化能力，为体可视化提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12217v1/fig-001.webp\", \"caption\": \"Table 1. Quantitative comparison on our zero-shot test datasets. We evaluate VVGT against both optimization-based and feed-forward methods. VVGT achieves superior rendering performance over feed-forward methods while remaining competitive with optimization-based methods. In addition, we demonstrate that applying a small number of post-optimization steps yields better rendering quality than optimization-based methods.\", \"page\": 6, \"index\": 1, \"width\": 1036, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2604.12217v1/fig-002.webp\", \"caption\": \"Fig. 4. Ablation study of the 3D Geometry Transformer, VBM initialization, and Volume Geometry Forcing. The visual results demonstrate the importance of these modules, as removing any of them leads to a noticeable degradation in visual quality.\", \"page\": 10, \"index\": 2, \"width\": 1048, \"height\": 437}, {\"url\": \"assets/figures/arxiv/2604.12217v1/fig-003.webp\", \"caption\": \"Fig. 2. Overview of the VVGT pipeline. VVGT employs a Dual-Transformer Network with a 2D Appearance Transformer and a 3D Geometry Transformer to jointly model appearance and volumetric geometry. Volume Geometry Forcing (VGF) aligns 2D and 3D features via epipolar cross-attention, enabling accurate Gaussian attribute learning for high-quality volumetric rendering.\", \"page\": 4, \"index\": 3, \"width\": 1038, \"height\": 544}, {\"url\": \"assets/figures/arxiv/2604.12217v1/fig-004.webp\", \"caption\": \"Fig. 3. Qualitative comparisons using 36 views on our test dataset. Our method demonstrates strong zero-shot performance, outperforming baseline methods in capturing sharp edges and fine-grained details. Moreover, applying a small number of post-optimization steps further improves rendering quality compared to optimization-based methods.\", \"page\": 9, \"index\": 4, \"width\": 1054, \"height\": 1249}]"
motivation: 旨在解决传统体绘制扩展性受限以及现有3DGS方法依赖高成本逐场景优化而难以实时交互的问题。
method: 采用双Transformer网络并引入体几何强制机制，通过极线交叉注意力将多视图观测直接映射为3D高斯原语。
result: VVGT实现了数量级的转换加速，并在多种数据集上展现出优异的几何一致性和零样本泛化性能。
conclusion: 该研究成功构建了一种无需逐场景优化的前馈式体绘制新架构，显著提升了体可视化的交互性和可扩展性。
---

## 摘要
体绘制长期以来由直接体渲染（DVR）主导，它在密集体素网格上运行，且随着分辨率和交互性需求的增加，其可扩展性受到限制。3D 高斯泼溅（3DGS）的最新进展提供了一种以表示为中心的替代方案；然而，现有的体素扩展仍依赖于昂贵的逐场景优化，限制了可扩展性和交互性。我们提出了 VVGT（视觉体素锚定 Transformer），这是一个前馈、表示优先的框架，可将体数据直接映射到 3D 高斯泼溅表示，推进了超越 DVR 的体绘制新范式。与之前针对以表面为中心的重建而设计的前馈 3DGS 方法不同，VVGT 明确考虑了体渲染，即每个像素沿射线聚合贡献。VVGT 采用双 Transformer 网络，并引入了体几何强迫（Volume Geometry Forcing），这是一种对极交叉注意力机制，可在无表面假设的情况下将多视图观测集成到分布式的 3D 高斯基元中。这种设计消除了逐场景优化，同时实现了准确的体表示。大量实验表明，VVGT 实现了高质量的可视化，转换速度提高了几个数量级，几何一致性得到改善，并在不同数据集上表现出强大的零样本泛化能力，从而实现了真正的交互式和可扩展体绘制。代码将在论文接收后公开发布。

## Abstract
Volumetric visualization has long been dominated by Direct Volume Rendering (DVR), which operates on dense voxel grids and suffers from limited scalability as resolution and interactivity demands increase. Recent advances in 3D Gaussian Splatting (3DGS) offer a representation-centric alternative; however, existing volumetric extensions still depend on costly per-scene optimization, limiting scalability and interactivity. We present VVGT (Visual Volume-Grounded Transformer), a feed-forward, representation-first framework that directly maps volumetric data to a 3D Gaussian Splatting representation, advancing a new paradigm for volumetric visualization beyond DVR. Unlike prior feed-forward 3DGS methods designed for surface-centric reconstruction, VVGT explicitly accounts for volumetric rendering, where each pixel aggregates contributions along a ray. VVGT employs a dual-transformer network and introduces Volume Geometry Forcing, an epipolar cross-attention mechanism that integrates multi-view observations into distributed 3D Gaussian primitives without surface assumptions. This design eliminates per-scene optimization while enabling accurate volumetric representations. Extensive experiments show that VVGT achieves high-quality visualization with orders-of-magnitude faster conversion, improved geometric consistency, and strong zero-shot generalization across diverse datasets, enabling truly interactive and scalable volumetric visualization. The code will be publicly released upon acceptance.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **VVGT (Visual Volume-Grounded Transformer)**，这是一个旨在改变传统体绘制（Volumetric Visualization）范式的前馈式深度学习框架。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：传统的直接体渲染（DVR）依赖密集体素网格采样，随着分辨率提升，交互性与扩展性受限。新兴的 3D 高斯泼溅（3DGS）虽然渲染极快，但现有的体素扩展方法通常需要针对每个场景进行耗时的迭代优化（Per-scene Optimization），无法实现即时的交互式探索。
*   **研究动机**：现有的前馈式（Feed-forward）3DGS 重建方法大多基于“表面中心”假设（即一个像素对应一个表面点），不适用于光线穿透、多点贡献的半透明体积数据。因此，需要一种能够直接从体积数据映射到 3DGS 表示，且无需逐场景优化的通用模型。

### 2. 方法论：VVGT 框架
VVGT 的核心思想是利用双 Transformer 架构，将多视图外观信息与 3D 几何先验进行深度融合。
*   **双 Transformer 网络 (DTN)**：
    *   **2D 外观 Transformer**：基于 DINOv2 编码器提取多视图图像特征，通过全局自注意力机制确保视图间的一致性，并显式注入相机位姿嵌入（Camera Pose Embedding）。
    *   **3D 几何 Transformer**：采用 PTV3 架构。首先利用**可变基映射（VBM）**对体积数据进行初始化，生成初始的 3D 高斯原语，然后通过 Transformer 细化这些原语的属性（位置偏移、不透明度、旋转、缩放、颜色）。
*   **体几何强制机制 (Volume Geometry Forcing, VGF)**：
    *   这是本文的关键创新。它通过**极线交叉注意力（Epipolar Cross-Attention）**建立 2D 与 3D 的联系。
    *   具体流程：将 3D 高斯点投影到所有输入视图的图像平面上，采样对应的多尺度视觉特征，使 3D 标记能够有选择地查询相关的 2D 观测信息，从而在不依赖表面假设的情况下学习准确的体积属性。
*   **训练目标**：结合了像素级 L1 损失、结构相似性（SSIM）损失和感知损失（LPIPS）。

### 3. 实验设计
*   **数据集**：使用了两类大规模直接数值模拟（DNS）数据：旋转分层湍流模拟和各向同性湍流模拟（原始分辨率 4096³）。从中提取了多个 512³ 的子体积场景，包含 100 个训练场景和 10 个测试场景。
*   **Benchmark 与对比方法**：
    *   **优化类方法**：原始 3DGS、iVRGS（针对体积优化的 3DGS）。
    *   **前馈类方法**：NoPoSplat、AnySplat（均为目前领先的通用 3DGS 重建模型）。
*   **评估指标**：PSNR、SSIM、LPIPS 以及转换耗时（Time）。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA RTX Pro 6000 GPU**（每台配备 96GB 显存）。
*   **训练时长**：训练过程持续了约 **4 天**（40K 次迭代）。
*   **推理效率**：转换一个新场景仅需 **2.4s - 4.7s**，而优化类方法通常需要 7 - 8 分钟。

### 5. 实验数量与充分性
*   **实验规模**：论文在零样本（Zero-shot）设置下对 10 个测试场景进行了评估，每个场景配合 10 种不同的传递函数（TF），总计进行了大量渲染测试。
*   **消融实验**：分别验证了 3D 几何 Transformer、VBM 初始化和 VGF 机制的有效性。
*   **充分性评价**：实验设计较为充分，特别是引入了“后优化（Post-optimization）”对比，证明了 VVGT 提供的初始表示非常接近最优，仅需极少量微调即可超越长时间优化的基准模型。实验对比涵盖了当前主流的前馈和优化路径，具有较高的客观性。

### 6. 主要结论与发现
*   **性能领先**：VVGT 在零样本泛化能力上显著优于现有的前馈式模型（如 AnySplat），在处理体积数据时表现出更强的几何一致性。
*   **速度优势**：实现了从体积数据到 3DGS 表示的近乎瞬时的转换，比传统优化方法快了两个数量级。
*   **表示转换**：证明了体绘制可以从“基于网格的采样”转向“基于表示的推理”，VVGT 成功将体积数据压缩并转化为高效的 3DGS 格式。

### 7. 优点
*   **范式创新**：首次将前馈式 3DGS 引入体绘制领域，打破了必须逐场景优化的僵局。
*   **非表面依赖**：VGF 机制巧妙地解决了体积渲染中“沿射线聚合”的特性，使其能处理复杂的半透明结构。
*   **实用性强**：支持快速的传递函数（TF）编辑和交互式探索，对科学可视化具有重要价值。

### 8. 不足与局限
*   **数据覆盖面**：目前主要在物理模拟湍流数据上验证，尚未在更大规模、更具异质性的真实医学影像（如高分辨率 CT/MRI）上进行全面测试。
*   **训练成本**：模型训练需要极高的显存（96GB VRAM）和算力支持，普通开发者难以复现训练过程。
*   **模型容量**：处理极高分辨率的解剖细节时，可能仍面临网络容量和内存瓶颈，需要进一步的架构优化。

（完）
