---
title: "GRVS: a Generalizable and Recurrent Approach to Monocular Dynamic View Synthesis"
title_zh: GRVS：一种用于单目动态视角合成的可泛化循环方法
authors: "Thomas Tanay, Mohammed Brahimi, Michal Nazarczuk, Qingwen Zhang, Sibi Catley-Chandar, Arthur Moreau, Zhensong Zhang, Eduardo Pérez-Pellitero"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2603.29734v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 单目动态视图合成与4D表示
tldr: 本研究针对单目动态视频的新视角合成挑战，提出了GRVS模型。现有方法在处理高动态区域时易失效或存在几何不一致性，且计算成本高。GRVS通过引入循环网络实现输入与目标视频间的异步映射，并利用动态平面扫描技术解耦相机与场景运动，实现了精细的六自由度相机控制。在UCSD和新推出的Kubric-4D-dyn数据集上的实验表明，该模型在重建静态和动态区域的几何细节方面优于现有的高斯泼溅和扩散模型。
selection_source: fresh_fetch
motivation: 现有的单目动态视角合成方法在处理复杂运动时常面临几何不一致性或高昂的计算开销。
method: 提出一种结合循环结构与动态平面扫描的通用模型，通过解耦相机与场景运动实现精细的视角控制。
result: 在多个数据集上，该模型在几何细节重建质量上显著超越了基于高斯泼溅和扩散模型的现有主流方法。
conclusion: GRVS为单目动态场景的新视角合成提供了一种高效、通用且具备强几何一致性的新方案。
---

## 摘要
从动态场景的单目视频中合成新视角仍然是一个具有挑战性的问题。利用显式运动先验优化 4D 表示的场景特定方法，在难以利用多视图信息的高动态区域往往会失效。将相机控制集成到大型预训练模型中的扩散方法可以生成视觉上合理的视频，但在静态和动态区域经常会出现几何不一致的问题。这两类方法都需要大量的计算资源。借鉴静态新视角合成的可泛化模型的成功经验，我们将该框架应用于动态输入，并提出了一个包含两个关键组件的新模型：(1) 一个循环回路，能够实现输入视频和目标视频之间无界且异步的映射；(2) 对动态输入高效利用平面扫描（plane sweeps）来解耦相机运动和场景运动，并实现细粒度的六自由度相机控制。我们在 UCSD 数据集和 Kubric-4D-dyn 上训练并评估了我们的模型，后者是一个新的单目动态数据集，与现有替代方案相比，具有更长、分辨率更高且场景动态更复杂的序列。我们的模型在重建静态和动态区域的细粒度几何细节方面，优于四种基于高斯泼溅（Gaussian Splatting）的场景特定方法以及两种基于扩散的方法。

## Abstract
Synthesizing novel views from monocular videos of dynamic scenes remains a challenging problem. Scene-specific methods that optimize 4D representations with explicit motion priors often break down in highly dynamic regions where multi-view information is hard to exploit. Diffusion-based approaches that integrate camera control into large pre-trained models can produce visually plausible videos but frequently suffer from geometric inconsistencies across both static and dynamic areas. Both families of methods also require substantial computational resources. Building on the success of generalizable models for static novel view synthesis, we adapt the framework to dynamic inputs and propose a new model with two key components: (1) a recurrent loop that enables unbounded and asynchronous mapping between input and target videos and (2) an efficient use of plane sweeps over dynamic inputs to disentangle camera and scene motion, and achieve fine-grained, six-degrees-of-freedom camera controls. We train and evaluate our model on the UCSD dataset and on Kubric-4D-dyn, a new monocular dynamic dataset featuring longer, higher resolution sequences with more complex scene dynamics than existing alternatives. Our model outperforms four Gaussian Splatting-based scene-specific approaches, as well as two diffusion-based approaches in reconstructing fine-grained geometric details across both static and dynamic regions.

---

## 论文详细总结（自动生成）

### 论文总结：GRVS —— 一种用于单目动态视角合成的可泛化循环方法

#### 1. 核心问题与整体含义（研究动机和背景）
从单目视频中合成动态场景的新视角（Novel View Synthesis, NVS）是计算机视觉中的难题。现有方法主要分为两类：
*   **场景特定优化法（如 4DGS）**：为每个场景单独优化 4D 表示，虽能较好重建静态区域，但在高动态区域往往失效，且每个场景需 20-40 分钟的优化，计算成本高。
*   **基于扩散模型的方法（如 Gen3C）**：利用预训练视频生成模型，虽能产生视觉合理的图像，但常存在几何不一致性，且相机控制精度较低。
**GRVS 的动机**在于结合“可泛化模型”的效率与“几何感知”的准确性，解决单目视频中相机运动与场景运动交织导致的重建难题。

#### 2. 方法论：核心思想与关键技术
GRVS 提出了一种前馈式（Feed-forward）模型，无需针对特定场景优化，其核心流程包含五个阶段：
1.  **输入选择（Input Selection）**：从视频中采样 $V$ 帧图像，采用膨胀因子 $d$ 进行均匀采样。大 $d$ 提供全局上下文（利于静态区域），小 $d$ 提供局部上下文（利于动态区域）。
2.  **动态平面扫描体（Dynamic Plane Sweep Volume, PSV）**：利用单目序列计算 PSV。通过单应性变换将输入帧投影到目标相机视角下的多个深度平面上。这一步是解耦相机运动与场景运动的关键。
3.  **分块化（Patchify）**：将高维 PSV 张量进行下采样和分块处理，以显著降低计算量并支持高分辨率输入。
4.  **循环潜变量渲染（Recurrent Latent Rendering）**：核心组件是一个 3D UNet。它不仅接收当前的 PSV，还接收经投影变换后的上一时刻循环隐藏状态 $Z_{i-1}$。这种循环机制确保了时间一致性，并允许模型通过多次迭代累积全局和局部信息。
5.  **去分块化（Unpatchify）**：将潜变量解码为最终的预测图像 $\tilde{J}_i$。

#### 3. 实验设计
*   **数据集**：
    *   **UCSD 数据集**：包含 96 个真实场景，用于验证模型在真实世界前向运动视频中的表现。
    *   **Kubric-4D-dyn（新提出）**：基于 Kubric 引擎生成的合成数据集，包含 5000 个训练场景和 100 个测试场景。相比现有数据集，它具有更高分辨率（512x512）、更长的序列和更复杂的物体动态。
*   **对比基准（Benchmarks）**：
    *   **场景特定方法**：D-3DGS、SC-GS、4DGS、MoSca。
    *   **基于扩散的方法**：GCD、Gen3C。
*   **评估指标**：PSNR、SSIM、LPIPS（分别针对全图和仅动态区域进行评估）。

#### 4. 资源与算力
*   **训练配置**：使用了 **4 块 32GB 显存的 GPU**（具体型号未标明，推测为 V100 或 A100 级别）。
*   **训练时长**：总计 100k 步迭代，耗时约 **4 天**。
*   **模型规模**：参数量约为 **40M**，远小于基于扩散模型的方法（如 Gen3C 的 7B 参数）。
*   **推理速度**：在测试时可达到约 **3 FPS**。

#### 5. 实验数量与充分性
论文进行了多维度的实验：
*   **跨数据集验证**：在真实（UCSD）和合成（Kubric）数据集上均进行了测试。
*   **消融实验**：详细研究了循环机制（Recurrence）的作用、深度平面数量 $D$、分块大小 $F$、输入视图数 $V$ 以及膨胀因子 $d$ 对性能的影响。
*   **公平性**：对比了当前最先进的各类路径（GS-based vs Diffusion-based），并针对不同距离的目标视角（Target 1/2/3）进行了细化评估。实验设计较为全面、客观。

#### 6. 主要结论与发现
*   **性能领先**：GRVS 在重建静态和动态区域的几何细节方面均优于现有的 GS 方法和扩散模型。
*   **循环机制的有效性**：通过循环迭代（逐渐减小膨胀因子 $d$），模型能同时捕获全局背景和局部动态细节，性能比无循环版本提升约 1dB。
*   **解耦能力**：动态 PSV 能有效分离相机轨迹和物体运动，使模型在处理复杂动态场景时保持几何稳定性。
*   **效率优势**：作为通用模型，它无需每场景优化，且比扩散模型更轻量、推理更快。

#### 7. 优点
*   **通用性强**：一次训练，直接推理，适用于各种单目动态视频。
*   **几何一致性高**：相比扩散模型，GRVS 产生的 3D 几何结构更准确，不会出现物体漂移或变形。
*   **异步映射**：支持自由视角渲染，包括“子弹时间”等特殊效果。
*   **轻量高效**：参数量极小（40M），对算力要求相对较低。

#### 8. 不足与局限
*   **遮挡处理**：虽然利用了循环信息，但在处理极大规模的遮挡区域（输入视频中从未出现的部分）时，生成能力可能弱于大型预训练扩散模型。
*   **单应性假设**：PSV 依赖于平面假设，在处理极端非平面几何或极近距离物体时可能存在伪影。
*   **数据依赖**：模型的泛化能力高度依赖于训练数据的多样性，对于完全超出训练分布的极端场景，表现尚待验证。

（完）
