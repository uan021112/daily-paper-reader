---
title: "Reliev3R: Relieving Feed-forward Reconstruction from Multi-View Geometric Annotations"
title_zh: Reliev3R：将前馈重建从多视图几何标注中解放出来
authors: "Youyu Chen, Junjun Jiang, Yueru Luo, Kui Jiang, Xianming Liu, Xu Yan, Dave Zhenyu Chen"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00548v1"
tags: ["query:ff3dr"]
score: 10.0
evidence: 基于单目深度的前馈重建模型（FFRMs）弱监督训练
tldr: Reliev3R 针对前馈重建模型（FFRM）过度依赖多视图几何标注（如3D点图和相机位姿）导致难以扩展的问题，提出了一种弱监督训练范式。该方法无需昂贵的几何标注或复杂的SfM预处理，而是利用预训练模型的单目相对深度和稀疏对应关系进行训练。通过引入歧义感知相对深度损失和三角重投影损失，Reliev3R 在减少数据依赖的同时，达到了与全监督模型相当的重建性能，推动了低成本、可扩展的3D重建。
selection_source: fresh_fetch
motivation: 解决前馈重建模型对多视图几何标注和计算密集型SfM预处理的过度依赖，以实现低成本的大规模训练。
method: 利用预训练模型的单目相对深度和稀疏对应关系，结合歧义感知深度损失和三角重投影损失实现多视图几何一致性监督。
result: 在不使用真实几何标注的情况下，Reliev3R 的性能追平了全监督模型，显著降低了训练成本。
conclusion: 该研究证明了通过弱监督信号训练高性能前馈重建模型的可行性，为可扩展的3D重建提供了新路径。
---

## 摘要
随着最近的进展，前馈重建模型 (FFRMs) 在重建质量和对多个下游任务的适应性方面展现出巨大潜力。然而，对多视图几何标注（例如 3D 点图和相机位姿）的过度依赖，使得 FFRMs 的全监督训练方案难以扩展。在本文中，我们提出了 Reliev3R，这是一种弱监督范式，用于在无需昂贵的多视图几何标注的情况下从头开始训练 FFRMs。我们的方法减轻了对几何传感器数据和计算密集型运动恢复结构 (SfM) 预处理的依赖，直接从预训练模型的零样本预测所提供的单目相对深度和图像稀疏对应关系中提取 3D 知识。Reliev3R 的核心在于，我们设计了一种歧义感知的相对深度损失和一种基于三角学的重投影损失，以促进多视图几何一致性的监督。通过使用更少的数据从头开始训练，Reliev3R 追赶上了其全监督的同类模型，向低成本 3D 重建监督和可扩展的 FFRMs 迈出了一步。

## Abstract
With recent advances, Feed-forward Reconstruction Models (FFRMs) have demonstrated great potential in reconstruction quality and adaptiveness to multiple downstream tasks. However, the excessive reliance on multi-view geometric annotations, e.g. 3D point maps and camera poses, makes the fully-supervised training scheme of FFRMs difficult to scale up. In this paper, we propose Reliev3R, a weakly-supervised paradigm for training FFRMs from scratch without cost-prohibitive multi-view geometric annotations. Relieving the reliance on geometric sensory data and compute-exhaustive structure-from-motion preprocessing, our method draws 3D knowledge directly from monocular relative depths and image sparse correspondences given by zero-shot predictions of pretrained models. At the core of Reliev3R, we design an ambiguity-aware relative depth loss and a trigonometry-based reprojection loss to facilitate supervision for multi-view geometric consistency. Training from scratch with the less data, Reliev3R catches up with its fully-supervised sibling models, taking a step towards low-cost 3D reconstruction supervisions and scalable FFRMs.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Reliev3R** 的新型 3D 重建训练范式，旨在解决前馈重建模型（FFRMs）对高质量多视图几何标注的过度依赖问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机与背景）
*   **核心问题**：当前最先进的前馈重建模型（如 DUSt3R, $\pi^3$ 等）虽然推理速度快、效果好，但其训练高度依赖于通过 SfM（运动恢复结构）和 MVS（多视图立体视觉）生成的 3D 点图和相机位姿标注。
*   **研究动机**：获取这些标注不仅计算成本极高，且在弱纹理或复杂场景下容易失败，严重限制了模型向大规模无标注视频数据扩展的能力。
*   **整体含义**：Reliev3R 提出了一种**弱监督训练范式**，允许模型直接从预训练模型生成的“伪标签”（单目深度和稀疏对应关系）中学习 3D 几何原理，从而摆脱对昂贵几何标注的依赖。

### 2. 方法论：核心思想与关键技术
Reliev3R 的核心思想是将 3D 重建任务分解为单目深度预测和多视图几何对齐，并利用弱监督信号进行约束：
*   **伪标签生成**：使用预训练的 `Depth Pro` 提取单目相对深度，使用 `CoTracker` 提取图像间的稀疏像素对应关系。
*   **歧义感知相对深度损失 ($L_d$)**：
    *   为了处理单目深度在不同视图间的不一致性（如天空、反射面），引入了**置信度图 $W_i$**。
    *   该损失函数会自动降低不可靠区域的权重，仅对具有几何意义的形状进行尺度不变的正则化。
*   **基于三角学的重投影损失 ($L_{rgst}$)**：
    *   这是实现 3D 空间对齐的关键。不同于传统的像素级重投影误差，该损失在 3D 空间中计算向量夹角。
    *   通过最小化预测 3D 点与相机中心连线之间的夹角，能够有效地优化相机位姿并实现深度图的全局对齐，且能避免点落在相机后方导致的梯度崩溃问题。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：DL3DV-10K（包含 10,000 个场景的大规模真实世界数据集）。
    *   **评测集**：DL3DV-benchmark（域内泛化测试）和 ScanNet++（跨域零样本测试）。
*   **对比方法**：
    *   **全监督 FFRMs**：Map-Anything, $\pi^3$, VGGT, CUT3R。
    *   **早期 FFRMs**：MVDUSt3R, FLARE。
    *   **弱监督相机估计模型**：AnyCam。
*   **Benchmark 指标**：点图重建精度（rel, $\tau$）、相机位姿误差（ATE, AUC）以及深度图精度。

### 4. 资源与算力
*   **算力设备**：使用了 **64 台 Ascend 910B3 NPU**。
*   **训练时长**：总计训练 **3 天**。
*   **训练细节**：进行了 60,000 步迭代，总 Batch Size 为 64，每个 Batch 包含 8 个视图。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了主对比实验、零样本泛化实验、半监督扩展实验以及针对超参数 $\alpha$ 的消融实验。
*   **充分性评价**：实验设计较为充分。作者特别训练了一个在相同数据集（DL3DV-10K）上使用全监督信号的基准模型（$\pi^3 \dagger$），以确保对比的公平性。通过跨数据集（ScanNet++）的测试，验证了模型在不同焦距和纵横比下的鲁棒性。

### 6. 主要结论与发现
*   **性能追赶**：Reliev3R 在不使用任何真实 3D 标注的情况下，性能超越了早期的全监督模型（如 FLARE 和 MVDUSt3R），并接近当前最先进的全监督模型。
*   **弱监督优势**：在相机位姿估计方面，Reliev3R 显著优于专门的弱监督模型 AnyCam。
*   **鲁棒性**：在零样本测试中，Reliev3R 表现出比全监督模型更好的稳健性，尤其是在深度估计的准确度上。

### 7. 优点与亮点
*   **首创性**：这是第一个实现从零开始、无需多视图几何标注训练前馈重建模型的范式。
*   **损失函数创新**：提出的三角重投影损失解决了相机位姿从随机初始化到收敛的梯度优化难题。
*   **低成本扩展**：该方法证明了利用现有的 2D/2.5D 预训练模型（如单目深度模型）来训练复杂的 3D 模型是可行的，极大地降低了数据准备门槛。

### 8. 不足与局限
*   **数据规模验证不足**：虽然理论上支持大规模扩展，但实验目前仅限于 DL3DV-10K，尚未在数百万级的超大规模数据集上验证其性能上限。
*   **动态场景限制**：目前的公式假设场景是静态的，无法处理视频中的运动物体，这限制了其在野外非受控视频中的应用。
*   **伪标签依赖**：模型的最终精度受限于所使用的单目深度模型和特征匹配模型的质量。
*   **焦距过拟合**：在零样本测试中发现，模型对训练集的相机焦距存在一定的过拟合现象。

（完）
