---
title: "SGS-Intrinsic: Semantic-Invariant Gaussian Splatting for Sparse-View Indoor Inverse Rendering"
title_zh: SGS-Intrinsic：面向稀疏视角室内逆渲染的语义不变高斯泼溅
authors: "Jiahao Niu, Rongjia Zheng, Wenju Xu, WeiShi Zheng, Qing Zhang"
date: 2026-03-29
pdf: "https://arxiv.org/pdf/2603.27516v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 用于室内逆向渲染和几何重建的神经渲染
tldr: 针对室内场景在稀疏视角下逆渲染质量差的问题，本文提出SGS-Intrinsic框架。该方法利用语义和几何先验构建稠密且几何一致的高斯语义场，为逆渲染奠定基础。通过结合混合光照模型、材质先验及去阴影模型，实现了材质与光照的精确解耦，显著提升了稀疏视角下的几何重建与材质恢复质量。
selection_source: fresh_fetch
motivation: 现有的3D高斯泼溅方法在处理室内稀疏视角图像时，难以实现高质量的几何重建和材质与光照的准确解耦。
method: 利用语义和几何先验构建一致的高斯语义场，并结合混合光照模型、光照不变材质约束及去阴影模型进行逆渲染。
result: 实验结果显示，该方法在重建保真度和逆渲染质量上均显著优于现有的基于3DGS的逆渲染方法。
conclusion: SGS-Intrinsic通过引入语义一致性和去阴影机制，有效解决了稀疏视角下室内场景的材质恢复和光照解耦难题。
---

## 摘要
我们提出了 SGS-Intrinsic，这是一个适用于稀疏视角图像的室内逆渲染框架。与现有的基于 3D 高斯泼溅 (3DGS) 的方法不同，这些方法通常侧重于以物体为中心的重建，且在稀疏视角设置下表现不佳，而我们的方法能够实现高质量的几何重建以及材质与光照的准确解耦。核心思想是在语义和几何先验的指导下，构建一个稠密且几何一致的高斯语义场，为后续的逆渲染提供可靠基础。在此基础上，我们通过结合混合光照模型和材质先验来进行材质-光照解耦，从而有效地捕捉光照与材质之间的相互作用。为了减轻投射阴影的影响并增强材质恢复的鲁棒性，我们引入了光照不变材质约束以及去阴影模型。在基准数据集上的广泛实验表明，与现有的基于 3DGS 的逆渲染方法相比，我们的方法在重建保真度和逆渲染质量方面均有持续提升。我们的代码可在 https://github.com/GrumpySloths/SGS_Intrinsic.github.io 获取。

## Abstract
We present SGS-Intrinsic, an indoor inverse rendering framework that works well for sparse-view images. Unlike existing 3D Gaussian Splatting (3DGS) based methods that focus on object-centric reconstruction and fail to work under sparse view settings, our method allows to achieve high-quality geometry reconstruction and accurate disentanglement of material and illumination. The core idea is to construct a dense and geometry-consistent Gaussian semantic field guided by semantic and geometric priors, providing a reliable foundation for subsequent inverse rendering. Building upon this, we perform material-illumination disentanglement by combining a hybrid illumination model and material prior to effectively capture illumination-material interactions. To mitigate the impact of cast shadows and enhance the robustness of material recovery, we introduce illumination-invariant material constraint together with a deshadowing model. Extensive experiments on benchmark datasets show that our method consistently improves both reconstruction fidelity and inverse rendering quality over existing 3DGS-based inverse rendering approaches. Our code is available at https://github.com/GrumpySloths/SGS_Intrinsic.github.io.

---

## 论文详细总结（自动生成）

以下是对论文《SGS-Intrinsic: Semantic-Invariant Gaussian Splatting for Sparse-View Indoor Inverse Rendering》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
该论文针对**室内场景在稀疏视角（Sparse-View）下的逆渲染（Inverse Rendering）**难题展开研究。逆渲染旨在从图像中分解出几何、材质和光照，但室内场景面临以下挑战：
*   **几何重建困难**：在稀疏视角下，传统 3D 高斯泼溅（3DGS）容易过拟合，导致几何结构不可靠。
*   **光照与材质耦合严重**：室内光照复杂（近场、高频），现有模型难以准确解耦，常导致阴影被错误地“烘焙”到材质（反射率/Albedo）中。
*   **缺乏监督信号**：稀疏视角提供的监督信息有限，难以支撑复杂的物理属性推断。

### 2. 方法论
论文提出了一个名为 **SGS-Intrinsic** 的两阶段框架：

*   **第一阶段：几何与语义重建**
    *   **初始化**：利用 VGGT 模型构建稠密的场景点云布局。
    *   **先验蒸馏**：引入预训练模型（StableNormal 和 LSEG）提供的法线和语义先验。
    *   **语义一致性约束**：在训练视角和伪视角（Pseudo-views）之间引入语义一致性损失（$L_{corresponding}$），通过区域掩码和多数投票机制缓解过拟合。
*   **第二阶段：固定几何下的逆渲染**
    *   **混合光照模型**：结合环境贴图（低频背景光）和球面高斯混合模型（SGMs，捕捉高频近场光）。
    *   **轻量化去阴影模块**：使用多分辨率哈希编码器（Hash Encoder）和 MLP 预测空间点与光源间的可见性（Visibility），显式建模阴影。
    *   **材质一致性正则化**：
        *   **光照不变性**：在固定视角下引入光照扰动，强制要求渲染的 Albedo 保持不变（$L_{relight}$）。
        *   **跨视角语义一致性**：基于“相似语义区域具有相似材质”的假设，约束 Albedo 的分布（$L_{cross\_semantic}$）。

### 3. 实验设计
*   **数据集**：
    *   **合成数据集**：Interiorverse（场景级）、TensoIR（物体级）。
    *   **真实数据集**：Mip-NeRF、FIPT、DL3DV（包含复杂光照环境）。
*   **Benchmark 与对比方法**：
    *   对比了 GSIR、GIGS、R3DG、IRGS、SVGIR 和 GeoSplat 等最先进的基于 3DGS 的逆渲染方法。
*   **评价指标**：PSNR、SSIM、LPIPS（图像质量）；MAE（法线误差）；MSE（粗糙度/材质误差）。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX 4090 GPU**。
*   **训练时长**：总计约 **40 分钟**（第一阶段 7000 次迭代，第二阶段 3000 次迭代）。
*   **预处理**：包括 VGGT、LSeg、StableNormal 等模型的推理，总计约 3 分钟。

### 5. 实验数量与充分性
*   **实验规模**：论文在 5 个不同类型的数据集上进行了测试，涵盖了从物体到场景、从合成到真实的多种情况。
*   **消融实验**：针对几何先验、混合光照、去阴影模块、材质约束等核心组件分别进行了消融测试，验证了各部分的必要性。
*   **鲁棒性测试**：专门测试了模型对噪声先验（几何、法线、语义噪声）以及不准确相机位姿的容忍度。
*   **评价**：实验设计全面，对比基准清晰，通过定量指标和定性视觉效果双重验证，体现了较高的客观性和公平性。

### 6. 主要结论与发现
*   **语义引导至关重要**：在稀疏视角下，引入语义一致性约束能显著提升几何重建的稳定性，尤其是在纹理缺失区域。
*   **显式建模阴影的必要性**：通过去阴影模块预测可见性，能有效防止阴影被错误地估计为材质颜色，从而获得更干净的 Albedo 图。
*   **混合光照的优势**：结合环境贴图和 SGMs 能够比单一光照模型更真实地还原室内复杂的点光源和面光源效果。

### 7. 优点（亮点）
*   **两阶段解耦设计**：先稳固几何再优化材质，降低了多变量同时优化的难度。
*   **创新的语义约束**：将语义特征作为一种“不变性”属性，跨视角约束材质恢复，思路新颖且有效。
*   **高效性**：在 40 分钟内完成高质量逆渲染，相比传统基于 Ray Tracing 的方法具有明显的效率优势。
*   **应用潜力**：展示了在光照编辑、物体插入和材质编辑等下游任务中的优秀表现。

### 8. 不足与局限
*   **依赖预训练模型**：方法的性能在很大程度上受限于第一阶段所使用的先验模型（如 StableNormal, LSEG）的质量。
*   **计算开销**：虽然训练时间较短，但引入多个预训练模型和哈希编码器增加了内存占用和推理复杂度。
*   **极端遮挡处理**：尽管有去阴影模块，但在极度复杂的间接光照（多次反射）环境下，物理准确性仍有提升空间。

（完）
