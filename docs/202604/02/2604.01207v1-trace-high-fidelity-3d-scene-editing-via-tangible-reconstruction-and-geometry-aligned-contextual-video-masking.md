---
title: "TRACE: High-Fidelity 3D Scene Editing via Tangible Reconstruction and Geometry-Aligned Contextual Video Masking"
title_zh: TRACE：通过实体重建与几何对齐上下文视频掩码实现的高保真三维场景编辑
authors: "Jiyuan Hu, Zechuan Zhang, Zongxin Yang, Yi Yang"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.01207v1"
tags: ["query:partseg"]
score: 9.0
evidence: 细粒度部件级操作与组件替换
tldr: TRACE是一个基于网格引导的3D高斯泼溅（3DGS）编辑框架，旨在实现高保真度的场景变换。它通过将视频扩散模型与显式3D几何锚定，实现了细粒度的局部姿态调整和组件替换，同时保持了主体结构的完整性。该方法包含多视图3D锚点合成、有形几何锚定（TGA）和上下文视频掩码（CVM）三个阶段，显著提升了编辑的灵活性和时空一致性。
selection_source: fresh_fetch
motivation: 现有的3D场景编辑方法在处理细粒度局部操作和保持结构完整性方面存在不足。
method: 提出了一种结合多视图3D锚点合成、两阶段几何注册（TGA）和自回归视频掩码（CVM）的网格引导编辑框架。
result: 实验表明，TRACE在编辑多样性和结构完整性方面均优于现有方法，能够实现物理上合理的稳定渲染。
conclusion: TRACE通过显式几何锚定和上下文视频掩码，为高保真、自动化的3D场景编辑提供了有效的解决方案。
---

## 摘要
我们提出了 TRACE，这是一个由网格（mesh）引导的 3DGS 编辑框架，实现了自动化的、高保真的场景变换。通过利用显式三维几何来锚定视频扩散过程，TRACE 独特地实现了细粒度的部件级操作——例如局部姿态偏移或组件替换——同时保留了核心主体的结构完整性，而这种能力在现有的编辑方法中很大程度上是缺失的。我们的方法包含三个关键阶段：(1) 多视图三维锚点合成，利用在 MV-TRACE 数据集（首个专门用于场景一致性物体添加和修改的多视图一致性数据集）上训练的稀疏视图编辑器来生成空间一致的三维锚点；(2) 实体几何锚定 (TGA)，通过两阶段配准确保插入网格与 3DGS 场景之间的精确空间同步；(3) 上下文视频掩码 (CVM)，将三维投影集成到自回归视频流水线中，以实现时间稳定且符合物理规律的渲染。大量实验表明，TRACE 在编辑通用性和结构完整性方面始终优于现有方法。

## Abstract
We present TRACE, a mesh-guided 3DGS editing framework that achieves automated, high-fidelity scene transformation. By anchoring video diffusion with explicit 3D geometry, TRACE uniquely enables fine-grained, part-level manipulatio--such as local pose shifting or component replacemen--while preserving the structural integrity of the central subject, a capability largely absent in existing editing methods. Our approach comprises three key stages: (1) Multi-view 3D-Anchor Synthesis, which leverages a sparse-view editor trained on our MV-TRACE datase--the first multi-view consistent dataset dedicated to scene-coherent object addition and modificatio--to generate spatially consistent 3D-anchors; (2) Tangible Geometry Anchoring (TGA), which ensures precise spatial synchronization between inserted meshes and the 3DGS scene via two-phase registration; and (3) Contextual Video Masking (CVM), which integrates 3D projections into an autoregressive video pipeline to achieve temporally stable, physically-grounded rendering. Extensive experiments demonstrate that TRACE consistently outperforms existing methods especially in editing versatility and structural integrity.

---

## 论文详细总结（自动生成）

### TRACE：通过实体重建与几何对齐上下文视频掩码实现的高保真三维场景编辑

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：尽管 3D 高斯泼溅（3DGS）在实时渲染方面取得了巨大进步，但对其进行高保真编辑仍具挑战。
*   **现有局限**：
    *   **外观中心框架**：侧重于风格和颜色，缺乏对几何结构的控制，且在 360 度视角合成时易出现“背面”伪影。
    *   **结构修改方法**：虽然引入了几何代理（如网格），但往往与原场景的光照和纹理不匹配，且难以进行细粒度的局部操作（如改变物体的姿态或替换部件）。
*   **核心目标**：提出 TRACE 框架，旨在将显式 3D 几何（Mesh）作为“锚点”来引导视频扩散模型，从而实现既具有结构稳定性又具有环境和谐度的细粒度 3D 场景编辑。

#### 2. 方法论
TRACE 包含三个关键阶段，核心思想是“几何引导生成”：
*   **多视图 3D 锚点合成 (Multi-view 3D-Anchor Synthesis)**：
    *   利用在作者自建的 **MV-TRACE 数据集**（包含 10k 对场景一致的物体添加/修改数据）上微调的 3D-LoRA 模型。
    *   通过 VLM 引导的提示词，生成空间一致的参考视图，解决单视图编辑中的深度和比例歧义。
*   **实体几何锚定 (Tangible Geometry Anchoring, TGA)**：
    *   **第一阶段：初始姿态对齐**。通过 Procrustes 优化算法，解决旋转、缩放和平移（Sim(3) 变换），将生成的 Mesh 资产与场景的单目深度先验对齐。
    *   **第二阶段：可微精细配准**。利用复合损失函数（几何损失 $L_{geo}$、掩码损失 $L_{mask}$ 和 SDF 损失 $L_{SDF}$）进行像素级同步，确保 Mesh 与 3DGS 场景精确嵌合。
*   **上下文视频掩码 (Contextual Video Masking, CVM)**：
    *   **自适应轨迹合成**：通过球面线性插值（SLERP）采样平滑视角，确保视频生成的稳定性。
    *   **自回归视频修复**：将 Mesh 投影作为掩码，利用视频扩散模型进行背景补全和光影合成，使新几何体自然融入场景（如生成阴影和反射）。

#### 3. 实验设计
*   **数据集/场景**：涵盖了来自 IN2N、BlendedMVS 和 Mip-NeRF 360 的 8 个多样化场景，共计 48 个编辑案例。
*   **任务类型**：包括几何变形（如让石马抬腿）、物体添加、纹理合成和风格迁移。
*   **对比方法 (Baselines)**：
    *   **结构持久型**：DGE, EditSplat, Vipe3dedit, GaussCtrl。
    *   **形状演化型**：TIP-Editor, GaussianEditor。
    *   **视频编辑模型**：Lucy Edit 1.1, Kling 1.6。
*   **评估指标**：CLIP Directional Similarity（语义对齐）、CLIP Similarity、DINO Similarity（多视图一致性）、Aesthetic Score（美学质量）。

#### 4. 资源与算力
*   **训练时长**：论文提到 TRACE 可以在 **10 分钟内**完成全场景编辑，具有前馈（feed-forward）效率。
*   **算力细节**：文中**未明确说明**具体的 GPU 型号（如 A100 或 H100）及具体数量，但强调了其速度优于基于迭代优化的基准方法。

#### 5. 实验数量与充分性
*   **实验规模**：在 8 个场景上进行了 48 组实验，涵盖了从全局风格到局部部件级修改的多种需求。
*   **消融实验**：针对 3D-LoRA 的有效性、TGA 的两阶段配准、以及 CVM 的精细化处理分别做了详细的消融对比。
*   **充分性评价**：实验设计较为全面，不仅有定量指标，还提供了大量的定性视觉对比（包括视频稳定性的对比），客观地展示了该方法在处理复杂几何形变时的优势。

#### 6. 主要结论与发现
*   **性能提升**：TRACE 在语义对齐指标（CLIP dir）上比基准方法提升了 **49.5%**，美学评分提升了 **13.4%**。
*   **结构完整性**：通过显式几何锚定，TRACE 能够完成基准方法无法实现的复杂结构编辑（如部件替换和姿态调整），且不会出现几何崩溃。
*   **环境和谐度**：CVM 模块成功解决了插入物体与原场景之间的光影不一致问题。

#### 7. 优点
*   **细粒度控制**：首次在 3DGS 编辑中实现了真正意义上的部件级（part-level）几何操纵。
*   **高效率**：相比于需要数小时迭代优化的方法，TRACE 的前馈流程极大地缩短了编辑时间。
*   **数据集贡献**：推出了 MV-TRACE 数据集，填补了 3D 场景一致性局部编辑数据的空白。
*   **时空一致性**：结合视频先验和 3D 掩码，保证了在连续视角下的渲染稳定性。

#### 8. 不足与局限
*   **依赖外部生成器**：编辑质量在一定程度上受限于所使用的 3D Mesh 生成器或编辑器的性能。
*   **复杂遮挡处理**：虽然有 CVM 模块，但在极端复杂的遮挡场景下，背景的完美修复仍可能存在挑战。
*   **自动化程度限制**：虽然流程自动化，但在某些复杂案例中可能仍需要人工辅助进行初始的空间对齐引导。

（完）
