---
title: "SceneExpander: Expanding 3D Scenes with Free-Form Inserted Views"
title_zh: SceneExpander：利用自由形式插入视图扩展 3D 场景
authors: "Zijian He, enjie Liu, Yihao Wang, Weizhi Zhong, Huan Yuan, Kun Gai, Guangrun Wang, Guanbin Li"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27084v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 利用生成模型和插入视图扩展3D场景
tldr: 针对3D场景构建中迭代扩展的需求，本文提出了SceneExpander，旨在将生成模型合成的新视角无缝融入现有实景重建中。面对新视角与原场景几何不对齐导致的全局一致性破坏问题，该方法通过测试时自适应技术，结合锚点蒸馏和插入视角自蒸馏信号，在保持原场景稳定的同时，有效处理了几何偏移和伪影，实现了高质量的3D场景扩展。
selection_source: fresh_fetch
motivation: 现有的3D场景扩展工作流中，用户插入的生成视角往往与原始重建存在几何不对齐，导致多视角一致性失效。
method: 提出一种基于测试时自适应的参数化前馈3D重建模型，利用锚点蒸馏稳定原场景并结合插入视角自蒸馏来适应不对齐的输入。
result: 在ETH场景和在线数据集上的实验证明，该方法在处理几何偏移时具有更好的扩展性能和更高的重建质量。
conclusion: SceneExpander为受控的3D场景迭代扩展提供了一种有效的解决方案，增强了重建模型对非对齐生成内容的鲁棒性。
---

## 摘要
使用 3D 场景表示进行世界构建对于内容创作、模拟和交互体验日益重要，然而实际的工作流程本质上是迭代的：创作者必须在用户控制下反复扩展现有场景。受此研究空白的启发，我们研究了一种以用户为中心的 3D 场景扩展工作流：从多视图图像捕获的真实场景开始，我们通过插入由生成模型合成的额外视图来扩展其覆盖范围。与固定场景中的简单对象编辑或风格迁移不同，插入的视图通常与原始重建在 3D 上不一致，从而引入几何偏移、幻觉内容或依赖于视图的伪影，破坏了全局多视图一致性。为了应对这一挑战，我们提出了 SceneExpander，它将测试时自适应应用于参数化前馈 3D 重建模型，并结合了两种互补的蒸馏信号：锚点蒸馏通过从捕获的视图中蒸馏几何线索来稳定原始场景，而插入视图自蒸馏则保留受观测支持的预测，同时调整潜在几何和外观以适应不一致的插入视图。在 ETH 场景和在线数据上的实验表明，在存在不一致的情况下，该方法具有更好的扩展行为和重建质量。

## Abstract
World building with 3D scene representations is increasingly important for content creation, simulation, and interactive experiences, yet real workflows are inherently iterative: creators must repeatedly extend an existing scene under user control. Motivated by this research gap, we study 3D scene expansion in a user-centric workflow: starting from a real scene captured by multi-view images, we extend its coverage by inserting an additional view synthesized by a generative model. Unlike simple object editing or style transfer in a fixed scene, the inserted view is often 3D-misaligned with the original reconstruction, introducing geometry shifts, hallucinated content, or view-dependent artifacts that break global multi-view consistency. To address the challenge, we propose SceneExpander, which applies test-time adaptation to a parametric feed-forward 3D reconstruction model with two complementary distillation signals: anchor distillation stabilizes the original scene by distilling geometric cues from the captured views, while inserted-view self-distillation preserves observation-supported predictions yet adapts latent geometry and appearance to accommodate the misaligned inserted view. Experiments on ETH scenes and online data demonstrate improved expansion behavior and reconstruction quality under misalignment.

---

## 论文详细总结（自动生成）

### 论文总结：SceneExpander

#### 1. 核心问题与整体含义（研究动机和背景）
在 3D 世界构建（World Building）中，创作者通常需要迭代式地扩展场景。现有的方法（如生成式视频或单次 3D 生成）难以在保留现有实景重建的基础上，受控地增加新区域。本文研究的核心问题是**利用自由形式插入的视图进行 3D 场景扩展**。
*   **挑战：** 用户通过 AI 生成模型（如 AIGC 工具）合成的新视角图像，虽然视觉上合理，但往往与原始 3D 重建在几何上不一致（3D-misaligned）。
*   **后果：** 直接将这些不一致的视图加入重建会导致原始场景崩溃、出现重影（Ghosting）或几何畸变。

#### 2. 方法论
SceneExpander 提出了一种基于**测试时自适应（Test-Time Adaptation, TTA）**的框架，通过微调预训练的前馈重建模型（以 WorldMirror 为骨干）来吸收新视图。
*   **核心思想：** 将原始捕获视图视为可靠的“锚点”，将插入的生成视图视为“带噪声的约束”，通过两种互补的蒸馏信号进行优化。
*   **关键技术细节：**
    *   **教师-学生架构：** 使用原始模型作为“锚点教师”（固定），并维护一个指数移动平均（EMA）的“生成教师”来提供动态软标签。
    *   **锚点蒸馏（Anchor Distillation）：** 在原始捕获视图上运行，强制学生模型保持原始的相机参数、深度图和法线预测，防止原始场景质量退化。
    *   **插入视图自蒸馏（Self-distillation）：** 针对插入视图，利用 EMA 教师生成的软几何目标进行引导，而非直接使用像素监督，从而提高对几何不一致的容忍度。
    *   **几何扰动增强（Geometry-Perturbation Augmentation）：** 通过全局和块状仿射变换模拟位姿漂移和局部几何不一致，增强模型处理不对齐数据的鲁棒性。
    *   **随机恢复（Stochastic Restoration）：** 定期将部分参数重置为初始权重，防止长期优化导致的模型漂移。

#### 3. 实验设计
*   **数据集：** 
    *   **ETH 场景：** 真实的户外宽基线多视图数据集。
    *   **在线数据集（Online Collection）：** 包含多样化的室内外场景。
*   **Benchmark 与对比方法：**
    *   对比了前馈重建模型：**WorldMirror**、**OmniVGGT**。
    *   对比了动态/序列重建模型：**TTT3R**。
    *   对比了优化类方法：**3D Gaussian Splatting (3DGS)**。
*   **评估指标：**
    *   **图像保真度：** PSNR、SSIM、LPIPS（针对原始区域）。
    *   **VLM 评估：** 利用 GPT-5.2 作为评估器，打分插入满意度（GPT-IS）和渲染质量（GPT-RQ）。
    *   **人类评估：** 39 名参与者对插入满意度（H-IS）和渲染质量（H-RQ）进行排序打分。

#### 4. 资源与算力
*   **算力说明：** 论文中**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   **实现细节：** 提到使用 AdamW 优化器，学习率为 $1 \times 10^{-5}$，使用 3DGS 作为底层表示，并配合微分渲染器进行测试时优化。

#### 5. 实验数量与充分性
*   **实验规模：** 实验涵盖了 ETH 和在线数据集的多个场景，并进行了详细的消融实验（验证了锚点蒸馏、数据增强、自蒸馏概率 $p$ 和随机恢复的作用）。
*   **充分性：** 引入了 GPT-5.2 自动评估和 39 人的大规模人类主观测试，这在缺乏 Ground Truth 的场景扩展任务中是非常客观且必要的。消融实验逐项拆解了技术贡献，证明了各模块的有效性。

#### 6. 主要结论与发现
*   **鲁棒性：** SceneExpander 在处理 3D 不对齐的插入视图时，表现显著优于直接优化（3DGS）或直接前馈推理（WorldMirror）。
*   **平衡性：** 通过设置较小的生成损失权重（$\lambda_G$）和较大的锚点损失权重（$\lambda_A$），可以在扩展新内容的同时，极好地保留原始场景的细节。
*   **自适应能力：** 测试时自适应（TTA）是解决生成内容与真实捕获内容之间几何冲突的有效途径。

#### 7. 优点
*   **工作流创新：** 提出了一种以用户为中心的“捕获-生成-插入-扩展”迭代式 3D 构建流程。
*   **技术巧妙：** 结合了锚点蒸馏和 EMA 自蒸馏，有效解决了 AIGC 视图常见的几何偏移问题。
*   **评估全面：** 结合了传统指标、前沿的 VLM 评估和人类主观评价，评估体系完整。

#### 8. 不足与局限
*   **严重不对齐失效：** 当插入视图与原始场景的几何冲突过于剧烈时（如文中提到的神社牌匾案例），模型仍无法完全消除重影或错误几何。
*   **依赖基础模型：** 效果高度依赖于预训练前馈重建模型（WorldMirror）的初始泛化能力。
*   **计算开销：** 虽然是测试时优化，但相比纯前馈方法，每扩展一个视角都需要额外的迭代优化时间，可能影响实时交互体验。

（完）
