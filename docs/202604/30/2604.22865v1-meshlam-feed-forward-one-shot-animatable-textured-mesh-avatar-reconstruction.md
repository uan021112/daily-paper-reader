---
title: "MeshLAM: Feed-Forward One-Shot Animatable Textured Mesh Avatar Reconstruction"
title_zh: MeshLAM：前馈式单图可驱动纹理网格头像重建
authors: "Yisheng He, Steven Hoi"
date: 2026-04-23
pdf: "https://arxiv.org/pdf/2604.22865v1"
tags: ["query:ff3dr"]
score: 10.0
evidence: 从单张图像进行单次动画网格头部重建的前馈框架
tldr: 本研究提出MeshLAM，一种前馈式单图可驱动网格头像重建框架。针对现有方法依赖耗时优化或多视图数据的问题，MeshLAM通过共享Transformer骨干网络同时处理形状和纹理，并利用迭代式GRU解码机制实现几何变形与纹理细化。该方法能从单张图像快速生成具有高保真度和拓扑完整性的可动画3D头像，在重建质量、动画效果和计算效率上均优于现有技术。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.22865v1/fig-001.webp\", \"caption\": \"Figure 2. Qualitative comparison of 3D head avatar creation and animation on challenging texture cases. Our mesh-based framework successfully models high-fidelity texture details with only 8K vertices, substantially outperforming the Gaussian-based LAM method that requires 80K Gaussian points while still failing to capture fine details.\", \"page\": 6, \"index\": 1, \"width\": 1036, \"height\": 725}, {\"url\": \"assets/figures/arxiv/2604.22865v1/fig-002.webp\", \"caption\": \"Figure 9. Qualitative comparison with more baselines.\", \"page\": 14, \"index\": 2, \"width\": 496, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2604.22865v1/fig-003.webp\", \"caption\": \"Figure 1. Overall Framework. Our method reconstructs an animatable 3D texture head mesh from a single image through dual shape and texture branches. After extracting features from the input image with a shared transformer, the shape branch predicts vertex deformations while the texture branch synthesizes UV texture maps, both conditioned on a FLAME template. Both branches are refined iteratively via GRU decoders with topology correction for shape refinement and reprojection guidance for texture enhancement.\", \"page\": 3, \"index\": 3, \"width\": 1032, \"height\": 390}, {\"url\": \"assets/figures/arxiv/2604.22865v1/fig-004.webp\", \"caption\": \"Figure 7. Reconstructed geometry and texture map visualization. Our method produces a mesh with high-fidelity texture, even when animated to an expression different from the input image. The sharp, consistently unwrapped texture details demonstrate the effectiveness of our dual-branch design.\", \"page\": 13, \"index\": 4, \"width\": 446, \"height\": 600}]"
motivation: 旨在解决现有3D头像重建方法中测试时优化耗时长以及对多视图数据依赖性强的问题。
method: 采用双形状与纹理图架构，结合迭代式GRU解码机制和重投影纹理引导，实现从单图到可驱动网格的快速生成。
result: 实验结果表明，该方法在重建精度、动画表现力以及推理速度方面均显著超越了当前最先进的技术。
conclusion: MeshLAM证明了通过前馈网络实现高质量、可直接驱动的单图3D头像重建的可行性与高效性。
---

## 摘要
我们介绍了 MeshLAM，这是一个用于单图可驱动网格头部重建的前馈框架，能够从单张图像生成高保真、可驱动的 3D 头部头像。与以往依赖耗时的测试时优化或大量多视图数据的工作不同，我们的方法通过单次前馈过程即可从单张图像生成具有固有可驱动性的完整网格表示。我们的方法采用双重形状和纹理贴图架构，利用共享 Transformer 骨干网络提取的图像特征，同时处理网格顶点和纹理贴图，从而实现连贯的形状雕刻和外观建模。为了防止前馈变形过程中的网格崩溃并确保拓扑完整性，我们提出了一种基于 GRU 的迭代解码机制，具有渐进式几何变形和纹理细化功能，并结合了一种新颖的基于重投影的纹理引导机制，将外观学习锚定到输入图像。大量实验表明，我们的方法在重建质量、动画能力和计算效率方面优于现有最先进的方法。项目主页见 https://meshlam.github.io。

## Abstract
We introduce MeshLAM, a feed-forward framework for one-shot animatable mesh head reconstruction that generates high-fidelity, animatable 3D head avatars from a single image. Unlike previous work that relies on time-consuming test-time optimization or extensive multi-view data, our method produces complete mesh representations with inherent animatability from a single image in a single forward pass. Our approach employs a dual shape and texture map architecture that simultaneously processes mesh vertices and texture map with extracted image features from a shared transformer backbone, allowing for coherent shape carving and appearance modeling. To prevent mesh collapse and ensure topological integrity during feed-forward deformation, we propose an iterative GRU-based decoding mechanism with progressive geometry deformation and texture refinement, coupled with a novel reprojection-based texture guidance mechanism that anchors appearance learning to the input image. Extensive experiments demonstrate that our method outperforms state-of-the-art approaches in reconstruction quality, animation capability, and computational efficiency. Project page at https://meshlam.github.io.

---

## 论文详细总结（自动生成）

这是一份关于论文《MeshLAM: Feed-Forward One-Shot Animatable Textured Mesh Avatar Reconstruction》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何从单张图像（One-shot）快速生成高保真、可直接驱动（Animatable）且具有完整拓扑结构的 3D 头部头像。
*   **研究背景**：
    *   **现有方法的局限**：2D 方法缺乏 3D 结构一致性；3D 意识模型（如 NeRF 或 Gaussian Splatting）通常需要耗时的逐人优化或多视图视频输入；基于 3DMM 的网格方法往往无法捕捉头发、配饰等精细几何及高分辨率纹理。
    *   **动机**：结合网格（Mesh）表示在拓扑和计算效率上的优势，利用 Transformer 的强大表征能力，实现无需测试时优化的前馈式（Feed-forward）重建。

### 2. 方法论：核心思想与关键技术
MeshLAM 采用**双分支 Transformer 架构**，通过单次前馈过程同时预测顶点变形和 UV 纹理贴图。
*   **双分支特征提取**：
    *   使用预训练的 **DINOv2** 作为骨干网络提取多尺度图像特征。
    *   **形状分支**：以 FLAME 模板顶点为输入，通过交叉注意力机制与图像特征交互。
    *   **纹理分支**：在与 UV 布局对齐的可学习 Token 网格上操作，提取空间感知的纹理特征。
*   **迭代式 GRU 解码机制**：
    *   为了防止直接回归导致的网格崩溃（Mesh Collapse），引入 **GRU 单元进行迭代细化**（通常为 2 次迭代）。
    *   **几何变形**：GRU 逐步预测顶点偏移，并结合**部分感知变形约束**（对头发、脸部、颈部设置不同的变形阈值）和**拓扑校正**（重采样、翻转边等）来维持网格完整性。
    *   **纹理细化**：引入**重投影引导**，将输入图像和当前预测误差反向投影到 UV 空间，为纹理生成提供直接的视觉监督。
*   **可选神经渲染器**：在最终输出阶段，可选择性地使用类似 StyleGAN 的 UNet 渲染器来进一步提升渲染图像的真实感。

### 3. 实验设计
*   **数据集**：主要在 **VFHQ** 数据集（包含 1.5 万个视频剪辑，约 300 万帧）上进行训练和评估。
*   **评估指标**：
    *   图像质量：PSNR, SSIM, LPIPS, FID。
    *   身份保持：CSIM（余弦相似度）。
    *   动作准确性：AKD（平均关键点距离）。
*   **对比方法**：
    *   网格类：**ROME**。
    *   高斯类：**LAM**（Large Avatar Model）。
    *   其他基准：**Portrait4Dv2**, **GAGAvatar**。

### 4. 资源与算力
*   **训练细节**：论文提到在 VFHQ 上训练 100 个 Epoch，使用 Adam 优化器、余弦退火和线性预热。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号和数量。
*   **推理效率**：重建一个完整的 3D 头像仅需 **0.7 秒**，显著快于高斯类方法（如 LAM 80K 需要 5.8 秒）。

### 5. 实验数量与充分性
*   **实验规模**：在 VFHQ 官方测试集上进行了大规模定量评估。
*   **消融实验**：非常充分，涵盖了纹理贴图必要性、GRU 迭代次数（1-3 次）、重投影机制、部分感知变形约束以及神经渲染器（UNet）的影响。
*   **应用拓展**：展示了在“文本生成 3D 头像”和“头像风格化编辑”中的应用。
*   **评价**：实验设计客观、公平，通过对比不同表示（Mesh vs Gaussian）在相同顶点/点数规模下的表现，有力证明了该方法的优越性。

### 6. 主要结论与发现
*   **网格表示的优势**：在处理高频纹理（如纹身、文字）时，Mesh + Texture Map 的组合比增加高斯点数更高效且清晰。
*   **迭代优于单次**：迭代式的 GRU 解码是解决前馈变形中网格畸变的关键。
*   **跨域能力**：该模型展现了极强的泛化性，能处理 AI 生成图像或低质量的野外（In-the-wild）图像。

### 7. 优点与亮点
*   **高效性**：实现了亚秒级的单图 3D 重建，且生成的模型可直接用于标准动画管线。
*   **高保真纹理**：通过重投影机制解决了前馈模型常见的纹理模糊问题，能捕捉到极细微的皮肤和配饰细节。
*   **拓扑稳健**：部分感知约束和拓扑校正确保了即使在长发等大幅度变形场景下，网格也不会崩溃。

### 8. 不足与局限
*   **模型依赖**：高度依赖 FLAME 模板，受限于 FLAME 的表达力，无法生成动态皱纹、舌头运动等极细微表情。
*   **参数估计误差**：重建质量受限于预训练的 FLAME 参数估计器的准确性。
*   **伦理风险**：由于能从单图生成极具真实感的 3D 身份模型，存在被用于深度伪造（Deepfake）或未经授权身份复制的风险。

（完）
