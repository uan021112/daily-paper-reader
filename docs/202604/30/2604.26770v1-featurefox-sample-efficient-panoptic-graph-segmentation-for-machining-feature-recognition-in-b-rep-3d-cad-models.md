---
title: "FeatureFox: Sample-Efficient Panoptic Graph Segmentation for Machining Feature Recognition in B-Rep 3D-CAD Models"
title_zh: FeatureFox：面向 B-Rep 3D-CAD 模型加工特征识别的样本高效全景图分割
authors: "Bertram Fuchs, Altay Kacan, Aaron Haag, Oliver Lohse"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26770v1"
tags: ["query:partseg"]
score: 9.0
evidence: 用于3D-CAD模型加工特征识别的全景图分割
tldr: 本研究提出FeatureFox，一种针对B-Rep 3D-CAD模型的全景加工特征识别方法。针对现有深度学习方法复杂度高且依赖大量数据的问题，该方法通过边缘分类定位边界、子图提取恢复实例并进行语义标注。FeatureFox在样本和计算效率上显著优于基准模型AAGNet，仅需少量训练数据即可达到高精度，并展现出极强的工业应用泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26770v1/fig-001.webp\", \"caption\": \"Fig. 4. Sample efficiency on MFInstSeg (excluding stock): PQ versus trainingset size for AAGNet and FeatureFox. FeatureFox dominates in the low-data regime and reaches > 90% PQ with ∼ 250 training parts, whereas AAGNet requires ∼ 5,000; AAGNet is only marginally better on the full training set.\", \"page\": 5, \"index\": 1, \"width\": 524, \"height\": 355}, {\"url\": \"assets/figures/arxiv/2604.26770v1/fig-002.webp\", \"caption\": \"Fig. 3. FeatureFox pipeline. Starting from a B-Rep 3D-CAD model, we construct a face-adjacency graph and attributes, propagate vertex information to edges (1), and localize the feature with a calibrated edge boundary classifier for instance grouping (2). We then compute connected-component attributes (3) and recognize the class of each predicted instance with a semantic model (4). Pink: Attribute calculation. Teal: Model training. Gray: Intermediate representation.\", \"page\": 5, \"index\": 2, \"width\": 816, \"height\": 277}]"
motivation: 现有的自动特征识别方法通常过于复杂、依赖海量标注数据，且无法同时高效处理实例分割与语义标注。
method: 该方法利用增强边缘属性的二元分类器识别特征边界，通过修剪面邻接图提取连通分量，并对子图属性进行聚合以预测加工类别。
result: 在MFInstSeg数据集上，FeatureFox仅需250个样本即可达到0.9以上的全景质量得分，且在全量训练下特征识别精度优于深度学习基准。
conclusion: FeatureFox是一种高效且实用的全景特征识别方案，凭借极低的数据需求和计算开销，能够有效应用于真实的工业CAD场景。
---

## 摘要
B-Rep 3D-CAD 模型上的自动特征识别 (AFR) 是 CAD/CAM 自动化的核心，但大多数基于学习的方法不仅复杂、对数据需求量大，而且将实例分组和语义标注分开评估。我们提出了 FeatureFox，这是一个输出带有语义标签的加工实例的全景 AFR 流水线：通过在增强的边属性上使用校准的二元边分类器来定位特征边界，在修剪后的面邻接图中将实例恢复为连通分量，并利用每个实例的分类器从聚合的子图属性中预测加工类别。我们在 MFInstSeg 数据集上使用全景质量 (PQ) 进行评估，该指标综合评分了实例分离和语义正确性。FeatureFox 的样本和计算效率显著高于深度基准模型 AAGNet，仅需约 250 个训练零件即可达到 PQ > 0.9，而 AAGNet 则需要约 5,000 个，且在完整 MFInstSeg 集上的训练在 GPU 上仅需数秒。在完整训练集上，AAGNet 在 PQ 指标上略微超过 FeatureFox，而 FeatureFox 在特征级识别和定位精度上仍保持微弱领先。最后，利用其低数据需求的优势，我们在 270 个手动标注的工业 CAD 零件上训练了 FeatureFox，并展示了其对未见过的真实工业零件的定性泛化能力，表明了其实际的现实应用价值。

## Abstract
Automatic feature recognition (AFR) on B-Rep 3D-CAD models is central to CAD/CAM automation, yet most learning-based methods are complex, data-hungry, and evaluate instance grouping and semantic labeling separately. We present FeatureFox, a panoptic AFR pipeline that outputs machining instances with semantic labels: a calibrated binary edge classifier on enriched edge attributes localizes feature boundaries, instances are recovered as connected components in a pruned face-adjacency graph, and a per-instance classifier predicts the machining class from aggregated subgraph attributes. We evaluate on MFInstSeg using Panoptic Quality (PQ), which jointly scores instance separation and semantic correctness. FeatureFox is substantially more sample- and compute-efficient than the deep baseline AAGNet, reaching $\mathrm{PQ}>0.9$ with $\sim250$ training parts versus $\sim5{,}000$ for AAGNet, and training on the full MFInstSeg set takes seconds on a GPU. On the full training set, AAGNet surpasses FeatureFox marginally in PQ, while FeatureFox remains slightly ahead in feature-level recognition and localization accuracy. Finally, leveraging its low data requirement, we train FeatureFox on $270$ manually labeled industrial CAD parts and show qualitative generalization to an unseen real industrial part, indicating practical real-world applicability.

---

## 论文详细总结（自动生成）

以下是对论文《FeatureFox: Sample-Efficient Panoptic Graph Segmentation for Machining Feature Recognition in B-Rep 3D-CAD Models》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究背景**：自动特征识别（AFR）是连接 CAD（设计）与 CAM（制造）的关键环节。传统的基于规则的方法难以应对特征交织和设计多变性，而现代深度学习方法（如 GNN）虽然泛化性强，但存在**数据饥渴**（需要数千个标注模型）、**计算昂贵**以及**任务分离**（实例分割与语义标注分开评估）等问题。
*   **核心问题**：如何在标注数据稀缺的工业环境下，实现高效、准确且能同时输出实例边界与语义标签的“全景”特征识别？
*   **整体含义**：论文提出 FeatureFox 框架，通过将全景分割引入图处理，证明了在 CAD 领域，精心设计的几何特征结合轻量化机器学习模型，在小样本场景下能显著超越复杂的深度学习模型。

### 2. 方法论
FeatureFox 采用了一种分层、可解释的流水线结构：
*   **图表示与特征增强**：基于 B-Rep 构建面邻接图（gAAG）。将面（Face）的 UV 网格信息传播至边（Edge），计算增强的边属性（如二面角、凹凸性、面积比、周长比等）。
*   **边缘分类（实例定位）**：使用**校准的二元 XGBoost 分类器**预测每条边是否为特征边界。通过等向校准（Isotonic Calibration）确保概率输出的稳定性，以便进行阈值化处理。
*   **图剪枝与聚类**：根据预测结果移除边界边，在剩余的图中通过**连通分量（Connected Components）**算法提取独立的加工特征实例。
*   **实例分类（语义识别）**：对提取出的子图进行属性聚合（如面数、对数特征长度、曲率分布、边界环结构等），使用另一个 **XGBoost 分类器**为整个实例分配语义标签（如孔、槽、口袋等）。
*   **全景评估指标**：首次在 AFR 领域倡导使用**全景质量（Panoptic Quality, PQ）**指标，统一衡量实例定位的准确性（IoU）和类别识别的正确性。

### 3. 实验设计
*   **数据集**：
    *   **MFInstSeg**：目前主流的公开加工特征数据集（约 6.2 万个零件）。
    *   **工业数据集**：作者手动标注的 270 个真实工业零件。
    *   **NIST 3D-CAD**：用于最终的跨域泛化性定性验证。
*   **Benchmark（基准）**：主要对比了 **AAGNet**（深度学习 SOTA 模型），同时也参考了 MFTReNet 和 EAGIS。
*   **对比维度**：样本效率（不同训练集规模下的 PQ 表现）、计算效率（训练耗时）、面级与特征级准确率。

### 4. 资源与算力
*   **硬件型号**：NVIDIA RTX 6000 Ada GPU。
*   **训练时长对比**：
    *   **AAGNet**：在全量数据集上训练 100 epoch 需 **38 分钟**（GPU 利用率约 18%）。
    *   **FeatureFox**：在相同环境下训练仅需 **8 秒**（GPU 利用率 100%）。
*   **效率提升**：FeatureFox 的训练速度比基准模型快了约 285 倍。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了从 50 个样本到 4.35 万个样本的**样本效率消融实验**，每组实验运行 3 个随机种子以示公平。
    *   在全量数据集上进行了面级（Face-level）和特征级（Feature-level）的全面对比。
    *   包含了一组针对真实工业零件的定性泛化实验。
*   **充分性评价**：实验设计非常客观。作者不仅展示了自家模型在小样本下的统治力，也诚实地指出在全量数据下深度学习模型（AAGNet）在 PQ 指标上仍有约 1% 的微弱优势，体现了严谨的学术态度。

### 6. 主要结论与发现
*   **样本效率极高**：FeatureFox 仅需 **250 个**训练零件即可达到 PQ > 0.9，而 AAGNet 达到同等水平需要约 **5000 个**零件。
*   **性能表现**：在全量数据训练下，FeatureFox 的特征级识别精度（95.99%）甚至略高于 AAGNet（95.49%），尽管 PQ 略低。
*   **工业可行性**：证明了在只有几百个标注样本的实际工业场景中，FeatureFox 是比深度学习更具落地价值的选择。

### 7. 优点
*   **轻量且快速**：极低的算力需求和秒级的训练速度，支持在客户端或本地服务器快速部署。
*   **全景统一性**：通过 PQ 指标解决了以往研究中实例和语义评估脱节的问题。
*   **强解释性**：基于几何属性和决策树，比“黑盒”GNN 更容易进行错误溯源和规则调整。
*   **小样本泛化**：在极少数据下即可捕捉到加工特征的核心几何逻辑。

### 8. 不足与局限
*   **非端到端风险**：采用分层架构，如果第一步的边缘预测（边界定位）出错，错误会直接传播到后续的语义分类中。
*   **复杂拓扑限制**：目前无法处理由不连续面组成的单个加工特征（例如被其他几何体完全隔断的特征实例）。
*   **数据依赖性**：虽然对样本量需求低，但仍依赖于高质量的 B-Rep 拓扑信息，对于质量较差（如存在缝隙或非流形）的 CAD 模型可能失效。

（完）
