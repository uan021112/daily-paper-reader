---
title: Towards Realistic and Consistent Orbital Video Generation via 3D Foundation Priors
title_zh: 通过 3D 基础模型先验实现逼真且一致的环绕视频生成
authors: "Rong Wang, Ruyi Zha, Ziang Cheng, Jiayu Yang, Pulak Purkait, Hongdong Li"
date: 2026-04-14
pdf: "https://arxiv.org/pdf/2604.12309v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 利用3D基础先验进行生成式3D建模以保证视频一致性
tldr: 本研究针对单图生成环绕视频中长程外推（如背面合成）一致性差的问题，提出利用3D基础模型的形状先验作为辅助约束。通过引入全局结构引导和多视图细粒度几何特征，并利用多尺度3D适配器注入视频模型，实现了具有高度几何真实感和多视角一致性的视频生成，在复杂轨迹和野外图像上表现优异。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.12309v1/fig-001.webp\", \"caption\": \"Table 1. Quantitative comparison with baseline methods. Best results are marked as bold. Our method achieves superior results than baseline methods [21, 25, 42, 46, 47, 55] in all visual quality, shape realism and view consistency metrics on multiple benchmarks [9, 10].\", \"page\": 6, \"index\": 1, \"width\": 975, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2604.12309v1/fig-002.webp\", \"caption\": \"Figure 6. We show more results of generated frames on diverse input views and unseen objects.\", \"page\": 15, \"index\": 2, \"width\": 1036, \"height\": 1067}, {\"url\": \"assets/figures/arxiv/2604.12309v1/fig-003.webp\", \"caption\": \"Figure 4. Reconstructed textured meshes from generated video results. Similar to [25, 32], our method can be extended to reconstruct textured 3D meshes, which also demonstrates the multi-view consistency of generated videos.\", \"page\": 14, \"index\": 3, \"width\": 832, \"height\": 732}, {\"url\": \"assets/figures/arxiv/2604.12309v1/fig-004.webp\", \"caption\": \"Figure 3. Qualitative Comparisons with NVS methods [5, 13, 37] for completeness. Our method produces more realistic results with improved fidelity in shape and appearances details. Moreover, we directly output smooth video results leveraging temporal priors from general video model [3], as shown in supplementary videos.\", \"page\": 13, \"index\": 4, \"width\": 1024, \"height\": 733}, {\"url\": \"assets/figures/arxiv/2604.12309v1/fig-005.webp\", \"caption\": \"Figure 7. Qualitative Comparisons with 3D generation methods [23, 28, 31]. Our method produces results with higher fidelity compared to 3DGS based methods [28, 31] and ensures better aligned base colors over foundation models that explicitly estimate mesh texture maps [23, 36]. This further verifies the efficacy of native video generation methods in producing visually improved results.\", \"page\": 16, \"index\": 5, \"width\": 1012, \"height\": 919}]"
motivation: 现有视频生成方法主要依赖像素级注意力，在处理缺乏像素对应关系的长程视角切换时，难以保持物体的几何结构一致性。
method: 提出利用3D基础模型的全局潜向量和投影体积特征作为几何约束，并通过多尺度3D适配器将这些特征注入预训练视频模型。
result: 实验证明该方法在视觉质量、形状真实感和多视角一致性方面均优于现有技术，且对复杂轨迹和野外图像具有鲁棒的泛化能力。
conclusion: 结合3D基础模型的形状先验能有效提升视频生成的几何一致性，为高质量单图转视频任务提供了高效且通用的解决方案。
---

## 摘要
我们提出了一种从单张物体图像生成几何逼真且一致的环绕视频的新方法。现有的视频生成工作主要依赖像素级注意力机制来增强帧间的视角一致性。然而，这种机制对于长距离外推（例如后视图合成）无法施加足够的约束，因为在这些情况下，与输入图像的像素对应关系非常有限。因此，这些工作往往无法生成具有合理且连贯结构的结果。为了解决这一问题，我们建议利用 3D 基础生成模型中丰富的形状先验作为辅助约束，其动机在于该模型能够建模从大规模 3D 资产库中学习到的逼真物体形状分布。具体而言，我们使用 3D 基础模型编码的两种尺度的潜特征来引导视频生成：(i) 作为整体结构指导的去噪全局潜向量，以及 (ii) 从体积特征投影得到的一组潜图像，用以提供视角相关的细粒度几何细节。与深度图或法向图等常用的 2.5D 表示相比，这些紧凑的特征可以建模完整的物体形状，并通过避免显式的网格提取来提高推理效率。为了实现有效的形状调节，我们引入了一个多尺度 3D 适配器，通过交叉注意力机制将特征标记注入基础视频模型，这保留了模型在通用视频预训练中的能力，并实现了一个简单且与模型无关的微调过程。在多个基准测试上的广泛实验表明，与现有最先进的方法相比，我们的方法在视觉质量、形状逼真度和多视角一致性方面均取得了优异的效果，并能稳健地泛化到复杂的摄像机轨迹和野外图像。

## Abstract
We present a novel method for generating geometrically realistic and consistent orbital videos from a single image of an object. Existing video generation works mostly rely on pixel-wise attention to enforce view consistency across frames. However, such mechanism does not impose sufficient constraints for long-range extrapolation, e.g. rear-view synthesis, in which pixel correspondences to the input image are limited. Consequently, these works often fail to produce results with a plausible and coherent structure. To tackle this issue, we propose to leverage rich shape priors from a 3D foundational generative model as an auxiliary constraint, motivated by its capability of modeling realistic object shape distributions learned from large 3D asset corpora. Specifically, we prompt the video generation with two scales of latent features encoded by the 3D foundation model: (i) a denoised global latent vector as an overall structural guidance, and (ii) a set of latent images projected from volumetric features to provide view-dependent and fine-grained geometry details. In contrast to commonly used 2.5D representations such as depth or normal maps, these compact features can model complete object shapes, and help to improve inference efficiency by avoiding explicit mesh extraction. To achieve effective shape conditioning, we introduce a multi-scale 3D adapter to inject feature tokens to the base video model via cross-attention, which retains its capabilities from general video pretraining and enables a simple and model-agonistic fine-tuning process. Extensive experiments on multiple benchmarks show that our method achieves superior visual quality, shape realism and multi-view consistency compared to state-of-the-art methods, and robustly generalizes to complex camera trajectories and in-the-wild images.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **Towards Realistic and Consistent Orbital Video Generation via 3D Foundation Priors** 的方法，旨在通过引入 3D 基础模型的形状先验，解决单张图像生成环绕视频时几何结构不一致和失真的问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何从单张物体图像生成既逼真又具有高度几何一致性的 360 度环绕视频。
*   **研究动机**：
    *   **现有方法的局限**：目前的视频生成模型（如 SVD）主要依赖像素级注意力机制。在处理大幅度视角变化（如合成物体的背面）时，由于缺乏与输入图像的像素对应关系，模型往往会生成扭曲、不自然的形状（即“Janus 问题”）。
    *   **2.5D 先验不足**：使用深度图或法向图等 2.5D 先验只能提供单视角约束，无法建模物体的完整 3D 结构。
    *   **3D 基础模型的潜力**：最近的 3D 基础模型（如 Hunyuan3D）在大规模 3D 数据上预训练，具备强大的物体形状分布建模能力，可以作为视频生成的强力几何约束。

### 2. 方法论
核心思想是将 3D 基础模型的潜空间特征作为“形状先验”，通过一个适配器注入到视频扩散模型中。
*   **3D 形状先验提取**：
    *   **全局结构引导**：利用 3D 基础模型（Hunyuan3D）提取一个去噪后的全局潜向量（Global Latent Vector），代表物体的整体拓扑结构。
    *   **细粒度几何细节**：将 3D 体积特征（Volumetric Features）投影到多个标准视角（如前、后、左、右等），生成一组潜图像（Latent Images），提供视角相关的局部细节。
*   **多尺度 3D 适配器（Multi-scale 3D Adapter）**：
    *   设计了一个轻量级的插件模块，通过**交叉注意力机制（Cross-Attention）**将上述全局和局部特征注入到基础视频模型（SVD）的 Transformer 块中。
    *   这种设计允许模型保留预训练视频生成的时序平滑性，同时受到 3D 几何的严格约束。
*   **推理流程**：输入图像 -> 3D 基础模型提取先验 -> 视频模型在先验引导下生成 21 帧环绕视频。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：使用 Objaverse-XL 的子集（约 16 万个 3D 资产）渲染的高质量环绕视频。
    *   **测试集**：Objaverse-XL（150 个未见物体）和 GSO（100 个未见物体）。
*   **Benchmark 与指标**：
    *   **视觉质量**：PSNR, SSIM, LPIPS。
    *   **形状真实感**：CLIP-Score（评估语义一致性）。
    *   **多视角一致性**：MEt3R（新提出的衡量帧间像素一致性的指标）。
*   **对比方法**：
    *   **视频生成类**：SV3D, Hi3D。
    *   **新视角合成（NVS）类**：Wonder3D, Era3D。
    *   **3D 重建类**：Trellis, Hunyuan3D-2（通过渲染重建后的网格得到视频）。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA H200 GPU** 进行训练。
*   **训练细节**：使用 Adam 优化器，学习率为 $1 \times 10^{-5}$，有效 Batch Size 为 16，训练迭代次数为 **80K 次**。
*   **推理效率**：在单张 H200 上，提取 3D 先验约需 2.25 秒，视频生成每步增加约 0.28 秒，整体流程非常高效，避免了耗时的网格提取过程。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流基准数据集（Objaverse-XL, GSO）上进行了定量对比，并提供了大量的定性视觉对比图。
*   **消融实验**：针对“有无 3D 先验”、“全局 vs 局部特征”、“适配器设计变体（连接 vs 堆叠 vs 交叉注意力）”进行了详细测试。
*   **泛化性测试**：展示了在“野外图像（In-the-wild）”和“动态摄像机轨迹（非零仰角）”下的生成效果。
*   **评价**：实验设计全面，对比了不同技术路线（视频生成 vs 3D 重建），验证了 3D 先验在解决几何畸变方面的核心作用，实验结果具有较高的说服力。

### 6. 主要结论与发现
*   **3D 先验是关键**：引入 3D 基础模型的形状先验能显著提升视频在极端视角下的结构稳定性。
*   **多尺度特征互补**：全局向量负责“骨架”稳定，局部投影特征负责“细节”填充，两者结合效果最优。
*   **适配器优越性**：相比于直接修改输入（如 Frame Stack），交叉注意力适配器能更好地平衡预训练视频知识与新增的 3D 约束。
*   **性能领先**：在所有测试指标上，该方法均优于 SV3D 和 Hi3D 等现有最先进方法。

### 7. 优点
*   **几何鲁棒性**：有效解决了单图转视频中常见的物体结构“崩坏”问题。
*   **推理高效**：无需显式重建 3D 网格或点云，直接在潜空间操作，速度快。
*   **通用性强**：适配器设计不改变原模型结构，理论上可以插入任何基于 Transformer 的视频生成模型。

### 8. 不足与局限
*   **分辨率限制**：生成的视频质量受限于基础视频模型（SVD）的分辨率，对于极细微的纹理表现有限。
*   **纹理先验未充分利用**：目前仅利用了 3D 模型的“形状”先验，未利用其“纹理”先验，导致在处理未观察区域的复杂颜色时仍有提升空间。
*   **依赖基础模型**：如果 3D 基础模型本身对某些复杂场景（如极小物体）预测失败，生成的视频也会受到影响。

（完）
