---
title: "GlobalSplat: Efficient Feed-Forward 3D Gaussian Splatting via Global Scene Tokens"
title_zh: GlobalSplat：通过全局场景 Token 实现的高效前馈 3D 高斯泼溅
authors: "Roni Itkin, Noam Issachar, Yehonatan Keypur, Xingyu Chen, Anpei Chen, Sagie Benaim"
date: 2026-04-16
pdf: "https://arxiv.org/pdf/2604.15284v2"
tags: ["query:ff3dr"]
score: 10.0
evidence: 通过全局场景令牌实现高效的前馈3D高斯泼溅
tldr: 本研究提出GlobalSplat，一种高效的前馈3D高斯泼溅框架。针对现有方法依赖局部对齐导致表示冗余和全局一致性差的问题，该方法采用“先对齐后解码”策略，通过全局场景Token编码多视图输入并解决跨视图对应关系。结合由粗到细的训练课程，GlobalSplat能以极少的Gaussian数量（约16K）实现紧凑且一致的场景重建，在保持高渲染质量的同时显著降低了存储开销并提升了推理速度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.15284v2/fig-001.webp\", \"caption\": \"Fig. 2: GlobalSplat Architecture Overview. Given a sparse set of input views, image features are extracted via a View Encoder. A fixed set of learnable latent scene tokens is iteratively refined through a dual-branch encoder block (repeated B times) designed to explicitly disentangle geometry and appearance. Within each branch, queries (QG, QA) cross-attend to multi-view features (KI , VI) and self-attend to global context. The streams are fused via a Mixer MLP to update the tokens for the subsequent block. Specialized Geometry and Appearance Decoders then transform these globallyaware tokens into explicit 3D Gaussians. As depicted on the right, the network employs a Coarse-to-Fine Training Curriculum strategy to progressively increase the decoded Gaussian capacity, supervised jointly by rendering, self-supervised consistency, and regularization losses.\", \"page\": 6, \"index\": 1, \"width\": 707, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2604.15284v2/fig-002.webp\", \"caption\": \"Fig. 1: Align First, Decode Later. Top: Existing feed-forward 3D Gaussian Splatting pipelines rely on view-centric, per-pixel primitive allocation. As the number of input views increases, these approaches bake massive redundancy into the 3D representation, scaling to hundreds of thousands or millions of Gaussians. In contrast, GlobalSplat aggregates multi-view inputs into a fixed set of global latent scene tokens before decoding geometry. This achieves an optimal balance: for instance, for 24 input views on RealEstate10K, GlobalSplat delivers highly competitive novel-view synthesis quality (28.5 PSNR), utilizing only 16K Gaussians and offering ultra-low GPU memory usage (1.79 GB), minimal disk size (<4 MB), and extremely fast inference times (<78 ms). Bottom Right: This scene-centric approach translates to a significantly stronger practical operating point in comparison to state-of-the art approaches, as demonstrated in the radar chart (evaluated on 24 input views on RealEstate10K). Bottom Left: By decoding from a global scene context (without a fixed grid), GlobalSplat stays sparse and places primitives at occupied 3D locations. This yields an adaptive allocation where low-frequency regions are covered by fewer Gaussians with larger spatial support (higher coverage), enabling complex scenes with far fewer primitives. We visualize the Gaussians as disks corresponding to their scale, as well as the Gaussian centers using a point cloud. As can be seen, this allows the model to effectively capture complex environments with drastically fewer primitives.\", \"page\": 2, \"index\": 2, \"width\": 719, \"height\": 568}, {\"url\": \"assets/figures/arxiv/2604.15284v2/fig-003.webp\", \"caption\": \"Table 6: Implementation details and training hyperparameters.\", \"page\": 28, \"index\": 3, \"width\": 520, \"height\": 856}, {\"url\": \"assets/figures/arxiv/2604.15284v2/fig-004.webp\", \"caption\": \"Fig. 4: Qualitative comparison on ACID. We compare GlobalSplat against baselines (Zpressor, DepthSplat, GGN, C3G) and the ground truth (GT) across 6 different ACID scenes (rows).\", \"page\": 19, \"index\": 4, \"width\": 716, \"height\": 742}, {\"url\": \"assets/figures/arxiv/2604.15284v2/fig-005.webp\", \"caption\": \"Fig. 3: Qualitative comparison. We compare GlobalSplat against baselines (Zpressor, DepthSplat, GGN, C3G) and the ground truth (GT) across 6 different scenes (rows).\", \"page\": 12, \"index\": 5, \"width\": 716, \"height\": 742}]"
motivation: 现有的前馈3DGS方法多采用像素或体素对齐，导致在处理多视图输入时存在严重的表示冗余和全局一致性缺失。
method: 提出一种“先对齐后解码”的架构，利用全局潜在场景表示编码多视图信息，并通过由粗到细的训练策略逐步增加解码容量。
result: 在RealEstate10K和ACID数据集上，仅需16K个高斯点（约4MB）即可实现高质量重建，且单次前馈推理时间低于78毫秒。
conclusion: GlobalSplat证明了通过全局Token化表示可以实现极度紧凑且高效的前馈3D场景重建，有效解决了密集基准方法的冗余与性能权衡问题。
---

## 摘要
基元的有效空间分配是 3D 高斯泼溅（3D Gaussian Splatting）的基础，因为它直接决定了表示紧凑性、重建速度和渲染保真度之间的协同作用。以往的解决方案，无论是基于迭代优化还是前馈推理，都在这些目标之间面临显著的权衡，这主要是由于它们依赖于缺乏全局场景感知、由启发式驱动的局部分配策略。具体而言，目前的前馈方法大多是像素对齐或体素对齐的。通过将像素反投影为密集的、视点对齐的基元，它们在 3D 资产中引入了冗余。随着输入视图的增加，表示规模也随之增大，且全局一致性变得脆弱。为此，我们提出了 GlobalSplat，这是一个基于“先对齐，后解码”原则构建的框架。我们的方法学习一种紧凑的全局潜空间场景表示，在解码任何显式 3D 几何结构之前，先对多视图输入进行编码并解决跨视图对应关系。至关重要的一点是，这种公式化方法实现了紧凑且全局一致的重建，而无需依赖预训练的像素预测骨干网络，也不需要重用来自密集基准模型的潜特征。通过利用一种逐渐增加解码能力的由粗到精的训练课程，GlobalSplat 从根本上防止了表示膨胀。在 RealEstate10K 和 ACID 数据集上，我们的模型在仅使用 1.6 万个高斯函数的情况下，实现了具有竞争力的创新视角合成性能，这远少于密集流水线所需的高斯数量，并获得了仅 4MB 的轻量化占用空间。此外，GlobalSplat 的推理速度显著快于基准模型，单次前馈运行时间低于 78 毫秒。项目页面见 https://r-itk.github.io/globalsplat/。

## Abstract
The efficient spatial allocation of primitives serves as the foundation of 3D Gaussian Splatting, as it directly dictates the synergy between representation compactness, reconstruction speed, and rendering fidelity. Previous solutions, whether based on iterative optimization or feed-forward inference, suffer from significant trade-offs between these goals, mainly due to the reliance on local, heuristic-driven allocation strategies that lack global scene awareness. Specifically, current feed-forward methods are largely pixel-aligned or voxel-aligned. By unprojecting pixels into dense, view-aligned primitives, they bake redundancy into the 3D asset. As more input views are added, the representation size increases and global consistency becomes fragile. To this end, we introduce GlobalSplat, a framework built on the principle of align first, decode later. Our approach learns a compact, global, latent scene representation that encodes multi-view input and resolves cross-view correspondences before decoding any explicit 3D geometry. Crucially, this formulation enables compact, globally consistent reconstructions without relying on pretrained pixel-prediction backbones or reusing latent features from dense baselines. Utilizing a coarse-to-fine training curriculum that gradually increases decoded capacity, GlobalSplat natively prevents representation bloat. On RealEstate10K and ACID, our model achieves competitive novel-view synthesis performance while utilizing as few as 16K Gaussians, significantly less than required by dense pipelines, obtaining a light 4MB footprint. Further, GlobalSplat enables significantly faster inference than the baselines, operating under 78 milliseconds in a single forward pass. Project page is available at https://r-itk.github.io/globalsplat/

---

## 论文详细总结（自动生成）

以下是对论文《GlobalSplat: Efficient Feed-Forward 3D Gaussian Splatting via Global Scene Tokens》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的前馈 3D 高斯泼溅（3DGS）方法主要依赖“像素对齐”或“体素对齐”的策略。这意味着模型会将每个像素或网格点反投影为 3D 基元，导致随着输入视图数量的增加，高斯点的数量呈爆炸式增长（冗余巨大），且难以维持全局一致性。
*   **核心问题**：如何在单次前馈推理中，利用多视图输入生成一个**极度紧凑**、**全局一致**且**渲染高质量**的 3D 场景表示，同时摆脱对输入视图数量的线性依赖？

### 2. 方法论
*   **核心思想**：提出**“先对齐，后解码”（Align First, Decode Later）**的原则。不再从单视图局部特征直接生成高斯点，而是先将所有视图信息聚合到一组固定数量的“全局场景 Token”中，再由这些 Token 解码出最终的 3D 几何。
*   **关键技术细节**：
    *   **场景标准化**：通过相似变换将所有相机位姿映射到规范坐标系，并根据相机群直径进行尺度缩放，提供几何先验。
    *   **双分支编码器**：采用迭代注意力机制，将几何（位置、形状）与外观（颜色、球谐系数）特征分离处理。固定数量的潜在 Token（如 2048 个）通过交叉注意力从多视图图像中提取信息。
    *   **由粗到精（Coarse-to-Fine）训练课程**：初始阶段将每个 Token 对应的多个候选高斯合并为一个，随着训练进行逐渐增加解码容量（如最终每个 Token 解码出 8 个高斯，总计 16K），确保模型先学习全局结构再优化局部细节。
    *   **自监督一致性损失**：将输入视图分为两个子集，分别进行前馈重建，并要求两者的渲染深度和透明度图保持一致，增强了结构的鲁棒性。

### 3. 实验设计
*   **数据集**：
    *   **RealEstate10K**：主要用于训练和评估室内外漫游场景。
    *   **ACID**：用于测试模型在自然、无边界景观上的**零样本（Zero-shot）泛化能力**。
*   **Benchmark 设置**：评估 12、24、36 个输入视图下的新视角合成质量。
*   **对比方法**：
    *   密集型前馈方法：NoPoSplat, AnySplat, DepthSplat, Zpressor 等。
    *   紧凑型前馈方法：C3G（并发工作）, GGN。
    *   非高斯方法：LVSM。

### 4. 资源与算力
*   **硬件环境**：所有效率基准测试均在单张 **NVIDIA A100 GPU (64GB VRAM)** 上完成。
*   **训练细节**：
    *   训练步数：220,000 步。
    *   全局 Batch Size：16。
    *   学习率：$5 \times 10^{-4}$，采用余弦退火衰减。
*   **推理效率**：单次前馈推理时间 **< 78 毫秒**，峰值显存占用仅 **1.79 GB**。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的定量对比（PSNR, SSIM, LPIPS, 高斯数量, 磁盘占用, 推理时间）和定性视觉对比。
*   **消融实验**：非常充分，涵盖了双分支架构、由粗到精策略、一致性损失、相机元数据注入等关键组件的有效性验证。
*   **客观性**：通过在不同视图数量（12/24/36）下进行测试，证明了该方法在处理大上下文时的稳定性，且跨数据集（ACID）的实验验证了其非过拟合性。

### 6. 主要结论与发现
*   **极度紧凑性**：GlobalSplat 仅需 **16K** 个高斯点即可达到甚至超过使用数十万个高斯点的基准模型效果，存储空间仅需 **4MB**（减少了 99% 以上的冗余）。
*   **视图无关性**：无论输入视图是多少，输出的高斯点数量保持恒定，解决了表示膨胀问题。
*   **性能领先**：在 RealEstate10K 上达到 28.5 PSNR，同时推理速度比 Zpressor 快 2.5 倍，比 GGN 快 20 倍以上。

### 7. 优点
*   **效率巅峰**：在保持高保真度的前提下，实现了极低的显存占用和极快的推理速度，非常适合移动端部署。
*   **架构创新**：通过全局 Token 化和双分支解耦，有效解决了多视图融合中的几何冲突问题。
*   **训练稳定**：由粗到精的课程学习策略显著提升了紧凑表示下的收敛质量。

### 8. 不足与局限
*   **固定容量限制**：对于极其复杂或城市级的超大规模场景，固定的 16K Token 可能会遇到表达能力瓶颈。
*   **静态场景假设**：目前仅针对静态场景设计，未考虑动态物体的处理。
*   **极稀疏视角挑战**：在极少视角（如 2-3 个）输入时，由于缺乏足够的视差信息来构建全局潜在空间，表现可能受限。

（完）
