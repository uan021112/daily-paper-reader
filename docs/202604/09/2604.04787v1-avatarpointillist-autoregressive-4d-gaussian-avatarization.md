---
title: "AvatarPointillist: AutoRegressive 4D Gaussian Avatarization"
title_zh: AvatarPointillist：自回归 4D 高斯化身生成
authors: "Hongyu Liu, Xuan Wang, Yating Wang, Zijian Wu, Ziyu Wan, Yue Ma, Runtao Liu, Boyao Zhou, Yujun Shen, Qifeng Chen"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.04787v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 从单张图像生成动态4D高斯头像
tldr: AvatarPointillist 是一种从单张肖像图像生成动态 4D 高斯化身的创新框架。该方法采用仅解码器的 Transformer 架构，通过自回归方式生成 3D 高斯点云，能够根据主体复杂度动态调整点密度。通过联合预测每点的绑定信息实现逼真动画，并利用专用解码器提升渲染保真度。该研究为高质量、可控的化身生成提供了一种全新的自回归范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04787v1/fig-001.webp\", \"caption\": \"Figure 3. Overview of our framework. It consists of two modules: an autoregressive (AR) model for Gaussian geometry generation and a Gaussian Decoder for predicting rendering attributes. The AR model takes image features from DINOv2 [50] and point cloud features as input. The point cloud feature extract via Pixel3DMM [15] and a point cloud encoder [85]. The AR model is trained to generate a Gaussian point cloud via next-token prediction, where each point is represented by four quantized tokens (T x n , T y n , T z n , T b n) corresponding to coordinates and binding information. After generation, the tokens are de-quantized to obtain the actual coordinates. We then combine the positional embeddings Pn with the internal features F p n from the AR model as input to the Gaussian Decoder to predict the final accurate Gaussian attributes. Finally, the result is animated using Linear Blend Skinning (LBS) and the binding information.\", \"page\": 4, \"index\": 1, \"width\": 975, \"height\": 386}, {\"url\": \"assets/figures/arxiv/2604.04787v1/fig-002.webp\", \"caption\": \"Figure 5. Visualization of ablation study on input setting of Gaussian decoder. The leftmost column shows the input. The FLAME Positions baseline, similar to the LAM method, uses the canonical FLAME mesh vertices as a template and only applies decoder-predicted offsets to deform this template into a final Gaussian point cloud. Pointwise AR Feature refers to using only the AR features (F p n ) without positional information, while Positional Encoding uses only the point embeddings (Pn) without AR features.\", \"page\": 8, \"index\": 2, \"width\": 794, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2604.04787v1/fig-003.webp\", \"caption\": \"Figure 4. Qualitative comparison with state-of-the-art methods. The leftmost column shows the input images, with the target image displayed in the bottom-right corner. The first row presents self-reenactment results, while the remaining three rows show cross-reenactment results. Our method demonstrates superior performance in expression and pose consistency, as well as better identity preservation compared to other approaches.\", \"page\": 7, \"index\": 3, \"width\": 826, \"height\": 709}, {\"url\": \"assets/figures/arxiv/2604.04787v1/fig-004.webp\", \"caption\": \"Table 1. Quantitative evaluation of state-of-the-art methods and our approach on the NeRSemble dataset [32]. ↓ indicates lower is better while ↑ indicates higher is better. Red highlights the best result, and Blue highlights the second-best result.\", \"page\": 7, \"index\": 4, \"width\": 825, \"height\": 203}]"
motivation: 旨在解决从单张图像生成高质量、可动态变形且点云分布自适应的 4D 数字化身挑战。
method: 利用自回归 Transformer 顺序生成点云及其绑定信息，并结合高斯解码器将潜在特征转化为可渲染的属性。
result: 实验证明该方法能够生成高保真、光影真实且具备良好动画控制能力的 4D 动态化身。
conclusion: 自回归点云生成范式为 4D 高斯化身建模提供了一种高效、灵活且具有启发性的新路径。
---

## 摘要
我们介绍了 AvatarPointillist，这是一个从单张肖像图像生成动态 4D 高斯化身（avatar）的新型框架。我们方法的核心是一个仅解码器（decoder-only）的 Transformer，它以自回归方式为 3D 高斯泼溅（3D Gaussian Splatting）生成点云。这种序列化方法允许进行精确且自适应的构建，根据主体的复杂程度动态调整点密度和总点数。在点生成过程中，自回归（AR）模型还联合预测每个点的绑定信息，从而实现逼真的动画效果。生成后，一个专门的高斯解码器将这些点转换为完整的、可渲染的高斯属性。我们证明，将解码器以 AR 生成器的潜在特征为条件，可以实现阶段间的有效交互，并显著提高保真度。大量实验验证了 AvatarPointillist 能够生成高质量、照片级真实感且可控的化身。我们认为这种自回归公式代表了化身生成的新范式，我们将发布代码以启发未来的研究。

## Abstract
We introduce AvatarPointillist, a novel framework for generating dynamic 4D Gaussian avatars from a single portrait image. At the core of our method is a decoder-only Transformer that autoregressively generates a point cloud for 3D Gaussian Splatting. This sequential approach allows for precise, adaptive construction, dynamically adjusting point density and the total number of points based on the subject's complexity. During point generation, the AR model also jointly predicts per-point binding information, enabling realistic animation. After generation, a dedicated Gaussian decoder converts the points into complete, renderable Gaussian attributes. We demonstrate that conditioning the decoder on the latent features from the AR generator enables effective interaction between stages and markedly improves fidelity. Extensive experiments validate that AvatarPointillist produces high-quality, photorealistic, and controllable avatars. We believe this autoregressive formulation represents a new paradigm for avatar generation, and we will release our code inspire future research.

---

## 论文详细总结（自动生成）

这是一份关于论文《AvatarPointillist: AutoRegressive 4D Gaussian Avatarization》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文旨在解决从**单张肖像图像**生成高质量、可驱动的**4D动态化身（Avatar）**的挑战。
*   **研究背景**：现有的3D高斯泼溅（3DGS）化身生成方法（如LAM、GAGAvatar）通常依赖于固定的点云模板（如FLAME模型的顶点）或固定的点数。
*   **核心问题**：这种“硬性模板”限制了模型捕捉特定身份细节（如独特的发型、胡须、配饰）的能力，无法发挥3DGS自适应调整点分布的优势。
*   **整体含义**：提出了一种全新的**自回归（AR）生成范式**，让模型像写文章一样“点对点”地生成高斯点云，从而实现几何结构的动态自适应构建。

### 2. 方法论
AvatarPointillist 包含两个核心模块：**自回归几何生成器**和**高斯属性解码器**。
*   **自回归几何生成（AR Model）**：
    *   使用仅解码器（Decoder-only）的 Transformer 架构。
    *   将每个高斯点表示为四个离散化的 Token：$(T_x, T_y, T_z, T_b)$，其中前三个是空间坐标，最后一个是**绑定索引（Binding Index）**，用于将点关联到 FLAME 变形模板的特定三角面片上。
    *   **输入条件**：通过 DINOv2 提取图像特征，并结合 Pixel3DMM 提取的粗略 3D 面部特征，通过交叉注意力机制注入 Transformer。
    *   **生成逻辑**：按照 $yzx$ 顺序排列点云，通过“预测下一个 Token”的方式顺序生成整个点云序列。
*   **高斯属性解码（Gaussian Decoder）**：
    *   在生成点云坐标后，使用另一个 Transformer 解码器预测每个高斯点的颜色、不透明度、缩放、旋转和位置偏移。
    *   **关键创新**：解码器不仅以坐标为输入，还以 AR 模型生成的**内部隐藏层特征**为条件，显著提升了渲染的保真度。
*   **动画驱动**：利用预测的绑定信息，通过线性混合蒙皮（LBS）技术，使生成的 3D 高斯点云随 FLAME 模型的表情和姿态参数动态变形。

### 3. 实验设计
*   **数据集**：主要使用 **Nersemble** 数据集（包含 419 个身份的多视图视频）。
*   **Benchmark（基准测试）**：
    *   **自驱动（Self-reenactment）**：使用同一人的图像驱动。
    *   **跨人驱动（Cross-reenactment）**：使用不同人的动作序列驱动。
*   **对比方法**：
    *   NeRF 类方法：Portrait4Dv2, AvatarArtist。
    *   3DGS 类方法：LAM, GAGAvatar。
*   **评估指标**：LPIPS、FID（图像质量）、AKD（表情准确度）、APD（姿态一致性）、CLIPScore（身份保持度）。

### 4. 资源与算力
论文明确说明了训练配置：
*   **AR 模型训练**：使用 **16 台 NVIDIA H20 GPU**，训练 50K 步，Batch Size 为 4。
*   **高斯解码器训练**：使用 **8 台 NVIDIA H20 GPU**，训练 12.5K 步，Batch Size 为 4。
*   **优化器**：AR 阶段使用 AdamW，解码器阶段使用 Adam。

### 5. 实验数量与充分性
*   **实验规模**：在 419 个身份中随机抽取 25 个作为测试集，其余用于训练。
*   **消融实验**：
    1.  **AR 模型有效性**：对比了使用固定 FLAME 顶点（类似 LAM）与 AR 生成点云的效果。
    2.  **输入特征分析**：验证了 AR 隐藏层特征对解码器性能的贡献。
*   **充分性评价**：实验涵盖了定性视觉对比、定量指标分析以及针对核心组件的消融研究。对比了当前最先进的 NeRF 和 3DGS 方案，实验设计较为客观、公平。

### 6. 主要结论与发现
*   **自回归范式的优越性**：AR 模型能够根据图像内容自适应地在复杂区域（如头发、胡须）放置更多点，生成的几何结构远比固定模板精确。
*   **特征耦合的重要性**：将 AR 模型的中间潜在特征传递给高斯解码器，比单纯使用坐标位置编码能获得更好的渲染细节。
*   **性能领先**：在所有定量指标（尤其是 FID 和身份保持度）上，AvatarPointillist 均显著优于现有的单图 4D 化身生成方法。

### 7. 优点
*   **几何灵活性**：打破了拓扑结构的限制，能够处理非面部区域（如长发、配饰）的建模。
*   **高保真度**：结合了 AR 模型的强大建模能力和 3DGS 的实时渲染优势。
*   **端到端一致性**：通过联合预测绑定信息，解决了生成点云与动画驱动之间的对齐问题。

### 8. 不足与局限
*   **推理延迟**：由于采用自回归序列生成，生成点云的时间随点数增加而线性增长，可能比非自回归方法慢。
*   **序列长度限制**：虽然使用了滑动窗口技术，但处理极大规模点云（如全身化身）时，长序列的建模压力依然存在。
*   **数据依赖**：模型性能高度依赖于高质量 4D 扫描数据的预处理和拟合质量。

（完）
