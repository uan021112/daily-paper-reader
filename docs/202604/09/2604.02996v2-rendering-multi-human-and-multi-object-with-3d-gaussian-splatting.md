---
title: Rendering Multi-Human and Multi-Object with 3D Gaussian Splatting
title_zh: 使用 3D 高斯泼溅进行多人物与多物体渲染
authors: "Weiquan Wang, Jun Xiao, Feifei Shao, Yi Yang, Yueting Zhuang, Long Chen"
date: 2026-04-03
pdf: "https://arxiv.org/pdf/2604.02996v2"
tags: ["query:3dgen"]
score: 9.0
evidence: 从稀疏视图输入中重建包含多个人物和物体的动态场景
tldr: 针对从稀疏视角重建包含多个互动人物和物体的动态场景（MHMO）这一挑战，本文提出了基于3D高斯泼溅（3DGS）的层次化框架MM-GS。该方法通过单实例多视图融合模块建立一致的实例表示，并利用场景级实例交互模块建模复杂的相互依赖关系。实验证明，MM-GS在处理严重遮挡和复杂交互方面表现优异，实现了高保真度的渲染效果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02996v2/fig-001.webp\", \"caption\": \"Fig. 2. Overview of MM-GS pipeline. Our method refines initial 3D Gaussian representations through three main stages. (a) Human-Object Deformation: We initialize the scene by deforming canonical human and object models to their target poses and representing them as collections of 3D Gaussians. (b) Per-Instance Multi-View Fusion: A Cross-View Fusion network refines each instance’s appearance and local geometry by aggregating visual features from all its visible viewpoints, ensuring a view-consistent representation. (c) Scene-Level Instance Interaction: Finally, an Instance Interaction network operates on a global scene graph to model the dependencies between all participants, enabling a final refinement to capture interaction-driven effects.\", \"page\": 4, \"index\": 1, \"width\": 1052, \"height\": 476}, {\"url\": \"assets/figures/arxiv/2604.02996v2/fig-002.webp\", \"caption\": \"Fig. 1. Core challenges in Multi-Human Multi-Object (MHMO) rendering. From sparse views, rendering complex interactions involves overcoming two key challenges: ensuring cross-view consistency under severe occlusion (top) and modeling the mutual influence between instances at contact regions (bottom). Our MM-GS is designed to address both.\", \"page\": 1, \"index\": 2, \"width\": 512, \"height\": 487}]"
motivation: 现有的动态场景重建方法在处理多人物、多物体间的严重遮挡和复杂交互依赖时，难以保持视图一致性并准确建模互动关系。
method: 提出MM-GS框架，结合单实例多视图融合模块以聚合视觉信息，并利用基于全局场景图的交互模块来优化实例属性。
result: 在挑战性数据集上的实验表明，该方法显著优于现有基准，能够生成具有高保真细节和合理接触关系的渲染结果。
conclusion: MM-GS为多主体动态场景的重建提供了有效的层次化建模方案，显著提升了复杂交互场景下的渲染质量。
---

## 摘要
从稀疏视角输入中重建包含多个交互人物和物体的动态场景是一项关键且具有挑战性的任务，对于为机器人技术和 VR/AR 创建高保真数字孪生至关重要。我们将这一问题称为多人物多物体（MHMO）渲染，它面临两个主要障碍：在严重的相互遮挡下实现单个实例的视角一致性表示，以及显式建模由交互产生的复杂且组合式的依赖关系。为了克服这些挑战，我们提出了 MM-GS，这是一个基于 3D 高斯泼溅（3D Gaussian Splatting）构建的新型分层框架。我们的方法首先采用单实例多视角融合模块，通过聚合所有可用视角的视觉信息，为每个实例建立鲁棒且一致的表示。随后，场景级实例交互模块在全局场景图上运行，以推理所有参与者之间的关系，并细化其属性以捕捉细微的交互效果。在挑战性数据集上的广泛实验表明，我们的方法显著优于强基准模型，在保持高保真细节和合理的实例间接触方面取得了最先进的结果。

## Abstract
Reconstructing dynamic scenes with multiple interacting humans and objects from sparse-view inputs is a critical yet challenging task, essential for creating high-fidelity digital twins for robotics and VR/AR. This problem, which we term Multi-Human Multi-Object (MHMO) rendering, presents two significant obstacles: achieving view-consistent representations for individual instances under severe mutual occlusion, and explicitly modeling the complex and combinatorial dependencies that arise from their interactions. To overcome these challenges, we propose MM-GS, a novel hierarchical framework built upon 3D Gaussian Splatting. Our method first employs a Per-Instance Multi-View Fusion module to establish a robust and consistent representation for each instance by aggregating visual information across all available views. Subsequently, a Scene-Level Instance Interaction module operates on a global scene graph to reason about relationships between all participants, refining their attributes to capture subtle interaction effects. Extensive experiments on challenging datasets demonstrate that our method significantly outperforms strong baselines, producing state-of-the-art results with high-fidelity details and plausible inter-instance contacts.

---

## 论文详细总结（自动生成）

以下是对论文《Rendering Multi-Human and Multi-Object with 3D Gaussian Splatting》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在**稀疏视角**输入下，实现包含多个交互人物与物体的动态场景（MHMO）的高保真重建与渲染。
*   **研究动机**：
    *   **遮挡严重**：在多主体场景中，实例间的相互遮挡导致单视角信息缺失，传统方法难以保持视角间的一致性。
    *   **交互复杂**：人物与物体、人物与人物之间的接触、阴影等细微交互效应难以通过独立的实例建模来捕捉。
    *   **应用需求**：机器人导航、VR/AR 及数字孪生技术需要能够实时渲染且具备高物理可信度的动态场景。

### 2. 方法论
MM-GS 采用了一种**分层细化框架**，核心流程分为三个阶段：
*   **人类-物体变形初始化**：
    *   **人类**：基于 SMPL 参数化模型，利用调制的线性混合蒙皮（LBS）将 3D 高斯分布从标准姿态变形到目标姿态。
    *   **物体**：利用已知的刚体变换将物体模板对齐到场景中。
*   **单实例多视角融合（Per-Instance Multi-View Fusion）**：
    *   利用 2D CNN 提取各视角特征，通过**点特征提升（Point-wise Feature Lifting）**将 2D 特征投影至 3D 高斯点。
    *   采用跨视角融合机制，让未遮挡视角的视觉信息引导被遮挡区域的重建，确保单实例的几何与外观一致性。
*   **场景级实例交互（Scene-Level Instance Interaction）**：
    *   **场景图构建**：以实例为节点，基于空间包围盒（Bounding Box）的重叠关系建立边。
    *   **交互建模**：使用**图注意力网络（GAT）**在实例间传播信息，专门优化接触区域的高斯属性（如位置偏移、颜色和透明度），以消除穿透伪影并生成接触阴影。

### 3. 实验设计
*   **数据集**：
    *   **HOI-M³**：包含客厅、健身房、办公室等复杂室内多主体交互场景。
    *   **CORE4D-Real**：侧重于双人协作搬运物体的真实场景。
*   **Benchmark 与对比方法**：
    *   **NeuralHOIFVV-MM**：基于 NeRF 的多主体扩展版本。
    *   **GTU-MM**：基于 SOTA 动态 3DGS 方法（GTU）扩展的多主体版本。
*   **评估指标**：PSNR、SSIM、LPIPS。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA A100 GPU**。
*   **训练时长**：根据消融实验数据，完整模型的训练时间约为 **40 分钟**。
*   **渲染性能**：推理速度达到 **160+ FPS**，具备极强的实时性。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流交互数据集的多个子序列上进行了测试，涵盖了 3 视角和 4 视角等极稀疏输入情况。
*   **消融实验**：系统地验证了“视角融合”和“实例交互”两个核心模块对最终质量的贡献。
*   **充分性评价**：实验设计较为全面，对比了 NeRF 和 3DGS 两种不同技术路线的强基准，结果显示 MM-GS 在所有指标上均有显著提升，证明了其分层设计的有效性。

### 6. 主要结论与发现
*   **分层建模的必要性**：先解决单实例的视角一致性，再解决实例间的交互依赖，是处理复杂 MHMO 场景的有效路径。
*   **交互优化效果**：引入场景图和 GAT 后，接触边界变得更加清晰，解决了传统方法中常见的“物体嵌入人体”或“边缘模糊”问题。
*   **稀疏视角鲁棒性**：通过跨视角特征聚合，模型在仅有 3-4 个视角的情况下仍能保持较高的渲染保真度。

### 7. 优点
*   **首创性**：首次专门针对多人物-多物体（MHMO）这一复杂渲染任务提出了系统性的 3DGS 解决方案。
*   **高质量交互**：通过显式的交互模块，能够生成物理上更合理的接触效果和视觉细节。
*   **效率平衡**：在显著提升渲染质量的同时，保留了 3DGS 的实时渲染优势。

### 8. 不足与局限
*   **先验依赖**：模型高度依赖于准确的 SMPL 参数、物体 CAD 模板以及预先标注的位姿和掩码（Mask）。
*   **物理范围限制**：目前主要针对刚体和人体，对于流体、布料等非刚体物体的复杂物理交互建模尚不完善。
*   **野外场景挑战**：在缺乏精确相机参数或存在动态背景干扰的非受控环境中，性能可能会受到影响。

（完）
