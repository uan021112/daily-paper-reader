---
title: "WorldMesh: Generating Navigable Multi-Room 3D Scenes via Mesh-Conditioned Image Diffusion"
title_zh: WorldMesh：通过网格条件图像扩散生成可导航的多房间3D场景
authors: "Manuel-Andreas Schneider, Angela Dai"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22972v2"
tags: ["query:3dgen"]
score: 10.0
evidence: 根据文本描述生成可导航的多房间3D场景
tldr: 针对现有文本生成图像/视频模型在生成大规模3D场景时缺乏几何一致性的问题，本文提出WorldMesh。该方法采用“几何优先”策略，将场景生成解耦为网格支架构建和基于网格条件的图像合成。通过先构建包含墙壁地板的几何网格并填充物体，再利用网格引导扩散模型生成外观，实现了具有高物体丰富度、强一致性且可无限扩展的多房间3D场景生成。
selection_source: fresh_fetch
motivation: 现有的生成模型因缺乏显式几何约束，在生成大规模、多房间3D场景时难以保持场景和物体的一致性。
method: 采用几何优先的方法，先根据文本构建网格支架并填充物体，再将其作为条件引导图像扩散模型生成具有一致性的外观。
result: 成功生成了具有高真实感、物体多样性且具备鲁棒3D一致性的大规模可导航多房间场景。
conclusion: 该研究通过解耦结构与外观，为生成环境级、沉浸式的3D世界迈出了重要一步。
---

## 摘要
图像和视频合成的最新进展激发了其在推动3D场景生成方面的应用。然而，我们观察到，由于缺乏显式的几何结构，文本到图像和文本到视频的方法在超出有限的环境尺度时，难以维持场景级和物体级的一致性。因此，我们提出了一种几何优先的方法，将大规模3D场景合成这一复杂问题解耦为结构组成（表现为网格支架）和逼真的外观合成（利用以网格支架为条件的强大图像合成模型）。根据输入的文本描述，我们首先构建一个捕捉环境几何结构（墙壁、地板等）的网格，然后利用图像合成、分割和物体重建，以逼真的布局在网格结构中填充物体。随后，该网格支架被渲染以作为图像合成的条件，为一致的外观生成提供结构骨架。这使得生成具有高度物体丰富度和多样性的、可扩展且任意尺寸的3D场景成为可能，并将鲁棒的3D一致性与照片级真实的细节相结合。我们相信，这标志着向生成真正的环境级沉浸式3D世界迈出了重要一步。

## Abstract
Recent progress in image and video synthesis has inspired their use in advancing 3D scene generation. However, we observe that text-to-image and -video approaches struggle to maintain scene- and object-level consistency beyond a limited environment scale due to the absence of explicit geometry. We thus present a geometry-first approach that decouples this complex problem of large-scale 3D scene synthesis into its structural composition, represented as a mesh scaffold, and realistic appearance synthesis, which leverages powerful image synthesis models conditioned on the mesh scaffold. From an input text description, we first construct a mesh capturing the environment's geometry (walls, floors, etc.), and then use image synthesis, segmentation and object reconstruction to populate the mesh structure with objects in realistic layouts. This mesh scaffold is then rendered to condition image synthesis, providing a structural backbone for consistent appearance generation. This enables scalable, arbitrarily-sized 3D scenes of high object richness and diversity, combining robust 3D consistency with photorealistic detail. We believe this marks a significant step toward generating truly environment-scale, immersive 3D worlds.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **WorldMesh** 的新型 3D 场景生成框架，旨在解决大规模、多房间 3D 环境生成中的一致性难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的“文本到 3D”方法（基于图像或视频扩散模型）在生成单物体或单房间场景时表现出色，但在扩展到**大规模、多房间环境**时，由于缺乏显式的几何约束，容易出现空间不一致、物体漂移、几何畸变以及房间连接处断裂等问题。
*   **研究动机**：作者认为，要生成可导航的、环境级的 3D 世界，必须将**全局空间结构（几何）**与**局部照片级细节（外观）**解耦。通过“几何优先”的策略，利用显式的网格支架（Mesh Scaffold）来锚定扩散模型的生成过程。

### 2. 方法论：核心思想与关键技术
WorldMesh 采用分阶段的流水线，将复杂问题拆解为以下步骤：
*   **网格支架构建（Mesh Scaffold Construction）**：
    *   使用大语言模型（Claude Opus）根据文本提示生成 JSON 格式的平面图（包含墙厚、层高、房间布局）。
    *   将平面图转换为 3D 结构网格（$M_{struct}$），处理共享墙体和门窗开口。
*   **物体实例化（Object Instantiation）**：
    *   渲染结构网格的深度图，引导图像生成模型（Flux）在房间内“想象”物体布局。
    *   利用分割模型（SAM3）和物体重建模型（SAM-3D-Objects）将生成的 2D 物体转化为 3D 几何体，并根据重力方向和支撑面放置在网格中，形成完整的几何网格 $M_{geo}$。
*   **网格锚定的外观合成（Mesh-Anchored Appearance Synthesis）**：
    *   **初始纹理化**：通过投影纹理映射将初始生成的图像贴回网格。
    *   **条件图像扩散**：使用图像合成模型（如 Nano Banana Pro），以“渲染的深度+带纹理的网格+风格参考图”为条件，迭代生成多视角图像。
    *   **视角生成策略**：采用螺旋式视角采样，确保相邻视角间有最大的旋转相似性，以维持风格连续。
*   **图像验证与 3DGS 优化**：
    *   **几何验证**：通过 Canny 边缘检测比较生成图与网格结构的边缘召回率，剔除不符合几何逻辑的幻觉图像。
    *   **几何正则化 3DGS**：使用 3D 高斯泼溅（3DGS）表示场景，在优化时加入深度损失函数（来自网格支架），防止几何漂移。

### 3. 实验设计
*   **场景与数据集**：生成了多种风格的大规模场景（如斯堪的纳维亚公寓、哥特式豪宅、超现实环境等），支持多房间连接。
*   **对比基准（Benchmarks）**：
    *   **布局引导类**：SpatialGen。
    *   **视频扩散类**：WorldExplorer, FlexWorld。
    *   **迭代提升类**：WonderWorld。
    *   **全景图类**：DreamScene360, LayerPano3D。
*   **评估指标**：
    *   **自动指标**：CLIP-IQA+（图像质量）、CLIP Aesthetic（美学评分）。
    *   **人工评测**：31 名参与者对物体一致性、结构一致性和整体质量进行 1-5 分打分，并进行两两偏好比较。

### 4. 资源与算力
*   **硬件环境**：所有结果均在单块 **NVIDIA RTX A5000 GPU (24 GB VRAM)** 上生成。
*   **生成规模**：每个房间生成 24 张高分辨率图像（1376 × 768）。
*   **模型调用**：本地运行 Flux2-Klein 进行物体放置，通过 API 调用 Nano Banana Pro 进行最终的外观合成。

### 5. 实验数量与充分性
*   **实验规模**：展示了多个具有挑战性的多房间案例，涵盖了从写实到超现实的多种视觉主题。
*   **消融实验**：非常充分。作者对比了“仅深度”、“深度+墙体+包围盒”、“深度+物体几何”以及“全条件（WorldMesh）”四种方案，直观展示了网格支架各组件对维持 3D 一致性的必要性。
*   **客观性**：通过大规模用户调研（31人）和两两偏好测试（胜率高达 96.2%），证明了该方法在处理近距离物体旋转和跨房间过渡时的显著优势。

### 6. 主要结论与发现
*   **解耦的有效性**：先构建几何支架再填充外观的策略，能有效解决纯 2D 扩散模型在 3D 空间中的“多面人”或几何扭曲问题。
*   **可扩展性**：该方法随房间数量线性扩展，能够生成真正可导航的环境级 3D 世界。
*   **几何引导的重要性**：实验表明，仅靠深度图不足以维持一致性，带有初始纹理和物体几何的网格支架是实现高质量 3D 重建的关键。

### 7. 优点（亮点）
*   **强一致性**：在物体近距离观察和跨房间移动时，表现出远超基准方法的稳定性。
*   **高丰富度**：结合了 LLM 的布局能力和图像扩散模型的物体想象力，场景内容极其丰富且符合逻辑。
*   **自动化流程**：从文本到最终的 3DGS 场景，实现了高度自动化的流水线。

### 8. 不足与局限
*   **层级限制**：目前仅限于单层布局，无法直接处理多层建筑（如复式或多层楼房）。
*   **物体背面缺失**：依赖的 SAM-3D-Objects 在处理被遮挡的物体背面时可能产生不完整的几何。
*   **交互成本**：虽然可以自动化，但物体分割阶段若追求极高精度，有时仍需轻微的人工点选干预（尽管文中提到可用 VLM 自动化）。
*   **实时性**：由于涉及多次图像扩散生成和 3DGS 优化，生成一个完整多房间场景的时间成本仍然较高。

（完）
