---
title: "TopoMesh: High-Fidelity Mesh Autoencoding via Topological Unification"
title_zh: TopoMesh：通过拓扑统一实现高保真网格自编码
authors: "Guan Luo, Xiu Li, Rui Chen, Xuanyu Yi, Jing Lin, Chia-Hao Chen, Jiahang Liu, Song-Hai Zhang, Jianfeng Zhang"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24278v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 为3D生成流水线提出了一种高保真网格自编码器
tldr: 针对3D生成中VAE重建质量受限的问题，本文提出TopoMesh。它通过Dual Marching Cubes (DMC) 框架统一了真值与预测网格的拓扑结构，解决了传统VAE因表示不匹配而依赖间接监督的问题。通过引入显式的顶点和面级监督信号，以及稀疏体素架构和渐进式训练，TopoMesh在保持几何细节和锐利特征方面显著优于现有方法。
selection_source: fresh_fetch
motivation: 现有3D VAE因真值网格与预测场之间的拓扑不匹配，导致无法进行显式网格级监督，从而难以保留锐利几何细节。
method: 提出一种基于DMC框架的稀疏体素VAE，通过重网格化统一拓扑结构，实现显式的顶点位置和面方向监督。
result: 实验表明，TopoMesh在重建保真度上显著超越了现有VAE模型，特别是在锐利特征和几何细节的还原上表现优异。
conclusion: 通过拓扑统一建立显式网格监督是提升3D网格重建质量的有效途径，为高保真3D生成奠定了基础。
---

## 摘要
高保真 3D 生成的主流范式依赖于 VAE-Diffusion 流水线，其中 VAE 的重建能力决定了生成质量的上限。限制现有 VAE 的一个根本挑战是真实值（GT）网格与网络预测之间的表示不匹配：GT 网格具有任意且可变的拓扑结构，而 VAE 通常预测固定结构的隐式场（例如正则网格上的 SDF）。这种固有的不匹配阻碍了建立显式的网格级对应关系，迫使先前的工作依赖于 SDF 或渲染损失等间接监督信号。因此，在重建过程中，精细的几何细节（特别是锐利特征）难以得到良好保留。为了解决这一问题，我们提出了 TopoMesh，这是一种基于稀疏体素的 VAE，它在共享的双重行进立方体（DMC）拓扑框架下统一了 GT 网格和预测网格。具体而言，我们通过一种重网格化算法将任意输入网格转换为符合 DMC 标准的表示，该算法使用 L∞ 距离度量来保留锐利边缘。我们的解码器以相同的 DMC 格式输出网格，确保预测网格和目标网格共享相同的拓扑结构。这在顶点和面级别建立了显式的对应关系，使我们能够为拓扑、顶点位置和面方向推导出具有清晰梯度的显式网格级监督信号。我们的稀疏 VAE 架构采用了这一统一框架，并结合教师强制（Teacher Forcing）和渐进式分辨率训练，以实现稳定且高效的收敛。大量实验表明，TopoMesh 在重建保真度方面显著优于现有的 VAE，实现了对锐利特征和几何细节的卓越保留。

## Abstract
The dominant paradigm for high-fidelity 3D generation relies on a VAE-Diffusion pipeline, where the VAE's reconstruction capability sets a firm upper bound on generation quality. A fundamental challenge limiting existing VAEs is the representation mismatch between ground-truth meshes and network predictions: GT meshes have arbitrary, variable topology, while VAEs typically predict fixed-structure implicit fields (\eg, SDF on regular grids). This inherent misalignment prevents establishing explicit mesh-level correspondences, forcing prior work to rely on indirect supervision signals such as SDF or rendering losses. Consequently, fine geometric details, particularly sharp features, are poorly preserved during reconstruction. To address this, we introduce TopoMesh, a sparse voxel-based VAE that unifies both GT and predicted meshes under a shared Dual Marching Cubes (DMC) topological framework. Specifically, we convert arbitrary input meshes into DMC-compliant representations via a remeshing algorithm that preserves sharp edges using an L$\infty$ distance metric. Our decoder outputs meshes in the same DMC format, ensuring that both predicted and target meshes share identical topological structures. This establishes explicit correspondences at the vertex and face level, allowing us to derive explicit mesh-level supervision signals for topology, vertex positions, and face orientations with clear gradients. Our sparse VAE architecture employs this unified framework and is trained with Teacher Forcing and progressive resolution training for stable and efficient convergence. Extensive experiments demonstrate that TopoMesh significantly outperforms existing VAEs in reconstruction fidelity, achieving superior preservation of sharp features and geometric details.

---

## 论文详细总结（自动生成）

### TopoMesh: 通过拓扑统一实现高保真网格自编码论文总结

#### 1. 论文的核心问题与整体含义
在当前的 3D 生成流水线（如 VAE-Diffusion）中，变分自编码器（VAE）的重建能力是决定生成质量上限的关键瓶颈。**核心挑战在于“表示不匹配”**：真实值（GT）网格具有任意且多变的拓扑结构，而现有的 VAE 通常预测固定的隐式场（如正则网格上的 SDF）。这种不匹配导致无法建立显式的网格级对应关系，迫使模型依赖 SDF 监督或渲染损失等间接信号，从而导致锐利边缘、孔洞等几何细节在重建中严重丢失。

#### 2. 论文提出的方法论
TopoMesh 提出了**拓扑统一（Topological Unification）**的原则，通过以下核心组件实现显式的网格级监督：

*   **Topo-Remesh（重网格化算法）**：
    *   **核心思想**：将任意输入网格转换为符合双重行进立方体（DMC）标准的表示。
    *   **关键技术**：引入 **$L_\infty$ 距离度量**代替传统的 $L_2$ 距离。$L_\infty$ 考虑了局部表面的平面结构，能够有效防止在表面扩张（Dilation）过程中磨平锐利角点。
    *   **流程**：体素化 -> 洪水填充 -> 基于 $L_\infty$ 的 SDF 计算 ->  isosurface 提取（使用 ODC 算法） -> DMC 压缩。
*   **Topo-VAE（稀疏体素架构）**：
    *   **Encoder**：采用**稀疏体素-点交叉注意力机制**。每个点仅与其所在的体素交互，将注意力图的存储开销从 $O(N \times P)$ 降低到 $O(P)$，支持处理数百万个点。
    *   **Decoder**：基于 FlexiCubes 但进行了**拓扑与几何解耦**。将参数分为拓扑（占用率 $o$）和几何（位移 $\delta$、插值权重等），从而实现对顶点位置和面方向的直接监督。
*   **训练策略**：
    *   **教师强制（Teacher Forcing）**：训练期间向解码器提供 GT 拓扑，避免拓扑预测错误导致的几何梯度不稳定。
    *   **渐进式分辨率训练**：从 32³ 逐渐提升至 128³ 分辨率，加速收敛并捕捉多尺度细节。

#### 3. 实验设计
*   **数据集**：
    *   训练集：Sketchfab（320k 高质量网格）。
    *   评估集：Objaverse、Thingi10K、Dora-Bench（1.4k 物体）。
    *   新基准：**Topo-Bench**（包含 1k 个具有大量锐利边缘的复杂物体）。
*   **对比方法**：
    *   **VecSet 类**：TripoSG, Dora, Hunyuan3D-2.1。
    *   **稀疏体素类**：Trellis, Direct3D-S2, SparseFlex。
*   **衡量指标**：倒角距离（CD）、F1 分数、法向一致性（ANC），以及专门针对锐利特征的 **F1-Sharp**。

#### 4. 资源与算力
*   **训练细节**：
    *   VAE 训练：总计 700k 步（32³ 160k, 64³ 160k, 128³ 380k），Batch Size 为 64。
    *   DiT 训练：总计 800k 步，Batch Size 为 512。
*   **算力说明**：论文**未明确说明具体的 GPU 型号和数量**，但提到重网格化算法在 GPU 上全加速，1024³ 分辨率处理仅需约 15 秒。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了大规模的定量对比（涵盖数千个测试物体）和定性视觉展示。
*   **消融实验**：针对 $L_\infty$ 距离的有效性、网格级监督与渲染监督的对比、多分辨率推理效果进行了详细消融。
*   **充分性评价**：实验设计较为全面，特别是引入了针对锐利边缘的 F1-Sharp 指标，能够客观地反映模型在处理工业件、建筑等硬表面物体时的优势。对比方法涵盖了当前最先进的开源和商业模型，具有较高的说服力。

#### 6. 论文的主要结论与发现
*   **拓扑统一是关键**：通过让预测值和 GT 共享 DMC 拓扑，建立显式对应关系，可以大幅提升重建保真度。
*   **$L_\infty$ 的优越性**：相比 $L_2$ 距离，$L_\infty$ 能够完美保留 90 度等锐利角点的二面角分布。
*   **性能提升**：在 Topo-Bench 上，TopoMesh 的 F-Score 相比现有方法提升了约 8%，且在 1024³ 分辨率下表现出极强的细节还原能力。

#### 7. 优点
*   **理论创新**：首次在 VAE 框架中提出拓扑统一的概念，解决了长期存在的表示不匹配问题。
*   **特征保留**：通过 $L_\infty$ 距离和显式法向监督，解决了 3D 重建中“磨圆”锐利边缘的顽疾。
*   **高效性**：GPU 加速的重网格化和稀疏注意力机制使其能够处理极高分辨率的几何数据。

#### 8. 不足与局限
*   **计算开销**：尽管使用了稀疏架构，但在 128³ 潜空间及更高分辨率上进行上采样仍需消耗大量计算资源。
*   **分辨率限制**：重网格化算法受限于基础体素分辨率，无法捕捉小于单个体素尺寸的超微细细节。
*   **应用范围**：目前主要聚焦于几何结构，对于材质（PBR）和纹理的联合学习在文中讨论较少。

（完）
