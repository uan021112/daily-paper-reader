---
title: "SLARM: Streaming and Language-Aligned Reconstruction Model for Dynamic Scenes"
title_zh: SLARM：面向动态场景的流式语言对齐重建模型
authors: "Zhicheng Qiu, Jiarui Meng, Tong-an Luo, Yican Huang, Xuan Feng, Xuanfu Li, ZHan Xu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22893v2"
tags: ["query:ff3dr"]
score: 10.0
evidence: 用于动态场景重建的前馈模型
tldr: SLARM是一个前馈模型，旨在统一动态场景重建、语义理解和实时流式推理。它通过高阶运动建模捕捉复杂非均匀运动，并利用LSeg蒸馏实现语言对齐的语义表示。该模型采用基于窗口的因果注意力机制，支持低延迟流式处理且不累积内存开销。在动态估计、渲染质量和场景解析方面均达到SOTA水平，显著提升了运动精度和重建质量。
selection_source: fresh_fetch
motivation: 旨在解决现有动态场景重建方法在处理复杂运动、语义对齐及实时流式推理方面的不足。
method: 提出一种结合高阶运动建模、语言对齐语义蒸馏及窗口因果注意力机制的统一前馈框架。
result: "在动态估计精度上提升21%，渲染PSNR提升1.6 dB，语义分割mIoU提升20%，并实现了低延迟流式推理。"
conclusion: SLARM通过几何与语义的深度耦合，为动态场景的实时重建、理解与交互提供了一个高效且准确的解决方案。
---

## 摘要
我们提出了 SLARM，这是一种统一了动态场景重建、语义理解和实时流式推理的前馈模型。SLARM 通过高阶运动建模捕捉复杂的非均匀运动，且仅在可微渲染上进行训练，无需任何光流监督。此外，SLARM 从 LSeg 中蒸馏语义特征，以获得语言对齐的表示。该设计支持通过自然语言进行语义查询，且语义与几何之间的紧密耦合进一步增强了动态重建的准确性和鲁棒性。此外，SLARM 使用基于窗口的因果注意力机制处理图像序列，在不累积内存开销的情况下实现了稳定、低延迟的流式推理。在这一统一框架下，SLARM 在动态估计、渲染质量和场景解析方面均达到了最先进的水平，相比现有方法，运动准确度提升了 21%，重建 PSNR 提升了 1.6 dB，分割 mIoU 提升了 20%。

## Abstract
We propose SLARM, a feed-forward model that unifies dynamic scene reconstruction, semantic understanding, and real-time streaming inference. SLARM captures complex, non-uniform motion through higher-order motion modeling, trained solely on differentiable renderings without any flow supervision. Besides, SLARM distills semantic features from LSeg to obtain language-aligned representations. This design enables semantic querying via natural language, and the tight coupling between semantics and geometry further enhances the accuracy and robustness of dynamic reconstruction. Moreover, SLARM processes image sequences using window-based causal attention, achieving stable, low-latency streaming inference without accumulating memory cost. Within this unified framework, SLARM achieves state-of-the-art results in dynamic estimation, rendering quality, and scene parsing, improving motion accuracy by 21%, reconstruction PSNR by 1.6 dB, and segmentation mIoU by 20% over existing methods.

---

## 论文详细总结（自动生成）

### 论文总结：SLARM - 面向动态场景的流式语言对齐重建模型

#### 1. 核心问题与整体含义（研究动机和背景）
论文旨在解决当前 3D/4D 场景重建中的三大核心挑战：
*   **运动建模过于简化**：现有前馈模型（如 STORM）通常假设匀速运动，难以捕捉现实世界中复杂的非线性动态（如行人步态）。
*   **缺乏语义理解**：大多数重建模型仅关注几何与外观，缺乏高层语义，限制了其在自动驾驶和具身智能中的感知与推理能力。
*   **推理效率低下**：现有方法往往需要批量处理多帧图像或进行离线插值，无法实现低延迟、内存恒定的增量流式推理。

SLARM 作为一个统一的前馈 Transformer 框架，通过高阶运动建模、语义蒸馏和因果注意力机制，实现了动态重建、语义理解与流式推理的深度融合。

#### 2. 方法论：核心思想与关键技术
SLARM 的核心是一个基于 4D Gaussian Splatting (4DGS) 的前馈推理模型：
*   **高阶运动建模**：不直接预测位移，而是利用**多阶泰勒展开（Taylor Expansion）**来建模位置随时间的变化。模型预测速度、加速度和加加速度（Jerk）等系数，通过公式 $\Gamma(\Delta t) = \sum_{l=0}^{L-1} \frac{m_l \cdot (\Delta t)^{l+1}}{(l+1)!}$ 计算位移。这使得模型能捕捉非匀速的复杂运动。
*   **语言对齐的语义蒸馏**：从 2D 基础模型 **LSeg** 中蒸馏语义特征到 3D 高斯基元中。通过将渲染的语义图与 LSeg 特征进行 MSE 对齐，并结合带标签数据的交叉熵损失，使重建的 3D 场景支持自然语言查询（如“查找行人”）。
*   **流式推理架构**：采用**基于窗口的因果注意力机制（Window-based Causal Attention）**。模型独立处理每一帧，同时传播紧凑的隐藏状态，确保推理延迟和内存占用不随序列长度增加，支持长时程部署。
*   **自监督学习**：运动估计完全基于渲染一致性（RGB 损失和 LPIPS 损失），无需真实的光流或场景流标注。

#### 3. 实验设计
*   **数据集**：主要在 **Waymo Open Dataset (WOD)** 上进行训练和评估，并在 **NuScenes** 和 **Argoverse 2** 上验证了泛化性。
*   **Benchmark 与对比方法**：
    *   **动态重建**：对比了 LGM、GS-LRM、MapAnything 和 STORM。
    *   **场景流估计**：对比了 NSFP、NSFP++ 和 STORM。
    *   **语义分割**：对比了 EfficientViT、SegFormer、Mask2Former、OffSeg 和 LSeg 等 SOTA 2D 分割模型。
*   **评估指标**：PSNR、SSIM（图像质量）；RMSE（深度精度）；EPE3D、角度误差（运动精度）；mIoU、Acc（语义精度）。

#### 4. 资源与算力
*   **硬件**：使用了 **64 台华为昇腾 (Huawei Ascend) 910B NPU**。
*   **训练细节**：总训练时长为 **4 天**，迭代次数为 200k 次，Batch Size 为 64。
*   **推理性能**：支持实时流式推理，相比离线模式，在线模式在保持高精度的同时显著降低了内存峰值。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 1000 个 Waymo 序列上进行了大规模验证（798 训练，202 测试），并跨数据集（NuScenes, Argoverse 2）进行了泛化测试。
*   **消融实验**：针对运动建模阶数（1-4 阶）、语义损失项（Lsem, Lcls）以及推理模式（离线 vs 流式）进行了详细的消融研究。
*   **客观性**：实验不仅评估了全图指标，还专门针对“仅动态区域”进行了掩码评估，证明了其在处理运动物体上的显著优势。对比基准涵盖了从几何重建到语义分割的多个领域，评价较为全面、客观。

#### 6. 主要结论与发现
*   **运动精度大幅提升**：3 阶运动建模足以捕捉大多数现实动态，相比匀速模型，运动准确度提升了 21%。
*   **语义增强几何**：引入语义一致性作为正则项，不仅实现了语言查询，还反哺了动态重建的鲁棒性，PSNR 提升了 1.6 dB。
*   **流式推理可行性**：通过窗口注意力实现的流式推理在长序列中表现稳定，解决了 4D 重建中的内存爆炸问题。
*   **超越 2D 基准**：由于利用了 3D 几何先验，SLARM 在语义分割任务上的 mIoU 甚至超过了专门的 2D 分割模型（提升约 20%）。

#### 7. 优点（亮点）
*   **高度集成**：在一个前馈模型中同时解决了重建、流估计和语义理解三个任务。
*   **无需流监督**：通过可微渲染实现自监督运动学习，降低了对高质量 4D 标注数据的依赖。
*   **高阶建模**：泰勒展开的应用有效解决了动态场景中“重影”和“拉伸”的常见伪影问题。
*   **流式友好**：因果注意力的设计使其非常适合自动驾驶等实时应用场景。

#### 8. 不足与局限
*   **位姿依赖**：模型仍需准确的相机内参和外参（位姿），在位姿估计不准的情况下性能会下降。
*   **复杂材质挑战**：由于依赖光度一致性，对于玻璃、镜面等非朗伯体材质的重建效果有限。
*   **计算开销**：虽然推理是流式的，但模型基于大型 Transformer 架构，对边缘设备的实时部署仍有一定算力门槛。
*   **远景处理**：对于极远距离的动态物体，受限于像素分辨率，运动建模的精度可能会降低。

（完）
