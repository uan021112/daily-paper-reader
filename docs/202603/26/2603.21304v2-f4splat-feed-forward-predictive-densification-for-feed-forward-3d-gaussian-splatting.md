---
title: "F4Splat: Feed-Forward Predictive Densification for Feed-Forward 3D Gaussian Splatting"
title_zh: F4Splat：面向前馈 3D 高斯泼溅的前馈预测加密
authors: "Injae Kim, Chaehyeon Kim, Minseong Bae, Minseok Joo, Hyunwoo J. Kim"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21304v2"
tags: ["query:ff3dr"]
score: 10.0
evidence: 用于单次前馈3D重建的前馈预测加密技术
tldr: F4Splat 针对前馈 3D 高斯泼溅（3DGS）中高斯分布冗余且难以控制数量的问题，提出了一种基于预测性加密的分配策略。该方法通过预测区域加密分数，根据空间复杂度和多视图重叠度自适应分配高斯点，实现了在显著减少高斯数量的同时提升重建质量，并支持在不重训的情况下显式控制高斯预算。
selection_source: fresh_fetch
motivation: 现有的前馈 3DGS 方法采用固定的像素或体素分配机制，导致高斯点在视图间存在大量冗余且缺乏对总量的灵活控制。
method: 提出一种加密分数引导的分配策略，通过预测区域加密分数来自适应分布高斯点，并允许在推理时显式控制高斯预算。
result: 实验表明，该模型在使用更少高斯点的情况下，其新视图合成性能优于此前未校准的前馈方法。
conclusion: F4Splat 通过空间自适应分配实现了更紧凑且高质量的 3D 表示，有效平衡了重建精度与计算效率。
---

## 摘要
前馈 3D 高斯泼溅（3D Gaussian Splatting）方法能够实现单次通过重建和实时渲染。然而，它们通常采用僵化的像素到高斯或体素到高斯流水线，均匀地分配高斯分布，导致跨视图的高斯冗余。此外，它们缺乏在保持重建保真度的同时控制高斯总数的有效机制。为了解决这些局限性，我们提出了 F4Splat，它为前馈 3D 高斯泼溅执行前馈预测加密，引入了一种由加密分数引导的分配策略，根据空间复杂度和多视图重叠自适应地分布高斯。我们的模型预测每个区域的加密分数以估计所需的高斯密度，并允许在无需重新训练的情况下显式控制最终的高斯预算。这种空间自适应分配减少了简单区域的冗余，并最大限度地减少了重叠视图中的重复高斯，从而产生紧凑且高质量的 3D 表示。大量实验表明，与之前未经校准的前馈方法相比，我们的模型在实现卓越的新视角合成性能的同时，使用的高斯数量显著减少。

## Abstract
Feed-forward 3D Gaussian Splatting methods enable single-pass reconstruction and real-time rendering. However, they typically adopt rigid pixel-to-Gaussian or voxel-to-Gaussian pipelines that uniformly allocate Gaussians, leading to redundant Gaussians across views. Moreover, they lack an effective mechanism to control the total number of Gaussians while maintaining reconstruction fidelity. To address these limitations, we present F4Splat, which performs Feed-Forward predictive densification for Feed-Forward 3D Gaussian Splatting, introducing a densification-score-guided allocation strategy that adaptively distributes Gaussians according to spatial complexity and multi-view overlap. Our model predicts per-region densification scores to estimate the required Gaussian density and allows explicit control over the final Gaussian budget without retraining. This spatially adaptive allocation reduces redundancy in simple regions and minimizes duplicate Gaussians across overlapping views, producing compact yet high-quality 3D representations. Extensive experiments demonstrate that our model achieves superior novel-view synthesis performance compared to prior uncalibrated feed-forward methods, while using significantly fewer Gaussians.

---

## 论文详细总结（自动生成）

### F4Splat 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
传统的 3D 高斯泼溅（3DGS）依赖于耗时的每场景迭代优化，而现有的**前馈 3DGS** 方法虽然实现了单次通过重建，但存在两个主要缺陷：
*   **分配效率低下**：大多采用像素到高斯或体素到高斯的固定映射，导致在视觉简单的区域（如平滑墙面）产生冗余，而在复杂细节区域（如花朵、纹理）分配不足。
*   **缺乏预算控制**：无法在不重新训练模型的情况下灵活调整高斯点的总数，难以适应不同硬件设备的计算限制。
**F4Splat** 的研究动机在于引入一种**空间自适应的分配机制**，通过预测“加密分数”来识别哪些区域需要更多高斯点，从而在有限的预算内实现更高质量的重建。

#### 2. 方法论：核心思想与关键技术
F4Splat 的核心思想是将 3DGS 的“自适应密度控制（ADC）”过程从迭代优化阶段提前到前馈预测阶段。
*   **几何骨干网络（Geometry Backbone）**：基于 VGGT 结构，利用预训练的 DINOv2 提取特征，通过自注意力机制预测相机参数和图像特征。
*   **多尺度预测（Multi-Scale Prediction）**：模型同时预测多层级（通常为 3 层）的高斯参数图和**加密分数图（Densification Score Maps）**。分辨率随层级倍增。
*   **空间自适应分配策略**：
    *   引入**加密分数 $\hat{d}_g$**：该分数代表特定区域对高斯密度的需求程度。
    *   **阈值控制机制**：通过设置阈值 $\tau$，模型从粗到细遍历层级。若某区域分数超过 $\tau$，则在该处分配更细粒度的高斯点。
    *   **预算匹配算法**：通过二分查找快速确定满足用户指定高斯总数 $\bar{N}_G$ 的最优阈值 $\tau$，实现无需重训的显式预算控制。
*   **训练策略**：
    *   **梯度监督**：借鉴 AbsGS，利用渲染损失对高斯位置的梯度范数作为真值，监督加密分数的预测。
    *   **新视角对齐**：在训练时将真值相机位姿对齐到预测的坐标系中，以实现稳定的新视角合成监督。

#### 3. 实验设计
*   **数据集**：主要在 **RealEstate10K (RE10K)** 和 **ACID** 大规模数据集上进行训练和评估。
*   **Benchmark 与场景**：
    *   **多视图设置**：8、16、24 个输入视图。
    *   **双视图设置**：极稀疏的 2 视图输入。
    *   **跨数据集泛化**：在 RE10K 上训练，直接在 ACID 上测试。
*   **对比方法**：
    *   **未校准（Uncalibrated）方法**：VicaSplat, AnySplat。
    *   **无位姿（Pose-Free）方法**：NoPoSplat, SPFSplat。
    *   **需位姿（Pose-Required）方法**：pixelSplat, MVSplat。

#### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA H200 GPU**。
*   **训练时长**：
    *   多视图模型训练约 **15 小时**（15,000 次迭代）。
    *   双视图模型训练约 **18,750 次迭代**。
*   **对比算力**：Retrain AnySplat 基准模型在相同环境下约需 27 小时。

#### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从 2 视图到 24 视图的多种配置，并在两个主流数据集上进行了详尽测试。
*   **消融实验**：针对分配策略（随机、基于频率、基于预测分数）、层级监督的必要性、以及场景缩放正则化项进行了 5 组对比实验。
*   **客观性**：通过 LPIPS、SSIM、PSNR 三项标准指标进行量化，并同时报告了高斯点数量（#GS），以体现效率与质量的权衡。实验设计考虑了未校准和已知内参等多种实际应用场景，对比具有公平性。

#### 6. 主要结论与发现
*   **高效率**：F4Splat 在仅使用基准方法 **10%-28%** 的高斯点数量时，仍能保持甚至超过其重建质量。
*   **自适应性**：预测的加密分数能有效识别物体边缘和复杂纹理，同时自动减少多视图重叠区域的冗余分配。
*   **位姿精度**：尽管重点在于高斯分配，但其几何骨干在未见数据集上的位姿估计精度也优于 DUSt3R 和 VGGT 等基准。

#### 7. 优点（亮点）
*   **预算可控性**：首个实现无需重新训练即可精确控制前馈 3DGS 高斯数量的方法。
*   **冗余感知**：通过学习到的分数图，模型能够意识到跨视图的重叠，避免在同一空间位置重复堆叠高斯点。
*   **即插即用**：多尺度预测架构简单高效，推理时间仅增加约 10%，显存占用增加不到 2%。

#### 8. 不足与局限
*   **推理开销**：虽然高斯点减少了，但多尺度解码器和阈值处理逻辑相比单尺度模型增加了少量的计算延迟。
*   **依赖骨干网络**：性能在很大程度上受限于底层几何骨干（如 DINOv2）提取特征的能力，对于极度缺乏纹理或光照剧烈变化的场景可能仍存在挑战。
*   **应用限制**：目前主要针对室内外自然场景（RE10K/ACID），在超大规模城市级场景或极微观场景下的表现尚未验证。

（完）
