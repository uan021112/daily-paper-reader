---
title: "SLARM: Streaming and Language-Aligned Reconstruction Model for Dynamic Scenes"
title_zh: SLARM：面向动态场景的流式与语言对齐重建模型
authors: "Zhicheng Qiu, Jiarui Meng, Tong-an Luo, Yican Huang, Xuan Feng, Xuanfu Li, ZHan Xu"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.22893v3"
tags: ["query:ff3dr"]
score: 10.0
evidence: 用于动态场景重建的前馈模型
tldr: 本研究提出SLARM，一种用于动态场景重建的前馈模型，集成了语义理解与实时流式推理。该模型通过高阶运动建模捕捉复杂非均匀运动，并利用LSeg蒸馏实现语言对齐的语义表示，支持自然语言查询。SLARM采用窗口因果注意力机制，实现了低延迟、无内存累积的流式处理。实验表明，该统一框架在动态估计、渲染质量和场景解析方面均达到SOTA水平，显著提升了运动精度和重建效果。
selection_source: fresh_fetch
motivation: 旨在解决动态场景重建中复杂运动捕捉难、缺乏语义理解以及无法进行实时流式推理的问题。
method: 结合高阶运动建模、LSeg语义特征蒸馏以及基于窗口的因果注意力机制，构建了一个统一的前馈推理框架。
result: "在动态估计精度上提升了21%，重建PSNR提升了1.6 dB，场景分割mIoU提升了20%，均达到领域领先水平。"
conclusion: SLARM通过几何与语义的紧密耦合，实现了高效、稳健且支持自然语言交互的动态场景实时重建。
---

## 摘要
我们提出了 SLARM，这是一种统一了动态场景重建、语义理解和实时流式推理的前馈模型。SLARM 通过高阶运动建模捕捉复杂的非均匀运动，且仅在可微渲染上进行训练，无需任何光流监督。此外，SLARM 从 LSeg 中蒸馏语义特征，以获得语言对齐的表示。这一设计实现了通过自然语言进行语义查询，且语义与几何之间的紧密耦合进一步增强了动态重建的准确性和鲁棒性。此外，SLARM 使用基于窗口的因果注意力机制处理图像序列，在不累积内存开销的情况下实现了稳定、低延迟的流式推理。在这一统一框架内，SLARM 在动态估计、渲染质量和场景解析方面均达到了最先进的结果，相比现有方法，运动准确度提升了 21%，重建 PSNR 提升了 1.6 dB，分割 mIoU 提升了 20%。

## Abstract
We propose SLARM, a feed-forward model that unifies dynamic scene reconstruction, semantic understanding, and real-time streaming inference. SLARM captures complex, non-uniform motion through higher-order motion modeling, trained solely on differentiable renderings without any flow supervision. Besides, SLARM distills semantic features from LSeg to obtain language-aligned representations. This design enables semantic querying via natural language, and the tight coupling between semantics and geometry further enhances the accuracy and robustness of dynamic reconstruction. Moreover, SLARM processes image sequences using window-based causal attention, achieving stable, low-latency streaming inference without accumulating memory cost. Within this unified framework, SLARM achieves state-of-the-art results in dynamic estimation, rendering quality, and scene parsing, improving motion accuracy by 21%, reconstruction PSNR by 1.6 dB, and segmentation mIoU by 20% over existing methods.

---

## 论文详细总结（自动生成）

### 论文总结：SLARM —— 面向动态场景的流式与语言对齐重建模型

#### 1. 核心问题与研究动机
*   **核心问题**：现有的动态 3D 场景重建方法（如基于 NeRF 或 3DGS 的优化方法）通常面临优化时间长、泛化能力差、运动建模过于简化（如假设匀速运动）以及缺乏高层语义理解等问题。此外，大多数模型无法支持低延迟、内存恒定的流式推理，难以应用于自动驾驶和具身智能等实时场景。
*   **研究动机**：旨在开发一个统一的前馈（Feed-forward）模型，能够同时实现动态几何重建、精确的非均匀运动估计、语言对齐的语义理解，并支持长序列的实时流式推理。

#### 2. 方法论
SLARM 采用基于 Transformer 的前馈架构，核心思想是将动态场景建模为随时间演化的 4D 高斯点云（4DGS）。
*   **高阶运动建模**：不同于以往的匀速假设，SLARM 使用三阶泰勒级数（Taylor Expansion）建模位移，显式预测速度、加速度和加加速度（Jerk）。通过公式 $\Gamma(\Delta t) = \sum_{l=0}^{L-1} \frac{m_l \cdot (\Delta t)^{l+1}}{(l+1)!}$ 计算位移，从而捕捉复杂的非线性运动。
*   **语言对齐的语义蒸馏**：从 2D 基础模型 LSeg 中蒸馏语义特征到 3D 高斯原语中。通过计算渲染特征与 CLIP 文本嵌入的相似度，使模型具备自然语言查询能力（如查询“行人”、“车辆”）。
*   **流式推理架构**：引入基于窗口的因果注意力机制（Window-based Causal Attention），模型仅依赖当前和历史帧，通过传播紧凑的隐藏状态实现增量更新，确保了恒定的推理延迟和内存开销。
*   **自监督学习**：模型仅通过可微渲染（Differentiable Rendering）进行训练，利用时间一致性（光度损失和感知损失 LPIPS）来隐式学习场景流，无需真实的光流标注。

#### 3. 实验设计
*   **数据集**：主要在 **Waymo Open Dataset (WOD)** 上进行评估，并扩展到了 **NuScenes** 和 **Argoverse 2** 等大型自动驾驶数据集。
*   **Benchmark 与对比方法**：
    *   **几何与渲染**：对比了 LGM、GS-LRM、MapAnything 和 STORM。
    *   **场景流估计**：对比了 NSFP、NSFP++ 和 STORM。
    *   **语义分割**：对比了 SegFormer、Mask2Former、LSeg 等 SOTA 2D 分割模型。
*   **评估指标**：PSNR、SSIM（图像质量）；RMSE（深度精度）；EPE3D、角度误差（运动精度）；mIoU、Acc（语义分割）。

#### 4. 资源与算力
*   **算力设备**：使用了 **64 台华为昇腾 (Ascend) 910B NPU**。
*   **训练配置**：Batch size 为 64，采用 AdamW 优化器。
*   **训练时长**：总计训练约 **4 天**，迭代次数为 **200k 次**。

#### 5. 实验数量与充分性
*   **实验规模**：在 WOD 数据集的 1000 个序列（约 20 万帧图像）上进行了大规模验证。
*   **消融实验**：针对运动建模阶数（1阶 vs 3阶）、语义损失项的影响、流式窗口注意力 vs 全局注意力、以及在线 vs 离线模式的性能/功耗平衡做了详细对比。
*   **充分性评价**：实验设计涵盖了从底层几何到高层语义的多个维度，并在多个主流自动驾驶数据集上验证了泛化性，实验结果客观且对比公平。

#### 6. 主要结论与发现
*   **性能提升**：SLARM 在运动准确度上提升了 **21%**，重建 PSNR 提升了 **1.6 dB**，语义分割 mIoU 提升了 **20%**。
*   **运动建模**：三阶运动模型足以捕捉现实世界中的大部分复杂动态（如行人的步态），更高阶的提升边际效应递减。
*   **语义辅助**：语义一致性可以作为时间正则项，显著提升动态物体的运动估计精度。
*   **流式优势**：SLARM-W（窗口模式）在保持高精度的同时，实现了线性的推理时间和稳定的内存占用。

#### 7. 优点
*   **统一性**：在一个前馈框架内解决了重建、运动和语义三个任务，实现了任务间的相互增强。
*   **高阶动力学**：突破了前人模型的匀速限制，对非均匀运动的重建更加细腻（如行人肢体动作）。
*   **零样本语义查询**：通过语言对齐，模型可以直接与大语言模型（LLM）集成，用于下游的感知和推理任务。
*   **工程实用性**：流式推理设计非常符合自动驾驶和机器人对实时性的需求。

#### 8. 不足与局限
*   **位姿依赖**：模型目前仍依赖于精确的相机位姿（Camera Poses），若位姿估计有误，重建质量会大幅下降。
*   **材质限制**：由于依赖光度一致性假设，模型在处理玻璃、镜面等复杂反射材质时表现欠佳。
*   **计算开销**：虽然是前馈模型，但 4D 高斯的大规模预测和渲染对移动端设备的实时部署仍具有挑战性。

（完）
