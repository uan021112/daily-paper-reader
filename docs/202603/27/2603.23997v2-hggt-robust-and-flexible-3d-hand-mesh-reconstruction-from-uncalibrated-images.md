---
title: "HGGT: Robust and Flexible 3D Hand Mesh Reconstruction from Uncalibrated Images"
title_zh: HGGT：基于未校准图像的鲁棒且灵活的三维手部网格重建
authors: "Yumeng Liu, Xiao-Xiao Long, Marc Habermann, Xuanze Yang, Cheng Lin, Yuan Liu, Yuexin Ma, Wenping Wang, Ligang Liu"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.23997v2"
tags: ["query:3dgen"]
score: 9.0
evidence: 从非标定图像进行3D手部网格重建
tldr: 针对3D手部重建中单视图深度歧义和多视图需校准的局限性，本文提出HGGT框架。该方法借鉴3D大模型思路，将任意视图的手部重建转化为视觉几何接地任务。通过前馈架构，HGGT首次实现了从非校准的多视图图像中同时推断3D手部网格和相机位姿。实验证明，该方法在精度和泛化性上均优于现有基准，适用于野外非校准场景。
selection_source: fresh_fetch
motivation: 旨在解决单视图重建存在的深度歧义问题以及传统多视图系统对固定、校准相机设置的依赖。
method: 提出一种前馈架构，通过视觉几何接地任务同时从非校准视图中推断3D手部网格和相机位姿。
result: 实验结果显示该方法在多个基准测试中优于现有技术，并能有效处理未经校准的野外图像数据。
conclusion: HGGT成功实现了兼具高精度与部署灵活性的3D手部重建，为机器人和VR/AR等领域提供了有力支持。
---

## 摘要
从图像中恢复高保真三维手部几何形状是计算机视觉中的一项关键任务，在机器人、动画以及虚拟现实/增强现实（VR/AR）等领域具有重要价值。至关重要的是，可扩展的应用既要求准确性，也要求部署的灵活性，这需要能够利用来自互联网的海量非结构化图像数据，或者能够在无需复杂校准的情况下部署在消费级 RGB 相机上。然而，目前的方法面临着两难境地。虽然单视图方法易于部署，但它们受到深度歧义和遮挡的影响。相反，多视图系统虽然解决了这些不确定性，但通常需要固定的、经过校准的设置，从而限制了其在现实世界中的实用性。为了弥补这一差距，我们从直接从视觉数据中学习显式几何形状的三维基础模型中汲取灵感。通过将任意视图下的手部重建重新表述为一项基于视觉几何的任务，我们提出了一种前馈架构，这在文献中尚属首次，能够从未校准的视图中联合推断三维手部网格和相机位姿。广泛的评估表明，我们的方法优于最先进的基准测试，并对未校准的野外场景表现出强大的泛化能力。项目页面链接：https://lym29.github.io/HGGT/。

## Abstract
Recovering high-fidelity 3D hand geometry from images is a critical task in computer vision, holding significant value for domains such as robotics, animation and VR/AR. Crucially, scalable applications demand both accuracy and deployment flexibility, requiring the ability to leverage massive amounts of unstructured image data from the internet or enable deployment on consumer-grade RGB cameras without complex calibration. However, current methods face a dilemma. While single-view approaches are easy to deploy, they suffer from depth ambiguity and occlusion. Conversely, multi-view systems resolve these uncertainties but typically demand fixed, calibrated setups, limiting their real-world utility. To bridge this gap, we draw inspiration from 3D foundation models that learn explicit geometry directly from visual data. By reformulating hand reconstruction from arbitrary views as a visual-geometry grounded task, we propose a feed-forward architecture that, for the first time in literature, jointly infers 3D hand meshes and camera poses from uncalibrated views. Extensive evaluations show that our approach outperforms state-of-the-art benchmarks and demonstrates strong generalization to uncalibrated, in-the-wild scenarios. Here is the link of our project page: https://lym29.github.io/HGGT/.

---

## 论文详细总结（自动生成）

以下是对论文《HGGT: Robust and Flexible 3D Hand Mesh Reconstruction from Uncalibrated Images》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在无需相机校准（Calibration-free）的情况下，从任意数量的多视图图像中稳健地重建高精度三维手部网格（3D Hand Mesh）。
*   **研究背景**：
    *   **单视图方法**（如 HaMeR）：部署简单，但受限于深度歧义和遮挡问题。
    *   **传统多视图方法**（如 POEM）：虽然能解决歧义，但极度依赖精确的相机校准，限制了在野外或消费级设备上的应用。
*   **动机**：借鉴 3D 基础模型（如 VGGT）的思路，将手部重建视为一个“视觉几何接地”（Visual-geometry Grounded）任务，实现相机位姿与手部几何的联合推断。

### 2. 核心方法论
*   **核心思想**：提出一种前馈（Feed-forward）Transformer 架构，通过可学习的“手部 Token”和“相机 Token”在多视图特征中提取几何信息。
*   **关键技术细节**：
    *   **VGGT 聚合器**：利用预训练的 VGGT 骨干网络提取多视图图像特征并初始化相机 Token。
    *   **统一交叉注意力细化模块**：引入一组可学习的随机手部 Token，与相机 Token 一起通过 4 层交叉注意力块。手部 Token 作为 Query，从多视图图像特征（Key/Value）中迭代细化几何表示。
    *   **双头预测架构**：
        *   **手部头（Hand Head）**：回归 MANO 模型的参数（姿态 $\theta$、形状 $\beta$、平移 $t$）。
        *   **相机头（Camera Head）**：预测每张视图相对于第一帧的旋转、平移和视场角（FoV）。
    *   **损失函数**：结合了手部 3D 损失、相机位姿损失以及 2D 投影一致性损失（用于在无 3D 标注时提供弱监督）。
    *   **混合数据训练策略**：通过梯度累积处理单视图（真实）、多视图（真实）和多视图（合成）的异构数据。

### 3. 实验设计
*   **数据集**：
    *   **单视图真实数据**：整合了 FreiHAND, HO3D, InterHand2.6M, COCO 等约 270 万张图像。
    *   **多视图真实数据**：DexYCB, HO3D, OakInk, ARCTIC, InterHand2.6M。
    *   **多视图合成数据**：基于 GraspXL 自建的大规模合成数据集（约 8.5 万组序列），提供极高的视角多样性。
*   **Benchmark 与对比方法**：
    *   主要对比对象是 **POEM**（当前多视图 SOTA）。
    *   对比设置：在“未校准”条件下，将 HGGT 与“使用预测相机位姿的 POEM”以及“使用真值相机位姿的 POEM（作为上限）”进行对比。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A100 GPU** 组成的集群。
*   **训练时长**：总计训练约 **5 天**（80,000 次迭代）。
*   **参数量**：模型包含约 **14 亿（1.4B）参数**。
*   **优化器**：AdamW，采用了混合精度训练（BF16）和梯度裁剪。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个主流多视图基准数据集上进行了详尽的定量评估。
*   **消融实验**：
    *   验证了骨干网络（Backbone）不同层级的冻结策略。
    *   验证了合成数据集对提升泛化性的贡献。
    *   探讨了交叉注意力块的数量、中间监督策略以及投影损失的位置。
*   **充分性评价**：实验设计非常充分且客观。通过在野外视频（双手机随机拍摄）上的推理演示，证明了模型在非实验室环境下的鲁棒性。

### 6. 主要结论与发现
*   **性能领先**：在未校准设置下，HGGT 显著优于现有方法；在某些数据集（如 HO3D）上，其精度甚至超过了使用真值相机的 POEM。
*   **联合学习的优势**：证明了联合推断相机位姿和手部几何比分步处理更有效，因为手部几何信息能辅助相机位姿的对齐。
*   **合成数据的价值**：大规模、高视角多样性的合成数据是突破传统多视图数据集“固定相机阵列”限制的关键。

### 7. 优点
*   **部署灵活**：首个无需校准的多视图前馈手部重建框架，极大地降低了应用门槛。
*   **鲁棒性强**：在严重运动模糊、重度遮挡（如手部仅在少数视图可见）的情况下仍能保持物理合理的重建效果。
*   **端到端**：无需迭代优化，推理速度快（每帧约 0.23 秒）。

### 8. 不足与局限
*   **依赖外部检测器**：目前仍需预先使用 2D 检测器（如 ViTPose）进行手部定位和裁剪，增加了系统复杂度和总耗时。
*   **绝对尺度问题**：由于未校准图像存在尺度歧义，恢复手部相对于相机的绝对度量深度（Metric Depth）仍是一个挑战。
*   **归一化空间限制**：预测的相机参数是在归一化空间定义的，与真实物理世界的绝对尺度对齐尚不完美。

（完）
