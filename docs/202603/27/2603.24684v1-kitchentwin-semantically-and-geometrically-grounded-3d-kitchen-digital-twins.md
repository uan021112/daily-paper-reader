---
title: "KitchenTwin: Semantically and Geometrically Grounded 3D Kitchen Digital Twins"
title_zh: KitchenTwin：语义与几何对齐的 3D 厨房数字孪生
authors: "Quanyun Wu, Kyle Gao, Daniel Long, David A. Clausi, Jonathan Li, Yuhao Chen"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24684v1"
tags: ["query:ff3dr"]
score: 9.0
evidence: 基于Transformer的单目视频前馈重建
tldr: KitchenTwin 针对具身智能训练中数字孪生环境的尺度模糊和坐标不一致问题，提出了一种语义与几何对齐的 3D 厨房数字孪生构建框架。该方法利用视觉语言模型（VLM）引导的几何锚点机制恢复真实物理尺度，并通过几何感知配准流程融合全局点云与局部物体网格。实验证明该方法在几何一致性和物体对齐方面表现优异，并提供了开源的度量级数字孪生数据集。
selection_source: fresh_fetch
motivation: 现有的基于 Transformer 的单目视频重建方法存在固有的尺度模糊和坐标不一致问题，难以构建精确的具身智能数字孪生环境。
method: 提出一种 VLM 引导的几何锚点机制来恢复物理尺度，并结合重力对齐、曼哈顿世界约束和无碰撞细化进行几何感知配准。
result: 在真实厨房环境中的实验表明，该方法显著提升了跨网络物体对齐精度、几何一致性以及多原语拟合等下游任务的性能。
conclusion: KitchenTwin 为构建具有精确度量尺度和语义信息的 3D 数字孪生提供了有效方案，并贡献了高质量的开源数据集。
---

## 摘要
具身智能（Embodied AI）的训练与评估需要具有精确度量几何和语义对齐的以物体为中心的数字孪生环境。近期基于 Transformer 的前馈重建方法可以从稀疏单目视频中高效预测全局点云，但这些几何结构存在固有的尺度二义性和坐标约定不一致的问题。这种不匹配阻碍了这些无量纲点云预测与局部重建的物体网格（meshes）之间的可靠融合。我们提出了一种新型的尺度感知 3D 融合框架，通过将视觉对齐的物体网格与 Transformer 预测的全局点云进行配准，来构建度量一致的数字孪生。我们的方法引入了一种由视觉语言模型（VLM）引导的几何锚点机制，通过恢复准确的真实世界度量尺度来解决这一根本性的坐标不匹配问题。为了融合这些网络，我们提出了一种几何感知配准流水线，通过重力对齐的垂直估计、曼哈顿世界（Manhattan-world）结构约束以及无碰撞局部细化，显式地增强物理合理性。在真实室内厨房环境中的实验表明，该方法在下游任务（包括多原语拟合和度量测量）中提升了跨网络物体对齐和几何一致性。此外，我们还引入了一个开源的室内数字孪生数据集，其中包含具有度量尺度的场景以及语义对齐且配准的以物体为中心的网格标注。

## Abstract
Embodied AI training and evaluation require object-centric digital twin environments with accurate metric geometry and semantic grounding. Recent transformer-based feedforward reconstruction methods can efficiently predict global point clouds from sparse monocular videos, yet these geometries suffer from inherent scale ambiguity and inconsistent coordinate conventions. This mismatch prevents the reliable fusion of these dimensionless point cloud predictions with locally reconstructed object meshes. We propose a novel scale-aware 3D fusion framework that registers visually grounded object meshes with transformer-predicted global point clouds to construct metrically consistent digital twins. Our method introduces a Vision-Language Model (VLM)-guided geometric anchor mechanism that resolves this fundamental coordinate mismatch by recovering an accurate real-world metric scale. To fuse these networks, we propose a geometry-aware registration pipeline that explicitly enforces physical plausibility through gravity-aligned vertical estimation, Manhattan-world structural constraints, and collision-free local refinement. Experiments on real indoor kitchen environments demonstrate improved cross-network object alignment and geometric consistency for downstream tasks, including multi-primitive fitting and metric measurement. We additionally introduce an open-source indoor digital twin dataset with metrically scaled scenes and semantically grounded and registered object-centric mesh annotations.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **KitchenTwin** 的框架，旨在通过单目视频构建具有精确物理尺度和语义信息的 3D 厨房数字孪生环境。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：具身智能（Embodied AI）需要具有精确物理尺度（Metric Scale）和独立物体实体的 3D 环境。然而，现有的前馈 Transformer 重建模型生成的点云通常是“无量纲”的（缺乏绝对尺度），且与局部生成的物体网格（Mesh）在坐标系和比例上不统一，导致无法直接融合。
*   **研究背景**：传统的 3D 重建方法（如 SLAM）计算成本高且存在尺度漂移；而现代生成模型虽然能产生高质量物体网格，但这些网格处于孤立的局部坐标系中，缺乏全局空间上下文。

### 2. 方法论
该框架将数字孪生构建分为三个协同流（Streams）：
*   **Stream A：全局场景重建与尺度恢复**
    *   使用 **Pi-Long**（基于 Transformer 的模型）从单目视频生成无量纲全局点云。
    *   **VLM 引导的几何锚点机制**：利用视觉语言模型（如 Gemini）结合网络搜索，识别场景中的特定锚点物体（如标准尺寸的电器）并获取其真实物理尺寸。通过对比虚拟尺寸与真实尺寸，计算缩放因子，将场景转换到度量空间。
*   **Stream B：物体识别与网格生成**
    *   利用 **Grounded-SAM-2** 进行物体跟踪和掩码提取。
    *   通过启发式评分函数选择最佳视角（避免遮挡和裁剪），并使用 **SAM3D** 将 2D 掩码提升为独立的 3D 物体网格。
*   **Stream C：几何感知配准（融合）**
    *   **粗对齐**：通过 RANSAC 拟合地面确定重力方向，并评估多个偏航角（Yaw）假设以对齐物体。
    *   **精细配准（TrICP）**：使用修剪迭代最近点算法（Trimmed ICP）处理局部网格与全局点云的配准，解决部分重叠问题。
    *   **结构约束**：引入曼哈顿世界（Manhattan-world）假设进行轴向对齐，并执行碰撞检测与处理，消除物体间的物理穿透。

### 3. 实验设计
*   **数据集**：引入了 **KitchenTwin 数据集**，这是一个专门针对北美厨房环境开发的开源数据集，包含 RGB 视频、2D 掩码、3D 点云以及经过物理尺（Ruler）验证的真实度量标注。
*   **基准方法（Benchmark）**：对比了 **SAM3D Baseline**（即直接聚合生成的物体网格，不进行尺度恢复和几何对齐）。
*   **评价指标**：提出了 **NVSS-IoU**（新视角语义合成交并比），通过将重建的 3D 场景重新投影到 2D 相机平面，并与真实掩码计算 IoU，以此严格衡量 6-DoF 位姿、尺度和形状的准确性。

### 4. 资源与算力
*   **算力说明**：论文中**未明确提及**具体的 GPU 型号、数量或训练/推理的具体时长。考虑到其使用了 Pi-Long、SAM-2、SAM3D 和 Gemini 等大型模型，该框架在推理阶段对显存和计算能力有一定要求，但其核心贡献在于前馈（Feedforward）流程，通常比迭代优化类方法更快。

### 5. 实验数量与充分性
*   **实验规模**：在真实的厨房场景中对 12 类主要物体（如冰箱、烤箱、瓶子、橱柜等）进行了详细测试。
*   **消融实验**：进行了三组关键的消融实验，分别验证了“物理锚点机制”、“TrICP 配准”和“几何约束（碰撞处理等）”的必要性。
*   **充分性评价**：实验设计较为客观，通过 NVSS-IoU 这一严苛指标量化了尺度不一致带来的影响。虽然场景数量相对有限（主要集中在厨房），但涵盖了从微小物体到大型家具的不同尺度，验证了框架的鲁棒性。

### 6. 主要结论与发现
*   **尺度恢复是关键**：没有物理锚点机制，mIoU 会从 0.5715 骤降至 0.0436，证明了 VLM 引导的尺度恢复对数字孪生的实用性至关重要。
*   **几何约束提升物理合理性**：通过曼哈顿世界约束和碰撞处理，消除了物体“嵌入”墙体或相互穿透的现象，使生成的环境更符合物理规律。
*   **跨网络融合可行性**：证明了可以将不同架构（全局重建 Transformer 与局部生成模型）的输出在统一的度量空间内有效融合。

### 7. 优点
*   **创新性地引入 VLM 解决尺度问题**：利用 VLM 的常识和搜索能力获取物体真实尺寸，为单目视频重建提供了一种低成本的绝对尺度恢复方案。
*   **端到端的物体中心化构建**：不仅重建场景，还实现了物体的结构独立，使其可直接用于具身智能的抓取、交互等任务。
*   **高质量数据集贡献**：提供了带有精确物理度量标注的开源数据集，填补了现有数据集在度量准确性上的部分空白。

### 8. 不足与局限
*   **场景依赖性**：曼哈顿世界假设（Manhattan-world assumption）虽然适用于大多数厨房，但对于非正交布局或不规则形状的室内环境可能失效。
*   **VLM 准确性风险**：尺度恢复高度依赖于 VLM 对锚点物体尺寸估计的准确性，若 VLM 识别错误或搜索到错误的规格，会导致整个场景尺度偏差。
*   **实验覆盖面**：目前主要集中在厨房场景，对于更复杂的动态环境或室外环境的适用性尚未验证。

（完）
