---
title: "GaussFusion: Improving 3D Reconstruction in the Wild with A Geometry-Informed Video Generator"
title_zh: GaussFusion：利用几何感知视频生成器改进野外环境下的三维重建
authors: "Liyuan Zhu, Manjunath Narayana, Michal Stary, Will Hutchcroft, Gordon Wetzstein, Iro Armeni"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25053v1"
tags: ["query:ff3dr"]
score: 9.0
evidence: 改进优化和前馈方法的3D高斯泼溅重建
tldr: GaussFusion 针对 3D 高斯泼溅（3DGS）在野外场景中常见的伪影、闪烁和模糊问题，提出了一种结合几何信息的视频生成方法。该方法通过渲染包含深度、法线、不透明度和协方差的几何缓冲视频，并利用视频到视频生成器进行细化，从而产生时空一致且无伪影的帧。其主要贡献包括引入几何感知生成器、伪影合成流水线以及实现 21 FPS 实时运行的高效变体，显著提升了重建质量。
selection_source: fresh_fetch
motivation: 旨在解决 3DGS 在相机位姿误差、覆盖不全或噪声初始化下产生的漂浮物、闪烁和模糊等视觉伪影问题。
method: 利用包含深度、法线等几何信息的视频到视频生成器对 3DGS 渲染结果进行细化，并引入伪影合成流水线增强模型的鲁棒性。
result: 在多个新视角合成基准测试中达到 SOTA 性能，且其实时变体支持 21 FPS 的交互式渲染。
conclusion: GaussFusion 通过将几何先验与视频生成相结合，有效提升了 3DGS 在复杂野外环境下的重建质量和实用性。
---

## 摘要
我们提出了 GaussFusion，这是一种通过几何感知视频生成来改进野外环境下三维高斯泼溅（3DGS）重建的新方法。GaussFusion 缓解了常见的 3DGS 伪影，包括由相机位姿误差、覆盖不完整和噪声几何初始化引起的漂浮物、闪烁和模糊。与以往局限于单一重建流水线的基于 RGB 的方法不同，我们的方法引入了一个几何感知的视频到视频生成器，能够优化基于优化和前馈方法的 3DGS 渲染。给定现有的重建结果，我们渲染一个编码了深度、法线、不透明度和协方差的高斯基元视频缓冲区，生成器对其进行细化，以产生时间上连贯且无伪影的帧。我们还引入了一个伪影合成流水线，用于模拟多种退化模式，从而确保鲁棒性和泛化能力。GaussFusion 在新视角合成基准测试中达到了最先进的性能，其高效变体在保持相似性能的同时能以 21 FPS 的速度实时运行，从而支持交互式三维应用。

## Abstract
We present GaussFusion, a novel approach for improving 3D Gaussian splatting (3DGS) reconstructions in the wild through geometry-informed video generation. GaussFusion mitigates common 3DGS artifacts, including floaters, flickering, and blur caused by camera pose errors, incomplete coverage, and noisy geometry initialization. Unlike prior RGB-based approaches limited to a single reconstruction pipeline, our method introduces a geometry-informed video-to-video generator that refines 3DGS renderings across both optimization-based and feed-forward methods. Given an existing reconstruction, we render a Gaussian primitive video buffer encoding depth, normals, opacity, and covariance, which the generator refines to produce temporally coherent, artifact-free frames. We further introduce an artifact synthesis pipeline that simulates diverse degradation patterns, ensuring robustness and generalization. GaussFusion achieves state-of-the-art performance on novel-view synthesis benchmarks, and an efficient variant runs in real time at 21 FPS while maintaining similar performance, enabling interactive 3D applications.

---

## 论文详细总结（自动生成）

### GaussFusion：利用几何感知视频生成器改进野外环境下的三维重建

#### 1. 核心问题与整体含义
该论文旨在解决 **3D 高斯泼溅（3DGS）** 在处理现实世界（野外）场景时面临的视觉伪影问题。尽管 3DGS 在重建速度和质量上表现优异，但在相机位姿不准、视角覆盖不足或初始化噪声较大的情况下，容易产生“漂浮物”（floaters）、视角闪烁、模糊以及几何畸变。
**核心动机**：现有的基于生成先验的修复方法大多仅依赖 RGB 信息，难以处理严重的几何错误。GaussFusion 提出通过引入丰富的几何基元信息（GP-Buffer）来指导视频生成器，从而实现跨重建范式（优化型和前馈型）的高质量、时空一致的 3D 重建修复。

#### 2. 方法论
GaussFusion 的核心是一个**几何感知的视频到视频（V2V）生成框架**：
*   **高斯基元缓冲区（GP-Buffer）**：除了颜色（RGB）外，还渲染并提取了深度（Depth）、法线（Normals）、不透明度（Opacity）和反向投影协方差（Inverse Covariance，用于衡量几何不确定性）。这些多模态信息为模型提供了识别和修复伪影的明确几何线索。
*   **几何适配器（Geometry Adapter, GA）**：在预训练的视频流匹配（Flow-matching）Transformer（基于 Wan-2.1）中插入 GA 模块。GA 作为一个并行分支，通过自注意力机制提取 GP-Buffer 特征，并通过交叉注意力将其注入主生成网络，实现几何引导的特征调制。
*   **伪影合成流水线**：为了提高泛化性，作者构建了一个包含 7.5 万个视频对的数据集，模拟了稀疏视角、多种初始化策略（SfM、随机点云、深度图）以及前馈模型特有的退化模式。
*   **三维重建更新**：利用细化后的视频帧作为伪标签，重新优化底层的 3D 高斯参数，从而提升整体 3D 表示的连贯性。
*   **蒸馏加速**：采用分布匹配蒸馏（DMD）技术，将多步生成压缩为 4 步，使推理速度提升至 **16-21 FPS**。

#### 3. 实验设计
*   **数据集**：主要在 **DL3DV**（大规模室内外场景）和 **RE10K**（真实房地产视频）上进行评估。
*   **Benchmark**：新视角合成（NVS）质量评估和 3D 重建一致性评估。
*   **对比方法**：
    *   **优化型基准**：Splatfacto (gsplat)。
    *   **修复型基准**：GenFusion, Difix3D+, ExploreGS。
    *   **前馈型基准**：MVSplat, DepthSplat, MVSplat360。
*   **评价指标**：PSNR, SSIM, LPIPS, FID。

#### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA H200 GPU**。
*   **训练细节**：训练时长为 100K 步，有效 Batch Size 为 32（通过梯度累积实现）。
*   **技术栈**：使用 PyTorch 2.7.1, CUDA 12.6, BF16 精度，并启用了 FlashAttention-2 和 ZeRO-2 优化以提升显存效率。

#### 5. 实验数量与充分性
*   **实验规模**：基于 7.5 万个配对视频进行大规模训练，涵盖了极其多样的退化场景。
*   **消融实验**：作者进行了详尽的消融研究，验证了：
    1.  **输入模态**：证明了深度、法线、协方差等几何通道对提升 PSNR 和降低 FID 的显著贡献。
    2.  **架构设计**：对比了直接微调 DiT 与使用 GA 模块的效果，证明 GA 收敛更快且性能更优。
    3.  **数据策略**：验证了混合伪影模拟对跨范式泛化的重要性。
*   **客观性**：实验涵盖了优化型和前馈型两种主流 3DGS 路径，对比基准均为近年顶会 SOTA 方法，实验设计较为公平、客观。

#### 6. 主要结论与发现
*   **几何信息至关重要**：仅靠 RGB 无法区分真实的场景结构和 3DGS 的漂浮物伪影，GP-Buffer 提供的几何线索是修复严重畸变的关键。
*   **跨范式泛化**：通过在多种退化模式下训练，单个模型即可同时修复优化型 3DGS 的噪声和前馈模型的过度平滑问题。
*   **效率与质量并重**：通过蒸馏技术，模型在保持 SOTA 修复质量的同时，达到了实时运行的水平（16-21 FPS），具备交互式应用潜力。

#### 7. 优点
*   **多模态融合**：创新性地将 3DGS 的内部几何属性（如协方差）引入生成模型。
*   **通用性强**：不绑定于特定的重建算法，对各种来源的 3DGS 渲染图均有显著提升。
*   **实时性**：4 步推理的设计极大地拓宽了该技术的应用场景。
*   **高质量数据集**：提出的伪影模拟流水线为该领域提供了极具价值的训练数据生成思路。

#### 8. 不足与局限
*   **运动模糊处理**：在处理具有剧烈相机运动或严重运动模糊的视频时，VAE 编码器可能会失效，导致生成质量下降。
*   **长期一致性**：虽然在 81 帧的窗口内表现良好，但对于超长视频，采用滑动窗口策略可能无法完全保证长期的全局时空同步。
*   **后处理依赖**：虽然能生成高质量图像，但要获得完美的 3D 模型，仍需进行一轮耗时的后验优化（Post-hoc Optimization）。

（完）
