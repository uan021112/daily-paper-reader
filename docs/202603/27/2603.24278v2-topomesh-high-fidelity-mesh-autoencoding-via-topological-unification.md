---
title: "TopoMesh: High-Fidelity Mesh Autoencoding via Topological Unification"
title_zh: TopoMesh：通过拓扑统一实现高保真网格自编码
authors: "Guan Luo, Xiu Li, Rui Chen, Xuanyu Yi, Jing Lin, Chia-Hao Chen, Jiahang Liu, Song-Hai Zhang, Jianfeng Zhang"
date: 2026-03-25
pdf: "https://arxiv.org/pdf/2603.24278v2"
tags: ["query:3dgen"]
score: 9.0
evidence: 用于高保真3D生成和网格重建的VAE扩散流水线
tldr: 本研究针对3D生成中VAE重建质量受限的问题，指出其根源在于真值网格与预测隐式场之间的拓扑不匹配。为此提出TopoMesh，一种基于稀疏体素的VAE，利用Dual Marching Cubes (DMC) 框架统一拓扑结构。通过将任意网格转换为DMC兼容表示，实现了预测与目标网格在顶点和面级别的显式对应与监督。该方法结合教师强制和渐进式分辨率训练，显著提升了对尖锐特征和几何细节的重建保真度。
selection_source: fresh_fetch
motivation: 现有3D VAE因真值网格与预测隐式场之间的拓扑不匹配，导致难以通过间接监督信号保留复杂的几何细节和尖锐特征。
method: 引入TopoMesh框架，利用Dual Marching Cubes将输入与预测网格统一在相同拓扑下，从而建立显式的顶点和面级监督。
result: 实验证明TopoMesh在重建保真度上大幅超越现有VAE，能够更完美地还原物体的尖锐边缘和精细几何结构。
conclusion: 通过拓扑统一化建立显式网格对应关系，是实现高保真3D网格自动编码和生成的关键突破口。
---

## 摘要
高保真 3D 生成的主流范式依赖于 VAE-Diffusion 流水线，其中 VAE 的重建能力为生成质量设定了坚实的上限。限制现有 VAE 的一个根本挑战是真值（GT）网格与网络预测之间的表示不匹配：GT 网格具有任意且可变的拓扑结构，而 VAE 通常预测固定结构的隐式场（例如规则网格上的 SDF）。这种固有的不匹配阻碍了建立显式的网格级对应关系，迫使先前的工作依赖于间接监督信号，如 SDF 或渲染损失。因此，在重建过程中，精细的几何细节（特别是锐利特征）难以得到良好保留。为了解决这一问题，我们提出了 TopoMesh，这是一种基于稀疏体素的 VAE，它在共享的双行进立方体（DMC）拓扑框架下统一了 GT 网格和预测网格。具体而言，我们通过一种重网格化算法将任意输入网格转换为符合 DMC 标准的表示，该算法利用 L∞ 距离度量来保留锐利边缘。我们的解码器以相同的 DMC 格式输出网格，确保预测网格和目标网格共享相同的拓扑结构。这在顶点和面级别建立了显式的对应关系，使我们能够为拓扑、顶点位置和面方向导出具有清晰梯度的显式网格级监督信号。我们的稀疏 VAE 架构采用了这一统一框架，并结合教师强制（Teacher Forcing）和渐进式分辨率训练进行训练，以实现稳定且高效的收敛。大量实验表明，TopoMesh 在重建保真度方面显著优于现有的 VAE，实现了对锐利特征和几何细节的卓越保留。

## Abstract
The dominant paradigm for high-fidelity 3D generation relies on a VAE-Diffusion pipeline, where the VAE's reconstruction capability sets a firm upper bound on generation quality. A fundamental challenge limiting existing VAEs is the representation mismatch between ground-truth meshes and network predictions: GT meshes have arbitrary, variable topology, while VAEs typically predict fixed-structure implicit fields (\eg, SDF on regular grids). This inherent misalignment prevents establishing explicit mesh-level correspondences, forcing prior work to rely on indirect supervision signals such as SDF or rendering losses. Consequently, fine geometric details, particularly sharp features, are poorly preserved during reconstruction. To address this, we introduce TopoMesh, a sparse voxel-based VAE that unifies both GT and predicted meshes under a shared Dual Marching Cubes (DMC) topological framework. Specifically, we convert arbitrary input meshes into DMC-compliant representations via a remeshing algorithm that preserves sharp edges using an L$\infty$ distance metric. Our decoder outputs meshes in the same DMC format, ensuring that both predicted and target meshes share identical topological structures. This establishes explicit correspondences at the vertex and face level, allowing us to derive explicit mesh-level supervision signals for topology, vertex positions, and face orientations with clear gradients. Our sparse VAE architecture employs this unified framework and is trained with Teacher Forcing and progressive resolution training for stable and efficient convergence. Extensive experiments demonstrate that TopoMesh significantly outperforms existing VAEs in reconstruction fidelity, achieving superior preservation of sharp features and geometric details.

---

## 论文详细总结（自动生成）

以下是对论文《TopoMesh: High-Fidelity Mesh Autoencoding via Topological Unification》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在当前的 3D 生成任务中，VAE-Diffusion 流水线是主流，但 VAE 的重建质量决定了生成的上限。
*   **核心挑战**：现有的 VAE 存在“表示不匹配”问题。真实值（GT）网格具有任意拓扑，而 VAE 通常预测固定结构的隐式场（如 SDF）。这种不匹配导致无法建立显式的网格级对应关系，迫使模型依赖 SDF 或渲染损失等间接监督，从而导致锐利边缘（Sharp Features）和精细几何细节在重建中丢失。
*   **核心目标**：设计一种能够实现“拓扑统一”的 VAE，使网络预测与 GT 网格共享相同的拓扑框架，从而实现直接的网格级监督。

### 2. 论文提出的方法论
该方法主要由两个核心模块组成：**Topo-Remesh**（预处理）和 **Topo-VAE**（核心架构）。

*   **Topo-Remesh（拓扑重网格化）**：
    *   **核心思想**：将任意输入网格转换为符合双行进立方体（DMC）标准的表示。
    *   **$L_\infty$ 度量**：不同于传统的 $L_2$ 距离（会使角点变圆），论文引入了基于局部表面结构的 $L_\infty$ 距离度量，通过偏移入射平面创建多面体包络，从而完美保留锐利边缘。
    *   **GPU 加速**：实现了全 GPU 加速的流水线（体素化、泛洪填充、SDF 计算、网格提取），在 1024³ 分辨率下仅需约 15 秒。
*   **Topo-VAE（稀疏体素自编码器）**：
    *   **稀疏体素-点编码器**：采用稀疏局部交叉注意力机制，每个体素仅关注其内部的点，将注意力复杂度从 $O(N \times P)$ 降低到 $O(P)$，支持处理数百万个点。
    *   **解耦解码器**：将 FlexiCubes 参数解耦为**拓扑**（占用率、三角化决策）和**几何**（SDF 幅值、顶点偏移）两部分。
*   **训练策略**：
    *   **教师强制（Teacher Forcing）**：训练时向解码器提供 GT 拓扑，让几何参数在稳定的配置下学习，解决拓扑与几何监督信号之间的“拉锯战”不稳定问题。
    *   **渐进式分辨率**：从低分辨率（32³）到高分辨率（128³）逐步训练，加速收敛。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：来自 Sketchfab 的 320k 个高质量网格。
    *   **评估集**：Objaverse、Thingi10K、Dora-Bench（1.4k 物体）以及新提出的 **Topo-Bench**（1k 个具有大量锐利边缘的物体）。
*   **对比方法**：
    *   **重网格化对比**：Mesh2SDF, ManifoldPlus, Dora, Sparc3D。
    *   **VAE 重建对比**：VecSet 类（TripoSG, Dora, Hunyuan3D-2.1）和稀疏体素类（Trellis, Direct3D-S2, SparseFlex）。
*   **衡量指标**：倒角距离（CD）、F1 分数、法线一致性（ANC），以及专门针对锐利特征的 **F1-Sharp**。

### 4. 资源与算力
*   **训练细节**：
    *   **VAE 训练**：总计 700k 步，Batch Size 为 64。
    *   **DiT 训练**：总计 800k 步，Batch Size 为 512。
    *   **优化器**：AdamW，学习率 0.0001。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号和数量，但考虑到其处理的分辨率（1024³）和训练步数，通常需要高性能计算集群（如 A100 或 H100 级显卡）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从基础的重网格化质量评估到复杂的端到端 3D 生成任务。
*   **消融实验**：针对 $L_\infty$ 距离的有效性、网格级监督与渲染监督的对比、多分辨率推理进行了详细验证。
*   **充分性评价**：实验设计较为充分且客观。通过引入 F1-Sharp 指标，专门量化了锐利特征的保留情况，这在以往研究中较少见。对比实验涵盖了当前最先进的（SOTA）开源和商业模型，具有较强的说服力。

### 6. 论文的主要结论与发现
*   **拓扑统一是关键**：通过 DMC 框架统一 GT 和预测网格，建立显式的顶点/面级对应关系，是提升重建保真度的核心。
*   **性能提升**：在 F-Score 上比现有方法提升了约 8%，在锐利特征保留（F1-Sharp）上显著优于 SparseFlex 等强力竞争对手。
*   **生成能力**：作为 3D 生成的骨干网络，TopoMesh 能够产生比 Trellis 和 Direct3D-S2 更清晰、更符合输入图像的几何结构。

### 7. 优点
*   **理论创新**：提出了“拓扑统一”原则，解决了长期存在的表示不匹配问题。
*   **技术突破**：引入 $L_\infty$ 度量进行重网格化，有效解决了 $L_2$ 距离导致的边缘平滑问题。
*   **效率高**：全 GPU 加速的重网格化和高效的稀疏注意力机制，使其能够处理极高分辨率的网格。
*   **训练稳定**：通过解耦解码器和教师强制策略，克服了复杂几何学习中的不收敛问题。

### 8. 不足与局限
*   **计算开销**：尽管使用了稀疏体素，但在上采样到极高分辨率时，仍会产生数百万个体素，对显存和计算资源有较高要求。
*   **分辨率限制**：重网格化算法受限于基础体素分辨率，无法捕捉小于体素尺寸的极微小细节。
*   **应用门槛**：由于需要特定的 DMC 预处理，该框架在处理非流形或极度破碎的原始输入时可能需要额外的清理工作。

（完）
