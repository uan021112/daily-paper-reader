---
title: "FG-Portrait: 3D Flow Guided Editable Portrait Animation"
title_zh: FG-Portrait：3D 流引导的可编辑人像动画
authors: "Yating Xu, Yunqi Miao, Evangelos Ververas, Jiankang Deng, Jifei Song"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23381v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 来自参数化3D头部模型的3D流引导运动对应
tldr: 针对人像动画中运动迁移不准确的问题，本文提出FG-Portrait。该方法利用参数化3D头部模型计算无需学习的3D流，作为几何驱动的运动对应关系。通过3D流编码和深度引导采样，将3D先验集成到扩散模型中，实现了高保真的人像动画和可编辑的表情与姿态，在运动迁移一致性和身份保持方面表现优异。
selection_source: fresh_fetch
motivation: 现有的扩散模型和2D流方法难以准确捕捉源图像与驱动视频之间的运动对应关系，导致动画效果欠佳。
method: 引入基于3D头部模型的几何驱动3D流，并结合3D流编码与深度引导采样技术，将精确的运动先验融入扩散生成过程。
result: 实验证明该方法在保持身份特征和实现一致性运动迁移方面优于现有技术，并支持灵活的表情与姿态编辑。
conclusion: FG-Portrait通过引入3D几何先验，有效解决了人像动画中的运动对应难题，提升了生成视频的质量与可控性。
---

## 摘要
从驱动人像到源人像的运动迁移仍然是人像动画中的一个关键挑战。目前基于扩散的方法仅以驱动运动为条件，无法捕捉源到驱动的对应关系，从而导致运动迁移效果欠佳。虽然流估计提供了一种替代方案，但从 2D 输入预测密集对应关系是一个病态问题，且通常会导致动画不准确。我们通过引入 3D 流（3D flows）来解决这一问题，这是一种直接从参数化 3D 头部模型计算出的、无需学习且由几何驱动的运动对应关系。为了将这种 3D 先验整合到扩散模型中，我们引入了 3D 流编码，为每个目标像素查询潜在的 3D 流，以指示其回到源位置的位移。为了获得与 2D 运动变化一致的 3D 流，我们进一步提出了深度引导采样，以准确定位每个像素对应的 3D 点。除了高保真人像动画外，我们的模型还支持用户指定的面部表情和头部姿态编辑。大量实验证明了我们的方法在一致的驱动运动迁移以及忠实的源身份保持方面的优越性。

## Abstract
Motion transfer from the driving to the source portrait remains a key challenge in the portrait animation. Current diffusion-based approaches condition only on the driving motion, which fails to capture source-to-driving correspondences and consequently yields suboptimal motion transfer. Although flow estimation provides an alternative, predicting dense correspondences from 2D input is ill-posed and often yields inaccurate animation. We address this problem by introducing 3D flows, a learning-free and geometry-driven motion correspondence directly computed from parametric 3D head models. To integrate this 3D prior into diffusion model, we introduce 3D flow encoding to query potential 3D flows for each target pixel to indicate its displacement back to the source location. To obtain 3D flows aligned with 2D motion changes, we further propose depth-guided sampling to accurately locate the corresponding 3D points for each pixel. Beyond high-fidelity portrait animation, our model further supports user-specified editing of facial expression and head pose. Extensive experiments demonstrate the superiority of our method on consistent driving motion transfer as well as faithful source identity preservation.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **FG-Portrait** 的新型人像动画生成框架，旨在通过 3D 几何先验解决驱动图像与源图像之间的运动对应难题。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在人像动画（Portrait Animation）中，如何准确地将驱动图像（Driving Image）的运动（姿态和表情）迁移到源图像（Source Image）上，同时保持源人物的身份特征（Identity）。
*   **研究背景与动机**：
    *   现有的基于扩散模型的方法通常仅以驱动运动（如地标或图像特征）为条件，缺乏源与驱动之间的显式对应关系，导致在大角度或大幅度表情下运动迁移不准确。
    *   传统的 2D 流（Flow）估计方法在处理 3D 运动时具有病态性（Ill-posed），且依赖大量数据训练，泛化性差。
    *   作者提出利用 **3D 参数化模型（FLAME）** 提供的几何一致性，构建一种无需学习且几何感知的 **3D 流（3D Flow）** 来引导生成过程。

### 2. 核心方法论
*   **核心思想**：利用 3D 头部模型的顶点对应关系，计算目标空间到源空间的 3D 位移向量（3D Flow），并将其作为扩散模型的控制信号。
*   **关键技术细节**：
    1.  **3D 流计算**：首先通过 FLAME 模型估计源图像和驱动图像的参数。通过组合源图像的形状（Shape）与驱动图像的姿态（Pose）和表情（Expr），构建目标 3D 头部模型。利用表面场（Surface Field）建立目标点到源点的点对点映射，计算位移向量 $f_{src \leftarrow tgt}$。
    2.  **3D 流编码（3D Flow Encoding）**：为了将 3D 信息引入 2D 扩散模型，为目标图像的每个像素沿反向投影射线采样 $N$ 个点，查询其对应的 3D 流，并堆叠成高维特征图。
    3.  **深度引导采样（Depth-Guided Sampling）**：为了使 3D 流与 2D 运动对齐，不使用均匀采样，而是根据渲染的深度图在 3D 表面附近进行采样，从而获得更精确的运动引导。
    4.  **模型架构**：基于 Stable Diffusion 1.5，包含外观网络（Appearance Net）提取源特征，以及 ControlNet 接收 3D 流编码作为运动控制信号。
    5.  **可编辑性**：由于基于参数化模型，用户可以通过调整 FLAME 的姿态和表情参数，在推理阶段直接对生成结果进行交互式编辑。

### 3. 实验设计
*   **数据集**：
    *   **VFHQ**：用于训练（1K 视频）和测试（官方测试集）。
    *   **FFHQ**：用于测试模型的泛化能力。
*   **实验场景**：
    *   **自重演（Self-reenactment）**：源和驱动来自同一视频。
    *   **跨重演（Cross-reenactment）**：源和驱动来自不同身份。
*   **对比方法（Benchmarks）**：包括 EMOPortrait、X-Portrait、Follow-Your-Emoji、Face-Adapter 和 HunyuanPortrait 等 SOTA 方法。
*   **评价指标**：LPIPS（图像质量）、CSIM（身份保持）、APD（姿态误差）、AED（表情误差）、FID（生成质量）和 FVD（视频时间一致性）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确列出**具体的 GPU 型号、数量或总训练时长。
*   **实现细节**：提到了使用 AdamW 优化器，学习率为 1e-5，采样点数 $N=20$。在消融实验的表格中提到了显存占用（Mem）约为 34GB 左右，暗示使用了高性能显卡（如 A100 或 H100 级别）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了大规模的定量对比（VFHQ 和 FFHQ 两个数据集）。
    *   提供了丰富的定性对比图，涵盖了大角度旋转、复杂配饰、不同族裔和年龄。
    *   **消融实验**：针对 3D 流编码的有效性、深度引导采样的必要性、采样点数 $N$ 和采样范围 $\delta$ 进行了详细测试。
*   **充分性评价**：实验设计较为全面，不仅对比了扩散模型，还对比了基于 GAN 的流预测方法。通过自重演和跨重演双重验证，证明了方法在运动准确性和身份保持之间的平衡能力。

### 6. 主要结论与发现
*   **3D 流的优越性**：相比于 2D 地标或隐式运动特征，3D 流能提供更精确的几何约束，显著降低了姿态（APD）和表情（AED）的误差。
*   **深度引导的重要性**：简单的均匀采样会导致运动引导模糊，而基于深度的采样能确保 3D 流与 2D 像素完美对齐。
*   **平衡性**：该方法在保持源人物身份（CSIM）的同时，实现了比现有方法更贴合驱动源的运动迁移。

### 7. 优点与亮点
*   **几何感知**：将 3D 参数化模型的结构化先验引入扩散模型，解决了纯 2D 方法难以处理的遮挡和大幅度旋转问题。
*   **无需学习的流**：3D 流直接计算得出，不依赖复杂的流预测网络，增强了鲁棒性。
*   **高可控性**：支持前向推理阶段的参数化编辑（姿态、表情强度），这是许多隐式控制方法不具备的。
*   **泛化性强**：在未见过的 FFHQ 数据集甚至卡通人像上均表现出良好的效果。

### 8. 不足与局限
*   **模型精度限制**：受限于 FLAME 模型的网格分辨率，可能无法完美捕捉极其细微的表情（如复杂的皱纹变化）。
*   **依赖前置估计**：效果高度依赖于第一步 3D 头部重建（FLAME Fitting）的准确性，如果 3D 拟合失败，生成质量会受损。
*   **领域偏差**：在卡通人像上虽然可行，但由于训练数据主要是真人，闭眼等动作可能会产生伪影。

（完）
