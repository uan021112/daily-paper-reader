---
title: Generative Shape Reconstruction with Geometry-Guided Langevin Dynamics
title_zh: 基于几何引导朗之万动力学的生成式形状重建
authors: "Linus Härenstam-Nielsen, Dmitrii Pozdeev, Thomas Dagès, Nikita Araslanov, Daniel Cremers"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.27016v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 使用朗之万动力学的3D形状生成模型
tldr: 本研究针对从不完整或有噪声的观测中重建3D形状这一难题，提出了GG-Langevin（几何引导朗之万动力学）。该方法结合了扩散模型的生成能力与几何测量的一致性约束，通过在朗之万动力学采样过程中实时修正轨迹，确保生成的形状既符合数据先验又满足观测限制。实验表明，该方法在几何精度和对缺失数据的鲁棒性上显著优于现有表面重建技术，有效解决了传统方法在现实复杂条件下的失效问题。
selection_source: fresh_fetch
motivation: 现有3D形状重建方法在处理不完整或高噪声数据时，难以同时兼顾几何保真度与生成形状的合理性。
method: 提出一种几何引导的朗之万动力学方法，利用扩散模型作为先验，并在采样轨迹的每一步强制执行测量一致性。
result: 实验结果显示，GG-Langevin在表面重建的几何准确性以及对大规模缺失数据的鲁棒性方面均达到了领先水平。
conclusion: 该研究成功统一了生成式建模与几何约束，为实现高质量、高鲁棒性的3D形状重建提供了新的概率化路径。
---

## 摘要
从不完整或有噪声的观测中重建完整的三维形状是一个根本性的病态问题，需要平衡测量一致性与形状合理性。现有的形状重建方法在理想条件下可以实现极高的几何保真度，但在存在不完整测量或噪声的现实条件下往往会失效。与此同时，近期的三维形状生成模型可以合成高度逼真且细节丰富的形状，但无法与观测到的测量结果保持一致。在这项工作中，我们提出了 GG-Langevin：几何引导的朗之万动力学，这是一种统一了这些互补视角的概率方法。通过遍历由扩散模型诱导的朗之万动力学轨迹，并在每一步保持测量一致性，我们以生成的方式重建出既符合测量结果又符合数据先验的形状。我们通过广泛的实验证明，与现有的表面重建方法相比，GG-Langevin 实现了更高的几何精度和对缺失数据更强的鲁棒性。

## Abstract
Reconstructing complete 3D shapes from incomplete or noisy observations is a fundamentally ill-posed problem that requires balancing measurement consistency with shape plausibility. Existing methods for shape reconstruction can achieve strong geometric fidelity in ideal conditions but fail under realistic conditions with incomplete measurements or noise. At the same time, recent generative models for 3D shapes can synthesize highly realistic and detailed shapes but fail to be consistent with observed measurements. In this work, we introduce GG-Langevin: Geometry-Guided Langevin dynamics, a probabilistic approach that unifies these complementary perspectives. By traversing the trajectories of Langevin dynamics induced by a diffusion model, while preserving measurement consistency at every step, we generatively reconstruct shapes that fit both the measurements and the data-informed prior. We demonstrate through extensive experiments that GG-Langevin achieves higher geometric accuracy and greater robustness to missing data than existing methods for surface reconstruction.

---

## 论文详细总结（自动生成）

这篇论文由慕尼黑工业大学（TUM）的研究团队提出，旨在解决从稀疏、有噪声或不完整的点云中重建完整三维形状的难题。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：三维形状重建是一个典型的“病态问题”（ill-posed problem），需要在**测量一致性**（符合观测到的点云）和**先验一致性**（符合真实形状的分布）之间取得平衡。
*   **研究背景**：
    *   **优化法**（如 IGR）：擅长拟合观测数据，但缺乏数据先验，在数据缺失时会产生过度平滑或不合理的形状。
    *   **学习法**（如 NKSR）：直接从数据学习映射，但在面对训练集之外的噪声模式时，往往无法保持测量一致性。
    *   **生成模型**（如 3D 扩散模型）：能生成极高质量的形状，但难以精确约束以匹配特定的测量结果。
*   **研究动机**：提出一种统一框架，既能利用扩散模型的强大先验，又能像优化法一样严格遵守几何约束。

### 2. 方法论：GG-Langevin 与 HDND 采样
*   **核心思想**：将重建任务重新解释为从“几何引导的形状分布”中进行概率采样。该分布由扩散模型提供的先验分布 $p(z)$ 和基于几何损失函数的权重因子 $\psi_P(z)$ 共同决定。
*   **关键技术细节**：
    *   **GG-Langevin（几何引导朗之万动力学）**：通过朗之万动力学在潜在空间（Latent Space）中移动。每一步更新包含两部分：一是向高概率形状区域移动（扩散模型引导），二是向几何损失减小的方向移动（几何梯度引导）。
    *   **HDND 采样算法（Half-Denoising-No-Denoising）**：这是本文的核心创新。
        *   **Half-Denoising**：扩散模型在带噪的潜在变量上操作，估计得分函数（Score Function）。
        *   **No-Denoising**：几何损失函数（IGR 损失，包含表面拟合项和 Eikonal 正则项）直接在去噪后的潜在变量上计算梯度。
        *   这种混合策略避免了现有方法（如 DPS）在极高噪声水平下计算几何梯度导致的轨迹发散问题。
    *   **重平衡 VAE 架构**：为了提高推理效率，作者重新设计了 VecSet VAE 架构，通过移动瓶颈层减小了解码器（Decoder）的大小，从而加快了推理过程中几何梯度的反向传播速度。

### 3. 实验设计
*   **数据集与场景**：使用 ShapeNet 中的四个类别（汽车、飞机、桌子、椅子）。设置了两种挑战场景：
    1.  **稀疏扫描**：具有空间变化的稀疏性和噪声。
    2.  **不完整扫描**：模拟大面积遮挡导致的几何缺失。
*   **Benchmark 与对比方法**：
    *   **优化类**：IGR, DiffCD。
    *   **学习类**：3DILG, VecSet, ShapeFormer。
    *   **先验类**：DeepSDF, NKSR。
*   **评估指标**：倒角距离（Chamfer Distance, CD）和倒角角度（Chamfer Angle, CA，用于衡量法线精度）。

### 4. 资源与算力
*   **训练算力**：
    *   **VAE 预训练**：在 8 张 NVIDIA A100 GPU 上训练约 3 天。
    *   **扩散模型训练**：在 8 张 NVIDIA A100 GPU 上训练约 5 天。
*   **推理开销**：GG-Langevin 是迭代式方法，通常需要 2000 到 5000 次迭代。通过重平衡 VAE，单次迭代速度提升了约 2 倍。

### 5. 实验数量与充分性
*   **实验规模**：在 4 个类别、2 种噪声模式下进行了详尽的定量对比。
*   **消融实验**：非常充分，涵盖了采样器对比（对比了 MAP, DPS, DAPS）、VAE 瓶颈位置影响、噪声水平 $\sigma$ 与引导强度 $\beta$ 的超参数敏感性分析、以及类条件（Class Conditioning）的影响。
*   **客观性**：作者重新训练了 DeepSDF 等基准方法，并为 DPS/DAPS 等对比方法适配了相同的编码器初始化，确保了对比的公平性。

### 6. 主要结论与发现
*   **性能领先**：GG-Langevin 在所有测试类别和场景中均显著优于现有 SOTA 方法，尤其在处理不完整数据时，其补全能力远超传统优化法。
*   **采样稳定性**：提出的 HDND 采样比现有的扩散逆问题求解器（如 DPS）更稳定，能产生更平滑、更精确的表面。
*   **架构影响**：发现较小的解码器不仅能加速推理，还能提供更清晰的梯度信号，有助于提升重建质量。

### 7. 优点与亮点
*   **理论优雅**：将确定性几何优化与随机概率采样完美结合，理论基础扎实。
*   **无需特定任务训练**：该方法使用通用的形状先验，不需要针对“补全”或“去噪”任务进行专门训练，具有极强的通用性。
*   **鲁棒性强**：对极度缺失的数据表现出惊人的幻觉（Hallucination）补全能力，且补全部分与已有部分衔接自然。

### 8. 不足与局限
*   **推理速度**：作为一种基于迭代采样的生成式方法，其推理速度远慢于前馈神经网络（如 NKSR），不适合实时应用。
*   **局部最优风险**：在极少数情况下，采样可能会陷入局部最优，导致产生多余的几何碎片（Spurious Geometry）或补全不完整。
*   **依赖潜在空间**：方法的有效性高度依赖于 VAE 潜在空间的质量，如果 VAE 无法很好地编码某种复杂几何，重建效果会受限。

（完）
