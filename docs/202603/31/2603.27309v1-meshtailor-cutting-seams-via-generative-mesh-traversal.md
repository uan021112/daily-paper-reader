---
title: "MeshTailor: Cutting Seams via Generative Mesh Traversal"
title_zh: MeshTailor：通过生成式网格遍历切割接缝
authors: "Xueqi Ma, Xingguang Yan, Congyue Zhang, Hui Huang"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27309v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 在3D表面合成接缝的生成式框架
tldr: 本研究提出MeshTailor，这是首个用于在3D表面合成边缘对齐接缝的网格原生生成框架。针对传统方法存在的投影伪影和对齐脆弱问题，MeshTailor直接在网格图上操作，引入了层次化序列化表示ChainingSeams和融合拓扑与几何信息的双流编码器。通过Transformer架构和自回归指针层，该方法能逐顶点追踪接缝，确保生成的布局具有专业级质量且完全对齐边缘，显著优于现有的优化和学习基准。
selection_source: fresh_fetch
motivation: 传统3D接缝生成方法因依赖投影或外部学习，常导致投影伪影和边缘对齐不精确的问题。
method: 开发了一个直接在网格图上运行的生成框架，结合层次化接缝表示与双流编码器，通过Transformer进行自回归顶点遍历。
result: 实验表明，MeshTailor能够生成比现有优化和学习方法更连贯、更具专业水准的边缘对齐接缝布局。
conclusion: 该研究通过网格原生的生成式遍历方法，有效解决了3D模型接缝合成中的拓扑一致性和几何对齐难题。
---

## 摘要
我们提出了 MeshTailor，这是首个用于在 3D 表面合成边缘对齐接缝的网格原生生成框架。与以往基于优化或基于外部学习的方法不同，MeshTailor 直接在网格图上操作，消除了投影伪影和脆弱的对齐启发式方法。我们引入了 ChainingSeams，这是一种接缝图的分层序列化方法，它以由粗到精的方式优先处理全局结构切割，然后再处理局部细节；此外还引入了一个融合拓扑和几何上下文的双流编码器。利用这种分层表示和丰富的顶点嵌入，我们的 MeshTailor Transformer 使用自回归指针层在局部邻域内逐顶点追踪接缝，确保了无投影且边缘对齐的接缝。广泛的评估表明，与最近基于优化和基于学习的基准方法相比，MeshTailor 能够产生更连贯、具有专业质量的接缝布局。

## Abstract
We present MeshTailor, the first mesh-native generative framework for synthesizing edge-aligned seams on 3D surfaces. Unlike prior optimization-based or extrinsic learning-based methods, MeshTailor operates directly on the mesh graph, eliminating projection artifacts and fragile snapping heuristics. We introduce ChainingSeams, a hierarchical serialization of the seam graph that prioritizes global structural cuts before local details in a coarse-to-fine manner, and a dual-stream encoder that fuses topological and geometric context. Leveraging this hierarchical representation and enriched vertex embeddings, our MeshTailor Transformer utilizes an autoregressive pointer layer to trace seams vertex-by-vertex within local neighborhoods, ensuring projection-free, edge-aligned seams. Extensive evaluations show that MeshTailor produces more coherent, professional-quality seam layouts compared to recent optimization-based and learning-based baselines.

---

## 论文详细总结（自动生成）

### MeshTailor: 通过生成式网格遍历切割接缝 —— 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
在3D建模和纹理贴图（UV Mapping）流程中，**接缝（Seams）**的放置至关重要。理想的接缝应遵循物体的几何逻辑（如对称性、曲率极值），以便将3D表面平展为低畸变、易于编辑的2D布局。
*   **现有挑战：** 传统基于优化的方法（如 OptCuts）虽能降低几何畸变，但往往产生破碎、无语义的“螺旋式”切割；现有的深度学习方法（如 SeamGPT）多在欧几里得空间预测坐标，再将其“吸附”回网格，这会导致投影伪影和边缘对齐不准。
*   **核心目标：** 论文提出 **MeshTailor**，旨在实现首个**网格原生（Mesh-native）**的生成式框架，直接在网格拓扑上通过“遍历”方式生成边缘对齐、符合专业审美且语义连贯的接缝。

#### 2. 方法论：核心思想与技术细节
MeshTailor 将接缝生成建模为网格图上的**自回归图遍历**任务。
*   **ChainingSeams（分层序列化）：** 为了让 Transformer 处理无序的接缝图，提出了一种“由粗到精”的排序策略：优先处理能平分大块表面的闭合环路（Loop Cuts），再处理局部细节。这种顺序模拟了专业裁缝的逻辑。
*   **双流编码器（Dual-stream Encoder）：**
    *   **拓扑流：** 使用 GraphSAGE 提取网格顶点的局部连接特征。
    *   **几何流：** 使用预训练的点云编码器提取全局形状语义。
    *   **融合：** 通过交叉注意力机制将全局形状信息注入局部顶点嵌入中。
*   **MeshTailor Transformer（自回归解码）：**
    *   使用**指针网络（Pointer Network）**架构。
    *   **关键约束：** 在每一步预测中，将候选顶点限制在当前顶点的 **1-ring 邻域**内。这确保了生成的接缝严格沿网格边缘行走，从根本上消除了投影伪影。
*   **分治推理（Divide-and-Conquer）：** 针对高分辨率网格，采用递归分割策略，先生成主环路分割网格，再在子块上递归生成。

#### 3. 实验设计
*   **数据集：** 使用了两个大规模数据集：**TexVerse**（30万个零件级组件）和 **GarmentCodeData**（11万个服装样本），涵盖了复杂的工业模型和缝纫模式。
*   **对比基准（Baselines）：**
    *   **传统/优化类：** xatlas（工业标准）、Blender Smart UV、OptCuts。
    *   **学习类：** Nuvo（神经UV映射）、PartUV（基于零件的解包）。
*   **评估指标：** 涵盖几何畸变（Area/Angular Distortion）、UV岛质量（紧凑度、凸性）、接缝属性（长度、平滑度/锯齿感）以及图表数量。

#### 4. 资源与算力
*   **硬件：** 使用了 **8 台 NVIDIA RTX 4090 GPU** 进行训练。
*   **训练细节：** 采用 AdamW 优化器，学习率为 $10^{-4}$，批大小为 64，训练时长为 30 个 epoch。
*   **数据规模：** 训练时将网格简化至 2000 面以内以提高效率，推理时可通过分治策略扩展。

#### 5. 实验数量与充分性
*   **实验规模：** 在数十万个样本上进行了训练和测试，实验规模巨大。
*   **消融实验：** 验证了图编码器（Enc G）、形状编码器（Enc P）以及“指针式遍历”对比“坐标预测式生成”的优越性。
*   **主观评估：** 组织了 **100 名参与者**进行了 **5000 次成对投票（2AFC）**，从专业审美角度验证了 MeshTailor 的结果更符合生产需求。
*   **客观性：** 实验涵盖了不同分辨率的泛化性测试、顶点抖动下的稳定性测试，评估维度全面且客观。

#### 6. 主要结论与发现
*   **网格原生优势：** 直接在拓扑上操作比在空间坐标预测再投影的方法（如类 SeamGPT 方法）生成的边界更平滑、无断裂。
*   **性能平衡：** 虽然在纯几何畸变指标上略逊于以畸变为唯一目标的优化方法（如 xatlas），但在**生产可用性**（UV岛的整齐度、接缝的语义对齐）上远超所有基准。
*   **泛化能力：** 模型在未见过的类别和更高分辨率的网格上表现出良好的鲁棒性。

#### 7. 优点
*   **首创性：** 首次实现了网格原生的接缝生成，解决了边缘对齐的顽疾。
*   **高质量输出：** 生成的接缝具有长程连贯性，尤其是能自动识别并生成关键的“环路切割（Loop Cuts）”。
*   **实用性强：** 结果直接兼容现有的 DCC 工具（如 Blender, Maya），生成的 UV 布局非常适合纹理绘制和编辑。

#### 8. 不足与局限
*   **分辨率限制：** 基础模型主要针对低多边形网格（约 2000 面），极高精度的模型依赖分治策略，可能增加计算开销。
*   **特定几何失效：** 在处理极细碎、多刺的几何体（如发丝、植被）时，接缝生成可能变得不稳定。
*   **自回归误差累积：** 作为自回归模型，一旦中间某个顶点预测错误，可能会导致整条接缝链偏离预想路径。

（完）
