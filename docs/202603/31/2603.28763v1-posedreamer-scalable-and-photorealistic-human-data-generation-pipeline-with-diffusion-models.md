---
title: "PoseDreamer: Scalable and Photorealistic Human Data Generation Pipeline with Diffusion Models"
title_zh: PoseDreamer：基于扩散模型的可扩展且写实的人体数据生成流水线
authors: "Lorenza Prospero, Orest Kupyn, Ostap Viniavskyi, João F. Henriques, Christian Rupprecht"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28763v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 利用扩散模型生成3D人体网格数据集
tldr: PoseDreamer是一个利用扩散模型生成大规模、高保真3D人体网格估计数据集的流水线。针对现有数据集规模有限或真实感不足的问题，该方法结合了可控图像生成、直接偏好优化（DPO）、课程学习硬样本挖掘及多阶段质量过滤。通过生成超过50万个高质量样本，PoseDreamer在图像质量上显著优于传统渲染数据集，并在模型训练效果上达到或超越了真实及传统合成数据集的水平。
selection_source: fresh_fetch
motivation: 现有的3D人体网格估计数据集面临真实数据标注难、规模小，以及传统合成数据缺乏真实感和多样性的双重挑战。
method: 提出PoseDreamer流水线，利用扩散模型结合DPO对齐、硬样本挖掘和质量过滤，生成带有精确3D标注的高保真人体图像。
result: "生成了超过50万个高质量样本，图像质量指标提升76%，且训练出的模型性能优于或等同于使用传统数据集训练的模型。"
conclusion: PoseDreamer证明了生成式数据在提升3D人体任务性能方面的巨大潜力，为构建大规模、高质量的视觉学习数据集提供了新路径。
---

## 摘要
由于深度歧义以及从单目图像标注 3D 几何结构的固有困难，获取用于 3D 人体网格估计的有标签数据集具有挑战性。现有数据集要么是真实的，具有手动标注的 3D 几何结构且规模有限；要么是合成的，由 3D 引擎渲染而成，虽然提供精确标签，但存在写实度有限、多样性低和生产成本高的问题。在这项工作中，我们探索了第三条路径：生成数据。我们介绍了 PoseDreamer，这是一个利用扩散模型生成带有 3D 网格标注的大规模合成数据集的新型流水线。我们的方法将可控图像生成与用于控制对齐的直接偏好优化（Direct Preference Optimization）、基于课程学习的难样本挖掘以及多阶段质量过滤相结合。这些组件共同自然地保持了 3D 标签与生成图像之间的对应关系，同时优先处理具有挑战性的样本以最大化数据集的效用。使用 PoseDreamer，我们生成了超过 500,000 个高质量合成样本，与基于渲染的数据集相比，在图像质量指标上实现了 76% 的提升。在 PoseDreamer 上训练的模型达到了与在真实世界和传统合成数据集上训练的模型相当或更优的性能。此外，将 PoseDreamer 与合成数据集结合使用，其性能优于将真实世界与合成数据集结合，证明了我们数据集的互补性。我们将发布完整的数据集和生成代码。

## Abstract
Acquiring labeled datasets for 3D human mesh estimation is challenging due to depth ambiguities and the inherent difficulty of annotating 3D geometry from monocular images. Existing datasets are either real, with manually annotated 3D geometry and limited scale, or synthetic, rendered from 3D engines that provide precise labels but suffer from limited photorealism, low diversity, and high production costs. In this work, we explore a third path: generated data. We introduce PoseDreamer, a novel pipeline that leverages diffusion models to generate large-scale synthetic datasets with 3D mesh annotations. Our approach combines controllable image generation with Direct Preference Optimization for control alignment, curriculum-based hard sample mining, and multi-stage quality filtering. Together, these components naturally maintain correspondence between 3D labels and generated images, while prioritizing challenging samples to maximize dataset utility. Using PoseDreamer, we generate more than 500,000 high-quality synthetic samples, achieving a 76% improvement in image-quality metrics compared to rendering-based datasets. Models trained on PoseDreamer achieve performance comparable to or superior to those trained on real-world and traditional synthetic datasets. In addition, combining PoseDreamer with synthetic datasets results in better performance than combining real-world and synthetic datasets, demonstrating the complementary nature of our dataset. We will release the full dataset and generation code.

---

## 论文详细总结（自动生成）

以下是对论文《PoseDreamer: Scalable and Photorealistic Human Data Generation Pipeline with Diffusion Models》的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：3D 人体网格估计（HMR）任务极度依赖大规模标注数据。然而，**真实数据**标注困难且存在深度歧义；**传统合成数据**（如 3D 引擎渲染）虽然标签精确，但视觉真实感（Photorealism）不足、场景多样性低且制作成本高昂。
*   **核心目标**：探索“生成式数据”路径，利用扩散模型（Diffusion Models）构建一个既具有高保真视觉效果，又具备精确 3D 标注的大规模人体数据集，以弥补真实数据与渲染数据之间的鸿沟。

### 2. 方法论
PoseDreamer 采用“逆向生成”逻辑，即先确定 3D 参数，再生成对应图像。
*   **数据来源与标签生成**：
    *   结合 **LAION**（提供真实世界的场景多样性和文本描述）和 **AMASS**（提供复杂的运动捕捉 3D 姿态）。
    *   使用 SMPL-X 模型参数化人体，并利用 VLM（如 Gemma）生成详细的场景描述。
*   **空间控制编码**：
    *   提出改进的 **Mesh-to-RGB 编码方案**（基于 PNCC 归一化 X,Y,Z 坐标映射至 RGB 通道），比传统的 DensePose 编码能提供更丰富的空间线索。
*   **控制模型对齐（DPO Alignment）**：
    *   引入**直接偏好优化（DPO）**，利用 2D 关键点相似度（OKS）作为奖励信号，微调扩散控制模型（LoRA），强制生成图像与 3D 姿态标签在空间上精确对齐。
*   **课程学习与硬样本挖掘**：
    *   两阶段流水线：先生成基准数据训练模型，再利用梯度提升决策树（GBDT）预测样本难度，优先生成模型表现较差的“硬样本”（如复杂肢体交叠）。
*   **多阶段质量过滤**：
    *   通过 YOLO 过滤拥挤场景、OKS 过滤姿态不匹配样本、VGGHeads 过滤 3D 头部姿态不一致样本。

### 3. 实验设计
*   **下游任务**：单人 3D 人体网格恢复（HMR）。
*   **评估基准（Benchmarks）**：
    *   **合成数据集**：AGORA。
    *   **真实世界数据集**：UBody（多场景）、EgoBody（第一视角）、3DPW（户外）、EHF（实验室）。
*   **对比方法**：与 BEDLAM、SynBody、GTA-Human II 等主流合成数据集，以及 MSCOCO、InstaVariety 等真实数据集进行对比。
*   **评估指标**：PVE（顶点误差）、MPJPE（关节误差）、IS（起始分数）、FID（分布距离）。

### 4. 资源与算力
*   **DPO 对齐训练**：使用 4 张 **NVIDIA A6000 GPU**，耗时 2 天（6,400 次迭代）。
*   **数据集生成**：总计消耗约 **6,700 GPU 小时**，从 80 万个初始样本中过滤出 50 万个高质量样本。
*   **基础模型**：采用 FLUX.1-dev 作为基础扩散模型。

### 5. 实验数量与充分性
*   **实验规模**：生成了 **50 万** 规模的数据集，实验涵盖了从 ViT-S 到 ViT-L 的不同模型缩放（Scaling）分析。
*   **消融实验**：针对过滤机制、AMASS 数据引入、硬样本挖掘等关键组件进行了详细的消融对比。
*   **领域扩展**：额外进行了针对“瑜伽（Yoga）”特定领域的生成与训练实验，验证了流水线的灵活性。
*   **充分性评价**：实验设计非常全面，不仅验证了精度，还通过 FID/IS 指标量化了图像质量，并利用 UMAP/t-SNE 可视化了数据分布，客观证明了生成数据在缩小领域间隙（Domain Gap）方面的优势。

### 6. 主要结论与发现
*   **质量飞跃**：PoseDreamer 生成的数据在图像质量指标上比传统渲染数据集提升了 **76%**。
*   **性能卓越**：仅在 PoseDreamer 上训练的模型，其泛化性能优于或等同于在昂贵的渲染数据集（如 BEDLAM）上训练的模型。
*   **互补性**：将 PoseDreamer 与渲染数据集混合训练，效果优于“真实+渲染”的组合，证明了生成数据能提供渲染引擎难以模拟的真实视觉细节。
*   **可扩展性**：通过简单的文本提示词（Prompt）变更，即可在固定 3D 姿态下生成无限多样的背景、服装和环境。

### 7. 优点
*   **高真实感**：解决了传统合成数据“一眼假”的问题，显著降低了 Sim-to-Real 的迁移难度。
*   **精确对齐**：通过 DPO 解决了扩散模型在生成人体时常见的“幻觉”和姿态偏移问题。
*   **低成本扩展**：相比于构建复杂的 3D 渲染场景，该流水线更易于扩展到特定领域（如体育、医疗）。

### 8. 不足与局限
*   **标签精度微差**：尽管经过对齐，生成图像的标签精度在极端情况下仍略逊于物理引擎渲染的“绝对真值”。
*   **模型偏见**：继承了基础模型（FLUX/LAION）的审美偏见，可能导致某些罕见姿态或特定人群的生成质量下降。
*   **物理合理性**：偶尔会出现人体“漂浮”或与环境交互不自然的物理违和感（如脚未着地）。

（完）
