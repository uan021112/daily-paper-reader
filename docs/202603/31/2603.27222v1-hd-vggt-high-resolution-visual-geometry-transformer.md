---
title: "HD-VGGT: High-Resolution Visual Geometry Transformer"
title_zh: HD-VGGT：高分辨率视觉几何 Transformer
authors: "Tianrun Chen, Yuanqi Hu, Yidong Han, Hanjie Xu, Deyi Ji, Qi Zhu, Chunan Yu, Xin Zhang, Cheng Chen, Chaotao Ding, Ying Zang, Xuanfu Li, Jin Ma, Lanyun Zhu"
date: 2026-03-28
pdf: "https://arxiv.org/pdf/2603.27222v1"
tags: ["query:ff3dr"]
score: 10.0
evidence: 用于高分辨率3D重建的前馈方法
tldr: 本研究针对高分辨率3D重建中Transformer计算开销大及视觉模糊区域导致特征不稳定的问题，提出了HD-VGGT。该模型采用双分支架构，通过低分辨率分支预测全局一致的粗略几何，高分辨率分支利用特征上采样模块细化细节。同时引入特征调制机制抑制不可靠特征，在不增加全分辨率计算负担的情况下，实现了高精度、鲁棒的3D重建，达到了SOTA水平。
selection_source: fresh_fetch
motivation: 现有的前馈3D重建模型在处理高分辨率图像时面临计算成本激增和视觉模糊区域特征不稳定的挑战。
method: 提出一种双分支架构，结合低分辨率全局几何预测与高分辨率细节细化，并引入特征调制机制以抑制不稳定特征。
result: 该方法在保持较低计算成本的同时，显著提升了高分辨率下的重建精度，并有效处理了弱纹理和重复模式等复杂场景。
conclusion: HD-VGGT证明了通过多尺度分支与特征调制，可以在有限资源下实现高效且鲁棒的高分辨率三维几何推理。
---

## 摘要
高分辨率图像对于精确的 3D 重建至关重要，因为许多几何细节仅在精细的空间尺度上显现。最近的前馈方法，如视觉几何基础 Transformer (VGGT)，已展示出在单次前向传递中从大量图像集合中推断场景几何的能力。然而，将这些模型扩展到高分辨率输入仍然具有挑战性：Transformer 架构中的 token 数量随图像分辨率和视图数量的增加而迅速增长，导致极高的计算和内存成本。此外，我们观察到视觉歧义区域（如重复图案、弱纹理或镜面反射表面）通常会产生不稳定的特征 token，从而降低几何推断的质量，尤其是在高分辨率下。我们提出了 HD-VGGT，这是一种用于高效且鲁棒的高分辨率 3D 重建的双分支架构。低分辨率分支预测粗糙且全局一致的几何形状，而高分辨率分支则通过学习到的特征上采样模块细化细节。为了处理不稳定的 token，我们提出了特征调制（Feature Modulation），它在 Transformer 的早期阶段抑制不可靠的特征。HD-VGGT 利用了高分辨率图像和监督，而无需承担全分辨率 Transformer 的成本，实现了最先进的重建质量。

## Abstract
High-resolution imagery is essential for accurate 3D reconstruction, as many geometric details only emerge at fine spatial scales. Recent feed-forward approaches, such as the Visual Geometry Grounded Transformer (VGGT), have demonstrated the ability to infer scene geometry from large collections of images in a single forward pass. However, scaling these models to high-resolution inputs remains challenging: the number of tokens in transformer architectures grows rapidly with both image resolution and the number of views, leading to prohibitive computational and memory costs. Moreover, we observe that visually ambiguous regions, such as repetitive patterns, weak textures, or specular surfaces, often produce unstable feature tokens that degrade geometric inference, especially at higher resolutions. We introduce HD-VGGT, a dual-branch architecture for efficient and robust high-resolution 3D reconstruction. A low-resolution branch predicts a coarse, globally consistent geometry, while a high-resolution branch refines details via a learned feature upsampling module. To handle unstable tokens, we propose Feature Modulation, which suppresses unreliable features early in the transformer. HD-VGGT leverages high-resolution images and supervision without full-resolution transformer costs, achieving state-of-the-art reconstruction quality.

---

## 论文详细总结（自动生成）

### HD-VGGT：高分辨率视觉几何 Transformer 论文总结

#### 1. 核心问题与研究背景
*   **研究动机**：精确的 3D 重建依赖高分辨率图像来捕捉几何细节。然而，现有的前馈（feed-forward）重建模型（如 VGGT）基于 Transformer 架构，其计算和内存开销随图像分辨率和视图数量呈**二次方增长**，导致处理高分辨率输入时成本极高。
*   **核心挑战**：除了计算瓶颈，高分辨率图像中的视觉歧义区域（如重复纹理、弱纹理、镜面反射）在 Transformer 早期层会产生不稳定的特征 token，这些噪声会随层传播，严重退化几何推断的准确性。

#### 2. 核心方法论
HD-VGGT 提出了一种层次化双分支架构，旨在平衡效率与精度：
*   **低分辨率分支（Low-Resolution Branch）**：使用标准的 VGGT 主干网络处理低分辨率图像，快速建立全局一致的粗略 3D 几何支架和相机位姿。
*   **高分辨率分支（High-Resolution Branch）**：
    *   **学习型特征上采样**：不使用简单的双线性插值，而是设计了一个引导式上采样模块，利用高分辨率原图作为引导，将粗略特征与高频视觉细节融合。
    *   **轻量级细化器**：使用较浅的 Transformer（如 6 层）对上采样后的特征进行局部细化，强制执行多视图一致性并回归高精度点图。
*   **特征调制机制（Feature Modulation, FM）**：
    *   **异常检测**：利用核化格拉姆矩阵（Gramian Statistics）分析特征流的二阶统计特性，识别跨视图不稳定的“几何奇点”。
    *   **信息门控抑制**：在 Transformer 的早期层通过掩码（Masking）手段抑制这些不稳定 token 的 Key 向量，防止噪声在深层网络中放大。

#### 3. 实验设计
*   **数据集与场景**：
    *   **通用重建与位姿**：CO3Dv2, RealEstate10K。
    *   **室内场景**：ScanNet, NYUv2, 7-Scenes, NRGBD。
    *   **物体与高分辨率基准**：DTU, ETH3D。
    *   **动态/挑战性环境**：Sintel, TUM-dynamics。
*   **对比方法（Baselines）**：包括原始 VGGT、DUSt3R、MASt3R、Fast3R、π3、WorldMirror、CUT3R 等十余种最先进的前馈重建模型。
*   **评估指标**：相机位姿 AUC、深度估计误差（Abs Rel, RMSE）、点云质量（Accuracy, Completeness, Chamfer distance）。

#### 4. 资源与算力
*   **训练设备**：使用了由 **16 台 Ascend 910C** 组成的集群（CloudMatrix384 超节点）。
*   **训练时长**：模型训练持续了 **2 周**。
*   **数据规模**：整合了 15 个公开数据集进行大规模预训练，涵盖室内外、虚幻合成及真实场景。

#### 5. 实验数量与充分性
*   **实验规模**：论文在位姿估计、点图重建、单目深度估计三个核心任务上进行了详尽测试，涉及超过 10 个主流基准数据集。
*   **消融实验**：专门设置了 `HD-VGGT (w/o FM)` 组，验证了特征调制机制对提升鲁棒性的具体贡献。
*   **客观性**：对比实验涵盖了从 2024 年到 2025 年最新的所有主流模型，且在不同分辨率和动态/静态环境下均有测试，实验设计全面且公平。

#### 6. 主要结论与发现
*   **性能领先**：HD-VGGT 在所有测试基准上均达到了 SOTA 水平。例如，在 ScanNet 深度估计中 AbsRel 降至 0.049，在 DTU 点云重建中精度显著提升。
*   **高分辨率优势**：通过双分支设计，模型成功捕捉到了低分辨率模型无法识别的细小结构（如灯杆、椅腿）。
*   **鲁棒性增强**：特征调制机制有效解决了弱纹理和镜面反射带来的几何噪声，使模型在无相机先验的情况下依然能保持稳定的重建效果。

#### 7. 优点
*   **架构高效**：巧妙避开了全分辨率 Transformer 的二次方复杂度，实现了“高分辨率监督、低分辨率计算”的平衡。
*   **细节还原度高**：引导式上采样模块能有效利用图像原始信息修复几何边缘。
*   **训练鲁棒**：引入统计学方法处理不稳定特征，增强了模型在复杂现实环境中的适用性。

#### 8. 不足与局限
*   **训练成本**：尽管推理高效，但大规模预训练仍需要极高的算力资源（16 张高性能加速卡训练 2 周）。
*   **实时性限制**：虽然是前馈模型，但在处理极多视图或超高分辨率时，双分支的内存占用仍可能成为移动端部署的障碍。
*   **动态场景处理**：虽然借鉴了动态场景的统计思路，但核心仍针对静态几何，对于剧烈运动物体的实时 4D 重建能力尚待进一步验证。

（完）
