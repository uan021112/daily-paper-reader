---
title: "Seen2Scene: Completing Realistic 3D Scenes with Visibility-Guided Flow"
title_zh: Seen2Scene：利用可见性引导流补全逼真的三维场景
authors: "Quan Meng, Yujin Chen, Lei Li, Matthias Nießner, Angela Dai"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28548v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 使用流匹配进行3D场景补全和生成
tldr: Seen2Scene是首个基于流匹配（Flow Matching）的3D场景补全与生成方法，直接利用不完整的真实世界扫描数据进行训练。通过引入可见性引导的流匹配技术，该方法能有效屏蔽真实扫描中的未知区域，并结合稀疏Transformer和TSDF表示法，实现了对复杂现实环境的高质量补全。其贡献在于摆脱了对合成数据的依赖，显著提升了真实场景生成的连贯性与真实感。
selection_source: fresh_fetch
motivation: 现有的3D场景补全方法大多依赖于完整的合成数据，难以处理真实世界扫描中存在的大量缺失和遮挡问题。
method: 提出可见性引导的流匹配技术，利用稀疏Transformer处理TSDF体积数据，并结合3D布局框等多种条件信号进行场景建模。
result: 实验证明，该方法在补全准确性和生成质量上均优于现有基准，能够生成连贯且真实的复杂3D场景。
conclusion: Seen2Scene通过直接从真实不完整数据中学习，为复杂现实环境的3D场景补全和生成提供了一种高效且灵活的解决方案。
---

## 摘要
我们提出了 Seen2Scene，这是首个基于流匹配（flow matching）的方法，直接在不完整的真实世界三维扫描数据上进行训练，用于场景补全与生成。与以往依赖完整（且通常是合成的）三维数据的方法不同，我们的方法引入了可见性引导的流匹配，它显式地掩蔽了真实扫描中的未知区域，从而能够从真实世界的局部观测中进行有效学习。我们使用编码在稀疏网格中的截断符号距离场（TSDF）体素来表示三维场景，并采用稀疏 Transformer 在掩蔽未知区域的同时高效地建模复杂的场景结构。我们采用三维布局框作为输入条件信号，且我们的方法可以灵活地适应文本或局部扫描等各种其他输入。通过直接从真实世界的不完整三维扫描中学习，Seen2Scene 能够为复杂、凌乱的真实环境实现逼真的三维场景补全。实验表明，我们的模型能够生成连贯、完整且逼真的三维场景，在补全准确度和生成质量方面均优于基准方法。

## Abstract
We present Seen2Scene, the first flow matching-based approach that trains directly on incomplete, real-world 3D scans for scene completion and generation. Unlike prior methods that rely on complete and hence synthetic 3D data, our approach introduces visibility-guided flow matching, which explicitly masks out unknown regions in real scans, enabling effective learning from real-world, partial observations. We represent 3D scenes using truncated signed distance field (TSDF) volumes encoded in sparse grids and employ a sparse transformer to efficiently model complex scene structures while masking unknown regions. We employ 3D layout boxes as an input conditioning signal, and our approach is flexibly adapted to various other inputs such as text or partial scans. By learning directly from real-world, incomplete 3D scans, Seen2Scene enables realistic 3D scene completion for complex, cluttered real environments. Experiments demonstrate that our model produces coherent, complete, and realistic 3D scenes, outperforming baselines in completion accuracy and generation quality.

---

## 论文详细总结（自动生成）

以下是对论文《Seen2Scene: Completing Realistic 3D Scenes with Visibility-Guided Flow》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的 3D 场景生成和补全模型高度依赖于**完整的合成数据集**（如 3D-FRONT），而真实世界的 3D 扫描（如 RGB-D 扫描）由于遮挡和传感器限制，本质上是**不完整且有噪声的**。这导致模型在处理真实复杂环境时存在巨大的领域鸿沟（Domain Gap）。
*   **研究动机**：开发一种能够直接从**不完整、真实的 3D 扫描数据**中学习的生成模型，使其能够想象缺失结构，同时尊重已观测到的几何形状，从而生成高保真、结构连贯的真实感场景。

### 2. 方法论
Seen2Scene 采用了一种基于**可见性引导流匹配（Visibility-Guided Flow Matching）**的架构，主要包含以下关键技术：
*   **场景表示**：使用截断符号距离场（TSDF）体素网格。TSDF 不仅编码表面，还通过特定值（如 -3 × 像素大小）自然地标记出相机未观测到的“未知区域”。
*   **掩蔽稀疏 VAE（Masked Sparse VAE）**：
    *   将 256³ 分辨率的 TSDF 块压缩为紧凑的几何潜变量（Latents）。
    *   **核心思想**：在训练编码器和解码器时，显式地掩蔽掉未知区域，仅对观测到的表面和空余空间进行重构损失计算，从而学习到干净的几何特征。
*   **可见性引导流匹配（Flow Matching）**：
    *   使用**稀疏 Transformer（Sparse DiT）**作为骨干网络，建模几何潜变量的分布。
    *   在训练流匹配模型时，同样排除未知区域的损失，使模型专注于学习真实观测到的几何分布。
*   **多模态条件引导**：
    *   **布局引导**：输入 3D 语义布局框（类别、位置、大小），通过 CLIP 编码语义标签。
    *   **扫描补全**：引入 **ControlNet** 分支，将更不完整的扫描作为条件注入预训练模型，引导其预测缺失部分。
    *   **文本引导**：利用 LLM 将文本描述转换为 3D 布局，再由模型生成几何。

### 3. 实验设计
*   **数据集**：
    *   **ScanNet++**：高保真真实世界室内场景（LiDAR 扫描）。
    *   **ARKitScenes**：移动端 LiDAR 采集的真实场景。
    *   **3D-FRONT**：合成室内场景（用于对比和模拟真实扫描）。
*   **Benchmark 与对比方法**：
    *   **场景补全**：对比了 SG-NN（稀疏生成网络）和 NKSR（神经核表面重建）。
    *   **场景生成**：对比了 BlockFusion、LT3SD 和 WorldGrow。
*   **评估指标**：倒角距离（CD）、L2 距离、多样性指标（TMD）、DINOv2-FID、Uni3D-FPD 以及基于 VLM（Qwen3-VL）的感知评分。

### 4. 资源与算力
*   **硬件**：使用了 **4 台 NVIDIA H100 GPU**。
*   **训练细节**：采用 BF16 混合精度训练，AdamW 优化器，学习率为 $10^{-4}$，Batch Size 为 64。
*   **时长**：文中未明确给出总训练小时数，但提到了使用了 1000 步预热（Warmup）和余弦退火策略。

### 5. 实验数量与充分性
*   **实验规模**：在三个大型数据集上进行了测试，评估样本量达到 4000 个。
*   **消融实验**：非常充分，涵盖了：
    1.  **掩蔽训练的影响**：证明了掩蔽未知区域对减少“空洞”和错误分布至关重要。
    2.  **真实数据的作用**：对比了仅用合成数据训练的效果，证明了真实数据对泛化能力提升显著。
    3.  **语义编码方式**：对比了 CLIP 嵌入与离散类别标签。
    4.  **标签增强**：验证了 LLM 生成同义词对处理长尾标签的有效性。
*   **客观性**：引入了基于视觉语言模型（VLM）的自动化感知评估，减少了人工评价的主观偏差。

### 6. 主要结论与发现
*   **直接学习真实数据更优**：直接在不完整的真实扫描上训练，比在完美合成数据上训练后再迁移的效果更好，能捕捉到更真实的表面细节和非轴向对齐的物体。
*   **可见性引导是关键**：通过在流匹配中掩蔽未知区域，模型能够有效地从局部观测中推断出合理的全局结构。
*   **灵活性**：该模型不仅能做补全，还能通过 ControlNet 和 LLM 接口实现高质量的文本到 3D 场景生成。

### 7. 优点
*   **首创性**：第一个直接在不完整真实 3D 扫描上训练流匹配模型的方法。
*   **鲁棒性**：能够处理凌乱、复杂且非对齐的真实室内环境。
*   **多功能**：统一了场景补全、布局生成和文本生成任务。
*   **评估创新**：利用 VLM 进行几何完整性和逻辑性的量化评分，具有前瞻性。

### 8. 不足与局限
*   **分辨率限制**：受限于 TSDF 体素表示和 VAE 的压缩倍率，极细微的几何细节可能会丢失。
*   **缺乏外观信息**：目前仅专注于几何生成，不包含纹理、材质或光照信息。
*   **计算开销**：虽然使用了稀疏卷积和 Transformer，但处理大规模场景（多房间融合）时仍需要分块（Tiled）处理，可能存在推理延迟。

（完）
