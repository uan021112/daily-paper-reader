---
title: "GaussianSSC: Triplane-Guided Directional Gaussian Fields for 3D Semantic Completion"
title_zh: GaussianSSC：用于三维语义场景补全的三平面引导有向高斯场
authors: "Ruiqi Xian, Jing Liang, He Yin, Xuewei Qi, Dinesh Manocha"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21487v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 单目图像的 3D 语义场景补全
tldr: GaussianSSC 是一种用于 3D 语义场景补全（SSC）的新框架。它通过引入“高斯锚定”和“高斯-三平面细化”模块，将高斯分布的优势融入体素网格中。该方法在不改变体素结构的前提下，利用各向异性的高斯场提升了体素与图像的对齐精度，并增强了对表面切向和遮挡的建模能力，在 SemanticKITTI 数据集上取得了显著的性能提升。
selection_source: fresh_fetch
motivation: 旨在解决现有 SSC 方法中体素与图像对齐不准以及缺乏对复杂几何表面和遮挡建模能力的问题。
method: 提出高斯锚定机制进行亚像素级图像特征聚合，并利用各向异性高斯场结合三平面表示进行局部与全局特征细化。
result: 在 SemanticKITTI 基准测试中，该方法在占用预测和语义补全指标上均超越了现有最先进模型。
conclusion: GaussianSSC 证明了将高斯场引入网格原生架构能有效提升 3D 场景补全的精度与效率。
---

## 摘要
我们提出了 GaussianSSC，这是一种两阶段、原生网格且由三平面引导的语义场景补全（SSC）方法，该方法在不替换体素网格或维护独立高斯集合的情况下，引入了高斯分布的优势。我们引入了“高斯锚定”（Gaussian Anchoring），这是一种在融合的 FPN 特征上进行的亚像素级、高斯加权图像聚合，它加强了体素与图像的对齐，并提升了单目占用估计的性能。我们进一步将点状体素特征转换为学习到的逐体素高斯场，并通过三平面对齐的“高斯-三平面细化”（Gaussian--Triplane Refinement）模块来优化三平面特征，该模块结合了局部收集（以目标为中心）和全局聚合（以源为中心）。这种有向的各向异性支持捕捉了表面切向、尺度以及感知遮挡的不对称性，同时保留了三平面表示的高效性。在 SemanticKITTI 数据集上，GaussianSSC 相比最先进的基准模型，在第一阶段占用估计中提升了 +1.0% 的召回率、+2.0% 的精确率和 +1.8% 的 IoU，在第二阶段语义预测中提升了 +1.8% 的 IoU 和 +0.8% 的 mIoU。

## Abstract
We present \emph{GaussianSSC}, a two-stage, grid-native and triplane-guided approach to semantic scene completion (SSC) that injects the benefits of Gaussians without replacing the voxel grid or maintaining a separate Gaussian set. We introduce \emph{Gaussian Anchoring}, a sub-pixel, Gaussian-weighted image aggregation over fused FPN features that tightens voxel--image alignment and improves monocular occupancy estimation. We further convert point-like voxel features into a learned per-voxel Gaussian field and refine triplane features via a triplane-aligned \emph{Gaussian--Triplane Refinement} module that combines \emph{local gathering} (target-centric) and \emph{global aggregation} (source-centric). This directional, anisotropic support captures surface tangency, scale, and occlusion-aware asymmetry while preserving the efficiency of triplane representations. On SemanticKITTI~\cite{behley2019semantickitti}, GaussianSSC improves Stage~1 occupancy by +1.0\% Recall, +2.0\% Precision, and +1.8\% IoU over state-of-the-art baselines, and improves Stage~2 semantic prediction by +1.8\% IoU and +0.8\% mIoU.

---

## 论文详细总结（自动生成）

这是一份关于论文《GaussianSSC: Triplane-Guided Directional Gaussian Fields for 3D Semantic Completion》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何从单目 RGB 图像中精确地恢复三维场景的几何结构和语义标签（即语义场景补全，SSC），特别是在处理遮挡区域和超出视野（out-of-FOV）区域时。
*   **研究动机**：
    *   **对齐误差**：单目 SSC 需要将 2D 图像特征提升到 3D 空间，由于深度不确定性，特征往往沿相机射线传播并扭曲几何形状。
    *   **网格局限性**：传统的体素或三平面（Triplane）方法在均匀晶格上处理特征，无法适应现实场景中各向异性的结构（如细长的道路、墙面）。
    *   **高斯分布的潜力**：3D 高斯分布具有灵活的尺度和方向适应性，能更好地建模几何不确定性，但直接使用纯高斯架构计算开销大且难以与现有的网格解码器集成。

### 2. 方法论
论文提出了 **GaussianSSC**，一个两阶段、原生网格且由三平面引导的框架，核心思想是将高斯表示的灵活性注入到高效的三平面/体素结构中。

*   **第一阶段：高斯锚定（Gaussian Anchoring）与占用预测**
    *   **核心思想**：不再简单地将体素中心投影到图像上的单个像素，而是学习一个亚像素级的 2D 高斯窗口。
    *   **技术细节**：在融合的 FPN 特征图上，为每个体素预测偏移量、尺度和不透明度，通过高斯加权聚合局部邻域特征。这增强了体素与图像的对齐鲁棒性，生成更准确的占用先验（Occupancy Prior）。
*   **第二阶段：高斯-三平面细化（Gaussian-Triplane Refinement）与语义预测**
    *   **核心思想**：利用学习到的 3D 高斯场来优化三平面特征。
    *   **关键技术**：
        1.  **局部收集（Local Gathering）**：以目标为中心，聚合高斯场内的邻域证据，减少量化伪影。
        2.  **全局聚合（Global Aggregation）**：以源为中心，将当前位置的特征分发给覆盖该点的所有高斯分布，增强长程一致性。
    *   **流程**：将体素嵌入转化为高斯参数，投影到三个正交平面进行细化，最后融合回体素空间进行语义分类。

### 3. 实验设计
*   **数据集**：使用自动驾驶领域权威的 **SemanticKITTI** 数据集。
*   **Benchmark**：在 256×256×32 的体素网格（0.2m 分辨率）上进行评估。
*   **对比方法**：
    *   **单目 SSC 基准**：MonoScene, TPVFormer, VoxFormer, OccFormer, MonoOcc, Symphonies, OctreeOcc, ET-Former。
    *   **评估指标**：占用预测的 IoU、精确率（Precision）、召回率（Recall）；语义预测的各类别 IoU 和平均 IoU（mIoU）。

### 4. 资源与算力
*   **硬件环境**：
    *   **训练与评估**：使用单张 **NVIDIA RTX A5000** GPU。
    *   **效率基准测试**：在 **RTX A6000** 上进行（为了与其他方法在统一硬件下对比）。
*   **算力细节**：论文提供了推理延迟（Latency）和显存峰值（Peak Memory）数据。GaussianSSC 的延迟约为 409.8ms，显存占用 2.8GB，在精度提升的同时保持了较好的效率平衡。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **定量对比**：在 SemanticKITTI 测试集和验证集上进行了全面对比。
    *   **消融实验**：针对“高斯锚定”、“负采样策略”、“细化模块的混合比例（$\beta$）”以及“两阶段耦合”分别做了 5 组以上的对比实验。
    *   **定性分析**：提供了多组可视化结果，对比了 VoxFormer 和 ET-Former 在复杂遮挡场景下的表现。
*   **充分性评价**：实验设计较为充分，涵盖了精度、效率和模块有效性验证。通过与多种 SOTA 方法在相同输入（MobileStereoNet 深度先验）下对比，保证了公平性。

### 6. 主要结论
*   **性能提升**：在第一阶段占用估计中，IoU 提升了 **1.8%**；在第二阶段语义预测中，IoU 提升了 **1.8%**，mIoU 提升了 **0.8%**。
*   **结构化优势**：高斯锚定显著提升了图像到体素的特征提升（Lifting）质量；高斯-三平面细化在处理建筑、车辆和植被等具有特定方向性的结构时表现优异。
*   **效率平衡**：虽然引入了高斯计算，但通过三平面投影而非全 3D 空间操作，成功将开销控制在可接受范围内。

### 7. 优点（亮点）
*   **混合架构创新**：巧妙地结合了高斯分布的各向异性表达能力和三平面的计算效率，无需维护复杂的独立高斯点集。
*   **亚像素对齐**：高斯锚定解决了传统方法中由于投影量化导致的特征错位问题。
*   **双向细化机制**：局部收集与全局聚合的结合，兼顾了边界的锐利度和全局语义的连贯性。

### 8. 不足与局限
*   **时序信息缺失**：目前仅基于单帧 RGB 输入，未利用视频流中的时序动态信息，这在处理动态物体时可能存在瓶颈。
*   **极端遮挡挑战**：在极度遮挡的区域，虽然模型尝试补全，但单目视觉固有的歧义性依然存在，可能导致过度补全或误判。
*   **计算开销微增**：相比于 ET-Former，延迟和显存略有增加（延迟增加约 50ms，显存增加 0.5GB），在极低功耗嵌入式设备上的部署仍需优化。

（完）
