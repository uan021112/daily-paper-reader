---
title: "SAP: Segment Any 4K Panorama"
title_zh: SAP：分割一切 4K 全景图
authors: "Lutao Jiang, Zidong Cao, Weikai Chen, Xu Zheng, Yuanhuiyi Lyu, Zhenyang Li, Zeyu HU, Yingda Yin, Keyang Luo, Runze Zhang, Kai Yan, Shengju Qian, Haidi Fan, Yifan Peng, Xin Wang, Hui Xiong, Ying-Cong Chen"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.12759v1"
tags: ["query:partseg"]
score: 8.0
evidence: 全景实例级分割基础模型
tldr: 针对全景图像在基础模型中表现不佳的问题，本文提出SAP模型，专门用于4K高分辨率全景实例分割。该方法创新性地将全景图重新表述为固定轨迹的透视视频分割，通过在球面遍历采样重叠补丁，既保持了4K原生分辨率，又实现了稳定的跨视图传播。结合利用InfiniGen合成的大规模数据集进行训练，SAP在真实世界4K全景基准测试中比SAM2提升了17.2 mIoU，展现了卓越的泛化能力。
selection_source: fresh_fetch
motivation: 现有的透视图像基础模型在处理360°全景图时性能显著下降，且难以直接高效处理4K高分辨率数据。
method: 将全景分割转化为固定轨迹的透视视频分割任务，通过球面遍历采样重叠补丁并利用大规模合成的4K全景数据进行训练。
result: 在真实世界4K全景基准测试中，SAP相比不同规模的SAM2实现了17.2的零样本mIoU提升。
conclusion: SAP通过轨迹对齐的范式有效解决了高分辨率全景分割难题，为具身智能和AR系统提供了强大的感知基础模型。
---

## 摘要
可提示的实例分割在具身智能和增强现实（AR）系统中被广泛采用，然而在透视图像上训练的基础模型在 360° 全景图上的性能往往会下降。在本文中，我们介绍了 Segment Any 4K Panorama (SAP)，这是一个用于 4K 高分辨率全景实例级分割的基础模型。我们将全景分割重新表述为固定轨迹的透视视频分割，将全景图分解为沿连续球面遍历采样的重叠透视补丁。这种内存对齐的重新表述在保留原生 4K 分辨率的同时，恢复了稳定跨视图传播所需的平滑视点过渡。为了实现大规模监督，我们使用 InfiniGen 引擎合成了 183,440 张带有实例分割标签的 4K 分辨率全景图像。在这种轨迹对齐范式下训练的 SAP 能够有效地泛化到真实世界的 360° 图像，在真实世界 4K 全景基准测试中，相比不同尺寸的原始 SAM2，实现了 +17.2 的零样本 mIoU 提升。

## Abstract
Promptable instance segmentation is widely adopted in embodied and AR systems, yet the performance of foundation models trained on perspective imagery often degrades on 360° panoramas. In this paper, we introduce Segment Any 4K Panorama (SAP), a foundation model for 4K high-resolution panoramic instance-level segmentation. We reformulate panoramic segmentation as fixed-trajectory perspective video segmentation, decomposing a panorama into overlapping perspective patches sampled along a continuous spherical traversal. This memory-aligned reformulation preserves native 4K resolution while restoring the smooth viewpoint transitions required for stable cross-view propagation. To enable large-scale supervision, we synthesize 183,440 4K-resolution panoramic images with instance segmentation labels using the InfiniGen engine. Trained under this trajectory-aligned paradigm, SAP generalizes effectively to real-world 360° images, achieving +17.2 zero-shot mIoU gain over vanilla SAM2 of different sizes on real-world 4K panorama benchmark.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《SAP: Segment Any 4K Panorama》** 的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：随着 360° 摄像头在机器人、AR/VR 及具身智能领域的普及，对高分辨率全景图像进行实例分割的需求日益增长。
*   **核心挑战**：
    1.  **几何畸变与接缝**：等距柱状投影（ERP）会导致严重的球面畸变和左右边界的不连续（接缝），导致模型生成的掩码破碎。
    2.  **超高分辨率处理**：现有的基础模型（如 SAM 系列）通常将输入调整为 1024x1024，这会丢失 4K 全景图中的精细细节。
    3.  **拓扑-内存失配**：直接将全景图切片输入视频分割模型（如 SAM2）会破坏物理视点的连续性，导致内存传播机制失效。
*   **整体含义**：本文提出了 **SAP**，通过将全景分割任务重新定义为“固定轨迹的透视视频分割”，解决了高分辨率全景图在基础模型中的适配难题。

### 2. 方法论
*   **核心思想**：不直接在畸变的 ERP 空间操作，而是模拟一个相机沿预设轨迹在球面上旋转扫描，将全景图转化为一段平滑过渡的透视伪视频，利用 SAM2 的时空记忆机制实现跨视图的实例一致性。
*   **关键技术细节**：
    1.  **全景转透视视频**：定义 $N$ 个模拟相机视点，通过坐标反向投影（ERP $\to$ 3D 射线 $\to$ 透视坐标）提取重叠的透视补丁。
    2.  **列优先锯齿形轨迹（Column-first Zigzag Traversal）**：设计了一种上下往复的扫描路径。相比行优先，该轨迹能实现无限循环且相邻帧间仅有一个维度的角度变化（偏航角或俯仰角），确保了视频流的平滑性。
    3.  **循环扩展（Cyclic Extension）**：为了支持从全景图任意位置开始提示（Prompt），将轨迹复制两遍形成闭环，确保覆盖所有视点。
    4.  **掩码融合**：将各帧预测的掩码重新投影回 ERP 平面，通过元素级取最大值（Element-wise Maximum）进行融合。
*   **算法流程**：输入 ERP 全景图与提示点 $\to$ 投影提示点至可见帧 $\to$ 生成透视视频流 $\to$ 输入微调后的 SAM2 $\to$ 掩码重投影与融合。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：使用 InfiniGen 引擎合成了 **183,440 张 4K 全景图**，包含约 640 万个实例标签，填补了大规模全景实例分割数据的空白。
    *   **测试集（零样本评估）**：
        *   **PAV-SOD**：真实世界 4K 全景视频抽帧（201 张图，427 个实例）。
        *   **HunyuanWorld 1.0**：生成式 8K 全景图（100 张图，264 个实例），用于测试跨域泛化性。
        *   **InfiniGen 验证集**：用于域内性能评估。
*   **对比方法**：以不同规模（Tiny, Small, Base, Large）的 **SAM2** 为基准，并对比了“SAM2 + 扫描策略（不微调）”的效果。
*   **评价指标**：1-click mIoU（单点提示）和 3-click Error Correction mIoU（三点纠错）。

### 4. 资源与算力
*   **数据合成**：使用了 **40,000 GPU 小时** 通过 InfiniGen 引擎生成合成数据集。
*   **模型训练**：
    *   基于 SAM2 的 Hiera-Large 编码器。
    *   冻结图像编码器，仅微调内存注意力、内存编码器、掩码解码器和提示编码器。
    *   **超参数**：Batch size 128，AdamW 优化器，学习率 $2 \times 10^{-4}$，采用余弦退火调度。
*   **文中未明确说明**：具体的 GPU 型号（如 A100 或 H100）及具体的训练总天数。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 Tiny 到 Large 四种模型尺寸，并在三个不同性质的数据集上进行了零样本测试。
*   **消融实验**：
    *   验证了**训练数据组合**（合成数据 vs. 原始数据混合）对缓解灾难性遗忘的作用。
    *   验证了**扫描策略**与**帧间重叠**的必要性（重叠显著提升了 mIoU）。
    *   对比了**透视变换**与直接在 ERP 上切片的优劣。
*   **客观性**：实验设计较为公平，通过引入第三方真实世界数据集（PAV-SOD）和生成式数据集（HunyuanWorld）证明了模型在合成数据训练后具有极强的零样本泛化能力。

### 6. 主要结论与发现
*   **性能飞跃**：SAP 在真实世界 4K 全景基准上，相比原始 SAM2 平均提升了 **17.2 mIoU**。在 Tiny 模型上提升甚至达到了 **24.2 mIoU**。
*   **解决极端区域问题**：在全景图的“接缝”和“极点”区域，SAP 的表现远超传统方法，证明了轨迹对齐策略的有效性。
*   **微调的必要性**：单纯使用扫描策略而不进行轨迹对齐微调（SAM2+Scan），在处理合成到真实的域迁移时效果有限，微调能显著增强内存机制的鲁棒性。

### 7. 优点
*   **架构巧妙**：将复杂的全景几何问题转化为成熟的视频分割问题，充分利用了 SAM2 的时空建模能力。
*   **高分辨率友好**：通过局部透视采样，绕过了 4K 图像直接输入导致的显存溢出（OOM）问题，同时保留了原生分辨率的细节。
*   **数据贡献**：开源了大规模高质量的 4K 全景实例分割合成数据集，对该领域具有重要价值。

### 8. 不足与局限
*   **推理开销**：由于需要将一张全景图转化为包含数十帧（如 24 帧或更多）的视频流，推理时间会显著长于直接处理单张图像。
*   **动态场景限制**：目前主要针对静态全景图的实例分割，虽然论文提到未来将扩展到全景视频，但当前版本尚未处理真实视频中的时序运动。
*   **依赖合成数据**：尽管泛化性良好，但模型核心知识来源于 InfiniGen 的合成环境，在某些极端复杂的真实光影或纹理下可能仍存在域差异。

（完）
