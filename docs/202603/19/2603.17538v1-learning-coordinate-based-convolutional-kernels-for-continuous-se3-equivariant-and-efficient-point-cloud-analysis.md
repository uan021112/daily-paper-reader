---
title: Learning Coordinate-based Convolutional Kernels for Continuous SE(3) Equivariant and Efficient Point Cloud Analysis
title_zh: 学习基于坐标的卷积核以实现连续 SE(3) 等变且高效的点云分析
authors: "Jaein Kim, Hee Bin Yoo, Dong-Sig Han, Byoung-Tak Zhang"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17538v1"
tags: ["query:partseg"]
score: 8.0
evidence: 针对大规模问题的SE(3)等变点云分析
tldr: 针对3D点云分析中SE(3)等变性与可扩展性难以兼得的问题，本文提出ECKConv。该方法利用双陪集空间定义卷积核域，并结合基于坐标的网络设计，实现了严格的连续SE(3)等变性。ECKConv在保持高效内存利用的同时，能处理大规模点云任务，在分类、配准及语义分割等多个基准测试中表现优异，显著提升了点云处理的鲁棒性与效率。
selection_source: fresh_fetch
motivation: 现有的群卷积方法在实现严格的SE(3)对称性与大规模问题的可扩展性之间存在难以调和的矛盾。
method: 提出ECKConv，通过在双陪集空间定义核域并利用坐标网络构建显式卷积核，实现高效且等变的特征提取。
result: 实验证明ECKConv在分类、姿态配准和大规模语义分割等任务上，其性能和内存效率均优于现有的等变方法。
conclusion: ECKConv成功解决了点云分析中等变性与效率的冲突，为大规模等变深度学习提供了强有力的技术支撑。
---

## 摘要
刚性运动的对称性是高效学习 3D 点云问题的关键因素之一。群卷积是提取等变特征的代表性方法，但其具体实现往往难以同时兼顾严格的对称性和可扩展性。我们主张利用交织器（intertwiner）框架来解决这一权衡，但以往的相关工作未能实现完全的 SE(3) 对称性或对大规模问题的可扩展性，因此需要更先进的卷积核架构。我们提出了等变基于坐标的卷积核卷积（Equivariant Coordinate-based Kernel Convolution），简称 ECKConv。它通过在双陪集空间（double coset space）中定义的卷积核域获得 SE(3) 等变性，其利用基于坐标的网络进行的显式卷积核设计增强了学习能力和内存效率。在分类、姿态配准、部分分割和大规模语义分割等多种点云任务上的实验验证了，与最先进的等变方法相比，ECKConv 具有刚性等变性、内存可扩展性和卓越的性能。

## Abstract
A symmetry on rigid motion is one of the salient factors in efficient learning of 3D point cloud problems. Group convolution has been a representative method to extract equivariant features, but its realizations have struggled to retain both rigorous symmetry and scalability simultaneously. We advocate utilizing the intertwiner framework to resolve this trade-off, but previous works on it, which did not achieve complete SE(3) symmetry or scalability to large-scale problems, necessitate a more advanced kernel architecture. We present Equivariant Coordinate-based Kernel Convolution, or ECKConv. It acquires SE(3) equivariance from the kernel domain defined in a double coset space, and its explicit kernel design using coordinate-based networks enhances its learning capability and memory efficiency. The experiments on diverse point cloud tasks, e.g., classification, pose registration, part segmentation, and large-scale semantic segmentation, validate the rigid equivariance, memory scalability, and outstanding performance of ECKConv compared to state-of-the-art equivariant methods.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **ECKConv**（等变基于坐标的卷积核卷积）的新型点云算子，旨在解决 3D 点云处理中“严格的连续 SE(3) 等变性”与“大规模问题可扩展性”之间的矛盾。

以下是对该论文的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：在 3D 点云学习中，如何让模型对刚性运动（旋转和平移，即 SE(3) 群）具有严格的对称性，同时保持处理大规模场景的高效性。
*   **研究背景**：
    *   传统的群卷积（Group Convolution）在严格等变性（如连续旋转）和计算效率之间存在权衡。
    *   现有的交织器（Intertwiner）框架虽然能处理连续群，但之前的实现要么仅限于 SO(3)（仅旋转），要么无法扩展到大规模点云任务（如 S3DIS 室内场景）。
    *   数据增强虽然能提高鲁棒性，但在效率和性能上通常不如架构内置的对称性。

### 2. 论文提出的方法论
*   **核心思想**：利用**双陪集空间（Double Coset Space）**来定义卷积核的定义域。通过将局部邻域点映射到 SE(3) 的双陪集参数上，确保卷积操作对任何连续的刚性变换保持等变。
*   **关键技术细节**：
    *   **双陪集参数化**：将 SE(3) 作用下的等变操作简化为对三个不变量参数 $[\bar{\beta}, \bar{r}, \bar{z}]$ 的处理，这些参数分别代表旋转角、径向距离和轴向距离。
    *   **显式坐标卷积核（Explicit Kernel）**：使用基于坐标的网络（Coordinate-based Networks）结合 **Gaussian 嵌入**来学习卷积核权重。
    *   **内存优化公式**：论文通过数学证明（Proposition 4.1），将卷积核计算重构为先对特征进行加权聚合再乘以基矩阵，将反向传播的计算复杂度从 $O(AKC_{in}C_{out})$ 降低到 $O(A(KC_{in} + C_{in}C_{out}))$，显著提升了内存效率。
    *   **S2 域增强**：如果输入没有法向量，提出了一种启发式的算法（Algorithm 1）从坐标中提取局部参考方向。

### 3. 实验设计
*   **数据集与场景**：
    *   **ModelNet40**：用于物体分类和姿态配准（Pose Registration）。
    *   **ShapeNet**：用于物体部分分割（Part Segmentation）。
    *   **S3DIS**：用于大规模室内场景语义分割（Area 5 测试）。
*   **Benchmark 与对比方法**：
    *   **非等变方法**：PointNet++、KPConv、DGCNN、PointTransformer v3 等。
    *   **模型无关等变方法**：Vector Neurons (VN)、Frame Averaging (FA) 等。
    *   **群卷积/等变方法**：TFN、SE(3)-Transformer、EPN、E2PN、CSEConv 等。
*   **测试协议**：采用了 I/I（平移训练/平移测试）、I/SO(3)（平移训练/旋转测试）等多种组合，验证模型在未见过的旋转下的泛化能力。

### 4. 资源与算力
*   **硬件环境**：所有实验均在**单张 NVIDIA RTX 3090 GPU** 上完成。
*   **训练细节**：
    *   分类任务：200 epochs，Adam 优化器，初始学习率 1e-4。
    *   语义分割（S3DIS）：80 epochs，处理 4096 个采样点构成的 4m² 区域。
    *   论文强调了其内存效率，例如在相同配置下，ECKConv 的内存占用远低于 CSEConv。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从物体级（分类、配准）到部件级（部分分割）再到场景级（语义分割）的全维度任务。
*   **消融实验**：针对锚点基数（A）、Gaussian 嵌入带宽、邻域数量等超参数进行了敏感性分析。
*   **客观性**：对比了多种最先进的（SOTA）等变和非等变模型，并在 S3DIS 这种极具挑战性的大规模数据集上验证了实用性。实验设计遵循了等变性研究的标准协议，结果具有说服力。

### 6. 主要结论与发现
*   **严格等变性**：ECKConv 在面对连续旋转时表现出极强的稳定性，分类精度在旋转测试下几乎不下降（ModelNet40 达到 91.37%）。
*   **可扩展性**：成功将等变卷积应用于 S3DIS 大规模场景，在旋转不变方法中达到了 SOTA 性能（mIoU 61.80）。
*   **姿态配准优势**：在 SE(3) 姿态配准任务中，ECKConv 的误差显著低于 E2PN 和 VN 等方法，证明了其对连续变换的精确建模能力。

### 7. 优点（亮点）
*   **数学与工程的结合**：将深奥的群论（双陪集空间）转化为高效的显式卷积核实现。
*   **内存效率**：通过 Proposition 4.1 的重构，解决了等变网络通常难以处理深层架构和大量点的痛点。
*   **通用性**：不依赖于特定的输入格式（如法向量），通过内置算法即可处理原始点云。

### 8. 不足与局限
*   **各向同性限制**：由于使用了双陪集元素且目前仅限于标量类型特征，卷积核在 SE(3) 动作下是各向同性的。这限制了它在需要方向表达能力的任务（如法向量估计、分子结构预测）中的表现。
*   **局部参考依赖**：虽然具有全局等变性，但其局部特征提取仍依赖于球形查询（Ball Query）的半径设定，对尺度变化的敏感性未深入探讨。
*   **计算开销**：尽管内存优化显著，但相比于最简单的 PointNet++，其基于坐标网络的核计算仍存在一定的额外计算耗时。

（完）
