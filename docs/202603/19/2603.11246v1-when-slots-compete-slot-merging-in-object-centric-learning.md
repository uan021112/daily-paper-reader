---
title: "When Slots Compete: Slot Merging in Object-Centric Learning"
title_zh: 当槽位竞争时：以对象为中心的学习中的槽位合并
authors: "Christos Chatzisavvas, Panagiotis Rigas, George Ioannakis, Vassilis Katsouros, Nikolaos Mitianoudis"
date: 2026-03-11
pdf: "https://arxiv.org/pdf/2603.11246v1"
tags: ["query:partseg"]
score: 6.0
evidence: 以对象为中心的学习和基于槽的实体表示
tldr: 本研究针对基于槽位（Slot）的对象中心学习中槽位数量固定导致多个槽位竞争同一实体区域的问题，提出了一种轻量级的“槽位合并”操作。该方法通过计算槽位注意力图之间的Soft-IoU得分来识别重叠，并利用重心更新合并选定槽位对，从而在训练过程中动态调整槽位。实验表明，该方法在不增加额外学习模块的情况下，显著提升了对象发现和分割的质量，超越了现有的自适应方法。
selection_source: fresh_fetch
motivation: 传统的对象中心学习使用固定数量的槽位，常导致多个槽位竞争重叠的同一实体区域，影响对象分解的准确性。
method: 提出一种基于Soft-IoU重叠得分和重心更新的槽位合并机制，在训练中动态合并重叠槽位且无需额外学习参数。
result: 在DINOSAUR框架下，该方法在对象发现和分割基准测试中显著提高了掩码质量和对象分解性能。
conclusion: 槽位合并是一种简单有效的即插即用操作，能有效解决槽位竞争问题并提升对象中心表示的质量。
---

## 摘要
基于槽位（Slot）的以对象为中心的学习将图像表示为一组潜在槽位，并使用解码器将它们组合成图像或特征。解码器规定了槽位如何组合成输出，但槽位集合通常是固定的：槽位数量是预先选定的，且槽位仅进行细化。这可能导致多个槽位竞争同一实体的重叠区域，而不是专注于不同的区域。我们引入了槽位合并（slot merging）：这是一种针对槽位集合的即插即用、轻量化操作，可在训练过程中合并重叠的槽位。我们通过槽位注意力图之间的 Soft-IoU 分数来量化重叠，并通过保持梯度流的重心更新（barycentric update）来组合选定的槽位对。合并遵循固定策略，决策阈值根据重叠统计数据推断得出，无需额外的可学习模块。该方法集成到 DINOSAUR 成熟的特征重建流水线中，提高了对象分解能力和掩码质量，在对象发现和分割基准测试中超越了其他自适应方法。

## Abstract
Slot-based object-centric learning represents an image as a set of latent slots with a decoder that combines them into an image or features. The decoder specifies how slots are combined into an output, but the slot set is typically fixed: the number of slots is chosen upfront and slots are only refined. This can lead to multiple slots competing for overlapping regions of the same entity rather than focusing on distinct regions. We introduce slot merging: a drop-in, lightweight operation on the slot set that merges overlapping slots during training. We quantify overlap with a Soft-IoU score between slot-attention maps and combine selected pairs via a barycentric update that preserves gradient flow. Merging follows a fixed policy, with the decision threshold inferred from overlap statistics, requiring no additional learnable modules. Integrated into the established feature-reconstruction pipeline of DINOSAUR, the proposed method improves object factorization and mask quality, surpassing other adaptive methods in object discovery and segmentation benchmarks.