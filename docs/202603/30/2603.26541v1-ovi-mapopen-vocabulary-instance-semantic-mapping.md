---
title: "OVI-MAP:Open-Vocabulary Instance-Semantic Mapping"
title_zh: OVI-MAP：开放词汇实例语义地图构建
authors: "Zilong Deng, Federico Tombari, Marc Pollefeys, Johanna Wald, Daniel Barath"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26541v1"
tags: ["query:partseg"]
score: 9.0
evidence: 开放词汇 3D 实例语义映射
tldr: 本研究针对自主智能体在复杂环境中的增量式开放词汇3D实例语义建图挑战，提出了OVI-MAP系统。该系统通过将实例重建与语义推理相解耦，利用RGB-D输入构建类别无关的3D实例地图，并仅从自动选择的关键视图中提取视觉语言模型特征。这种设计实现了稳定的实例跟踪和零样本语义标注，在保证实时性的同时，在标准基准测试上超越了现有最先进的开放词汇建图方法。
selection_source: fresh_fetch
motivation: 现有的3D建图方法受限于闭集假设或密集的逐像素语言融合，难以兼顾实时性、可扩展性和时间一致性。
method: 提出一种解耦架构，通过构建类别无关的3D实例图并结合视觉语言模型（VLM）从精选视图中提取语义特征。
result: 该系统在保持实时运行的同时，在多个标准基准测试中表现优于现有的开放词汇建图基准。
conclusion: OVI-MAP为在线探索中的稳定实例跟踪和灵活的零样本语义标注提供了一种高效且可扩展的解决方案。
---

## 摘要
增量式开放词汇 3D 实例语义地图构建对于在复杂日常环境中运行的自主智能体至关重要。然而，由于需要鲁棒的实例分割、实时处理以及灵活的开集推理，这仍然具有挑战性。现有方法通常依赖于闭集假设或密集的逐像素语言融合，这限制了可扩展性和时间一致性。我们提出了 OVI-MAP，将实例重建与语义推理相解耦。我们建议构建一个类别无关的 3D 实例地图，该地图由 RGB-D 输入增量构建，而语义特征仅通过视觉语言模型从一小部分自动选择的视图中提取。这种设计在整个在线探索过程中实现了稳定的实例跟踪和零样本语义标注。我们的系统能够实时运行，并在标准基准测试中优于最先进的开放词汇地图构建基准。

## Abstract
Incremental open-vocabulary 3D instance-semantic mapping is essential for autonomous agents operating in complex everyday environments. However, it remains challenging due to the need for robust instance segmentation, real-time processing, and flexible open-set reasoning. Existing methods often rely on the closed-set assumption or dense per-pixel language fusion, which limits scalability and temporal consistency. We introduce OVI-MAP that decouples instance reconstruction from semantic inference. We propose to build a class-agnostic 3D instance map that is incrementally constructed from RGB-D input, while semantic features are extracted only from a small set of automatically selected views using vision-language models. This design enables stable instance tracking and zero-shot semantic labeling throughout online exploration. Our system operates in real time and outperforms state-of-the-art open-vocabulary mapping baselines on standard benchmarks.

---

## 论文详细总结（自动生成）

### OVI-MAP: 开放词汇实例语义地图构建论文总结

#### 1. 核心问题与研究动机
*   **核心问题**：如何在复杂的室内环境中，利用流式 RGB-D 数据实时、增量地构建具有“开放词汇”理解能力的 3D 实例语义地图。
*   **研究动机**：
    *   **闭集限制**：传统方法依赖固定类别的标签，无法处理现实世界中无穷无尽的物体种类。
    *   **计算冗余**：现有的开放词汇方法往往采用逐像素的视觉语言模型（VLM）特征融合，导致内存开销巨大且处理速度慢。
    *   **一致性挑战**：在没有语义先验的情况下，在线进行稳定的 3D 实例跟踪和分割非常困难，容易产生碎片化。

#### 2. 方法论
OVI-MAP 的核心思想是**将实例重建与语义推理相解耦**。
*   **类别无关的实例地图重建**：
    *   **2D 实体分割与几何细化**：使用 CropFormer 生成类别无关的 2D 掩码，并结合深度图的几何不连续性进行细化，以减少物体间的粘连。
    *   **3D 提升与空间投票**：将 2D 掩码投影到 3D 空间，通过在 TSDF（截断符号距离场）体素网格中进行空间投票来关联现有实例或创建新实例。
    *   **增量融合**：利用加权融合更新体素的实例标签，确保时间上的稳定性。
*   **目标中心化的视图自适应语义聚合**：
    *   **视图选择机制**：为每个实例维护一个单位球面的视图覆盖图。只有当新视角提供了显著的“新观测面积”（超过阈值 $\theta_{novel}$）时，才触发 VLM 提取特征。
    *   **语义提取与融合**：对选定视图进行多尺度裁剪（原图裁剪和掩码裁剪），通过 SigLIP 模型提取特征，并根据可见像素数量进行加权平均，生成稳定的实例级嵌入向量。

#### 3. 实验设计
*   **数据集**：Replica（8个场景）和 ScanNet（18个场景，使用 ScanNet200 标签集）。
*   **Benchmark 与对比方法**：
    *   **在线方法**：OVO-SLAM, OpenFusion。
    *   **离线方法**：Mask3D, Segment3D, OpenScene, OpenMask3D, Semantic Gaussian 等。
*   **评估指标**：平均交并比 (mIoU)、平均准确率 (mAcc) 以及不同阈值下的实例级平均精度 (AP25, AP50, AP75)。
*   **实时性测试**：在 30 FPS 的约束下对比系统性能。

#### 4. 资源与算力
*   **硬件环境**：单张 **Nvidia RTX 3090 GPU** 和 **Intel Core i7-12700K CPU**。
*   **运行效率**：系统采用多线程并行架构（分割、关联、视图选择、特征提取各占独立线程）。
*   **处理速度**：通过跳帧处理（如每10帧处理一次语义）实现实时运行。具体组件耗时：RGB 分割约 964ms，深度分割约 88ms，特征提取约 131ms。

#### 5. 实验数量与充分性
*   **实验规模**：在两个主流数据集上进行了详尽测试，涵盖了语义分割和实例分割两个维度。
*   **消融实验**：
    *   对比了不同的**视图选择策略**（随机 vs. 像素计数 vs. 视图覆盖），证明了视图覆盖法能减少约 50% 的 VLM 调用。
    *   对比了不同的 **2D 分割器**（SAM2 vs. CropFormer）。
    *   对比了不同的 **VLM 后干网络**（CLIP, SigLIP 及其变体）。
    *   对比了不同的**特征融合方式**。
*   **评价**：实验设计客观、公平，通过与离线 SOTA 方法的对比，充分验证了在线解耦架构的优越性。

#### 6. 主要结论与发现
*   **解耦架构的有效性**：将几何实例与语义分离，不仅提高了系统的鲁棒性，还大幅降低了计算负担。
*   **性能领先**：在在线建图任务中，OVI-MAP 在 AP 指标上显著优于之前的 SOTA（如 OVO-SLAM），甚至在某些指标上超越了离线方法。
*   **高效性**：提出的视图覆盖策略在保持高精度的同时，极大地减少了冗余的 VLM 推理次数。

#### 7. 优点
*   **实时性与可扩展性**：避免了存储高维体素特征，内存占用低，适合移动机器人。
*   **高质量实例分割**：通过几何细化和空间投票，解决了在线建图中常见的物体过分割或欠分割问题。
*   **灵活的查询能力**：支持自然语言长句查询（如“哪里可以睡觉”），展现了强大的零样本泛化能力。

#### 8. 不足与局限
*   **依赖 2D 分割质量**：如果前端 2D 分割器（如 CropFormer）失败，3D 实例重建也会受影响，尤其是对于极小或视觉复杂的物体。
*   **背景偏差**：从裁剪图像中提取特征时，仍可能受到背景像素的干扰。
*   **VLM 局限性**：目前的视觉语言模型在视觉和文本空间的对齐仍存在弱点，可能导致某些模糊类别的误判。

（完）
