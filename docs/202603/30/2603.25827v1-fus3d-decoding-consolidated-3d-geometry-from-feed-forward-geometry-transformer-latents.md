---
title: "Fus3D: Decoding Consolidated 3D Geometry from Feed-forward Geometry Transformer Latents"
title_zh: "Fus3D: 从前馈几何 Transformer 潜变量中解码整合的 3D 几何"
authors: "Laura Fink, Linus Franke, George Kopanas, Marc Stamminger, Peter Hedman"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25827v1"
tags: ["query:ff3dr"]
score: 10.0
evidence: 从图像进行稠密SDF回归的前馈方法
tldr: Fus3D 是一种高效的前馈 3D 重建方法，能在 3 秒内从无序图像集中回归出稠密有符号距离场（SDF）。该方法利用预训练多视图几何 Transformer 的中间特征，通过学习到的体积提取机制将多视图信息整合进结构化体积潜空间，并由卷积解码器生成 SDF。它无需相机校准或后期融合，在稀疏和稠密视图下均能实现高质量的几何补全。
selection_source: fresh_fetch
motivation: 现有方法通常在每个视图预测后进行后期融合，导致丢失了 Transformer 中间特征中蕴含的完整几何信息并累积误差。
method: 通过交替的交叉和自注意力机制将多视图特征提取到体素化潜空间网格中，并使用卷积解码器映射为稠密 SDF。
result: 在无需相机校准的情况下，仅需不到 3 秒即可生成完整且几何合理的 3D 几何体，支持稀疏和稠密视图输入。
conclusion: 该研究证明了直接从几何 Transformer 的潜空间提取 3D 信息比传统的后期融合更有效，为快速 3D 重建提供了新思路。
---

## 摘要
我们提出了一种前馈方法，可在不到三秒的时间内从非结构化图像集合中回归出稠密有向距离场 (SDF)，且无需相机校准或事后融合。我们的核心见解是，预训练的多视图前馈几何 Transformer 的中间特征空间已经编码了强大的联合世界表示；然而，现有的流水线往往将其丢弃，在事后组装 3D 几何之前将特征通过逐视图预测头进行路由，这导致丢失了宝贵的完整性信息并累积了误差。相比之下，我们通过学习到的体积提取直接从几何 Transformer 特征中进行 3D 提取：体素化的规范嵌入通过交错的交叉注意力和自注意力，逐步将多视图几何信息吸收进结构化的体积潜变量网格中。随后，一个简单的卷积解码器将该网格映射为稠密 SDF。此外，我们还提出了一种可扩展的、有效性感知的监督方案，直接使用从深度图或 3D 资产导出的 SDF，从而解决了非水密网格等实际问题。我们的方法在稀疏和稠密视图设置下均能产生完整且定义良好的距离值，并展示了在几何上合理的补全效果。代码和更多资料可在 https://lorafib.github.io/fus3d 找到。

## Abstract
We propose a feed-forward method for dense Signed Distance Field (SDF) regression from unstructured image collections in less than three seconds, without camera calibration or post-hoc fusion. Our key insight is that the intermediate feature space of pretrained multi-view feed-forward geometry transformers already encodes a powerful joint world representation; yet, existing pipelines discard it, routing features through per-view prediction heads before assembling 3D geometry post-hoc, which discards valuable completeness information and accumulates inaccuracies.   We instead perform 3D extraction directly from geometry transformer features via learned volumetric extraction: voxelized canonical embeddings that progressively absorb multi-view geometry information through interleaved cross- and self-attention into a structured volumetric latent grid. A simple convolutional decoder then maps this grid to a dense SDF. We additionally propose a scalable, validity-aware supervision scheme directly using SDFs derived from depth maps or 3D assets, tackling practical issues like non-watertight meshes. Our approach yields complete and well-defined distance values across sparse- and dense-view settings and demonstrates geometrically plausible completions. Code and further material can be found at https://lorafib.github.io/fus3d.

---

## 论文详细总结（自动生成）

### Fus3D: 从前馈几何 Transformer 潜变量中解码整合的 3D 几何 —— 论文深度总结

#### 1. 核心问题与研究动机
*   **核心问题**：传统的从非结构化图像进行 3D 重建的方法（如 DUSt3R, VGGT）通常采用“先预测、后融合”的模式。即先生成每张图像的深度图或点云，再通过事后融合（如 TSDF 融合或泊松重建）来组装 3D 场景。
*   **研究动机**：
    *   **信息丢失**：Transformer 的中间特征空间已经包含了强大的联合多视图几何先验，但现有方法在进入 2D 预测头时丢弃了这些全局信息。
    *   **稀疏视图不完整**：在视图稀疏时，局部预测无法推断未观察到的区域，导致重建结果存在空洞。
    *   **误差累积**：在视图密集时，微小的逐视图预测不一致会随视图增加而累积，引入噪声并降低细节质量。
*   **整体含义**：Fus3D 旨在跳过逐视图预测和事后融合，直接从 Transformer 的潜空间回归出完整的 3D 有向距离场（SDF）。

#### 2. 方法论
*   **核心思想**：引入一个学习到的“体积提取模块”，将 2D 图像特征直接“提升”到 3D 结构化潜空间网格中。
*   **关键技术细节**：
    *   **骨干网络 (Backbone)**：采用预训练的 VGGT（多视图几何 Transformer），它能同时处理多个视图并建立联合特征表示。
    *   **3D 提取 Transformer (E)**：
        *   **规范嵌入 (Canonical Embedding)**：初始化一个 $16^3$ 的体素化学习嵌入，作为 3D 记忆的初始状态。
        *   **交错注意力机制**：通过“2D-to-3D 交叉注意力”吸收图像特征，再通过“3D 自注意力”在体积内传播几何信息。
    *   **3D 解码头 (H3D)**：使用简单的卷积上采样网络，将 $16^3$ 的潜变量网格解码为 $64^3$ 的稠密 SDF 网格。
    *   **有效性感知监督 (Validity-aware Supervision)**：
        *   针对非水密网格（Non-watertight meshes）和未观察区域，引入**有效性掩码 (Mval)** 和 **Eikonal 掩码 (Meik)**。
        *   在 Eikonal 违反严重的区域（如符号翻转处），自动切换到无向距离监督，提高训练鲁棒性。
*   **算法流程**：输入图像集 → VGGT 提取多尺度 2D 特征 → 提取 Transformer 逐步融合特征至 3D 网格 → 卷积解码器输出 SDF → 行进立方体算法（Marching Cubes）提取表面。

#### 3. 实验设计
*   **数据集**：
    *   **Objaverse**：约 1.7 万个合成 3D 对象，用于评估 360° 物体重建和视图缩放性能。
    *   **DTU**：真实场景数据集，用于评估前向场景下的泛化能力。
*   **对比方法 (Benchmarks)**：
    *   **直接 SDF 回归类**：VolRecon, UFORecon（在无已知相机参数的 feed-forward 设定下对比）。
    *   **先预测后融合类**：VGGT + TSDF 融合、VGGT + 泊松重建。
*   **评估指标**：Chamfer 距离 (CD)、F-score、SDF 平均绝对误差 (MAE) 等。

#### 4. 资源与算力
*   **硬件**：使用了多达 4 台 **NVIDIA A100 80GB GPU** 进行训练。
*   **训练细节**：采用了两阶段课程学习（低分辨率 SDF 预训练 + 高分辨率全损失微调）。
*   **推理速度**：在不到 **3 秒** 内完成整个 3D 重建过程，比需要相机校准和复杂融合的基准方法快约 40 倍。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 Objaverse 的 170 个测试场景上进行了详尽的视图数量（2 到 24 视图）缩放实验。
    *   在 DTU 数据集上区分了“有利”和“不利”视图组合进行鲁棒性测试。
    *   进行了消融实验，验证了 Eikonal 掩码、近表面监督项以及 3D 查询方式的有效性。
*   **充分性评价**：实验设计较为充分，涵盖了合成与真实、稀疏与密集视图等多种场景。通过 PCA 分析可视化了 3D 潜空间的语义结构，增强了结果的可解释性。对比实验在相同的骨干网络和微调策略下进行，确保了公平性。

#### 6. 主要结论与发现
*   **直接提取优于融合**：直接从潜空间提取 3D 几何能显著提升表面的完整性，尤其是在稀疏视图下。
*   **良好的缩放性**：随着视图增加，Fus3D 不会像 TSDF 融合那样累积噪声，反而能持续提升重建质量。
*   **几何先验强大**：模型展现出了强大的几何补全能力，能够推断出物体未被观察到的背面或内部结构。
*   **无需相机校准**：在完全不知道相机姿态的情况下，性能依然优于需要已知姿态的传统方法。

#### 7. 优点
*   **高效性**：端到端前馈推理，速度极快（<3s），适合实时应用。
*   **完整性**：利用 Transformer 的全局注意力，实现了优秀的几何补全效果。
*   **鲁棒性**：提出的有效性感知监督方案解决了大规模 3D 资产中常见的网格质量问题。
*   **简洁性**：无需复杂的投影几何计算或多阶段的后处理流水线。

#### 8. 不足与局限
*   **分辨率限制**：受限于稠密体积网格的内存开销，目前输出分辨率较低（$64^3$），导致精细纹理和尖锐边缘可能被平滑。
*   **场景范围限制**：目前主要针对以物体为中心的场景（Object-centric），对于大规模室内外场景的适用性有待验证。
*   **细节丢失**：相比于基于点云或高分辨率深度图融合的方法，在视图非常充足时，其精细细节的还原能力略逊一筹。

（完）
