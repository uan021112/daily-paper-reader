---
title: "GaussianGPT: Towards Autoregressive 3D Gaussian Scene Generation"
title_zh: GaussianGPT：迈向自回归 3D 高斯场景生成
authors: "Nicolas von Lützow, Barbara Rössle, Katharina Schmid, Matthias Nießner"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26661v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 自回归 3D 高斯场景生成
tldr: 本研究提出GaussianGPT，一种基于自回归Transformer的3D高斯场景生成模型。不同于主流的扩散模型，该方法通过预测下一个Token直接生成3D高斯原语。首先利用稀疏3D卷积自编码器将高斯原语压缩为离散潜变量网格，随后通过带有3D旋转位置编码的因果Transformer进行序列化建模。该模型支持场景补全、外扩及可控采样，为可控且感知上下文的3D生成提供了新范式。
selection_source: fresh_fetch
motivation: 旨在探索除扩散模型外的自回归替代方案，以实现更具组合归纳偏置和可控性的3D场景生成。
method: 结合稀疏3D卷积自编码器进行矢量量化压缩，并使用因果Transformer及3D旋转位置编码对离散Token进行序列化建模。
result: 成功实现了步进式的场景构建，支持场景补全、外扩以及通过温度调节的可控采样。
conclusion: 证明了自回归Transformer在处理显式3D表示时，是实现可控和上下文感知生成的有效补充范式。
---

## 摘要
3D 生成建模的最新进展大多依赖于扩散或流匹配公式。我们转而探索一种完全自回归的替代方案，并引入了 GaussianGPT，这是一个基于 Transformer 的模型，通过下一标记预测直接生成 3D 高斯，从而实现完整的 3D 场景生成。我们首先使用带有向量量化的稀疏 3D 卷积自编码器，将高斯基元压缩到离散潜空间网格中。生成的标记被序列化，并使用带有 3D 旋转位置嵌入的因果 Transformer 进行建模，从而实现空间结构和外观的顺序生成。与整体优化场景的基于扩散的方法不同，我们的方案逐步构建场景，自然地支持补全、外扩、通过温度进行的可控采样以及灵活的生成范围。该方案利用了自回归建模的组合归纳偏置和可扩展性，同时在与现代神经渲染管线兼容的显式表示上运行，将自回归 Transformer 定位为可控且上下文感知的 3D 生成的一种补充范式。

## Abstract
Most recent advances in 3D generative modeling rely on diffusion or flow-matching formulations. We instead explore a fully autoregressive alternative and introduce GaussianGPT, a transformer-based model that directly generates 3D Gaussians via next-token prediction, thus facilitating full 3D scene generation. We first compress Gaussian primitives into a discrete latent grid using a sparse 3D convolutional autoencoder with vector quantization. The resulting tokens are serialized and modeled using a causal transformer with 3D rotary positional embedding, enabling sequential generation of spatial structure and appearance. Unlike diffusion-based methods that refine scenes holistically, our formulation constructs scenes step-by-step, naturally supporting completion, outpainting, controllable sampling via temperature, and flexible generation horizons. This formulation leverages the compositional inductive biases and scalability of autoregressive modeling while operating on explicit representations compatible with modern neural rendering pipelines, positioning autoregressive transformers as a complementary paradigm for controllable and context-aware 3D generation.

---

## 论文详细总结（自动生成）

### GaussianGPT：迈向自回归 3D 高斯场景生成 —— 论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
当前的 3D 生成建模（如物体或场景合成）主要由**扩散模型（Diffusion）**或**流匹配（Flow-matching）**主导。这些方法通常采用全局去噪或细化的方式，虽然视觉效果出色，但在处理**增量构建、局部补全和灵活编辑**时显得不够自然。
论文的研究动机在于：现实中的 3D 环境往往是逐步构建、扩展和修改的。因此，作者提出将 3D 生成建模为一种**自回归（Autoregressive）过程**，模仿大语言模型（LLM）“预测下一个 Token”的模式，利用 Transformer 的组合归纳偏置来实现结构化、上下文感知的 3D 场景生成。

#### 2. 方法论：核心思想与关键技术
GaussianGPT 的核心思想是将连续的 3D 高斯原语（Gaussian Primitives）转化为离散序列，并使用因果 Transformer 进行建模。

*   **场景压缩与标记化（Tokenization）：**
    *   **稀疏 3D 特征网格：** 将 3D 高斯属性（位置、不透明度、尺寸、旋转、颜色）映射到空间体素网格中。
    *   **稀疏 3D 卷积自编码器：** 使用编码器 $E$ 将特征网格压缩为紧凑的潜变量表示，解码器 $D$ 负责重建。
    *   **无查找表量化（LFQ）：** 采用 LFQ 将潜变量离散化为二进制索引（Codebook Indices），从而将 3D 空间转化为可供 Transformer 处理的离散 Token。
*   **自回归建模：**
    *   **序列化策略：** 采用简单的 **xyz 遍历顺序** 将 3D 网格展平为 1D 序列。
    *   **词汇表设计：** 交替排列“位置 Token”（预测下一个被占用的体素）和“特征 Token”（预测该位置的高斯属性），实现几何与外观的解耦。
    *   **3D 旋转位置编码（3D RoPE）：** 为了弥补 1D 序列化丢失的空间局部性，在注意力机制中注入实际的 3D 坐标信息，使模型能感知空间邻近性。
*   **推理与生成：**
    *   支持从起始符（BOS）开始的**无条件生成**。
    *   支持给定部分场景作为前缀的**场景补全**。
    *   通过滑动窗口机制实现无限空间的**场景外扩（Outpainting）**。

#### 3. 实验设计
*   **数据集：**
    *   **PhotoShape：** 用于物体级（椅子）生成的定量评估。
    *   **3D-FRONT：** 高质量合成室内场景，用于生成和补全实验。
    *   **Aria Synthetic Environments (ASE)：** 大规模合成环境，用于长程外扩实验。
    *   **ScanNet++：** 真实世界数据，用于验证模型的泛化能力和微调效果。
*   **Benchmark 与对比方法：**
    *   **物体生成对比：** π-GAN, EG3D, DiffRF, L3DG。评估指标包括 FID、KID（外观）以及 COV、MMD（几何）。
    *   **场景生成对比：** 主要对比 L3DG（基于扩散模型的 3D 高斯生成）。

#### 4. 资源与算力
论文详细列出了训练所需的计算资源：
*   **自编码器训练：** 使用 4 张 **NVIDIA RTX A6000** GPU。场景训练约 4 天，物体训练约 2 天。
*   **Transformer 训练：** 使用 4 张 **NVIDIA GH200** GPU。场景训练约 1 天，物体训练约 4.5 小时。
*   **推理效率：** 在 GH200 上，生成一个 4m × 4m 的场景块约需 90 秒；生成一个 12m × 12m 的超大场景约需 6000 秒。

#### 5. 实验数量与充分性
*   **实验规模：** 涵盖了从单个物体到复杂室内场景，再到超大规模（12m × 12m）场景的生成。
*   **消融实验：** 专门针对 **3D 序列化策略** 做了对比，测试了 Z-order、Hilbert 曲线及 xyz 顺序对交叉熵损失的影响，证明了在有 3D RoPE 支持下，简单顺序即可达到最优效果。
*   **充分性评价：** 实验设计较为全面，不仅有合成数据的定量对比，还包含了真实数据的微调实验（附录 A），展示了模型在处理复杂现实环境时的潜力与局限。对比实验选择了当前最先进的扩散模型基准，具有较高的参考价值。

#### 6. 主要结论与发现
*   **自回归可行性：** 证明了基于 Token 的自回归 Transformer 在 3D 高斯生成任务上不仅可行，且在多个指标上优于现有的扩散模型。
*   **空间偏置的重要性：** 3D RoPE 是自回归模型处理 3D 数据的关键，它允许模型忽略序列化顺序的局限，直接学习空间依赖。
*   **多功能性：** 同一个模型可以自然地处理生成、补全和外扩任务，无需针对特定任务修改架构。

#### 7. 优点
*   **灵活性极高：** 相比扩散模型的全局细化，GaussianGPT 支持步进式构建，非常适合交互式设计和动态环境生成。
*   **显式表示兼容：** 直接生成 3D 高斯原语，与现代实时神经渲染管线无缝对接。
*   **长程生成能力：** 通过滑动窗口自回归，理论上可以生成无限大的连贯场景。

#### 8. 不足与局限
*   **重建精度限制：** 自编码器在处理真实世界数据（如 ScanNet++）时，高频细节（纹理、微小几何）存在丢失现象。
*   **生成速度：** 由于是逐 Token 生成，且为了保证质量可能需要多次重采样（Resampling），生成大规模场景的时间成本较高。
*   **累积误差：** 尽管实验显示外扩效果尚可，但长程自回归生成理论上仍存在误差累积导致场景漂移或结构崩溃的风险。

（完）
