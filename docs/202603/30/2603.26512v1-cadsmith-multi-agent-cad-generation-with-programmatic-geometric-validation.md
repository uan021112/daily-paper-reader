---
title: "CADSmith: Multi-Agent CAD Generation with Programmatic Geometric Validation"
title_zh: CADSmith：基于程序化几何验证的多智能体 CAD 生成
authors: "Jesse Barkley, Rumi Loghmani, Amir Barati Farimani"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.26512v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 具有程序化几何验证的文本到CAD生成
tldr: CADSmith是一个多智能体CAD生成框架，旨在解决现有文本转CAD方法缺乏几何验证或视觉反馈精度不足的问题。该系统通过生成CadQuery代码，并利用嵌套的纠错循环（内部修复执行错误，外部结合OpenCASCADE内核的精确测量与视觉语言模型评估）进行迭代优化。通过检索增强生成（RAG）保持API知识更新，CADSmith在复杂几何建模的准确性、执行率和形状一致性上显著优于零样本基准。
selection_source: fresh_fetch
motivation: 现有的文本转CAD方法缺乏精确的几何验证机制，难以处理尺寸误差和复杂的形状约束。
method: 提出一种多智能体流水线，通过嵌套的执行纠错和基于程序化几何验证（结合物理内核测量与VLM评价）的闭环反馈来优化CadQuery代码生成。
result: "在包含三个难度等级的基准测试中，CADSmith实现了100%的代码执行率，并显著提升了中值IoU（从0.8085至0.9629）并大幅降低了倒角距离。"
conclusion: 结合程序化几何反馈的闭环细化机制能有效提升大语言模型生成CAD模型的质量与可靠性。
---

## 摘要
现有的文本到 CAD 生成方法要么采用无几何验证的单次生成模式，要么依赖于无法解决尺寸误差的有损视觉反馈。我们提出了 CADSmith，这是一个从自然语言生成 CadQuery 代码的多智能体流水线。它通过两个嵌套的修正循环进行迭代优化：一个用于解决执行错误的内环，以及一个基于程序化几何验证的外环。外环结合了来自 OpenCASCADE 内核的精确测量数据（包围盒尺寸、体积、实体有效性）以及来自独立视觉语言模型裁判（Judge）的整体视觉评估。这提供了收敛到正确几何形状所需的数值精度和高层形状感知。该系统对 API 文档使用检索增强生成（RAG）而非微调，从而在底层 CAD 库演进时保持数据库的实时性。我们在包含三个难度等级（T1 到 T3）的 100 个提示词的自定义基准测试上进行了评估，并设置了三种消融配置。与零样本（zero-shot）基准相比，CADSmith 实现了 100% 的执行率（从 95% 提升），中值 F1 分数从 0.9707 提高到 0.9846，中值 IoU 从 0.8085 提高到 0.9629，并将平均倒角距离（Chamfer Distance）从 28.37 降低到 0.74。这表明，带有程序化几何反馈的闭环优化显著提高了大语言模型（LLM）生成的 CAD 模型的质量和可靠性。

## Abstract
Existing methods for text-to-CAD generation either operate in a single pass with no geometric verification or rely on lossy visual feedback that cannot resolve dimensional errors. We present CADSmith, a multi-agent pipeline that generates CadQuery code from natural language. It then undergoes an iterative refinement process through two nested correction loops: an inner loop that resolves execution errors and an outer loop grounded in programmatic geometric validation. The outer loop combines exact measurements from the OpenCASCADE kernel (bounding box dimensions, volume, solid validity) with holistic visual assessment from an independent vision-language model Judge. This provides both the numerical precision and the high-level shape awareness needed to converge on the correct geometry. The system uses retrieval-augmented generation over API documentation rather than fine-tuning, maintaining a current database as the underlying CAD library evolves. We evaluate on a custom benchmark of 100 prompts in three difficulty tiers (T1 through T3) with three ablation configurations. Against a zero-shot baseline, CADSmith achieves a 100% execution rate (up from 95%), improves the median F1 score from 0.9707 to 0.9846, the median IoU from 0.8085 to 0.9629, and reduces the mean Chamfer Distance from 28.37 to 0.74, demonstrating that closed-loop refinement with programmatic geometric feedback substantially improves the quality and reliability of LLM-generated CAD models.

---

## 论文详细总结（自动生成）

### CADSmith: 基于程序化几何验证的多智能体 CAD 生成论文总结

#### 1. 核心问题与整体含义（研究动机和背景）
计算机辅助设计（CAD）要求极高的尺寸精确度和几何有效性。现有的文本转 CAD（Text-to-CAD）方法存在两大缺陷：
*   **缺乏验证机制**：大多采用单次生成模式，容易产生 API 误用或尺寸幻觉。
*   **反馈信号模糊**：现有的闭环方法多依赖视觉反馈（如图像对比），无法识别毫米级的尺寸偏差或拓扑错误（如非流形实体）。
**CADSmith** 的提出旨在通过多智能体协作和“数值+视觉”双重验证，解决从自然语言到精确、可制造 CAD 模型的转化难题。

#### 2. 方法论：核心思想与技术细节
CADSmith 采用多智能体流水线架构，核心思想是**双层嵌套修正循环**：
*   **多智能体分工**：
    *   **Planner（规划者）**：将自然语言解析为包含尺寸、约束和组件分解的 JSON 设计规范。
    *   **Coder（编码者）**：利用 **RAG（检索增强生成）** 技术，从 CadQuery API 文档和错误解决方案库中检索上下文，生成 Python 代码。
    *   **Executor（执行者）**：在沙盒中运行代码，利用 **OpenCASCADE (OCCT)** 内核提取精确几何度量（体积、包围盒、实体有效性等）。
    *   **Validator（验证者）**：由更强大的模型（Claude Opus）担任“裁判”，结合内核数值数据和三视图渲染图进行综合评估。
    *   **Refiner（优化者）**：根据验证反馈（数值差异+视觉缺陷）迭代修改代码。
*   **双环修正流程**：
    *   **内环（执行修正）**：针对代码语法或 API 调用错误，匹配错误模式库进行快速修复（最多 3 次）。
    *   **外环（几何修正）**：针对几何形状不符或尺寸超差，结合 VLM 的视觉判断和内核的精确测量进行迭代（最多 5 次）。

#### 3. 实验设计
*   **数据集/Benchmark**：构建了一个包含 **100 个提示词** 的自定义基准测试，分为三个难度等级：
    *   **T1（基础原语）**：简单几何体（如圆柱、圆锥）。
    *   **T2（工程零件）**：涉及布尔运算和孔阵列（如法兰、齿轮）。
    *   **T3（复杂零件）**：涉及放样、扫掠、壳体等复杂操作（如无人机机架、联轴器）。
*   **对比方法**：
    1.  **Zero-shot Baseline**：单次生成，无规划、无 RAG、无反馈。
    2.  **No-vision Ablation**：全流程但移除视觉反馈（仅靠内核数值验证）。
    3.  **CADSmith Full**：完整的多智能体视觉+数值验证流程。
*   **评价指标**：倒角距离（Chamfer Distance）、F1 分数（阈值 1.0mm）、体积 IoU。所有指标均在绝对毫米空间计算，而非归一化空间，以体现尺寸精度。

#### 4. 资源与算力
*   **模型使用**：生成智能体使用 **Claude 3.5 Sonnet**，裁判智能体（Judge）使用 **Claude 3 Opus**。
*   **算力说明**：论文**未明确说明**具体的 GPU 硬件型号或训练时长。由于该系统基于现成的大语言模型 API 和 CPU 密集型的 OpenCASCADE 内核运算，其核心开销在于 API 调用成本和几何渲染，而非本地模型训练。

#### 5. 实验数量与充分性
*   **实验规模**：共进行了 100 组完整实验，涵盖了从简单到复杂的零件。
*   **充分性**：实验设计了清晰的难度分级（T1-T3）和消融实验（无视觉 vs 有视觉），能够明确区分各组件的贡献。
*   **客观性**：通过使用独立的、更高级的模型作为裁判，有效避免了 LLM 自我修正中的“自我确认偏差”。对比实验在相同的绝对空间度量下进行，保证了公平性。

#### 6. 主要结论与发现
*   **性能飞跃**：CADSmith 实现了 **100% 的代码执行成功率**（基准为 95%）。
*   **精度提升**：平均倒角距离从 28.37 骤降至 **0.74**；中值 IoU 从 0.8085 提升至 **0.9629**。
*   **视觉的重要性**：在复杂零件（T3）中，视觉反馈至关重要。移除视觉后，T3 的平均倒角距离增加了 35 倍，证明了单纯的数值指标无法捕捉全局形状的连贯性。
*   **RAG 的有效性**：通过 RAG 保持了对 CadQuery API 的最新支持，无需昂贵的微调过程。

#### 7. 优点
*   **高精度验证**：结合了物理内核的确定性（数值）和 VLM 的启发式（视觉），解决了 CAD 建模对尺寸极其敏感的问题。
*   **架构灵活**：多智能体解耦设计使得每个环节（如 Planner 或 Coder）都可以独立升级。
*   **实用性强**：生成的 CadQuery 代码具有参数化特性，比单纯生成网格（Mesh）更符合工程实际需求。

#### 8. 不足与局限
*   **视觉盲区**：固定的三视图渲染可能无法捕捉微小的几何间隙（如论文中提到的无人机机架臂部微小裂缝）。
*   **迭代成本**：多轮迭代和使用高级模型（Opus）作为裁判会增加推理成本和响应延迟。
*   **复杂装配体支持**：目前主要针对单体零件，尚未深入探讨多零件装配体（Assembly）的逻辑。
*   **依赖闭源模型**：系统性能高度依赖于 Anthropic 的闭源模型 API，开源模型的替代效果尚待验证。

（完）
