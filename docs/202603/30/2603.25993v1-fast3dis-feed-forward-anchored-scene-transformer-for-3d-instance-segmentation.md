---
title: "FAST3DIS: Feed-forward Anchored Scene Transformer for 3D Instance Segmentation"
title_zh: FAST3DIS：用于三维实例分割的前馈锚定场景 Transformer
authors: "Changyang Li, Xueqing Huang, Shin-Fang Chng, Huangying Zhan, Qingan Yan, Yi Xu"
date: 2026-03-27
pdf: "https://arxiv.org/pdf/2603.25993v1"
tags: ["query:ff3dr"]
score: 10.0
evidence: 用于场景理解的前馈3D重建模型
tldr: FAST3DIS 是一种针对 3D 实例分割的端到端前馈锚定场景 Transformer。针对传统“提升并聚类”方法不可微且扩展性差的问题，该方法通过在基础深度骨干网络上构建 3D 锚定查询架构，实现了视图一致的分割。它引入了 3D 锚点生成器和采样交叉注意力机制，并结合双层正则化策略，在保持高精度的同时显著提升了推理速度和内存效率。
selection_source: fresh_fetch
motivation: 传统的 3D 实例分割依赖非微分聚类，导致表示学习与分割目标脱节且难以扩展到多视图场景。
method: 提出一种基于 3D 锚定查询的 Transformer 架构，利用 3D 锚点生成器和跨视图采样机制实现端到端的实例分割。
result: 在复杂室内 3D 数据集上，该方法在保持竞争力的分割准确率的同时，显著优于现有聚类方法的推理速度和内存扩展性。
conclusion: FAST3DIS 证明了通过端到端查询机制替代后处理聚类，可以更高效、更一致地处理大规模 3D 场景理解任务。
---

## 摘要
虽然最近的前馈三维重建模型为场景理解提供了强大的几何基础，但将其扩展到三维实例分割通常依赖于脱节的“提升并聚类”（lift-and-cluster）范式。通过不可微聚类对密集像素级嵌入进行分组，其扩展性随视图数量增加而变差，且使表示学习与最终分割目标脱节。在本文中，我们提出了 FAST3DIS（Feed-forward Anchored Scene Transformer for 3D Instance Segmentation），这是一种端到端的方法，有效地绕过了事后聚类。我们引入了一种基于查询的 3D 锚定 Transformer 架构，该架构构建在基础深度骨干网络之上，在保留其零样本几何先验的同时，能够高效地学习特定实例的语义。我们设计了一个学习型 3D 锚点生成器，并结合锚点采样交叉注意力机制，以实现视图一致的三维实例分割。通过将 3D 物体查询直接投影到多视图特征图中，我们的方法能够高效地采样上下文。此外，我们引入了一种双层正则化策略，将多视图对比学习与动态调度的空间重叠惩罚相结合，以明确防止查询冲突并确保精确的实例边界。在复杂室内 3D 数据集上的实验表明，与最先进的基于聚类的方法相比，我们的方法在实现具有竞争力的分割准确率的同时，显著提高了内存可扩展性和推理速度。

## Abstract
While recent feed-forward 3D reconstruction models provide a strong geometric foundation for scene understanding, extending them to 3D instance segmentation typically relies on a disjointed "lift-and-cluster" paradigm. Grouping dense pixel-wise embeddings via non-differentiable clustering scales poorly with the number of views and disconnects representation learning from the final segmentation objective. In this paper, we present a Feed-forward Anchored Scene Transformer for 3D Instance Segmentation (FAST3DIS), an end-to-end approach that effectively bypasses post-hoc clustering. We introduce a 3D-anchored, query-based Transformer architecture built upon a foundational depth backbone, adapted efficiently to learn instance-specific semantics while retaining its zero-shot geometric priors. We formulate a learned 3D anchor generator coupled with an anchor-sampling cross-attention mechanism for view-consistent 3D instance segmentation. By projecting 3D object queries directly into multi-view feature maps, our method samples context efficiently. Furthermore, we introduce a dual-level regularization strategy, that couples multi-view contrastive learning with a dynamically scheduled spatial overlap penalty to explicitly prevent query collisions and ensure precise instance boundaries. Experiments on complex indoor 3D datasets demonstrate that our approach achieves competitive segmentation accuracy with significantly improved memory scalability and inference speed over state-of-the-art clustering-based methods.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《FAST3DIS: Feed-forward Anchored Scene Transformer for 3D Instance Segmentation》** 的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的前馈（Feed-forward）三维场景理解模型虽然在几何重建（如深度和位姿估计）上表现出色，但在**三维实例分割**任务上仍依赖于“先提取密集嵌入、后进行离线聚类”的脱节范式。
*   **研究动机**：
    *   **扩展性差**：传统的聚类算法（如 HDBSCAN）计算复杂度随视图数量呈平方级增长，且极其耗费内存，难以处理大规模或高分辨率场景。
    *   **非端到端**：聚类过程不可微，导致特征学习与最终的分割目标脱节。
    *   **缺乏一致性**：在无类别（Class-agnostic）设置下，仅靠空间距离难以区分物理上相邻的同类物体。
*   **整体含义**：FAST3DIS 旨在通过一种**端到端的、基于查询（Query-based）的 Transformer 架构**，直接从多视图图像中预测一致的三维实例掩码，彻底消除对事后聚类的需求。

### 2. 核心方法论
FAST3DIS 引入了 3D 锚定查询机制，将实例分割建模为连续 3D 空间中的直接集合预测问题。
*   **双路 LoRA 适配骨干网络**：采用预训练的 **Depth Anything V3 (DA3)** 作为冻结的几何基础，通过引入 **LoRA（低秩适配）** 模块提取实例相关的语义特征，同时保留原模型的零样本几何先验。
*   **3D 锚点生成器（3D Anchor Generator）**：根据全局场景上下文生成 $N_q$ 个可学习的 3D 锚点（代表物体中心）和内容查询。
*   **锚点采样交叉注意力（Anchor-Sampling Cross-Attention）**：
    *   **核心思想**：不进行全局密集匹配，而是将 3D 锚点投影到各个 2D 视图的特征图中，仅在投影点附近采样局部特征。
    *   **优势**：将交叉注意力的复杂度从 $O(HW \cdot S)$ 降低到 $O(S)$（$S$ 为视图数），实现了内存需求与空间分辨率的解耦。
*   **双层正则化策略**：
    *   **特征级**：使用多视图对比损失（Contrastive Loss）拉近同一实例的特征，推开不同实例。
    *   **空间级**：引入**动态调度的空间重叠惩罚（Dynamic Overlap Penalty）**。通过惩罚不同查询对同一物理区域的占用，强制查询之间进行空间竞争，从而划定清晰的实例边界。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：Aria Synthetic Environments（高度写实的室内模拟环境，抽取了约 40% 的场景）。
    *   **测试集（零样本泛化评估）**：ScanNet V2（真实室内场景）、ScanNet++（高分辨率激光扫描场景）、Replica（高质量合成场景）。
*   **Benchmark 与对比方法**：
    *   **聚类基准**：HDBSCAN、Felzenszwalb 等基于几何的聚类。
    *   **2D 提升方法**：SAM3D、Segment3D（利用 SAM 的 2D 能力投影到 3D）。
    *   **前馈 SOTA**：PanSt3R（全景分割）、**IGGT**（目前最先进的基于聚类的实例分割模型，作为主要对比对象）。

### 4. 资源与算力
*   **硬件环境**：使用单台计算节点，配置为 **32 核 AMD EPYC 9654 处理器** 和 **单张 NVIDIA H200 GPU (144GB VRAM)**。
*   **训练细节**：文中未明确给出具体的训练总时长，但提到在 Aria 数据集的大规模子集上进行训练，且 $N_q$ 设置为 80。
*   **推理效率**：在 30 个视图下，FAST3DIS 推理耗时 7.9 秒，比 IGGT（GPU 版 HDBSCAN 易 OOM，CPU 版需 1141 秒）快了 **115 倍**；在 50 视图下加速比达到 **250 倍**。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了三个主流的 3D 场景理解数据集，进行了定量 AP 指标对比、定性可视化对比、推理效率/内存扩展性分析。
*   **消融实验**：针对“特征/空间正则化的必要性”以及“动态权重调度与固定权重的对比”做了专门的消融研究。
*   **充分性评价**：实验设计较为充分，特别是在**扩展性分析**上，通过增加视图数量展示了模型在处理大规模场景时的结构化优势。对比实验考虑了不同范式（聚类 vs. 提升 vs. 端到端），具有较好的客观性和公平性。

### 6. 主要结论与发现
*   **端到端优于聚类**：通过 3D 锚点查询机制，可以在不损失精度的情况下，极大地提升推理速度并降低内存占用。
*   **空间正则化的关键性**：在无类别分割中，简单的二分匹配不足以区分相邻物体，动态重叠惩罚对于消除“查询劫持”（多个查询抢占同一物体）和模糊边界至关重要。
*   **几何先验的价值**：利用强大的深度估计模型（如 DA3）作为骨干，可以使模型在未见过的真实数据集上表现出极强的零样本泛化能力。

### 7. 优点（亮点）
*   **架构创新**：首次将 3D-to-2D 投影采样机制引入前馈 3D 实例分割，解决了多视图注意力机制的计算瓶颈。
*   **效率极高**：彻底解决了聚类算法在多视图场景下的 $O(N^2)$ 扩展性难题，支持处理数百张图像。
*   **训练策略巧妙**：动态调度重叠惩罚权重，平衡了训练初期的探索和后期的边界精细化。

### 8. 不足与局限
*   **查询数量限制**：模型设置了固定的 $N_q=80$，在极其拥挤的场景（如 ScanNet++，单场景可能超过 200 个物体）中会出现漏检，存在容量瓶颈。
*   **依赖几何质量**：分割精度高度依赖于冻结骨干网络生成的深度图和位姿质量，若底层几何重建失败，分割也会失效。
*   **类别不可知**：目前主要针对无类别实例分割，尚未深入探讨如何高效整合细粒度的语义标签。

（完）
