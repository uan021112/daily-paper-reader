---
title: "IRIS: Intersection-aware Ray-based Implicit Editable Scenes"
title_zh: IRIS：相交感知的基于射线的隐式可编辑场景
authors: "Grzegorz Wilczyński, Mikołaj Zieliński, Krzysztof Byrski, Joanna Waczyńska, Dominik Belter, Przemysław Spurek"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15368v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 用于交互式编辑的高效神经渲染与场景表示
tldr: IRIS 针对神经辐射场训练渲染慢以及混合方法中体积采样效率低的问题，提出了一种基于相交感知的射线隐式可编辑场景框架。该方法通过解析采样策略精确识别射线与基元的交点，消除了空域处理，并引入沿射线的连续特征聚合机制，绕过了昂贵的空间邻域搜索。IRIS 实现了高保真、实时的渲染效果，并支持灵活的场景形状编辑。
selection_source: fresh_fetch
motivation: 现有的将高斯作为代理引导神经场评估的方法仍受限于随机体积采样带来的计算低效和渲染性能瓶颈。
method: 采用解析采样策略精确确定射线与基元的交点，并结合沿射线的连续特征聚合机制来插值潜在属性。
result: 该方法有效消除了空域处理和昂贵的 3D 邻域搜索，在保证几何一致性的同时实现了高保真度的实时渲染。
conclusion: IRIS 为高效、交互式的场景编辑提供了一种兼顾渲染性能与形状编辑灵活性的新颖解决方案。
---

## 摘要
神经辐射场实现了高保真场景表示，但面临训练和渲染成本高昂的问题，而 3D 高斯泼溅（3D Gaussian Splatting）则凭借强大的实证结果提供了实时性能。最近，一些通过使用高斯函数作为代理来引导神经场评估以结合两者优势的解决方案，仍然存在显著的计算效率低下问题。它们通常依赖随机体采样来聚合特征，这严重限制了渲染性能。为了解决这一问题，本文引入了一个名为 IRIS（相交感知的基于射线的隐式可编辑场景）的新型框架，这是一种专为高效且交互式场景编辑而设计的方法。为了克服标准光线步进（ray marching）的局限性，本文采用了一种解析采样策略，能够精确识别射线与场景基元之间的交互点，从而有效地消除了对空白空间的处理。此外，为了解决空间邻域查找的计算瓶颈，本文引入了一种直接沿射线运行的连续特征聚合机制。通过对排序后的交点进行潜在属性插值，绕过了昂贵的 3D 搜索，确保了几何一致性，实现了高保真、实时渲染以及灵活的形状编辑。代码可在 https://github.com/gwilczynski95/iris 找到。

## Abstract
Neural Radiance Fields achieve high-fidelity scene representation but suffer from costly training and rendering, while 3D Gaussian splatting offers real-time performance with strong empirical results. Recently, solutions that harness the best of both worlds by using Gaussians as proxies to guide neural field evaluations, still suffer from significant computational inefficiencies. They typically rely on stochastic volumetric sampling to aggregate features, which severely limits rendering performance. To address this issue, a novel framework named IRIS (Intersection-aware Ray-based Implicit Editable Scenes) is introduced as a method designed for efficient and interactive scene editing. To overcome the limitations of standard ray marching, an analytical sampling strategy is employed that precisely identifies interaction points between rays and scene primitives, effectively eliminating empty space processing. Furthermore, to address the computational bottleneck of spatial neighbor lookups, a continuous feature aggregation mechanism is introduced that operates directly along the ray. By interpolating latent attributes from sorted intersections, costly 3D searches are bypassed, ensuring geometric consistency, enabling high-fidelity, real-time rendering, and flexible shape editing. Code can be found at https://github.com/gwilczynski95/iris.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **IRIS**（Intersection-aware Ray-based Implicit Editable Scenes）的新型神经渲染框架，旨在解决现有混合 3D 表示方法在渲染效率和场景可编辑性之间的矛盾。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 3D 场景表示方法中，NeRF（神经辐射场）虽能实现高保真度但渲染极慢；3DGS（3D 高斯泼溅）虽快且易编辑，但在处理复杂视图相关效果时略显不足。现有的“高斯引导神经场”的混合方法（如 EKS）虽然结合了两者的优点，但受限于**随机体积采样**和**昂贵的 3D 空间邻域搜索（KNN）**，导致渲染性能低下，难以支持实时交互和物理模拟。
*   **研究动机**：开发一种既能保持神经场的高保真连续性，又能利用显式几何（高斯）实现高效采样和灵活编辑的框架。

### 2. 方法论
IRIS 的核心思想是将显式的高斯几何作为“神经锚点（Neural Anchors）”，并通过解析几何手段优化采样过程：
*   **射线相交选择器（RIS）**：放弃了传统的启发式或随机采样，利用 OptiX API 解析地计算射线与高斯椭球体的精确交点。这消除了对空白空间的处理，只在有意义的几何位置生成样本。
*   **射线相干特征聚合（RCA）**：为了避免昂贵的 3D KNN 搜索，IRIS 沿射线方向使用“滑动窗口”机制进行 1D 特征聚合。它通过插值排序后的交点属性来合成局部连续的潜在向量。
*   **神经-显式几何解码**：使用浅层 MLP 将聚合后的特征解码为颜色和密度。最终密度由显式高斯的不透明度进行空间调制，确保神经场被约束在显式几何范围内。
*   **一致性编辑与物理集成**：由于特征被绑定在显式锚点上，场景可以通过更新高斯参数直接编辑（如变形、缩放），并能无缝集成到物理引擎（如 Blender）中进行动力学模拟。

### 3. 实验设计
*   **数据集**：
    *   **物体中心**：NeRF Synthetic。
    *   **大规模/无界场景**：Mip-NeRF 360、Tanks & Temples、Deep Blending。
    *   **挑战性室内场景**：HuSc3D（光照变化大）。
    *   **变形基准**：NeuralEditor 变形数据集。
*   **对比方法**：
    *   **静态方法**：Instant-NGP、Mip-NeRF 360、3DGS、Triangle Splatting。
    *   **可编辑方法**：Radiance Meshes、EKS、NeuralEditor、GaMeS。
*   **评估指标**：PSNR、SSIM、LPIPS（质量）；FPS（渲染速度）；Training Time（训练耗时）。

### 4. 资源与算力
*   **硬件**：所有实验均在单块 **NVIDIA RTX 5090 GPU** 上完成。
*   **训练时长**：在 NeRF Synthetic 数据集上，IRIS 的平均训练时间约为 **51 分钟**，远低于对比方法 EKS（约 26 小时）。
*   **渲染速度**：IRIS 达到了约 **6 FPS**，比 EKS（0.18 FPS）快了约 30 倍，虽然未达到 3DGS 的极致速度，但在神经场类方法中表现优异。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了从室内到室外、从静态重建到动态变形、从手动编辑到物理模拟的多种场景。
*   **消融实验**：针对采样预算（IPR）和特征初始化策略（是否使用 Hash Grid）进行了详细消融，验证了各组件的必要性。
*   **充分性评价**：实验设计非常全面，不仅对比了图像质量，还重点分析了采样器的扩展性（Scalability）和在物体移出初始边界框时的渲染稳定性。对比基准涵盖了最新的 SOTA 方法，实验结果客观且具有说服力。

### 6. 主要结论与发现
*   **效率飞跃**：通过 RIS 和 RCA 机制，IRIS 成功绕过了体积渲染中最耗时的步骤，实现了比同类可编辑神经场快一个数量级的渲染速度。
*   **高质量重建**：在室内场景（如 Mip-NeRF 360 室内部分）中，IRIS 的表现优于所有可编辑基线和显式静态方法。
*   **鲁棒的编辑性**：IRIS 能够处理非刚性变形和物理碰撞，且在物体移动到训练体积之外时仍能保持渲染完整性，解决了传统 NeRF 容易出现的“裁剪”问题。

### 7. 优点
*   **采样创新**：RIS 机制利用硬件加速（RT Cores）进行解析采样，极大地提高了计算效率。
*   **架构优雅**：RCA 将 3D 搜索简化为 1D 序列处理，设计巧妙且易于并行化。
*   **实用性强**：支持物理引擎集成，为电影制作、游戏开发等需要交互式编辑的领域提供了有力工具。

### 8. 不足与局限
*   **远景处理**：由于显式锚点的数量是有限的，在处理极大规模室外场景的远景几何时，精度可能受限。
*   **实时性差距**：虽然比 EKS 快很多，但 6 FPS 距离真正的 60 FPS 实时交互仍有差距，尤其是在处理超大规模点云时。
*   **依赖初始化**：性能在一定程度上依赖于初始点云或高斯的质量。

（完）
