---
title: "CustomTex: High-fidelity Indoor Scene Texturing via Multi-Reference Customization"
title_zh: CustomTex：通过多参考定制实现高保真室内场景纹理生成
authors: "Weilin Chen, Jiahao Rao, Wenhao Wang, Xinyang Li, Xuan Cheng, Liujuan Cao"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19121v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 高质量3D资产生成与场景纹理化
tldr: 针对室内场景纹理生成中控制精度低、质量差及光影残留等问题，本文提出 CustomTex 框架。该方法通过参考图像驱动，实现实例级的高保真纹理定制。其核心采用双重蒸馏策略：语义级蒸馏确保实例对齐，像素级蒸馏提升视觉保真度，并统一在 VSD 优化框架下。实验证明，CustomTex 在一致性、清晰度和减少伪影方面显著优于现有方法，为 3D 场景编辑提供了高效路径。
selection_source: fresh_fetch
motivation: 现有的文本驱动纹理生成方法缺乏精确的实例级控制，且生成的纹理往往存在质量低、伪影多及预烘焙光影严重等缺陷。
method: 提出一种双重蒸馏方法，通过实例交叉注意力实现语义对齐，并结合像素级蒸馏在变分分数蒸馏（VSD）框架下优化生成高分辨率纹理。
result: 实验结果显示，该方法能生成与参考图像高度一致的实例级纹理，且在清晰度和减少伪影方面表现优异。
conclusion: CustomTex 为高质量、可定制的 3D 室内场景外观编辑提供了一种更直接且用户友好的技术方案。
---

## 摘要
创建高保真、可定制的3D室内场景纹理仍然是一个重大挑战。虽然文本驱动的方法提供了灵活性，但它们缺乏细粒度、实例级控制的精度，且通常生成的纹理质量不足，存在伪影和预烘焙阴影。为了克服这些局限性，我们提出了 CustomTex，这是一个由参考图像驱动的实例级、高保真场景纹理生成新框架。CustomTex 输入一个无纹理的3D场景和一组指定每个物体实例所需外观的参考图像，并生成一个统一的高分辨率纹理贴图。我们方法的核心是一种双重蒸馏方法，它将语义控制与像素级增强分离开来。我们采用了配备实例交叉注意力的语义级蒸馏，以确保语义合理性和“参考-实例”对齐，并采用像素级蒸馏来增强视觉保真度。两者都统一在变分分数蒸馏（VSD）优化框架内。实验表明，与现有最先进的方法相比，CustomTex 实现了与参考图像精确的实例级一致性，并生成了具有更高清晰度、更少伪影和极少预烘焙阴影的纹理。我们的工作为高质量、可定制的3D场景外观编辑建立了一条更直接、更用户友好的路径。

## Abstract
The creation of high-fidelity, customizable 3D indoor scene textures remains a significant challenge. While text-driven methods offer flexibility, they lack the precision for fine-grained, instance-level control, and often produce textures with insufficient quality, artifacts, and baked-in shading. To overcome these limitations, we introduce CustomTex, a novel framework for instance-level, high-fidelity scene texturing driven by reference images. CustomTex takes an untextured 3D scene and a set of reference images specifying the desired appearance for each object instance, and generates a unified, high-resolution texture map. The core of our method is a dual-distillation approach that separates semantic control from pixel-level enhancement. We employ semantic-level distillation, equipped with an instance cross-attention, to ensure semantic plausibility and ``reference-instance'' alignment, and pixel-level distillation to enforce high visual fidelity. Both are unified within a Variational Score Distillation (VSD) optimization framework. Experiments demonstrate that CustomTex achieves precise instance-level consistency with reference images and produces textures with superior sharpness, reduced artifacts, and minimal baked-in shading compared to state-of-the-art methods. Our work establishes a more direct and user-friendly path to high-quality, customizable 3D scene appearance editing.

---

## 论文详细总结（自动生成）

以下是对论文《CustomTex: High-fidelity Indoor Scene Texturing via Multi-Reference Customization》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的3D室内场景纹理生成方法主要依赖文本驱动，存在**控制精度低**（文本难以描述细微纹理）、**质量差**（模糊、伪影多）以及**光影残留**（Baked-in shading，即纹理中自带了不可调节的阴影和高光）等问题。
*   **研究背景**：虽然图像驱动的方法能提供直观参考，但现有技术通常只能进行全局粗粒度控制，无法针对场景中不同的物体实例（如沙发、墙壁、柜子）分别指定精确的纹理。

### 2. 方法论
CustomTex 提出了一种基于**多参考图像驱动**的实例级纹理生成框架，核心思想是**双重蒸馏（Dual-distillation）**：
*   **语义级蒸馏（Semantic-level Distillation）**：
    *   采用**实例引导的变分分数蒸馏（InsVSD）**。
    *   利用**实例交叉注意力机制**：通过物体掩码（Instance Mask）将特定参考图像的特征与渲染图中的对应物体对齐，确保每个物体都能精准继承参考图的语义和风格。
    *   引入可训练的 **LoRA 模块**来学习参考图像的分布，从而在优化过程中提供更准确的梯度。
*   **像素级蒸馏（Pixel-level Distillation）**：
    *   集成预训练的**超分辨率（SR）扩散模型**。
    *   在优化过程中直接蒸馏 SR 模型的先验知识，用于增强纹理的清晰度和细节，解决传统扩散模型生成的纹理过于平滑或模糊的问题。
*   **纹理表示与优化**：
    *   在 UV 空间中使用**多分辨率哈希网格（Multi-resolution Hash Grid）**表示隐式纹理场。
    *   通过可微分渲染器将 3D 场景投影到 2D，在 VSD 框架下统一优化语义对齐和像素保真度。

### 3. 实验设计
*   **数据集/场景**：从 **3D-FRONT** 数据集中选取了 10 个具有代表性的室内场景（包括客厅、卧室、厨房、办公室等）。
*   **对比方法（Benchmark）**：
    *   **图像转纹理**：Paint3D, HY3D-2.1, SceneTex-IPA（作者改进的图像驱动版）。
    *   **文本转纹理**：TEXture, Paint3D, SyncMVD, HY3D-2.1, SceneTex。
*   **评估指标**：
    *   分布相似性：CLIP-Score (CLIP-I/T), CLIP-FID。
    *   视觉质量：Q-Align IQA（质量评估）, Q-Align IAA（美学评估）, Inception Score (IS)。
    *   主观评价：60 人参与的用户调研（视觉质量与提示词一致性）。

### 4. 资源与算力
*   **算力设备**：单张 **NVIDIA RTX A800 GPU**。
*   **训练时长**：每个场景的训练优化大约需要 **48 小时**（迭代 30,000 次）。
*   **推理效率**：生成 4K 分辨率纹理仅需约 **2.41 秒**，支持最高 12K 分辨率的快速推理。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了多种复杂的室内布局和材质需求。
*   **消融实验**：设计了四组消融实验（对比“后处理超分”、“无像素蒸馏”、“无特征掩码”、“单参考图输入”），验证了双重蒸馏和实例对齐机制的必要性。
*   **充分性评价**：实验设计较为全面，不仅有客观指标，还引入了最新的多模态大模型（Q-Align）进行自动评分，并结合了大规模用户调研，结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **实例级一致性**：CustomTex 能够精准地将不同参考图的纹理应用到指定的 3D 物体上，解决了多物体场景下的纹理混淆问题。
*   **高质量视觉效果**：通过像素级蒸馏，生成的纹理在清晰度和细节丰富度上显著优于 SOTA 方法。
*   **减少光影残留**：由于采用了局部实例引导而非全局生成，有效避免了扩散模型在纹理中引入全局性的“预烘焙”阴影，使纹理更适合后续的重照明渲染。

### 7. 优点
*   **高精度控制**：首次实现了基于多张参考图的室内场景实例级定制。
*   **双重蒸馏策略**：巧妙地将语义对齐与像素增强解耦，解决了生成质量与控制精度之间的矛盾。
*   **极高分辨率**：支持生成高达 12K 的纹理贴图，且推理速度极快。
*   **风格迁移能力**：实验展示了该方法在梵高风格、赛博朋克等夸张风格上的强大泛化能力。

### 8. 不足与局限
*   **训练成本高**：单个场景 48 小时的优化时间对于实时应用或大规模生产来说仍然较慢。
*   **材质属性单一**：目前主要生成**漫反射反照率（Diffuse Albedo）**，尚未涉及法线贴图（Normal）或粗糙度贴图（Roughness）等 PBR 材质属性的生成。
*   **依赖几何质量**：纹理质量在一定程度上受限于输入 3D 网格的 UV 展开质量。

（完）
