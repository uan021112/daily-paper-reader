---
title: Uncertainty-aware Prototype Learning with Variational Inference for Few-shot Point Cloud Segmentation
title_zh: 基于变分推理的不确定性感知原型学习，用于少样本点云分割
authors: "Yifei Zhao, Fanyu Zhao, Yinsheng Li"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19757v1"
tags: ["query:partseg"]
score: 9.0
evidence: 点云的少样本3D语义分割
tldr: 针对少样本3D点云语义分割中确定性原型难以捕捉稀疏监督带来的不确定性问题，本文提出了一种不确定性感知原型学习（UPL）框架。该方法通过双流原型细化模块整合支持集与查询集信息，并将原型学习建模为变分推理问题，将原型视为隐变量以显式建模不确定性。实验证明，UPL在ScanNet和S3DIS基准上达到了SOTA性能，并提供了可靠的不确定性估计。
selection_source: fresh_fetch
motivation: 现有的确定性原型表示无法捕捉少样本场景下因监督信息匮乏而产生的内在不确定性，限制了模型的鲁棒性和泛化能力。
method: 提出UPL框架，通过双流模块细化原型，并利用变分推理将原型建模为隐变量，从而实现显式的不确定性建模。
result: 在ScanNet和S3DIS等主流基准测试中，该方法在不同设置下均取得了最先进的性能，并能生成可靠的掩码预测。
conclusion: 通过将概率建模引入原型学习，本研究为提升少样本3D点云分割的鲁棒性和可解释性提供了一种有效的解决方案。
---

## 摘要
少样本3D语义分割旨在仅利用少量标注的支持样本，为查询点云生成准确的语义掩码。现有的基于原型的方法通常从支持集中构建紧凑且确定性的原型，以指导查询分割。然而，这种僵化的表示形式无法捕捉由稀疏监督引入的内在不确定性，这往往导致鲁棒性下降和泛化能力受限。在这项工作中，我们提出了 UPL（不确定性感知原型学习），这是一种旨在将不确定性建模融入少样本3D分割原型学习的概率方法。我们的框架引入了两个关键组件：首先，UPL 引入了一个双流原型细化模块，通过联合利用来自支持样本和查询样本的有限信息来丰富原型表示；其次，我们将原型学习建模为一个变分推理问题，将类别原型视为隐变量。这种概率公式化实现了显式的不确定性建模，提供了鲁棒且可解释的掩码预测。在广泛使用的 ScanNet 和 S3DIS 基准测试上的大量实验表明，我们的 UPL 在不同设置下均实现了一致的最先进性能，同时提供了可靠的不确定性估计。代码可在 https://fdueblab-upl.github.io/ 获取。

## Abstract
Few-shot 3D semantic segmentation aims to generate accurate semantic masks for query point clouds with only a few annotated support examples. Existing prototype-based methods typically construct compact and deterministic prototypes from the support set to guide query segmentation. However, such rigid representations are unable to capture the intrinsic uncertainty introduced by scarce supervision, which often results in degraded robustness and limited generalization. In this work, we propose UPL (Uncertainty-aware Prototype Learning), a probabilistic approach designed to incorporate uncertainty modeling into prototype learning for few-shot 3D segmentation. Our framework introduces two key components. First, UPL introduces a dual-stream prototype refinement module that enriches prototype representations by jointly leveraging limited information from both support and query samples. Second, we formulate prototype learning as a variational inference problem, regarding class prototypes as latent variables. This probabilistic formulation enables explicit uncertainty modeling, providing robust and interpretable mask predictions. Extensive experiments on the widely used ScanNet and S3DIS benchmarks show that our UPL achieves consistent state-of-the-art performance under different settings while providing reliable uncertainty estimation. The code is available at https://fdueblab-upl.github.io/.

---

## 论文详细总结（自动生成）

以下是对论文《Uncertainty-aware Prototype Learning with Variational Inference for Few-shot Point Cloud Segmentation》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的少样本3D点云语义分割（FS-PCS）方法主要依赖**确定性原型（Deterministic Prototypes）**。这种方法在监督信息极度匮乏的情况下，无法捕捉类内的多样性和类间的相似性，导致模型鲁棒性差，且无法提供预测的不确定性估计。
*   **研究背景**：3D点云标注成本极高，少样本学习成为必然选择。然而，少样本场景下的“监督稀疏性”天然带有不确定性，忽视这一特性会限制模型在处理复杂或模糊场景时的泛化能力。

### 2. 方法论
论文提出了 **UPL（不确定性感知原型学习）** 框架，核心包含两个模块：
*   **双流原型细化（DPR）模块**：
    *   **核心思想**：打破仅从支持集提取原型的局限，通过支持集与查询集的相互作用来增强原型的判别力。
    *   **技术细节**：利用通道注意力机制（Channel-wise Attention）在支持特征和查询特征之间进行信息交换，通过“值替换”操作生成更丰富的原型表示，并使用自适应融合门控整合原始特征与细化特征。
*   **变分原型推理正则化（VPIR）模块**：
    *   **核心思想**：将原型学习建模为变分推理问题，将类别原型视为服从高斯分布的**隐变量**。
    *   **算法流程**：
        1.  **先验分布**：基于支持集特征构建先验分布 $p(z)$。
        2.  **后验分布**：基于查询集特征构建后验分布 $q(z)$。
        3.  **训练阶段**：通过 KL 散度最小化先验与后验的差异，并利用重参数化技巧（Reparameterization Trick）进行采样。
        4.  **推理阶段**：通过多次蒙特卡洛（Monte Carlo）采样获取原型集合，生成集成预测。
*   **不确定性估计**：通过计算多次采样预测结果的方差（认知不确定性）和均值分布的熵，生成每点的预测不确定性图。

### 3. 实验设计
*   **数据集**：使用了两个主流的室内 3D 扫描数据集：**S3DIS** 和 **ScanNet**。
*   **Benchmark 设置**：遵循标准化的 CoSeg 基准，采用 N-way K-shot 设置（包括 1-way 1-shot, 1-way 5-shot, 2-way 1-shot, 2-way 5-shot）。
*   **对比方法**：对比了 AttMPTI、QGE、QPGA 和 CoSeg 等最先进的 FS-PCS 方法。

### 4. 资源与算力
*   **算力平台**：论文提到计算工作在复旦大学的 **CFFF 平台**上完成。
*   **具体细节**：文中**未明确说明**具体的 GPU 型号、数量以及训练所需的总时长。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个数据集上分别进行了 4 种 few-shot 设置的测试。
    *   每项实验均在两个随机种子（S0/S1）下运行并取平均值，确保了结果的客观性。
    *   **消融实验**：分别验证了 DPR 和 VPIR 模块的有效性，并探讨了采样次数 $T$ 对性能（mIoU）和校准误差（ECE）的影响。
*   **充分性评价**：实验设计较为全面，不仅有定量指标对比，还提供了定性的不确定性可视化分析，证明了模型在边界和模糊区域的判断能力。

### 6. 主要结论与发现
*   **性能领先**：UPL 在所有设置下均达到了 SOTA 性能。在 ScanNet 的 2-way 设置下，相比最强基准提升了约 3.6%~3.7% 的 mIoU。
*   **采样优势**：增加推理时的采样次数 $T$ 能显著提升性能并降低校准误差，证明了概率建模的集成效应。
*   **不确定性价值**：高不确定性区域与预测错误区域高度相关，这为模型提供了良好的可解释性，能有效识别遮挡或标签模糊的区域。

### 7. 优点
*   **首创性**：这是首个在少样本点云分割任务中引入显式不确定性建模的工作。
*   **理论严谨**：将原型学习转化为变分推理问题，为少样本学习提供了坚实的概率论基础。
*   **鲁棒性强**：通过双流细化和随机采样，有效缓解了类内差异大和类间相似度高的问题。

### 8. 不足与局限
*   **推理开销**：由于推理阶段需要进行多次蒙特卡洛采样（如 $T=5$），其计算耗时和内存占用会高于传统的确定性方法，论文未详细讨论推理延迟。
*   **超参数敏感性**：采样次数 $T$ 的最优值在不同数据集和设置下（1-shot vs 5-shot）存在波动，可能需要针对特定场景微调。
*   **场景局限**：实验主要集中在室内场景，尚未验证其在室外大规模场景（如自动驾驶点云）中的表现。

（完）
