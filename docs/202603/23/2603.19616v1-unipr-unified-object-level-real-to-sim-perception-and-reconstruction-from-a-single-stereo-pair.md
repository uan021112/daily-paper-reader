---
title: "UniPR: Unified Object-level Real-to-Sim Perception and Reconstruction from a Single Stereo Pair"
title_zh: UniPR：基于单对立体图像的统一物体级实转虚感知与重建
authors: "Chuanrui Zhang, Yingshuang Zou, ZhengXian Wu, Yonggen Ling, Yuxiao Yang, Ziwei Wang"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19616v1"
tags: ["query:ff3dr"]
score: 9.0
evidence: 基于立体图像对的端到端物体级感知与重建
tldr: UniPR是首个端到端的物体级Real-to-Sim感知与重建框架，旨在解决传统模块化流水线效率低和误差累积的问题。该方法仅需一对立体图像，利用几何约束解决尺度二义性，并通过姿态感知形状表示统一了重建与姿态估计任务。此外，研究者构建了包含6300多个物体的大规模立体数据集LVS6D。实验证明，UniPR能单次前向传播并行重建场景内所有物体，具有极高的效率和物理比例准确性。
selection_source: fresh_fetch
motivation: 传统的模块化感知流水线存在效率低下和各阶段误差累积的问题，且难以利用全局上下文信息。
method: 提出UniPR框架，通过立体图像对引入几何约束，并采用姿态感知形状表示来统一物体重建与姿态估计。
result: 在单次前向传播中实现了场景内所有物体的并行重建，并在保持物理比例和处理多样化物体方面表现出色。
conclusion: UniPR显著提升了Real-to-Sim任务的效率和准确性，为大规模机器人应用提供了强有力的技术支撑。
---

## 摘要
从图像中感知和重建物体对于在机器人领域广泛使用的实转虚（real-to-sim）迁移任务至关重要。现有方法依赖于检测、分割、形状重建和姿态估计等多个子模块来完成整个流程。然而，这种模块化流程存在效率低下和累积误差的问题，因为每个阶段仅处理部分或局部细化的信息，而丢弃了全局上下文。为了解决这些局限性，我们提出了 UniPR，这是首个端到端的物体级实转虚感知与重建框架。UniPR 直接作用于单对立体图像，利用几何约束来解决尺度歧义问题。我们引入了姿态感知形状表示（Pose-Aware Shape Representation），消除了对每个类别定义规范坐标系的需要，并弥合了重建与姿态估计任务之间的差距。此外，我们构建了一个包含超过 6,300 个物体的大词汇量立体数据集 LVS6D，以促进该领域的大规模研究。大量实验表明，UniPR 能够在单次前向传播中并行重建场景中的所有物体，实现了显著的效率提升，并在不同物体类型中保持了真实的物理比例，突显了其在实际机器人应用中的潜力。

## Abstract
Perceiving and reconstructing objects from images are critical for real-to-sim transfer tasks, which are widely used in the robotics community. Existing methods rely on multiple submodules such as detection, segmentation, shape reconstruction, and pose estimation to complete the pipeline. However, such modular pipelines suffer from inefficiency and cumulative error, as each stage operates on only partial or locally refined information while discarding global context. To address these limitations, we propose UniPR, the first end-to-end object-level real-to-sim perception and reconstruction framework. Operating directly on a single stereo image pair, UniPR leverages geometric constraints to resolve the scale ambiguity. We introduce Pose-Aware Shape Representation to eliminate the need for per-category canonical definitions and to bridge the gap between reconstruction and pose estimation tasks. Furthermore, we construct a large-vocabulary stereo dataset, LVS6D, comprising over 6,300 objects, to facilitate large-scale research in this area. Extensive experiments demonstrate that UniPR reconstructs all objects in a scene in parallel within a single forward pass, achieving significant efficiency gains and preserves true physical proportions across diverse object types, highlighting its potential for practical robotic applications.

---

## 论文详细总结（自动生成）

以下是对论文《UniPR: Unified Object-level Real-to-Sim Perception and Reconstruction from a Single Stereo Pair》的结构化深度总结：

### 1. 论文的核心问题与整体含义
*   **研究动机**：在机器人领域，将现实世界物体准确地迁移到仿真环境（Real-to-Sim）是基础任务。现有方法通常采用模块化流水线（检测→分割→形状重建→姿态估计），这导致了严重的**误差累积**、**计算效率低下**以及**全局上下文信息的丢失**。
*   **核心挑战**：单目重建存在尺度二义性，难以获得机器人操作所需的绝对物理尺寸；而类别级方法依赖预定义的规范空间（Canonical Space），难以扩展到大规模物体类别。
*   **整体含义**：UniPR 提出了首个**端到端**的物体级感知与重建框架，通过立体视觉（Stereo Pair）引入几何约束，实现了场景中多个物体的并行检测与物理尺度一致的形状重建。

### 2. 论文提出的方法论
*   **核心思想**：将感知（检测/定位）与重建（形状/姿态）统一在一个基于 Transformer 的端到端网络中，利用**姿态感知形状表示（PASR）**直接在观察空间中编码物体。
*   **关键技术细节**：
    *   **姿态感知形状表示 (PASR)**：不同于传统将物体归一化到规范空间的方法，PASR 直接学习带旋转的物体形状。通过预训练一个**姿态感知形状 VAE**，将物体表面点云压缩为紧凑的嵌入向量（Embedding）。
    *   **球形体素空间 (Spherical Voxel Space)**：在 VAE 中使用单位球而非立方体进行归一化，解决了物体旋转时超出边界导致的尺度歧义问题。
    *   **三平面编码器 (Triplane Encoder)**：利用 DINOv2 提取立体图像特征，通过交叉注意力机制将特征投影到 UVD 空间的三个正交平面上，聚合全局几何信息。
    *   **物体嵌入解码器**：借鉴 DETR 的设计，使用物体查询（Object Queries）从三平面特征中提取实例信息，并行预测物体的 3D 位置、物理缩放比例（Scale）和形状嵌入。
*   **算法流程**：输入一对立体图像 → 提取特征并构建三平面表示 → Transformer 解码器生成物体嵌入 → 预测头输出位置/尺度/形状向量 → 预训练的形状解码器恢复 3D 占用率（Occupancy）。

### 3. 实验设计
*   **数据集**：
    *   **LVS6D (新构建)**：包含 192 个类别、超过 6,300 个物体实例，生成了 40 万张训练图像。
    *   **公共数据集**：TOD（透明物体数据集）和 SS3D（立体 3D 数据集）。
*   **Benchmark 与对比方法**：
    *   **重建对比**：与 SOTA 生成模型 **Trellis** 和 **Hunyuan2.1** 对比（在提供完美掩码和姿态的前提下）。
    *   **感知对比**：与唯一的立体视觉类别级方法 **Coders** 对比。
*   **评估指标**：倒角距离 (CD)、F-Score、形状比例误差 (SPE)、平均精度 (AP)、平均位置误差 (APE) 等。

### 4. 资源与算力
*   **硬件环境**：训练使用了 **8 台 NVIDIA RTX 3090 GPU**。
*   **训练细节**：训练时长为 24 个 epoch，batch size 为 8。
*   **推理性能**：在单张 RTX 3090 上，UniPR 处理全场景（含多个物体）仅需约 0.63 秒，比传统流水线快 100 倍。

### 5. 实验数量与充分性
*   **实验规模**：论文在包含 192 个类别的超大规模数据集上进行了验证，并划分了简单、中等、困难三个子集。
*   **消融实验**：
    *   验证了 **PASR** 的必要性（移除后重建误差增加近 10 倍）。
    *   对比了**立体 vs 单目**（立体视觉在深度恢复上显著占优）。
    *   验证了**球形体素空间**对训练稳定性的提升。
*   **公平性与客观性**：在与生成模型对比时，即使给予基线模型“完美”的输入（GT Mask/Pose），UniPR 在形状比例准确性上依然领先，证明了其几何约束的有效性。此外，还进行了真实机器人抓取实验，验证了实用性。

### 6. 论文的主要结论与发现
*   **效率飞跃**：UniPR 实现了全场景并行处理，推理速度比模块化方法提升了两个数量级。
*   **物理一致性**：通过立体几何约束，UniPR 解决了生成模型常见的“形状比例失真”问题，形状比例准确度提升了 3 倍。
*   **泛化能力**：PASR 允许模型在不定义规范空间的情况下处理数千种未知物体，展现了极强的零样本泛化潜力。

### 7. 优点（亮点）
*   **端到端统一性**：消除了检测、分割等中间步骤，避免了误差在模块间的级联放大。
*   **摆脱规范空间限制**：PASR 的引入解决了类别级方法难以扩展到大词汇量的痛点。
*   **高效率并行化**：单次前向传播即可完成全场景重建，非常适合实时机器人应用。
*   **高质量数据集**：贡献了 LVS6D 数据集，填补了大词汇量立体视觉 6D 姿态估计的空白。

### 8. 不足与局限
*   **单目退化**：在单目设置下性能下降明显，高度依赖立体相机的基线信息。
*   **纹理缺失**：目前主要关注几何形状重建，不直接生成高分辨率纹理（虽然文中展示了结合外部模型上色的效果）。
*   **遮挡处理**：尽管比模块化方法好，但在极端遮挡或物体高度堆叠的复杂场景下，重建精度仍有提升空间。

（完）
