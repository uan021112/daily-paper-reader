---
title: "Points-to-3D: Structure-Aware 3D Generation with Point Cloud Priors"
title_zh: Points-to-3D：基于点云先验的结构感知 3D 生成
authors: "Jiatong Xia, Zicheng Duan, Anton van den Hengel, Lingqiao Liu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18782v2"
tags: ["query:3dgen"]
score: 10.0
evidence: 基于扩散模型的几何可控3D资产生成框架
tldr: Points-to-3D是一个基于扩散模型的3D生成框架，旨在利用点云先验（如LiDAR或预测点云）来增强几何可控性。该方法基于TRELLIS模型，通过将点云先验引入潜在空间初始化，并结合结构修复网络与分阶段采样策略，实现了在保留输入可见区域的同时补全全局几何。实验证明，该方法在物体和场景生成任务中均优于现有基准，显著提升了渲染质量和几何保真度。
selection_source: fresh_fetch
motivation: 现有的3D生成模型多依赖图像或文本，未能充分利用传感器或预测器提供的显式点云几何约束。
method: 在TRELLIS框架基础上，将稀疏结构潜变量初始化替换为点云先验，并利用结构修复网络和分阶段采样策略完成全局几何补全。
result: 在物体和场景生成实验中，该方法在渲染质量和几何保真度方面均显著优于当前最先进的基准模型。
conclusion: 通过显式嵌入点云先验，Points-to-3D实现了更精确且几何可控的3D资产与场景生成。
---

## 摘要
近期 3D 生成的进展主要由以图像或文本为条件的模型驱动，而现成的 3D 先验仍未得到充分利用。在许多现实场景中，可见区域的点云很容易从 LiDAR 等主动传感器或 VGGT 等前馈预测器中获得，它们提供了当前方法未能利用的显式几何约束。在这项工作中，我们介绍了 Points-to-3D，这是一个基于扩散的框架，利用点云先验进行几何可控的 3D 资产和场景生成。Points-to-3D 构建在潜空间 3D 扩散模型 TRELLIS 之上，首先将纯噪声的稀疏结构潜变量初始化替换为针对点云先验定制的输入公式。随后，使用在 TRELLIS 框架内针对旨在学习全局结构修复的任务特定数据训练的结构修复网络，配合阶段性采样策略（结构修复后进行边界细化）进行推理，在保留输入先验可见区域的同时补全全局几何。在实践中，Points-to-3D 可以接受精确的点云先验或从单张图像中由 VGGT 估计的点云作为输入。在物体和场景方案上的实验一致表明，在渲染质量和几何保真度方面，该方法优于最先进的基准模型，突显了显式嵌入点云先验对于实现更准确且结构可控的 3D 生成的有效性。

## Abstract
Recent progress in 3D generation has been driven largely by models conditioned on images or text, while readily available 3D priors are still underused. In many real-world scenarios, the visible-region point cloud are easy to obtain from active sensors such as LiDAR or from feed-forward predictors like VGGT, offering explicit geometric constraints that current methods fail to exploit. In this work, we introduce Points-to-3D, a diffusion-based framework that leverages point cloud priors for geometry-controllable 3D asset and scene generation. Built on a latent 3D diffusion model TRELLIS, Points-to-3D first replaces pure-noise sparse structure latent initialization with a point cloud priors tailored input formulation.A structure inpainting network, trained within the TRELLIS framework on task-specific data designed to learn global structural inpainting, is then used for inference with a staged sampling strategy (structural inpainting followed by boundary refinement), completing the global geometry while preserving the visible regions of the input priors. In practice, Points-to-3D can take either accurate point-cloud priors or VGGT-estimated point clouds from single images as input. Experiments on both objects and scene scenarios consistently demonstrate superior performance over state-of-the-art baselines in terms of rendering quality and geometric fidelity, highlighting the effectiveness of explicitly embedding point-cloud priors for achieving more accurate and structurally controllable 3D generation.

---

## 论文详细总结（自动生成）

### Points-to-3D: 基于点云先验的结构感知 3D 生成论文深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
现有的 3D 生成模型（如 TRELLIS、LGM 等）主要依赖 2D 图像或文本提示词作为条件。尽管这些模型能生成视觉上合理的 3D 资产，但由于缺乏显式的几何约束，其生成的结构往往无法精确遵循真实的 3D 测量数据。
*   **研究动机**：在现实应用中，通过 LiDAR 传感器或前馈预测器（如 VGGT）获取局部可见区域的点云非常容易。
*   **核心问题**：如何将这些易于获取的“点云先验”有效地整合进生成框架中，以实现几何可控、结构精确的 3D 生成，同时补全未观察到的区域。

#### 2. 方法论：核心思想与技术细节
Points-to-3D 基于 **TRELLIS** 框架，将其从“纯噪声初始化”改造为“先验引导的修复”模式。
*   **核心思想**：将可见区域的点云体素化并编码到稀疏结构潜空间（SS Latent），将其作为生成的起点而非随机噪声。
*   **关键技术细节**：
    1.  **输入公式化**：将可见点云 $P$ 编码为潜变量 $q_{vis}$，并结合一个可见性掩码 $m_s$。输入潜变量 $q_{comb}$ 由可见区域的特征和不可见区域的随机噪声组成。
    2.  **掩码感知修复网络 ($G_{inp}$)**：在 TRELLIS 的 Flow Transformer 基础上，通过增加输入通道来接收掩码信息，并使用条件流匹配（CFM）损失进行微调，使其学习如何根据已知几何推断缺失结构。
    3.  **两阶段采样策略**：
        *   **阶段 1：结构修复（Structural Inpainting）**：在前 $s$ 步中，利用掩码强制保持已知区域的几何。
        *   **阶段 2：边界细化（Boundary Refinement）**：在剩余步数中，移除掩码约束进行全局去噪，以消除已知区域与生成区域交界处的“空洞”或不连续现象。

#### 3. 实验设计
*   **数据集/场景**：
    *   **物体级**：Toys4K（500个物体）、ABO、3D-FUTURE。
    *   **场景级**：3D-FRONT（500个复杂室内场景）。
    *   **真实世界**：Pix3D 数据集。
*   **Benchmark 与对比方法**：
    *   对比了 **TRELLIS**（基准）、**GaussianAnything**、**Real3D**、**LGM**、**VoxHammer**（3D反演方法）、**SAM3D**、**MIDI**（场景生成）和 **SceneGen**。
    *   评估指标包括渲染质量（PSNR, SSIM, LPIPS, DINO）和几何保真度（Chamfer Distance, F-Score, 法线图质量）。

#### 4. 资源与算力
*   **硬件**：使用了 **4 张 Nvidia A100 GPU**。
*   **训练配置**：迭代次数为 **20k iterations**，Batch Size 为 8。
*   **推理配置**：总采样步数为 50 步（25步修复 + 25步细化）。

#### 5. 实验数量与充分性
*   **实验规模**：论文在物体级和场景级两个维度进行了大规模定量评估，并针对单视图、多视图、文本到 3D 等多种模态进行了测试。
*   **消融实验**：详细探讨了“修复步数”与“细化步数”的分配比例对最终几何质量的影响（见表4和图6）。
*   **客观性**：实验涵盖了“精确点云先验”和“VGGT 估计点云”两种实际场景，证明了模型在先验数据存在噪声时的鲁棒性。对比实验均采用了官方代码和推荐设置，具有较高的客观性和公平性。

#### 6. 主要结论与发现
*   **几何精度大幅提升**：在 Toys4K 数据集上，Points-to-3D 的 F-Score 达到了 **0.964**，远高于 TRELLIS 的 0.832。
*   **可见区域完美保留**：在点云覆盖的区域，F-Score 接近 **0.998**，证明了模型对硬约束的极强保持能力。
*   **场景生成优势明显**：在复杂的 3D-FRONT 场景中，显式几何先验显著降低了物体摆放和结构的错误率。
*   **两阶段采样的必要性**：单纯的修复会导致边界出现几何空洞，而引入细化阶段能显著提升整体一致性。

#### 7. 优点（亮点）
*   **强可控性**：首次在潜空间扩散模型中实现了对 3D 点云先验的直接、显式嵌入。
*   **灵活性**：既能处理传感器获取的高精度点云，也能处理从单图预测的模糊点云。
*   **高质量补全**：利用 TRELLIS 强大的先验知识，在保持已知结构的同时，生成的未知部分具有极高的语义一致性。
*   **即插即用潜力**：该方法为未来结合更先进的深度估计或点云预测模型提供了标准范式。

#### 8. 不足与局限
*   **依赖先验质量**：如果输入的点云（如 VGGT 预测的）存在严重偏差，生成的 3D 模型也会继承这些几何错误。
*   **体素化损失**：在将点云转换为 16x16x16 的稀疏结构潜变量时，不可避免地会丢失细微的几何细节。
*   **计算开销**：虽然是前馈生成，但相比于纯图像生成，增加了点云预处理和 VAE 编码的步骤。
*   **应用限制**：目前主要针对具有明确结构的物体和室内场景，对于极度稀疏或无序的户外大场景表现尚待验证。

（完）
