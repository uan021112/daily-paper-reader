---
title: "StreetForward: Perceiving Dynamic Street with Feedforward Causal Attention"
title_zh: StreetForward：利用前馈因果注意力感知动态街道
authors: "Zhongrui Yu, Zhao Wang, Yijia Xie, Yida Wang, Xueyang Zhang, Yifei Zhan, Kun Zhan"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.19552v1"
tags: ["query:ff3dr"]
score: 10.0
evidence: 动态街道重建的前馈框架
tldr: StreetForward是一个面向自动驾驶的动态街道重建框架，旨在解决传统方法中逐场景优化耗时的问题。该方法无需相机位姿和跟踪器，通过前馈因果注意力机制从图像序列中提取运动感知表征。它采用3D高斯泼溅（3DGS）统一表示静态背景和动态实例，并利用跨帧渲染实现时空一致性优化。实验表明，该模型在Waymo等数据集上的新视角合成和深度估计表现优异，且具备良好的泛化能力。
selection_source: fresh_fetch
motivation: 传统的动态场景重建依赖耗时的逐场景优化，难以高效利用大规模驾驶数据进行闭环仿真。
method: 提出一种基于时间掩码注意力的前馈框架，结合3D高斯泼溅统一建模动静态内容，实现无位姿、无跟踪的端到端推理。
result: 在Waymo数据集上取得了领先的新视角合成和深度估计性能，并在CARLA等数据集上展现了出色的零样本泛化能力。
conclusion: StreetForward通过高效的前馈机制实现了高质量的动态街道重建，为大规模自动驾驶仿真提供了有力支持。
---

## 摘要
前馈重建对于自动驾驶应用至关重要，快速场景重建能够实现在闭环仿真和其他下游任务中高效利用大规模驾驶数据集，从而消除了耗时的逐场景优化需求。我们提出了 StreetForward，这是一个用于动态街道重建的无需位姿且无需跟踪器的前馈框架。基于视觉几何基础 Transformer (VGGT) 的交替注意力机制，我们提出了一个简单而有效的时空掩码注意力模块，该模块从图像序列中捕捉动态运动信息，并生成具有运动感知能力的潜在表示。静态内容和动态实例统一使用 3D 高斯泼溅（3D Gaussian Splatting）表示，并通过具有时空一致性的跨帧渲染进行联合优化，使模型能够推断逐像素速度，并在新的位姿和时间点生成高保真新视角。我们在 Waymo 开放数据集上训练并评估了我们的模型，结果表明在视角合成和深度估计方面，其性能优于现有方法。此外，在 CARLA 和其他数据集上的零样本推理验证了我们方法的泛化能力。更多可视化结果可在我们的项目页面查看：https://streetforward.github.io。

## Abstract
Feedforward reconstruction is crucial for autonomous driving applications, where rapid scene reconstruction enables efficient utilization of large-scale driving datasets in closed-loop simulation and other downstream tasks, eliminating the need for time-consuming per-scene optimization. We present StreetForward, a pose-free and tracker-free feedforward framework for dynamic street reconstruction. Building upon the alternating attention mechanism from Visual Geometry Grounded Transformer (VGGT), we propose a simple yet effective temporal mask attention module that captures dynamic motion information from image sequences and produces motion-aware latent representations. Static content and dynamic instances are represented uniformly with 3D Gaussian Splatting, and are optimized jointly by cross-frame rendering with spatio-temporal consistency, allowing the model to infer per-pixel velocities and produce high-fidelity novel views at new poses and times. We train and evaluate our model on the Waymo Open Dataset, demonstrating superior performance on novel view synthesis and depth estimation compared to existing methods. Furthermore, zero-shot inference on CARLA and other datasets validates the generalization capability of our approach. More visualizations are available on our project page: https://streetforward.github.io.

---

## 论文详细总结（自动生成）

以下是对论文《StreetForward: Perceiving Dynamic Street with Feedforward Causal Attention》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的动态场景重建方法（如基于 NeRF 或 3DGS 的方法）通常依赖于耗时的“逐场景优化”（per-scene optimization），且往往需要预先提供相机位姿、物体跟踪信息或 LiDAR 数据。这限制了在大规模自动驾驶数据集上进行快速闭环仿真的效率。
*   **研究背景**：虽然现有的前馈（Feedforward）模型（如 VGGT、DUSt3R）可以快速重建静态场景，但在处理包含移动车辆和行人的动态街道时，存在几何退化和运动建模困难的问题。
*   **整体含义**：StreetForward 旨在建立一个**无需相机位姿、无需跟踪器、无需分割模型**的端到端前馈框架，通过一次前馈推理即可实现高保真的动态 4D 街道重建。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：在视觉几何基础 Transformer（VGGT）的基础上，引入因果掩码注意力机制来捕捉时间维度的运动特征，并利用 3D 高斯泼溅（3DGS）统一表示动静态内容。
*   **关键技术细节**：
    *   **因果掩码注意力（Causal Masked Attention）**：在 VGGT 的交替注意力（AA）层之后，添加了一个带有时间掩码的模块，显式建模“源帧→目标帧”的注意力流，从而增强特征的运动感知能力。
    *   **统一 3DGS 表示**：将场景分解为静态高斯和动态高斯。静态部分跨帧持久存在，动态部分则根据预测的逐像素速度（Velocity）进行时空传播。
    *   **速度解码与运动建模**：通过一个 DPT 风格的解码器预测前向和后向速度场，以及动态概率图（用于区分动静态区域）。
    *   **损失函数与正则化**：
        *   **局部刚性约束（Local Rigidity）**：在 2D 图像空间和 3D 空间中强制邻域点具有相似运动。
        *   **时空一致性（$L_{fb}$）**：利用前向/后向对称性约束，确保运动轨迹平滑。
        *   **不透明度稳定（$L_\alpha$）**：防止高斯点在优化过程中因几何错误而消失（Opacity Collapse）。

### 3. 实验设计
*   **数据集**：
    *   **Waymo Open Dataset**：主要训练和评估基准，包含 798 个训练场景和 202 个测试场景。
    *   **CARLA**：用于验证模型的**零样本（Zero-shot）泛化能力**，在 Waymo 上训练后直接在 CARLA 仿真数据上测试。
*   **Benchmark 与对比方法**：
    *   **前馈方法**：STORM、DGGT、AnySplat、VGGT、Pi3、DepthAnythingv3。
    *   **优化方法**：PVG（基于 3DGS 的动态重建）。
*   **评估指标**：渲染质量（PSNR、SSIM）和几何精度（LiDAR 投影深度 RMSE）。特别关注“仅动态区域”（Dynamic-only）的指标。

### 4. 资源与算力
*   **模型规模**：使用了 36 层交替注意力层（18 层帧内，18 层全局），因果注意力模块为 4 层。
*   **算力说明**：论文中**未明确列出**具体的 GPU 型号、数量及总训练时长。但提到使用了 DINOv2 作为冻结的编码器，并采用了分阶段训练策略（先训练几何，再激活运动头）。

### 5. 实验数量与充分性
*   **实验规模**：在 Waymo 202 个测试场景上进行了详尽评估，并在 CARLA 上进行了 100 个片段的跨域测试。
*   **消融实验**：针对因果注意力、局部刚性约束、后向速度、时空一致性损失和不透明度正则化等关键组件进行了 6 组对比实验。
*   **充分性评价**：实验设计较为充分，涵盖了新视角合成（插值与外推）和深度估计两个核心任务。通过对比“有无跟踪器”的方法，客观展示了该模型在减少外部依赖方面的优势。

### 6. 主要结论与发现
*   **性能领先**：StreetForward 在 Waymo 动态区域的 PSNR/SSIM 以及全图深度估计 RMSE 上均达到了 SOTA 水平。
*   **运动感知**：因果掩码注意力能有效区分静止车辆（如路边停放车辆）和移动车辆，避免了误报运动。
*   **泛化性强**：在无需任何微调的情况下，模型在 CARLA 数据集上的表现优于现有的前馈基准，证明了其学习到了通用的几何与运动先验。

### 7. 优点
*   **高效简洁**：实现了真正的“Pose-free”和“Tracker-free”，极大简化了自动驾驶场景重建的流水线。
*   **统一建模**：将动静态物体统一在 3DGS 框架下，通过速度场实现平滑的时间插值和外推。
*   **鲁棒性**：相比依赖外部跟踪器（如 DGGT）的方法，StreetForward 在跟踪失败或遮挡情况下表现更稳定。

### 8. 不足与局限
*   **计算开销**：虽然是前馈模型，但 36 层 Transformer 加上 3DGS 渲染在实时性方面可能仍有挑战（文中未给出具体推理延迟）。
*   **运动假设**：模型在短时间内假设恒定速度，对于急转弯或剧烈变速的极端动态场景，预测精度可能会下降。
*   **长程依赖**：目前的因果窗口较短（±1 到 ±4 帧），对于长序列的连贯性维护能力尚待进一步验证。

（完）
