---
title: "LagerNVS: Latent Geometry for Fully Neural Real-time Novel View Synthesis"
title_zh: LagerNVS：用于全神经实时新视角合成的潜几何
authors: "Stanislaw Szymanowicz, Minghao Chen, Jianyuan Wang, Christian Rupprecht, Andrea Vedaldi"
date: 2026-03-20
pdf: "https://arxiv.org/pdf/2603.20176v1"
tags: ["query:ff3dr"]
score: 9.0
evidence: 具有3D归纳偏置的确定性前馈新视角合成
tldr: LagerNVS 是一种用于实时新视角合成（NVS）的编码器-解码器网络。它通过引入“3D 感知”潜空间几何特征，在无需显式 3D 重建的情况下利用 3D 归纳偏置。该模型利用预训练的 3D 重建网络初始化编码器，并配合轻量级解码器进行端到端训练。它在 Re10k 等数据集上达到了 SOTA 性能，支持实时渲染，并能泛化至野外数据或结合扩散模型进行生成式外推。
selection_source: fresh_fetch
motivation: 尽管神经网络可以在没有显式 3D 重建的情况下执行 NVS，但引入强 3D 归纳偏置仍能显著提升模型性能。
method: 采用基于 3D 感知潜特征的编码器-解码器架构，利用预训练 3D 重建网络初始化编码器，并通过光度损失进行端到端训练。
result: 在 Re10k 数据集上达到 31.4 PSNR 的 SOTA 确定性前馈合成效果，支持实时渲染并具有良好的泛化能力。
conclusion: LagerNVS 证明了结合 3D 几何先验的神经表示在高效、高质量的新视角合成任务中具有极大的潜力。
---

## 摘要
最近的研究表明，神经网络可以在没有显式三维重建的情况下执行三维任务，例如新视角合成（NVS）。尽管如此，我们认为在设计此类网络时，强大的三维归纳偏置仍然是有益的。我们通过引入 LagerNVS 来证明这一点，这是一种基于“三维感知”潜特征的新视角合成编码器-解码器神经网络。该编码器初始化自一个使用显式三维监督预训练的三维重建网络。它与一个轻量级解码器配对，并使用光度损失进行端到端训练。LagerNVS 实现了最先进的确定性前馈新视角合成（包括在 Re10k 数据集上达到 31.4 PSNR），无论是否已知相机参数，均可实时渲染，能够泛化到野外数据，并可与扩散解码器配对进行生成式外推。

## Abstract
Recent work has shown that neural networks can perform 3D tasks such as Novel View Synthesis (NVS) without explicit 3D reconstruction. Even so, we argue that strong 3D inductive biases are still helpful in the design of such networks. We show this point by introducing LagerNVS, an encoder-decoder neural network for NVS that builds on `3D-aware' latent features. The encoder is initialized from a 3D reconstruction network pre-trained using explicit 3D supervision. This is paired with a lightweight decoder, and trained end-to-end with photometric losses. LagerNVS achieves state-of-the-art deterministic feed-forward Novel View Synthesis (including 31.4 PSNR on Re10k), with and without known cameras, renders in real time, generalizes to in-the-wild data, and can be paired with a diffusion decoder for generative extrapolation.

---

## 论文详细总结（自动生成）

### LagerNVS：用于全神经实时新视角合成的潜几何——论文深度总结

#### 1. 核心问题与研究动机
*   **核心问题**：如何在不进行显式三维重建（如点云、网格或 3D 高斯）的情况下，实现高质量、实时且具有泛化能力的前馈新视角合成（NVS）。
*   **研究动机**：
    *   传统 NVS 方法（如 NeRF, 3DGS）通常需要针对每个场景进行耗时的优化。
    *   现有的前馈神经渲染方法（如 SRT, LVSM）虽然速度快，但往往缺乏足够的三维归纳偏置（3D Inductive Bias），导致在复杂几何或稀疏视角下的渲染质量受限。
    *   作者认为，即使不输出显式的 3D 几何，在网络设计中引入“3D 感知”的特征表示对于提升 NVS 性能至关重要。

#### 2. 核心方法论
LagerNVS 采用了一种**编码器-解码器（Encoder-Decoder）**架构，其核心思想是利用预训练的 3D 知识来增强潜空间特征。
*   **3D 感知编码器**：
    *   **初始化**：编码器并非从头训练，而是使用 **VGGT**（一种预训练的显式 3D 重建模型）的权重进行初始化。这使得提取的特征在潜空间中已经包含了丰富的几何信息。
    *   **相机注入**：如果已知输入视角相机参数，通过 MLP 将其投影为 Token 并与图像特征融合；若未知，则设为空向量。
*   **高效解码器**：
    *   **输入表示**：目标视角通过 **Plucker 射线图（Ray Map）**进行参数化，作为解码器的查询（Query）。
    *   **架构选择**：提出了“**高速公路（Highway）**”架构，即解码器可以直接访问所有源图像的特征 Token，避免了固定维度的“瓶颈（Bottleneck）”限制。
    *   **注意力机制**：提供两种变体：全注意力（Full Attention，$O(V^2)$ 复杂度）和双向交叉注意力（Bidirectional Cross-attention，$O(V)$ 复杂度），后者在多视角输入时更具实时性。
*   **训练策略**：使用 $L_2$ 损失和感知损失（LPIPS）进行**端到端（End-to-End）**微调，证明了微调预训练 3D 骨干网络对保留外观细节至关重要。

#### 3. 实验设计
*   **数据集**：在 13 个多视角数据集的混合体上进行训练（包括 RealEstate10k, DL3DV, CO3D, WildRGBD, TartanAir 等），涵盖了室内、室外、物体和野外场景。
*   **Benchmark**：
    *   **RealEstate10k (Re10k)**：用于与确定性 NVS 方法对比。
    *   **DL3DV & CO3D**：用于测试泛化能力和多视角合成。
*   **对比方法**：
    *   **隐式/神经方法**：LVSM（之前的 SOTA）、SRT、RayZer。
    *   **显式前馈 3DGS 方法**：DepthSplat、AnySplat、Flare、NoPoSplat。

#### 4. 资源与算力
*   **硬件**：推理测试在单张 **NVIDIA H100 GPU** 上完成。
*   **性能**：在 512×512 分辨率下，输入多达 9 张图像时，渲染速度超过 **30 FPS**。
*   **训练细节**：主模型在 512 分辨率下训练了 250k 次迭代，Batch Size 为 512。虽然未明确总训练时长，但如此大规模的 Batch Size 和迭代次数表明其消耗了相当可观的计算资源（通常为数天至数周的集群训练）。

#### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括 SOTA 对比、跨数据集泛化测试、单视角 NVS 测试以及扩散模型扩展实验。
*   **消融实验**：非常充分。作者对比了：
    1.  有无 3D 预训练（对比从头训练和 DinoV2 预训练）。
    2.  不同架构（Highway vs. Bottleneck vs. Decoder-only）。
    3.  是否端到端微调（对比冻结骨干网络）。
    4.  注意力机制变体（Full vs. Cross-attention）。
*   **客观性**：实验设置考虑了已知相机和未知相机两种情况，并使用了标准的 PSNR、SSIM、LPIPS 指标，对比基准公平且具有代表性。

#### 6. 主要结论与发现
*   **3D 偏置的必要性**：使用 3D 重建任务预训练的特征比通用的 2D 特征（如 DinoV2）在 NVS 任务上表现显著更好（PSNR 提升约 2.9dB）。
*   **架构优势**：Highway 编码器-解码器架构在保持实时性的同时，性能优于传统的 Bottleneck 架构和纯 Decoder 架构。
*   **超越显式重建**：在稀疏视角下，LagerNVS 的隐式表示在处理反射、薄结构和遮挡填充方面优于显式的前馈 3D 高斯投影方法。
*   **SOTA 性能**：在 Re10k 上达到了 31.4 PSNR，比之前的 SOTA (LVSM) 提升了 1.7dB。

#### 7. 优点与亮点
*   **实时性与高质量并存**：在不使用自定义算子或复杂 JIT 编译的情况下，实现了全神经网络的实时高分辨率渲染。
*   **极强的泛化与鲁棒性**：单个模型即可处理 360° 场景、非正方形比例、单视角输入以及相机参数缺失的情况。
*   **灵活性**：模型不仅可以作为确定性回归器，还可以轻松微调为扩散模型，以处理高不确定性区域的生成。

#### 8. 不足与局限
*   **确定性回归的局限**：在处理完全未观测到的区域时，确定性训练会导致结果模糊（回归至均值），虽然扩散模型版本可缓解，但会丧失实时性。
*   **静态场景假设**：模型目前仅适用于静态场景，未考虑动态物体（如行人）或复杂的非刚性形变。
*   **伪影问题**：在渲染未观察区域时，偶尔会出现块状伪影或视频闪烁。
*   **数据依赖**：高性能高度依赖于大规模、高质量的多视角数据集混合训练。

（完）
