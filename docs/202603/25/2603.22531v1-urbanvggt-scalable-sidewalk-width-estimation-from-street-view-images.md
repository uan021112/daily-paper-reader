---
title: "UrbanVGGT: Scalable Sidewalk Width Estimation from Street View Images"
title_zh: UrbanVGGT：基于街景图像的可扩展人行道宽度估算
authors: "Kaizhen Tan, Fan Zhang"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.22531v1"
tags: ["query:ff3dr"]
score: 9.0
evidence: 用于人行道宽度估计的前馈3D重建
tldr: 针对城市人行道宽度数据匮乏且获取成本高的问题，本文提出UrbanVGGT，一种基于单张街景图像估算人行道宽度的自动化流水线。该方法整合了语义分割、3D重建、地平面拟合及相机高度标定等技术，实现了高精度的度量级宽度测量。在华盛顿特区的测试中表现优异，并成功应用于多城市数据集生成，为大规模城市步行环境评估提供了高效、低成本的解决方案。
selection_source: fresh_fetch
motivation: 传统的人行道宽度测量方法依赖高成本实地调研或低精度遥感影像，难以实现大规模且准确的数据覆盖。
method: 该流水线结合了语义分割、前馈3D重建、自适应地平面拟合及基于相机高度的尺度标定，从单张街景图中提取物理宽度。
result: "在华盛顿特区基准测试中，该方法实现了0.252米的平均绝对误差，且95.5%的估算误差在0.5米以内。"
conclusion: 研究证明街景图像结合几何重建技术可实现可扩展的人行道宽度估算，为城市规划和无障碍环境评估提供了有力的数据支持。
---

## 摘要
人行道宽度是衡量行人可达性、舒适度和网络质量的重要指标，但在大多数城市中，大规模的宽度数据仍然匮乏。现有方法通常依赖于昂贵的实地调查、高分辨率遥感影像或简化的几何假设，这限制了可扩展性或引入了系统误差。为填补这一空白，我们提出了 UrbanVGGT，这是一个从单张街景图像估算度量级人行道宽度的测量流程。该方法结合了语义分割、前馈 3D 重建、自适应地平面拟合、基于相机高度的尺度校准以及在恢复平面上的定向宽度测量。在华盛顿特区的地面真值基准测试中，UrbanVGGT 实现了 0.252 米的平均绝对误差，95.5% 的估算值与参考宽度的误差在 0.50 米以内。消融实验表明，度量尺度校准是最关键的组件，与替代几何骨干网络的对照比较证明了整体设计的有效性。作为可行性演示，我们进一步将该流程应用于三个城市，并生成了 SV-SideWidth，这是一个涵盖 527 个 OpenStreetMap 街道路段的原型人行道宽度数据集。结果表明，街景图像可以支持大规模生成候选人行道宽度属性，但在作为权威规划数据部署之前，仍需进行更广泛的跨城市验证和本地地面真值审计。

## Abstract
Sidewalk width is an important indicator of pedestrian accessibility, comfort, and network quality, yet large-scale width data remain scarce in most cities. Existing approaches typically rely on costly field surveys, high-resolution overhead imagery, or simplified geometric assumptions that limit scalability or introduce systematic error. To address this gap, we present UrbanVGGT, a measurement pipeline for estimating metric sidewalk width from a single street-view image. The method combines semantic segmentation, feed-forward 3D reconstruction, adaptive ground-plane fitting, camera-height-based scale calibration, and directional width measurement on the recovered plane. On a ground-truth benchmark from Washington, D.C., UrbanVGGT achieves a mean absolute error of 0.252 m, with 95.5% of estimates within 0.50 m of the reference width. Ablation experiments show that metric scale calibration is the most critical component, and controlled comparisons with alternative geometry backbones support the effectiveness of the overall design. As a feasibility demonstration, we further apply the pipeline to three cities and generate SV-SideWidth, a prototype sidewalk-width dataset covering 527 OpenStreetMap street segments. The results indicate that street-view imagery can support scalable generation of candidate sidewalk-width attributes, while broader cross-city validation and local ground-truth auditing remain necessary before deployment as authoritative planning data.

---

## 论文详细总结（自动生成）

以下是对论文《UrbanVGGT: Scalable Sidewalk Width Estimation from Street View Images》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何从单张街景图像中高效、大规模且准确地估算人行道的物理宽度（度量级宽度）。
*   **研究动机**：人行道宽度是城市步行环境和无障碍评估的关键指标，但目前大多数城市（如纽约、内罗毕等）在 OpenStreetMap (OSM) 中严重缺失此类数据。传统方法（实地调研、高分辨率遥感影像）存在成本高、易受遮挡（树木、建筑物）或几何假设过于简化等局限。

### 2. 提出的方法论：UrbanVGGT 流水线
该方法将宽度估算视为一个**平面约束的 3D 测量问题**，包含五个关键阶段：
1.  **语义分割**：使用 SegFormer-B5 模型识别图像中的“人行道”和“道路”区域，确定边界候选点。
2.  **3D 几何重建**：采用 **VGGT**（Visual Geometry Grounded Transformer）前馈模型，从单张图像直接预测密集的 3D 点云图和相机参数。
3.  **自适应地平面拟合**：提取语义标签为道路和人行道的 3D 点，利用 SVD 和 RANSAC 算法拟合地平面。通过中值绝对偏差（MAD）动态调整 RANSAC 阈值，以增强对重建噪声的鲁棒性。
4.  **度量尺度标定**：由于单目重建具有尺度不确定性，利用已知的相机安装高度（如 Google 街景车通常为 2.5m）计算缩放因子 $s = h_{cam} / h_{pred}$，将 3D 坐标转换为米制单位。
5.  **列向宽度测量**：在图像中心区域进行列扫描，识别内外边界点，将其投影到拟合的 3D 地平面上，计算投影点之间的定向距离，并通过鲁棒统计量聚合最终宽度。

### 3. 实验设计
*   **数据集与基准**：
    *   **核心基准**：使用华盛顿特区（Washington, D.C.）的 300 张 Google 街景图像，配有高精度的地面真值（Ground Truth）宽度数据。
    *   **原型部署**：在纽约（Midtown）、圣保罗和内罗毕三个城市进行实地应用，生成了涵盖 527 个路段的 **SV-SideWidth** 数据集。
*   **对比方法**：共对比了 9 种主流深度估计与重建模型（如 ZoeDepth, DepthPro, Metric3Dv2, UniDepthV2, MapAnything, $\pi^3$ 等），涵盖了“原生度量深度”、“单目深度+针孔投影”和“直接 3D 点云重建”三大类共 16 种配置。

### 4. 资源与算力
*   **论文说明**：文中**未明确提及**具体的 GPU 型号、数量及训练时长。
*   **相关信息**：作者提到 Google 街景 API 的调用费用由第一作者自费承担。由于使用的是前馈模型（Feed-forward models），推理过程相对高效，适合大规模批处理。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 16 种不同几何骨干网络的对比实验。
    *   针对相机高度敏感性、消融实验（验证尺度标定、3D 重建、测量带宽的作用）进行了多组测试。
    *   在三个不同城市进行了跨区域的可行性验证。
*   **充分性评价**：实验设计较为**客观且公平**，所有对比模型均使用相同的下游测量流水线。消融实验清晰地展示了各组件的贡献。但定量评估主要集中在华盛顿特区的 300 张图像上，样本量对于全球范围的泛化性验证略显不足。

### 6. 主要结论与发现
*   **精度表现**：UrbanVGGT 在基准测试中实现了 **0.252 米**的平均绝对误差（MAE），95.5% 的误差在 0.5 米以内，优于所有对比模型。
*   **关键组件**：消融实验证明**度量尺度标定**是准确测量的最关键步骤；相比简单的针孔几何模型，3D 重建能显著提升精度。
*   **尺度敏感性**：宽度估算对相机高度假设高度敏感，高度偏差会线性传导至宽度结果。
*   **数据填补**：成功在 OSM 缺失数据的区域生成了候选宽度属性，证明了该方法在城市规划中的实用价值。

### 7. 优点与亮点
*   **单图测量**：仅需单张图像即可实现度量级测量，无需双目视觉或昂贵的 LiDAR 数据。
*   **几何鲁棒性**：通过 3D 点云投影而非 2D 像素几何，能够更好地处理透视畸变。
*   **自适应性**：地平面拟合算法能够自动适应不同的路面粗糙度和重建质量。
*   **可扩展性**：流程自动化程度高，易于集成到现有的城市地理信息系统中。

### 8. 不足与局限
*   **验证范围有限**：定量验证仅基于一个城市的少量样本，缺乏在不同气候、光照和街道风格下的广泛测试。
*   **遮挡问题**：当路缘石或建筑物边界被违停车辆、植被或行人遮挡时，测量精度会大幅下降。
*   **高度依赖先验**：方法依赖于相机安装高度的准确性，若不同城市的街景车高度不一且未校准，会产生系统性偏差。
*   **复杂场景失效**：在非平整地面（如陡坡、路缘石切口）或非平行边界（如异形路口）场景下，模型假设可能失效。

（完）
