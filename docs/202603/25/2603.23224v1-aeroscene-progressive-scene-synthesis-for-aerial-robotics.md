---
title: "AeroScene: Progressive Scene Synthesis for Aerial Robotics"
title_zh: AeroScene：面向空中机器人的渐进式场景合成
authors: "Nghia Vu, Tuong Do, Dzung Tran, Binh X. Nguyen, Hoan Nguyen, Erman Tjiputra, Quang D. Tran, Hai-Nguyen Nguyen, Anh Nguyen"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23224v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 用于渐进式 3D 场景合成的分层扩散模型
tldr: 针对无人机仿真环境手动构建耗时且难以扩展的问题，本文提出AeroScene，一种用于渐进式3D场景生成的层次化扩散模型。该模型通过层次感知标记化和多分支特征提取，实现了全局布局与局部细节的协同推理，确保了场景的物理合理性与语义一致性。实验证明其在生成高质量、物理可用的无人机仿真场景方面优于现有方法，并贡献了包含1000多个场景的大规模数据集。
selection_source: fresh_fetch
motivation: 现有的无人机仿真环境主要依赖手动创建，效率低下且难以大规模扩展，限制了生成式模型在机器人场景合成中的应用。
method: 提出一种层次化扩散模型，利用层次感知标记化和多分支特征提取技术，实现从全局布局到局部细节的渐进式3D场景生成。
result: 在自建数据集和公开基准上均显著优于现有方法，并成功生成了1000多个可直接集成至NVIDIA Isaac Sim的高保真物理仿真场景。
conclusion: AeroScene为航空机器人任务提供了高效、逼真的场景生成方案，显著提升了下游无人机导航等任务的仿真训练能力。
---

## 摘要
生成模型在多个领域展现了巨大影响，但其在机器人场景合成方面的潜力仍有待挖掘。这种差距在无人机模拟器中尤为明显，目前的模拟环境仍高度依赖人工构建，不仅耗时且难以扩展。在这项工作中，我们提出了 AeroScene，一种用于渐进式 3D 场景合成的分层扩散模型。我们的方法利用层次感知分词（hierarchy-aware tokenization）和多分支特征提取，对全局布局和局部细节进行推理，确保了物理合理性和语义一致性。这使得 AeroScene 特别适用于为导航、降落和栖息等空中机器人任务生成逼真的场景。我们通过在最新收集的数据集和公开基准测试上的广泛实验证明了其有效性，结果表明 AeroScene 显著优于现有方法。此外，我们利用 AeroScene 生成了一个包含 1,000 多个具备物理属性、高保真 3D 场景的大规模数据集，这些场景可直接集成到 NVIDIA Isaac Sim 中。最后，我们展示了这些生成的环境在下游无人机导航任务中的实用性。我们的代码和数据集已在 aioz-ai.github.io/AeroScene/ 公开。

## Abstract
Generative models have shown substantial impact across multiple domains, their potential for scene synthesis remains underexplored in robotics. This gap is more evident in drone simulators, where simulation environments still rely heavily on manual efforts, which are time-consuming to create and difficult to scale. In this work, we introduce AeroScene, a hierarchical diffusion model for progressive 3D scene synthesis. Our approach leverages hierarchy-aware tokenization and multi-branch feature extraction to reason across both global layouts and local details, ensuring physical plausibility and semantic consistency. This makes AeroScene particularly suited for generating realistic scenes for aerial robotics tasks such as navigation, landing, and perching. We demonstrate its effectiveness through extensive experiments on our newly collected dataset and a public benchmark, showing that AeroScene significantly outperforms prior methods. Furthermore, we use AeroScene to generate a large-scale dataset of over 1,000 physics-ready, high fidelity 3D scenes that can be directly integrated into NVIDIA Isaac Sim. Finally, we illustrate the utility of these generated environments on downstream drone navigation tasks. Our code and dataset are publicly available at aioz-ai.github.io/AeroScene/

---

## 论文详细总结（自动生成）

### AeroScene：面向空中机器人的渐进式场景合成论文总结

#### 1. 研究动机与背景
空中机器人（如无人机）在物流、巡检和监控等领域的应用日益广泛，但其仿真环境的构建长期依赖人工设计，存在**耗时长、难以规模化扩展、缺乏多样性**等问题。现有的场景合成方法往往将场景视为平坦的对象集合，缺乏对“全局布局（如建筑、地形）”与“局部细节（如障碍物、降落点）”之间层次关系的推理，导致生成的场景在物理合理性和导航可行性上难以满足空中机器人的高标准需求。

#### 2. 核心方法论
AeroScene 提出了一种**层次化扩散模型（Hierarchical Diffusion Model）**，旨在通过渐进式方式合成 3D 场景：
*   **层次感知标记化（Hierarchy-aware Tokenization）：** 在扩散过程的每一步，模型根据物体的“可标记化分数”将其分类为粗粒度（Coarse）或细粒度（Fine）标记。
*   **多分支特征提取：** 
    *   **粗粒度分支：** 使用 3D CNN 处理建筑、地形等宏观结构特征。
    *   **细粒度分支：** 使用图神经网络（GNN）处理物体间的空间邻接关系和局部几何细节。
*   **跨尺度渐进注意力（Cross-scale Progressive Attention）：** 通过堆叠的自上而下（粗→细）和自下而下（细→粗）注意力块，实现全局上下文与局部细节的特征融合。
*   **任务感知引导函数（Guidance Objectives）：** 在推理阶段引入梯度引导，包括：
    1.  **碰撞规避引导：** 利用 3D IoU 惩罚物体重叠。
    2.  **粗到细引导：** 确保细粒度物体放置在合理的粗粒度区域内。
    3.  **语义约束引导：** 基于类别先验确保物体间关系的语义合理性。

#### 3. 实验设计
*   **数据集：** 
    *   **3D-FRONT：** 经典的室内场景基准数据集。
    *   **AeroScene Dataset（本文贡献）：** 包含 1016 个高保真场景（812 个训练，204 个测试），涵盖室内外环境，标注了 16 万个物体及专门的无人机降落/交互区域。
*   **对比基准（Baselines）：** ATISS、Diffusion-SDF、DiffuScene、PhyScene 等先进的生成式场景合成模型。
*   **评价指标：** FID/KID（感知相似度）、CR（碰撞率）、CFC（粗细一致性）、SP（语义合理性）。

#### 4. 资源与算力
*   **硬件：** 使用了单张 **NVIDIA A100 GPU**。
*   **训练配置：** Adam 优化器，学习率 $2 \times 10^{-4}$，Batch Size 为 64，训练时长为 **800 个 Epoch**。
*   **推理效率：** 在 A100 上生成一个场景大约需要 **2 分钟**（100 步反向扩散），与主流扩散模型基准相当。

#### 5. 实验数量与充分性
论文开展了多维度的实验，具有较高的充分性和客观性：
*   **定量对比：** 在两个数据集上进行了详尽的指标对比，样本量达 1000 个场景。
*   **消融实验：** 针对三种引导函数（碰撞、层次、语义）分别进行了剔除实验，验证了各组件对物理合理性的贡献。
*   **下游任务验证：** 将生成的场景导入 **NVIDIA Isaac Sim**，进行了 300 条无人机导航与降落轨迹的仿真测试，成功率达 91%，证明了生成场景的实用价值。

#### 6. 主要结论与发现
*   AeroScene 在所有核心指标（FID、碰撞率、层次一致性等）上均显著优于现有方法。
*   层次化建模是提升场景真实性的关键，能够有效避免物体悬空或穿模等常见生成错误。
*   生成的场景具备“物理就绪（Physics-ready）”特性，可直接用于无人机控制算法的闭环仿真训练。

#### 7. 论文优点
*   **层次化视角：** 突破了传统方法将场景扁平化处理的局限，更符合现实世界“建筑-房间-家具”的逻辑。
*   **端到端工具链：** 不仅提出了算法，还贡献了高质量数据集，并打通了从生成到 Isaac Sim 仿真的全流程。
*   **引导机制：** 引入的物理引导函数显著降低了生成场景中的物理违和感。

#### 8. 不足与局限
*   **静态限制：** 目前仅关注静态场景布局，未考虑动态物体（如行人、车辆）或环境变化（如风力、光照）。
*   **Sim-to-Real 缺位：** 实验完全在仿真环境中进行，缺乏在真实物理世界无人机上的验证。
*   **推理耗时：** 2 分钟生成一个场景虽然在离线任务中可接受，但对于需要实时生成环境的交互式应用仍显稍慢。

（完）
