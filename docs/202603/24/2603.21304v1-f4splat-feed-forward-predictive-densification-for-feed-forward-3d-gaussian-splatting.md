---
title: "F4Splat: Feed-Forward Predictive Densification for Feed-Forward 3D Gaussian Splatting"
title_zh: F4Splat：用于前馈 3D 高斯泼溅的前馈预测性稠密化
authors: "Injae Kim, Chaehyeon Kim, Minseong Bae, Minseok Joo, Hyunwoo J. Kim"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21304v1"
tags: ["query:ff3dr"]
score: 10.0
evidence: 用于单次通过重建的前馈3D高斯泼溅
tldr: F4Splat 针对前馈 3D 高斯泼溅（3DGS）中高斯分布冗余且难以控制数量的问题，提出了一种基于预测性加密的分配策略。该方法通过预测区域加密分数，根据空间复杂度和多视图重叠度自适应分配高斯点，实现了在显著减少高斯数量的同时提升重建质量，并支持在不重训的情况下显式控制高斯预算。
selection_source: fresh_fetch
motivation: 现有的前馈 3DGS 方法采用固定的像素或体素分配机制，导致高斯点在视图间存在大量冗余且缺乏对总量的灵活控制。
method: 提出一种加密分数引导的分配策略，通过预测区域加密分数来自适应分布高斯点，并允许在推理时显式控制高斯预算。
result: 实验表明，该模型在使用更少高斯点的情况下，其新视图合成性能优于此前未校准的前馈方法。
conclusion: F4Splat 通过空间自适应分配实现了更紧凑且高质量的 3D 表示，有效平衡了重建精度与计算效率。
---

## 摘要
前馈 3D 高斯泼溅（3D Gaussian Splatting）方法能够实现单次通过式重建和实时渲染。然而，这些方法通常采用固定的“像素到高斯”或“体素到高斯”流水线，均匀地分配高斯分布，导致跨视图的高斯冗余。此外，它们缺乏在保持重建保真度的同时控制高斯总数的有效机制。为了解决这些局限性，我们提出了 F4Splat，它为前馈 3D 高斯泼溅执行前馈预测性稠密化，引入了一种基于稠密化评分引导的分配策略，根据空间复杂度和多视图重叠情况自适应地分布高斯。我们的模型预测每个区域的稠密化评分以估计所需的高斯密度，并允许在无需重新训练的情况下显式控制最终的高斯预算。这种空间自适应分配减少了简单区域的冗余，并最大限度地减少了重叠视图中的重复高斯，从而生成紧凑且高质量的 3D 表示。大量实验表明，与之前未经校准的前馈方法相比，我们的模型在显著减少高斯数量的同时，实现了更优的新视角合成性能。

## Abstract
Feed-forward 3D Gaussian Splatting methods enable single-pass reconstruction and real-time rendering. However, they typically adopt rigid pixel-to-Gaussian or voxel-to-Gaussian pipelines that uniformly allocate Gaussians, leading to redundant Gaussians across views. Moreover, they lack an effective mechanism to control the total number of Gaussians while maintaining reconstruction fidelity. To address these limitations, we present F4Splat, which performs Feed-Forward predictive densification for Feed-Forward 3D Gaussian Splatting, introducing a densification-score-guided allocation strategy that adaptively distributes Gaussians according to spatial complexity and multi-view overlap. Our model predicts per-region densification scores to estimate the required Gaussian density and allows explicit control over the final Gaussian budget without retraining. This spatially adaptive allocation reduces redundancy in simple regions and minimizes duplicate Gaussians across overlapping views, producing compact yet high-quality 3D representations. Extensive experiments demonstrate that our model achieves superior novel-view synthesis performance compared to prior uncalibrated feed-forward methods, while using significantly fewer Gaussians.

---

## 论文详细总结（自动生成）

### F4Splat 论文深度总结：用于前馈 3D 高斯泼溅的前馈预测性稠密化

#### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：3D 高斯泼溅（3DGS）凭借其显式的 3D 表示和实时渲染能力，已成为 3D 重建的主流方法。然而，传统 3DGS 依赖逐场景的迭代优化，耗时且需要已知相机参数。
*   **核心问题**：现有的“前馈式（Feed-Forward）”3DGS 方法虽然实现了单次推理重建，但存在两大缺陷：
    1.  **分配僵化**：通常采用“像素到高斯”的固定映射，导致简单区域（如平滑墙面）高斯冗余，而复杂区域（如花朵细节）表示不足。
    2.  **不可控性**：缺乏在不重新训练模型的情况下，根据计算资源灵活调整高斯总数（Budget）的机制。
*   **研究目标**：开发一种能够根据场景复杂度自适应分配高斯点，并允许用户显式控制高斯预算的前馈框架。

#### 2. 方法论：核心思想与关键技术
*   **核心思想**：将传统 3DGS 中的“自适应密度控制（ADC）”机制转化为一个**可学习的预测问题**。通过预测“稠密化分数（Densification Score）”，在单次前馈过程中决定哪些区域需要更多高斯点。
*   **关键技术细节**：
    *   **几何骨干网络（Geometry Backbone）**：基于 VGGT 结构，利用 DINOv2 提取特征，通过 Transformer 层联合估计相机参数和场景几何。
    *   **多尺度预测（Multi-Scale Prediction）**：模型同时预测 $L$ 个尺度的 Gaussian 参数图和稠密化分数图。分辨率逐级倍增，允许在不同层级间进行选择。
    *   **稠密化分数引导分配**：
        *   **分数定义**：借鉴 AbsGS，将渲染损失对高斯位置的梯度模长（log 尺度）作为监督信号。梯度大意味着该区域未被充分表示，需要加密。
        *   **分配逻辑**：从最粗尺度开始，若某区域分数超过阈值 $\tau$，则在该位置调用更高分辨率的高斯点。
    *   **预算匹配算法（Budget Matching）**：通过二分查找快速确定满足用户指定高斯总数 $\bar{N}_G$ 的最优阈值 $\tau$，实现无需重训的动态控制。
*   **训练策略**：引入“新视角监督”和“场景尺度正则化（Scene-scale Regularization）”，确保在未校准（Uncalibrated）设置下的训练稳定性。

#### 3. 实验设计
*   **数据集**：
    *   **RealEstate10K (RE10K)**：大规模室内外场景视频数据集。
    *   **ACID**：自然景观数据集，用于测试泛化能力。
*   **Benchmark 与设置**：
    *   **输入视图**：涵盖 2 视图、8 视图、16 视图及 24 视图的稀疏输入。
    *   **对比维度**：分为“未校准（仅图像输入）”和“无姿态（已知内参但无位姿）”两种设置。
*   **对比方法**：
    *   **前馈 3DGS 基准**：NoPoSplat, VicaSplat, AnySplat, SPFSplat, pixelSplat, MVSplat 等。
    *   **其他基准**：DUSt3R, MASt3R, pixelNeRF 等。

#### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA H200 GPU**。
*   **训练时长**：
    *   多视图模型：15,000 次迭代，耗时约 **15 小时**。
    *   两视图模型：18,750 次迭代。
*   **推理效率**：相比于均匀分配方法，自适应分配引入的额外计算开销较小（推理时间增加约 10.1%，显存占用增加约 1.8%）。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了多视图（8/16/24 视图）性能评估。
    *   进行了跨数据集（RE10K 训练，ACID 测试）的零样本泛化实验。
    *   **消融实验**：对比了随机分配、基于频率的分配、无多尺度监督、无场景正则化等变体，验证了各模块的必要性。
    *   **姿态估计实验**：专门测试了模型在未校准输入下的相机位姿预测精度。
*   **评价**：实验设计全面，涵盖了质量（PSNR/SSIM/LPIPS）、效率（高斯数量）和几何准确性（姿态误差），对比基准均为近年顶级会议成果，实验结果具有较高的客观性和说服力。

#### 6. 主要结论与发现
*   **高效率**：F4Splat 在仅使用基准方法 **10% - 28%** 的高斯数量时，仍能达到甚至超过其渲染质量。
*   **空间自适应性**：模型成功学会了在复杂细节（如花朵、物体边缘）分配更多高斯，而在重叠视图或简单平面减少分配。
*   **未校准优势**：在完全不依赖相机先验的情况下，其性能显著优于 VicaSplat 和 AnySplat 等同类方法。
*   **可控性**：通过调整阈值，模型能平滑地在“极低带宽/高速度”与“高质量/高细节”模式间切换。

#### 7. 优点（亮点）
*   **首创性**：首次在单次前馈 3DGS 框架中实现了类似传统 3DGS 的动态密度控制。
*   **灵活性**：支持推理时的实时预算调整，无需针对不同资源限制重复训练模型。
*   **冗余消除**：通过稠密化分数有效识别多视图重叠区域，避免了前馈方法中常见的跨视图高斯堆叠问题。
*   **鲁棒性**：在未校准的稀疏视图重建中表现出极强的几何推理能力。

#### 8. 不足与局限
*   **推理开销**：虽然高斯数量减少了，但为了生成多尺度图和进行预算匹配，推理时间比最简单的像素级前馈方法略长（约 10%）。
*   **尺度限制**：目前仅使用了 3 个尺度层级，对于极大规模场景的精细度提升可能存在上限。
*   **依赖骨干网络**：性能在很大程度上受限于预训练几何骨干（如 DINOv2）的特征表达能力。
*   **应用限制**：主要针对稀疏视图重建，在极高密度视图输入下的优势可能不如稀疏场景明显。

（完）
