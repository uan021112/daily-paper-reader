---
title: "From Part to Whole: 3D Generative World Model with an Adaptive Structural Hierarchy"
title_zh: 从局部到整体：具有自适应结构层级的 3D 生成式世界模型
authors: "Bi'an Du, Daizong Liu, Pufan Li, Wei Hu"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21557v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 具有自适应结构层次的3D生成世界模型
tldr: 针对单图3D生成中跨类别泛化和结构复杂性适配的难题，本文提出了一种具有自适应结构层次的3D生成世界模型。该模型通过从图像Token中推断软组合掩码来自动发现潜在结构槽位，并利用自适应槽位门控机制动态调整槽位激活，结合类别无关的原型库实现几何先验共享。这种从局部到整体的建模方式显著提升了模型在处理新颖布局和复杂结构时的泛化能力与鲁棒性。
selection_source: fresh_fetch
motivation: 现有的3D生成方法多采用整体建模或固定数量的部件划分，难以应对多样化的语义类别和复杂的结构变化。
method: 提出一种自适应槽位门控机制来动态发现结构槽位，并结合类别无关的几何原型库进行跨类别的形状共享与去噪。
result: 实验证明该方法在跨类别迁移和部件数量外推方面表现优异，能够生成结构完整且紧凑的3D模型。
conclusion: 通过学习自适应的局部到整体层次结构，该模型有效解决了单图3D生成中的结构泛化与组合建模难题。
---

## 摘要
单图像 3D 生成是现实世界中视觉到图形模型的核心。然而，在稀疏监督下，实现跨不同语义类别和高度多变结构复杂性的可靠泛化仍然是一个根本性挑战。现有方法通常以整体方式对物体建模，或依赖固定数量的部件，包括最近的部件感知模型（如 PartCrafter），这些模型仍需要用户手动指定部件数量，费时费力。这种设计容易导致过拟合、结构组件破碎或缺失，以及在遇到新颖物体布局时组合泛化能力有限。为此，本文将单图像 3D 生成重新构思为在灵活的 3D 潜空间中学习自适应的局部-整体层级。我们提出了一种新型的从局部到整体的 3D 生成式世界模型，该模型通过直接从图像 token 中推断软组合掩码，自主发现潜在的结构槽位（structural slots）。具体而言，一种自适应槽位门控机制动态确定每个槽位的激活概率，并平滑地整合不同物体中的冗余槽位，确保生成的结构在跨类别时保持紧凑且具有表现力。每个提取出的槽位随后与一个可学习的、类别无关的原型库对齐，通过现实世界中的通用几何原型实现强大的跨类别形状共享和去噪。此外，引入了一个轻量级 3D 去噪器，通过统一的扩散目标重建几何形状和外观。实验表明，该方法在跨类别迁移和部件数量外推方面取得了持续提升，消融实验证实了原型库在形状先验共享以及槽位门控在结构自适应方面的互补优势。

## Abstract
Single-image 3D generation lies at the core of vision-to-graphics models in the real world. However, it remains a fundamental challenge to achieve reliable generalization across diverse semantic categories and highly variable structural complexity under sparse supervision. Existing approaches typically model objects in a monolithic manner or rely on a fixed number of parts, including recent part-aware models such as PartCrafter, which still require a labor-intensive user-specified part count. Such designs easily lead to overfitting, fragmented or missing structural components, and limited compositional generalization when encountering novel object layouts. To this end, this paper rethinks single-image 3D generation as learning an adaptive part-whole hierarchy in the flexible 3D latent space. We present a novel part-to-whole 3D generative world model that autonomously discovers latent structural slots by inferring soft and compositional masks directly from image tokens. Specifically, an adaptive slot-gating mechanism dynamically determines the slot-wise activation probabilities and smoothly consolidates redundant slots within different objects, ensuring that the emergent structure remains compact yet expressive across categories. Each distilled slot is then aligned to a learnable, class-agnostic prototype bank, enabling powerful cross-category shape sharing and denoising through universal geometric prototypes in the real world. Furthermore, a lightweight 3D denoiser is introduced to reconstruct geometry and appearance via unified diffusion objectives. Experiments show consistent gains in cross-category transfer and part-count extrapolation, and ablations confirm complementary benefits of the prototype bank for shape-prior sharing as well as slot-gating for structural adaptation.

---

## 论文详细总结（自动生成）

以下是对论文《From Part to Whole: 3D Generative World Model with an Adaptive Structural Hierarchy》的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
该论文聚焦于**单图像 3D 生成**任务。尽管现有模型在稀疏视图重建上取得了进展，但在处理现实世界中多样化的语义类别和复杂的结构变化时仍面临两大瓶颈：
*   **固定粒度的形状表示**：现有方法要么将物体视为整体（难以恢复细长或遮挡结构），要么依赖固定数量的部件划分（如 PartCrafter 需要手动指定部件数），导致简单物体过度分割或复杂场景分割不足。
*   **缺乏共享的部件级先验**：不同类别的物体往往拥有相似的局部组件（如桌腿、把手），但现有模型通常将部件视为独立组件，无法跨类别复用这些几何模式，限制了组合泛化能力。
**研究动机**：通过学习一种“从局部到整体”的自适应结构层级，构建一个能够自主发现部件并共享几何先验的 3D 生成式世界模型。

### 2. 方法论：核心思想与关键技术
论文提出了一种基于**自适应结构层级**的 3D 生成框架，核心组件包括：
*   **结构化潜在槽位（Structured Latent Slots）**：将 3D 世界状态表示为一组潜在槽位。每个槽位对应一个局部 3D 潜变量，通过预训练的 3D VAE 进行编解码。
*   **自适应槽位门控机制（Adaptive Slot-gating）**：利用图像编码器（DINOv2）提取特征，通过门控头预测每个槽位的激活概率。这使得模型能根据输入图像动态决定生成的部件数量，无需人工干预。
*   **类别无关原型库（Class-agnostic Prototype Bank）**：在槽位空间维护一组可学习的几何原型。每个激活的槽位通过软注意力机制与原型对齐，并将原型引导的残差注入潜变量。这促进了跨类别的几何先验共享（如不同家具共享相似的支撑结构）。
*   **掩码流匹配（Masked Flow-matching）**：在 3D DiT 主干网络中，仅对激活的槽位施加流匹配损失监督，确保训练的稳定性和对冗余槽位的抑制。
*   **训练流程**：分为“槽位与原型预热”和“主干网络联合微调”两个阶段，以平衡结构发现与几何重建。

### 3. 实验设计
*   **数据集**：
    *   **物体级**：Objaverse（高质量子集）、ShapeNet-Core、Amazon Berkeley Objects (ABO)。
    *   **场景级**：3D-Front（室内家具组合场景）。
*   **基准测试（Benchmark）**：评估了全局保真度（Chamfer Distance, F-Score）和部件独立性（Voxel IoU，越低表示部件间重叠越少、物理独立性越好）。
*   **对比方法**：包括单体生成模型 **TripoSG**、部件感知模型 **HoloPart** 和 **PartCrafter**，以及场景生成模型 **MIDI**。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量及总训练时长。
*   **实现细节**：提到基于预训练的 PartCrafter 风格的 3D VAE + DiT 架构进行改进，使用了 DINOv2 作为图像特征提取器。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在三个主流物体数据集（Objaverse, ShapeNet, ABO）上进行了定量对比。
    *   在 3D-Front 数据集上进行了场景级生成实验，并专门测试了遮挡情况下的表现。
    *   进行了消融实验（Tab III），验证了自适应门控（ASG）、原型库（PB）和预热阶段（Warm-up）的必要性。
*   **充分性评价**：实验覆盖了从单个物体到复杂场景的跨度，且包含了定量指标与定性可视化对比。通过 IoU 指标评估部件重叠，体现了对“结构化”这一核心目标的客观考量，实验设计较为充分。

### 6. 主要结论与发现
*   **自适应性**：模型能够根据图像内容自主推断合理的部件数量，解决了固定部件数导致的结构破碎问题。
*   **泛化性**：原型库显著提升了跨类别生成能力，使得模型在面对新颖布局时能利用共享的几何先验保持结构连贯。
*   **鲁棒性**：在处理遮挡和复杂布局（如室内场景）时，该模型比现有方法表现出更强的补全能力和更清晰的部件边界。

### 7. 优点（亮点）
*   **动态结构发现**：引入槽位门控机制，实现了 3D 部件数量的端到端自适应预测。
*   **先验共享机制**：原型库的设计巧妙地平衡了“类别无关的通用性”与“实例相关的精细度”。
*   **层级化建模**：从整体物体到自适应槽位再到几何原型的三级架构，更符合现实世界物体的构成逻辑。

### 8. 不足与局限
*   **计算开销**：虽然引入了门控，但维护多个潜在槽位和原型库可能会增加推理时的内存占用。
*   **数据依赖**：模型性能高度依赖于训练数据中部件分解的质量（如 IoU 过滤标准），对于缺乏清晰部件定义的自然场景可能适用性受限。
*   **算力透明度**：缺乏训练资源消耗的具体数据，难以评估其在大规模部署时的成本效益。

（完）
