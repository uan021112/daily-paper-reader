---
title: "GeoFusion-CAD: Structure-Aware Diffusion with Geometric State Space for Parametric 3D Design"
title_zh: GeoFusion-CAD：用于参数化三维设计的几何状态空间结构感知扩散模型
authors: "Xiaolei Zhou, Chuangjie Fang, Jie Wu, Jingyi Yang, Boyi Lin, Jianwei Zheng"
date: 2026-03-23
pdf: "https://arxiv.org/pdf/2603.21978v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 用于参数化3D CAD设计生成的端到端扩散框架
tldr: 针对参数化CAD生成中长序列建模难、计算开销大的问题，本文提出GeoFusion-CAD。该框架将CAD程序编码为层次树，并结合扩散模型与轻量化C-Mamba块（状态空间模型）来捕捉长程几何与拓扑依赖。通过引入DeepCAD-240长序列基准，实验证明该方法在保持几何保真度和拓扑一致性方面优于Transformer模型，为长序列CAD生成树立了新标杆。
selection_source: fresh_fetch
motivation: 现有的基于Transformer的CAD生成方法在处理具有复杂几何依赖的长命令序列时，面临计算开销大和上下文窗口受限的挑战。
method: 提出一种端到端的扩散框架，利用层次树编码CAD程序，并采用C-Mamba块通过选择性状态转移建模长程结构依赖。
result: 在长序列基准DeepCAD-240上，GeoFusion-CAD在几何保真度和拓扑一致性方面均显著优于现有的Transformer模型。
conclusion: 该研究通过结合扩散模型与状态空间模型，为可扩展且结构感知的下一代参数化CAD建模系统奠定了基础。
---

## 摘要
参数化计算机辅助设计（CAD）是现代三维建模的基础，然而现有方法在生成长命令序列时面临挑战，特别是在复杂的几何和拓扑依赖关系下。基于 Transformer 的架构因其强大的依赖建模能力在 CAD 序列生成中占据主导地位，但其二次方复杂度的注意力机制成本和有限的上下文窗口阻碍了其向长程序的扩展。我们提出了 GeoFusion-CAD，这是一个用于可扩展且结构感知生成的端到端扩散框架。该方案将 CAD 程序编码为分层树，在状态空间扩散过程中共同捕捉几何和拓扑信息。具体而言，轻量级的 C-Mamba 模块通过选择性状态转移对长程结构依赖进行建模，从而实现跨长命令序列的连贯生成。为了支持长序列评估，我们引入了 DeepCAD-240，这是一个扩展基准，在保留 ABC 数据集草图拉伸（sketch-extrusion）语义的同时，将序列长度增加到 40 至 240 之间。广泛的实验表明，GeoFusion-CAD 在短命令和长命令范围内均取得了优异的性能，在基于 Transformer 的模型性能下降的情况下，仍能保持高度的几何保真度和拓扑一致性。我们的方法在长序列参数化 CAD 生成方面刷新了最先进的性能指标，为下一代 CAD 建模系统奠定了可扩展的基础。代码和数据集已在 GitHub 上发布。

## Abstract
Parametric Computer-Aided Design (CAD) is fundamental to modern 3D modeling, yet existing methods struggle to generate long command sequences, especially under complex geometric and topological dependencies. Transformer-based architectures dominate CAD sequence generation due to their strong dependency modeling, but their quadratic attention cost and limited context windowing hinder scalability to long programs. We propose GeoFusion-CAD, an end-to-end diffusion framework for scalable and structure-aware generation. Our proposal encodes CAD programs as hierarchical trees, jointly capturing geometry and topology within a state-space diffusion process. Specifically, a lightweight C-Mamba block models long-range structural dependencies through selective state transitions, enabling coherent generation across extended command sequences. To support long-sequence evaluation, we introduce DeepCAD-240, an extended benchmark that increases the sequence length ranging from 40 to 240 while preserving sketch-extrusion semantics from the ABC dataset. Extensive experiments demonstrate that GeoFusion-CAD achieves superior performance on both short and long command ranges, maintaining high geometric fidelity and topological consistency where Transformer-based models degrade. Our approach sets new state-of-the-art scores for long-sequence parametric CAD generation, establishing a scalable foundation for next-generation CAD modeling systems. Code and datasets are available at GitHub.

---

## 论文详细总结（自动生成）

以下是对论文《GeoFusion-CAD: Structure-Aware Diffusion with Geometric State Space for Parametric 3D Design》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保证几何保真度和拓扑一致性的前提下，实现长序列参数化 CAD（计算机辅助设计）模型的自动生成。
*   **研究背景**：
    *   **Transformer 的局限性**：当前主流方法多采用 Transformer 架构，但其自注意力机制具有二次方复杂度（$O(L^2)$），难以扩展到包含数百条命令的长程序。
    *   **结构感知缺失**：全局注意力机制将所有 Token 等同对待，忽略了 CAD 数据中“草图-拉伸-面-边-顶点”的严格层次化拓扑关系，导致长序列生成时容易出现几何断裂或逻辑不连贯。
*   **研究动机**：开发一种既能高效处理长序列，又能显式建模 CAD 层次结构的生成框架。

### 2. 方法论
*   **核心思想**：结合**扩散模型（Diffusion）**的生成能力与**状态空间模型（SSM/Mamba）**的线性效率，通过层次树表示来捕捉 CAD 的几何与拓扑依赖。
*   **关键技术细节**：
    1.  **层次树表示（Hierarchical Tree）**：将 CAD 程序编码为树状结构，根节点为实体，子节点依次为草图/拉伸、面、环、边和顶点。这种结构避免了节点重复，保留了完整的建模历史。
    2.  **G-Mamba 扩散编码器**：
        *   采用线性复杂度的 **G-Mamba 块**替代 Transformer。
        *   **几何状态混合器（GSM）**：引入几何条件向量（包含尺度、层次深度、曲率等信息），使 SSM 的状态转移矩阵能够感知局部几何特征。
        *   **GSM-SSD 层**：在选择性状态空间（SSD）中集成几何特征融合，实现全局拓扑与局部几何的统一建模。
    3.  **扩散过程**：在分层树的潜在空间中进行去噪。前向过程逐步注入噪声，反向过程利用 G-Mamba 块逐层预测并消除噪声，最终由 CAD 解码器重建命令和参数。

### 3. 实验设计
*   **数据集**：
    *   **DeepCAD**：原始基准，命令长度限制在 60 以内。
    *   **DeepCAD-240（新提出）**：基于 ABC 数据集扩展，将命令长度提升至 40-240 步，包含约 21.5 万个模型，是目前最长、结构最复杂的参数化 CAD 数据集。
*   **对比方法**：DeepCAD、SkexGen、HNC-CAD 等基于 Transformer 的最先进模型。
*   **评估指标**：
    *   **准确性**：命令准确率（ACC cmd）、参数准确率（ACC param）。
    *   **几何质量**：覆盖率（COV）、最小匹配距离（MMD）、JSD 散度。
    *   **效率**：内存占用、计算量（FLOPs）。

### 4. 资源与算力
*   **硬件环境**：使用单张 **NVIDIA RTX 3090 GPU** 进行训练。
*   **训练细节**：
    *   Batch Size 为 512，训练 1000 个 Epoch。
    *   采用 AdamW 优化器，学习率为 $1 \times 10^{-4}$。
*   **效率优势**：在处理长序列时，GeoFusion-CAD 仅需 **5198MiB** 显存和 **34.6G FLOPs**，远低于 HNC-CAD（10342MiB / 87.3G）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在短序列（DeepCAD）和长序列（DeepCAD-240）两个量级上进行了全面对比。
    *   进行了详细的**消融实验**，验证了层次树表示、G-Mamba 块、以及不同架构（MLP、Transformer、Vanilla Mamba）对结果的影响。
*   **充分性评价**：实验设计较为充分且客观。通过引入新的长序列基准，填补了现有研究在长程序生成评估上的空白；对比实验涵盖了从生成精度到计算效率的多个维度，证明了该方法在长序列任务中的压倒性优势。

### 6. 主要结论与发现
*   **长序列性能卓越**：在 DeepCAD-240 任务中，GeoFusion-CAD 的命令准确率达到 91.2%，比最强基准 HNC-CAD 高出 8.4%。
*   **线性扩展性**：得益于 SSM 架构，模型在处理长达 240 步的序列时仍能保持稳定的显存消耗和生成质量，而 Transformer 模型在此时性能显著下降。
*   **拓扑一致性**：层次树表示显著提升了生成的 CAD 模型的物理有效性（如水密性），减少了表面畸变和断裂。

### 7. 优点（亮点）
*   **架构创新**：首次将 Mamba（SSM）引入 CAD 生成领域，并针对几何特性进行了定制化改造（GSM）。
*   **效率极高**：实现了 $O(L)$ 的线性复杂度，解决了 CAD 建模向复杂长程序扩展的瓶颈。
*   **端到端优化**：不同于以往分阶段训练的方法，该框架支持端到端的扩散优化，减少了特征对齐误差。
*   **贡献基准**：发布的 DeepCAD-240 数据集为后续长序列 CAD 研究提供了重要工具。

### 8. 不足与局限
*   **操作类型限制**：目前主要聚焦于“草图-拉伸（Sketch-Extrusion）”范式，对于旋转（Revolution）、放样（Sweep）等更复杂的参数化操作覆盖较少。
*   **局部细节微瑕**：在处理极高复杂度的表面时，偶尔仍会出现细微的边界不规则现象。
*   **数据集偏差**：虽然使用了 ABC 数据集，但生成结果仍受限于训练数据的设计风格和分布。

（完）
