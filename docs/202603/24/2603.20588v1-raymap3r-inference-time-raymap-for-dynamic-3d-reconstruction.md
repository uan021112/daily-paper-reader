---
title: "RayMap3R: Inference-Time RayMap for Dynamic 3D Reconstruction"
title_zh: RayMap3R：用于动态三维重建的推理时 RayMap
authors: "Feiran Wang, Zezhou Shang, Gaowen Liu, Yan Yan"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20588v1"
tags: ["query:ff3dr"]
score: 10.0
evidence: 基于RGB图像的流式前馈3D重建
tldr: RayMap3R是一个无需训练的流式动态三维重建框架，旨在解决移动物体在实时重建中引起的伪影和位姿漂移问题。该方法发现RayMap预测具有天然的静态场景偏差，据此构建了双分支推理方案，通过对比RayMap与图像预测来识别并抑制动态干扰。此外，引入了重置度量对齐和状态感知平滑技术以增强一致性，在多个动态场景基准测试中取得了领先的重建性能。
selection_source: fresh_fetch
motivation: 传统的流式三维重建模型在面对动态场景中的移动物体时，往往会产生严重的几何伪影和相机轨迹漂移。
method: 利用RayMap的静态偏差特性，通过双分支对比识别动态区域，并结合度量对齐与平滑技术优化内存更新和轨迹预测。
result: 该方法在多个动态场景重建基准测试中均优于现有的流式重建算法，显著提升了重建精度和鲁棒性。
conclusion: RayMap3R通过推理时的动态感知机制，在无需重新训练的情况下有效解决了流式三维重建中的动态干扰问题。
---

## 摘要
流式前馈三维重建能够从 RGB 图像中实时联合估计场景几何和相机位姿。然而，由于缺乏显式的动态推理，流式模型容易受到移动物体的影响，从而导致伪影和漂移。在这项工作中，我们提出了 RayMap3R，这是一个用于动态场景重建的无需训练的流式框架。我们观察到，基于 RayMap 的预测表现出静态场景偏置，这为动态识别提供了内部线索。基于这一观察，我们构建了一个双分支推理方案，通过对比 RayMap 和图像预测来识别动态区域，从而在记忆更新过程中抑制其干扰。我们进一步引入了重置度量对齐和状态感知平滑，以保持度量一致性并稳定预测轨迹。在多个基准测试中，我们的方法在动态场景重建的流式方法中达到了最先进的性能。

## Abstract
Streaming feed-forward 3D reconstruction enables real-time joint estimation of scene geometry and camera poses from RGB images. However, without explicit dynamic reasoning, streaming models can be affected by moving objects, causing artifacts and drift. In this work, we propose RayMap3R, a training-free streaming framework for dynamic scene reconstruction. We observe that RayMap-based predictions exhibit a static-scene bias, providing an internal cue for dynamic identification. Based on this observation, we construct a dual-branch inference scheme that identifies dynamic regions by contrasting RayMap and image predictions, suppressing their interference during memory updates. We further introduce reset metric alignment and state-aware smoothing to preserve metric consistency and stabilize predicted trajectories. Our method achieves state-of-the-art performance among streaming approaches on dynamic scene reconstruction across multiple benchmarks.

---

## 论文详细总结（自动生成）

这是一份关于论文《RayMap3R: Inference-Time RayMap for Dynamic 3D Reconstruction》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：流式前馈三维重建（Streaming 3D Reconstruction）旨在从连续图像流中实时估计几何和位姿。然而，现有模型在面对**动态场景（如移动物体）**时，由于缺乏显式的动态推理机制，容易产生严重的几何伪影和相机轨迹漂移。
*   **研究动机**：现有的动态处理方法通常依赖外部模块（如光流估计或分割模型），增加了计算开销。作者发现基于 RayMap（像素级相机射线表示）的模型在仅给定 RayMap 特征时，预测结果具有天然的**静态场景偏置（Static-scene Bias）**，即倾向于重建稳定的背景而抑制瞬时的动态物体。

### 2. 方法论：核心思想与关键技术
RayMap3R 是一个**无需训练（Training-free）**的推理时框架，其核心流程如下：
*   **双分支推理方案（Dual-branch Inference）**：
    *   **主分支**：同时输入图像特征和 RayMap 特征，预测包含动态物体的完整场景深度和位姿。
    *   **RayMap 分支**：仅输入基于预测位姿生成的 RayMap 特征。利用其静态偏置，该分支预测的深度图更倾向于背景。
*   **动态识别与记忆更新**：
    *   通过对比两个分支的深度差异（$\delta$）和置信度，计算**静态权重（Staticness Weights, $\alpha_t$）**。
    *   使用 $\alpha_t$ 对隐式记忆状态（State）的更新进行门控（$s_t = s_{t-1} + \alpha_t \odot \Delta s_t$），从而在记忆中过滤掉动态干扰。
*   **重置度量对齐（Reset Metric Alignment）**：
    *   针对长序列重建中的内存重置问题，通过估计重置前后重复帧的 Sim(3) 变换，修正尺度不一致和位姿偏移。
*   **状态感知平滑（State-aware Smoothing）**：
    *   结合轨迹加速度和内部状态变化幅度，自适应地计算平滑系数 $\beta_t$，在线稳定预测轨迹，减少噪声。

### 3. 实验设计
*   **数据集与场景**：
    *   **动态场景**：MPI Sintel（合成）、TUM-Dynamics（真实）、KITTI（户外）、Bonn（真实室内）。
    *   **静态场景**：ScanNet、7-Scenes。
*   **评估任务（Benchmark）**：视频深度估计（Abs Rel, $\delta < 1.25$）、相机位姿估计（ATE, RPE）、三维重建质量（Accuracy, Completion, Chamfer distance）。
*   **对比方法**：
    *   **流式方法**：Spann3R, CUT3R, Point3R, StreamVGGT, TTT3R。
    *   **离线方法**（作为上限参考）：DUSt3R, MASt3R, MonST3R, VGGT。

### 4. 资源与算力
*   **硬件环境**：所有实验均在单张 **NVIDIA RTX A6000 (48GB)** GPU 上完成。
*   **训练时长**：该方法是**推理时（Inference-time）**框架，直接构建在预训练的 CUT3R 模型之上，**无需额外训练**。
*   **效率**：在 ScanNet 上达到约 **13.8 FPS**，且内存占用保持恒定（约 9.4 GB），不随序列长度增加。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从合成到真实、从室内到户外的 6 个主流数据集，总计处理了数千帧图像。
*   **消融实验**：详细验证了双分支识别（R）、度量对齐（M）和状态平滑（S）三个组件的贡献。
*   **充分性评价**：实验设计非常充分。作者不仅对比了精度，还分析了推理速度、内存随时间的变化，并对“静态偏置”这一核心假设进行了定量分析（通过 108 个序列验证了深度差异与动态掩码的相关性）。对比实验涵盖了最新的 SOTA 流式模型，评估标准客观公平。

### 6. 主要结论与发现
*   **性能领先**：RayMap3R 在动态场景下的位姿估计和深度预测精度显著优于现有的流式方法。
*   **静态偏置的有效性**：实验证实 RayMap-only 预测确实能作为一种无需监督的信号来识别动态区域。
*   **长序列稳定性**：引入的重置对齐和平滑机制有效解决了流式模型在长序列中的尺度漂移和轨迹抖动问题。

### 7. 优点
*   **无需训练**：不需要动态场景的标注数据进行微调，具有极强的通用性。
*   **即插即用**：可以作为一种推理策略应用到其他基于 RayMap 的前馈模型中。
*   **鲁棒性强**：在保持静态场景精度的同时，大幅提升了在复杂动态环境下的生存能力。
*   **内存友好**：采用隐式状态设计，支持无限长序列的实时处理。

### 8. 不足与局限
*   **模型依赖**：该方法的效果高度依赖于预训练骨干网络（如 CUT3R）是否在训练中形成了足够的几何感知能力。如果骨干网络本身对静态/动态区分度不高，该方法效果会受限。
*   **计算开销**：双分支推理虽然保证了实时性，但相比单分支模型（如 CUT3R）仍增加了约 30% 的计算延迟。
*   **极端场景**：在动态物体占据绝大部分画面的极端情况下，静态偏置信号可能会被削弱，导致识别失效。

（完）
