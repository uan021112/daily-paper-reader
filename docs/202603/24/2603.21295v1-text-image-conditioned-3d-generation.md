---
title: Text-Image Conditioned 3D Generation
title_zh: 文本-图像条件的 3D 生成
authors: "Jiazhong Cen, Jiemin Fang, Sikuang Li, Guanjun Wu, Chen Yang, Taoran Yi, Zanwei Zhou, Zhikuan Bao, Lingxi Xie, Wei Shen, Qi Tian"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21295v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 结合文本和图像模态进行3D资产生成
tldr: 针对现有3D生成模型仅依赖单一模态（文本或图像）导致视角偏差或细节缺失的问题，本文提出了文本-图像联合条件的3D生成任务。研究发现两种模态具有强互补性，并据此开发了TIGON模型。该模型采用双支路架构，分别处理图像和文本输入，并通过轻量级融合机制实现联合推理。实验证明，这种多模态引导显著提升了3D生成的灵活性与保真度，为未来研究提供了新方向。
selection_source: fresh_fetch
motivation: 现有的3D生成模型受限于单一模态，图像条件易产生视角偏差，而文本条件则缺乏底层视觉细节。
method: 提出了TIGON模型，采用包含独立图像和文本骨干网络的双支路架构，并结合轻量级跨模态融合模块。
result: 实验表明，文本-图像联合条件生成的表现一致优于单一模态方法，有效结合了语义引导与视觉保真度。
conclusion: 视觉与语言的互补引导是提升3D生成质量的关键，为更灵活、忠实的用户意图表达开辟了新路径。
---

## 摘要
高质量 3D 资产对于 VR/AR、工业设计和娱乐至关重要，这激发了人们对根据用户提示创建 3D 内容的生成模型的日益增长的兴趣。然而，大多数现有的 3D 生成器依赖于单一的条件模态：图像条件模型通过利用像素对齐的线索实现了高视觉保真度，但在输入视角受限或模糊时会受到视角偏差的影响；而文本条件模型提供了广泛的语义指导，但缺乏底层视觉细节。这限制了用户表达意图的方式，并提出了一个自然的问题：这两种模态能否结合起来，以实现更灵活、更忠实的 3D 生成？我们的诊断性研究表明，即使是文本和图像条件预测的简单后期融合，其表现也优于单模态模型，揭示了强大的跨模态互补性。因此，我们正式定义了文本-图像条件的 3D 生成任务，该任务需要对视觉示例和文本规范进行联合推理。为了解决这一任务，我们引入了 TIGON，这是一个极简的双分支基准模型，具有独立的图像和文本条件主干网络以及轻量级的跨模态融合。大量实验表明，文本-图像条件化方法始终优于单模态方法，突显了互补的视觉-语言引导是未来 3D 生成研究的一个有前景的方向。项目页面：https://jumpat.github.io/tigon-page

## Abstract
High-quality 3D assets are essential for VR/AR, industrial design, and entertainment, motivating growing interest in generative models that create 3D content from user prompts. Most existing 3D generators, however, rely on a single conditioning modality: image-conditioned models achieve high visual fidelity by exploiting pixel-aligned cues but suffer from viewpoint bias when the input view is limited or ambiguous, while text-conditioned models provide broad semantic guidance yet lack low-level visual detail. This limits how users can express intent and raises a natural question: can these two modalities be combined for more flexible and faithful 3D generation? Our diagnostic study shows that even simple late fusion of text- and image-conditioned predictions outperforms single-modality models, revealing strong cross-modal complementarity. We therefore formalize Text-Image Conditioned 3D Generation, which requires joint reasoning over a visual exemplar and a textual specification. To address this task, we introduce TIGON, a minimalist dual-branch baseline with separate image- and text-conditioned backbones and lightweight cross-modal fusion. Extensive experiments show that text-image conditioning consistently improves over single-modality methods, highlighting complementary vision-language guidance as a promising direction for future 3D generation research. Project page: https://jumpat.github.io/tigon-page

---

## 论文详细总结（自动生成）

这是一份关于论文《Text-Image Conditioned 3D Generation》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 3D 生成模型大多依赖单一模态（仅图像或仅文本），这限制了生成的灵活性和准确性。
    *   **图像条件（Image-only）**：虽能提供像素级细节，但极度依赖视角。当输入视角包含遮挡或信息不足时，模型必须“幻觉”出缺失部分，导致结果偏离用户意图（视角偏差）。
    *   **文本条件（Text-only）**：能提供全局语义指导，但缺乏底层视觉细节（如精确的纹理和几何形状）。
*   **研究动机**：探索文本和图像模态是否具有互补性，并正式定义“文本-图像联合条件 3D 生成”任务，旨在结合图像的视觉保真度与文本的语义消歧能力。

### 2. 方法论：TIGON 模型
TIGON（Text-Image conditioned GeneratiON）是一个极简的双分支基准模型，核心思想是保留模态特异性的处理能力，同时进行轻量级融合。
*   **双分支架构**：采用两个并行的 Diffusion Transformer (DiT) 骨干网络，分别处理图像特征（由 DINOv2 提取）和文本特征（由 CLIP 提取）。
*   **早期融合（Early Fusion）**：在两个分支的对应 DiT 块之间插入“跨模态桥接器”（Cross-modal bridges）。这些桥接器使用**零初始化线性层（Zero Linears）**，确保训练初期稳定性，并允许模型逐渐学习模态间的特征交换。
*   **后期融合（Late Fusion）**：在推理的每个去噪步骤中，将图像分支和文本分支预测的流场速度（Velocity field $v$）进行简单加权平均。
*   **训练策略**：
    1.  **分阶段训练**：先分别预训练单模态分支，再进行联合微调。
    2.  **条件丢弃（Condition Dropout）**：训练时以 50% 概率独立丢弃图像或文本条件，使模型能够支持纯文本、纯图像或联合条件的自由形式推理。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：TRELLIS-500K（包含来自 Objaverse、ABO、3D-FUTURE 等的高质量 3D 资产，并由 GPT-4o 生成详细描述）。
    *   **测试集**：Toys4K（105 类高质量对象）和 UniLat1K（包含 Sketchfab 和 Toys4K 的混合硬案例）。
*   **Benchmark 与对比方法**：
    *   对比了 TripoSR、TRELLIS、UniLat3D、Step1X-3D、Hunyuan3D-2.1、Direct3D-S2 等前沿模型。
    *   设计了“诊断性研究”，对比不同视角（View-0 理想视角 vs View-1 差视角）下的表现。
*   **评估指标**：渲染图的 CLIP 相似度、FD DINOv2（分布相似度）、ULIP 和 Uni3D（点云-图像对齐度）。

### 4. 资源与算力
*   **算力投入**：使用了 **64 台 NVIDIA A800 GPU**。
*   **训练细节**：
    *   文本分支预训练：1,000,000 次迭代，Batch Size 为 256。
    *   联合微调：50,000 次迭代，学习率为 $1 \times 10^{-5}$。
    *   技术栈：PyTorch、DeepSpeed ZeRO-2、FlashAttention、BF16 精度。

### 5. 实验数量与充分性
*   **实验规模**：论文涵盖了大规模定量对比（两个主流测试集）、定性视觉对比、以及针对核心组件的消融实验。
*   **充分性评价**：
    *   **充分性**：实验不仅对比了 SOTA 模型，还通过改变视角信息量验证了多模态的必要性。
    *   **客观性**：使用了统一的渲染协议和多种 3D 感知指标，并对比了简单的“后期融合”基准（SimFusion），证明了 TIGON 设计的有效性而非仅仅是参数量堆砌。
    *   **公平性**：在相同的潜在空间（Latent Space）和骨干网络下进行对比，确保了结论的可靠性。

### 6. 主要结论与发现
*   **互补性显著**：即使是简单的预测平均（SimFusion），在差视角下的表现也远超单模态模型。
*   **TIGON 的优越性**：通过早期桥接和联合微调，TIGON 在保持单模态生成能力的同时，在联合条件下达到了最高的保真度和语义一致性。
*   **视角鲁棒性**：文本条件能有效修正图像视角不佳带来的几何畸变或幻觉错误。
*   **可控性**：用户可以通过改变文本提示词，在保持参考图像主体特征的同时，灵活调整 3D 资产的局部属性。

### 7. 优点与亮点
*   **任务定义前瞻**：正式化了文本-图像联合驱动的 3D 生成任务，符合实际工业应用需求。
*   **设计极简高效**：没有采用复杂的交叉注意力机制，而是通过零初始化的线性桥接实现了稳定的跨模态推理。
*   **灵活性高**：一个模型同时兼容三种输入模式（图、文、图+文），具有极强的实用价值。

### 8. 不足与局限
*   **冲突处理**：当图像和文本信息发生显式冲突时，模型倾向于优先遵循图像（因为图像信息更具体），这可能并不总是符合用户意图。
*   **数据依赖**：模型性能高度依赖于高质量的 3D-文本对数据集（如 TRELLIS-500K），这类数据的获取成本极高。
*   **计算开销**：双分支架构虽然提升了效果，但在推理时相比单分支模型增加了计算负担和内存占用。

（完）
