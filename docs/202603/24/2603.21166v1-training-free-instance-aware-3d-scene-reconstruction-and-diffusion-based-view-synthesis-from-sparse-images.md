---
title: Training-Free Instance-Aware 3D Scene Reconstruction and Diffusion-Based View Synthesis from Sparse Images
title_zh: 无需训练的实例感知三维场景重建及基于扩散模型的稀疏图像视图合成
authors: "Jiatong Xia, Lingqiao Liu"
date: 2026-03-22
pdf: "https://arxiv.org/pdf/2603.21166v1"
tags: ["query:ff3dr"]
score: 9.0
evidence: 从稀疏图像进行无需训练的 3D 场景重建
tldr: 本研究提出了一种无需训练的室内场景3D重建与渲染系统，仅需少量无位姿RGB图像即可实现。该系统通过基于扭曲的异常移除策略构建鲁棒点云，利用2D到3D实例提升机制实现实例感知，并结合3D感知扩散模型优化渲染效果。该方法不仅能有效填补几何缺失，还支持无需重新训练的对象级场景编辑（如实例删除），为高效、可编辑的3D内容生成提供了新方向。
selection_source: fresh_fetch
motivation: 传统辐射场方法依赖密集视图和逐场景优化，难以处理稀疏输入且缺乏灵活的实例级编辑能力。
method: 集成点云异常过滤、2D到3D实例传播以及基于扩散模型的视图细化技术，构建了一个无需训练的端到端流水线。
result: 在稀疏输入下实现了高保真渲染，并展示了自然支持实例移除等对象级场景编辑任务的能力。
conclusion: 该系统证明了结合点云几何与生成式扩散模型可以在无需场景特定优化的情况下，实现高效且可编辑的3D场景重建。
---

## 摘要
我们介绍了一种新颖的、无需训练的系统，用于从一组稀疏的、无位姿的 RGB 图像中重建、理解和渲染三维室内场景。与需要密集视图和逐场景优化的传统辐射场方法不同，我们的流水线在无需任何训练或位姿预处理的情况下实现了高保真结果。该系统集成了三项关键创新：(1) 一个鲁棒的点云重建模块，利用基于扭曲（warping）的异常移除策略过滤不可靠的几何结构；(2) 一种扭曲引导的 2D 到 3D 实例提升机制，将 2D 分割掩码传播为一致的、实例感知的 3D 表示；(3) 一种新颖的渲染方法，将点云投影到新视图中，并使用 3D 感知扩散模型对渲染结果进行精细化。我们的方法利用扩散模型的生成能力来弥补缺失的几何结构并增强真实感，特别是在稀疏输入条件下。我们进一步证明，通过仅修改点云，我们的流水线可以自然地支持物体级场景编辑（如实例移除），从而在无需重新训练的情况下合成一致的、编辑后的视图。我们的结果为不依赖于特定场景优化的、高效且可编辑的三维内容生成确立了一个新方向。

## Abstract
We introduce a novel, training-free system for reconstructing, understanding, and rendering 3D indoor scenes from a sparse set of unposed RGB images. Unlike traditional radiance field approaches that require dense views and per-scene optimization, our pipeline achieves high-fidelity results without any training or pose preprocessing. The system integrates three key innovations: (1) A robust point cloud reconstruction module that filters unreliable geometry using a warping-based anomaly removal strategy; (2) A warping-guided 2D-to-3D instance lifting mechanism that propagates 2D segmentation masks into a consistent, instance-aware 3D representation; and (3) A novel rendering approach that projects the point cloud into new views and refines the renderings with a 3D-aware diffusion model. Our method leverages the generative power of diffusion to compensate for missing geometry and enhances realism, especially under sparse input conditions. We further demonstrate that object-level scene editing such as instance removal can be naturally supported in our pipeline by modifying only the point cloud, enabling the synthesis of consistent, edited views without retraining. Our results establish a new direction for efficient, editable 3D content generation without relying on scene-specific optimization. Project page: https://jiatongxia.github.io/TID3R/

---

## 论文详细总结（自动生成）

以下是对论文《Training-Free Instance-Aware 3D Scene Reconstruction and Diffusion-Based View Synthesis from Sparse Images》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在**无需场景特定训练（Training-Free）**、**无需预知相机位姿**且**输入图像极度稀疏**（通常仅10-20张）的情况下，实现高质量的3D场景重建、实例级语义理解及照片级的新视图合成。
*   **研究动机**：
    *   传统的辐射场方法（如 NeRF, 3DGS）依赖密集视图和耗时的逐场景优化，在稀疏输入下易产生伪影。
    *   现有的稀疏视图方法往往缺乏对场景实例的结构化理解，难以支持物体级的交互式编辑（如删除或移动物体）。
    *   旨在建立一个端到端的流水线，将几何重建、语义感知和生成式渲染结合起来。

### 2. 方法论
该系统（TID3R）由三个核心模块组成：
*   **鲁棒几何重建**：
    *   利用前馈模型（如 MV-DUSt3R）从无位姿图像预测初始点云。
    *   **关键技术：基于扭曲（Warping）的异常点消除**。通过多视图深度一致性检查，对比投影深度与原生深度，若差异超过阈值 $\tau$，则判定为离群点并剔除，从而获得干净的场景点云。
*   **实例感知感知（2D-to-3D）**：
    *   使用基础模型（如 SAM）生成单帧 2D 掩码。
    *   **关键技术：基于扭曲的实例统一策略**。不依赖时间跟踪，而是利用几何投影将不同视图的掩码映射到 3D 空间，通过计算交并比（mIoU）合并属于同一物体的点组，实现跨视图的一致性实例标注。
*   **基于扩散模型的渲染流水线**：
    *   将 3D 点云投影到目标视图生成稀疏 RGB 图。
    *   **关键技术：3D 感知扩散细化**。使用预训练的 See3D 模型，以原始图像为参考，对投影产生的空洞和缺失几何进行修复（Inpainting），生成高保真图像。
    *   **编辑支持**：支持物体删除。在渲染编辑后的场景时，通过“参考掩码策略”遮盖参考图中已删除物体的区域，确保扩散模型生成的一致性。

### 3. 实验设计
*   **数据集**：主要在 **ScanNet** 和 **ScanNet++** 室内场景数据集上进行评估。
*   **Benchmark 与对比方法**：
    *   **实例分割**：对比了 DEVA 和 PE3R。
    *   **深度估计**：对比了 DepthAnythingV2、PE3R 和 MV-DUSt3R+。
    *   **视图合成**：对比了 3DGS、2DGS、GaussianGrouping、DNGaussian、CoR-GS、SE-GS（针对稀疏视图优化的 GS 方法）以及 LVSM（基于扩散的方法）。
*   **评估指标**：AP/AP50/AP75（分割）；RMSE/$\delta < 1.25$（深度）；PSNR/SSIM/LPIPS（渲染质量）。

### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA 4090 GPU** 上运行。
*   **耗时**：
    *   该方法无需训练，总处理时间约为 **3.5 分钟**（其中位姿处理与重建约 1 分钟，10 个测试视图的推理约 2.5 分钟）。
    *   相比之下，NeRF 需要约 150 分钟，3DGS 需要约 9 分钟（含位姿预处理）。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个 ScanNet 场景上做分割评估，在 7 个 ScanNet/ScanNet++ 场景上做深度和渲染评估。
*   **消融实验**：针对“实例统一策略”、“异常点消除”以及“渲染掩码”分别做了对比实验，验证了各组件对最终质量的提升。
*   **充分性评价**：实验覆盖了重建、感知、渲染和编辑四个维度，对比了当前最先进的（SOTA）稀疏视图 3DGS 变体。虽然场景数量不算海量，但涵盖了复杂的室内环境，实验设计逻辑严密，对比公平（均在稀疏输入条件下）。

### 6. 主要结论与发现
*   **性能领先**：在稀疏视图下，该方法在 PSNR 和 LPIPS 等指标上显著优于需要优化的 3DGS 类方法。
*   **无需训练的优势**：证明了结合“几何点云”与“生成式扩散先验”可以绕过复杂的逐场景优化过程。
*   **编辑灵活性**：通过直接操作点云即可实现一致的物体删除和移动，且渲染效果保持了极高的纹理一致性。

### 7. 优点
*   **高效性**：完全无需训练，部署速度极快。
*   **鲁棒性**：通过几何一致性检查有效解决了前馈重建模型常见的噪声问题。
*   **多功能性**：统一了重建、分割和编辑任务，尤其是支持实例级的场景操作。
*   **低门槛**：不需要相机内参或外参作为输入，对随手拍摄的图像非常友好。

### 8. 不足与局限
*   **非实时渲染**：由于依赖扩散模型进行逐帧细化，目前无法达到实时渲染速度（每个视图需数秒）。
*   **位姿依赖性**：虽然不需要输入位姿，但渲染质量仍受限于前馈网络预测位姿的精度，位姿误差会直接导致渲染对齐失败。
*   **场景限制**：目前主要针对室内场景验证，在超大规模室外场景或极度空旷环境下的表现尚待观察。

（完）
