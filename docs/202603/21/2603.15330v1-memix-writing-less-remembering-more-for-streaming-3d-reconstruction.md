---
title: "MeMix: Writing Less, Remembering More for Streaming 3D Reconstruction"
title_zh: MeMix：少写多记，助力流式三维重建
authors: "Jiacheng Dong, Huan Li, Sicheng Zhou, Wenhao Hu, Weili Xu, Yan Wang"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15330v1"
tags: ["query:ff3dr"]
score: 9.0
evidence: 高效流式3D重建
tldr: 针对流式3D重建中循环模型在长序列下容易出现状态漂移和遗忘的问题，本文提出了MeMix。这是一个无需训练、即插即用的模块，通过将循环状态划分为多个独立的记忆补丁，并仅更新对齐程度最低的部分，从而在保持常数级推理内存的同时有效缓解灾难性遗忘。实验表明，该方法在多个标准基准测试中显著提升了重建的完整性，且无需任何微调或额外参数。
selection_source: fresh_fetch
motivation: 现有的循环在线3D重建模型在处理长序列时，常因状态漂移和遗忘导致重建质量随时间推移而逐渐恶化。
method: 提出一种记忆混合机制，将循环状态划分为多个独立补丁，仅更新与当前输入最不匹配的部分以保留关键历史信息。
result: "在ScanNet和7-Scenes等基准测试中，MeMix在相同推理设置下平均降低了15.3%的重建完整性误差，最高提升达40%。"
conclusion: MeMix作为一种高效且通用的即插即用模块，为提升流式3D重建的长期记忆能力提供了一种无需微调的新方案。
---

## 摘要
重建是三维视觉中的一项基础任务，也是空间智能的一项基本能力。特别是，流式三维重建对于实时空间感知至关重要，然而现有的循环在线模型在长序列上往往会因为状态漂移和遗忘而遭受渐进式性能退化，这促使了推理阶段补救措施的研究。我们提出了 MeMix，这是一个无需训练、即插即用的模块，它通过将循环状态重构为“记忆混合”（Memory Mixture）来改进流式重建。MeMix 将状态划分为多个独立的记忆补丁（memory patches），并仅更新对齐程度最低的记忆补丁，同时精确保留其他补丁。这种选择性更新在保持 O(1) 推理内存的同时缓解了灾难性遗忘，且不需要微调或额外的可学习参数，使其能够直接应用于现有的循环重建模型。在标准基准测试（ScanNet、7-Scenes、KITTI 等）中，在相同的主干网络和推理设置下，MeMix 在 7-Scenes 的 300-500 帧流上平均降低了 15.3% 的重建完整性误差（最高可达 40.0%）。代码可在 https://dongjiacheng06.github.io/MeMix/ 获取。

## Abstract
Reconstruction is a fundamental task in 3D vision and a fundamental capability for spatial intelligence. Particularly, streaming 3D reconstruction is central to real-time spatial perception, yet existing recurrent online models often suffer from progressive degradation on long sequences due to state drift and forgetting, motivating inference-time remedies. We present MeMix, a training-free, plug-and-play module that improves streaming reconstruction by recasting the recurrent state into a Memory Mixture. MeMix partitions the state into multiple independent memory patches and updates only the least-aligned memory patches while exactly preserving others. This selective update mitigates catastrophic forgetting while retaining $O(1)$ inference memory, and requires no fine-tuning or additional learnable parameters, making it directly applicable to existing recurrent reconstruction models. Across standard benchmarks (ScanNet, 7-Scenes, KITTI, etc.), under identical backbones and inference settings, MeMix reduces reconstruction completeness error by 15.3% on average (up to 40.0%) across 300--500 frame streams on 7-Scenes. The code is available at https://dongjiacheng06.github.io/MeMix/

---

## 论文详细总结（自动生成）

以下是对论文《MeMix: Writing Less, Remembering More for Streaming 3D Reconstruction》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：流式三维重建（Streaming 3D Reconstruction）是实时空间感知的核心。现有的在线模型主要分为两类：基于 KV 缓存的模型（内存随序列长度线性增长，易导致 OOM）和固定状态的循环模型（如 CUT3R，内存恒定但存在“灾难性遗忘”）。
*   **核心问题**：在循环模型中，每一帧都会完全重写循环状态（Recurrent State），导致历史信息被新信息覆盖，从而在长序列处理中出现几何漂移、姿态误差累积和一致性下降。
*   **整体含义**：本文旨在寻找一种在保持常数级内存（O(1)）的同时，能够有效保留长期记忆并减少状态漂移的推理阶段补救方案。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：提出 **MeMix** 模块，将循环状态视为“记忆混合体”（Memory Mixture）。通过将状态划分为多个独立的记忆补丁（Memory Patches），在每个时间步仅更新对齐程度最低（Bottom-k）的补丁，而精确保留其他补丁。
*   **关键技术细节**：
    *   **记忆分区**：将状态 $S$ 划分为 $p$ 个独立的补丁。
    *   **选择性更新（Bottom-k 策略）**：计算候选状态与当前图像特征之间的点积相似度得分。选择得分最低的 $k$ 个补丁进行更新，因为这些补丁与当前观测最不匹配，更新它们对保留已有重要记忆的干扰最小。
    *   **稀疏路由掩码**：生成一个二进制掩码 $M_t$，仅对选中的补丁执行写操作：$S_t = M_t \odot \hat{S}_t + (1 - M_t) \odot S_{t-1}$。
    *   **即插即用**：该模块无需训练，不增加额外参数，可直接集成到 CUT3R、TTT3R 等现有循环模型中。

### 3. 实验设计
*   **数据集与场景**：
    *   **三维重建**：7-Scenes, NRGBD（测试 300/400/500 帧的长序列）。
    *   **相机姿态估计**：TUM, ScanNet, Sintel。
    *   **视频深度估计**：KITTI, Bonn, Sintel。
*   **Benchmark 与对比方法**：
    *   **离线模型**：VGGT, DUSt3R, MASt3R 等。
    *   **在线/流式模型**：StreamVGGT, STREAM3R, Point3R, 以及基础循环模型 CUT3R, TTT3R, TTSA3R。
*   **评价指标**：准确度（Acc）、完整性（Comp）、法线一致性（NC）、绝对轨迹误差（ATE）、相对位移误差（RPE）等。

### 4. 资源与算力
*   **硬件环境**：实验在单张 **NVIDIA A100 (40GB)** 或 **RTX 4090 (24GB)** 上运行。
*   **算力开销**：由于是无需训练（Training-free）的方法，不存在训练时长。在推理阶段，MeMix 的引入对 FPS（帧率）和峰值显存的影响几乎可以忽略不计（例如在 CUT3R 上，FPS 仅从 14.39 降至 14.13）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 7 个主流数据集，针对重建、姿态、深度三个任务进行了全面测试。
*   **消融实验**：非常充分。作者对比了：
    *   **路由策略**：Top-k vs. Bottom-k vs. Random-k（证明了 Bottom-k 最优，因为它能更均匀地分布更新）。
    *   **评分函数**：余弦相似度 vs. 点积 vs. 注意力得分。
    *   **参数敏感性**：对 $k$ 值的选取进行了详细的网格搜索（发现 $k=708/768$ 时效果最佳）。
*   **客观性**：在相同的主干网络、分辨率和推理设置下进行对比，确保了实验的公平性。

### 6. 主要结论与发现
*   **性能提升**：MeMix 在 7-Scenes 长序列上平均降低了 **15.3%** 的重建完整性误差，最高提升达 **40%**。
*   **长程稳定性**：随着输入帧数增加（从 50 帧到 1000 帧），MeMix 相比基准模型的优势愈发明显，有效缓解了状态退化。
*   **Bottom-k 的优越性**：发现 Top-k 会导致部分 Token 被过度更新而另一部分被闲置，而 Bottom-k 策略能实现更平衡的状态利用。

### 7. 优点
*   **无需训练**：作为插件模块，无需昂贵的重训练或微调过程。
*   **通用性强**：能够无缝集成到多种不同的循环神经网络架构中。
*   **高效性**：在保持 $O(1)$ 内存复杂度的同时，显著提升了长序列重建的质量。
*   **鲁棒性**：在多个任务和数据集上表现出一致的改进。

### 8. 不足与局限
*   **超参数依赖**：$k$ 值的选取目前主要依靠启发式搜索，缺乏自动化的自适应机制。
*   **极端尺度测试不足**：虽然测试了 1000 帧，但对于公里级、数万帧的超长序列导航场景，其表现尚待验证。
*   **可解释性**：对于为什么 Bottom-k 在所有几何任务中都优于 Top-k，虽然有直观解释，但缺乏更深层的理论证明。

（完）
