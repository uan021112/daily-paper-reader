---
title: "S2GS: Streaming Semantic Gaussian Splatting for Online Scene Understanding and Reconstruction"
title_zh: S2GS：用于在线场景理解与重建的流式语义高斯泼溅
authors: "Renhe Zhang, Yuyang Tan, Jingyu Gong, Zhizhong Zhang, Lizhuang Ma, Yuan Xie, Xin Tan"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14232v1"
tags: ["query:ff3dr"]
score: 9.0
evidence: 在线前馈联合场景理解与重建
tldr: 针对长视频流场景理解与重建中全局计算导致的显存和耗时激增问题，本文提出S2GS框架。该框架采用因果增量式3D高斯语义场，无需重新处理历史帧即可持续更新几何、外观及实例语义。通过几何-语义解耦的双骨干设计，结合2D基础模型与实例存储机制，实现了高效的在线联合重建与理解，在长序列扩展性上显著优于传统离线方法。
selection_source: fresh_fetch
motivation: 现有的离线方法在处理长图像流时因重复全局计算导致运行时间和显存随序列长度激增，限制了大规模场景的应用。
method: 提出一种因果增量式3D高斯语义场框架，利用几何-语义解耦的双骨干设计和实例存储实现无需回溯历史帧的在线更新。
result: 在联合重建与理解基准上达到或超越了强离线基线，且能处理超过1000帧的序列，而离线基线在80帧左右即显存溢出。
conclusion: S2GS通过流式处理机制解决了长时场景理解的扩展性难题，为在线大规模场景重建与语义分割提供了高效方案。
---

## 摘要
现有的针对长图像流进行联合场景理解与重建的离线前馈方法，通常会对不断增长的历史观测集重复进行全局计算，导致运行时间和 GPU 显存随序列长度增加而迅速增长，从而限制了可扩展性。我们提出了流式语义高斯泼溅（Streaming Semantic Gaussian Splatting，简称 S2GS），这是一个严格因果且增量的 3D 高斯语义场框架：它不利用未来帧，并在不重新处理历史帧的情况下持续更新场景几何、外观和实例级语义，从而实现了可扩展的在线联合重建与理解。S2GS 采用了几何-语义解耦的双骨干设计：几何分支进行因果建模以驱动增量式高斯更新，而语义分支则利用 2D 视觉基础模型和查询驱动的解码器来预测分割掩码和身份嵌入，并通过查询级对比对齐以及与实例存储器的轻量级在线关联进一步增强稳定性。实验表明，S2GS 在联合重建与理解基准测试中达到或超过了强大的离线基线模型，同时显著提升了长时程的可扩展性：它在处理 1,000 多帧图像时，运行时间和 GPU 显存的增长速度要慢得多，而离线全局处理基线在相同设置下通常在 80 帧左右就会耗尽显存。

## Abstract
Existing offline feed-forward methods for joint scene understanding and reconstruction on long image streams often repeatedly perform global computation over an ever-growing set of past observations, causing runtime and GPU memory to increase rapidly with sequence length and limiting scalability. We propose Streaming Semantic Gaussian Splatting (S2GS), a strictly causal, incremental 3D Gaussian semantic field framework: it does not leverage future frames and continuously updates scene geometry, appearance, and instance-level semantics without reprocessing historical frames, enabling scalable online joint reconstruction and understanding. S2GS adopts a geometry-semantic decoupled dual-backbone design: the geometry branch performs causal modeling to drive incremental Gaussian updates, while the semantic branch leverages a 2D foundation vision model and a query-driven decoder to predict segmentation masks and identity embeddings, further stabilized by query-level contrastive alignment and lightweight online association with an instance memory. Experiments show that S2GS matches or outperforms strong offline baselines on joint reconstruction-and-understanding benchmarks, while significantly improving long-horizon scalability: it processes 1,000+ frames with much slower growth in runtime and GPU memory, whereas offline global-processing baselines typically run out of memory at around 80 frames under the same setting.

---

## 论文详细总结（自动生成）

### S2GS：用于在线场景理解与重建的流式语义高斯泼溅 论文深度总结

#### 1. 核心问题与研究动机
*   **研究背景**：现有的联合 3D 重建与语义理解方法（如 SIU3R、Uni3R）大多采用“离线前馈”模式。当处理长图像序列时，这些方法需要对不断增长的历史观测值进行重复的全局计算。
*   **核心痛点**：
    *   **扩展性差**：运行时间和 GPU 显存随帧数增加而迅速飙升。实验显示，即使在拥有 140GB 显存的 H200 上，领先的离线方法在处理约 80 帧后也会发生显存溢出（OOM）。
    *   **非因果性**：依赖未来帧或全局优化，无法满足实时在线系统（如机器人导航、AR/VR）的需求。
*   **研究目标**：提出一种**严格因果（Strictly Causal）**且**无需重复处理（Reprocessing-free）**的流式框架，实现长序列下的高效在线联合重建与理解。

#### 2. 方法论：核心思想与技术细节
S2GS 采用增量式 3D 高斯语义场框架，核心设计包括：
*   **几何-语义解耦的双骨干设计**：
    *   **几何流（Geometry Stream）**：使用因果 Transformer 编码器处理当前帧，并结合 KV 缓存（KV Caching）复用历史信息。通过预测深度、相机参数和高斯属性，增量式地将像素投影为 3D 高斯点。为了稳定几何，引入了来自预训练 3D 基础模型的蒸馏监督。
    *   **语义流（Semantic Stream）**：独立于几何流，利用冻结的 2D 视觉基础模型（如 SigLIP2）提取特征。采用查询驱动（Query-based）的解码器预测掩码和身份嵌入，避免几何更新的噪声干扰语义表达。
*   **在线实例跟踪与语义稳定**：
    *   **查询级对比学习（CL）**：在训练阶段，通过对比损失拉近同一实例在不同帧中的查询嵌入，推开不同实例，增强身份一致性。
    *   **轻量级实例存储（Memory Bank）**：在推理阶段，通过余弦相似度和二分匹配将当前帧查询与存储器中的原型进行关联，并使用指数移动平均（EMA）更新原型。
*   **开放词汇语义理解**：
    *   设计了一个**查询语义投影器（Query Semantic Projector）**，将内部查询嵌入映射到 SigLIP2 的视觉-语言空间，从而支持通过文本提示（Text Prompts）进行零样本（Zero-shot）目标检索。

#### 3. 实验设计
*   **数据集**：主要在 **ScanNet** 上进行训练和评估，并在 **ScanNet++** 和 **Replica** 上进行跨数据集泛化测试。
*   **Benchmark 与指标**：
    *   **重建指标**：PSNR、SSIM（衡量新视角合成质量）。
    *   **理解指标**：mIoU（语义分割）、T-mIoU 和 T-SR（衡量跨帧实例一致性）。
*   **对比方法**：
    *   **联合方法**：SIU3R、Uni3R、LSM。
    *   **重建专用**：pixelSplat、MVSplat、NoPoSplat。
    *   **2D 分割基线**：LSeg、Mask2Former。

#### 4. 资源与算力
*   **硬件平台**：论文提到了使用 **NVIDIA H200 GPU (140GB VRAM)** 进行长序列扩展性测试对比。
*   **训练细节**：文中未明确给出具体的训练总时长和使用的 GPU 数量，但提到在推理阶段，S2GS 在处理 1024 帧时仍能保持较低的延迟和显存占用，而基线方法在 80-128 帧时即崩溃。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了短序列（2/8/14/32 视图）和长序列（64/256/512/1024 视图）的多维度对比。
    *   包含针对“几何-语义解耦”、“对比学习”、“几何蒸馏”等核心模块的**消融实验**。
    *   提供了**零样本跨数据集泛化**实验和**开放词汇文本查询**的可视化分析。
*   **评价**：实验设计较为充分，特别是在长序列扩展性方面的对比非常直观，客观地展示了流式处理相对于全局处理的代际优势。

#### 6. 主要结论与发现
*   **性能持平或超越**：在常规短序列任务中，S2GS 的重建和语义指标与最强的离线基线（如 SIU3R）相当。
*   **扩展性飞跃**：S2GS 能够处理 **1000 帧以上**的超长序列，且每帧处理时间（Latency）和显存增长极其缓慢。
*   **稳定性强**：通过对比学习和实例存储，S2GS 在长时程下的实例 ID 切换（ID Switches）显著减少，语义一致性更高。

#### 7. 优点与亮点
*   **架构优雅**：成功将 3DGS 的高效渲染与流式因果推理结合，解决了长视频流处理的瓶颈。
*   **解耦设计**：几何与语义的解耦有效防止了深度估计误差对语义分类的负面影响。
*   **实用性高**：支持未校正（Uncalibrated）和无位姿（Unposed）的 RGB 视频输入，非常适合实际在线应用场景。

#### 8. 不足与局限
*   **极稀疏视角下的弱势**：在仅有 2 个视图的极端情况下，由于缺乏离线方法的全局对齐能力，其重建质量（PSNR/SSIM）略逊于部分离线基线。
*   **依赖基础模型**：几何和语义的稳定性在很大程度上依赖于预训练的 3D 和 2D 基础模型（如 SigLIP2），若基础模型在某些特定场景失效，系统表现可能受限。
*   **计算开销累积**：虽然增长缓慢，但随着 3D 高斯点云数量的持续累积，长期运行后的渲染效率仍可能面临挑战（文中未深入讨论点云剪枝或合并机制）。

（完）
