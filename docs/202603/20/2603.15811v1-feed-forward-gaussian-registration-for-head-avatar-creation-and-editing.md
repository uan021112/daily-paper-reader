---
title: Feed-forward Gaussian Registration for Head Avatar Creation and Editing
title_zh: 面向头部化身创建与编辑的前馈高斯配准
authors: "Malte Prinzler, Paulo Gotardo, Siyu Tang, Timo Bolkart"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15811v1"
tags: ["query:ff3dr"]
score: 9.0
evidence: 每帧0.5秒直接预测高斯着色纹理
tldr: MATCH 是一种高效的多视图高斯配准方法，旨在解决传统头像生成中耗时的跟踪和优化问题。它通过 Transformer 模型直接从多视图图像预测具有拓扑对应关系的高斯纹理，每帧仅需 0.5 秒。该方法不仅实现了高质量的新视角合成，还支持表情迁移、语义编辑和身份插值，且生成速度比现有基准快 10 倍。
selection_source: fresh_fetch
motivation: 现有的多视图头像生成方法依赖耗时的头部跟踪和昂贵的优化过程，导致创建周期通常超过一天。
method: 提出一种基于 Transformer 的前馈模型，利用注册引导的注意力机制将多视图图像映射到固定 UV 布局的高斯纹理上。
result: 在视角合成、几何配准和头像生成质量上均优于现有方法，且创建速度比最接近的基准快 10 倍。
conclusion: MATCH 实现了高效、高质量且具备跨主体对应关系的头像创建与编辑，为实时头像应用提供了新方案。
---

## 摘要
我们提出了 MATCH（基于拓扑对应头部的多视图化身），这是一种用于高质量头部化身创建与编辑的多视图高斯配准方法。现有的最先进多视图头部化身方法需要耗时的头部追踪以及昂贵的化身优化，通常导致总创建时间超过一天。相比之下，MATCH 仅需每帧 0.5 秒即可从校准的多视图图像中直接预测具有对应关系的高斯泼溅（Gaussian splat）纹理，且无需数据预处理。学习到的跨帧受试者内对应关系实现了个性化头部化身的快速创建，而跨受试者的对应关系则支持表情迁移、免优化追踪、语义编辑和身份插值等应用。我们使用基于 Transformer 的模型端到端地建立这些对应关系，该模型在模板网格的固定 UV 布局中预测高斯泼溅纹理。为实现这一目标，我们引入了一种新颖的配准引导注意力模块，其中每个 UV 映射 token 仅关注描绘其对应网格区域的图像 token。与密集跨视图注意力相比，该设计提高了效率和性能。MATCH 在新视图合成、几何配准和头部化身生成方面优于现有方法，同时使化身创建速度比最接近的竞争基准快 10 倍。代码和模型权重可在项目网站上获取。

## Abstract
We present MATCH (Multi-view Avatars from Topologically Corresponding Heads), a multi-view Gaussian registration method for high-quality head avatar creation and editing. State-of-the-art multi-view head avatar methods require time-consuming head tracking followed by expensive avatar optimization, often resulting in a total creation time of more than one day. MATCH, in contrast, directly predicts Gaussian splat textures in correspondence from calibrated multi-view images in just 0.5 seconds per frame, without requiring data preprocessing. The learned intra-subject correspondence across frames enables fast creation of personalized head avatars, while correspondence across subjects supports applications such as expression transfer, optimization-free tracking, semantic editing, and identity interpolation. We establish these correspondences end-to-end using a transformer-based model that predicts Gaussian splat textures in the fixed UV layout of a template mesh. To achieve this, we introduce a novel registration-guided attention block, where each UV-map token attends exclusively to image tokens depicting its corresponding mesh region. This design improves efficiency and performance compared to dense cross-view attention. MATCH outperforms existing methods in novel-view synthesis, geometry registration, and head avatar generation, while making avatar creation 10 times faster than the closest competing baseline. The code and model weights are available on the project website.

---

## 论文详细总结（自动生成）

这是一份关于论文 **《Feed-forward Gaussian Registration for Head Avatar Creation and Editing》 (MATCH)** 的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：创建高质量、可控的多视图头部化身（Head Avatar）目前面临巨大的计算瓶颈。传统流程（如 GEM）需要先进行复杂的网格追踪（Mesh Tracking），再进行长时间的场景优化，整个过程通常耗时超过 24 小时，难以大规模扩展。
*   **核心问题**：如何在保证高保真度的前提下，实现极速的头部几何配准与外观重建，并建立跨主体、跨表情的语义对应关系？
*   **整体含义**：MATCH 提出了一种前馈式（Feed-forward）模型，能够直接从校准的多视图图像中，在 0.5 秒内推断出具有密集语义对应关系的高斯泼溅（Gaussian Splatting）纹理，将化身创建时间缩短了 10 倍。

### 2. 方法论
*   **核心思想**：将头部表示为固定 UV 布局的高斯属性纹理图，利用 Transformer 架构直接从多视图图像中回归这些属性。
*   **关键技术细节**：
    *   **粗糙配准（Coarse Registration）**：首先利用预训练模型（如 TEMPEH）从输入图像中估计一个粗糙的 3D 网格。
    *   **UV 与图像 Tokenization**：将输入图像和 UV 纹理图分别切片并编码为 Token。UV Token 包含了初始的 3D 位置和重投影的颜色信息。
    *   **注册引导注意力（Registration-Guided Attention）**：这是本文的核心创新。为了避免全局注意力带来的高计算开销和泛化问题，模型利用粗糙网格将 UV 区域投影到图像平面，使每个 UV Token 仅关注与其对应的图像区域 Token。
    *   **分组注意力（Grouped Attention）**：在 UV Token 内部以及单张图像的 Token 内部进行注意力计算，以传播信息并处理未观察到的区域。
    *   **高斯属性回归**：Transformer 输出处理后的 UV Token，最终解码为包含颜色、不透明度、位置偏移、各向异性缩放和旋转四元数的高斯纹理。

### 3. 实验设计
*   **数据集**：
    *   **Ava-256**：包含 256 个受试者的高质量多视图视频及地面真值（GT）网格配准。
    *   **NeRSemble v2**：包含 425 个受试者的多视图视频（用于验证跨数据集泛化能力）。
*   **对比方法（Baselines）**：
    *   **新视角合成**：GPAvatar, FastAvatar, LAM, Avat3r, FaceLift, CAP4D。
    *   **几何配准**：TEMPEH。
    *   **化身生成**：GaussianAvatars, RGBAvatar, GEM。
*   **评估指标**：LPIPS, PSNR, SSIM, CSIM（身份相似度）, P2P/P2S（几何误差）。

### 4. 资源与算力
*   **硬件设备**：使用了 **4 张 NVIDIA H100 80GB GPU**。
*   **训练时长**：总计训练了 **860k 个迭代**，耗时约 **11.8 天**。
*   **推理速度**：每帧处理仅需 **0.5 秒**。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在两个大型数据集上进行了广泛的定量对比。
    *   **消融实验**：验证了注册引导注意力、Sapiens 特征提取器、跳跃连接（Skip-connections）以及 UV 分辨率对结果的影响。
    *   **跨数据集测试**：在没有几何监督的情况下，在 NeRSemble 数据集上验证了模型的泛化能力。
*   **充分性评价**：实验设计非常充分且客观。作者不仅对比了图像质量，还深入探讨了几何精度、推理速度以及下游应用（如表情迁移、语义编辑）的效果，证明了该方法在多个维度上的优越性。

### 6. 主要结论与发现
*   **性能领先**：MATCH 在新视角合成和几何配准指标上均优于现有的前馈式和优化式方法。
*   **极速创建**：通过绕过耗时的网格追踪和昂贵的优化，将轻量级 GEM 化身的创建时间从 45 小时缩短至 **4.6 小时**（10 倍加速）。
*   **语义对应**：由于输出的高斯分布在 UV 空间是固定的，模型天然实现了跨身份和跨表情的密集对应，这使得无需额外训练即可进行精准的表情迁移和局部语义编辑（如换鼻子、换发型）。

### 7. 优点
*   **高效性**：前馈架构避免了逐个案例的优化，极大地提升了生产效率。
*   **局部注意力机制**：注册引导注意力机制巧妙地利用了粗糙几何先验，既降低了计算复杂度，又增强了模型对未见主体的泛化能力。
*   **多功能性**：不仅是一个重建模型，更是一个强大的配准工具，支持多种下游编辑任务。

### 8. 不足与局限
*   **依赖粗糙配准**：模型的最终质量在一定程度上受限于初始粗糙网格的准确性。
*   **身份泄露风险**：在进行直接的算术表情迁移时，如果源主体和目标主体特征差异过大，可能会出现外观泄露（Identity Leakage）。
*   **几何伪影**：将预测的高斯位置转换为网格时，偶尔会出现表面自相交的情况。
*   **应用限制**：目前主要针对受控的实验室环境（校准相机、均匀光照），在野外（In-the-wild）极端环境下的表现仍有提升空间。

（完）
