---
title: "Generation Models Know Space: Unleashing Implicit 3D Priors for Scene Understanding"
title_zh: 生成模型懂空间：释放用于场景理解的隐式3D先验
authors: "Xianjin Wu, Dingkang Liang, Tianrui Feng, Kui Xia, Yumeng Zhang, Xiaofan Li, Xiao Tan, Xiang Bai"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19235v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 从视频生成模型中释放隐式3D先验用于场景理解
tldr: 针对多模态大模型在空间理解和几何推理方面的不足，本文提出 VEGA-3D 框架。该方法不依赖稀缺的显式 3D 数据，而是利用预训练视频生成模型中蕴含的隐式 3D 结构先验。通过从视频扩散模型的中间噪声层提取时空特征，并利用自适应门控融合机制将其与语义表示结合，显著提升了模型在 3D 场景理解、空间推理和具身操作任务中的表现，证明了生成式先验在物理世界理解中的巨大潜力。
selection_source: fresh_fetch
motivation: 多模态大模型在精细几何推理和物理动力学方面存在“空间盲区”，且现有的显式 3D 数据方案面临数据稀缺和泛化挑战。
method: 提出 VEGA-3D 框架，将预训练视频扩散模型作为潜在世界模拟器，通过自适应门控融合机制提取并整合其内部的隐式时空特征。
result: 实验表明，该方法在 3D 场景理解、空间推理和具身操作基准测试中均优于现有最先进模型。
conclusion: 视频生成模型内部蕴含的隐式 3D 先验为物理世界理解提供了可扩展的基础，无需显式 3D 监督即可增强模型的空间感知。
---

## 摘要
尽管多模态大语言模型（MLLMs）展现出令人印象深刻的语义能力，但它们往往面临“空间盲区”的问题，在细粒度几何推理和物理动力学方面表现欠佳。现有的解决方案通常依赖于显式的3D模态或复杂的几何支架，这受到数据稀缺和泛化挑战的限制。在这项工作中，我们提出了一种范式转变，即利用大规模视频生成模型中的隐式空间先验。我们假设，为了合成时间上连贯的视频，这些模型本质上学习了鲁棒的3D结构先验和物理规律。我们介绍了 VEGA-3D（视频提取生成感知），这是一个即插即用的框架，它将预训练的视频扩散模型重新利用为潜世界模拟器（Latent World Simulator）。通过从中间噪声水平提取时空特征，并通过令牌级自适应门控融合机制将其与语义表示相结合，我们在没有显式3D监督的情况下，为 MLLMs 丰富了密集的几何线索。在3D场景理解、空间推理和具身操纵基准测试上的广泛实验表明，我们的方法优于最先进的基准模型，验证了生成先验为物理世界理解提供了可扩展的基础。代码已在 https://github.com/H-EmbodVis/VEGA-3D 公开。

## Abstract
While Multimodal Large Language Models demonstrate impressive semantic capabilities, they often suffer from spatial blindness, struggling with fine-grained geometric reasoning and physical dynamics. Existing solutions typically rely on explicit 3D modalities or complex geometric scaffolding, which are limited by data scarcity and generalization challenges. In this work, we propose a paradigm shift by leveraging the implicit spatial prior within large-scale video generation models. We posit that to synthesize temporally coherent videos, these models inherently learn robust 3D structural priors and physical laws. We introduce VEGA-3D (Video Extracted Generative Awareness), a plug-and-play framework that repurposes a pre-trained video diffusion model as a Latent World Simulator. By extracting spatiotemporal features from intermediate noise levels and integrating them with semantic representations via a token-level adaptive gated fusion mechanism, we enrich MLLMs with dense geometric cues without explicit 3D supervision. Extensive experiments across 3D scene understanding, spatial reasoning, and embodied manipulation benchmarks demonstrate that our method outperforms state-of-the-art baselines, validating that generative priors provide a scalable foundation for physical-world understanding. Code is publicly available at https://github.com/H-EmbodVis/VEGA-3D.

---

## 论文详细总结（自动生成）

### 论文总结：Generation Models Know Space (VEGA-3D)

#### 1. 核心问题与整体含义
多模态大语言模型（MLLMs）虽然在语义识别上表现出色，但在处理细粒度几何推理、空间关系和物理动力学时常表现出“空间盲区（Spatial Blindness）”。现有的提升方法通常依赖于稀缺的显式 3D 数据（如点云）或复杂的几何重建算法。本文的研究动机在于：**视频生成模型**为了生成时间连贯、物理合理的视频，其内部必然学习到了深刻的 3D 结构和物理规律。作者旨在探索如何将这种“隐式 3D 先验”提取出来，作为即插即用的插件增强 MLLM 的空间感知能力。

#### 2. 方法论
VEGA-3D 框架将预训练的视频扩散模型重新利用为**潜在世界模拟器（Latent World Simulator）**，核心步骤如下：
*   **3D 感知分析**：通过“多视图对应得分（Multi-view Correspondence Score）”量化不同架构的几何一致性，发现基于 Transformer（DiT）的生成模型比基于 UNet 的模型具有更强的空间一致性。
*   **隐式先验提取**：
    1.  将输入视频通过 VAE 编码到潜空间。
    2.  利用 **Flow Matching** 路径向潜变量注入适量噪声（实验发现中间噪声水平最能激发模型的结构推理能力）。
    3.  从冻结的视频生成模型（如 Wan2.1）的中间层提取时空特征。
*   **自适应门控融合（Adaptive Gated Fusion）**：
    *   设计了一个令牌级（token-level）的融合模块，通过学习一个门控权重 $g \in [0, 1]$，动态平衡语义特征（来自 SigLIP 等编码器）和生成式几何特征。
    *   公式表达：$F_{fused} = (1 - g) \cdot F_{gen} + g \cdot F_{sem}$。这使得模型在需要定位时侧重几何特征，在需要识别时侧重语义特征。

#### 3. 实验设计
*   **数据集与场景**：
    *   **3D 场景理解**：ScanRefer, Multi3DRefer, Scan2Cap, ScanQA, SQA3D（均基于 ScanNet 室内场景）。
    *   **空间推理**：VSI-Bench（涵盖物体计数、相对距离、路径规划等）。
    *   **具身操作**：LIBERO 机器人操纵基准测试。
*   **对比方法**：包括专门的 3D 任务模型（如 3D-VisTA）、通用 3D MLLM（如 Video-3D LLM, 3DRS, Inst3D-LLM）以及强大的开源基座（如 Qwen2.5VL）。

#### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA H100 GPU**。
*   **训练细节**：采用 Adam 优化器，Batch Size 为 128。语言模型学习率为 $1 \times 10^{-5}$，视觉骨干学习率为 $2 \times 10^{-6}$。
*   **推理优化**：为了降低开销，对每个场景的生成式特征进行了缓存处理。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 3 大领域、超过 10 个主流基准测试上进行了实验。
*   **消融实验**：非常详尽，涵盖了不同生成模型（SVD, SD2.1, Vmem, Wan2.1 等）的对比、不同噪声水平（Timestep $k$）的影响、不同 DiT 层深度的选择，以及多种特征融合策略的对比。
*   **充分性**：实验设计客观公平，通过相关性分析（图 3）有力地证明了多视图一致性与下游任务性能之间的正相关关系，论证逻辑严密。

#### 6. 主要结论与发现
*   **生成模型确实懂空间**：视频生成模型（尤其是 DiT 架构）内部蕴含的隐式先验能显著提升 MLLM 的定位和空间推理能力。
*   **中间态最有效**：最强的几何先验并非来自最终生成的像素，而是来自扩散过程的**中间噪声水平**和网络的**中间层特征**。
*   **架构决定上限**：DiT 架构由于其全局注意力机制，在保持长程几何一致性方面远优于传统的 UNet 架构。
*   **即插即用**：该框架无需任何显式 3D 监督信号，即可在多种下游任务中达到或超过 SOTA 水平。

#### 7. 优点
*   **范式创新**：摆脱了对昂贵 3D 标注数据的依赖，开辟了利用生成模型作为物理世界先验的新路径。
*   **通用性强**：作为即插即用框架，可以轻松集成到现有的各种 MLLM 中。
*   **性能卓越**：在定位敏感型任务（如 ScanRefer）和具身智能任务中表现尤为突出。

#### 8. 不足与局限
*   **计算开销**：引入额外的视频扩散模型作为特征提取器，增加了推理时的显存占用和计算延迟（尽管有缓存策略）。
*   **语义-几何权衡**：在某些纯语义任务（如密集描述生成）中，加入几何先验可能会导致语义细节的轻微损失（如 CIDEr 分数微降）。
*   **场景限制**：目前主要在室内静态或准静态场景验证，对于极高动态或室外超大规模场景的泛化性尚待进一步验证。

（完）
