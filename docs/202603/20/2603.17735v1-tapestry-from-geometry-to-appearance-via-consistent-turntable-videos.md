---
title: "TAPESTRY: From Geometry to Appearance via Consistent Turntable Videos"
title_zh: TAPESTRY：通过一致的转盘视频实现从几何到外观的生成
authors: "Yan Zeng, Haoran Jiang, Kaixin Yao, Qixuan Zhang, Longwen Zhang, Lan Xu, Jingyi Yu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17735v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 3D模型的神经渲染和纹理合成
tldr: 针对无纹理3D模型生成高质量一致性外观的挑战，本文提出TAPESTRY框架。该方法将3D外观生成重构为几何约束的视频扩散问题，通过渲染多模态几何特征引导视频生成模型产生精确的360度转台视频。此外，结合3D感知修复技术解决自遮挡区域的补全问题，生成的视频可直接用于UV纹理映射或3DGS重建，显著提升了3D资产创作的质量与一致性。
selection_source: fresh_fetch
motivation: 现有的通用视频扩散模型在生成3D转台视频时难以保持严格的几何一致性和外观稳定性，导致其输出难以直接用于高质量的3D重建。
method: 提出一种以显式3D几何为条件的视频扩散框架，通过编码多模态几何特征实现像素级约束，并利用多阶段流水线和3D感知修复技术处理遮挡区域。
result: 实验结果表明，该方法在视频生成的一致性以及最终3D重建的质量上均显著优于现有的主流方法。
conclusion: TAPESTRY通过将视频生成与几何约束深度结合，为从无纹理网格自动化创建生产级、完整的3D资产提供了一种高效且可靠的中间表示方案。
---

## 摘要
为无纹理 3D 模型自动生成具有照片级真实感且自洽的外观是数字内容创作中的一项关键挑战。大规模视频生成模型的进步提供了一种自然的方法：直接合成 360 度转盘视频 (TTV)，这些视频不仅可以作为高质量的动态预览，还可以作为驱动纹理合成和神经渲染的中间表示。然而，现有的通用视频扩散模型难以在全视角范围内保持严格的几何一致性和外观稳定性，导致其输出不适用于高质量的 3D 重建。为此，我们提出了 TAPESTRY，这是一个以显式 3D 几何为条件的生成高保真 TTV 的框架。我们将 3D 外观生成任务重新定义为一个以几何为条件的视频扩散问题：给定一个 3D 网格，我们首先渲染并编码多模态几何特征，以像素级精度约束视频生成过程，从而能够创建高质量且一致的 TTV。在此基础上，我们还设计了一种基于 TTV 输入的下游重建任务方法，该方法包含一个具有 3D 感知修复（3D-Aware Inpainting）功能的多阶段流水线。通过旋转模型并执行上下文感知的二次生成，该流水线有效地补全了自遮挡区域，从而实现全表面覆盖。TAPESTRY 生成的视频不仅是高质量的动态预览，还可以作为可靠的、具备 3D 感知的中间表示，能够无缝地反向投影到 UV 纹理中，或用于监督 3DGS 等神经渲染方法。这使得从无纹理网格自动创建生产级、完整的 3D 资产成为可能。实验结果表明，我们的方法在视频一致性和最终重建质量方面均优于现有方法。

## Abstract
Automatically generating photorealistic and self-consistent appearances for untextured 3D models is a critical challenge in digital content creation. The advancement of large-scale video generation models offers a natural approach: directly synthesizing 360-degree turntable videos (TTVs), which can serve not only as high-quality dynamic previews but also as an intermediate representation to drive texture synthesis and neural rendering. However, existing general-purpose video diffusion models struggle to maintain strict geometric consistency and appearance stability across the full range of views, making their outputs ill-suited for high-quality 3D reconstruction. To this end, we introduce TAPESTRY, a framework for generating high-fidelity TTVs conditioned on explicit 3D geometry. We reframe the 3D appearance generation task as a geometry-conditioned video diffusion problem: given a 3D mesh, we first render and encode multi-modal geometric features to constrain the video generation process with pixel-level precision, thereby enabling the creation of high-quality and consistent TTVs. Building upon this, we also design a method for downstream reconstruction tasks from the TTV input, featuring a multi-stage pipeline with 3D-Aware Inpainting. By rotating the model and performing a context-aware secondary generation, this pipeline effectively completes self-occluded regions to achieve full surface coverage. The videos generated by TAPESTRY are not only high-quality dynamic previews but also serve as a reliable, 3D-aware intermediate representation that can be seamlessly back-projected into UV textures or used to supervise neural rendering methods like 3DGS. This enables the automated creation of production-ready, complete 3D assets from untextured meshes. Experimental results demonstrate that our method outperforms existing approaches in both video consistency and final reconstruction quality.

---

## 论文详细总结（自动生成）

### TAPESTRY：通过一致的转盘视频实现从几何到外观的生成 —— 论文深度总结

#### 1. 核心问题与研究动机
*   **核心问题**：如何为无纹理的 3D 模型自动生成高质量、照片级真实感且在 360 度视角下保持严格几何一致性的外观。
*   **研究动机**：
    *   传统的纹理贴图制作依赖人工，耗时耗力且难以规模化。
    *   现有的视频生成模型虽然能产生流畅的动态效果，但在生成 360 度转盘视频（TTV）时，往往会出现内容漂移、结构扭曲或“多头/双面（Janus problem）”现象，无法直接用于 3D 重建。
    *   现有的多视角图像生成方法（如生成 6 视图）在处理自遮挡区域时容易产生接缝和模糊。

#### 2. 方法论
TAPESTRY 将 3D 外观生成任务重构为一个**几何约束的视频扩散问题**，核心流程如下：
*   **几何引导的视频生成**：
    *   **多模态条件准备**：给定 3D 网格，沿圆形轨道渲染**法线视频（Normal Video）**和**位置图视频（Position Map Video）**。法线提供局部细节，位置图提供全局空间参考，防止长程漂移。
    *   **潜空间注入**：使用预训练视频 VAE 的编码器将几何视频转换为潜特征，通过一个轻量级的几何融合模块（卷积层+残差块）融合，并与噪声潜变量在通道维度拼接，输入到 DiT（Diffusion Transformer）架构中。
    *   **参考引导**：通过 CLIP 和文本编码器注入参考图像和文本提示，确保风格一致。
*   **渐进式纹理化流水线**：
    *   **多视角投影**：利用射线追踪将生成的视频帧反向投影到 UV 空间，并使用角度激励和深度梯度惩罚进行加权融合。
    *   **3D 感知修复（3D-Aware Inpainting）**：针对自遮挡区域，算法自动旋转物体以暴露未着色区域，渲染包含已生成纹理的“部分纹理视频”和“修复掩码视频”，作为条件引导模型进行二次生成，确保新旧内容无缝衔接。

#### 3. 实验设计
*   **数据集**：
    *   **训练集**：从 Objaverse 选取 3 万个高质量 3D 资产，通过 Blender 渲染生成约 12 万个转盘视频，涵盖多种环境光照和随机旋转。
    *   **测试集**：从 GSO (Google Scanned Objects) 和 Objaverse 测试集中随机抽取 200 个网格。
*   **对比方法**：
    *   **视频生成对比**：Wan2.1-Fun (基于 Depth/Canny 约束)、Diffusion as Shader (DaS)。
    *   **纹理生成对比**：Paint3D、Hunyuan3D-2.0、TEXGen、MV-Adapter。
*   **评估指标**：
    *   视频：PSNR、SSIM、LPIPS、FVD。
    *   纹理：FID、KID、CLIP Score、运行时间。
    *   用户研究：针对对齐度、一致性和整体质量进行主观评分。

#### 4. 资源与算力
*   **硬件**：在一台 **DGX Spark** 超级计算机上完成训练。
*   **时长**：训练过程耗时约 **10 天**。
*   **推理效率**：使用 4 步推理（4-step inference），在 DGX 系统上生成一段 61 帧的视频仅需 **30 秒**。
*   **模型基础**：基于开源的 Wan2.1-Fun-V1.1-Control 模型进行全微调，并使用 LoRA（秩为 128）和 LoRA+ 策略。

#### 5. 实验数量与充分性
*   **实验规模**：使用了 12 万组视频数据进行训练，测试涵盖了 200 个不同类别的复杂网格，实验规模较大。
*   **消融实验**：
    *   验证了法线图（局部细节）和位置图（全局稳定性）的必要性。
    *   验证了 3D 感知修复机制相对于简单融合（Naive Fusion）在消除接缝方面的优越性。
*   **充分性评价**：实验设计较为全面，不仅对比了视频指标，还通过下游的纹理贴图和 3DGS 重建效果验证了生成视频的“可重建性”，客观性较强。

#### 6. 主要结论与发现
*   **几何约束是关键**：显式的 3D 几何特征（尤其是位置图）能有效消除视频生成中的内容漂移和 Janus 问题。
*   **TTV 作为中间表示的优势**：相比于稀疏的多视角图像，连续的转盘视频提供了更密集的表面覆盖，显著减少了纹理重建中的歧义。
*   **多用途性**：生成的视频不仅可用于烘焙 UV 纹理，还能直接作为 3DGS 的训练信号，捕捉毛发等复杂体积细节。

#### 7. 优点与亮点
*   **极高的一致性**：通过像素级几何引导，实现了目前 SOTA 级别的 360 度旋转一致性。
*   **创新的修复机制**：3D 感知修复解决了 3D 生成中长期存在的自遮挡和接缝问题。
*   **生产可用性**：生成的资产可直接导出为 UV 贴图或神经渲染格式，符合工业界工作流。
*   **效率优化**：在相对较低的算力预算下实现了高质量生成。

#### 8. 不足与局限
*   **几何依赖性**：输出质量高度依赖于输入 3D 网格的拓扑质量，若网格本身有缺陷，生成效果会受限。
*   **光照耦合**：生成的外观中包含了视频模型自带的环境光照，而非纯粹的材质属性（Albedo），这给后续的动态重光照（Relighting）带来了挑战。
*   **推理成本**：虽然单次生成快，但为了达到全表面覆盖，需要多次迭代生成和投影，总耗时高于单次图像生成方法。

（完）
