---
title: "PC-CrossDiff: Point-Cluster Dual-Level Cross-Modal Differential Attention for Unified 3D Referring and Segmentation"
title_zh: PC-CrossDiff：用于统一3D指代与分割的点-簇双层跨模态差分注意力
authors: "Wenbin Tan, Jiawen Lin, Fangyong Wang, Yuan Xie, Yong Xie, Yachao Zhang, Yanyun Qu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17753v1"
tags: ["query:partseg"]
score: 9.0
evidence: 复杂多物体场景中的点云分割
tldr: 针对3D视觉定位在复杂多物体场景中难以解析隐式线索和抑制空间干扰的问题，本文提出PC-CrossDiff框架。该框架通过点级（PLDA）和聚类级（CLDA）双层跨模态差分注意力机制，分别提取隐式定位特征并增强关键空间关系，实现了3D引用表达理解与分割任务的统一，在多个基准测试中达到SOTA性能。
selection_source: fresh_fetch
motivation: 现有3DVG方法在复杂场景下难以有效解析隐式定位线索，且容易受到共存物体的动态空间干扰。
method: 提出一种双层差分注意力架构，利用点级差分注意力提取隐式特征，并通过聚类级差分注意力优化空间关系建模。
result: "在ScanRefer、NR3D和SR3D基准上均取得领先，特别是在ScanRefer隐式子集上的3DREC任务中Overall@0.50提升了10.16%。"
conclusion: PC-CrossDiff通过精细化的双层差分注意力机制，显著提升了模型在复杂3D场景下的定位精度和鲁棒性。
---

## 摘要
3D视觉定位（3DVG）旨在通过两个核心任务——指代表达理解（3DREC）和分割（3DRES），对自然语言指代表达的参照物进行定位。虽然现有方法在简单的单物体场景中取得了较高的准确率，但在现实设置中常见的复杂多物体场景中，它们的性能会严重下降，从而阻碍了实际部署。现有方法在复杂多物体场景中面临两个关键挑战：对区分视觉相似物体至关重要的隐式定位线索解析不足，以及对共存物体的动态空间干扰抑制无效，导致定位准确率下降。为了应对这些挑战，我们提出了PC-CrossDiff，这是一个具有双层跨模态差分注意力架构的统一双任务框架，用于3DREC和3DRES。具体而言，该框架引入了：(i) 点级差分注意力（PLDA）模块，在文本和点云之间应用双向差分注意力，通过可学习权重自适应地提取隐式定位线索，以提高判别性表示；(ii) 簇级差分注意力（CLDA）模块，建立了一种分层注意力机制，通过定位感知差分注意力块自适应地增强与定位相关的空间关系，同时抑制模糊或无关的空间关系。我们的方法在ScanRefer、NR3D和SR3D基准测试中达到了最先进的性能。值得注意的是，在ScanRefer的Implicit子集上，它将3DREC任务的Overall@0.50分数提高了+10.16%，突显了其解析隐式空间线索的强大能力。

## Abstract
3D Visual Grounding (3DVG) aims to localize the referent of natural language referring expressions through two core tasks: Referring Expression Comprehension (3DREC) and Segmentation (3DRES). While existing methods achieve high accuracy in simple, single-object scenes, they suffer from severe performance degradation in complex, multi-object scenes that are common in real-world settings, hindering practical deployment. Existing methods face two key challenges in complex, multi-object scenes: inadequate parsing of implicit localization cues critical for disambiguating visually similar objects, and ineffective suppression of dynamic spatial interference from co-occurring objects, resulting in degraded grounding accuracy. To address these challenges, we propose PC-CrossDiff, a unified dual-task framework with a dual-level cross-modal differential attention architecture for 3DREC and 3DRES. Specifically, the framework introduces: (i) Point-Level Differential Attention (PLDA) modules that apply bidirectional differential attention between text and point clouds, adaptively extracting implicit localization cues via learnable weights to improve discriminative representation; (ii) Cluster-Level Differential Attention (CLDA) modules that establish a hierarchical attention mechanism to adaptively enhance localization-relevant spatial relationships while suppressing ambiguous or irrelevant spatial relations through a localization-aware differential attention block. Our method achieves state-of-the-art performance on the ScanRefer, NR3D, and SR3D benchmarks. Notably, on the Implicit subsets of ScanRefer, it improves the Overall@0.50 score by +10.16% for the 3DREC task, highlighting its strong ability to parse implicit spatial cues.

---

## 论文详细总结（自动生成）

### 论文总结：PC-CrossDiff —— 用于统一3D指代与分割的点-簇双层跨模态差分注意力

#### 1. 核心问题与整体含义（研究动机和背景）
3D视觉定位（3DVG）包含两个互补任务：**3D指代理解（3DREC）**（定位物体框）和**3D指代分割（3DRES）**（点云分割）。
*   **研究动机**：现有方法在简单场景表现良好，但在**复杂多物体场景**中性能骤降。
*   **核心挑战**：
    1.  **隐式线索解析不足**：难以处理如“在笔记本电脑旁边”这类非直接描述的隐式空间关系。
    2.  **动态空间干扰**：多物体场景中，共存的无关物体会产生空间噪声，干扰目标定位。
*   **目标**：构建一个统一的框架，在提升复杂场景鲁棒性的同时，兼顾REC和RES任务的协同。

#### 2. 方法论：核心思想与关键技术
论文提出了 **PC-CrossDiff** 框架，核心在于引入了受 Differential Transformer 启发的**双层差分注意力机制**：

*   **点级差分注意力 (PLDA)**：
    *   在点云特征和文本特征之间进行双向交互。
    *   利用差分注意力公式 $DiffAttn = (A_1 - \lambda \odot A_2)V$，其中 $A_1$ 捕捉语义对齐，$A_2$ 捕捉干扰信号，通过可学习的 $\lambda$ 抵消噪声，从而精准提取文本中的隐式定位线索。
*   **簇级差分注意力 (CLDA)**：
    *   **空间关系建模 (SRM)**：通过采样和聚类（KNN）将点云划分为多个簇，建模簇间空间关系。
    *   **定位感知差分注意力 (LDA)**：采用“过滤-增强”策略，利用文本特征作为查询，从空间关系中筛选出与定位相关的特征，抑制无关物体的干扰。
*   **双几何任务协调损失 ($L_{DGTL}$)**：
    *   针对REC和RES任务间的梯度冲突和尺度差异，设计了双几何一致性约束（框与掩码互转一致性）和任务平衡权重，减少多任务训练时的干扰。

#### 3. 实验设计
*   **数据集**：ScanRefer、NR3D、SR3D。
*   **特殊子集**：为了验证复杂场景性能，专门测试了 **Multiple**（多物体）和 **Implicit**（隐式描述）子集。
*   **Benchmark 与对比方法**：
    *   对比了单任务 SOTA 方法（如 X-RefSeg3D, 3D-STMN, RefMask3D）。
    *   对比了目前唯一的双任务统一模型 **MCLN**。
*   **评价指标**：Overall@0.25/0.50, mIoU, Acc@0.25。

#### 4. 资源与算力
*   **硬件**：实验在 **NVIDIA 3090 GPU** 上运行。
*   **软件/参数**：使用 AdamW 优化器；视觉编码器学习率 $2 \times 10^{-3}$，其余层 $2 \times 10^{-4}$；温度系数 $\tau=0.5$；网络层数 $L=6$。
*   **推理效率**：推理速度达到 **4.07 FPS**，在保证精度的同时优于部分两阶段模型。
*   **未明确信息**：文中未明确提及具体的 GPU 数量及总训练时长。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了三大主流 3D 视觉定位数据集，实验组数充足。
*   **消融实验**：非常详尽，分别验证了 $L_{DGTL}$、PLDA、CLDA 的有效性，并探讨了聚类大小（$N_{clust}$）对性能的影响。
*   **客观性**：通过在 Multiple 和 Implicit 子集上的对比，证明了模型在处理复杂逻辑和空间干扰方面的真实提升，而非仅仅依靠数据集偏见。对比实验均在相同协议下进行，具有较高的公平性。

#### 6. 主要结论与发现
*   **性能领先**：PC-CrossDiff 在所有基准测试中均达到 SOTA。在 ScanRefer 的 3DRES 任务中，Overall@0.50 达到 52.52%。
*   **隐式线索突破**：在 ScanRefer 的 Implicit 子集上，3DREC 任务的 Overall@0.50 提升了 **10.16%**，证明了差分注意力在解析复杂语义方面的卓越能力。
*   **多任务协同**：证明了通过合理的损失函数设计，REC 和 RES 任务可以相互促进而非相互干扰。

#### 7. 优点（亮点）
*   **双层差分设计**：巧妙地将点级语义提取与簇级空间过滤结合，针对性地解决了 3DVG 的痛点。
*   **轻量化模块**：PLDA 仅增加 2.10M 参数，在提升性能的同时保持了较快的推理速度。
*   **统一框架**：一个模型同时处理两个任务，且在复杂场景下的鲁棒性显著强于现有方法。

#### 8. 不足与局限
*   **聚类依赖性**：虽然实验证明了对聚类大小具有一定鲁棒性，但 KNN 聚类本身对点云分布的均匀性有一定要求，在极端稀疏或不规则的场景下可能受限。
*   **端到端复杂性**：尽管是统一框架，但内部包含 SRM、LDA 等多个阶段，对于实时性要求极高的移动端部署可能仍有优化空间。
*   **数据集偏差**：虽然在隐式子集表现优异，但 3D 场景理解仍受限于现有数据集的标注规模，模型在更广阔、未见过的开放场景中的泛化能力仍待验证。

（完）
