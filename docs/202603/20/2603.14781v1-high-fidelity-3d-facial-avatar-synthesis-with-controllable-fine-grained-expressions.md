---
title: High-Fidelity 3D Facial Avatar Synthesis with Controllable Fine-Grained Expressions
title_zh: 具有可控细粒度表情的高保真3D面部化身合成
authors: "Yikang He, Jichao Zhang, Wei Wang, Nicu Sebe, Yao Zhao"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14781v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 3D面部化身合成与纹理编辑
tldr: 针对现有3D面部生成方法在细粒度表情控制上的不足，本文提出一种高保真3D人脸头像合成方法。通过引入双映射器（Dual Mappers）同时优化预训练3D感知GAN的纹理潜码和3DMM的表情参数，并结合基于CLIP的文本引导优化及子空间投影机制，实现了对细粒度表情的精确控制，在保持视图一致性的同时提升了表情编辑的细腻度。
selection_source: fresh_fetch
motivation: 现有的3D面部编辑方法在处理细粒度表情时缺乏精确的控制能力。
method: 提出双映射器模块协同优化纹理与网格参数，并利用文本引导的子空间投影机制实现精准的表情映射。
result: 实验证明该方法在生成高保真、视图一致且具备细粒度表情控制的3D头像方面优于现有技术。
conclusion: 该研究通过结合生成对抗网络与3D可变形模型，为可控的细粒度3D面部表情合成提供了有效的解决方案。
---

## 摘要
面部表情编辑方法根据其架构主要可分为两类：基于2D的方法和基于3D的方法。前者缺乏3D面部建模能力，难以有效地编辑3D因素。后者在利用单视图2D人脸图像生成高质量且视角一致的渲染图方面表现出了卓越的性能。尽管这些方法已成功使用可动画模型来控制面部表情，但在实现对细粒度表情的精确控制方面仍存在局限性。为了解决这一问题，本文提出了一种新颖的方法，通过同时细化用于纹理编辑的预训练3D感知GAN模型的潜码（latent code）以及用于网格编辑的驱动3DMM模型的表情码。具体而言，我们引入了一个双映射器（Dual Mappers）模块，由纹理映射器（Texture Mapper）和情感映射器（Emotion Mapper）组成，分别学习给定纹理潜码和网格表情码的变换。为了优化双映射器，我们提出了一种文本引导优化方法，利用以表情文本提示为目标的基于CLIP的目标函数，同时集成了一种子空间投影（SubSpace Projection）机制，将文本嵌入投影到表情子空间，从而实现对细粒度表情更精确的控制。大量的实验和对比分析证明了我们所提方法的有效性和优越性。

## Abstract
Facial expression editing methods can be mainly categorized into two types based on their architectures: 2D-based and 3D-based methods. The former lacks 3D face modeling capabilities, making it difficult to edit 3D factors effectively. The latter has demonstrated superior performance in generating high-quality and view-consistent renderings using single-view 2D face images. Although these methods have successfully used animatable models to control facial expressions, they still have limitations in achieving precise control over fine-grained expressions. To address this issue, in this paper, we propose a novel approach by simultaneously refining both the latent code of a pretrained 3D-Aware GAN model for texture editing and the expression code of the driven 3DMM model for mesh editing. Specifically, we introduce a Dual Mappers module, comprising Texture Mapper and Emotion Mapper, to learn the transformations of the given latent code for textures and the expression code for meshes, respectively. To optimize the Dual Mappers, we propose a Text-Guided Optimization method, leveraging a CLIP-based objective function with expression text prompts as targets, while integrating a SubSpace Projection mechanism to project the text embedding to the expression subspace such that we can have more precise control over fine-grained expressions. Extensive experiments and comparative analyses demonstrate the effectiveness and superiority of our proposed method.

---

## 论文详细总结（自动生成）

以下是对论文《High-Fidelity 3D Facial Avatar Synthesis with Controllable Fine-Grained Expressions》的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在仅使用单张2D人脸图像的情况下，合成具有**高保真度**、**视角一致性**且能实现**细粒度表情精确控制**的3D面部化身（Avatar）。
*   **研究背景**：
    *   **2D方法**：缺乏3D几何建模，难以保持多视角一致性。
    *   **现有3D方法**（如Next3D、AniFace-GAN）：依赖于DECA等3D重建先验，但这些先验在训练时缺乏细粒度表情数据，导致难以捕捉和生成微妙的表情细节（如皱眉、闭眼等）。
    *   **扩散模型方法**：虽然图像质量高，但缺乏显式3D表示，视角一致性较差，且往往需要针对特定人物进行耗时的训练。
*   **动机**：利用预训练3D感知GAN的强大生成能力，结合多模态模型（CLIP）的语义指导，解决纹理与几何更新不协调的问题。

### 2. 方法论
论文提出了一种结合3D感知GAN与3D可变形模型（3DMM）的框架，核心技术包括：
*   **双映射器模块（Dual Mappers）**：
    *   **纹理映射器（Texture Mapper）**：负责细化预训练StyleGAN2的潜码 $w$，用于调整面部纹理。
    *   **情感映射器（Emotion Mapper）**：负责优化FLAME模型的表情参数 $\alpha$，用于调整面部网格几何。
    *   **交叉注意力机制（Cross-Attention）**：在两个映射器之间建立联系，确保几何变形与纹理变化（如笑时的皱纹）在空间和语义上保持同步协调。
*   **文本引导优化（Text-Guided Optimization）**：
    *   利用**CLIP模型**将文本指令（如“一个正在皱眉的人”）转化为视觉指导信号。
    *   **子空间投影机制（Subspace Projection）**：为了防止表情编辑导致身份（Identity）坍塌，将文本嵌入投影到特定的“表情子空间”中，仅修改与表情相关的属性，保留原始身份特征。
*   **几何初始化**：采用**EMOCA**替代传统的DECA，因为EMOCA在情感表达空间上更丰富，能提供更精确的初始表情参数。

### 3. 实验设计
*   **数据集**：
    *   **FFHQ**：用于GAN的预训练及真实图像反演实验。
    *   **FaceScape**：提供3D先验参数。
*   **对比方法**：
    *   **3D感知模型**：Next3D, AniFace-GAN, DiscoFaceGAN, Diffusion-rig。
    *   **最新技术**：Morphable Diffusion (基于扩散模型), Gaussian Head Avatar (基于3DGS)。
    *   **2D文本编辑**：StyleCLIP, DeltaEdit。
*   **评估指标**：
    *   **图像质量**：FID, KID。
    *   **表情准确性**：**AU Acc**（动作单元准确率，利用OpenFace检测肌肉微动作）、CLIP Score（语义一致性）。
    *   **身份保持**：ID Loss（基于ArcFace）。

### 4. 资源与算力
*   **硬件环境**：单台工作站，配备 **NVIDIA RTX 4090 GPU (24GB VRAM)**。
*   **训练时长**：针对每个特定表情训练一个双映射器，大约需要 **14小时**。
*   **推理效率**：一旦训练完成，推理过程无需优化。单次运行时间约为 **0.71秒**，优于扩散模型（1.26s-1.58s），与Next3D（0.52s）相当。

### 5. 实验数量与充分性
*   **实验规模**：针对6种主要细粒度表情（微笑、愤怒、悲伤、闭眼、抬眉、皱眉）进行了详尽测试。
*   **消融实验**：分别验证了身份损失（$L_{ID}$）、子空间投影、交叉注意力模块以及EMOCA初始化的必要性。
*   **客观性**：对比了不同架构（GAN vs 扩散模型 vs 3DGS），并使用了身份无关的客观指标（AU Acc），实验设计较为全面且公平。

### 6. 主要结论
*   **协同编辑是关键**：同时优化纹理潜码和几何参数，并使用交叉注意力进行耦合，能显著减少编辑伪影。
*   **子空间投影的有效性**：成功解决了CLIP引导编辑中常见的身份偏移问题。
*   **性能领先**：在保持严格视角一致性的前提下，该方法在细粒度表情的捕捉（尤其是“闭眼”等挑战性动作）上显著优于现有3D感知模型。

### 7. 亮点
*   **细粒度控制**：通过CLIP绕过了对大规模标注细粒度3D数据集的依赖。
*   **拓扑鲁棒性**：能够处理如“闭眼”这种涉及较大拓扑或纹理变化的复杂表情，而不会产生“鬼影”或纹理撕裂。
*   **无需逐人训练**：相比于需要针对每个ID训练模型的Gaussian Head Avatar，该方法更具通用性。

### 8. 不足与局限
*   **训练成本**：虽然推理快，但每个表情需要单独训练一个映射器（14小时），缺乏一个统一的、能处理所有表情的通用映射器。
*   **依赖初始化**：如果输入的参考图像表情极其夸张或与目标文本冲突严重，可能会产生伪影。
*   **数据偏差**：受限于预训练GAN（FFHQ）的数据分布，对于极端视角或非人脸区域（如复杂的发饰）的渲染可能仍存在局限。

（完）
