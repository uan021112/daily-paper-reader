---
title: "Points-to-3D: Structure-Aware 3D Generation with Point Cloud Priors"
title_zh: Points-to-3D：基于点云先验的结构感知 3D 生成
authors: "Jiatong Xia, Zicheng Duan, Anton van den Hengel, Lingqiao Liu"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18782v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 用于几何可控3D资产和场景生成的扩散框架
tldr: Points-to-3D 是一种基于扩散模型的 3D 生成框架，旨在利用点云先验（如 LiDAR 采集或单图预测）来增强几何可控性。该方法基于 TRELLIS 模型，通过结构修复网络和分阶段采样策略，将稀疏点云转化为完整的 3D 资产或场景。它有效解决了现有模型对 3D 几何约束利用不足的问题，在保持输入区域准确性的同时实现了高质量的全局几何补全，显著提升了生成结果的保真度。
selection_source: fresh_fetch
motivation: 现有的 3D 生成模型主要依赖图像或文本，未能充分利用传感器或预测器提供的显式点云几何约束。
method: 基于 TRELLIS 框架，引入结构修复网络和分阶段采样策略（结构补全与边界细化），将点云先验融入潜在 3D 扩散过程。
result: 在物体和场景生成任务中，该方法在渲染质量和几何保真度方面均优于现有最先进的基线模型。
conclusion: 通过显式嵌入点云先验，Points-to-3D 实现了更精确且结构可控的高质量 3D 生成。
---

## 摘要
近期 3D 生成的进展主要由以图像或文本为条件的模型驱动，而现成的 3D 先验仍未得到充分利用。在许多现实场景中，可见区域的点云很容易通过 LiDAR 等主动传感器或 VGGT 等前馈预测器获得，它们提供了当前方法未能利用的显式几何约束。在这项工作中，我们介绍了 Points-to-3D，这是一个基于扩散的框架，利用点云先验进行几何可控的 3D 资产和场景生成。Points-to-3D 构建在潜空间 3D 扩散模型 TRELLIS 之上，首先将纯噪声的稀疏结构潜变量初始化替换为针对点云先验定制的输入公式。随后，在 TRELLIS 框架内使用旨在学习全局结构修复的任务特定数据训练的结构修复网络，通过阶段性采样策略（结构修复后进行边界细化）进行推理，在保留输入先验可见区域的同时补全全局几何。在实践中，Points-to-3D 可以接受精确的点云先验或从单张图像中由 VGGT 估计的点云作为输入。在物体和场景方案上的实验一致表明，在渲染质量和几何保真度方面，该方法优于最先进的基准模型，突显了显式嵌入点云先验对于实现更准确且结构可控的 3D 生成的有效性。

## Abstract
Recent progress in 3D generation has been driven largely by models conditioned on images or text, while readily available 3D priors are still underused. In many real-world scenarios, the visible-region point cloud are easy to obtain from active sensors such as LiDAR or from feed-forward predictors like VGGT, offering explicit geometric constraints that current methods fail to exploit. In this work, we introduce Points-to-3D, a diffusion-based framework that leverages point cloud priors for geometry-controllable 3D asset and scene generation. Built on a latent 3D diffusion model TRELLIS, Points-to-3D first replaces pure-noise sparse structure latent initialization with a point cloud priors tailored input formulation.A structure inpainting network, trained within the TRELLIS framework on task-specific data designed to learn global structural inpainting, is then used for inference with a staged sampling strategy (structural inpainting followed by boundary refinement), completing the global geometry while preserving the visible regions of the input priors.In practice, Points-to-3D can take either accurate point-cloud priors or VGGT-estimated point clouds from single images as input. Experiments on both objects and scene scenarios consistently demonstrate superior performance over state-of-the-art baselines in terms of rendering quality and geometric fidelity, highlighting the effectiveness of explicitly embedding point-cloud priors for achieving more accurate and structurally controllable 3D generation.

---

## 论文详细总结（自动生成）

### 论文总结：Points-to-3D：基于点云先验的结构感知 3D 生成

#### 1. 核心问题与整体含义（研究动机和背景）
当前的 3D 生成模型（如 TRELLIS、LGM 等）主要依赖 2D 图像或文本提示词作为条件。尽管这些模型能生成视觉上合理的 3D 资产，但它们往往缺乏**几何可控性**，即无法严格遵循真实的 3D 测量数据。
在现实应用中，通过 LiDAR 传感器或前馈深度预测器（如 VGGT）获取局部点云是非常容易的。然而，现有的生成管线未能有效利用这些显式的几何约束。本论文的核心问题在于：**如何将不完整的点云先验显式地集成到 3D 扩散模型中，以实现既能保持观测区域精确几何、又能合理补全未观测区域的高质量 3D 生成。**

#### 2. 方法论
Points-to-3D 建立在 **TRELLIS**（一种基于稀疏结构潜变量的 3D 扩散模型）之上，其核心流程如下：

*   **核心思想**：将 3D 生成任务重新定义为一个**潜空间内的结构修复（Inpainting）问题**。
*   **输入公式化**：
    1.  将输入的可见区域点云 $P$ 体素化，并通过预训练的 VAE 编码器转换为稀疏结构（SS）潜变量 $q_{vis}$。
    2.  提取可见性掩码 $m_s$，将 $q_{vis}$ 与高斯噪声 $\epsilon_s$ 结合，形成混合输入 $q_{comb}$。
    3.  将掩码 $m_s$ 与潜变量在通道维度拼接，作为修复网络 $G_{inp}$ 的输入。
*   **关键技术细节**：
    *   **结构修复网络**：微调 TRELLIS 的 Flow Transformer，使其学习从局部几何推断全局结构。
    *   **训练数据构造**：通过渲染已知 3D 模型的深度图并提取可见点云，构造“局部-完整”对进行监督训练。
    *   **分阶段采样策略**：
        *   **阶段 1：结构修复**（前 $s$ 步）：利用掩码强制保持输入点云的几何形状。
        *   **阶段 2：边界细化**（后 $t-s$ 步）：移除掩码约束进行全局优化，消除生成区域与原始区域之间的接缝或空洞。

#### 3. 实验设计
*   **数据集与场景**：
    *   **物体级**：Toys4K（测试）、3D-FUTURE、ABO、HSSD（训练）。
    *   **场景级**：3D-FRONT。
    *   **真实世界**：Pix3D。
*   **Benchmark 与对比方法**：
    *   对比了多种 SOTA 模型：TRELLIS, GaussianAnything, Real3D, LGM, VoxHammer（3D 反演版）, SAM3D, SceneGen, MIDI。
*   **评估指标**：
    *   **渲染质量**：PSNR, SSIM, LPIPS, DINO 相似度。
    *   **几何保真度**：Chamfer Distance (CD), F-Score, 法线图 PSNR/LPIPS。

#### 4. 资源与算力
*   **硬件**：使用了 **4 张 Nvidia A100 GPU**。
*   **训练配置**：迭代次数为 20k 次，Batch Size 为 8。
*   **推理**：采样步数设为 50 步（25 步修复 + 25 步细化）。
*   **时长**：文中未明确给出总训练时长，但基于 20k 迭代和 A100 算力，预计训练时间在数天之内。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 Toys4K 和 3D-FRONT 上各使用了 500 个样本进行定量评估。
    *   涵盖了**精确点云输入**和**VGGT 预测点云输入**两种实用场景。
    *   进行了**多视图输入**（3 视图）和**文本到 3D** 的扩展实验。
*   **消融实验**：详细探讨了采样步数分配（修复步数 vs 细化步数）对生成质量的影响。
*   **充分性评价**：实验设计非常充分，不仅对比了生成质量，还专门分析了“可见区域”的几何对齐度。对比方法涵盖了从单图生成到 3D 编辑的多种类型，评估维度客观且全面。

#### 6. 主要结论与发现
*   **几何保真度大幅提升**：在可见区域，Points-to-3D 的 F-Score 达到了 0.998，远超所有基线模型，证明了其对 3D 先验的极强保持能力。
*   **全局一致性**：通过分阶段采样，模型能有效消除修复边界的空洞，生成比直接反演（Inversion）更连贯的未观测区域。
*   **鲁棒性**：即使点云是由 VGGT 预测的（存在一定误差），该方法依然能显著提升生成资产的结构准确性。

#### 7. 优点
*   **显式控制**：解决了扩散模型在 3D 生成中“随机性强、控制力弱”的痛点，使传感器数据能直接指导生成。
*   **分阶段采样设计**：巧妙地平衡了“硬约束保持”与“软边界平滑”之间的矛盾。
*   **通用性强**：支持物体和复杂场景生成，且能与文本、多图等多种模态结合。

#### 8. 不足与局限
*   **依赖先验质量**：如果输入的点云预测（如 VGGT）存在严重偏差，生成的几何也会受到误导。
*   **分辨率限制**：受限于 TRELLIS 潜空间的体素分辨率（16^3），对于极细微结构的刻画可能仍有提升空间。
*   **计算开销**：虽然是前馈生成，但相比于纯 2D 扩散模型，3D 潜空间的 VAE 编码和修复仍需一定的计算资源。
*   **应用限制**：目前主要针对静态资产和场景，尚未讨论动态或 4D 场景的扩展。

（完）
