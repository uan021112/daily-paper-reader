---
title: Towards High-Fidelity CAD Generation via LLM-Driven Program Generation and Text-Based B-Rep Primitive Grounding
title_zh: 通过大语言模型驱动的程序生成与基于文本的 B-Rep 基元定位实现高保真 CAD 生成
authors: "Jiahao Li, Qingwang Zhang, Qiuyu Chen, Guozhan Qiu, Yunzhong Lou, Xiangdong Zhou"
date: 2026-03-12
pdf: "https://arxiv.org/pdf/2603.11831v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 用于高保真3D生成的文本到CAD框架
tldr: FutureCAD是一个高保真文本到CAD生成框架，旨在解决参数化建模与边界表示（B-Rep）合成之间的脱节问题。它利用大语言模型生成可执行的CadQuery脚本，并引入BRepGround转换器，通过自然语言查询实现几何基元的精准定位。通过监督微调和强化学习，该方法在处理复杂工业产品设计方面达到了SOTA水平，显著提升了CAD生成的精确度与实用性。
selection_source: fresh_fetch
motivation: 现有的CAD生成方法在参数化建模与B-Rep基元选择之间存在脱节，限制了其在复杂工业设计中的应用。
method: 提出FutureCAD框架，结合LLM生成CadQuery脚本，并利用BRepGround转换器将文本查询映射到具体的B-Rep几何基元。
result: 实验表明，FutureCAD在复杂CAD模型生成任务中取得了当前最先进（SOTA）的性能表现。
conclusion: 通过结合LLM的程序生成能力与B-Rep基元定位技术，FutureCAD为高保真、可编辑的工业级CAD自动建模提供了有效路径。
---

## 摘要
计算机辅助设计（CAD）生成领域近年来取得了显著进展。现有方法通常分为两个独立的类别：参数化 CAD 建模和直接边界表示（B-Rep）合成。在现代基于特征的 CAD 系统中，参数化建模与 B-Rep 本质上是交织在一起的，因为高级参数化操作（如圆角和倒角）需要显式选择 B-Rep 几何基元，而 B-Rep 本身又是从参数化操作中推导出来的。因此，这种范式差距仍然是限制 AI 驱动的复杂工业产品设计 CAD 建模的关键因素。本文提出了 FutureCAD，这是一个利用大语言模型（LLM）和 B-Rep 定位 Transformer（BRepGround）实现高保真 CAD 生成的新型文本到 CAD 框架。我们的方法生成可执行的 CadQuery 脚本，并引入了一种基于文本的查询机制，使 LLM 能够通过自然语言指定几何选择，随后由 BRepGround 将其定位到目标基元。为了训练我们的框架，我们构建了一个包含真实世界 CAD 模型的新数据集。对于 LLM，我们应用监督微调（SFT）来建立基础的 CAD 生成能力，随后通过强化学习（RL）来提高泛化能力。实验表明，FutureCAD 达到了最先进的 CAD 生成性能。

## Abstract
The field of Computer-Aided Design (CAD) generation has made significant progress in recent years. Existing methods typically fall into two separate categorie: parametric CAD modeling and direct boundary representation (B-Rep) synthesis. In modern feature-based CAD systems, parametric modeling and B-Rep are inherently intertwined, as advanced parametric operations (e.g., fillet and chamfer) require explicit selection of B-Rep geometric primitives, and the B-Rep itself is derived from parametric operations. Consequently, this paradigm gap remains a critical factor limiting AI-driven CAD modeling for complex industrial product design. This paper present FutureCAD, a novel text-to-CAD framework that leverages large language models (LLMs) and a B-Rep grounding transformer (BRepGround) for high-fidelity CAD generation. Our method generates executable CadQuery scripts, and introduces a text-based query mechanism that enables the LLM to specify geometric selections via natural language, which BRepGround then grounds to the target primitives. To train our framework, we construct a new dataset comprising real-world CAD models. For the LLM, we apply supervised fine-tuning (SFT) to establish fundamental CAD generation capabilities, followed by reinforcement learning (RL) to improve generalization. Experiments show that FutureCAD achieves state-of-the-art CAD generation performance.

---

## 论文详细总结（自动生成）

### 论文结构化深度总结：FutureCAD

#### 1. 核心问题与整体含义（研究动机和背景）
现代计算机辅助设计（CAD）生成面临一个核心矛盾：**参数化建模**（提供可编辑性但难以处理圆角、倒角等依赖几何基元的操作）与**边界表示（B-Rep）合成**（几何精确但缺乏设计意图和可编辑性）之间的脱节。
在真实的工业设计中，这两者是高度耦合的：高级特征（如圆角）必须在执行时显式选择 B-Rep 中的特定面或边。现有的 AI 方法通常将两者孤立对待，导致无法生成包含复杂工业细节的高保真模型。本论文旨在通过 LLM 驱动的程序生成与语义化的基元定位（Grounding）来弥合这一鸿沟。

#### 2. 方法论：核心思想与关键技术
论文提出了 **FutureCAD** 框架，其核心流程如下：
*   **LLM 驱动的程序生成**：使用大语言模型（如 Qwen2.5）生成可执行的 **CadQuery** 脚本。当脚本涉及需要选择几何基元的操作（如 `fillet`）时，LLM 不直接预测坐标，而是生成一段**自然语言查询**（例如：“顶部的所有圆形孔洞”）。
*   **BRepGround 定位模块**：这是一个专门的 Transformer 架构。
    *   **编码器**：使用 BERT 编码文本查询，使用 UV-Net 编码 B-Rep 的面和边（几何与拓扑信息）。
    *   **融合层**：通过交叉注意力机制（Cross-Attention）将文本语义与几何特征融合。
    *   **预测头**：对 B-Rep 中的每个基元进行二分类评分，确定哪些基元符合 LLM 的查询描述。
*   **两阶段训练策略**：
    1.  **监督微调 (SFT)**：在 140k 规模的数据集上训练 LLM，使其掌握 CadQuery 语法和语义查询生成。
    2.  **强化学习 (RL)**：采用 **GSPO (Group Sequence Policy Optimization)** 算法。以生成的 CAD 模型与真值之间的 **Chamfer Distance (CD)** 作为奖励信号，优化模型的几何精确度和程序可执行率。

#### 3. 实验设计
*   **数据集**：构建了 **FutureCAD 数据集**，包含超过 140k 个来自真实世界的工业 CAD 模型（解析自 ABC 数据集）。该数据集包含高级特征（圆角、倒角、抽壳）、详细的几何标注以及对应的 CadQuery 代码。
*   **Benchmark 与对比方法**：
    *   **任务**：文本到 CAD 生成、基于文本的 B-Rep 基元定位。
    *   **对比方法**：包括 CAD-Translator, Text2CAD, CADFusion, CAD-LLaMA 等参数化方法，以及针对定位任务的 CLIP-DE 和 Late Fusion 等基准。
    *   **指标**：G-F1/R-F1（命令准确率）、Chamfer Distance（几何相似度）、Invalidity Ratio（IR，非法率）。
*   **泛化性测试**：在 **Fusion360** 数据集上进行了分布外（OOD）测试。

#### 4. 资源与算力
论文中提到：
*   **模型底座**：使用 Qwen2.5-7B-Instruct 作为 LLM 骨干。
*   **训练细节**：SFT 阶段训练 3 个 Epoch；RL 阶段使用 veRL 框架，每个 Prompt 采样 8 个候选响应进行策略优化。
*   **算力说明**：**文中未明确给出具体的 GPU 型号、数量及总训练时长**。但根据 7B 模型和 140k 数据量的 SFT+RL 流程推断，通常需要多节点 A100 或 H100 集群支持。

#### 5. 实验数量与充分性
*   **实验规模**：论文在 140k 规模的大型数据集上进行了验证，并区分了“标准”和“高级”两个子集进行细化评估。
*   **消融实验**：对比了“仅 SFT”、“无 BRepGround（改用点匹配）”以及“完整 FutureCAD”的效果，证明了 RL 和语义定位模块的必要性。
*   **客观性**：通过 OOD 测试（Fusion360）验证了模型的泛化能力，而非仅仅在特定数据集上过拟合。实验设计涵盖了从命令准确性到最终几何质量的全方位评价，较为客观公平。

#### 6. 主要结论与发现
*   **性能领先**：FutureCAD 在几何保真度（CD）和程序合法性（IR）上均达到了 SOTA 水平。
*   **语义定位的优势**：相比于传统的基于坐标或启发式规则的基元选择，基于文本的 BRepGround 能更鲁棒地处理几何歧义。
*   **RL 的作用**：强化学习显著降低了生成程序的非法率（从 42.67% 降至 1.01%），并提升了参数预测的精度。

#### 7. 优点（亮点）
*   **范式统一**：首次将 LLM 的代码生成能力与 B-Rep 的底层几何感知通过“语义查询”有机结合，解决了复杂特征生成的难题。
*   **高质量数据集**：贡献了一个包含 140k 真实人类设计模型的数据集，相比以往的程序增强数据集（如 WHUCAD）更具工业实用价值。
*   **闭环优化**：通过 RL 将几何执行反馈引入训练流程，确保了生成结果的可制造性和准确性。

#### 8. 不足与局限
*   **空间推理限制**：LLM 在处理复杂的 3D 空间相对位置（如多个零件的精确对齐）时仍存在偶发性错误。
*   **类别不平衡**：在 B-Rep 定位任务中，由于目标基元（如特定的圆角边）在整体模型中占比极小，模型有时会因类别不平衡而遗漏细微特征。
*   **文本-模型一致性**：对于极其复杂的自然语言描述，模型生成的几何结构有时会与文本中的空间排列逻辑产生冲突。

（完）
