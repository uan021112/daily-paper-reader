---
title: "OAHuman: Occlusion-Aware 3D Human Reconstruction from Monocular Images"
title_zh: "OAHuman: 基于单目图像的遮挡感知 3D 人体重建"
authors: "Yuanwang Yang, Hongliang Liu, Muxin Zhang, Nan Ma, Jingyu Yang, Yu-Kun Lai, Kun Li"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14249v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 从单张RGB图像进行3D人体重建
tldr: 针对单目图像中由于遮挡导致的3D人体重建不完整和真实感差的问题，本文提出OAHuman框架。该框架通过解耦几何重建与纹理合成，解决了遮挡区域的几何-纹理交叉污染问题。几何重建在遮挡处得到感知增强，而纹理合成仅从可见区域学习，从而在复杂遮挡环境下实现了高保真、结构完整的3D人体模型重建，显著提升了重建的鲁棒性。
selection_source: fresh_fetch
motivation: 现有神经隐式方法在处理遮挡时因几何与纹理建模相互纠缠，导致遮挡区域出现严重的几何缺失和纹理错误。
method: 提出一种解耦感知范式，将几何重建与纹理合成显式分离，确保几何重建不受纹理干扰且纹理合成仅依赖可见区域。
result: 在遮挡丰富的基准测试中，该方法在结构完整性、表面细节和纹理真实感方面均显著优于现有先进技术。
conclusion: OAHuman通过有效的解耦策略，成功解决了单目3D人体重建中遮挡导致的质量退化这一长期挑战。
---

## 摘要
由于周围物体、人员的频繁遮挡或图像截断，现实场景中的单目 3D 人体重建仍然具有极大的挑战性。这种遮挡会导致几何形状缺失和不可靠的外观线索，严重降低了重建人体模型的完整性和真实感。尽管最近的神经隐式方法在干净的输入上取得了令人印象深刻的结果，但由于形状和纹理的纠缠建模，它们在遮挡情况下表现不佳。在本文中，我们提出了 OAHuman，这是一个遮挡感知框架，它显式地解耦了几何重建和纹理合成，以便从单张 RGB 图像中进行鲁棒的 3D 人体建模。核心创新在于解耦感知范式，它解决了遮挡区域几何-纹理交叉污染的根本问题。我们的框架确保了几何重建即使在遮挡区域也能得到感知增强，并将其与纹理干扰隔离开来。与此同时，纹理合成仅从可见区域学习，防止纹理错误转移到遮挡区域。这种解耦方法使 OAHuman 能够在遮挡下实现鲁棒且高保真的重建，这一直是该领域长期存在的挑战。在遮挡丰富的基准测试上进行的广泛实验表明，OAHuman 在结构完整性、表面细节和纹理真实感方面取得了卓越的性能，显著改善了遮挡条件下的单目 3D 人体重建。

## Abstract
Monocular 3D human reconstruction in real-world scenarios remains highly challenging due to frequent occlusions from surrounding objects, people, or image truncation. Such occlusions lead to missing geometry and unreliable appearance cues, severely degrading the completeness and realism of reconstructed human models. Although recent neural implicit methods achieve impressive results on clean inputs, they struggle under occlusion due to entangled modeling of shape and texture. In this paper, we propose OAHuman, an occlusion-aware framework that explicitly decouples geometry reconstruction and texture synthesis for robust 3D human modeling from a single RGB image. The core innovation lies in the decoupling-perception paradigm, which addresses the fundamental issue of geometry-texture cross-contamination in occluded regions. Our framework ensures that geometry reconstruction is perceptually reinforced even in occluded areas, isolating it from texture interference. In parallel, texture synthesis is learned exclusively from visible regions, preventing texture errors from being transferred to the occluded areas. This decoupling approach enables OAHuman to achieve robust and high-fidelity reconstruction under occlusion, which has been a long-standing challenge in the field. Extensive experiments on occlusion-rich benchmarks demonstrate that OAHuman achieves superior performance in terms of structural completeness, surface detail, and texture realism, significantly improving monocular 3D human reconstruction under occlusion conditions.

---

## 论文详细总结（自动生成）

以下是对论文《OAHuman: Occlusion-Aware 3D Human Reconstruction from Monocular Images》的深度结构化总结：

### 1. 核心问题与研究背景
*   **核心问题**：在现实场景中，单目3D人体重建面临严重的**遮挡问题**（如环境物体遮挡、人体自遮挡、图像截断等）。
*   **研究动机**：现有的神经隐式重建方法（如 PIFu, ECON 等）通常假设输入图像是完整且干净的。当存在遮挡时，这些方法往往表现不佳，原因在于：
    1.  **几何与纹理纠缠**：形状和外观在共享的潜在空间中建模，导致遮挡区域的错误相互传播（几何缺失导致纹理混乱，反之亦然）。
    2.  **结构不确定性**：遮挡破坏了人体拓扑，使得不可见区域的几何推断极具挑战。
    3.  **外观不一致**：缺乏可靠的外观线索，导致合成的纹理不真实或不连贯。

### 2. 方法论：解耦感知范式
OAHuman 提出了一个**两阶段框架**，显式地将几何重建与纹理合成解耦：

*   **第一阶段：几何重建（从粗到精）**
    *   **可见性引导的粗略补全（VGCC）**：利用傅里叶占用场（FOF）作为基础表示，并引入拟合的 SMPL 模型作为结构先验。
    *   **特征级监督正则化（FLSR）**：引入一个在干净数据上预训练的“导师网络”，通过分层特征加权（深层特征权重更高以强调全局结构）来引导遮挡区域的特征对齐，减少幻觉。
    *   **双视图法线细化（DVNR）**：预测正反两面的高分辨率法线贴图，将粗略几何与图像细节结合，恢复衣物褶皱等高频几何细节。
*   **第二阶段：几何忠实纹理渲染**
    *   **扩散渲染器**：基于扩散模型（Diffusion Model），以第一阶段生成的精确 3D 法线贴图为条件进行引导。
    *   **参考网络（ReferenceNet）**：从原始遮挡图像中提取风格嵌入，确保生成纹理与可见区域的风格一致。
    *   **几何一致性约束**：通过 3D 几何先验确保多视图下的纹理空间一致性，抑制伪影。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：THuman2.0（高质量 3D 人体扫描数据集）。
    *   **测试集/基准**：CustomHumans、2k2k 数据集，以及从互联网收集的真实世界（In-the-wild）图像。
*   **对比方法**：ECON、SiTH、R2Human、LHM、PSHuman 等近年最先进（SOTA）的方法。
*   **评价指标**：
    *   **几何指标**：倒角距离（CD）、点到表面距离（P2S）、法线误差。
    *   **纹理指标**：SSIM、PSNR、LPIPS（感知相似度）。

### 4. 资源与算力
*   **算力说明**：论文中**未明确说明**具体的 GPU 型号、数量及训练总时长。
*   **实现细节**：提到使用了 CNN 编码器、Transformer 架构（在对比方法中提及）以及基于扩散模型的生成组件，通常这类模型需要高性能显卡（如 NVIDIA A100 或 3090/4090 系列）进行训练。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了大规模的定量对比（在两个主流测试集上）。
    *   **消融实验**：针对 FLSR（特征监督）、DVNR（法线细化）以及不同法线来源对渲染器的影响进行了详细验证。
    *   **鲁棒性分析**：专门测试了在不同遮挡比例（20% 到 80%）下的性能变化曲线。
    *   **对比实验**：还对比了“先 2D 图像修复再 3D 重建”的替代方案，证明了直接 3D 推断的优越性。
*   **充分性评价**：实验设计全面，涵盖了从合成数据到真实复杂场景的转化，对比维度丰富，客观地展示了该方法在极端遮挡下的稳定性。

### 6. 主要结论与发现
*   **解耦的有效性**：显式分离几何与纹理建模能有效防止遮挡区域的“错误污染”。
*   **性能领先**：OAHuman 在所有几何和纹理指标上均优于现有方法，尤其在 CD 和 P2S 距离上提升显著。
*   **高鲁棒性**：即使在遮挡率高达 80% 的情况下，该方法仍能推断出合理的身体结构，而基准方法通常会出现肢体断裂或几何崩溃。

### 7. 优点与亮点
*   **创新的解耦范式**：将遮挡处理从被动补全转变为主动的“感知-推理”过程。
*   **特征级引导**：利用导师网络进行深层特征对齐，比单纯的像素级补全更能捕捉人体全局拓扑。
*   **高质量纹理**：结合了 3D 几何约束与扩散模型的生成能力，解决了单目重建中背面和侧面纹理模糊的问题。

### 8. 不足与局限
*   **分辨率敏感性**：当目标人物在图像中占比极小或分辨率极低时，几何细节恢复和纹理合成的质量会受限。
*   **SMPL 依赖**：虽然对 SMPL 估计误差有一定的容忍度，但极度错误的初始姿态估计仍可能影响最终重建的准确性。
*   **计算开销**：引入扩散模型作为渲染器，虽然提升了质量，但相比于纯隐式函数方法，推理速度可能较慢（文中未详细讨论实时性）。

（完）
