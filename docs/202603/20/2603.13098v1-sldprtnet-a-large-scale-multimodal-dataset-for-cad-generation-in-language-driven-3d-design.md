---
title: "SldprtNet: A Large-Scale Multimodal Dataset for CAD Generation in Language-Driven 3D Design"
title_zh: SldprtNet：一个用于语言驱动 3D 设计中 CAD 生成的大规模多模态数据集
authors: "Ruogu Li, Sikai Li, Yao Mu, Mingyu Ding"
date: 2026-03-13
pdf: "https://arxiv.org/pdf/2603.13098v1"
tags: ["query:partseg"]
score: 10.0
evidence: 用于CAD生成和3D设计的大规模多模态数据集
tldr: 本研究推出了SldprtNet，一个包含超过24.2万个工业零件的大规模多模态CAD数据集，旨在支持语义驱动的3D设计。该数据集提供.step和.sldprt格式模型，并配备了支持13种CAD指令的编解码工具，实现了3D模型与结构化文本的无损转换。通过结合多视角渲染图和Qwen2.5-VL-7B生成的自然语言描述，并经过人工验证，该数据集为CAD生成和跨模态学习提供了高质量的对齐数据。
selection_source: fresh_fetch
motivation: 针对现有CAD数据集在规模、多模态对齐及工业级参数化建模支持方面的不足，旨在推动语言驱动的3D设计发展。
method: 开发了支持13种CAD指令的编解码器，并利用多模态大模型结合多视角渲染图生成并人工校验了零件的功能性描述。
result: 实验证明，在CAD生成任务中，结合图像与文本的多模态输入效果显著优于纯文本输入，验证了多模态数据的价值。
conclusion: SldprtNet作为一个高度对齐、模态多样且涵盖真实工业零件的大规模数据集，为语义驱动的CAD建模提供了关键支持。
---

## 摘要
我们介绍了 SldprtNet，这是一个包含超过 242,000 个工业零件的大规模数据集，旨在用于语义驱动的 CAD 建模、几何深度学习以及 3D 设计多模态模型的训练和微调。该数据集提供 .step 和 .sldprt 两种格式的 3D 模型，以支持多样化的训练和测试。为了实现参数化建模并促进数据集的可扩展性，我们开发了配套工具（编码器和解码器），支持 13 种 CAD 命令，并实现了 3D 模型与结构化文本表示之间的无损转换。此外，每个样本都配有一张由 3D 模型不同视角的七个渲染视图合并而成的复合图像，有效减少了输入 token 长度并加速了推理。通过将该图像与编码器输出的参数化文本相结合，我们利用轻量级多模态语言模型 Qwen2.5-VL-7B 生成了每个零件外观和功能的自然语言描述。为确保准确性，我们对生成的描述、渲染图像和 3D 模型进行了人工验证和对齐。这些描述与参数化建模脚本、渲染图像和 3D 模型文件完全对齐，共同构成了 SldprtNet。为了评估其有效性，我们在数据集子集上微调了基准模型，并对比了“图像+文本”输入与“仅文本”输入的效果。结果证实了多模态数据集对于 CAD 生成的必要性和价值。该数据集具有精心挑选的真实世界工业零件、支持可扩展数据集扩充的配套工具、多样的模态，并确保了模型复杂度和几何特征的多样性，使其成为一个专为语义驱动的 CAD 建模和跨模态学习而构建的综合性多模态数据集。

## Abstract
We introduce SldprtNet, a large-scale dataset comprising over 242,000 industrial parts, designed for semantic-driven CAD modeling, geometric deep learning, and the training and fine-tuning of multimodal models for 3D design. The dataset provides 3D models in both .step and .sldprt formats to support diverse training and testing. To enable parametric modeling and facilitate dataset scalability, we developed supporting tools, an encoder and a decoder, which support 13 types of CAD commands and enable lossless transformation between 3D models and a structured text representation. Additionally, each sample is paired with a composite image created by merging seven rendered views from different viewpoints of the 3D model, effectively reducing input token length and accelerating inference. By combining this image with the parameterized text output from the encoder, we employ the lightweight multimodal language model Qwen2.5-VL-7B to generate a natural language description of each part's appearance and functionality. To ensure accuracy, we manually verified and aligned the generated descriptions, rendered images, and 3D models. These descriptions, along with the parameterized modeling scripts, rendered images, and 3D model files, are fully aligned to construct SldprtNet. To assess its effectiveness, we fine-tuned baseline models on a dataset subset, comparing image-plus-text inputs with text-only inputs. Results confirm the necessity and value of multimodal datasets for CAD generation. It features carefully selected real-world industrial parts, supporting tools for scalable dataset expansion, diverse modalities, and ensured diversity in model complexity and geometric features, making it a comprehensive multimodal dataset built for semantic-driven CAD modeling and cross-modal learning.

---

## 论文详细总结（自动生成）

以下是对论文《SldprtNet: A Large-Scale Multimodal Dataset for CAD Generation in Language-Driven 3D Design》的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：现有的 CAD 数据集（如 ABC, ShapeNet, Fusion 360）存在规模有限、缺乏多模态对齐（尤其是自然语言与参数化建模序列的对齐）以及不支持复杂工业特征等问题，限制了“语言驱动 3D 设计”和“文本转 CAD（Text-to-CAD）”技术的发展。
*   **研究背景**：CAD 建模不仅是几何形状的堆叠，更包含设计意图和参数化逻辑。传统的网格（Mesh）或点云格式不可编辑，而现有的参数化数据集往往只包含简单的拉伸操作，缺乏真实工业零件的复杂性和自然语言描述。

### 2. 方法论
*   **核心思想**：构建一个包含 24.2 万个真实工业零件的大规模、多模态、高度对齐的数据集，并提供配套的编解码工具实现 3D 模型与结构化文本的无损转换。
*   **关键技术细节**：
    *   **五种模态对齐**：每个样本包含源模型（.sldprt）、标准几何（.step）、七视角复合渲染图（.png）、参数化建模脚本（Encoder_txt）和自然语言描述（Des_txt）。
    *   **编解码工具（Encoder/Decoder）**：基于 SolidWorks API 开发，支持 13 种核心 CAD 指令（如草图、拉伸、旋转、孔向导、圆角、阵列等），远超以往研究（如 DeepCAD 仅支持 2 种）。
    *   **自动化流水线**：通过宏脚本自动抓取模型、渲染图像、提取特征树并转换格式。
    *   **语义生成**：利用多模态大模型 Qwen2.5-VL-7B，结合渲染图和参数化脚本，生成描述零件功能与外观的自然语言，并经过人工校验。

### 3. 实验设计
*   **数据集/场景**：从 GrabCAD、McMaster-Carr 和 FreeCAD 收集了约 68 万个模型，最终精选出 242,606 个高质量工业零件。
*   **Benchmark 与对比方法**：
    *   **基准模型**：对比了纯文本模型 **Qwen2.5-7B**（仅输入参数化文本）与多模态模型 **Qwen2.5-7B-VL**（输入图像+参数化文本）。
    *   **评估指标**：精确匹配得分（Exact Match）、BLEU 分数、指令级 F1 值（Command-Level F1）、参数容差准确率（Tolerance Accuracy）和部分匹配率（Partial Match Rate）。
*   **实验设置**：在 5 万个样本的子集上进行微调训练，并在 3644 个测试样本上评估。

### 4. 资源与算力
*   **算力消耗**：使用了 **12 台 NVIDIA A100 GPU**。
*   **时长**：仅在生成 24.2 万个零件的自然语言描述（推理阶段）就耗费了 **368 个 GPU 小时**。

### 5. 实验数量与充分性
*   **实验规模**：论文对 24.2 万个样本进行了全面的统计分析（包括特征分布和复杂度分级）。基准测试使用了 5 万个样本进行微调，这在 CAD 生成领域属于较大规模。
*   **充分性与公平性**：
    *   实验将模型复杂度分为四个等级（简单、中等、高级、专家），确保了评估的全面性。
    *   对比实验在相同的模型架构（Qwen2.5）下进行，仅改变输入模态，客观地证明了多模态信息的价值。
    *   通过人工验证确保了生成描述的准确性。

### 6. 主要结论与发现
*   **多模态的必要性**：多模态模型（Qwen2.5-7B-VL）在几乎所有关键指标上均优于纯文本模型。例如，指令级 F1 值从 0.3247 提升至 0.3670，部分匹配率从 0.5554 提升至 0.6162。
*   **几何语义理解**：引入渲染图作为辅助模态，显著增强了模型对几何语义和程序逻辑的理解能力。
*   **工业实用性**：数据集中的特征分布（如草图和圆角的高频使用）符合真实工业设计流程。

### 7. 优点与亮点
*   **规模与质量**：24.2 万个样本是目前已知最大的支持多模态对齐的工业 CAD 数据集之一。
*   **无损转换**：开发的编解码工具实现了从 3D 几何到可读文本的闭环，支持参数化编辑和逆向工程。
*   **特征丰富度**：支持 13 种 CAD 操作，能够覆盖绝大多数工业零件的建模需求。
*   **高效推理设计**：将七个视角合并为一张图，有效降低了多模态模型的 Token 消耗。

### 8. 不足与局限
*   **软件依赖**：编解码工具高度依赖 SolidWorks 及其 API，这是一种商业专有软件，限制了在开源环境（如 Linux 服务器）中的直接运行。
*   **特征覆盖上限**：虽然支持 13 种特征，但相比 SolidWorks 数百种复杂功能（如曲面建模、钣金等），仍有扩展空间。
*   **长尾分布**：专家级复杂度（100 个以上特征）的零件仅占 0.51%，对于极端复杂零件的生成能力可能仍需进一步验证。
*   **应用限制**：目前主要关注零件级（Part）生成，尚未涉及更复杂的装配体（Assembly）建模。

（完）
