---
title: "OneWorld: Taming Scene Generation with 3D Unified Representation Autoencoder"
title_zh: OneWorld：利用 3D 统一表示自动编码器实现场景生成
authors: "Sensen Gao, Zhaoqing Wang, Qihang Cao, Dongdong Yu, Changhu Wang, Tongliang Liu, Mingming Gong, Jiawang Bian"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16099v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 统一表示空间中基于扩散的3D场景生成
tldr: OneWorld 针对现有 3D 场景生成方法在 2D 潜空间操作导致的一致性难题，提出在统一的 3D 表征空间内直接进行扩散生成。核心是 3D 统一表征自编码器（3D-URAE），结合了几何、外观和语义信息。通过跨视图对应一致性损失（CVC）和流形漂移强制（MDF）技术，显著提升了生成场景的跨视图一致性和结构稳定性，超越了现有基于 2D 的方法。
selection_source: fresh_fetch
motivation: 现有的基于 2D 潜空间的 3D 生成方法难以维持跨视图的外观和几何一致性。
method: 提出 3D 统一表征自编码器（3D-URAE），并引入跨视图对应一致性损失（CVC）和流形漂移强制（MDF）策略。
result: 实验证明 OneWorld 在生成高质量 3D 场景方面表现出色，且跨视图一致性优于目前的 SOTA 2D 方法。
conclusion: 该研究通过在 3D 统一表征空间进行扩散，为实现高一致性的 3D 场景生成提供了有效的解决方案。
---

## 摘要
现有的基于扩散的 3D 场景生成方法主要在 2D 图像/视频潜空间中运行，这使得维持跨视图外观和几何一致性具有内在的挑战性。为了弥补这一差距，我们提出了 OneWorld，这是一个直接在连贯的 3D 表示空间内执行扩散的框架。我们方法的核心是 3D 统一表示自动编码器 (3D-URAE)；它利用预训练的 3D 基础模型，通过注入外观并将语义蒸馏到统一的 3D 潜空间中，增强了其以几何为中心的特性。此外，我们引入了令牌级跨视图对应 (CVC) 一致性损失，以显式地强制执行跨视图的结构对齐，并提出了流形漂移强制 (MDF)，通过混合漂移表示和原始表示来减轻训练与推理之间的曝光偏差，并塑造鲁棒的 3D 流形。全面的实验表明，与最先进的基于 2D 的方法相比，OneWorld 生成的高质量 3D 场景具有更优越的跨视图一致性。我们的代码将在 https://github.com/SensenGao/OneWorld 上发布。

## Abstract
Existing diffusion-based 3D scene generation methods primarily operate in 2D image/video latent spaces, which makes maintaining cross-view appearance and geometric consistency inherently challenging. To bridge this gap, we present OneWorld, a framework that performs diffusion directly within a coherent 3D representation space. Central to our approach is the 3D Unified Representation Autoencoder (3D-URAE); it leverages pretrained 3D foundation models and augments their geometry-centric nature by injecting appearance and distilling semantics into a unified 3D latent space. Furthermore, we introduce token-level Cross-View-Correspondence (CVC) consistency loss to explicitly enforce structural alignment across views, and propose Manifold-Drift Forcing (MDF) to mitigate train-inference exposure bias and shape a robust 3D manifold by mixing drifted and original representations. Comprehensive experiments demonstrate that OneWorld generates high-quality 3D scenes with superior cross-view consistency compared to state-of-the-art 2D-based methods. Our code will be available at https://github.com/SensenGao/OneWorld.

---

## 论文详细总结（自动生成）

以下是对论文《OneWorld: Taming Scene Generation with 3D Unified Representation Autoencoder》的结构化深入总结：

### 1. 核心问题与整体含义
*   **研究动机**：现有的 3D 场景生成方法（如基于视频扩散模型的方法）大多在 2D 图像或视频的潜空间（Latent Space）中运行。由于 2D 潜空间缺乏显式的 3D 几何约束，生成的场景在不同视角间往往存在外观不一致、几何畸变或结构漂移等问题。
*   **核心目标**：提出一个名为 **OneWorld** 的框架，旨在直接在一个统一的、具有几何感知能力的 **3D 表征空间**内进行扩散生成，从而从根本上解决跨视图一致性难题。

### 2. 方法论
OneWorld 的核心思想是将 3D 场景生成任务从 2D 潜空间转移到 3D 统一表征空间。
*   **3D 统一表征自编码器 (3D-URAE)**：
    *   基于预训练的 3D 基础模型（如 $\pi^3$），通过**外观注入（Appearance Injection）**分支引入细粒度纹理信息，弥补语义令牌（Tokens）的细节丢失。
    *   通过**语义蒸馏（Semantic Distillation）**分支，将视觉基础模型（如 DINOv2）的知识转移到 3D 令牌中，构建一个紧凑且具有语义信息的 3D 流形。
*   **跨视图对应一致性 (CVC) 损失**：
    *   在扩散模型（DiT）训练期间，引入令牌级的对应损失。通过计算目标视图与条件视图令牌之间的余弦相似度，强制模型在去噪过程中保持正确的结构对应关系，而非仅仅减小像素误差。
*   **流形漂移强制 (MDF) 策略**：
    *   针对扩散模型常见的“训练-推理曝光偏差（Exposure Bias）”问题，MDF 在训练 3D 解码器时，将扩散模型生成的“漂移（带误差）”表征与“真值（Ground Truth）”表征进行混合训练。这增强了解码器的鲁棒性，使其能将偏离流形的采样结果映射回高质量的 3D 几何。

### 3. 实验设计
*   **数据集**：使用了两个大规模真实世界多视图数据集：**RealEstate10K (Re10K)**（室内/建筑）和 **DL3DV-10K**（多样化真实场景），共计约 70,000 个场景。
*   **评估协议**：
    *   **有参考评估**：单图条件下的新视角合成（NVS），对比 PSNR、SSIM、LPIPS 以及 VBench 指标。
    *   **无参考评估**：使用 **WorldScore** 评估协议，衡量 3D 一致性、光度一致性、风格一致性及主观质量。
*   **对比方法**：包括 LVSM、Gen3C、GF (Geometry Forcing)、Aether、FlashWorld 和 Gen3R 等最先进的（SOTA）3D/世界生成模型。

### 4. 资源与算力
*   **硬件环境**：使用了 **32 张 NVIDIA A100-SXM4-80G GPU**。
*   **训练时长**：
    *   3D-URAE 微调：30,000 步（全局 Batch Size 为 64）。
    *   DiT 扩散模型训练：100,000 步（全局 Batch Size 为 256）。
    *   MDF 策略训练：10,000 步。
*   **模型规模**：DiT 主干网络初始化自 Wan-2.1-T2I-1.3B。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流大规模数据集上进行了详尽测试，涵盖了室内和室外场景。
*   **消融实验**：论文针对外观注入、语义蒸馏、CVC 损失、MDF 策略分别进行了消融分析，验证了每个组件对提升 PSNR 和跨视图一致性的贡献。
*   **客观性**：通过引入第三方基准（WorldScore, VBench）和多种量化指标，实验结果较为客观、公平，且定性对比图清晰展示了其在几何稳定性上的优势。

### 6. 主要结论与发现
*   **3D 空间扩散的优越性**：直接在 3D 表征空间进行扩散比在 2D 潜空间更有效，能显著减少视角切换时的闪烁和变形。
*   **语义与几何的统一**：通过蒸馏 VFM 语义，3D 令牌变得更容易被扩散模型建模，加速了收敛并提升了生成质量。
*   **解决采样漂移**：MDF 策略被证明是稳定长路径采样、提升解码鲁棒性的关键，有效缓解了推理时的质量退化。

### 7. 优点（亮点）
*   **架构创新**：打破了依赖 2D VAE 的传统路径，利用 3D 基础模型构建统一表征空间。
*   **一致性极高**：生成的 3DGS（3D Gaussian Splatting）表示具有极强的空间连续性。
*   **端到端推理**：支持从单张图像快速生成可交互的 3D 场景，无需逐场景优化。

### 8. 不足与局限
*   **分辨率限制**：受限于显存和计算成本，目前训练和生成的图像分辨率（224x448）相对较低，精细纹理仍有提升空间。
*   **数据依赖**：模型性能高度依赖于高质量的相机轨迹标注数据，对于无标定数据的泛化能力尚待验证。
*   **算力门槛**：训练过程需要大量的 A100 算力，对于资源受限的研究团队具有一定挑战。

（完）
