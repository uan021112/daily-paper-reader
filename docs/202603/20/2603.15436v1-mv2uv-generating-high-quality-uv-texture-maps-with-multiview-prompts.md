---
title: "MV2UV: Generating High-quality UV Texture Maps with Multiview Prompts"
title_zh: MV2UV：利用多视图提示生成高质量 UV 纹理贴图
authors: "Zheng Zhang, Qinchuan Zhang, Yuteng Ye, Zhi Chen, Penglei Ji, Mengfei Li, Wenxiao Zhang, Yuan Liu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.15436v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 使用多视图提示为3D资产生成高质量UV纹理贴图
tldr: 针对3D资产纹理生成中存在的多视角不一致和遮挡部分缺失问题，本文提出MV2UV方法。该方法结合了多视角生成的2D先验与UV细化的修复能力，通过在UV空间构建生成模型，同时修复未见区域并解决视角间的不一致性。实验证明，MV2UV在处理遮挡区域和提升纹理质量方面优于现有方法，能生成高质量且一致的UV纹理贴图。
selection_source: fresh_fetch
motivation: 现有多视角纹理生成方法难以保证视角间的一致性且常导致遮挡区域纹理缺失，而传统UV修复方法缺乏有效的2D生成先验。
method: 提出一种UV空间生成模型，通过整合多视角图像的2D先验来同步修复未见区域并消除视角间的不一致性。
result: 实验表明，MV2UV在纹理生成质量上显著优于现有方法，尤其在处理复杂遮挡和多视角冲突部分表现优异。
conclusion: MV2UV成功结合了多视角生成先验与UV空间修复能力，为生成高质量、一致性的3D纹理贴图提供了新思路。
---

## 摘要
为 3D 资产生成高质量纹理是一项具有挑战性的任务。现有的多视图纹理生成方法面临多视图不一致和未见部分纹理缺失的问题，而 UV 修复纹理方法由于 UV 数据不足导致泛化能力较差，且无法很好地利用 2D 图像扩散先验。在本文中，我们提出了一种名为 MV2UV 的新方法，它结合了来自多视图生成的 2D 生成先验和 UV 精细化的修复能力，以获得高质量的纹理贴图。我们的核心思想是采用一种 UV 空间生成模型，在解决多视图图像不一致性的同时，同步修复多视图图像中的未见部分。实验表明，我们的方法比现有方法具有更好的纹理生成质量，特别是在未见的遮挡部分和多视图不一致的部分。

## Abstract
Generating high-quality textures for 3D assets is a challenging task. Existing multiview texture generation methods suffer from the multiview inconsistency and missing textures on unseen parts, while UV inpainting texture methods do not generalize well due to insufficient UV data and cannot well utilize 2D image diffusion priors. In this paper, we propose a new method called MV2UV that combines 2D generative priors from multiview generation and the inpainting ability of UV refinement to get high-quality texture maps. Our key idea is to adopt a UV space generative model that simultaneously inpaints unseen parts of multiview images while resolving the inconsistency of multiview images. Experiments show that our method enables a better texture generation quality than existing methods, especially in unseen occluded and multiview-inconsistent parts.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **MV2UV** 的新型 3D 纹理生成框架，旨在解决现有方法在多视角一致性和遮挡区域纹理缺失方面的痛点。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何为 3D 模型生成既具有高视觉质量、又具备空间一致性且覆盖完整的 UV 纹理贴图。
*   **研究背景**：
    *   **多视角方法（Multiview-based）**：通过生成多张 2D 图像并反向投影到网格。缺点是视角间存在不一致（导致模糊或重影），且无法处理被遮挡的“死角”。
    *   **UV 空间方法（UV-based）**：直接在 UV 贴图上生成。缺点是 UV 空间缺乏 3D 语义逻辑，且缺乏高质量的 UV 训练数据，导致生成的纹理结构混乱。
*   **整体含义**：MV2UV 提出将生成的“多视角图像”作为“语义提示（Prompts）”，引导 UV 空间的扩散模型进行纹理合成。这种方式结合了 2D 扩散模型的强大先验和 UV 空间修复的完整性。

### 2. 方法论
MV2UV 采用两阶段流程：**多视角生成** + **图像提示的 UV 生成**。

*   **核心思想**：不直接投影图像，而是利用多视角图像作为参考特征，通过注意力机制在 UV 空间“画”出纹理。
*   **关键技术细节**：
    *   **几何位置编码（Geometric Position Encoding）**：这是本文的核心创新。利用 3D 坐标（XYZ）和法线作为位置编码，将 UV 空间的像素与多视角图像中的像素在物理空间上对齐。
    *   **参考注意力层（Reference Attention）**：UV 空间的像素会根据 3D 坐标，动态地去多视角图像中寻找对应的语义特征。这使得模型能自动解决视角冲突（取长补短）并修复遮挡区域（参考邻近语义）。
    *   **UV 自注意力层（UV Self-Attention）**：针对 UV 岛屿（Islands）断裂的问题，利用 3D 几何距离引导自注意力，让 3D 空间邻近但在 UV 空间分离的区域保持纹理连续。
    *   **架构设计**：基于 Stable Diffusion XL (SDXL) 进行微调，采用并行注意力结构，保留原始自注意力以维持生成能力，同时加入参考注意力和 UV 自注意力。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：使用 Objaverse 的子集（约 80,000 个样本），经过 X-atlas 重新展 UV。
    *   **测试集**：Google Scanned Objects (GSO) 和 Digital Twin Catalog (DTC)，涵盖家具、织物等多种类别。
*   **Benchmark 与对比方法**：
    *   对比了 **TEXGen**（UV 生成类）、**Hunyuan3D 2.1**、**UniTEX**、**MV-Adapter**（多视角投影类）。
*   **评价指标**：FID（感知质量）、KID（分布差异）、PSNR/SSIM（重建精度）。

### 4. 资源与算力
*   **模型基础**：基于 Stable Diffusion XL (SDXL)。
*   **训练配置**：
    *   **样本量**：80,000 个 3D 模型。
    *   **训练参数**：训练 10 个 epoch，学习率为 5e-5，Batch Size 为 32。
    *   **分辨率**：多视角图像 768×768，UV 贴图 1024×1024。
*   **算力说明**：文中未明确给出具体的 GPU 型号和数量，但根据 SDXL 微调和 Batch Size 32 推测，通常需要 8 张 A100 或同级别 GPU 的计算集群。

### 5. 实验数量与充分性
*   **实验规模**：在 GSO 和 DTC 数据集上各随机抽取 200 个实例进行定量评估。
*   **消融实验**：非常充分。分别验证了“几何位置编码”、“参考注意力层”和“UV 自注意力层”的必要性，并专门针对“遮挡区域修复”和“视角冲突解决”做了定性对比。
*   **客观性**：通过定量指标（FID/KID）和定性视觉对比（放大遮挡细节）双重验证，证明了其在处理复杂几何体时的优越性。

### 6. 主要结论与发现
*   **性能领先**：在 GSO 数据集上 FID 提升了 50.8（对比 TEXGen），在遮挡区域的修复质量比 MV-Adapter 提升了 2 倍。
*   **冲突解决**：MV2UV 能够自动调和多视角图像之间的颜色或位置偏差，生成的纹理比直接投影更清晰、无重影。
*   **语义修复**：即使在多视角图像完全看不见的区域，模型也能根据 3D 几何关联，从已知区域“借用”合理的纹理逻辑进行填充。

### 7. 优点（亮点）
*   **巧妙的对齐方式**：利用 XYZ 坐标作为 Cross-Attention 的位置编码，优雅地解决了 2D 图像与 UV 空间之间的跨模态对应问题。
*   **端到端一致性**：通过 UV 自注意力解决了 UV 展开导致的纹理断裂（Seams）问题。
*   **鲁棒性**：对多视角生成阶段产生的不一致性有很强的容错能力。

### 8. 不足与局限
*   **文字处理能力弱**：由于 SDXL 的 VAE 限制和分辨率瓶颈，纹理中的文字（如标签、说明）容易出现扭曲和模糊。
*   **计算开销**：作为两阶段方法，需要先生成多视角图像再生成 UV，推理时间可能长于纯 UV 生成方法。
*   **依赖 UV 质量**：虽然有 UV 自注意力，但极度糟糕的 UV 展开（如碎片化严重）仍可能影响最终效果。

（完）
