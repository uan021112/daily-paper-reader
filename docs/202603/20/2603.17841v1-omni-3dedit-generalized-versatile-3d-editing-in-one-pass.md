---
title: "Omni-3DEdit: Generalized Versatile 3D Editing in One-Pass"
title_zh: Omni-3DEdit：单次前向传播的通用多功能 3D 编辑
authors: "Chen Liyi, Wang Pengfei, Zhang Guowen, Ma Zhiyuan, Zhang Lei"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17841v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 统一的基于学习的模型，隐式泛化各种3D编辑任务
tldr: Omni-3DEdit 提出了一种统一的、基于学习的 3D 编辑模型，解决了传统方法依赖迭代优化导致的效率低下和任务通用性差的问题。通过构建高质量的多视图编辑数据集，并结合 SEVA 骨干网络与双流 LoRA 模块，该模型实现了在单次前向传播中完成多种 3D 编辑任务（如外观修改、物体移除等），将推理时间从数十分钟缩短至约两分钟，显著提升了 3D 编辑的通用性与效率。
selection_source: fresh_fetch
motivation: 现有的 3D 编辑方法依赖耗时的迭代优化且缺乏处理不同编辑任务（如外观修改与几何移除）的统一框架。
method: 构建了大规模配对多视图编辑数据集，并采用 SEVA 模型作为骨干，结合双流 LoRA 模块实现多视图特征解耦与单次前向推理。
result: 该模型能够高效处理多种 3D 编辑任务，推理速度比传统优化方法快一个数量级，仅需约两分钟。
conclusion: Omni-3DEdit 证明了通过端到端学习可以实现通用且高效的 3D 编辑，为 3D 内容创作提供了新的范式。
---

## 摘要
大多数指令驱动的 3D 编辑方法依赖 2D 模型来引导 3D 表示的显式和迭代优化。然而，这种范式存在两个主要缺点。首先，它缺乏针对不同 3D 编辑任务的通用设计，因为对 3D 几何的显式操作需要依赖于任务的规则，例如，3D 外观编辑需要保留原始源 3D 几何，而 3D 移除则会改变源几何。其次，迭代优化过程非常耗时，通常需要数千次 2D/3D 更新调用。我们提出了 Omni-3DEdit，这是一个统一的、基于学习的模型，它能够隐式地泛化各种 3D 编辑任务。实现该目标的一个关键挑战是用于训练的成对“源-编辑后”多视图资产的稀缺。为了解决这个问题，我们构建了一个数据流水线，合成了大量高质量的成对多视图编辑样本。随后，我们采用预训练生成模型 SEVA 作为骨干网络，通过在序列空间中拼接源视图潜变量与条件标记。我们还提出了一个双流 LoRA 模块来解耦不同的视图线索，从而大大增强了模型的表示学习能力。作为一个基于学习的模型，我们的模型无需耗时的在线优化，可以在单次前向传播中完成各种 3D 编辑任务，将推理时间从数十分钟缩短至约两分钟。大量实验证明了 Omni-3DEdit 的有效性和效率。

## Abstract
Most instruction-driven 3D editing methods rely on 2D models to guide the explicit and iterative optimization of 3D representations. This paradigm, however, suffers from two primary drawbacks. First, it lacks a universal design of different 3D editing tasks because the explicit manipulation of 3D geometry necessitates task-dependent rules, e.g., 3D appearance editing demands inherent source 3D geometry, while 3D removal alters source geometry. Second, the iterative optimization process is highly time-consuming, often requiring thousands of invocations of 2D/3D updating. We present Omni-3DEdit, a unified, learning-based model that generalizes various 3D editing tasks implicitly. One key challenge to achieve our goal is the scarcity of paired source-edited multi-view assets for training. To address this issue, we construct a data pipeline, synthesizing a relatively rich number of high-quality paired multi-view editing samples. Subsequently, we adapt the pre-trained generative model SEVA as our backbone by concatenating source view latents along with conditional tokens in sequence space. A dual-stream LoRA module is proposed to disentangle different view cues, largely enhancing our model's representational learning capability. As a learning-based model, our model is free of the time-consuming online optimization, and it can complete various 3D editing tasks in one forward pass, reducing the inference time from tens of minutes to approximately two minutes. Extensive experiments demonstrate the effectiveness and efficiency of Omni-3DEdit.

---

## 论文详细总结（自动生成）

### Omni-3DEdit: 论文结构化深度总结

#### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的指令驱动 3D 编辑方法（如 Instruct-N2N）主要依赖 2D 扩散模型引导 3D 表示（NeRF 或 3DGS）进行迭代优化。这种范式存在两大痛点：
    1.  **缺乏通用性**：不同编辑任务（如外观修改 vs. 物体移除）需要不同的显式几何处理规则，难以统一。
    2.  **效率低下**：迭代优化过程通常需要数千次调用，耗时数十分钟甚至更久。
*   **整体含义**：论文提出了 **Omni-3DEdit**，这是一个统一的、基于学习的框架，旨在通过单次前向传播（One-Pass）在多视图潜空间内隐式完成各种 3D 编辑任务，将推理时间缩短至约 2 分钟。

#### 2. 方法论
*   **核心思想**：将 3D 编辑转化为一个多视图图像翻译问题。利用预训练的多视图生成模型作为骨干，通过学习如何将一个视图的编辑效果一致地传播到其他所有视图。
*   **关键技术细节**：
    *   **数据流水线**：针对成对 3D 编辑数据稀缺的问题，利用 Gemini-2.5 Pro 生成指令，Qwen-Image 进行单图编辑，并结合 SEVA 模型进行一致性细化（基于 SDEdit 原理），构建了包含移除、添加、外观修改的成对多视图数据集。
    *   **OmniNet 架构**：基于 SEVA 骨干网络，输入包括源视图潜变量、参考编辑视图潜变量、噪声目标潜变量以及相机位姿（Plücker embedding）。
    *   **双流 LoRA (Dual-stream LoRA)**：这是模型的核心改进。为了解决源视图（提供几何背景）和参考视图（提供编辑引导）在特征空间中的冲突，模型引入了独立的“几何 LoRA”和“引导 LoRA”来分别编码这两类信息，并在共享的注意力层中进行交互。
*   **算法流程**：
    1.  输入源 3D 资产的多视图及编辑指令。
    2.  随机选一视图，用 2D 编辑器生成参考图。
    3.  OmniNet 接收所有源视图、参考图及位姿，单次推理生成所有编辑后的视图。
    4.  （可选）使用 AnySplat 等快速重建模型生成最终 3D 资产。

#### 3. 实验设计
*   **数据集与场景**：
    *   **3D 移除**：使用 360-USID（无边界 360 度场景数据集）。
    *   **3D 添加/新视角合成**：使用 CO3Dv2 验证集。
    *   **通用编辑**：涵盖 DL3DV 和 WildRGB-D 等复杂室内外场景。
*   **Benchmark 与对比方法**：
    *   对比了 SPIn-NeRF、Aurafusion360、Gaussian Grouping、GScream 等专门针对移除的方法。
    *   对比了 DGE、GaussianEditor、ViCANeRF 等基于优化的通用编辑方法。
    *   使用了 PSNR、LPIPS、CLIP 分数以及基于 Gemini-2.5 Pro 的多模态大模型评分。

#### 4. 资源与算力
*   **算力配置**：使用了 **16 张 NVIDIA H20 GPU**。
*   **训练细节**：训练迭代次数为 4,000 次，Batch Size 为 32。
*   **推理效率**：推理时间约为 **2 分钟**，而对比的优化类方法通常需要 20-30 分钟。

#### 5. 实验数量与充分性
*   **实验规模**：论文在多个公开数据集上进行了定量评估，并展示了大量定性对比图（包括移除、添加、外观修改及多轮复杂编辑）。
*   **消融实验**：针对网络架构（特征拼接 vs. 序列拼接 vs. 双流 LoRA）、输入信号（位姿、指示符）进行了详细的消融研究。
*   **充分性评价**：实验设计较为全面，不仅覆盖了单一任务，还验证了模型在处理复杂、非分布内（Out-of-distribution）场景时的泛化能力，实验结果具有较强的说服力。

#### 6. 主要结论与发现
*   **通用性验证**：Omni-3DEdit 能够通过同一个模型处理几何改变（移除/添加）和纹理改变（外观修改），证明了隐式学习比显式规则更具通用性。
*   **双流 LoRA 的有效性**：实验发现简单的特征拼接会导致模型忽略源视图几何或编辑引导，而双流架构能显著提升多视图一致性和细节保留。
*   **效率飞跃**：单次前向传播范式在保持甚至超过优化类方法质量的同时，实现了极高的推理效率。

#### 7. 优点
*   **高效统一**：打破了 3D 编辑任务分类处理的局限，实现了真正的“一键式”通用编辑。
*   **无需掩码**：与许多需要手动提供物体掩码（Mask）的方法不同，该模型是端到端自动识别编辑区域的。
*   **数据贡献**：提出了一套自动化的 3D 编辑数据合成方案，为后续研究提供了参考。

#### 8. 不足与局限
*   **数据规模限制**：目前训练数据量（约 0.1M）相对较小，对于极细粒度的编辑（如在手腕上添加细小饰品）可能表现不足。
*   **分辨率瓶颈**：受限于骨干生成模型，处理的分辨率（576x576）尚有提升空间。
*   **依赖首图质量**：最终效果在很大程度上取决于第一步 2D 编辑器生成的参考图质量，若 2D 编辑出错，模型会传播该错误。

（完）
