---
title: "SwiftTailor: Efficient 3D Garment Generation with Geometry Image Representation"
title_zh: SwiftTailor：基于几何图像表示的高效三维服装生成
authors: "Phuc Pham, Uy Dieu Tran, Binh-Son Hua, Phong Nguyen"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.19053v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 使用几何图像高效生成服装3D资产
tldr: SwiftTailor是一个高效的3D服装生成框架，旨在解决现有方法推理速度慢的问题。它通过几何图像表示法统一了缝制模式推理与几何网格合成。该框架包含PatternMaker预测缝制模式和GarmentSewer生成几何图像两个轻量级模块，通过逆映射和动态缝合技术快速重建3D网格，在保持高保真度的同时显著提升了生成效率。
selection_source: fresh_fetch
motivation: 现有的3D服装生成方法依赖大型模型和复杂的物理模拟，导致推理过程极其耗时且效率低下。
method: 提出一种基于几何图像表示的两阶段框架，利用视觉语言模型预测缝制模式，并通过Transformer将其转换为编码3D表面的几何图像。
result: 在Multimodal GarmentCodeData上的实验表明，该方法在达到SOTA精度和视觉效果的同时，大幅缩短了推理时间。
conclusion: SwiftTailor为下一代3D服装生成提供了一个可扩展、可解释且高性能的解决方案。
---

## 摘要
逼真且高效的三维服装生成仍然是计算机视觉和数字时尚领域的一项长期挑战。现有方法通常依赖大型视觉语言模型来生成二维缝制图样的序列化表示，随后利用 GarmentCode 等服装建模框架将其转换为可供仿真的三维网格。尽管这些方法能产生高质量的结果，但其推理时间往往较慢，通常在 30 秒到 1 分钟之间。在这项工作中，我们提出了 SwiftTailor，这是一个新颖的两阶段框架，通过紧凑的几何图像表示将缝制图样推理与基于几何的网格合成相统一。SwiftTailor 包含两个轻量级模块：PatternMaker，一个能从多种输入模态预测缝制图样的高效视觉语言模型；以及 GarmentSewer，一个高效的密集预测变换器，它将这些图样转换为一种新颖的服装几何图像（Garment Geometry Image），在统一的 UV 空间中对所有服装裁片的三维表面进行编码。最终的三维网格通过高效的逆映射过程重建，该过程结合了重网格化和动态缝合算法来直接组装服装，从而分摊了物理仿真的成本。在 Multimodal GarmentCodeData 上的广泛实验表明，SwiftTailor 在显著缩短推理时间的同时，实现了最先进的准确度和视觉保真度。这项工作为下一代三维服装生成提供了一种可扩展、可解释且高性能的解决方案。

## Abstract
Realistic and efficient 3D garment generation remains a longstanding challenge in computer vision and digital fashion. Existing methods typically rely on large vision- language models to produce serialized representations of 2D sewing patterns, which are then transformed into simulation-ready 3D meshes using garment modeling framework such as GarmentCode. Although these approaches yield high-quality results, they often suffer from slow inference times, ranging from 30 seconds to a minute. In this work, we introduce SwiftTailor, a novel two-stage framework that unifies sewing-pattern reasoning and geometry-based mesh synthesis through a compact geometry image representation. SwiftTailor comprises two lightweight modules: PatternMaker, an efficient vision-language model that predicts sewing patterns from diverse input modalities, and GarmentSewer, an efficient dense prediction transformer that converts these patterns into a novel Garment Geometry Image, encoding the 3D surface of all garment panels in a unified UV space. The final 3D mesh is reconstructed through an efficient inverse mapping process that incorporates remeshing and dynamic stitching algorithms to directly assemble the garment, thereby amortizing the cost of physical simulation. Extensive experiments on the Multimodal GarmentCodeData demonstrate that SwiftTailor achieves state-of-the-art accuracy and visual fidelity while significantly reducing inference time. This work offers a scalable, interpretable, and high-performance solution for next-generation 3D garment generation.

---

## 论文详细总结（自动生成）

### 论文总结：SwiftTailor —— 基于几何图像表示的高效三维服装生成

#### 1. 核心问题与整体含义（研究动机和背景）
三维服装生成在数字时尚和计算机视觉领域具有重要意义，但现有方法面临两大瓶颈：
*   **推理效率低下**：现有框架（如 AIpparel, ChatGarment）通常依赖大型视觉语言模型（如 7B 参数的 LLaVA）生成缝制模式，且后续需要复杂的物理仿真引擎（如 GarmentCode, NVIDIA Warp）进行缝合与垂坠模拟，导致单次生成耗时通常在 30 秒至 1 分钟。
*   **物理不稳定性**：基于规则的裁片放置和迭代仿真容易在复杂服装上失败，导致网格撕裂或穿模。
**SwiftTailor** 的研究动机是：能否在不依赖物理仿真的情况下，实现高质量、符合工业标准的 3D 服装生成？

#### 2. 方法论
SwiftTailor 提出了一个两阶段框架，核心思想是利用**服装几何图像（Garment Geometry Image, GGI）**作为中间表示，绕过物理仿真。
*   **第一阶段：PatternMaker**
    *   采用轻量级的视觉语言模型（InternVL-3-2B），通过微调预测服装的缝制模式（包括裁片布局、边缘连接和缝合标签）。
    *   它将离散的结构标记与连续的几何参数（顶点坐标、旋转平移）相结合。
*   **第二阶段：GarmentSewer**
    *   **核心表示 (GGI)**：将 3D 服装表面编码为 2D UV 空间中的三张对齐图像：**语义图**（裁片类型）、**几何图**（3D 坐标像素化）和**缝合图**（边缘缝合关系）。
    *   **网络架构**：使用密集预测变换器（DPT），将 PatternMaker 生成的语义图映射为密集的几何图。
    *   **损失函数**：结合了边缘感知回归损失（$L_{reg}$）、缝合一致性损失（$L_{stitch}$，基于倒角距离）和法线平滑损失（$L_{normal}$）。
*   **后期处理**：通过逆映射过程，利用重网格化（Remeshing）和动态缝合算法（基于动态时间规整 DTW）直接组装 3D 网格。

#### 3. 实验设计
*   **数据集**：使用 **Multimodal GarmentCodeData (GCD-MM)**，这是目前最大的包含文本、图像和缝制模式的多模态服装数据集。
*   **Benchmark 与对比方法**：
    *   对比了 **AIpparel**、**ChatGarment** 和 **SewingLDM**。
    *   评估指标包括：裁片/边缘准确率、缝合准确率、顶点 L2 误差、最小匹配距离（MMD）、覆盖率（COV）以及推理时间。
*   **任务场景**：涵盖了多模态生成（文本+图像）、纯文本生成、纯图像生成以及文本引导的服装编辑。

#### 4. 资源与算力
*   **硬件**：使用了 **4 台 NVIDIA A100 GPU**。
*   **训练时长**：模型训练耗时约 **3 天**。
*   **推理效率**：SwiftTailor 的总推理时间约为 **14.78 秒**，而对比方法通常需要 50-64 秒，实现了约 **4 倍的加速**。

#### 5. 实验数量与充分性
*   **实验规模**：在 5000 多个测试样本上进行了缝制模式评估，并随机抽取 500 个样本进行 3D 指标计算。
*   **消融实验**：针对语义 UV 映射的必要性、回归损失和缝合损失的影响进行了详细的消融研究。
*   **充分性评价**：实验覆盖了从 2D 模式推理到 3D 网格生成的全流程，并对比了不同模块组合（如 PatternMaker + GarmentCode）的效果。实验设计客观，通过 MMD 和 COV 等生成模型标准指标验证了多样性和质量。

#### 6. 主要结论与发现
*   **高效性**：通过 GGI 表示和前馈神经网络，SwiftTailor 显著降低了计算开销，尤其是第二阶段（网格构建）仅需 0.02 秒。
*   **准确性**：尽管参数量更小（2B vs 7B），PatternMaker 在缝制模式预测上的表现优于基于 LLaVA 的基准模型。
*   **鲁棒性**：相比物理仿真引擎，GarmentSewer 提供的初始状态更稳定，减少了因裁片放置不当导致的仿真失败。

#### 7. 优点
*   **创新表示**：引入 GGI 将复杂的 3D 拓扑问题转化为 2D 图像预测问题，极大地简化了生成流程。
*   **模块化设计**：PatternMaker 和 GarmentSewer 均可独立替换或集成到现有工作流中。
*   **工业兼容性**：生成的缝制模式符合 CAD 标准，具有很强的可解释性和可制造性。

#### 8. 不足与局限
*   **细节缺失**：由于模型倾向于学习平滑的几何表示，生成的服装往往**缺乏高频褶皱细节**，需要额外的物理精细化处理。
*   **泛化风险**：实验主要基于合成数据集，对于背景复杂、遮挡严重或极端廓形的“野外（In-the-wild）”真实图像，其鲁棒性仍有待验证。
*   **应用限制**：目前主要针对单件服装，对于多层穿搭或复杂配饰的处理能力尚未充分展示。

（完）
