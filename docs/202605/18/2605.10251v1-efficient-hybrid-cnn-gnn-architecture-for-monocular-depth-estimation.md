---
title: Efficient Hybrid CNN-GNN Architecture for Monocular Depth Estimation
title_zh: 用于单目深度估计的高效混合 CNN-GNN 架构
authors: Ishan Narayan
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10251v1"
tags: ["query:ff3dr"]
score: 6.5
evidence: 使用CNN-GNN编码器-解码器进行单目深度估计
tldr: 本研究提出GraphDepth，一种将图神经网络（GNN）集成到卷积编码器-解码器框架中的单目深度估计架构。通过在ResNet-101 U-Net的多尺度阶段嵌入高效的GraphSAGE层，该方法克服了传统卷积感受野受限的问题，实现了长程空间关系的显式建模。相比Transformer架构，GraphDepth具有线性计算复杂度，在保持高精度的同时显著降低了显存占用并提升了推理速度，在多个室内外数据集上表现优异。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.10251v1/fig-001.webp\", \"caption\": \"Table 3: Performance comparison across datasets. Bold: best result. Underlined: second best.\", \"page\": 5, \"index\": 1, \"width\": 1054, \"height\": 354}, {\"url\": \"assets/figures/arxiv/2605.10251v1/fig-002.webp\", \"caption\": \"Figure 1: Overview of GraphDepth. GraphSAGE layers are embedded at multiple scales within the U-Net decoder, enabling hierarchical relational reasoning alongside local convolutional features.\", \"page\": 3, \"index\": 2, \"width\": 508, \"height\": 385}]"
motivation: 旨在解决传统卷积神经网络感受野受限以及Transformer架构在处理高分辨率图像时计算复杂度过高的问题。
method: 在U-Net骨干网络的多尺度阶段引入GraphSAGE层，并结合并行化图构建、通道注意力门控跳跃连接及异方差不确定性估计。
result: 在NYU Depth V2等数据集上达到与Transformer相当的精度，且推理速度提升近3倍，显存占用减少一半以上，并在WHU Aerial数据集上取得最优结果。
conclusion: 实验证明，通过显式关系推理结合CNN与GNN，可以在保证计算效率的同时实现强大的泛化能力和长程上下文建模。
---

## 摘要
我们提出了 GraphDepth，这是一种单目深度估计架构，它将图神经网络（GNN）协同集成到卷积编码器-解码器框架中。我们的方法在 ResNet-101 U-Net 骨干网络的多尺度上嵌入了高效的 GraphSAGE 层，从而能够对超出局部卷积感受野的长程空间关系进行显式建模。主要技术贡献包括：（1）具有可配置 k-NN 和基于网格邻接关系的批并行化图构建，以实现可扩展训练；（2）在瓶颈层和解码器阶段（1/32、1/16、1/8 分辨率）集成多尺度 GraphSAGE，以在整个特征层级中传播全局上下文；（3）通道注意力门控跳跃连接，在融合前自适应地对编码器特征进行加权；（4）通过专门的偶然不确定性头进行异方差不确定性估计，从而在优化过程中实现置信度感知的损失加权。与受限于序列长度二次复杂度的混合 Transformer 架构不同，GraphDepth 随空间分辨率线性扩展，同时通过迭代消息传递实现相当的全局感受野。在 NYU Depth V2、WHU Aerial、ETH3D 和 Mid-Air 基准测试上的实验表明，在室内场景中，其精度与最先进的 Transformer 相比差距在 4.6% 以内，且计算成本显著降低（25 FPS 对比 9 FPS，3.8 GB 对比 8.8 GB 显存）。GraphDepth 在 WHU Aerial 上取得了目前报道的最佳结果（RMSE 8.24 m），并在 Mid-Air 合成航空数据集上表现出优异的零样本跨域迁移能力，验证了显式关系推理在深度估计中的泛化能力。

## Abstract
We present GraphDepth, a monocular depth estimation architecture that synergistically integrates Graph Neural Networks (GNNs) within a convolutional encoder-decoder framework. Our approach embeds efficient GraphSAGE layers at multiple scales of a ResNet-101 U-Net backbone, enabling explicit modeling of long-range spatial relationships that lie beyond the receptive field of local convolutions. Key technical contributions include: (1) batch-parallelized graph construction with configurable k-NN and   grid-based adjacency for scalable training; (2) multi-scale GraphSAGE integration at bottleneck and decoder stages   (1/32, 1/16, 1/8 resolution) to propagate global context   throughout the feature hierarchy; (3) channel-attention gated skip connections that adaptively weight encoder   features before fusion; and (4) heteroscedastic uncertainty estimation via a dedicated aleatoric   uncertainty head, enabling confidence-aware loss weighting during   optimization. Unlike transformer-based hybrids, which suffer from quadratic complexity in sequence length, GraphDepth scales linearly with spatial resolution while achieving comparable global receptive fields through iterative message passing. Experiments on NYU Depth V2, WHU Aerial, ETH3D, and Mid-Air benchmarks demonstrate competitive accuracy within 4.6\% of state-of-the-art transformers on indoor scenes with substantially lower computational cost (25 FPS vs 9 FPS, 3.8 GB vs 8.8 GB VRAM). GraphDepth achieves the best reported result on WHU Aerial (RMSE 8.24 m) and exhibits superior zero-shot cross-domain transfer to the Mid-Air synthetic aerial dataset, validating the generalization power of explicit relational reasoning for depth estimation.