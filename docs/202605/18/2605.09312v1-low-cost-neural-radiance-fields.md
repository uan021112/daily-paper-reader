---
title: Low-Cost Neural Radiance Fields
title_zh: 低成本神经辐射场
authors: "Alice Huang, Prathamesh Sonawane, Yashdeep Thorat, Yug Rao"
date: 2026-05-10
pdf: "https://arxiv.org/pdf/2605.09312v1"
tags: ["query:3dgen"]
score: 8.0
evidence: 用于新视图合成和神经渲染的加速NeRF变体
tldr: 本研究针对神经辐射场（NeRF）训练时间长且依赖密集视图的问题，探讨了低计算和低数据场景下的加速方案。通过对比DS-NeRF、TensoRF和HashNeRF，作者提出了为TensoRF添加深度监督、消融特征解码MLP以及为HashNeRF设计残差和卷积架构等改进。虽然在等时评估中未显著超越基准，但为受限环境下的NeRF优化提供了重要参考。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09312v1/fig-001.webp\", \"caption\": \"Figure 1. Output image (left) and depth map (right) for different configurations. Configurations from top to bottom: Full data TensoRF, Low data TensoRF, Low data TensoRF-DS Note: Though the bottom two images look similar, they are indeed different depth maps. We discuss a reason for this in Sec. 5\", \"page\": 4, \"index\": 1, \"width\": 417, \"height\": 471}, {\"url\": \"assets/figures/arxiv/2605.09312v1/fig-002.webp\", \"caption\": \"Table 2. Table of various feature decoding architectures tried and their corresponding train and test PSNR values. Note that these PSNR values were generated in 2500 iterations and by downsampling train and test by a factor of 4.\", \"page\": 4, \"index\": 2, \"width\": 468, \"height\": 739}]"
motivation: 旨在解决NeRF在训练效率和对输入视图数量依赖性方面的局限，使其更易于在低算力环境下应用。
method: 对比研究了三种加速变体，并提出了深度监督TensoRF、输入下采样消融以及HashNeRF的多种网络架构改进。
result: 实验表明在相同训练时间下，所提扩展方案未能显著优于现有基准，但揭示了不同改进在受限设置下的表现。
conclusion: 研究明确了哪些架构扩展能迁移至低成本设置，并为未来在资源受限环境下的NeRF设计提出了关键问题。
---

## 摘要
神经辐射场 (NeRF) 实现了高质量的新视角合成，但其漫长的训练时间和对密集输入视角的依赖限制了其普及性。我们对三种加速 NeRF 变体（DS-NeRF、TensoRF 和 HashNeRF）进行了对比研究，并探索了针对低计算、低数据场景的扩展。首先，我们将源自 COLMAP 关键点的深度监督损失添加到 TensoRF 中（TensoRF-DS），并在减少视角数量的情况下在 LLFF 数据集上对其进行了评估。其次，我们对 TensoRF 的特征解码 MLP 进行了消融实验，并研究了输入下采样对合成 Lego 场景的 PSNR 和运行时间的影响。第三，我们提出了 HashNeRF 颜色和密度网络的四种架构变体，包括残差和卷积设计，并报告了在相同迭代预算下的 PSNR 与训练时间的权衡。在等时评估下，我们的扩展均未能在结果上明确超越已发表的基准模型，但实验刻画了哪些扩展可以迁移到受限设置中，并为未来的工作提出了设计问题。

## Abstract
Neural Radiance Fields (NeRF) achieve high-quality novel-view synthesis, but their long training times and reliance on dense input views limit accessibility. We present a comparative study of three accelerated NeRF variants - DS-NeRF, TensoRF, and HashNeRF and explore extensions targeted at the low-compute, low-data regime. First, we add a depth-supervision loss derived from COLMAP keypoints to TensoRF (TensoRF-DS) and evaluate it on the LLFF dataset under reduced view counts. Second, we ablate the feature-decoding MLP of TensoRF and study the effect of input downsampling on PSNR and runtime on the synthetic Lego scene. Third, we propose four architectural variants of the HashNeRF color and density networks, including residual and convolutional designs, and report PSNR/training-time tradeoffs under matched iteration budgets. Under iso-time evaluation, none of our extensions conclusively outperform the published baselines, but the experiments characterize which extensions transfer to constrained settings and surface design questions for future work.