---
title: "FFAvatar: Few-Shot, Feed-Forward, and Generalizable Avatar Reconstruction"
title_zh: FFAvatar：少样本、前馈且可泛化的化身重建
authors: "Thuan Hoang Nguyen, Jiahao Luo, Yinyu Nie, Hao Li, Gordon Guocheng Qian, Jian Wang"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.15320v1"
tags: ["query:ff3dr"]
score: 10.0
evidence: 从像素重建3D高斯头部化身的泛化前馈框架
tldr: FFAvatar是一个高效的3D头像重建框架，旨在解决传统方法耗时长、预处理复杂的问题。它利用Multi-View Query-Former从少量无姿态图像中快速生成可驱动的3D高斯头像，并通过三阶段训练策略（大规模预训练、多视角微调及可选个性化）实现了极高的泛化能力和几何保真度。该方法无需离线参数提取，支持实时渲染，在NeRSemble基准测试中显著超越现有技术。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.15320v1/fig-001.webp\", \"caption\": \"Figure 4: FFAvatar qualitative comparison for self-reenactment on the Ava256 test set (top two rows) and cross-reenactment on the NeRSemble benchmark (bottom two rows). FFAvatar-1 view achieves more faithful and geometrically consistent results than the baselines. GAGAvatar [2] often produces over-smoothed textures and pose misalignment, while LAM [9] shows geometry artifacts under challenging views. Additional input views and optional personalization further improve identity preservation and detail.\", \"page\": 6, \"index\": 1, \"width\": 826, \"height\": 532}, {\"url\": \"assets/figures/arxiv/2605.15320v1/fig-002.webp\", \"caption\": \"Figure 2: Three-stage training of FFAvatar. Scalable pretraining fosters generalization across unseen identities by training on our private large-scale multi-frame-per-identity dataset MFHQ-1M, multi-view fine-tuning enhances geometric fidelity by optimizing the pretrained weights on a smallscale set of 360◦ multi-view captures (e.g. Ava256 [18]), and lightweight personalization efficiently improves identity preservation with a few hundred tuning steps for a target identity in < 7 seconds on a single A100 GPU.\", \"page\": 2, \"index\": 2, \"width\": 822, \"height\": 197}, {\"url\": \"assets/figures/arxiv/2605.15320v1/fig-003.webp\", \"caption\": \"Figure 3: FFAvatar pipeline. FFAvatar reconstructs a canonical Gaussian head avatar from few-shot views using a Multi-view Query-Former, with canonical FLAME vertices as queries and source features as keys/values. An end-to-end FLAME Estimator predicts expression ψ, local articulation θ, and head pose π from driving frames, avoiding offline FLAME preprocessing. A few-to-many objective further improves generalization to unseen expressions and poses.\", \"page\": 4, \"index\": 3, \"width\": 781, \"height\": 316}, {\"url\": \"assets/figures/arxiv/2605.15320v1/fig-004.webp\", \"caption\": \"Figure 6: Personalization dynamics. Feed-forward initialization improves quality and converges within 500 steps, while random initialization (from scratch), remains blurry and poorly preserves identity.\", \"page\": 9, \"index\": 4, \"width\": 479, \"height\": 301}]"
motivation: 针对传统头像重建方法计算成本高、预处理复杂且难以扩展到新身份的问题，提出一种快速且泛化性强的解决方案。
method: 采用Multi-View Query-Former融合多视角信息生成3D高斯表示，并结合端到端FLAME参数预测和三阶段训练课程。
result: 在NeRSemble基准上比SOTA方法LAM提升了5.5 PSNR，且重建仅需2秒，支持49 FPS的实时动画。
conclusion: FFAvatar在身份保持、几何一致性和动画保真度方面树立了新标准，是实现高质量、实时可泛化头像重建的有效途径。
---

## 摘要
传统的化身重建通常依赖于针对每个受试者的优化，这需要数小时的计算，或者依赖于限制可扩展性的昂贵预处理。我们推出了 FFAvatar，这是一个可泛化的前馈框架，能够在几秒钟内从少样本、无姿态的肖像图像中重建高质量、可驱动的 3D 高斯头部化身。FFAvatar 通过多视图 Query-Former 将来自多个源图像的信息融合到统一的标准高斯表示中，并通过直接从像素端到端预测的 FLAME 参数进行驱动，消除了离线提取 FLAME 的开销。我们进一步提出了一种三阶段训练课程，以实现广泛的泛化和高保真重建：(i) 在包含超过 100 万个身份的海量单目视频数据上进行可扩展预训练，以学习强大的泛化先验；(ii) 在一个规模较小但高质量的 360 度捕捉数据集上进行多视图微调，以增强几何保真度和极端视角感知；以及 (iii) 可选的个性化步骤，在 500 个优化步内适应特定身份以实现最高保真度。大量实验表明，FFAvatar 在身份保持、几何一致性和动画保真度方面树立了新标准。在 NeRSemble 基准测试中，它的表现优于最先进的 LAM，PSNR 显著提升了 5.5。此外，FFAvatar 支持实时部署，在不进行个性化的情况下仅需 2 秒即可重建化身，进行个性化则需 10 秒，同时在单块 NVIDIA A100 GPU 上支持 49 FPS 的动画。

## Abstract
Avatar reconstruction has traditionally relied on per-subject optimization that requires hours of computation or on expensive preprocessing that limits scalability. We introduce FFAvatar, a generalizable feed-forward framework that reconstructs high-quality, animatable 3D Gaussian head avatars from few-shot unposed portrait images in seconds. FFAvatar fuses information from multiple source images into a unified canonical Gaussian representation through Multi-View Query-Former, which is animated via FLAME parameters predicted end-to-end directly from pixels, eliminating the overhead of offline FLAME extraction. We further propose a three-stage training curriculum that achieves both broad generalization and high-fidelity reconstruction: (i) scalable pretraining on extensive monocular video data with over 1M identities to learn strong generalizable priors; (ii) multi-view fine-tuning on a small but high-quality dataset of 360-degree captures to enhance geometric fidelity and extreme-view awareness; and (iii) optional personalization that adapts to specific identities for maximum fidelity within 500 optimization steps. Extensive experiments demonstrate that FFAvatar sets a new standard for identity preservation, geometric consistency, and animation fidelity. On the NeRSemble benchmark, it outperforms the state-of-the-art LAM by a substantial 5.5 PSNR gain. Furthermore, FFAvatar enables real-time deployment, reconstructing avatars in 2 seconds without personalization and 10 seconds with personalization, while supporting 49 FPS animation on a single NVIDIA A100 GPU.

---

## 论文详细总结（自动生成）

这是一份关于论文《FFAvatar: Few-Shot, Feed-Forward, and Generalizable Avatar Reconstruction》的结构化深入总结：

### 1. 核心问题与整体含义
该论文致力于解决**高质量3D数字人头像重建**中的效率与泛化性矛盾。传统方法（如基于NeRF或3DGS的优化方法）通常需要针对每个身份进行数小时的训练，且依赖昂贵的预处理（如相机标定、FLAME参数提取）。
**FFAvatar的核心目标**是实现：
*   **少样本（Few-Shot）**：仅需少量无姿态肖像图。
*   **前馈（Feed-Forward）**：无需长时间迭代优化，秒级生成。
*   **可泛化（Generalizable）**：能够处理训练集中未见过的身份，并保持几何一致性。

### 2. 方法论：核心思想与关键技术
FFAvatar 提出了一个端到端的框架，主要包含以下三个关键组件：
*   **Multi-View Query-Former (Q-Former)**：
    *   利用FLAME模型的标准顶点作为3D查询（Queries），通过交叉注意力机制从多个输入视角的图像特征中提取信息。
    *   该模块能有效融合多视角下的几何与纹理线索，生成一个统一的标准空间（Canonical Space）3D高斯表示。
*   **端到端 FLAME 估计器**：
    *   直接从原始像素预测表情（$\psi$）、局部关节（$\theta$）和全局姿态（$\pi$）。
    *   **意义**：消除了对外部离线追踪工具（如VHAP）的依赖，解决了大规模训练时的预处理瓶颈。
*   **三阶段训练课程（Training Curriculum）**：
    1.  **大规模预训练**：在包含100万+身份的单目视频集（MFHQ-1M）上训练，建立强大的身份泛化先验。
    2.  **多视图微调**：在高质量360度捕捉数据集（Ava256）上微调，提升几何保真度和极端视角的稳定性。
    3.  **可选个性化**：针对特定目标，通过优化高斯残差进行极速微调（500步内）。
*   **Few-to-Many 训练目标**：训练时输入少量视角，要求模型重建并渲染出更多目标视角，强迫模型学习补全缺失的几何信息。

### 3. 实验设计
*   **数据集**：
    *   **训练**：私有数据集 MFHQ-1M（100万身份）和 Ava256（248个身份）。
    *   **测试/基准**：在 **NeRSemble** 数据集（45个未见身份）上进行泛化性评估。
*   **对比方法**：
    *   **GAGAvatar**：基于高斯的泛化头像模型。
    *   **LAM (Large Avatar Model)**：目前最先进的单图前馈头像模型。
*   **评估指标**：PSNR、SSIM、LPIPS（感知质量）和 CSIM（身份保持度/人脸相似度）。

### 4. 资源与算力
*   **硬件**：使用了 NVIDIA **A100** GPU。
*   **训练时长**：
    *   第一阶段（预训练）：8块 A100，耗时 **3天**。
    *   第二阶段（微调）：8块 A100，耗时 **1.5天**。
    *   第三阶段（个性化）：单块 A100，仅需 **7秒**（500步）。
*   **推理性能**：重建仅需 2 秒，动画驱动速度达到 **49 FPS**。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了跨数据集的泛化测试（在Ava256训练，在NeRSemble测试），涵盖了自驱动（Self-reenactment）和跨身份驱动（Cross-reenactment）。
*   **消融实验**：非常充分。分别验证了 GAN 损失、大规模预训练、多视图微调以及 Few-to-Many 损失对最终效果的贡献。
*   **客观性**：通过定量指标（PSNR 提升 5.5dB）和定性视觉对比，客观展示了其在处理极端视角和身份保持方面的优势。

### 6. 主要结论与发现
*   **规模效应**：在百万级身份的数据上预训练是实现强泛化能力的关键。
*   **多视图融合**：相比单图模型（如LAM），多视图输入能显著减少几何畸变和纹理模糊。
*   **端到端优势**：自研的 FLAME 估计器在精度上媲美离线工具，但速度快了 200 倍以上，且使模型更具鲁棒性。
*   **极速个性化**：基于前馈模型的初始化，比从零开始优化（From Scratch）收敛快 60 倍，且质量更高。

### 7. 优点（亮点）
*   **效率极高**：实现了从“小时级”到“秒级”重建的跨越，且支持实时动画。
*   **无需预处理**：真正实现了从原始像素到3D头像的端到端转换，降低了使用门槛。
*   **几何一致性强**：通过 Q-Former 融合多视角信息，解决了单图重建中常见的“背面/侧面崩坏”问题。

### 8. 不足与局限
*   **模型空间限制**：受限于 FLAME 参数空间，模型难以精细刻画舌头、口腔内部细节及复杂的眼球转动。
*   **遮挡处理**：在极少数视角下，如果输入图像完全缺失某些区域（如长发背面），模型仍需依赖“幻觉”补全，可能产生伪影。
*   **数据依赖**：高性能高度依赖于其私有的百万级身份数据集（MFHQ-1M），开源社区可能难以完全复现其预训练强度。

（完）
