---
title: "FeatureFox: Sample-Efficient Panoptic Graph Segmentation for Machining Feature Recognition in B-Rep 3D-CAD Models"
title_zh: FeatureFox：用于 B-Rep 3D-CAD 模型加工特征识别的样本高效全景图分割
authors: "Bertram Fuchs, Altay Kacan, Aaron Haag, Oliver Lohse"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26770v1"
tags: ["query:partseg"]
score: 9.0
evidence: 用于3D-CAD模型加工特征识别的全景图分割
tldr: 本研究提出FeatureFox，一种针对B-Rep 3D-CAD模型的全景自动特征识别（AFR）流水线。针对现有深度学习方法数据需求量大且复杂度高的问题，该方法通过校准的二元边分类器定位特征边界，利用修剪后的面邻接图恢复实例，并对子图属性进行分类。FeatureFox在样本和计算效率上显著优于基准模型AAGNet，仅需少量训练数据即可达到高精度，并展现出良好的工业泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26770v1/fig-001.webp\", \"caption\": \"Fig. 4. Sample efficiency on MFInstSeg (excluding stock): PQ versus trainingset size for AAGNet and FeatureFox. FeatureFox dominates in the low-data regime and reaches > 90% PQ with ∼ 250 training parts, whereas AAGNet requires ∼ 5,000; AAGNet is only marginally better on the full training set.\", \"page\": 5, \"index\": 1, \"width\": 524, \"height\": 355}, {\"url\": \"assets/figures/arxiv/2604.26770v1/fig-002.webp\", \"caption\": \"Fig. 3. FeatureFox pipeline. Starting from a B-Rep 3D-CAD model, we construct a face-adjacency graph and attributes, propagate vertex information to edges (1), and localize the feature with a calibrated edge boundary classifier for instance grouping (2). We then compute connected-component attributes (3) and recognize the class of each predicted instance with a semantic model (4). Pink: Attribute calculation. Teal: Model training. Gray: Intermediate representation.\", \"page\": 5, \"index\": 2, \"width\": 816, \"height\": 277}]"
motivation: 现有的基于学习的自动特征识别方法通常过于复杂、依赖大量数据，且未能有效整合实例分割与语义标注任务。
method: 该方法利用增强的边属性进行二元分类以确定特征边界，通过面邻接图的连通分量提取实例，并结合子图属性进行语义分类。
result: FeatureFox仅需约250个训练样本即可达到0.9以上的全景质量得分，且在全量数据集上的识别精度与深度学习基准模型相当。
conclusion: FeatureFox是一种高效且实用的AFR方案，凭借极低的数据需求和快速的训练速度，在实际工业CAD模型处理中具有显著优势。
---

## 摘要
B-Rep 3D-CAD 模型上的自动特征识别 (AFR) 是 CAD/CAM 自动化的核心，但大多数基于学习的方法往往复杂且耗费数据，并且将实例分组与语义标注分开评估。我们提出了 FeatureFox，这是一种输出带有语义标签的加工实例的全景 AFR 流水线：通过在丰富的边缘属性上运行校准的二元边缘分类器来定位特征边界，在剪枝后的面邻接图中将实例恢复为连通分量，并利用每实例分类器从聚合的子图属性中预测加工类别。我们在 MFInstSeg 数据集上使用全景质量 (PQ) 指标进行评估，该指标综合衡量了实例分离和语义正确性。FeatureFox 在样本和计算效率上显著优于深度学习基准 AAGNet，仅需约 250 个训练零件即可达到 PQ > 0.9，而 AAGNet 则需要约 5,000 个，且在完整 MFInstSeg 集上的训练在 GPU 上仅需数秒。在完整训练集上，AAGNet 在 PQ 指标上略微超过 FeatureFox，而 FeatureFox 在特征级识别和定位精度上仍保持微弱领先。最后，利用其低数据需求特性，我们在 270 个手动标注的工业 CAD 零件上训练了 FeatureFox，并展示了其对未见过的真实工业零件的定性泛化能力，表明了其实际应用价值。

## Abstract
Automatic feature recognition (AFR) on B-Rep 3D-CAD models is central to CAD/CAM automation, yet most learning-based methods are complex, data-hungry, and evaluate instance grouping and semantic labeling separately. We present FeatureFox, a panoptic AFR pipeline that outputs machining instances with semantic labels: a calibrated binary edge classifier on enriched edge attributes localizes feature boundaries, instances are recovered as connected components in a pruned face-adjacency graph, and a per-instance classifier predicts the machining class from aggregated subgraph attributes. We evaluate on MFInstSeg using Panoptic Quality (PQ), which jointly scores instance separation and semantic correctness. FeatureFox is substantially more sample- and compute-efficient than the deep baseline AAGNet, reaching $\mathrm{PQ}>0.9$ with $\sim250$ training parts versus $\sim5{,}000$ for AAGNet, and training on the full MFInstSeg set takes seconds on a GPU. On the full training set, AAGNet surpasses FeatureFox marginally in PQ, while FeatureFox remains slightly ahead in feature-level recognition and localization accuracy. Finally, leveraging its low data requirement, we train FeatureFox on $270$ manually labeled industrial CAD parts and show qualitative generalization to an unseen real industrial part, indicating practical real-world applicability.

---

## 论文详细总结（自动生成）

### 论文总结：FeatureFox - 用于 B-Rep 3D-CAD 模型加工特征识别的样本高效全景图分割

#### 1. 研究动机与背景
自动特征识别（AFR）是连接 CAD 设计与 CAM 制造自动化的关键环节。然而，现有的 AFR 方法面临三大挑战：
*   **数据饥渴：** 深度学习模型（如 GNN）通常需要数千个标注零件才能达到高精度，但工业级标注数据极其稀缺且昂贵。
*   **任务割裂：** 现有方法往往将“实例分割”（区分不同特征）和“语义标注”（识别特征类型）作为独立任务处理，缺乏统一的评估标准。
*   **鲁棒性不足：** 传统规则方法难以处理特征相交和复杂的拓扑变化。
**核心目标：** 开发一种轻量级、样本高效且能同时输出实例和语义标签（全景分割）的 AFR 流水线。

#### 2. 方法论
FeatureFox 采用了一种分层的、基于树模型（XGBoost）的流水线，将 AFR 建模为**全景图分割**任务：
*   **图表示：** 将 B-Rep 模型转化为几何属性邻接图（gAAG），以面为节点，面间的共享边为图的边。
*   **特征增强：** 提取丰富的边缘属性（如二面角、凹凸性、面积比等），并从相邻面传播信息到边上。
*   **实例定位（边分类）：** 训练一个经过**等向校准（Isotonic Calibration）**的二元分类器，预测每条边是否属于同一个加工特征。通过概率阈值剪枝掉“边界边”，利用连通分量（Connected Components）算法提取特征实例。
*   **语义识别（实例分类）：** 对提取出的每个子图（实例）进行全局属性聚合（如面/边数量、特征尺度、曲率分布等），训练另一个分类器预测该实例的加工类别（如孔、槽、口袋等）。

#### 3. 实验设计
*   **数据集：** 主要使用公开的 **MFInstSeg** 数据集（包含约 6.2 万个 STEP 文件）。
*   **基准模型（Benchmark）：** 对比了当前最先进的深度学习模型 **AAGNet** 和 **MFTReNet**。
*   **评估指标：** 引入了计算机视觉中的**全景质量（Panoptic Quality, PQ）**，统一衡量定位精度和分类正确性。
*   **工业验证：** 在 270 个手动标注的真实工业零件上进行训练，并在 NIST（美国国家标准与技术研究院）的公开工业 CAD 模型上进行定性测试。

#### 4. 资源与算力
*   **硬件：** 使用单张 **NVIDIA RTX 6000 Ada** GPU。
*   **训练时长：** 
    *   **FeatureFox：** 在完整数据集上训练仅需 **8 秒**（GPU 利用率 100%）。
    *   **AAGNet：** 训练 100 个 epoch 需要 **38 分钟**（GPU 利用率约 18%）。
*   **效率优势：** FeatureFox 的训练速度比深度学习基准快了约 285 倍。

#### 5. 实验数量与充分性
论文进行了多维度的对比实验：
*   **样本效率实验：** 测试了从 50 个到 4.3 万个训练样本下模型的表现，绘制了学习曲线。
*   **性能对比：** 在面级（Face-level）和特征级（Feature-level）分别进行了准确率和 PQ 指标的对比。
*   **消融/泛化实验：** 验证了在极小规模工业数据集上的可行性。
**评价：** 实验设计客观、公平，特别是在样本效率的对比上非常直观，充分证明了该方法在小样本下的压倒性优势。

#### 6. 主要结论与发现
*   **极高的样本效率：** FeatureFox 仅需 **250 个** 训练样本即可达到 PQ > 0.9，而 AAGNet 需要约 **5,000 个** 样本才能达到同等水平。
*   **性能持平：** 在全量数据集上，FeatureFox 的 PQ 指标（96.87%）仅略低于 AAGNet（97.44%），但在特征完全识别率上甚至略有领先。
*   **工业可行性：** 证明了在仅有几百个标注零件的情况下，部署定制化的工业 AFR 系统是可行的。

#### 7. 优点与亮点
*   **全景视角：** 首次在 AFR 领域倡导使用 PQ 指标，将实例和语义任务合二为一。
*   **轻量高效：** 放弃了复杂的深度 GNN，转而使用精心设计的几何特征和高效的梯度提升树（XGBoost），极大地降低了算力和数据门槛。
*   **可解释性：** 相比于黑盒神经网络，基于手工特征和树模型的流水线在工业应用中更易于调试和理解。

#### 8. 不足与局限
*   **非端到端风险：** 采用分步流水线，如果第一步的边界预测出错，错误会直接传播到后续的语义分类。
*   **复杂拓扑限制：** 目前无法处理由非连通面组成的单个加工特征（虽然这种情况在 B-Rep 中较少见，但在某些极端相交情况下存在）。
*   **特征工程依赖：** 性能高度依赖于预先设计的几何属性，对于某些极其特殊的自由曲面特征，可能需要额外的特征工程。

（完）
