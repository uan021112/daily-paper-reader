Title: FUS3DMaps: Scalable and Accurate Open-Vocabulary Semantic Mapping by 3D Fusion of Voxel- and Instance-Level Layers

URL Source: https://arxiv.org/pdf/2605.03669v1

Published Time: Wed, 06 May 2026 01:09:57 GMT

Number of Pages: 9

Markdown Content:
# FUS3DMaps: Scalable and Accurate Open-Vocabulary Semantic Mapping by 3D Fusion of Voxel- and Instance-Level Layers 

Timon Homberger, Finn Lukas Busch, Jes´ us Gerardo Ortega Peimbert, Quantao Yang, Olov Andersson 

Abstract — Open-vocabulary semantic mapping enables robots to spatially ground previously unseen concepts without requiring predefined class sets. Current training-free methods commonly rely on multi-view fusion of semantic embeddings into a 3D map, either at the instance-level via segmenting views and encoding image crops of segments, or by projecting image patch embed-dings directly into a dense semantic map. The latter approach sidesteps segmentation and 2D-to-3D instance association by operating on full uncropped image frames, but existing methods remain limited in scalability. We present FUS3DMaps, an online dual-layer semantic mapping method that jointly maintains both dense and instance-level open-vocabulary layers within a shared voxel map. This design enables further voxel-level semantic fusion of the layer embeddings, combining the complementary strengths of both semantic mapping approaches. We find that our proposed semantic cross-layer fusion approach improves the quality of both the instance-level and dense layers, while also enabling a scalable and highly accurate instance-level map where the dense layer and cross-layer fusion are restricted to a spatial sliding window. Experiments on established 3D semantic segmentation benchmarks as well as a selection of large-scale scenes show that FUS3DMaps achieves accurate open-vocabulary semantic mapping at multi-story building scales. Additional material and code will be made available: 

https://githanonymous.github.io/FUS3DMaps/ .

I. I NTRODUCTION 

Semantic mapping is instrumental in enabling scene understanding and autonomous decision-making for mobile systems. Traditional approaches often rely on a predefined, environment-specific set of classes [1]. However, advances in vision foundation models [2] [3] enable spatial grounding of open-set semantic concepts. Training-free open-vocabulary semantic mapping methods can be grouped into methods that generate dense semantic 3D maps, storing an embedding vector for each point or voxel, and those that generate instance-level semantic maps, grouping points or voxels into instances and storing one embedding vector per instance. Typically, dense maps have a higher capacity to store fine-grained detail, whereas instance-level maps are substantially more memory efficient and scalable, as they store fewer high-dimensional embedding vectors per mapped volume [4] [5]. Instance-level semantic mapping approaches commonly use segmentation and encode image crops separately for each segment [6] [5]. In contrast, recent dense semantic mapping approaches use patch-level semantic embeddings of a language-aligned vision model and aggregate them     

> This work was partially supported by the Wallenberg AI, Autonomous Systems and Software Program (WASP) funded by the Knut and Alice Wallenberg Foundation. The authors are with the Division of Robotics, Perception, and Learn-ing, KTH Royal Institute of Technology, Sweden. Contact: {timonh, flbusch, olovand }@kth.se
> Fig. 1. FUS3DMaps maintains an object focused semantic map layer with instance-level embeddings. Simultaneously, it maintains a dense semantic layer, that can be restricted to a sliding window, centered at the camera pose. Semantic fusion between the dense and instance-level layers can produce mutual benefits. The sliding window approach helps to bound computational expense and reduce the memory footprint, which enables accurate semantic mapping at large scales. The visualized maps are created using scene 00824
> of the HM3DSem dataset [8].

in a dense semantic voxel map [7]. These methods are not affected by segmentation errors, viewpoint-dependent variations in segmentation granularity of object parts, or subsequent association errors between new segments and existing 3D instances. However, since they do not explicitly model object instances, depending on viewing distance and angle, the image patches may not align well with object boundaries. We propose combining the complementary strengths of instance-level and dense semantic mapping. To this end, we introduce FUS3DMaps, a 3D incremental open-vocabulary voxel mapping method that maintains a two-layered semantic map comprising of a dense voxel-level layer, a multi-hypothesis instance-level layer, as well as a fusion mechanism for combining embeddings of the two layers. The proposed cross-layer semantic fusion mechanism addresses viewpoint-dependent segmentation inconsistencies by accounting for the per-voxel observation counts of each instance hypothesis. We further propose a sliding window mechanism in which the dense layer and cross-layer fusion are restricted to a spatial window, centered at the camera pose, while the instance-level map layer is maintained globally. This design enables enriching the instance-level embeddings with embeddings from the dense semantic layer while retaining the favorable scalability properties of instance-level maps. We show that our 

> arXiv:2605.03669v1 [cs.RO] 5 May 2026

fusion approach can produce an instance-level semantic map layer with substantially improved 3D semantic segmentation accuracy and find complementary improvements for the fused dense semantic layer. To the best of our knowledge, this is the first open-vocabulary semantic mapping method to perform 3D semantic fusion of dense map embeddings with instance-level map embeddings. In summary, our contributions are:  

> •

An online 3D open-vocabulary semantic voxel map-ping method that simultaneously generates dense and instance-level semantic embedding layers to leverage the complementary benefits of both mapping strategies.  

> •

An uncertainty-aware 3D semantic cross-layer fusion mechanism that combines embeddings of multiple, potentially overlapping instance hypotheses with those of the dense semantic map layer.  

> •

A sliding window mapping approach that enables ac-curate instance-level semantic mapping in large scale environments by performing 3D semantic fusion between the embeddings of a dense semantic map layer, restricted to an egocentric sliding window, and a global instance-level semantic layer. We extensively evaluate FUS3DMaps on established 3D semantic segmentation benchmarks, comparing the outputs of our cross-layer fusion method with prior work, as well as with the unfused versions of our method’s dense and instance-level layers. We present results using two different pairs of language-aligned vision encoders, indicating that the method is not limited to a specific encoder. Furthermore, we evaluate the sliding window mapping approach in large scale environments, reporting semantic segmentation accuracy and the memory footprint. These experiments show that FUS3DMaps achieves highly accurate and scalable open-set instance-level semantic mapping. We will make the source code available 1.II. R ELATED WORK 

Open-set semantic mapping methods commonly rely on internet-scale contrastively pre-trained vision models. To preserve the foundational generalization of these models, open-set approaches often avoid fine-tuning with labeled semantic segmentation datasets and instead use training-free strategies to spatially ground semantic embeddings [1] [6]. 

Instance-Level Semantic Maps. Open-Fusion [4] intro-duces a real-time semantic TSDF voxel mapping method that uses segment-level embeddings produced by a gener-alist segmentation model and performs efficient Hungarian matching for segment-to-instance associations in image space. The method assigns per-instance embedding vectors upon first observation and does not incorporate semantic updates from subsequent views, which limits the multi-view semantic consistency of the map. ConceptGraphs [6] and HOV-SG [5] generate instance-level scene-graphs. Both methods use segmentation and subsequent encoding of the corresponding image crops to generate an embedding for each instance. HOV-SG additionally encodes 

> 1https://githanonymous.github.io/FUS3DMaps/

the segment with the rest of the crop masked out, and the full RGB frame and averages them together with the aim of making instance semantics aware of the context the segment appears in. However, as the full frame embeddings reflect every instance in the image, averaging them together also bleeds semantics across instances. Both of these methods are designed for offline scene-graph generation. [9] presents an online semantic mapping approach with a similar per-instance embedding combination strategy to [5]. However, instead of using fixed weights as in [5], they train a neural network that learns to predict weights to blend embeddings of image crops, segments with masked background, and full RGB frames. Addressing segmentation variations from different view-points by maintaining multiple instance hypotheses has previously been suggested in OpenVox [10]. However, prior to segmentation, [10] relies on an open-vocabulary object detector to propose image regions, which acts as a filter on which instance segments are processed by the pipeline. 

Dense Semantic Maps. ConceptFusion [1] uses segmen-tation and encoding of separate image crops that contain the segments. A local-to-global zero-shot embedding fusion scheme combines image crop embeddings with the embedding of the full RGB frame to generate pixel-level embeddings. These are subsequently lifted to a dense point-cloud repre-sentation of the environment. In [11] the authors propose to aggregate embeddings, obtained from a 2D open-vocabulary semantic segmentation model, in 3D point clouds through 3D projection and average pooling. Subsequently, they use them to supervise the training of a model that predicts semantic embeddings directly from 3D points. However, both [1] and [11] are designed as offline methods and assume upfront availability a complete sequence of RGB-D frames. The dense semantic layer used in FUS3DMaps is similar to [7]. The authors introduce a novel patch-level semantic encoder (NARADIO) which they use to construct dense 3D semantic maps online. The encoder consists of a combination of an agglomerative vision model backbone [12], a head that projects the features into the semantic space of a contrastively pretrained language-aligned model [13], and an additional training-free mechanism that improves the locality of patch embeddings. [14] recently proposed an improved attention mechanism for the front-end vision model. Compared to combining image crop embeddings with full image embed-dings like in [5] and [9], patch-based methods can provide more localized embeddings while also retaining some image context. Furthermore, such methods are not affected by issues induced by segmentation variations. However, patches may not align with instance boundaries, and given that these methods produce dense semantic maps with one embedding per voxel, they scale worse with map volume than instance-based approaches. In FUS3DMaps we propose to combine patch embedding based methods with multi-hypothesis instance based methods in a principled way, providing both accurate and scalable semantic voxel mapping. Fig. 2. FUS3DMaps simultaneously maintains a dense semantic layer and an instance-level semantic layer in a shared voxel map. Both mapping pipelines use encoders that produce embeddings in a shared language-aligned open-vocabulary semantic space. A cross-layer semantic fusion module allows combining embeddings of the dense and instance-level map layers to produce a fused dense semantic voxel map layer and a fused instance-level semantic voxel layer. 

III. M ETHOD 

FUS3DMaps simultaneously maintains a dense semantic 3D voxel layer MD with aggregated semantic patch-level embeddings and a sparser instance-level voxel layer MI,constructed using a segmentation and image crop encoding strategy. As illustrated in Fig. 2, our proposed cross-layer voxel-level semantic fusion method combines embeddings of MD and MI to produce the fused semantic map layers 

MDfused and MIfused . The remainder of the section is organized as follows. The dense semantic mapping pipeline is described in Section III-A and the instance-level semantic mapping pipeline in Section III-B, with Section III-C presenting the proposed cross-layer semantic fusion method. Finally, Section III-D details the sliding window approach that maintains the dense layer locally and the instance-level layer globally to combine the scalability of instance-based maps with the semantic accuracy of dense semantic maps. 

A. Dense Semantic Voxel Mapping 

For each RGB frame Ct, we project a subsample 2 of its pixels to 3D using the depth frame Dt and the corresponding camera pose. Additionally, we encode Ct with an encoder Enc patch (·) to obtain a semantic embedding vector f patch ∈ Rd

for each image patch in Ct. An embedding vector is associated with all points that were sampled and projected from the corresponding patch. We aggregate the patch-level semantic embeddings in the dense voxel layer MD by forming per-voxel dense embedding vectors f D 

> i

as 

wD 

> i

← wD 

> i

+ N D 

> i

,

f D 

> i

← wD 

> i

· f D 

> i

+ P 

> j

f patch 

> j

wD

> i

, (1) where N D 

> i

is the number of points p D 

> j

that fall within voxel 

vD 

> i

and f patch  

> j

are the patch embeddings associated with the points pD 

> j

. The aggregated weight wD 

> i

is the total number of point hits of voxel vD 

> i

. In the following we will refer to the accumulated point hits as the observation count of a voxel. 

> 2The pixel sampling is detailed in Section IV-B.

B. Instance-Level Semantic Voxel Mapping 

Alongside MD, FUS3DMaps generates a semantically sparse instance-level semantic layer MI. To enhance the robustness to segmentation inaccuracy, ambiguity caused by variations in segmentation granularity, and partial occlusions, we employ a 3D multi-hypothesis voxel-mapping scheme. 

1) Instance-Level Map Structure: MI groups voxels into discrete instances Iid . Each voxel vI 

> i

stores up to K instance hypotheses in the form of observation counts wI 

> i, id

that represent evidence that the voxel vI 

> i

is part of instance Iid .Furthermore, every instance Iid has an associated instance-level embedding vector f Iid ∈ Rd.

2) Segmentation and Embedding Extraction: We utilize a class-agnostic segmentation model Seg (·) to generate a set of mask proposals Q = {Qm} from an input RGB frame Ct.We project pixels, sampled from Ct and contained in Qm,to 3D using the depth frame Dt. For each mask Qm, this produces an instance proposal Pm as a set of 3D points. We filter masks with contours that have significant contact with the image border or are below a size threshold, as these often lack sufficient visual context for reliable semantic encoding. Additionally, we extract an image crop from Ct that bounds 

Qm with a small padding and encode it using an image-level encoder Enc img (·). This produces an embedding vector 

f P 

> m

∈ Rd for each instance proposal Pm. The image-level encoder Enc img (·) is chosen such that f P 

> m

resides in the same language-aligned semantic space as the embedding vectors produced by Enc patch (·).

3) Geometric Instance Association: We associate new 3D instance proposals Pm with existing instances through geometric overlap. Let Vm denote the set of voxels occupied by Pm. We compute the Intersection-over-Union (IoU) between Vm and the voxel sets of existing instances. If the IoU with the most overlapping instance Iid ∗ exceeds a threshold 

τiou , we consider it a match. In case of a match, we update the observation counts of the voxels vi ∈ V m as wI 

> i, id ∗

← wI 

> i, id ∗

+ N I 

> i

, where 

N I 

> i

is the number of points pI 

> j

∈ Pm that fall within voxel vI

> i

. If there is no match for Pm, we initialize a new instance Iid ′ with observation counts wI 

> i, id ′

← N I 

> i

. If an instance is newly associated with a voxel that already maintains K instance hypotheses, the hypothesis with the lowest observation count is pruned. Maintaining per-voxel observation counts in analogous fashion to the dense mapping pipeline allows to model observational support across voxels, independent of the instance geometry, and thereby facilitates voxel-level cross-layer semantic fusion (Section III-C). 

4) Instance-Level Embedding Update: For proposals with a match, we define NP as the number of points in the instance proposal Pm and update the instance-level embeddings as 

wIid ∗ ← wIid ∗ + NP

f Iid ∗ ← wIid ∗ · f Iid ∗ + NP · f P

> m

wIid ∗

. (2) Consequently, embeddings that correspond to more com-plete views of an object receive a relatively higher weight compared to partially occluded views. New instances are initialized with embedding f Iid ′ = f P 

> m

and observation count 

wIid ′ = NP . The voxel grids of MI and MD are spatially aligned. Therefore, in the following, we will refer to voxels simply as vi.

C. 3D Cross-Layer Semantic Fusion 

To combine embeddings of the dense semantic map layer 

MD and the instance-level semantic map layer MI, we devise a voxel-level cross-layer semantic fusion approach. Observation counts in our dense and instance-level semantic mapping pipelines vary across voxels depending on the scene coverage and occlusions in the processed RGB-D sequence. The instance-level layer is additionally subject to viewpoint-dependent segmentation variations. For example, a close-up view might separately segment the keyboard and the screen of a laptop, whereas in a more distant view, the laptop may be enclosed by a single segment. This results in a non-uniform distribution of observations across voxels and instances. Furthermore, the fidelity of the extracted semantic embeddings of the respective mapping pipeline can vary based on the vision models used. We propose a minimal probabilistic fusion mechanism to account for these uncertainties. We model f D 

> i

and f I 

> i

as two independent unbiased Gaussian estimates of the true semantic embedding of a voxel vi. Each is formed by averaging N D

> i

and N I 

> i

observations with per-observation noise variances 

RD and RI, respectively. We assume uniform RD, RI across all observations in the respective pipeline. Thus, we obtain the variances of the estimates as 

σ2 

> D,i

= RD

N D

> i

, σ2 

> I,i

= RI

N I

> i

. (3) Under independence, the minimum mean squared error (MMSE) estimate is the precision-weighted average 

ffused ,i = ΠD 

> i

f D 

> i

+ Π I 

> i

f I

> i

ΠD 

> i

+ Π I

> i

, Πi = 1 /σ 2 

> i

. (4) Substituting ΠD 

> i

= N D 

> i

/R D, ΠI 

> i

= N I 

> i

/R I, and λ := 

RD/R I, and using N D 

> i

= wD 

> i

and N I 

> i

= wI 

> i

, the observation counts of the voxel in the respective map layer, we obtain 

ffused ,i = wD 

> i

· f D 

> i

+ λ w I 

> i

· f I

> i

wD 

> i

+ λ w I

> i

, (5) where λ encodes the relative variance of the two sources. While the individual variances are not known, in this formulation the only parameter left is their relative variance 

λ, which is easy to estimate empirically by hyperparameter sweeps for any pair of vision models Enc patch (·), Enc img (·).

Instance-Level Fused Semantic Layer. For each instance 

Iid in MI, we first perform voxel-level semantic fusion for all voxels vi ∈ I id ,

f *fused ,i = wD 

> i

· f D 

> i

+ λw I 

> i, id

· f Iid 

wD 

> i

+ λw I

> i, id

, (6) where wI 

> i, id

is the observation count at voxel vi correspond-ing to instance Iid . f Iid is the same for all voxels vi ∈ I id . To derive the instance-level fused embeddings f Ifused ,id of MIfused ,we aggregate these voxel-level estimates f *fused ,i , weighted by the total precision per voxel ρi = wD 

> i

+ λw I

> i, id

,

f Ifused ,id =

P 

> vi∈I id

ρi · f *fused ,i 

P 

> vi∈I id

ρi

(7) which weights voxel embeddings proportionally to the total observations they received from both sources. 

Dense Fused Semantic Layer. For each voxel vi, the dense fused embedding f Dfused ,i of MDfused is computed as 

f Dfused ,i = wD 

> i

· f D 

> i

+ λ ˆwI 

> i

· ˆf I

> i

wD 

> i

+ λ ˆwI

> i

(8) where ˆf I 

> i

is the embedding vector of the instance hypothesis with the highest observation count ˆwI 

> i

at voxel vi.The 3D cross-layer fusion is stateless and can therefore be performed at any time to produce MDfused and MIfused , for example, after receiving a text prompt or at a regular rate. 

D. Sliding Window Semantic Fusion 

Open-set semantic maps that store a high-dimensional embedding vector per voxel grow very memory intensive when scaled beyond room-sized environments. 3

Therefore, we propose a functionality that limits the spatial extent of the dense layer by maintaining it within a sliding window with radius rw, centered at the camera position. The dual-layer mapping approach of FUS3DMaps allows the voxels of MD with associated dense semantic embeddings f D

to simply be pruned once they leave the window, while the instances of MI along with the sparse raw and fused instance embeddings f I, f Ifused continue to be maintained globally. Consequently, the semantic vectors scale only linearly with the number of instances Θ( |I| ) as in a pure instance map, rather than cubically with the resolution of the map geometry 

Θ  Vd3 + |I| , where V is the total mapped voxel volume and d is the voxel cube size. Semantic fusion is performed 

> 3The vision models used in our experiments produce embedding vectors of dimensions 1152 (NARADIO) and 512 (CLIP-DINOiser).

at a regular rate within the sliding window as described in Section III-C. To ensure that only instances with sufficient aggregate semantic information are updated during fusion, we maintain an evidence score for each instance Iid that is derived as 

escore id = X

> vi∈I id

ρi, (9) where ρi is the precision at voxel vi as used in (7). Only if the new evidence score for Iid , exceeds the previously stored evidence score, the fused embedding vector f Ifused ,id 

is updated along with escore id .Taken together, we refer to this mechanism as the sliding window mode . It enables combining aggregated patch embed-dings of the dense layer with instance-level embeddings, while reducing memory requirements and limiting the computation required for semantic fusion, even if environments are very large. A top-down view of the maps generated using an HM3DSem sequence [8] is shown in Fig. 1. 

E. Implementation Details 

All embeddings that are part of the voxel maps MD, MI,

MDfused and MIfused share a joint semantic vision-language space. Using a text prompt encoded with Enc text (·), which operates in the same space, we can derive semantic similarities for all voxels using cosine similarity. Following prior work [9] [4], we select a set of key-frames for processing. To this end, we use angular and translational motion thresholding, which helps prevent the accumulation of information from redundant viewpoints, e.g. if the camera is not in motion. Since instance-level mapping involves segmentation and image-crop encoding, which are relatively computationally expensive, we opt to process these frames less frequently by selecting higher thresholds for the instance-level mapping pipeline compared to the dense mapping pipeline. To keep the number of observations per traveled distance and rotation balanced between the two maps, we use a proportionally higher pixel sampling density for 3D projection of points in the instance-level mapping. The values used in our experiments are described in Section IV-B. IV. E XPERIMENTAL SETUP 

We demonstrate our proposed cross-layer semantic fusion method for two different sets of patch-level and image-level encoders Enc patch (·), Enc img (·), which we detail in the following. Subsequently, we present a parameter overview and the evaluation protocol for 3D semantic segmentation. 

A. Vision Models 1) NARADIO Setup: As the primary combination of vision-models, we employ variants of the NARADIO encoder, proposed by [7]. NARADIO is based on an agglomerative vision foundation model, RadioV2.5 [12], which is trained by distilling multiple teacher models. The features are projected into the summary token ( CLS ) space of SIGLIP [13], using a pre-trained adapter head. A spatial attention mechanism [15] helps reinforce the spatial consistency of semantic embed-dings in patch neighborhoods. As image-level embeddings (Enc img (·)) we use the CLS token of the SIGLIP adapter. 

2) Clip-DINOiser Setup: In Section V-A.4, we show results with a second pair of encoders. To extract patch-level embeddings, we use CLIP-DINOiser [16]. The work shows that the output embeddings of MaskCLIP [17] can be refined through a guided pooling operation that is trained to replicate the patch affinity as produced by DINO [18]. The method involves a saliency estimation step. Rather than applying a hard threshold to the saliency estimates to extract a binary background mask as in [16], we use them as weights in the dense semantic mapping update (Equation 1), replacing the observation counts. We complement the patch-level em-beddings with the image-level embeddings of OpenClip [19], whose semantic space CLIP-DINOiser inherits. 

3) Segmentation Model: For image segmentation ( Seg (·))we use FastSAM [20], a class-agnostic segmentation model that adapts an efficient convolutional neural network (CNN) architecture to the Segment Anything task introduced in [2]. 

B. Parameter Overview 

In all experiments, we use a voxel side length of 5 cm 

and resize depth and RGB frames to a resolution of 480 ×

640 . For dense semantic mapping, we use tighter motion thresholds (0.08 m, 0.06 rad ) than for instance-level mapping (0.14 m, 0.11 rad ), but a lower pixel sampling density (5 vs. 9 pixels per image patch; patches correspond to those of Enc patch (·)). The overlap threshold τiou for the geometrical instance association (Section. III-B.3) is 0.2 and the maximum number of instance-hypotheses K is 3 per voxel. We use a variance ratio λ of 1.0 with NARADIO and 5.0 with the Clip-DINOiser setup. 

C. Dataset Experiments 

Following prior work [1] [7] we evaluate semantic 3D segmentation with a set of RGB-D sequences from the synthetic Replica dataset [21] (scenes: office: [0-4] ,

room: [0-2] ) and real camera sequences from Scannet [22] (scenes: 0011 , 0050 , 0231 , 0378 , 0518 ). While Replica and ScanNet are standard data sets for semantic 3D seg-mentation, they consist mostly of single rooms. To better quantify the scaling properties of the sliding window mode (Section III-D), we additionally conduct experiments with RGB-D sequences from Habitat Matterport 3D Semantic (HM3DSem) [8]. We use seven sequences ( 00824 , 00829 ,

00843 , 00847 , 00873 , 00877 , 00890 ), with trajectories generated by [5]. They include apartments and multi-story houses with, e.g., 8 rooms ( 00824 ) and 10 rooms ( 00847 ), respectively. We use a sliding window with a radius of 6 m 

and evaluate using the 40 class labels of Matterport [8]. We use the semantic segmentation evaluation protocol from the authors of [7]. This entails evaluating both performance over the entire set of classes of each respective dataset, as well as without the classes that can be considered to be background, such as ’floor’, ’wall’, ’ceiling’, ’door’, ’window’. Reported metrics are the mean Intersection-over-Union (mIoU), frequency-modulated mIoU (fmIoU) [1], and mean accuracy (Acc). For details on the evaluation protocol, we refer to [7]. As in [10], when evaluating the instance-level semantic maps ( MI, MIfused ), for each voxel, we use the embedding vector from the instance hypothesis with the highest observation count. V. R ESULTS 

We evaluate FUS3DMaps’ 3D semantic segmentation performance, focusing exclusively on training-free open-vocabulary semantic mapping approaches. 

A. Semantic 3D Segmentation 

We propose grouping methods into those that produce dense semantic maps with embeddings on voxel- or point-level and methods that produce instance-level semantic maps. The methods within each of these groups share similar scaling properties in large environments. For dense semantic mapping methods, we report metrics with and without background classes, while for instance-level methods, we report them without background only. 

1) Instance-Level Semantic Maps: We compare the instance-level output of FUS3DMaps ( MIfused ), using the NARADIO encoder setup, to the recent scene-graph ap-proaches ConceptGraphs [6] and HOV-SG [5] and the real-time 3D semantic mapping method Open-Fusion [4]. To generate instance proposals, OpenVox [10] relies on an open-vocabulary object detector that is provided with a predefined list of classes 4, thereby avoiding the challenges associated with integrating class-agnostic segmentation [20] [2] into the pipeline. We therefore include its metrics as a reference only. In addition, we report metrics for the sliding window mode of our method ( MIfused , Sliding Window ). As shown in Table I, FUS3DMaps substantially outperforms all instance-level semantic mapping baselines.                                                       

> TABLE I SEMANTIC 3D S EGMENTATION W . I NSTANCE -L EVEL REPRESENATIONS
> class-agnostic Replica ScanNet OpenVox [10]
> mIoU 0.2427 0.4449
> ✗fmIoU 0.5361 0.5466
> Acc 0.5995 0.6181
> ConceptGraphs 1[6]
> mIoU 0.1163 0.2162
> ✓fmIoU 0.1661 0.2432
> Acc 0.1980 0.3104
> HOV-SG 1[5]
> mIoU 0.1693 0.2679
> ✓fmIoU 0.3145 0.3605
> Acc 0.3474 0.4417
> Open-Fusion [4]
> mIoU 0.1848 0.2997
> ✓fmIoU 0.4364 0.3958
> Acc 0.4887 0.4552
> FUS3DMaps w. NARADIO (Ours) (MIfused )
> mIoU 0.3137 0.4689
> ✓fmIoU 0.5627 0.5231 Acc 0.6522 0.6316 FUS3DMaps w. NARADIO (Ours) (MIfused ,Sliding Window )
> mIoU 0.3237 0.4671
> ✓fmIoU 0.5648 0.5161
> Acc 0.6519 0.6250
> 1Numbers taken from [7].
> 4We use the author’s publicly available implementation.

2) Dense Semantic Maps: FUS3DMaps is primarily de-signed for instance-level semantic mapping. However, we find that cross-layer semantic fusion can also yield improvements for dense-semantic mapping. Table II shows a comparison of the dense fused output ( MDfused ) of FUS3DMaps with the NARADIO setup with two dense semantic mapping baselines ConceptFusion [1] and Rayfronts [7], which is similar to our unfused dense layer. Our method improves with respect to both baselines across the Replica and ScanNet evaluations, with ( w/ bgr ) and without background ( w/o bgr ) classes.                                                         

> TABLE II SEMANTIC 3D S EGMENTATION W . D ENSE SEMANTIC MAPS ,
> WITH (W/BGR )AND WITHOUT BACKGROUND CLASSES (WO /BGR )
> Replica ScanNet w/o bgr w/ bgr w/o bgr w/ bgr ConceptFusion 1
> [1]
> mIoU 0.2107 0.2038 0.2176 0.1857
> fmIoU 0.3151 0.3575 0.2671 0.2306
> Acc 0.3565 0.4158 0.3413 0.2877
> Rayfronts 1[7]
> mIoU 0.3937 0.2773 0.4129 0.3229
> fmIoU 0.6203 0.4337 0.4642 0.3904
> Acc 0.6880 0.5445 0.5676 0.4915
> FUS3DMaps w. NARADIO (Ours) (MDfused )
> mIoU 0.4117 0.2940 0.4303 0.3352 fmIoU 0.6416 0.4615 0.4801 0.3923 Acc 0.7095 0.5695 0.5765 0.4959
> 1Numbers taken from [7].
> Fig. 3. Qualitative examples of differences between the fused and unfused map layers in scene room0 of Replica. The color scheme reflects cosine similarity to the text prompt in the row label. The top two rows are examples of the instance-level layer, the bottom two rows show the dense layer.

3) Qualitative Results: We additionally show a set of qualitative examples on the synthetic Replica dataset with the NARADIO encoder setup. Replica was selected because the absence of geometric errors from noisy depth and drift allows visualizing the impact of the semantic fusion more clearly compared to ScanNet. Fig. 3 depicts a comparison of semantic similarities with text embeddings between the fused Fig. 4. Qualitative examples of predicted class labels of the instance-level layer (top row) and dense layer (bottom row) of the voxel map. The colors are randomly chosen per class label and consistent between ground truth and predictions. The ground truth is visualized without background labels. 

and unfused semantic maps. The examples in the upper two rows ( MI, MIfused ) indicate that fusion helps to distinguish pillows from cushions that are part of the sofa and helps to separate small objects, such as vases, from other objects and the background. The lower two rows ( MD, MDfused ) show how fusion helps to reduce semantic noise within larger objects. This illustrates how fusion captures complementary strengths of instance-level and dense representations. Similar improvements are seen for segmentation predictions in Fig. 4. 

4) Ablations: In Tables III and IV we compile comparisons between the fused dense and instance-level maps, with their non-fused counterparts. We additionally show results using the alternative pair of image encoders ( ClipDINO ). We find that fusion improves semantic 3D segmentation performance on nearly every metric, for both instance-level and dense maps, and across both encoder setups. 

TABLE III ABLATION STUDY : I NSTANCE -L EVEL SEMANTIC MAPS 

Replica ScanNet unfused fused unfused fused FUS3DMaps w. NARADIO 

(MI/MIfused )

mIoU 0.2665 0.3137 0.3808 0.4689 fmIoU 0.4905 0.5627 0.4227 0.5231 Acc 0.5687 0.6522 0.5113 0.6316 FUS3DMaps w. ClipDINO 

(MI/MIfused )

mIoU 0.2499 0.2455 0.3382 0.3741 fmIoU 0.4203 0.4300 0.4424 0.4619 Acc 0.4803 0.4974 0.5085 0.5350 

TABLE IV ABLATION STUDY : D ENSE SEMANTIC MAPS , 

> WITH

(W/ BGR ) AND WITHOUT BACKGROUND CLASSES (WO / BGR )

Replica ScanNet wo/ bgr w/ bgr wo/ bgr w/ bgr FUS3DMaps w. NARADIO 

(MD)

mIoU 0.4016 0.2911 0.4045 0.3226 

fmIoU 0.6172 0.4480 0.4635 0.3879 

Acc 0.6848 0.5533 0.5610 0.4897 

FUS3DMaps w. NARADIO 

(MDfused )

mIoU 0.4117 0.2940 0.4303 0.3352 fmIoU 0.6416 0.4615 0.4801 0.3923 Acc 0.7095 0.5695 0.5765 0.4959 FUS3DMaps w. ClipDINO 

(MD)

mIoU 0.1256 0.1558 0.2902 0.2769 

fmIoU 0.3359 0.3777 0.3582 0.3450 

Acc 0.3986 0.4705 0.4370 0.4397 

FUS3DMaps w. ClipDINO 

(MDfused )

mIoU 0.2657 0.2262 0.3610 0.3014 fmIoU 0.4684 0.4053 0.4397 0.3447 Acc 0.5204 0.4975 0.5059 0.4306 

Finally, we show parameter sweeps for the variance ratio λ

(Section III-C) in Table V and report average metrics of the fused instance-level layer of all Replica and ScanNet scenes combined. 

TABLE V EFFECT OF λ ON INSTANCE -L EVEL MAPS (AVG . OVER REPLICA &SCAN NET ) BOLD : BEST PER ENCODER UNDERLINE : CHOSEN VALUE 

λ 0.1 0.2 1.0 5.0 10.0 

FUS3DMaps w. NARADIO 

(MIfused )mIoU 0.391 0.389 0.391 0.381 0.366 fmIoU 0.532 0.532 0.543 0.517 0.504 mAcc 0.630 0.630 0.642 0.610 0.594 

FUS3DMaps w. ClipDINO 

(MIfused )mIoU 0.243 0.257 0.306 0.310 0.296 fmIoU 0.379 0.404 0.444 0.446 0.439 mAcc 0.464 0.490 0.525 0.516 0.506 

5) Framerates: Table VI presents an overview of the average frame rates measured on the Replica and ScanNet scenes with a desktop with NVIDIA RTX 4090 and a Jetson AGX Orin 64 GB. Using motion thresholding as described in Section III-E generally leads to different frame-rates for the dense and instance-level mapping pipelines. Table VI shows the average of all processed key-frames, with the settings used in our evaluation. We report frame rates of FUS3DMaps without fusion ( no fusion ), assuming that fusion is performed upon receiving a text prompt. Furthermore, we provide frame rates using the sliding window mode, which involves semantic fusion at every fifth processed RGB-D frame ( Sliding Window ), serving as an indicator of the impact of semantic fusion on the processing rate of our method. Frame rates with NARADIO on the Jetson Orin are measured using TensorRT [23] optimization for the backbone model. 

TABLE VI AVERAGE FREQUENCY PER PROCESSED FRAME (H Z)

NARADIO Clip-DINOiser Setup Replica ScanNet Replica ScanNet RTX 4090 (no fusion) 11.85 11.24 16.50 15.48 

RTX 4090 (Sliding Window ) 11.00 10.15 15.88 14.59 

Jetson (no fusion) 3.54 3.44 3.23 3.10 

Jetson (Sliding Window ) 3.14 2.94 3.16 3.00 

B. Sliding Window Mode 

Table VII shows the results of experiments with seven large scale HM3DSem sequences. It compares the instance-level semantic layer ( MI), the fused instance-level layer ( MIfused )and the fused instance-level layer produced by the sliding window mode ( MIfused , Sliding Window ). Additionally, we include an ablation without the evidence scoring mechanism of the sliding window mode ( MIfused , Sliding Window , no 

escore id ), described in Section III-D. We add results for the strongest of the instance-level baselines [4] as a reference. The results show that the sliding window mode does not sig-nificantly reduce the instance-level 3D semantic segmentation performance, compared to the full dual-layer mapping. Table VIII shows a comparison of the memory required to maintain the dense semantic layer MD with 3D voxel locations and dense semantic embeddings. It is compared TABLE VII SEMANTIC 3D S EGMENTATION PERFORMANCE ON 

HM3DS EM SEQUENCES (AVG . OVER 7 SCENES )

Open-Fusion [4] 

mIoU 0.2587 

fmIoU 0.3031 

Acc 0.3797 

FUS3DMaps w. NARADIO 

(MI)

mIoU 0.3553 

fmIoU 0.4336 

Acc 0.5538 

FUS3DMaps w. NARADIO 

(MIfused )

mIoU 0.4062 fmIoU 0.4708 Acc 0.5887 FUS3DMaps w. NARADIO 

(MIfused , Sliding Window )

mIoU 0.4022 

fmIoU 0.4622 

Acc 0.5778 

FUS3DMaps w. NARADIO 

(MIfused , Sliding Window , no escore id )

mIoU 0.3530 

fmIoU 0.4135 

Acc 0.5278 

with the memory footprint of the FUS3DMaps sliding window mode ( SW ), using a radius of 6m. These numbers show the total required memory, including voxel locations, observation counts, raw and fused instance embeddings, and dense embed-dings within the sliding window. The data are measured after processing the last frame of the respective dataset sequence. Additionally, we show the memory expense of a sequence (Hospital diff P1004 ) from TartanGround [24], which covers three levels of a hospital building and a sequence that we collected using a LiDAR-RGB-D sensing module [25] covering two floors of a university building. In all sequences, we use a voxel side-length of 5cm. 

TABLE VIII MEMORY COMPARISON OF SEMANTIC MAPS (IN MIB) 

NARADIO ClipDINO Scene MD SW MD SW 

HM3DSem 00824 1243 355 623 180 

HM3DSem 00829 899 268 451 137 

HM3DSem 00843 1145 254 574 130 

HM3DSem 00847 1201 546 603 276 

HM3DSem 00873 1194 311 599 158 

HM3DSem 00877 1162 323 583 164 

HM3DSem 00890 1473 231 739 118 

Tartanground Hospital 5970 354 2994 185 

Odin1 University 5862 1054 2940 534 

The results show that, at equal voxel resolution, the memory required for our dual-layer sliding window mode is substantially lower and scales better to larger environments compared to the memory required for a dense semantic map. VI. C ONCLUSIONS 

We present an online open-vocabulary semantic mapping method that fuses embeddings of instance hypotheses with those of a dense semantic voxel layer. The proposed sliding window semantic mapping approach enables strong open-vocabulary semantic mapping by enriching instance-level embeddings with embeddings of the dense map layer. Fur-thermore, it scales substantially better with environment size than purely dense semantic maps. REFERENCES [1] K. M. Jatavallabhula, A. Kuwajerwala, Q. Gu, M. Omama, T. Chen, A. Maalouf, S. Li, G. Iyer, S. Saryazdi, N. Keetha, A. Tewari, J. B. Tenenbaum, C. M. de Melo, M. Krishna, L. Paull, F. Shkurti, and A. Torralba, “Conceptfusion: Open-set multimodal 3d mapping,” 2023. [Online]. Available: https://arxiv.org/abs/2302.07241 [2] A. Kirillov, E. Mintun, N. Ravi, H. Mao, C. Rolland, L. Gustafson, T. Xiao, S. Whitehead, A. C. Berg, W.-Y. Lo, P. Doll ´ar, and R. Girshick, “Segment anything,” arXiv:2304.02643 , 2023. [3] A. Radford, J. W. Kim, C. Hallacy, A. Ramesh, G. Goh, S. Agarwal, G. Sastry, A. Askell, P. Mishkin, J. Clark, G. Krueger, and I. Sutskever, “Learning transferable visual models from natural language supervision,” 2021. [Online]. Available: https://arxiv.org/abs/2103.00020 [4] K. Yamazaki, T. Hanyu, K. Vo, T. Pham, M. Tran, G. Doretto, A. Nguyen, and N. Le, “Open-fusion: Real-time open-vocabulary 3d mapping and queryable scene representation,” 2023. [Online]. Available: https://arxiv.org/abs/2310.03923 [5] A. Werby, C. Huang, M. B ¨uchner, A. Valada, and W. Burgard, “Hierarchical open-vocabulary 3d scene graphs for language-grounded robot navigation,” Robotics: Science and Systems , 2024. [6] Q. Gu, A. Kuwajerwala, S. Morin, K. M. Jatavallabhula, B. Sen, A. Agarwal, C. Rivera, W. Paul, K. Ellis, R. Chellappa, C. Gan, C. M. de Melo, J. B. Tenenbaum, A. Torralba, F. Shkurti, and L. Paull, “Conceptgraphs: Open-vocabulary 3d scene graphs for perception and planning,” 2023. [Online]. Available: https://arxiv.org/abs/2309.16650 [7] O. Alama, A. Bhattacharya, H. He, S. Kim, Y. Qiu, W. Wang, C. Ho, N. Keetha, and S. Scherer, “Rayfronts: Open-set semantic ray frontiers for online scene understanding and exploration,” 2025. [Online]. Available: https://arxiv.org/abs/2504.06994 [8] K. Yadav, R. Ramrakhya, S. K. Ramakrishnan, T. Gervet, J. Turner, A. Gokaslan, N. Maestre, A. X. Chang, D. Batra, M. Savva et al. , “Habitat-matterport 3d semantics dataset,” 

arXiv preprint arXiv:2210.05633 , 2022. [Online]. Available: https: //arxiv.org/abs/2210.05633 [9] T. B. Martins, M. R. Oswald, and J. Civera, “Open-vocabulary online semantic mapping for slam,” IEEE Robotics and Automation Letters ,2025. [10] Y. Deng, B. Yao, Y. Tang, Y. Yang, and Y. Yue, “Openvox: Real-time instance-level open-vocabulary probabilistic voxel representation,” 2025. [Online]. Available: https://arxiv.org/abs/2502.16528 [11] S. Peng, K. Genova, C. M. Jiang, A. Tagliasacchi, M. Pollefeys, and T. Funkhouser, “Openscene: 3d scene understanding with open vocab-ularies,” in Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) , 2023. [12] G. Heinrich, M. Ranzinger, Hongxu, Yin, Y. Lu, J. Kautz, A. Tao, B. Catanzaro, and P. Molchanov, “Radiov2.5: Improved baselines for agglomerative vision foundation models,” 2025. [Online]. Available: https://arxiv.org/abs/2412.07679 [13] X. Zhai, B. Mustafa, A. Kolesnikov, and L. Beyer, “Sigmoid loss for language image pre-training,” in Proceedings of the IEEE/CVF International Conference on Computer Vision (ICCV) , 2023, pp. 11 997– 12 008. [14] O. Alama, D. Jariwala, A. Bhattacharya, S. Kim, W. Wang, and S. Scherer, “Radseg: Unleashing parameter and compute efficient zero-shot open-vocabulary segmentation using agglomerative models,” 2025. [Online]. Available: https://arxiv.org/abs/2511.19704 [15] S. Hajimiri, I. Ben Ayed, and J. Dolz, “Pay attention to your neighbours: Training-free open-vocabulary semantic segmentation,” in Proceedings of the IEEE/CVF Winter Conference on Applications of Computer Vision , 2025. [16] M. Wysocza ´nska, O. Sim ´eoni, M. Ramamonjisoa, A. Bursuc, T. Trzci ´nski, and P. P ´erez, “Clip-dinoiser: Teaching clip a few dino tricks for open-vocabulary semantic segmentation,” 2024. [Online]. Available: https://arxiv.org/abs/2312.12359 [17] C. Zhou, C. C. Loy, and B. Dai, “Extract free dense labels from clip,” in European Conference on Computer Vision (ECCV) , 2022. [18] M. Caron, H. Touvron, I. Misra, H. J ´egou, J. Mairal, P. Bojanowski, and A. Joulin, “Emerging properties in self-supervised vision transformers,” 2021. [Online]. Available: https://arxiv.org/abs/2104.14294 [19] G. Ilharco, M. Wortsman, R. Wightman, C. Gordon, N. Carlini, R. Taori, A. Dave, V. Shankar, H. Namkoong, J. Miller, H. Hajishirzi, A. Farhadi, and L. Schmidt, “Openclip,” Jul. 2021, if you use this software, please cite it as below. [Online]. Available: https://doi.org/10.5281/zenodo.5143773 [20] X. Zhao, W. Ding, Y. An, Y. Du, T. Yu, M. Li, M. Tang, and J. Wang, “Fast segment anything,” 2023. [Online]. Available: https://arxiv.org/abs/2306.12156 [21] J. Straub, T. Whelan, L. Ma, Y. Chen, E. Wijmans, S. Green, J. J. Engel, R. Mur-Artal, C. Ren, S. Verma, A. Clarkson, M. Yan, B. Budge, Y. Yan, X. Pan, J. Yon, Y. Zou, K. Leon, N. Carter, J. Briales, T. Gillingham, E. Mueggler, L. Pesqueira, M. Savva, D. Batra, H. M. Strasdat, R. D. Nardi, M. Goesele, S. Lovegrove, and R. Newcombe, “The Replica dataset: A digital replica of indoor spaces,” arXiv preprint arXiv:1906.05797 , 2019. [22] A. Dai, A. X. Chang, M. Savva, M. Halber, T. Funkhouser, and M. Nießner, “Scannet: Richly-annotated 3d reconstructions of indoor scenes,” in Proc. Computer Vision and Pattern Recognition (CVPR), IEEE , 2017. [23] NVIDIA, “NVIDIA TensorRT SDK,” 2025. [Online]. Available: https://developer.nvidia.com/tensorrt [24] M. Patel, F. Yang, Y. Qiu, C. Cadena, S. Scherer, M. Hutter, and W. Wang, “Tartanground: A large-scale dataset for ground robot perception and navigation,” arXiv preprint arXiv:2505.10696 , 2025. [25] Manifold Tech, “Odin1 Spatial Memory Module,” Available online, 2025, accessed: 2026-02-06. [Online]. Available: https: //www.manifoldtech.cn/