---
title: "FeatureFox: Sample-Efficient Panoptic Graph Segmentation for Machining Feature Recognition in B-Rep 3D-CAD Models"
title_zh: FeatureFox：用于 B-Rep 3D-CAD 模型加工特征识别的样本高效全景图分割
authors: "Bertram Fuchs, Altay Kacan, Aaron Haag, Oliver Lohse"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26770v1"
tags: ["query:partseg"]
score: 9.0
evidence: 用于3D-CAD模型加工特征识别的全景图分割
tldr: 本研究提出FeatureFox，一种针对B-Rep 3D-CAD模型的高效全景自动特征识别（AFR）流水线。该方法通过边缘分类定位特征边界，利用修剪后的面邻接图恢复实例，并进行语义标注。相比深度学习基准AAGNet，FeatureFox在极少样本（约250个）下即可达到高精度，且训练速度极快，在工业实际应用中展现出极强的泛化能力和实用性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26770v1/fig-001.webp\", \"caption\": \"Fig. 4. Sample efficiency on MFInstSeg (excluding stock): PQ versus trainingset size for AAGNet and FeatureFox. FeatureFox dominates in the low-data regime and reaches > 90% PQ with ∼ 250 training parts, whereas AAGNet requires ∼ 5,000; AAGNet is only marginally better on the full training set.\", \"page\": 5, \"index\": 1, \"width\": 524, \"height\": 355}, {\"url\": \"assets/figures/arxiv/2604.26770v1/fig-002.webp\", \"caption\": \"Fig. 3. FeatureFox pipeline. Starting from a B-Rep 3D-CAD model, we construct a face-adjacency graph and attributes, propagate vertex information to edges (1), and localize the feature with a calibrated edge boundary classifier for instance grouping (2). We then compute connected-component attributes (3) and recognize the class of each predicted instance with a semantic model (4). Pink: Attribute calculation. Teal: Model training. Gray: Intermediate representation.\", \"page\": 5, \"index\": 2, \"width\": 816, \"height\": 277}]"
motivation: 现有的自动特征识别方法通常过于复杂、依赖大量数据，且无法同时高效处理实例分割与语义标注。
method: 采用校准的二元边缘分类器定位边界，通过面邻接图的连通分量提取实例，并利用聚合子图属性进行类别预测。
result: 在MFInstSeg数据集上，FeatureFox仅需250个样本即可达到0.9以上的全景质量（PQ），且训练时间缩短至秒级。
conclusion: FeatureFox是一种样本和计算效率极高的全景特征识别方案，特别适用于标注数据稀缺的工业实际场景。
---

## 摘要
B-Rep 3D-CAD 模型上的自动特征识别 (AFR) 是 CAD/CAM 自动化的核心，但大多数基于学习的方法往往过于复杂、依赖大量数据，且将实例分组与语义标注分开评估。我们提出了 FeatureFox，这是一个输出带有语义标签的加工实例的全景 AFR 流水线：通过在增强的边属性上使用校准的二元边分类器来定位特征边界，将实例恢复为剪枝后的面相邻图中的连通分量，并利用聚合的子图属性通过逐实例分类器预测加工类别。我们在 MFInstSeg 数据集上使用全景质量 (PQ) 指标进行评估，该指标共同衡量实例分离和语义正确性。FeatureFox 在样本和计算效率上显著优于深度学习基准模型 AAGNet，仅需约 250 个训练零件即可达到 PQ > 0.9，而 AAGNet 则需要约 5,000 个，且在完整 MFInstSeg 集上的训练在 GPU 上仅需数秒。在全量训练集上，AAGNet 的 PQ 略高于 FeatureFox，而 FeatureFox 在特征级识别和定位精度上仍保持微弱领先。最后，利用其低数据需求的优势，我们在 270 个手动标注的工业 CAD 零件上训练了 FeatureFox，并展示了其对未见过的真实工业零件的定性泛化能力，表明了其在现实世界中的实际应用价值。

## Abstract
Automatic feature recognition (AFR) on B-Rep 3D-CAD models is central to CAD/CAM automation, yet most learning-based methods are complex, data-hungry, and evaluate instance grouping and semantic labeling separately. We present FeatureFox, a panoptic AFR pipeline that outputs machining instances with semantic labels: a calibrated binary edge classifier on enriched edge attributes localizes feature boundaries, instances are recovered as connected components in a pruned face-adjacency graph, and a per-instance classifier predicts the machining class from aggregated subgraph attributes. We evaluate on MFInstSeg using Panoptic Quality (PQ), which jointly scores instance separation and semantic correctness. FeatureFox is substantially more sample- and compute-efficient than the deep baseline AAGNet, reaching $\mathrm{PQ}>0.9$ with $\sim250$ training parts versus $\sim5{,}000$ for AAGNet, and training on the full MFInstSeg set takes seconds on a GPU. On the full training set, AAGNet surpasses FeatureFox marginally in PQ, while FeatureFox remains slightly ahead in feature-level recognition and localization accuracy. Finally, leveraging its low data requirement, we train FeatureFox on $270$ manually labeled industrial CAD parts and show qualitative generalization to an unseen real industrial part, indicating practical real-world applicability.

---

## 论文详细总结（自动生成）

以下是对论文《FeatureFox: Sample-Efficient Panoptic Graph Segmentation for Machining Feature Recognition in B-Rep 3D-CAD Models》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
自动特征识别（AFR）是连接 CAD 设计与 CAM 制造自动化的关键环节。尽管深度学习在 B-Rep（边界表示）模型上取得了进展，但现有方法（如 AAGNet）存在三大痛点：
*   **数据饥渴**：通常需要数千个标注零件才能达到高精度，而工业界高质量标注数据稀缺且昂贵。
*   **任务割裂**：往往将实例分割（区分不同特征）与语义标注（识别特征类型）分开处理，缺乏统一的评估标准。
*   **计算复杂**：基于图神经网络（GNN）的模型训练耗时长，难以在资源受限的工业现场快速部署。
**FeatureFox** 的提出旨在通过一种轻量级、样本高效的“全景图分割”框架，解决小样本下的工业级特征识别问题。

### 2. 方法论：核心思想与关键技术
FeatureFox 采用了一种层次化的树状模型流水线，将 AFR 建模为**全景分割（Panoptic Segmentation）**任务：
*   **图表示与属性增强**：将 B-Rep 模型转化为面邻接图（Face-Adjacency Graph）。通过采样面（UV-grid）和边（Edge sampling）提取几何特征，并将面属性传播至边，形成“增强边属性”。
*   **校准的边缘分类器**：使用 **XGBoost** 训练一个二元分类器，预测每条边是否为特征边界。引入**等值回归（Isotonic Calibration）**对概率进行校准，以确保决策阈值的稳定性。
*   **实例恢复**：根据预测的边标签对原始图进行剪枝，将保留的连通分量（Connected Components）识别为独立的加工特征实例。
*   **逐实例语义分类**：对每个提取出的实例子图进行属性聚合（如面数量、对数特征长度、曲率分布等），再通过另一个 XGBoost 分类器预测其语义类别（如孔、槽、口袋等）。

### 3. 实验设计
*   **数据集**：主要使用公开的 **MFInstSeg** 数据集（包含约 6.2 万个零件）。此外，还使用了一个包含 **270 个手动标注的真实工业零件**的小型私有数据集。
*   **基准模型（Benchmark）**：对比了当前最先进的深度学习模型 **AAGNet** 和 **MFTReNet**。
*   **评估指标**：引入了计算机视觉中的**全景质量（PQ, Panoptic Quality）**，该指标能同时衡量实例定位的准确性（IoU）和类别识别的正确性。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX 6000 Ada** GPU。
*   **训练时长**：
    *   **FeatureFox**：在完整数据集上训练仅需 **8 秒**，GPU 利用率 100%。
    *   **AAGNet**：训练 100 个 epoch 耗时 **38 分钟**，且 GPU 利用率较低（约 18%）。
*   **效率对比**：FeatureFox 的训练速度比深度学习基准快了约 285 倍。

### 5. 实验数量与充分性
*   **实验规模**：进行了多组对比实验，包括全量数据性能对比、**样本效率消融实验**（从 50 到 4.3 万个样本的不同梯度）、以及在 NIST 工业标准零件上的定性泛化测试。
*   **客观性**：实验采用了随机种子多次运行取平均值，并使用了统一的 PQ 指标进行公平对比。结果显示，在小样本（<10,000 样本）情况下，FeatureFox 具有压倒性优势。

### 6. 主要结论与发现
*   **样本效率极高**：FeatureFox 仅需 **250 个**训练样本即可达到 PQ > 0.9，而 AAGNet 达到同等水平需要约 **5,000 个**样本。
*   **性能表现**：在全量数据集上，AAGNet 的 PQ 略高（约 1%），但 FeatureFox 在特征级完全识别率和定位精度上仍有微弱优势。
*   **工业泛化性**：仅用 270 个工业样本训练的模型，即可在未见过的 NIST 复杂工业零件上正确识别出大部分孔、槽和口袋特征。

### 7. 优点与亮点
*   **全景视角**：首次在 AFR 领域倡导使用 PQ 指标，统一了实例和语义任务。
*   **极速训练**：秒级的训练时间使得“针对特定客户/零件库进行现场微调”成为可能。
*   **可解释性**：相比黑盒 GNN，基于决策树和手工几何特征的流水线更易于工业调试和理解。
*   **低门槛**：极低的数据需求打破了深度学习在工业 AFR 落地中的壁垒。

### 8. 不足与局限
*   **非端到端风险**：由于是层次化结构，第一步边缘分类的错误会直接传播到后续的语义分类。
*   **复杂拓扑处理**：目前无法处理“非连通面集”构成的特征（即一个加工特征由物理上不连续的面组成）。
*   **特征覆盖**：对于某些极其罕见的复杂特征（如多级阶梯孔），由于训练样本极少，模型仍存在误判风险。
*   **依赖手工特征**：虽然高效，但可能无法像深度学习那样自动挖掘出极其细微的几何线索。

（完）
