---
title: "DelowlightSplat: Feed-Forward Gaussian Splatting for Lowlight 3D Scene Reconstruction"
title_zh: DelowlightSplat：用于低光3D场景重建的前馈式高斯泼溅
authors: "Fuzhen Jiang, Zengtian Xie, Zhuoran Li"
date: 2026-05-26
pdf: "https://arxiv.org/pdf/2605.26629v1"
tags: ["query:ffdr"]
score: 9.0
evidence: 前馈式高斯泼溅用于低光照3D重建
tldr: 低光照下前馈3D高斯泼溅重建因噪声、颜色偏移和匹配失败而性能严重退化。本文提出DelowlightSplat，一个低光照感知的前馈框架，通过轻量级低光照适配器进行残差增强以改善特征匹配，并利用代价体积多视图推理直接预测干净3D高斯。在可控低光照基准上，该方法在新视角合成质量上显著优于已有前馈方法和两阶段流水线。该工作为退化环境下的3D场景重建提供了高效且实用的解决方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.26629v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1499, \"height\": 483, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.26629v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1506, \"height\": 793, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.26629v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 830, \"height\": 606, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.26629v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 745, \"height\": 261, \"label\": \"Table\"}]"
motivation: 前馈3D高斯重建在低光照条件下因噪声、颜色偏移和对应不可靠而失败，亟需低光照感知的增强方法。
method: 引入轻量级低光照适配器进行残差增强以提升匹配性，耦合代价体积多视图推理直接预测干净3D高斯。
result: 在可控低光照基准上，DelowlightSplat的PSNR和SSIM显著优于先前前馈方法和两阶段流水线。
conclusion: DelowlightSplat实现了低光照下高质量新视角合成与3D重建，拓展了高斯泼溅在退化场景中的应用。
---

## 摘要
新视角合成和从稀疏位姿图像进行3D重建是机器人和增强现实/虚拟现实的核心。然而，在低光条件下，前馈式3D高斯重建因噪声、颜色偏移和不可靠的对应关系而失败。我们提出了DelowlightSplat，一个低光感知的前馈式高斯泼溅框架，用于清洁的新视角渲染。我们通过仅退化上下文视图而保持目标视图清洁，构建了一个可控的多视角低光基准。我们引入了一个轻量级的低光适配器用于残差增强以提高匹配性，并将其与基于成本体积的多视角推理相结合，直接预测清洁的3D高斯。实验表明，DelowlightSplat在低光条件下显著优于先前的前馈式方法和两阶段流程。

## Abstract
Novel-view synthesis and 3D reconstruction from sparse posed images are central to robotics and AR/VR. Yet, feed-forward 3D Gaussian reconstruction fails under lowlight due to noise, color shifts, and unreliable correspondence. We propose DelowlightSplat, a lowlight-aware feed-forward Gaussian splatting framework for clean novel-view rendering. We build a controllable multi-view lowlight benchmark by degrading only context views while keeping target views clean. We introduce a lightweight Lowlight Adapter for residual enhancement to improve matchability, and couple it with cost-volume-based multi-view inference to directly predict clean 3D Gaussians. Experiments show that DelowlightSplat significantly outperforms previous feed-forward method and two-stage pipeline under lowlight conditions.

---

## 论文详细总结（自动生成）

# DelowlightSplat：用于低光3D场景重建的前馈式高斯泼溅

## 1. 论文的核心问题与整体含义

- **研究动机**：前馈式3D高斯泼溅（3DGS）重建在稀疏位姿图像下能够快速预测3D场景，但在低光条件下，由于传感器噪声、颜色偏移和图像间对应关系不可靠，现有方法性能严重退化。直接使用低光输入的重建几何失真、纹理过度平滑；先用2D增强模型恢复每张图再重建的两阶段流程则可能引入视角依赖的颜色/光照偏移，破坏多视图一致性。
- **整体含义**：本文旨在设计一个低光感知的前馈式3DGS框架，能够从低光输入直接预测清洁、正常照明的3D高斯场景，从而在退化环境下实现高效且高质量的新视角合成与3D重建。

## 2. 方法论

### 核心思想
- 在现有的前馈式3DGS框架（以MVSplat为基础）前插入一个轻量级**低光适配器（Lowlight Adapter）**，对低光上下文视图进行残差增强，提升特征匹配性；然后利用基于代价体积的多视图编码器进行几何和外观推理，直接预测一组清洁的3D高斯，并通过可微高斯泼溅渲染器从任意目标视角渲染出正常照明的图像。

### 关键技术细节
1. **低光适配器**：  
   - 对输入图像 I_i 预测一个残差 Δ(I_i)，输出适应后的图像：  
     \(\tilde{I}_i = \text{clip}(I_i + \lambda \cdot \tanh(\Delta(I_i)))\)  
   - Δ(·) 由少量卷积残差块实现，λ 控制残差幅度。残差设计使得在退化微弱时可保持接近恒等映射，避免过度增强。适配器与重建主干端到端联合训练，目的是改善多视图匹配性，而非独立的亮度提升模块。

2. **前馈式高斯泼溅网络**：  
   - 从适应后的上下文视图提取2D特征，通过代价体积（cost-volume）进行多视图交互聚合，得到支持深度和外观推理的多视图特征。  
   - 在每个像素位置预测深度和密度相关量，反投影到3D形成高斯中心 μ(u,d) = o + d·r(u; K, T)。协方差 Σ 通过预测的尺度和旋转参数化保证正定性，不透明度 α 通过非线性激活限幅在 (0,1)。  
   - 每个高斯携带球谐系数 h 表示视角依赖颜色。  
   - 训练损失：像素损失（L2或Charbonnier损失） + 感知损失（LPIPS）。

## 3. 实验设计

### 使用的数据集/场景
- 基于大型位姿视图数据集**RealEstate10K**（文中引用[11] Stereo Magnification）。  
- 构建**可控多视图低光基准**：仅对上下文视图施加一系列概率性低光变换，目标视图保持清洁。变换包括：伽马暗化、曝光缩放、RGB通道偏移、高斯模糊。每个变换的概率和能力可调，保证可解释性和可复现。

### 对比方法
1. **Lowlight MVSplat**：直接将低光上下文视图输入MVSplat，无任何增强。  
2. **DarkIR → MVSplat**：先用DarkIR（一种低光图像恢复模型）增强每张低光视图，再输入MVSplat重建。  
3. **DelowlightSplat（本文方法）**。

### 评估指标
- PSNR、SSIM、LPIPS。

### 实验充分性
- 定性与定量比较（图2、表I），显示本文方法在PSNR（23.08 vs 基线8.49和17.11）、SSIM（0.833 vs 0.372和0.723）及LPIPS（0.162 vs 0.355和0.225）上均大幅领先。  
- 消融实验（表II、图3）：逐步叠加各低光退化（清洁→+伽马→+曝光→+RGB偏移→+模糊），观察性能变化，表明每个因素都损害重建，而本文适配器能有效缓解。  
- 训练时采用课程学习，逐步增加退化概率p，稳定优化。  
- 实验覆盖了合成低光场景的多种退化类型，但缺少真实低光数据的验证。

## 4. 资源与算力

- **GPU型号**：NVIDIA RTX 4090 × 1  
- **Batch size**：8  
- **训练步数**：10,000 steps  
- 文中未明确给出训练总时间，但基于单卡和1万步估算，应可在数小时内完成。

## 5. 实验数量与充分性

- 共有两组主要对比实验（定性+定量），一组消融实验（5种退化水平）。  
- 实验设计**相对简洁但针对性明确**：重点证明了低光适配器和端到端学习相较于直接输入和两阶段流程的优越性。消融实验展示了各退化因素的影响及方法的鲁棒性。  
- **局限性**：实验仅在合成退化数据上进行，缺乏真实低光场景的评估；未与更多低光3D重建方法（如NeRF in the Dark或其他3DGS变体）对比；仅使用两视图输入，未扩展到多视图或时间序列。  
- 整体上，实验**足以支撑论文主要结论，但充分性有待加强**（例如缺少在真实低光数据集上的泛化测试）。

## 6. 论文的主要结论与发现

- DelowlightSplat在合成低光基准上显著优于直接重建和先增强后重建的基线，PSNR提升超过14dB，SSIM提高0.46，LPIPS降低0.19。  
- 低光适配器能够改善多视图特征匹配性，而端到端训练避免了独立增强带来的跨视图不一致问题。  
- 可控基准表明，伽马暗化、曝光缩放、颜色偏移和模糊均会严重损害前馈式3DGS重建，而本文方法能有效缓解这些退化。

## 7. 优点

- **创新性**：首次将低光感知适配器集成到前馈式3DGS框架中，直接预测清洁场景，避免了先增强后重建的缺点。  
- **可控基准构建**：设计了可调节退化强度的多视图低光基准，方便系统评估和消融分析。  
- **轻量高效**：适配器仅由少量卷积残差块组成，计算开销小，可与主干端到端训练。  
- **跨视图一致性**：通过联合优化适配器和重建网络，输出视角一致的清洁渲染结果。  
- **实用价值**：为退化环境下的实时3D重建（如机器人、AR/VR）提供了可行方案。

## 8. 不足与局限

- **实验覆盖不全**：仅在合成退化数据上验证，未在真实低光场景（如夜间街景、室内弱光）中进行测试，泛化性存疑。  
- **仅支持两视图输入**：许多实际应用需要更多视角（如视频序列），扩展至多视图和长期时间上下文是关键挑战。  
- **合成退化的逼真度有限**：简单的伽马、曝光、偏移和模糊组合可能不足以模拟真实传感器噪声（如空间变异噪声、运动模糊、量化误差）。  
- **与最新技术对比不足**：未与针对低光的NeRF变体（如NeRF in the Dark）或其他3DGS低光方法（如Low-Light 3DGS）比较。  
- **评估偏向**：仅报告了定量指标，缺少用户研究或感知质量评价。  
- **计算资源**：单卡1万步训练可能未充分收敛，且未分析推理速度。

（完）
