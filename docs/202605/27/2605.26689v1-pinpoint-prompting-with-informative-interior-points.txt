Title: PinPoint: Prompting with Informative Interior Points

URL Source: https://arxiv.org/pdf/2605.26689v1

Published Time: Wed, 27 May 2026 01:08:05 GMT

Number of Pages: 21

Markdown Content:
# PinPoint: Training-Free Referring Segmentation via Prompt Disambiguation 

Pouya Sadeghi 1,2 Shawn He 3 Pedro Pablo Guerrero Vela 3 C. Thomas 3

Alex Wong 3 Sirisha Rambhatla 1,2 

> 1

University of Waterloo 2Critical ML, University of Waterloo 3Apple 

{pouya.sadeghi,sirisha.rambhatla}@uwaterloo.ca 

## Abstract 

Modern referring image segmentation pipelines couple a vision-language model (VLM) for grounding with a promptable segmenter such as the Segment Anything Model (SAM) for mask generation. Prior training-free instances of this recipe consistently trail fine-tuned and reinforcement-learning (RL)-tuned specialists, and it has been unclear whether the gap comes from the VLM’s grounding, SAM’s capacity, or the prompt. We show that the gap is dominated by prompt ambiguity :a VLM-proposed bounding box (bbox) leaves SAM to guess which pixels inside the bbox belong to the object the expression denotes. Interior points are the natural disambiguator, but where they fall matters; prior work relies on naively sampled points that land on boundaries, distractors, and background clutter, and can even hurt performance compared to the bbox alone. Supervised and RL-tuned methods close this gap by training a VLM to predict better points; we show that this training is unnecessary. At a matched budget of five interior points, replacing naive sampling with stable, informative point selection improves cumulative Intersection-over-Union (cIoU) by 12–18 points across RefCOCO/+/g, with every model fixed. We turn this observation into PinPoint , a deterministic, training-free point selector that fuses four visual cues into a consensus map, selects compact, spatially diverse points away from boundaries, and uses the frozen VLM to label each point. Without any task-specific training, PinPoint matches supervised and RL-tuned specialists on the same stack while issuing only two VLM calls per query. 

## 1 Introduction 

Referring image segmentation generates a pixel-level binary mask of the object that a free-form natural-language expression denotes [Kazemzadeh et al., 2014, Yu et al., 2016, Mao et al., 2016]. Unlike traditional semantic or instance segmentation, which depend on a predefined label set [He et al., 2017, Cheng et al., 2022, Ding et al., 2025], referring segmentation must handle compositional, attribute-rich expressions such as “the partially hidden zebra” or “the white pot in the upper right corner” . This setting better reflects how humans describe objects in real-world use cases such as instruction-following agents [Hong et al., 2024] and image-editing tools [Brooks et al., 2023, Yu et al., 2023], while imposing higher demands on the model: nuanced grounding of language to image regions, fine-grained visual perception, and precise pixel-level mask generation. Modern systems for this task have converged on a modular recipe that pairs two pretrained foundation models: a vision-language model (VLM) [Liu et al., 2023b, Bai et al., 2025b,a] for semantic grounding, and a promptable segmenter such as the Segment Anything Model (SAM) [Kirillov et al., 2023, Ravi et al., 2024] for pixel-level mask generation [Lai et al., 2024, Chen et al., 2024, Xia et al., 2024, Liu et al., 2025b, Huang et al., 2026, Du et al., 2026].          

> Preprint.
> arXiv:2605.26689v1 [cs.CV] 26 May 2026 ZZZZ
> Ground truth
> “white pot upper right corner”
> SAM4MLLM †PinPoint (ours)
> “The fully visible zebra” “black cat” “entire flower in vase”
> Overlaid Mask Bounding box Positive point Negative point
> Stable interior
> points on target
> Near -boundary
> points are unstable
> Points isolate
> target zebra
> Points leak to
> distractor
> Error region
> Points fall on
> background
> Points isolate
> the target
> Point falls on
> shadow
> Points isolate
> the target
> IoU
> 95.2
> IoU
> 58.9
> IoU
> 91.7
> IoU
> 95.7
> IoU
> 71.9
> IoU
> 86.1
> IoU
> 90.1
> IoU
> 48.7
> ↑ 23.8 ↑ 5.6 ↑ 5.1 ↑ 10.2
> GT annotation
> is imperfect
> Q

Figure 1: Prompt disambiguation drives the training-free referring segmentation gap. For each query (columns), we show the ground truth (top), matched-budget SAM4MLLM † (middle), and 

PinPoint (bottom). All methods use the same frozen VLM, point budget ( N =5 ), and single SAM forward pass; only the point selector changes. IoU is per-example mask overlap (higher is better). †

denotes our training-free reproduction of SAM4MLLM’s PQPP setting: uniformly sampled interior points (naive sampling) labeled by the frozen VLM. This recipe is appealing because it reuses two strong off-the-shelf foundation models. However, the interface between them is fragile: prior training-free instances consistently remain behind their fine-tuned and RL-tuned counterparts on benchmarks by a noticeable margin and it has not been clear whether the gap lies in the VLM’s grounding, in SAM’s capacity, or in the prompt that connects them. We argue that the bottleneck is the prompt itself, a phenomenon we call prompt ambiguity : a VLM-predicted bounding box is an under-specified region that contains the target alongside partial occluders, distractors, boundaries, and background clutter [Liu et al., 2023a]. This leaves SAM to guess which pixels inside it belong to the referent. SAM, in turn, is the least stable near object boundaries [Zhang et al., 2025, Fan et al., 2025, Wang et al., 2025a], and its mask predictions degrade when prompted with redundant or off-target points [Fan et al., 2025, Dai et al., 2023]; this makes the choice of interior points a critical question. Figure 1 makes this visible: at a matched point budget, naïve interior sampling places points on boundaries and nearby distractors, while PinPoint selects stable points that isolate the target. Existing work closes this gap through additional training, falling into three broad categories. The first adds segmentation-aware tokens to the VLM so that it can emit mask-shaped outputs [Lai et al., 2024, Ren et al., 2024, Yan et al., 2024, Xia et al., 2024, Pi et al., 2024, Yuan et al., 2025, Liu et al., 2025a]; auxiliary threads layer chain-of-thought, decomposition, or mask-feedback loops on top [Kao et al., 2025b, Wang and Zhang, 2025, Wang et al., 2024, Kao et al., 2025a]. The second fine-tunes the VLM to produce SAM-compatible point prompts directly [Chen et al., 2024]. The third replaces supervision with reinforcement learning, post-training a VLM with GRPO on a few thousand curated samples to emit better single-shot points or refine them iteratively [Liu et al., 2025b, Huang et al., 2026, Du et al., 2026, Zhu et al., 2026]. Across all three families, the recurring object of optimization is the same: produce a better spatial prompt for SAM. A simpler question has not been answered yet: do these informative points actually require learning, or can they be proposed without any training? We argue that the points do not need to be generated by a trained policy; they can be selected from image-side evidence and verified by the frozen VLM. Classical visual cues (saliency, local edges, entropy, and a spatial prior) already encode where reliable evidence sits inside a coarse box, and a frozen VLM is a competent per-point verifier. Prompt disambiguation, in this view, is not a learning problem but a deterministic, image-side computation. We turn this idea into PinPoint (Prompting with Informative Interior Points ), a deterministic, training-free point selector that converts a coarse VLM bounding box into a clarifying composite prompt: 2the bounding box plus a small set of semantically labeled interior points. Inside each predicted box, PinPoint fuses visual cues into a consensus map, extracts a compact and spatially diverse interior point set, and reuses the frozen VLM to label each point as positive or negative; SAM is then run once with the resulting prompt, and the entire pipeline costs only two VLM calls per query. At a matched point budget across RefCOCO/+/g, swapping naive sampling for PinPoint improves cIoU by 12–18 points with every model frozen (Table 1); on the same stack used by recent SFT and RL specialists, PinPoint is competitive with all of them without a single task-specific gradient update (Section 4). Together, these results identify prompt ambiguity as the dominant remaining bottleneck in modular VLM+SAM referring segmentation, and show that the post-training step recent methods perform to learn point selection is replaceable by a principled image-side computation. 

## 2 Related Work 

Supervised fine-tuning. A large body of work equips VLMs with segmentation-specific output interfaces, typically by adding mask-producing heads or segmentation tokens that decode into pixel masks [Lai et al., 2024, Ren et al., 2024, Yan et al., 2024, Xia et al., 2024, Pi et al., 2024, Yuan et al., 2025, Liu et al., 2025a], with reasoning and feedback-loop variants layering chain-of-thought, decomposition, or mask-feedback on top [Kao et al., 2025b, Wang and Zhang, 2025, Wang et al., 2024, Kao et al., 2025a]. These methods change the output head of the VLM, not the prompt fed to SAM. The closest training-based work to ours is Chen et al. [2024]: it uniformly samples candidate interior points, and supervises the VLM to ground the query and label each extracted point. Their results show that VLM-as-labeler outperforms VLM-as-generator , which suggests select-then-label 

as the regime worth pursuing. PinPoint reuses select-then-label without the supervised tuning step .

Reinforcement learning for prompt policies. A more recent line keeps the frozen-segmentor design and swaps supervision for reinforcement learning, shaping the VLM’s prompt with rewards derived from the box or mask. Single-shot variants train the VLM to emit better bounding boxes and points in one pass [Liu et al., 2025b, Zhu et al., 2026], with Huang et al. [2026] deriving the reward directly from the resulting SAM mask. Du et al. [2026] go further, casting the VLM as an iterative agent that proposes refinement points and learns when to terminate the refinement loop. The recurring object of optimization across all variants is a learned policy in the language model that emits or refines the spatial prompt. PinPoint removes the learned policy entirely by selecting stable and informative points off the image with a deterministic procedure and reusing the frozen VLM to label them. 

Training-free prompt disambiguation. Liu et al. [2023a] first frames the bounding box as an ambiguous spatial signal, and a growing empirical body characterizes when SAM’s mask becomes unstable: it is least reliable near object boundaries, degrades under redundant or off-target prompts, and benefits from explicit uncertainty modeling [Zhang et al., 2025, Fan et al., 2025, Wang et al., 2025a]. Existing responses to this evidence either operate on the input side without selecting better points to begin with [Dai et al., 2023], or sidestep promptable segmentation altogether by refining CLIP Grad-CAM heatmaps outside the VLM+SAM regime [Wang et al., 2025b]. Prior training-free work thus diagnoses prompt sensitivity or escapes it by changing the regime; PinPoint acts on the same evidence from inside the select-then-label pipeline , with a deterministic, image-side selector replacing learned point selection and the frozen VLM to verify each candidate. 

## 3 Method 

PinPoint disambiguates the prompt passed to SAM before mask generation. Each VLM-predicted bounding box is converted into a composite prompt of bounding box (bbox) + a small set of semantically labeled interior points ; with all backbones frozen and the point budget matched, the choice of these interior points has a large effect on cIoU (Section 4). 

3.1 Problem setup 

We are given an image I ∈ RH×W ×3 and a natural-language query Q that may specify zero, one, or multiple visible target instances. The goal is a binary mask M ∈ { 0, 1}H×W whose positive pixels correspond to the object(s) denoted by Q (or the all-zero mask when there is no visible referent). We construct M through a five-stage pipeline (Figure 2): 3H p                                             

> Local Entropy
> Sp
> Color Saliency
> Ep
> Edge Density
> Gp
> Gaussian Prior
> Cropped image (input) Cue maps Consensus map Diverse selection
> Selected points 𝒫 !
> Cp
> Cp=Sp1−Ep1−HpGp
> S𝑝
> 1−𝐻 𝑝
> 1−𝐸 𝑝
> G𝑝
> PinPoint
> Input
> Q:“The fully visible zebra”
> 𝐼
> Segmentation
> Segmentation mask 𝑀
> Frozen
> VLM
> Point Filtering
> 𝒫 !
> ∗={p!:c!≥τ,ℓ!∈A}
> 𝒫 !
> ∗,l!
> ∗
> Localization
> Bounding box b"
> b!
> Point Selection
> Interior points 𝒫 "
> Frozen
> VLM
> Point Labeling
> Positive Negative
> 𝑙 #,𝑐 #
> Frozen
> SAM
> Output

Figure 2: Training-free referring segmentation with PinPoint. A frozen VLM localizes the referent with candidate bboxes; PinPoint selects informative interior points inside each bbox; the same frozen VLM labels each point as positive or negative; confidence filtering removes unreliable labels; SAM produces the final mask in a single forward pass. The lower inset shows the PinPoint selector: cue maps computed on a crop, fused into a consensus map, and converted into a diverse interior point set. 

(I, Q ) Localize 

−−−−→ B PinPoint 

−−−−→ { (bk, Pk)} Label 

−−−→ { (bk, Pk, ℓk, ck)} Filter 

−−−→ { (bk, P⋆k , ℓ⋆k)} SAM 

−−−→ M. (1) 

Localization turns (I, Q ) into candidate bboxes B = {b1, . . . , b K }. PinPoint extracts a small set of interior points Pk inside each bk. Point labeling attaches a label ℓk,i ∈ { positive , negative }

and confidence ck,i ∈ [0 , 1] to every pk,i ∈ P k via the same VLM; we collect these into the per-bbox tuples ℓk = ( ℓk,i )pk,i ∈P k and ck = ( ck,i )pk,i ∈P k . Filtering keeps the subset P⋆k ⊆ P k that passes a confidence threshold and a label allowlist, with ℓ⋆k the corresponding labels restricted to 

P⋆k . Segmentation hands (bk, P⋆k , ℓ⋆k) to SAM in a single forward pass, and the per-bbox masks are combined into M .

3.2 Localization 

The first stage uses the frozen VLM as a grounded proposer that turns (I, Q ) into a set of bboxes 

B = {b1, . . . , b K } in pixel coordinates, where K may be 0 (no detected referent), 1, or larger. We use grammar-guided decoding [Kwon et al., 2023] to constrain the VLM’s output to valid JSON to avoid fragile text post-processing; the prompt and EBNF grammar are in Appendix C. We only require this stage to provide coarse spatial support; a predicted bbox may include background clutter or overlap nearby instances, and therefore remain an under-specified prompt for SAM. 

3.3 Point selection via PinPoint 

PinPoint is applied inside each predicted bbox to convert this coarse signal into an informative prompt. A useful point prompt should identify stable visual evidence inside the localized region while avoiding regions where SAM and the VLM verifier are unreliable; SAM is least stable near object boundaries [Zhang et al., 2025] and its mask predictions degrade when prompted with redundant or off-target points [Fan et al., 2025, Dai et al., 2023]. Given a bbox b and a budget N , PinPoint computes four pixel-wise cues over an extended crop, fuses them into a consensus score, and greedily selects up to N spatially diverse points inside b.

Cues. We score each pixel using four complementary cues (full formulas in Appendix A): color-contrast saliency S, the ℓ2 distance between mean CIELAB colors in a local disk and an annulus around p [Itti et al., 1998]; edge density E, a box-averaged Sobel gradient magnitude flagging boundary-adjacent pixels; local Shannon entropy H over quantized gradient magnitudes, identifying textural clutter; and a Gaussian spatial prior G centered at the bbox centroid with bandwidth proportional to the bbox diagonal. Cues are computed over an extended crop bext obtained by extending b by a factor α. Each raw map is rescaled to [0 , 1] via a percentile-clipped min–max 4normalizer N (·) (Eq. 12 in the appendix); we reuse S, E, H, G for the normalized cues. The cues are classical signals from the saliency literature [Harel et al., 2006, Hou and Zhang, 2007, Judd et al., 2009]; PinPoint’s contribution is how they are composed: inside each VLM-proposed bbox, coupled to a downstream verifier, with a budget tied to cue agreement through Soft-NMS [Bodla et al., 2017] so the selector can return fewer than N points when reliable evidence is sparse. 

Fusion. We combine the normalized cues into a consensus score map C using one of two fusion modes. The multiplicative consensus variant requires all cues to agree: 

Cmult = S × (1 − E) × (1 − H) × G, (2) so that a pixel scores high only if it is visually distinctive, away from strong boundaries, locally homogeneous, and spatially plausible; a weak value in any cue suppresses the score, making this variant conservative in cluttered scenes. The max-saliency-smooth variant keeps saliency as the dominant signal and uses edge density and entropy as smoothness guards: 

Cmsm = S × max   1 − λE E − λH H, 0  × G, (3) where λE , λ H ≥ 0 control the strength of the edge and entropy penalties; textured but salient interior regions can still receive high scores while pixels with strong boundary evidence or heavy clutter are zeroed out by the max( ·, 0) rectifier. Both variants share the factored form C = ( visual evidence ) ×

G but differ in how strongly the smoothness cues gate the saliency signal: Cmult enforces strict conjunction, while Cmsm shows that the strict-consensus constraint can be loosened without loss. 

Candidate extraction and diversity selection. We extract local maxima of C as the candidate pool eP ⊂ interior( b) with initial scores C(p) (cue maps live on the extended crop, but candidates are restricted to the original bbox), and grow a selected set P greedily from eP. To avoid redundant prompts, the next point is chosen by a Soft-NMS-style [Bodla et al., 2017] virtual score eCP (p) that multiplies C(p) by a Gaussian penalty in distance (with bandwidth σnms ) to the nearest point already in P (Eq. 13 in Appendix A). The first selection reduces to arg max p C(p); after each selection, virtual scores are updated, so nearby candidates are suppressed while distant high-scoring candidates remain competitive. The procedure stops in three cases: N points have been selected, no candidates remain, or every remaining virtual score has fallen below the floor ε. PinPoint can therefore return fewer than N points rather than padding with weak or redundant ones. PinPoint adds no VLM calls beyond localization and labeling, and its CPU cost is dominated by the surrounding VLM and SAM forward passes (Section 4.3). 

3.4 Point labeling 

PinPoint’s points are geometrically interior but carry no semantic understanding of the target. We use the same frozen VLM as a per-point semantic verifier : for each candidate pk,i ∈ P k, the VLM receives (I, Q, b k, p k,i ) and returns a label and a verbalized confidence, 

(ℓk,i , c k,i ) = gver 

 I, Q, b k, p k,i 

, ℓk,i ∈ { positive , negative }, c k,i ∈ [0 , 1] , (4) where ℓk,i = positive iff pk,i lies on the target described by Q; a point on a nearby distractor is therefore negative even when it lies on a foreground object. In implementation we batch all selected points from an image into a single grammar-guided generation call, reducing labeling to one VLM call per image regardless of N ; for K> 1 predicted bboxes we issue one prompt per bbox and dispatch them as a single vLLM batch, so per-image latency stays bounded by one VLM round-trip independent of K (full prompt and EBNF grammar in Appendix D). Verification helps when a point falls on a boundary, background, or distractor; it can also misfire on target-interior points, and the reported confidence is a noisy self-report rather than a calibrated probability [Groot and Valdenegro-Toro, 2024, Zhang et al., 2024], so labeling complements rather than replaces the proactive boundary avoidance built into PinPoint (quantified in Section 4.3). 

3.5 Filtering and segmentation Point filtering. Before prompting SAM we filter the labeled points using a confidence threshold τ

and a label allowlist A ⊆ { positive , negative }:

P⋆k = { pk,i ∈ P k : ck,i ≥ τ ∧ ℓk,i ∈ A } , ℓ⋆k = ( ℓk,i )pk,i ∈P ⋆k . (5) 5Algorithm 1 PinPoint point selection inside a single bounding box. Defaults for all hyperparameters are listed in Appendix B.  

> 1:

Input: image I, bounding box b, budget N , fusion mode m ∈ { mult , msm } 

> 2:

Output: selected points P ⊂ interior( b) with |P| ≤ N 

> 3:

bext ← Extend( b, α ) 

> 4:

for each cue X ∈ { S, E, H, G } do  

> 5:

compute raw cue map Xraw over bext ▷ Eqs. 8–11  

> 6:

X ← N (Xraw ) ▷ Eq. 12  

> 7:

end for  

> 8:

C ← Fuse( S, E, H, G, m ) ▷ Eq. 2 or Eq. 3  

> 9:

eP ← LocalMaxima( C) ∩ interior( b) 

> 10:

P ← ∅  

> 11:

while |P| < N and eP \ P̸ = ∅ do  

> 12:

compute eCP (p) for all p ∈ eP \ P ▷ Eq. 13; eC∅(p) = C(p) 

> 13:

if max p∈ eP\P eCP (p) ≤ ε then  

> 14:

break  

> 15:

end if  

> 16:

p⋆ ← arg max p∈ eP\P eCP (p) 

> 17:

P ← P ∪ { p⋆} 

> 18:

end while  

> 19:

return PA = {positive } gives a positive-only prompt; A = {positive , negative } passes both target-supporting and target-suppressing points; τ =0 disables confidence filtering. Unless otherwise stated we use τ =0 .7 and A={positive , negative }.

SAM prompting and mask merging. For each retained bbox we run SAM once, 

Mk = SAM  I, b k, P⋆k , ℓ⋆k

, Mk ∈ { 0, 1}H×W , (6) and combine per-bbox masks pixelwise: 

M = WKk=1 Mk. (7) When the VLM returns no bboxes ( K=0 ), we set M = 0H×W , which contributes zero intersection and full ground-truth area to the cIoU denominator. 

## 4 Experiments 

4.1 Experimental setup Implementation details. Unless otherwise stated, experiments use Qwen2.5-VL-7B [Bai et al., 2025b] (served via vLLM [Kwon et al., 2023]) as the frozen VLM and SAM 2-L [Ravi et al., 2024] as the frozen segmenter 1, and report both PinPoint fusion modes: PinPoint mult (multiplicative consensus, Eq. 2) and PinPoint msm (max-saliency-smooth, Eq. 3). Both VLM stages use grammar-guided structured decoding with T emperature =0 .1 and a fixed seed, so PinPoint’s end-to-end output is stable in practice; the matched-budget naive baseline (Table 1) involves stochastic sampling and is averaged over three seeds. All inference uses a single NVIDIA A6000 GPU at engine batch size 1.Verbatim prompts and EBNF grammars for the localization and labeling stages are reproduced in Appendices C–D; full hyperparameters are in Appendix B. 

Datasets and Evaluation metrics. We evaluate on both referring and reasoning segmentation benchmarks. For referring segmentation, we use three standard benchmarks: RefCOCO , Ref-COCO+ [Kazemzadeh et al., 2014, Yu et al., 2016], and RefCOCOg [Mao et al., 2016]. RefCO-CO/+/g are built from MSCOCO images with natural-language referring expressions; RefCOCO+ 

> 1We refer to SAM 2-L simply as “SAM” in prose throughout for readability.

6Table 1: Informative points close the prompt-ambiguity gap. Training-free cIoU on RefCOCO/+/g (higher is better); best and second-best per column. All non-Oracle rows use the same frozen VLM boxes, VLM point labels, point budget ( N =5 ), and single SAM forward pass; only the point selector changes. SAM4MLLM † denotes our training-free reproduction of SAM4MLLM’s PQPP setting, which uses uniformly sampled interior points. The ∆ rows therefore measure the effect of point selection alone, showing that replacing naive points with PinPoint points yields consistent gains.                                                               

> Method (cIoU ↑)RefCOCO RefCOCO+ RefCOCOg
> val testA testB val testA testB val(U) test(U) Bounding box only 69.2 72.0 63.9 60.7 68.7 56.9 65.1 67.4 SAM4MLLM †61.9 65.5 57.6 53.2 59.6 46.9 55.9 57.1 PIN POINT mult 75.9 81.7 71.1 71.4 76.9 62.5 72.3 69.0 PIN POINT msm 75.4 81.9 72.4 71.3 77.1 62.8 73.6 71.2
> ∆mult vs. SAM4MLLM †+14.0 +16.2 +13.5 +18.2 +17.3 +15.6 +16.4 +11.9
> ∆msm vs. SAM4MLLM †+13.5 +16.4 +14.8 +18.1 +17.5 +15.9 +17.7 +14.1

excludes spatial-relation descriptions, while RefCOCOg contains longer and more complex expres-sions. For reasoning segmentation, we evaluate on ReasonSeg [Lai et al., 2024], where the model must segment the target specified by an implicit or reasoning-intensive instruction rather than an explicit referring expression. Following common practice, we report cumulative Intersection-over-Union (cIoU) on all benchmarks, and additionally report the per-sample averaged IoU (gIoU) on ReasonSeg 2:

cIoU = 

PNi=1 |Pi ∩ Gi|

PNi=1 |Pi ∪ Gi| , gIoU = 1

N

> N

X

> i=1

|Pi ∩ Gi||Pi ∪ Gi| .

Here, Pi is the predicted binary mask, Gi is the ground-truth mask, and N is the number of samples. 

4.2 Comparisons 

We compare PinPoint with both training-free and training-required methods, with results summa-rized in Tables 1 and 2. Table 1 isolates the contribution of prompt quality against directly-related training-free baselines, and Table 2 situates PinPoint within the broader literature dominated by training-required methods. Among training-free methods (Table 1), both PinPoint variants substan-tially outperform naive point sampling at the same budget, with the dedicated ∆ rows showing improvements of +12 to +18 cIoU across all eight RefCOCO/+/g splits. The only thing that differs between the SAM4MLLM † row and the PinPoint rows is which interior points are fed to SAM; the backbones, the bounding boxes, the budget, and the rest of the pipeline are identical, so the 

∆ rows cleanly isolate the contribution of prompt quality. Compared with training-required meth-ods (Table 2), PinPoint is on par with fine-tuned and RL-tuned baselines without any task-specific training: it matches or exceeds fine-tuned baselines like LISA-7B and PixelLM-7B across most splits, and on RefCOCO testA reaches 81.9 cIoU, edging past all listed baselines including the SFT entry SAM4MLLM and the reinforcement-learning entries Seg-Zero and SAM-R1. The RL comparison is the cleanest one available, since Seg-Zero, SAM-R1, and SAM-Veteran use the same Qwen2.5-VL-7B + frozen SAM2 stack as PinPoint plus a few thousand GRPO training samples; PinPoint matches or beats Seg-Zero and SAM-R1 on both testA splits with no task-specific updates whatsoever, indicating that RL post-training to learn point selection is unnecessary on this stack. On the ReasonSeg test, PinPoint reaches 61 .7/55 .4 (gIoU/cIoU), surpassing Seg-Zero ( 57 .5/52 .0) and SAM-R1 ( 60 .2/54 .3) and trailing the iterative agent SAM-Veteran ( 62 .6/56 .1) by under one cIoU point, again training-free. 

> 2We follow the LISA/ReasonSeg convention in calling the per-sample averaged IoU “gIoU”; the same quantity is sometimes denoted mIoU elsewhere in the segmentation literature.

7Table 2: Comparison against training-required methods. cIoU on RefCOCO/+/g and gIoU/cIoU on ReasonSeg test, grouped by training regime. The RL block shares our Qwen2.5-VL-7B + SAM 2-L stack and is the closest direct comparison. Empirical Upper Bound : cIoU with both VLM stages replaced by oracles. Best and second-best per column (excluding Oracle); baseline numbers are from the original publications, matched to our stack where reported or to the closest 7B-scale variant otherwise; absent cells (—) were not reported in the original paper.                                                                                                                              

> Method RefCOCO RefCOCO+ RefCOCOg ReasonSeg test
> val testA testB val testA testB val(U) test(U) gIoU cIoU
> Oracle reference Empirical Upper Bound 91.2 91.2 92.0 91.2 91.2 92.1 90.9 90.1 90.4 90.6 Supervised Fine-tuning
> LISA-7B [Lai et al., 2024] 74.9 79.1 72.3 65.1 70.8 58.1 67.9 70.6 48.7 48.8 PixelLM-7B [Ren et al., 2024] 73.0 76.5 68.2 66.3 71.7 58.3 69.3 70.5 ——VISA-7B [Yan et al., 2024] 72.4 75.5 68.1 59.8 64.8 53.1 65.5 66.4 ——PerceptionGPT-7B [Pi et al., 2024] —78.6 ——73.9 ——71.7 ——SAM4MLLM [Chen et al., 2024] 77.1 80.9 72.5 71.5 76.8 64.7 74.5 75.2 ——
> Reinforcement-Learning Tuning
> Seg-Zero-7B [Liu et al., 2025b] —80.3 ——76.2 ——72.6 57.5 52.0 SAM-R1-7B [Huang et al., 2026] —79.2 ——74.7 ——73.1 60.2 54.3 SAM-Veteran-7B [Du et al., 2026] —80.8 ——76.6 ——73.4 62.6 56.1
> Training-Free
> PinPoint mult (ours) 75.9 81.7 71.1 71.4 76.9 62.5 72.3 69.0 61.7 55.4 PinPoint msm (ours) 75.4 81.9 72.4 71.3 77.1 62.8 73.6 71.2 60.9 54.9

4.3 Ablation study 

All ablations run on RefCOCO val with Qwen2.5-VL-7B + SAM 2. The detailed end-to-end ablation table is reported in Appendix E; rows differ only in the point selector and the verifier configuration, with every other stage held fixed. Below, we summarize our key findings. 

Hyperparameter sensitivity. We sweep the three scalar hyperparameter classes whose effect on cIoU is non-trivial: the Gaussian-prior bandwidth σG, the edge/entropy penalty weights (λE , λ H )

in Cmsm , and the percentile-clip endpoints (q5, q 95 ) (Appendix B). PinPoint is robust to moderate perturbations of any single knob; across every grid point, the cIoU on RefCOCO val and testB stays close to the default configuration, with (λE , λ H ) the only axis where the choice meaningfully matters. The default (λE , λ H )=(0 .5, 1.5) achieves the highest mean cIoU across val and testB ( 73 .9, vs. 73 .2

for the runner-up (1 .5, 0.5) ); it wins val by 1.2 cIoU and trails the testB-best by only 0.5, where the top three testB settings cluster within 0.5 cIoU of each other. Holding λE =0 .5 and increasing 

λH from 0.5 to 1.5 improves val by +2 .4 and testB by +1 .6, suggesting λH has a slightly stronger marginal effect than λE inside Cmsm , though the global landscape is non-monotonic and the two splits do not pick the same operating point. 

Effect of filtering. Restricting A to a single label class is brittle: positive-only at τ =0 .7 gains +0 .4

cIoU on val but loses 1.5 on testB, and at τ =0 .8 the optimum flips again (positive-only wins on val, negative-only on testB), so the best label class is split-dependent. The threshold τ is more robust but bounded: verbalized confidences below ∼0.7 are systematically mis-labeled and benefit from removal, while above that cutoff confidence and correctness become uncorrelated, so pushing τ higher discards correct labels alongside wrong ones, and once the kept-prompt count drops the box-only fallback rate dominates ( −6.7 cIoU; Table 5). At τ =0 .9 the both-labels cIoU sits −0.8 below default on both splits despite a ∼ 45% discard rate. We therefore keep τ =0 .7, A={positive , negative }

as the most stable operating point; full grids in Appendix E. 

Inference efficiency. Under our default configuration, a single query costs two VLM calls , one for localization and one batched for point-labeling, independent of the point budget N , the number of proposed boxes K, and the choice of fusion mode . This matches the inference-time call budget of RL-trained single-shot methods such as Seg-Zero [Liu et al., 2025b] and SAM-R1 [Huang et al., 8Q: R Sppooon Q: Yea h            

> Q: UpQ: Yes (a) Noisy or corrupted queries. Q: Keyboar dQ: Round base
> Q: Vase Q: Bottle (b) Multiple valid targets.

Figure 3: Dataset noise and ambiguity limit the performance. Even when a system selects a visually plausible region, evaluation can penalize it because the benchmark provides only one annotated target. (a) Some queries are corrupted, unnatural, or under-specified, so the intended referent is not recoverable from language alone. (b) Other queries are valid but ambiguous: multiple objects satisfy the expression, while only one is labeled as ground truth. Green boxes denote the benchmark target; red dashed boxes denote the predicted or plausible alternative target. 2026], and is strictly cheaper than decomposition pipelines [Wang and Zhang, 2025] or loop-based pipelines [Wang et al., 2024, Kao et al., 2025a] whose call counts scale with expression length or refinement rounds. On RefCOCO val (single A6000) the two VLM calls together account for ∼95% 

of per-query wall-clock and PinPoint’s geometry-aware selector consumes only ∼1% ; the per-stage breakdown and a detailed call-count comparison are in Appendix F. 

Benchmark noise and the oracle gap. The empirical oracle reference in Table 2 replaces VLM localization and point labeling with ground-truth-derived inputs while keeping PinPoint and SAM unchanged. The remaining gap to 100 cIoU therefore should not be interpreted as a VLM-grounding error; rather, it reflects the limits of SAM mask generation and the benchmark annotation protocol. PinPoint’s gap to this oracle reference primarily reflects the portion still attributable to imperfect localization and point verification. Figure 3 illustrates one residual source: some queries are corrupted, unnatural, or under-specified, making the intended referent ambiguous (Fig. 3a), while others are valid but admit multiple plausible targets even though the benchmark provides only one (Fig. 3b). 

## 5 Conclusion 

We argued that, in training-free VLM+SAM referring segmentation, the remaining gap to fine-tuned and RL-tuned specialists comes from prompt ambiguity rather than from either backbone’s capacity. Augmenting the VLM-predicted bounding box with semantically labeled points is a natural remedy. Still, the choice of points is decisive: poorly placed ones drift onto boundaries and distractors, and can degrade performance. The question that motivated this paper was whether selecting informative points requires learning at all. Our answer is PinPoint: a deterministic method that fuses four classical cues into a consensus map, selects compact and spatially diverse candidates, and uses a frozen VLM as a per-point verifier. Because every component is frozen, PinPoint inherits their failure modes: the cue fusion biases against off-center, camouflaged, or heavily textured referents; and the VLM verifier remains systematically overconfident. Calibrating that confidence is a promising direction within the same pipeline. Despite these limitations, PinPoint closes 12–18 cIoU over prior training-free methods at a matched budget on RefCOCO/+/g, and on the same stack as recent SFT and RL methods, it is competitive with them across RefCOCO/+/g and ReasonSeg at only two VLM calls per query. Learned point selection is therefore replaceable by image-side computation paired with semantic verification. 9Acknowledgments and Disclosure of Funding 

The authors would like to acknowledge the support from Prof. Sirisha Rambhatla’s Natural Sciences and Engineering Research Council of Canada (NSERC) Alliance Grant ALLRP 587270-2023. 

## References 

Shuai Bai, Yuxuan Cai, Keqin Chen, Junyang Lin, Bowen Yu, Jingren Zhou, et al. Qwen3-VL technical report, 2025a. URL https://arxiv.org/abs/2511.21631 .Shuai Bai, Keqin Chen, Xuejing Liu, Jialin Wang, Wenbin Ge, Sibo Song, Kai Dang, Peng Wang, Shijie Wang, Jun Tang, Humen Zhong, Yuanzhi Zhu, Mingkun Yang, Zhaohai Li, Jianqiang Wan, Pengfei Wang, Wei Ding, Zheren Fu, Yiheng Xu, Jiabo Ye, Xi Zhang, Tianbao Xie, Zesen Cheng, Hang Zhang, Zhibo Yang, Haiyang Xu, and Junyang Lin. Qwen2.5-VL technical report, 2025b. URL https://arxiv.org/abs/2502.13923 .Navaneeth Bodla, Bharat Singh, Rama Chellappa, and Larry S Davis. Soft-NMS–improving object detection with one line of code. In Proceedings of the IEEE international conference on computer vision , pages 5561–5569, 2017. Tim Brooks, Aleksander Holynski, and Alexei A. Efros. InstructPix2Pix: Learning to follow image editing instructions. In Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) , 2023. URL https://arxiv.org/abs/2211.09800 .Yi-Chia Chen, Wei-Hua Li, Cheng Sun, Yu-Chiang Frank Wang, and Chu-Song Chen. SAM4MLLM: Enhance multi-modal large language model for referring expression segmentation. In Computer Vision – ECCV 2024: 18th European Conference, Milan, Italy, September 29–October 4, 2024, Proceedings, Part LXXXI , page 323–340, Berlin, Heidelberg, 2024. Springer-Verlag. ISBN 978-3-031-73003-0. doi: 10.1007/978-3-031-73004-7_19. URL https://doi.org/10.1007/ 978-3-031-73004-7_19 .Bowen Cheng, Ishan Misra, Alexander G. Schwing, Alexander Kirillov, and Rohit Girdhar. Masked-attention mask transformer for universal image segmentation. In Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) , 2022. URL https://arxiv. org/abs/2112.01527 .Haixing Dai, Chong Ma, Zhiling Yan, Zhengliang Liu, Enze Shi, Yiwei Li, Peng Shu, Xiaozheng Wei, Lin Zhao, Zihao Wu, Fang Zeng, Dajiang Zhu, Wei Liu, Quanzheng Li, Lichao Sun, Shu Zhang, Tianming Liu, and Xiang Li. SAMAug: Point prompt augmentation for segment anything model. ArXiv , abs/2307.01187, 2023. URL https://arxiv.org/abs/2307.01187 .Henghui Ding, Song Tang, Shuting He, Chang Liu, Zuxuan Wu, and Yu-Gang Jiang. Multimodal referring segmentation: A survey, 2025. URL https://arxiv.org/abs/2508.00265 .Tianyuan Du, Haopeng Li, Zhen Fan, Jiarui Zhang, Panwang Pan, and Yang Zhang. SAM-Veteran: An MLLM-based human-like SAM agent for reasoning segmentation. In The Four-teenth International Conference on Learning Representations (ICLR) , 2026. URL https: //openreview.net/forum?id=oN55r8iJJW .Qi Fan, Xin Tao, Lei Ke, Mingqiao Ye, Di Zhang, Pengfei Wan, Yu-Wing Tai, and Chi-Keung Tang. Stable segment anything model. In International Conference on Learning Representations (ICLR) ,2025. URL https://openreview.net/forum?id=ooxj2Audlq .Tobias Groot and Matias Valdenegro-Toro. Overconfidence is key: Verbalized uncertainty evaluation in large language and vision-language models. In Proceedings of the 4th Workshop on Trustworthy Natural Language Processing (TrustNLP), NAACL , 2024. URL https://arxiv.org/abs/2405. 02917 .Jonathan Harel, Christof Koch, and Pietro Perona. Graph-based visual saliency. In Advances in Neural Information Processing Systems (NeurIPS) , volume 19, pages 545–552, 2006. 10 Kaiming He, Georgia Gkioxari, Piotr Dollár, and Ross Girshick. Mask R-CNN. In Proceedings of the IEEE International Conference on Computer Vision (ICCV) , 2017. URL https://arxiv. org/abs/1703.06870 .Wenyi Hong, Weihan Wang, Qingsong Lv, Jiazheng Xu, Wenmeng Yu, Junhui Ji, Yan Wang, Zihan Wang, Yuxuan Zhang, Juanzi Li, Bin Xu, Yuxiao Dong, Ming Ding, and Jie Tang. CogAgent: A visual language model for GUI agents. In Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) , 2024. URL https://arxiv.org/abs/2312.08914 .Xiaodi Hou and Liqing Zhang. Saliency detection: A spectral residual approach. In Proceedings of the IEEE Conference on Computer Vision and Pattern Recognition (CVPR) , pages 1–8, 2007. doi: 10.1109/CVPR.2007.383267. Jiaqi Huang, Zunnan Xu, Jun Zhou, Ting Liu, Yicheng Xiao, Mingwen Ou, Bowen Ji, Xiu Li, and Kehong Yuan. SAM-r1: Leveraging SAM for reward feedback in multimodal segmentation via reinforcement learning. In The Thirty-ninth Annual Conference on Neural Information Processing Systems , 2026. URL https://openreview.net/forum?id=dHOSTp8MBl .Laurent Itti, Christof Koch, and Ernst Niebur. A model of saliency-based visual attention for rapid scene analysis. IEEE Transactions on Pattern Analysis and Machine Intelligence , 20(11): 1254–1259, 1998. doi: 10.1109/34.730558. Tilke Judd, Krista Ehinger, Frédo Durand, and Antonio Torralba. Learning to predict where humans look. In Proceedings of the IEEE International Conference on Computer Vision (ICCV) , pages 2106–2113, 2009. doi: 10.1109/ICCV.2009.5459462. Shiu-hong Kao, Yu-Wing Tai, and Chi-Keung Tang. CoT-RVS: Zero-shot chain-of-thought reasoning segmentation for videos, 2025a. URL https://arxiv.org/abs/2505.18561 .Shiu-hong Kao, Yu-Wing Tai, and Chi-Keung Tang. Think before you segment: High-quality reasoning segmentation with gpt chain of thoughts, 2025b. URL https://arxiv.org/abs/ 2503.07503 .Sahar Kazemzadeh, Vicente Ordonez, Mark Matten, and Tamara Berg. ReferItGame: Referring to objects in photographs of natural scenes. In Proceedings of the 2014 Conference on Empirical Methods in Natural Language Processing (EMNLP) , pages 787–798, 2014. Alexander Kirillov, Eric Mintun, Nikhila Ravi, Hanzi Mao, Chloe Rolland, Laura Gustafson, Tete Xiao, Spencer Whitehead, Alexander C. Berg, Wan-Yen Lo, Piotr Dollar, and Ross Girshick. Segment anything. In Proceedings of the IEEE/CVF International Conference on Computer Vision (ICCV) , pages 4015–4026, October 2023. Woosuk Kwon, Zhuohan Li, Siyuan Zhuang, Ying Sheng, Lianmin Zheng, Cody Hao Yu, Joseph E. Gonzalez, Hao Zhang, and Ion Stoica. Efficient memory management for large language model serving with PagedAttention. In Proceedings of the ACM SIGOPS 29th Symposium on Operating Systems Principles , 2023. Xin Lai, Zhuotao Tian, Yukang Chen, Yanwei Li, Yuhui Yuan, Shu Liu, and Jiaya Jia. LISA: Reasoning segmentation via large language model. In Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) , pages 9579–9589, June 2024. Chang Liu, Henghui Ding, and Xudong Jiang. GRES: Generalized referring expression segmentation. In 2023 IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) , page 23592–23601. IEEE, June 2023a. doi: 10.1109/cvpr52729.2023.02259. URL http://dx.doi. org/10.1109/cvpr52729.2023.02259 .Haotian Liu, Chunyuan Li, Qingyang Wu, and Yong Jae Lee. Visual instruction tuning. In Advances in Neural Information Processing Systems (NeurIPS) , 2023b. URL https://arxiv.org/abs/ 2304.08485 .Ye Liu, Zongyang Ma, Junfu Pu, Zhongang Qi, Yang Wu, Ying Shan, and Chang Wen Chen. UniPixel: Unified object referring and segmentation for pixel-level visual reasoning. In Advances in Neural Information Processing Systems (NeurIPS) , 2025a. URL https://arxiv.org/abs/ 2509.18094 .11 Yuqi Liu, Bohao Peng, Zhisheng Zhong, Zihao Yue, Fanbin Lu, Bei Yu, and Jiaya Jia. Seg-Zero: Reasoning-chain guided segmentation via cognitive reinforcement, 2025b. URL https: //arxiv.org/abs/2503.06520 .Junhua Mao, Jonathan Huang, Alexander Toshev, Oana Camburu, Alan L Yuille, and Kevin Murphy. Generation and comprehension of unambiguous object descriptions. In Proceedings of the IEEE Conference on Computer Vision and Pattern Recognition (CVPR) , pages 11–20, 2016. Renjie Pi, Lewei Yao, Jiahui Gao, Jipeng Zhang, and Tong Zhang. PerceptionGPT: Effectively fusing visual perception into LLM. In IEEE/CVF Conference on Computer Vision and Pattern Recognition, CVPR 2024, Seattle, WA, USA, June 16–22, 2024 , pages 27114–27123. IEEE, 2024. doi: 10.1109/CVPR52733.2024.02561. Nikhila Ravi, Valentin Gabeur, Yuan-Ting Hu, Ronghang Hu, Chaitanya Ryali, Tengyu Ma, Haitham Khedr, Roman Radle, Chloe Rolland, Laura Gustafson, Eric Mintun, Junting Pan, Kalyan Vasudev Alwala, Nicolas Carion, Chao-Yuan Wu, Ross Girshick, Piotr Dollar, and Christoph Feichtenhofer. SAM 2: Segment anything in images and videos, 2024. URL https://arxiv.org/abs/2408. 00714 .Zhongwei Ren, Zhicheng Huang, Yunchao Wei, Yao Zhao, Dongmei Fu, Jiashi Feng, and Xiaojie Jin. PixelLM: Pixel reasoning with large multimodal model. 2024 IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) , pages 26364–26373, 2024. URL https://openaccess.thecvf.com/content/CVPR2024/html/Ren_PixelLM_ Pixel_Reasoning_with_Large_Multimodal_Model_CVPR_2024_paper.html .Ruiqi Wang and Hao Zhang. RESAnything: Attribute prompting for arbitrary referring segmentation. In The Thirty-ninth Annual Conference on Neural Information Processing Systems (NeurIPS) ,2025. Xiaoqi Wang, Clint Sebastian, Wenbin He, and Liu Ren. ProSAM: Enhancing the robustness of SAM-based visual reference segmentation with probabilistic prompts. In Proceedings of the IEEE/CVF International Conference on Computer Vision (ICCV) , 2025a. URL https://arxiv. org/abs/2506.21835 .XuDong Wang, Shaolun Zhang, Shufan Li, Konstantinos Kallidromitis, Kehan Li, Yusuke Kato, Kazuki Kozuka, and Trevor Darrell. SegLLM: Multi-round reasoning segmentation, 2024. URL 

https://arxiv.org/abs/2410.18923 .Yuji Wang, Jingchen Ni, Yong Liu, Chun Yuan, and Yansong Tang. IteRPrimE: Zero-shot re-ferring image segmentation with iterative Grad-CAM refinement and primary word empha-sis. In Proceedings of the AAAI Conference on Artificial Intelligence , 2025b. URL https: //ojs.aaai.org/index.php/AAAI/article/view/32880 .Zhuofan Xia, Dongchen Han, Yizeng Han, Xuran Pan, Shiji Song, and Gao Huang. GSVA: Gen-eralized segmentation via multimodal large language models. In Proceedings of the IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) , pages 3858–3869, June 2024. Cilin Yan, Haochen Wang, Shilin Yan, Xiaolong Jiang, Yao Hu, Guoliang Kang, Weidi Xie, and Efstratios Gavves. VISA: reasoning video object segmentation via large language models. In Ales Leonardis, Elisa Ricci, Stefan Roth, Olga Russakovsky, Torsten Sattler, and Gül Varol, editors, 

Computer Vision – ECCV 2024 – 18th European Conference, Milan, Italy, September 29–October 4, 2024, Proceedings, Part XV , Lecture Notes in Computer Science, pages 98–115. Springer, 2024. doi: 10.1007/978-3-031-72633-0_6. Licheng Yu, Patrick Poirson, Shan Yang, Alexander C. Berg, and Tamara L. Berg. Modeling context in referring expressions. In European Conference on Computer Vision (ECCV) , pages 69–85, 2016. Tao Yu, Runseng Feng, Ruoyu Feng, Jinming Liu, Xin Jin, Wenjun Zeng, and Zhibo Chen. Inpaint anything: Segment anything meets image inpainting, 2023. URL https://arxiv.org/abs/ 2304.06790 .12 Haobo Yuan, Xiangtai Li, Tao Zhang, Yueyi Sun, Zilong Huang, Shilin Xu, Shunping Ji, Yunhai Tong, Lu Qi, Jiashi Feng, and Ming-Hsuan Yang. Sa2VA: Marrying SAM2 with LLaVA for dense grounded understanding of images and videos, 2025. URL https://arxiv.org/abs/2501. 04001 .Mozhi Zhang, Mianqiu Huang, Rundong Shi, Linsen Guo, Chong Peng, Peng Yan, Yaqian Zhou, and Xipeng Qiu. Calibrating the confidence of large language models by eliciting fidelity. In 

Proceedings of the 2024 Conference on Empirical Methods in Natural Language Processing (EMNLP) , 2024. URL https://aclanthology.org/2024.emnlp-main.173/ .Yixin Zhang, Nicholas Konz, Kevin Kramer, and Maciej A. Mazurowski. Quantifying the limits of segmentation foundation models: Modeling challenges in segmenting tree-like and low-contrast objects, 2025. URL https://arxiv.org/abs/2412.04243 .Lianghui Zhu, Bin Ouyang, Yuxuan Zhang, Tianheng Cheng, Rui Hu, Haocheng Shen, Longjin Ran, Xiaoxin Chen, Li Yu, Wenyu Liu, and Xinggang Wang. LENS: Learning to segment anything with unified reinforced reasoning. In Proceedings of the AAAI Conference on Artificial Intelligence ,2026. URL https://arxiv.org/abs/2508.14153 .

## A PinPoint cue definitions, normalization, and Soft-NMS 

This appendix gives the per-pixel formulas for the four cues used by PinPoint, the percentile-clipped normalization that puts them on a common scale, and the Soft-NMS recurrence used for diversity-aware point selection. All defaults are listed in Appendix B. 

Color-contrast saliency. S rewards pixels whose local color distribution departs from their sur-roundings: 

S(p) = ∥ μlab in (p) − μlab ann (p) ∥2, (8) where μlab in (p) and μlab ann (p) are mean CIELAB colors in a local disk around p and in the surrounding annulus, respectively. We work in CIELAB because it is approximately perceptually uniform: the ℓ2

distance between mean colors is only a meaningful contrast signal in a space where geometric distance tracks perceived color difference, which is not the case for RGB or HSV. CIELAB’s perceptual uniformity is itself defined relative to a fixed reference illuminant (D65 in our implementation), and we do not perform white-balance or colour-constancy correction prior to computing S. Under strong shadows or specular highlights the ℓ2 contrast can therefore over- or under-estimate true perceptual difference, so we treat S as a robust-but-approximate cue rather than a calibrated photometric quantity. 

Edge density. E flags boundary-adjacent pixels, precisely the regions where SAM’s decoder is most uncertain: 

E(p) =  Kbox ∗ ∥∇ Igray ∥(p), (9) where “ ∗” denotes 2D convolution and Kbox is a box-average kernel applied to the Sobel gradient magnitude ∥∇ Igray ∥. Prior to gradient computation, Igray is convolved with a Gaussian of σpre = 1 

pixel to suppress high-frequency noise that would otherwise inflate E (and H) on flat regions. 

Local Shannon entropy. H measures textural clutter: a homogeneous neighborhood scores low, a busy one scores high: 

H(p) = −X

> v∈V

bqv (p) log bqv (p), (10) where bqv (p) is the empirical frequency of quantized gradient-magnitude bin v over a disk-shaped neighborhood of p, and V is a set of |V| = 256 equal-width bins. Concretely, we compute the per-pixel gradient magnitude ∥∇ Igray ∥, clip it to its 5th and 95 th percentiles over the extended crop, rescale the clipped values linearly to the 8-bit integer range [0 , 255] , and compute a 256 -level histogram over each disk-shaped neighborhood. The fixed (5 , 95) percentile clip used here is for entropy quantization and is independent of the percentile-clip endpoints (q5, q 95 ) used in the cue normalizer (Eq. 12); both happen to default to the same percentiles. Like E, H identifies regions where both SAM’s decoder and the VLM labeler are least reliable. 13 Gaussian spatial prior. G softly favors central positions inside the box: 

G(p) = exp 



− ∥p−cb∥2 

> 2σ2
> G



, σG = 0 .25 ddiag (b), (11) where cb and ddiag (b) are the center and the diagonal length of the bounding box b, and σG is the Gaussian-prior bandwidth. G acts as a weak geometric prior: it biases the consensus toward the interior without forbidding peripheral points. 

Normalization. The four raw cue maps have different scales, so we normalize each map to [0 , 1] 

using percentile-clipped min–max scaling: 

N (X)( p) = clip 

 X(p) − q5(X)

q95 (X) − q5(X) + δ , 0, 1



, (12) where qα(X) is the α-th percentile of X over the extended bounding box and δ = 10 −8 is a small numerical constant that prevents division by zero on degenerate (constant-valued) maps. We reuse 

S, E, H, G to denote the normalized cues. Since high E and high H indicate unstable regions, the fusion rules in Section 3.3 use (1 − E) and (1 − H) as smoothness/homogeneity evidence to be rewarded. 

Soft-NMS diversity selection. Given a partially-selected set P ⊆ eP, each remaining candidate 

p ∈ eP \ P receives the virtual score 

eCP (p) = C(p)



1 − exp 



− min p′∈P ∥p − p′∥2

2σ2

> nms

 

, (13) with the convention min p′∈∅ ∥p − p′∥2 = + ∞ so that eC∅(p) = C(p) and the first selection reduces to 

arg max p∈ eP C(p). Subsequent points are chosen by p⋆ = arg max p eCP (p). The selection terminates when |P| = N , when eP \ P = ∅, or when every remaining virtual score falls below the floor ε (early termination prevents PinPoint from padding the prompt with weak or redundant points). Algorithm 1 in the main text is the operational form of this recurrence. 

Complexity. Each cue map is computed over the extended crop, so cue extraction is O(|bext |) with small constants from box filtering, Sobel gradients, and local histograms. Local-maxima extraction is also O(|bext |), and Soft-NMS [Bodla et al., 2017] selection over the candidate pool costs O(N · | eP| ).PinPoint is deterministic image processing and adds no VLM calls beyond the localization and point-labeling stages; in practice, its cost is dominated by the surrounding VLM and SAM forward passes. 

## B Reproducibility details 

This appendix lists the default values for every symbol introduced in Section 3. All PinPoint radii are expressed relative to the diagonal length ddiag (b) of the VLM-proposed bounding box so that the selector is scale-invariant across images and objects. Unless stated otherwise, these are the values used throughout every experiment in Section 4. 

Deterministic behavior. Every stage of PinPoint is deterministic for a fixed image, box, and budget. The only stochastic component is VLM sampling in the localization and point-labeling stages; we use a low temperature ( 0.1) and a fixed nucleus ( 0.9) so that the structured JSON outputs are stable in practice, and every benchmark number reported in Section 4 is a single pass with a fixed seed. 

Hardware. All inference runs use a single NVIDIA A6000 GPU with batch size 1 at the engine level; vLLM internally batches the per-image point-labeling calls. The two-VLM-call cost structure is hardware-independent; see Section 4.3 for the call-count analysis. 

Hyperparameter sensitivity. Table 4 sweeps the three scalar hyperparameter classes whose effect on cIoU is non-trivial: the Gaussian-prior bandwidth σG (Eq. 11), the edge / entropy penalty weights 

(λE , λ H ) in Cmsm (Eq. 3), and the percentile-clip endpoints (q5, q 95 ) in the cue normalizer (Eq. 12). 14 Table 3: Default hyperparameters for PinPoint, point labeling, filtering, and SAM. Radii marked with 

ddiag (b) scale with the per-box diagonal; fixed values are constants across all boxes and images. 

Symbol Meaning Default Reference 

α Box-extension factor for cue-map ROI 0.20 §3.3 

rin Saliency inner (local) window radius 0.03 · ddiag (b) Eq. 8 

rann Saliency annular window radius 0.09 · ddiag (b) Eq. 8 

|Kbox | Edge-density box-average kernel side 0.05 · ddiag (b) Eq. 9 

σpre Pre-Sobel Gaussian smoothing 1.0 pixel Eq. 9 

rH Entropy disk-neighborhood radius 0.03 · ddiag (b) Eq. 10 

|V| Gradient-magnitude quantization bins 256 (8-bit) Eq. 10 

σG Gaussian-prior bandwidth 0.25 · ddiag (b) Eq. 11 

(q5, q 95 ) Cue-normalizer percentile-clip endpoints (5 th , 95 th ) Eq. 12 

δ Numerical-stability constant in N (·) 10 −8 Eq. 12 

(pH 

> lo

, p H 

> hi

) Gradient-mag. clip for H quantization (5 th , 95 th ) Eq. 10 

λE Edge penalty in Cmsm 0.5 Eq. 3 

λH Entropy penalty in Cmsm 1.5 Eq. 3 

Wpeak Local-maxima peak-finding window 3 × 3, 8-connected §3.3 

σnms Soft-NMS Gaussian suppression radius 0.10 · ddiag (b) Eq. 13 

ε Soft-NMS termination floor 10 −3 §3.3 

N Requested point budget per box 5 §3.3 

τ Confidence threshold for filtering 0.7 Eq. 5 

A Label allowlist {positive , negative } Eq. 5 

VLM decoding 

Temperature Sampling temperature 0.1 §3.4 top-p Nucleus sampling 0.9 §3.4 max-new-tokens Generation budget per call 512 §3.4 Labeling batching All Pk batched; ∼2 calls/image on §3.4 

SAM 

Checkpoint SAM 2 model identifier (HF Hub) facebook/sam2-hiera-large §3.5 

multimask_output Mask proposals per prompt False (single-mask) Eq. 6 

Each block sweeps one parameter while holding the remaining defaults from Table 3 fixed; the reported cIoU values are based on runs on RefCOCO val and the more spatially-challenging testB split. Three observations follow. First, σG is essentially flat across the reasonable range [0 .10 , 0.33] 

(val cIoU spans only 1.1 points), with a meaningful drop only at the over-smoothed endpoint 

σG=0 .45 ; the default 0.25 is robust. Second, the (λE , λ H ) grid is the most informative axis but the two splits do not agree on a single winner: (0 .5, 1.5) wins val with 75 .4 cIoU and reaches 72 .4 on testB, while (1 .5, 0.5) tops testB with 72 .9 but loses 1.9 cIoU on val. We adopt (0 .5, 1.5) as the default reflected in Table 3 because (i) it wins val by a clear margin and is within 0.5 cIoU of the best on testB (where the top settings cluster within a narrow band of 0.5 cIoU), and (ii) holding λE =0 .5

fixed and sweeping λH ∈ { 0.5, 1.0, 1.5} yields a monotonic gain on both splits ( +2 .4 val, +1 .6

testB), indicating that within the smallest-edge-penalty slice the entropy penalty does have a slightly stronger marginal effect; the global 3×3 landscape is, however, non-monotonic, so this should be read as a local rather than universal trend. Third, the percentile-clip endpoints (q5, q 95 ) are stable: the wider (1 , 99) clip edges (5 , 95) on val by 0.4 cIoU but loses 1.6 cIoU on testB, so we keep the more stable (5 , 95) default. Across every grid point the cIoU stays close to the default configuration (highlighted in bold), confirming that PinPoint does not depend on a brittle hyperparameter setting. 

## C Localization prompt and grammar 

The first stage of the pipeline uses the VLM as a grounded proposer: given an image I and a natural-language query Q, it returns a (possibly empty) set of candidate bounding boxes. To make the output consumable by downstream stages we constrain the generation with a formal EBNF grammar via vLLM’s structured outputs. The prompt template below is what the VLM sees; the grammar enforces that its output is a single valid JSON object with chain-of-thought and box coordinates. 15 Table 4: Hyperparameter sensitivity. cIoU on RefCOCO val and testB while sweeping σG (as a fraction of ddiag (b)), (λE , λ H ), and (q5, q 95 ); ∆ columns report the gap to the block’s default row. 

Best and second-best per metric per block; the default row labels are bolded. The (λE , λ H ) block is reported under Cmsm since these weights only appear in Eq. 3; the σG and (q5, q 95 ) blocks are reported under the paper-default Cmult , which is why the “default” rows do not all share the same cIoU value. 

Parameter Setting val cIoU ↑ ∆ val testB cIoU ↑ ∆ testB 

Gaussian-prior bandwidth σG (frac. of ddiag (b))

σG 0.10 75 .9 0.0 69 .9 −1.2

σG 0.18 76 .6 +0 .7 70 .8 −0.3

σG 0.25 (default) 75 .9 0.0 71 .1 0.0

σG 0.33 75 .5 −0.4 70 .7 −0.4

σG 0.45 73 .6 −2.3 70 .5 −0.6

Edge / entropy penalty weights (λE , λ H ) in Cmsm 

(λE , λ H ) (0 .5, 0.5) 73 .0 −2.4 70 .8 −1.6(λE , λ H ) (0 .5, 1.0) 72 .6 −2.8 71 .0 −1.4

(λE , λ H ) (0 .5, 1.5) (default) 75 .4 0.0 72 .4 0.0

(λE , λ H ) (1 .0, 0.5) 74 .2 −1.2 70 .0 −2.4(λE , λ H ) (1 .0, 1.0) 73 .0 −2.4 70 .5 −1.9(λE , λ H ) (1 .0, 1.5) 73 .0 −2.4 72 .7 +0 .3(λE , λ H ) (1 .5, 0.5) 73 .5 −1.9 72 .9 +0 .5(λE , λ H ) (1 .5, 1.0) 71 .9 −3.5 71 .4 −1.0(λE , λ H ) (1 .5, 1.5) 73 .1 −2.3 69 .6 −2.8

Percentile-clip endpoints (q5, q 95 ) in the cue normalizer 

(q5, q 95 ) (1 , 99) 76 .3 +0 .4 69 .5 −1.6

(q5, q 95 ) (5 , 95) (default) 75 .9 0.0 71 .1 0.0

(q5, q 95 ) (10 , 90) 75 .6 −0.3 69 .6 −1.5

Localization Prompt 

You are a visual grounding assistant. Given an image and a natural-language query, return every object in the image that the query refers to, as a bounding box in pixel coordinates. Image size: {H} × {W} (height × width). Query: “{Q}” Respond with a single JSON object that conforms to the grammar below. Include a short reasoning string explaining your choice, followed by a boxes list in which each element has integer fields x_min, y_min, x_max, y_max, all in pixel coordinates of the image. If the query does not refer to any visible object, return an empty list for boxes and briefly explain why in reasoning. 

EBNF Grammar (Localization) 

root ::= "{" ws "\"reasoning\"" ws ":" ws string ws "," ws "\"boxes\"" ws ":" ws box_list ws "}" box_list ::= "[" ws "]" | "[" ws box (ws "," ws box)* ws "]" box ::= "{" ws "\"x_min\"" ws ":" ws integer ws "," ws "\"y_min\"" ws ":" ws integer ws "," ws "\"x_max\"" ws ":" ws integer ws "," ws "\"y_max\"" ws ":" ws integer ws "}" integer ::= "0" | [1-9] [0-9]* string ::= "\"" ( [^"\\] | "\\" . )* "\"" 

16 ws ::= ( " " | "\t" | "\n" | "\r" )* 

Implementation note. The grammar is enforced at decoding time via vLLM’s 

StructuredOutputsParams . If the grammar-constrained decoder truncates early (e.g., the generation budget is exhausted mid-JSON), we fall back to a single unconstrained retry and discard the sample only if the retry also fails to produce a parseable object; empirically this happens on fewer than 0.1% of the RefCOCO/+/g validation samples with the default max-new-tokens budget. 

## D Point-labeling prompt and grammar 

The third stage of the pipeline uses the VLM as a semantic verifier: for each PinPoint-selected point 

pk,i ∈ P k inside the proposed box bk, the VLM returns a label ℓk,i ∈ { positive , negative } and a verbalized confidence ck,i ∈ [0 , 1] . As discussed in Section 3.4, the VLM receives the image, the query, the box, and the point coordinates via text; no pixel-level annotations (dots, crosses, rectangles) are rendered onto the image before it is shown to the VLM. All points inside an image are labeled in a single batched generation call. 

Point-Labeling Prompt 

You are a visual grounding assistant. Given an image, a natural-language query, a bounding box, and a list of points inside that bounding box, decide for each point whether it lies on the object described by the query (positive) or on the background (negative), and report your confidence. Image size: {H} × {W} (height × width). Query: “{Q}” Bounding box (pixel coordinates): {x_min, y_min, x_max, y_max}. Points: [{x_1, y_1}, {x_2, y_2}, ..., {x_n, y_n}]. Respond with a single JSON object that conforms to the grammar below. The labels field must contain exactly one entry per input point, in the same order. Each entry has a label string (positive or negative) and a confidence number in [0 , 1] .Do not hedge: pick the label that better describes the point, and report your own honest confidence. 

EBNF Grammar (Point Labeling) 

root ::= "{" ws "\"labels\"" ws ":" ws label_list ws "}" label_list ::= "[" ws label (ws "," ws label)* ws "]" label ::= "{" ws "\"label\"" ws ":" ws label_enum ws "," ws "\"confidence\"" ws ":" ws confidence ws "}" label_enum ::= "\"positive\"" | "\"negative\"" confidence ::= "0" | "1" | "0." [0-9]+ | "1.0" | "1.00" ws ::= ( " " | "\t" | "\n" | "\r" )* 

Batching. In practice, all points for a single image (and, when the benchmark is run in batched mode, all points across a minibatch of images) are passed to the VLM in one structured-output call. This collapses point labeling from |P k| sequential forward passes to a single forward pass per image, and the whole pipeline consumes approximately two VLM calls per image in total: one for localization and one for the entire labeling batch. The template above handles one box per call; 17 multi-box images dispatch K such prompts concurrently as a single vLLM batch, so per-image latency stays bounded by one VLM round-trip independent of K.

Length-mismatch fallback. The EBNF grammar enforces the JSON structure of the response but does not constrain the labels array length to match the input point count. We handle the two failure modes deterministically: if the VLM returns a labels array longer than the input point list, we truncate to the input length; if it returns a shorter array, the missing trailing entries are treated as low-confidence labels and are dropped by the confidence filter (Eq. 5). No retry is issued, and length mismatches do not propagate to the SAM call. 

## E End-to-end ablation 

This appendix reports the detailed end-to-end ablation summarized in Section 4.3. Each row holds every stage of the PinPoint pipeline fixed except the named factor; N =5 throughout. Table 5: End-to-end ablation on RefCOCO val. Each row holds every stage of the pipeline fixed except the named factor; N =5 throughout. “Random” samples points uniformly inside the VLM box (averaged over three seeds); “All-positive” skips the VLM verifier and labels every PinPoint point as 

positive ; “ τ =0 ” disables confidence filtering. Bold marks the default PinPoint configuration (also the best result) used throughout the rest of the paper; underline marks the second-best.                                    

> Selector Verifier Filter cIoU ↑∆vs. default
> Bounding box only ——69 .2−6.7
> Naive sampling VLM labels τ=0 .761 .9−14 .0
> PinPoint mult all-positive —61 .1−14 .8
> PinPoint mult VLM labels τ=0 73 .6−2.3
> PinPoint mult VLM labels τ=0 .7(default) 75 .90.0
> PinPoint msm VLM labels τ=0 .775 .4−0.5

Roles of the verifier and the filter. The verifier and the confidence filter contribute distinct, complementary roles even though they both operate on the labeled point set. PinPoint’s point selection is semantically blind: the four cues observe the image and the bounding box, but never the referring expression. Treating every selected point as positive (the all-positive row) therefore makes the prompt more ambiguous, since any candidate that lands on co-occurring background or a distractor object pollutes the support; the VLM verifier converts these geometrically-chosen candidates into positive/negative SAM prompts that align with the referring expression. Numerically, dropping the verifier costs 14 .8 cIoU while disabling the threshold ( τ =0 ) costs only 2.3 cIoU, so the verifier carries the bulk of the work and the filter acts as a stabilizer that discards low-confidence labels so occasional VLM misjudgments do not propagate into the SAM mask. The two fusion modes (last two rows) sit within 0.5 cIoU of each other on this split, supporting our framing of Cmsm as a softer relaxation of Cmult rather than a separate method. 

Threshold sensitivity and VLM portability. Two questions follow from the −2.3 cIoU gap between τ =0 and τ =0 .7 in the ablation above. First, is τ =0 .7 brittle on the VLM we use, or is the entire [0 , 0.7] range close to the default? The two endpoints give a 2.3 cIoU envelope, and verbalized confidences from grammar-guided Qwen2.5-VL are concentrated above 0.5 in our runs, so most of that envelope is contributed by a small slice of low-confidence labels rather than by a sharp peak around 0.7; PinPoint is therefore not relying on a finely tuned cutoff for this VLM. Second, does the same value of τ port to a different VLM? Verbalized confidences are systematically overconfident and miscalibrated, with the degree of miscalibration differing meaningfully across language and vision–language models tested side by side [Groot and Valdenegro-Toro, 2024], so the absolute scale of ck,i is VLM-specific and the optimal τ is unlikely to transfer numerically. The role of the threshold (suppress the lowest-confidence labels), however, is a structural property of the filter and not of the value itself: any VLM that reports a usable confidence score will benefit from removing its bottom slice, even if the cut-off needs to be re-located. We therefore recommend a coarse one-pass val sweep over τ ∈ { 0.0, 0.3, 0.5, 0.7, 0.9} when porting PinPoint to a new VLM, rather than treating 0.7 as a universal default; the verifier itself, the only stage with a large effect, is unchanged across this sweep. 18 Stage decomposition and per-cue robustness. Table 5 holds the pipeline fixed at the default configuration and ablates one factor at a time; we complement it here with two analyses that probe different axes. Table 6 walks a candidate prompt set through five progressive pipeline configurations, from the box-only floor to the full PinPoint pipeline, isolating what each stage contributes on top of the previous one. Table 7 zeros each of the four PinPoint cues—colour-contrast saliency S, edge density E, local entropy H, and the Gaussian spatial prior G—one at a time, asking whether the gain is robust to dropping any single cue. Together the two answer two questions Table 5 does not address by construction: where along the pipeline the gain enters, and whether the four-cue selector relies on any single cue. The cue ablation runs under a diagnostic linear-penalty fusion mode, 

C(p) = S(p) · max(1 − λE E(p) − λH H(p), 0) · G(p), which is the only fusion that admits a clean off-switch for E and H via the existing λE and λH knobs (the headline Cmult multiplies S, (1 − E),

(1 − H), G with no scalar weights, so an off-switch for any single cue would change the formula rather than zero a parameter); we therefore report ∆cIoU vs. the all-cues-on baseline rather than absolute cIoU. S enters every fusion as a multiplicative factor and admits no zero-weight off-switch in any mode, so a no_ S row is intentionally omitted. Table 6: Per-stage contribution on RefCOCO val. Five progressive configurations from the box-only floor to the full PinPoint pipeline; N =5 throughout. Each row adds one stage on top of the previous: random points replaces PinPoint with uniform interior sampling and skips labelling; PinPoint points restores geometry-aware selection but still skips labelling; + VLM labels enables the verifier; + filter enables confidence filtering at τ =0 .7. Bold marks the full pipeline (the headline run from Table 1); underline marks the second-best.                     

> Stage Description cIoU ↑
> s1 Bounding box only 69 .2
> s2 +Random interior points (no labels) 65 .4
> s3 +PinPoint points (no labels) 68 .7
> s4 +VLM labels (no filter) 73 .6
> s5 +Confidence filter τ=0 .7(full pipeline) 75 .9

Where each stage helps. The stage walk reveals that the gain enters in clearly separable steps rather than smoothly. Replacing the box-only baseline with random interior points (s1 →s2) actually 

costs 3.8 cIoU: SAM treats every passed point as a positive prompt, so uninformative samples landing on co-occurring background or distractors pollute the support and shift the predicted mask away from the referent. Switching to geometry-aware PinPoint points (s2 →s3) recovers most of that loss ( +3 .3

cIoU), confirming that point selection alone—without semantic supervision—is enough to bring the prompt set back to the box-only floor by avoiding obviously bad positions. The biggest single jump is the VLM verifier (s3 →s4, +4 .9 cIoU): converting a geometric set into per-point positive/negative labels gives SAM both anchors and explicit exclusions, which is what disambiguates the referent from co-occurring objects inside the same box. Confidence filtering on top of labelling (s4 →s5, 

+2 .3 cIoU) is the final stabiliser, suppressing the small fraction of low-confidence labels the verifier mislabels under grammar-guided decoding; Tables 8 and 9 dissect this stage further. Table 7: Per-cue robustness on RefCOCO val. Each row zeros one cue of the four-cue PinPoint selector under the diagnostic linear-penalty fusion that admits clean off-switches for E and H via λE

and λH , and for G via σG → ∞ . We report ∆cIoU relative to the all-cues-on baseline of this fusion; the absolute level under the diagnostic fusion is not directly comparable to the headline Cmult runs in Tables 1 and 5.              

> Setting Cue removed ∆cIoU vs. all-on all-on —0.0
> no_ Eedge density ( λE=0 )−0.7
> no_ GGaussian prior ( σG→∞ )−1.5
> no_ Hlocal entropy ( λH=0 )+1 .7

Robustness to single-cue dropout. No single cue moves cIoU by more than 1.7 points on Re-fCOCO val, supporting the robustness claim already made in §4.3 (Hyperparameter sensitivity): 19 PinPoint does not rely on any single cue. Removing the Gaussian prior costs the most ( −1.5 cIoU), consistent with G’s role as a soft centroid prior that suppresses peripheral candidates close to the box border. Removing the edge-density penalty costs only −0.7 cIoU at unit weight. The small positive 

∆ for no_ H (+1 .7) is a property of the unit-weighted linear-penalty diagnostic, not of the headline fusion: the entropy penalty at λH =1 is somewhat over-aggressive, which is consistent with the Cmsm 

sweep in §4.3 preferring a different operating point rather than H being unhelpful in general. The headline Cmult uses (1 − E) and (1 − H) as smoothness/homogeneity factors with no scalar weights, so E and H contribute as fixed multiplicative penalties there and cannot be probed by zeroing a parameter; the diagnostic mode used in Table 7 exists precisely to make these per-cue interventions well-defined. 

Filter sweep: confidence threshold. The verifier ablation in Table 5 reports two threshold end-points, τ =0 and τ =0 .7. Table 8 sweeps the interior, τ ∈ { 0.5, 0.6, 0.7, 0.8, 0.9}, on the multiplicative variant with the default allowlist A={positive , negative }; we report cIoU on the full RefCOCO val and testB splits along with the fraction of labeled points discarded and the mean number of points passed to SAM ( N =5 throughout). cIoU is non-monotone in τ on both splits and peaks at τ =0 .8;the τ =0 .5, τ =0 .6, τ =0 .7 rows sit within a 1.0 cIoU band on val. This pattern is consistent with the calibration of grammar-guided Qwen2.5-VL: verbalized confidences below ∼0.7 are systematically mis-labeled and benefit from removal, but above ∼0.7 confidence and label correctness are essentially uncorrelated, so further filtering discards correct labels along with the wrong ones. Pushing past 

τ =0 .8 then begins to cost: at τ =0 .9, ∼ 48% of labeled points are discarded on val (mean kept prompts 2.51 , vs. 4.27 at default), and a non-trivial sample fraction retains no prompts at all and falls back to SAM’s box-only output ( 69 .2 cIoU; Table 5). The both-labels cIoU at τ =0 .9 ends −0.8

below default on both splits, giving a clean illustration of the box-only-fallback tradeoff. Table 8: Confidence-threshold sweep on RefCOCO val and testB. cIoU at each τ on the default PinPoint mult configuration with A={positive , negative }, N =5 . “% filt.” is the fraction of labeled points discarded; “mean kept” is the mean number of points passed to SAM. The bold row marks the paper default; underlined cells are the best within each split.                                     

> τval cIoU testB cIoU val % filt. testB % filt. val mean kept testB mean kept
> 0.576 .069 .00.91.04.71 4.75 0.675 .273 .04.33.14.56 4.57
> 0.775 .971 .112 .88.84.27 4.43
> 0.878 .171 .929 .020 .63.47 3.67 0.975 .170 .347 .842 .42.51 2.72

Filter sweep: label allowlist. Table 9 extends the sweep to the joint grid of τ ∈ { 0.7, 0.8, 0.9}

and A ∈ {{ +, −} , {+}, {−}} . The optimal allowlist is split-dependent. Val prefers positive-only at τ =0 .8 (80 .3 cIoU, +4 .4 over default), while testB prefers negative-only at the same τ (72 .1

cIoU, +1 .0); the two splits flip the sign of the allowlist effect, so committing to a single label class is brittle. SAM uses positives to anchor and negatives to exclude , and the relative importance of the two roles depends on the cluttered-box population of the split: testB has more distractors, so removing negatives hurts more there. Beyond τ =0 .8 all three allowlists move toward or past the box-only-fallback regime: positive-only retains only ∼1.1 prompts on val and ∼1.2 on testB at 

τ =0 .9, and the both-labels cell drops below default on both splits. 

## F Per-stage runtime breakdown 

This appendix expands the brief efficiency discussion in Section 4.3. We characterise PinPoint’s inference cost in terms of VLM calls per query, the dominant cost contributor in any training-free VLM + SAM pipeline and a hardware-independent measure of architectural efficiency, before reporting the per-stage wall-clock breakdown. 

Why the per-query budget is exactly two VLM calls. Under PinPoint’s default configuration (grammar-guided structured decoding in both the localization and point-labeling stages, no separate chain-of-thought reasoning pass, and batched per-image labeling), a single query results in (i) one 20 Table 9: Threshold × label-allowlist sweep on RefCOCO val and testB. cIoU under three allowlists 

A: both labels, positive-only ( {+}), negative-only ( {−} ). N =5 , multiplicative fusion, oracle off. The bold row is the paper default; underlined cells are the best within each split.                                                      

> τAval cIoU testB cIoU val mean kept testB mean kept
> 0.7{+,−} 75 .971 .14.27 4.43
> 0.7{+}76 .369 .61.46 1.88 0.7{−} 73 .169 .42.70 2.41 0.8{+,−} 78 .171 .93.47 3.67 0.8{+}80 .371 .41.33 1.79 0.8{−} 77 .272 .12.25 2.05 0.9{+,−} 75 .170 .32.51 2.72 0.9{+}79 .068 .81.09 1.18 0.9{−} 77 .469 .91.51 1.51

grammar-guided localization call producing the candidate set B, and (ii) one batched grammar-guided point-labeling call across all (bk, p ) pairs. The two-call budget is therefore independent of the point budget N , the number of proposed boxes K, and the choice of fusion mode. The remaining stages of the pipeline (PinPoint point selection, point filtering, and SAM segmentation) are not VLM calls: cue computation and Soft-NMS run on the CPU, and SAM is a single feed-forward pass through the frozen segmenter. 

Call-count comparison with related training-free pipelines. Training-free referring-segmentation methods divide cleanly along their inference-time call budget. Decomposition pipelines such as RESAnything [Wang and Zhang, 2025] parse the referring expression into multiple semantic attributes and issue one VLM call per attribute. Loop-based pipelines such as SegLLM [Wang et al., 2024] and CoT-RVS [Kao et al., 2025a] iteratively feed segmentation outputs back into the VLM for refinement, spending three or more VLM calls per refinement round. RL-trained single-shot methods such as Seg-Zero [Liu et al., 2025b] and SAM-R1 [Huang et al., 2026] issue a single VLM call at inference but require GRPO post-training on thousands of curated samples. PinPoint matches the inference-time call budget of the RL-trained category without any training, and issues fewer VLM calls than decomposition or loop-based methods on expressions of nontrivial length. 

Per-stage wall-clock. Table 10 reports the per-query wall-clock decomposition of PinPoint on RefCOCO val with Qwen2.5-VL-7B + SAM, measured on a single A6000. Means and standard deviations are computed across the val split. Total cost is dominated by the two VLM calls (localiza-tion and point labeling, together ∼95% of wall-clock); PinPoint’s geometry-aware point selection contributes only 1.3% and confidence filtering is below 0.1 ms. Table 10: Per-query wall-clock breakdown on RefCOCO val (single A6000). Confidence filtering was disabled in this measurement; its arithmetic cost is below 0.1 ms.                       

> Stage Mean (ms) Std (ms) % of total
> Localization (VLM) 3290 .1952 .966 .5%
> Point selection (PinPoint) 62 .214 .31.3%
> Point labeling (VLM) 1413 .7196 .928 .6%
> Point filtering <0.1<0.10.0%
> SAM segmentation 182 .523 .13.7%
> Total 4948 .5100 .0%

The ratio of PinPoint point-selection cost to total VLM cost is 62 .2 / (3290 .1 + 1413 .7) ≈ 1.3% ,confirming that the per-query budget is dominated by the two structured-decoding VLM calls and that PinPoint’s overhead is negligible despite being more expensive than uniform random sampling inside the box. 21