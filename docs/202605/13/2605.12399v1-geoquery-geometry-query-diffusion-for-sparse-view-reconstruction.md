---
title: "GeoQuery: Geometry-Query Diffusion for Sparse-View Reconstruction"
title_zh: GeoQuery：用于稀疏视图重建的几何查询扩散模型
authors: "Xiao Cao, Yuze Li, Youmin Zhang, Jiayu Song, Cheng Yan, Wen Li, Lixin Duan"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.12399v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 利用几何引导扩散进行稀疏视图3D重建
tldr: 针对3D高斯泼溅（3DGS）在稀疏视图下重建质量差、伪影严重的问题，本文提出GeoQuery框架。该框架引入几何引导交叉视图注意力机制（GCA），利用深度图和相机位姿构建几何对应场，生成代理查询特征以替代受损的渲染特征，并结合局部窗口注意力进行特征聚合。实验证明，该方法能有效消除渲染伪影，在极稀疏视图下实现高质量的三维重建。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.12399v1/fig-001.webp\", \"caption\": \"Fig. 4. MoreQualitative comparisons on artifact removal.\", \"page\": 6, \"index\": 1, \"width\": 1056, \"height\": 612}, {\"url\": \"assets/figures/arxiv/2605.12399v1/fig-002.webp\", \"caption\": \"Fig. 5. Same-scene comparison under varying input views on the bicycle scene of Mip-NeRF360 [Barron et al. 2022]. We fix the same target novel view and compare DIFIX3D+ and GeoQuery under 3, 6, and 9 training views. GeoQuery maintains more stable reconstruction quality as view sparsity increases.\", \"page\": 6, \"index\": 2, \"width\": 1058, \"height\": 486}, {\"url\": \"assets/figures/arxiv/2605.12399v1/fig-003.webp\", \"caption\": \"Fig. 6. MoreQualitative results on Mip-NeRF360 dataset [Barron et al. 2022].\", \"page\": 7, \"index\": 3, \"width\": 1049, \"height\": 612}, {\"url\": \"assets/figures/arxiv/2605.12399v1/fig-004.webp\", \"caption\": \"Fig. 7. MoreQualitative results on DL3DV-Benchmark dataset [Ling et al. 2024].\", \"page\": 7, \"index\": 4, \"width\": 1042, \"height\": 498}, {\"url\": \"assets/figures/arxiv/2605.12399v1/fig-005.webp\", \"caption\": \"Table 2. Quantitative comparison of rendering quality on Mip-NeRF360 [Barron et al. 2022] and DL3DV-Benchmark [Ling et al. 2024] under different numbers of input views (3, 6, and 9 views). ‡ denotes results reproduced using the official implementations of the corresponding methods.\", \"page\": 8, \"index\": 5, \"width\": 1010, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2605.12399v1/fig-006.webp\", \"caption\": \"Fig. 2. Overview of GeoQuery. Starting from a sparse training set, we optimize a 3D Gaussian Splatting (3DGS) representation and progressively refine it through iterative rendering and supervision updates. At each step, 3DGS produces an artifact-prone rendering 𝐼𝑡 . We estimate metric depth to construct a geometric correspondence field, which is used to retrieve proxy features from the reference view. The proposed Geometry-Guided Cross-View Attention (GCA) restricts retrieval to a local 𝑘 × 𝑘 neighborhood around the indexed correspondence, and an adaptive fusion module integrates the geometry-guided features into the diffusion backbone. The restored output 𝐼𝑡 is then used as a pseudo-observation for subsequent 3DGS refinement.\", \"page\": 4, \"index\": 6, \"width\": 1052, \"height\": 645}, {\"url\": \"assets/figures/arxiv/2605.12399v1/fig-007.webp\", \"caption\": \"Fig. 9. Visual comparisons on Mip-NeRF360 dataset and DL3DV dataset between our GeoQuery and baseline methods, including 3DGS [Kerbl et al. 2023], FSGS [Zhu et al. 2024], and DIFIX3D+ [Wu et al. 2025b]. Compared to the baselines, our method produces more reliable renderings.\", \"page\": 9, \"index\": 7, \"width\": 1050, \"height\": 546}, {\"url\": \"assets/figures/arxiv/2605.12399v1/fig-008.webp\", \"caption\": \"Fig. 3. Qualitative comparisons on artifact removal. From left to right: the artifact-corrupted 3DGS rendering, DIFIX3D+ [Wu et al. 2025b], our GeoQuery, and the ground truth. The examples illustrate that GeoQuery can more reliably restore rendering structure, producing results that are visually closer to the ground truth.\", \"page\": 5, \"index\": 8, \"width\": 1048, \"height\": 318}]"
motivation: 现有的扩散模型修复方法在渲染图严重受损时，因查询特征质量差导致跨视图特征检索失效，无法保持视图一致性。
method: 提出几何引导交叉视图注意力机制，通过深度和位姿构建几何对应场来生成代理查询，并在局部窗口内进行特征聚合。
result: 在稀疏视图合成和渲染伪影去除任务上表现优异，显著提升了重建的鲁棒性和视觉质量。
conclusion: GeoQuery通过将显式几何线索引入扩散模型，解决了稀疏视图重建中的特征匹配难题，为高质量三维重建提供了新方案。
---

## 摘要
3D高斯泼溅（3DGS）已成为3D重建和新视图合成的一种重要范式。然而，在稀疏视图约束下进行训练时，它仍然容易产生严重的伪影。虽然最近的方法尝试利用图像扩散模型来纠正渲染视图中的伪影，但它们通常依赖多视图自注意力机制从参考图像中检索信息。我们观察到，当3DGS输出的渲染新视图严重损坏时，这种机制往往会失效：受损的查询特征会导致错误的跨视图检索，从而导致渲染细化不一致。为了解决这个问题，我们提出了GeoQuery，这是一个几何引导的扩散框架，它通过一种新型的几何引导跨视图注意力（GCA）机制，将生成先验与显式几何线索相结合。首先，通过利用预测的深度图和相机位姿，我们构建了一个几何诱导的对应场来采样参考特征，形成一个几何对齐的代理查询，以取代损坏的渲染特征。此外，我们设计了一个新的跨视图特征聚合流水线，其中我们将跨视图注意力限制在每个代理查询周围的局部窗口内，以有效地检索有用特征，同时抑制伪匹配。GeoQuery可以无缝集成到现有的基于扩散的流水线中，即使在极端的视图稀疏情况下也能实现鲁棒的重建。在稀疏视图新视图合成和渲染伪影去除方面的广泛实验证明了我们方法的有效性。

## Abstract
3D Gaussian Splatting (3DGS) has emerged as a prominent paradigm for 3D reconstruction and novel view synthesis. However, it remains vulnerable to severe artifacts when trained under sparse-view constraints. While recent methods attempt to rectify artifacts in rendered views using image diffusion models, they typically rely on multi-view self-attention to retrieve information from reference images. We observe that this mechanism often fails when the rendered novel views output by 3DGS are heavily corrupted: damaged query features lead to erroneous cross-view retrieval, resulting in inconsistent rendering refinement. To address this, we propose GeoQuery, a geometry-guided diffusion framework that integrates generative priors with explicit geometric cues via a novel Geometry-guided Cross-view Attention (GCA) mechanism. First, by leveraging predicted depth maps and camera poses, we construct a geometry-induced correspondence field to sample reference features, forming a geometry-aligned proxy query that replaces the corrupted rendering features. Furthermore, we design a new cross-view feature aggregation pipeline, in which we restrict the cross-view attention to a local window around each proxy query to effectively retrieve useful features while suppressing spurious matches. GeoQuery can be seamlessly integrated into existing diffusion-based pipelines, enabling robust reconstruction even under extreme view sparsity. Extensive experiments on sparse-view novel view synthesis and rendering artifact removal demonstrate the effectiveness of our approach.

---

## 论文详细总结（自动生成）

这是一份关于论文《GeoQuery: Geometry-Query Diffusion for Sparse-View Reconstruction》的结构化深入总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：3D高斯泼溅（3DGS）在稀疏视图（如仅3-9张图）下训练时，由于缺乏足够的观测约束，容易产生严重的几何坍塌和漂浮伪影（Floaters）。
*   **研究动机**：现有的“渲染-细化”（Render-and-Refine）流水线尝试利用扩散模型（Diffusion Models）修复受损的渲染图。然而，这些方法依赖的**多视图自注意力机制**存在“**查询污染（Query Contamination）**”问题：当渲染图本身质量极差时，提取的查询特征（Query）也是错误的，导致模型从参考视图中检索到错误的纹理，从而强化了伪影而非消除它们。

### 2. 论文提出的方法论
GeoQuery 的核心思想是**用显式的几何对应关系来引导扩散模型的特征检索过程**，绕过受损的渲染特征。

*   **几何对应场构建**：利用离线估计的度量深度图（Metric Depth）和相机位姿，通过反投影和投影操作，建立目标视图像素到参考视图像素的物理对应关系 $C_{t \to r}$。
*   **几何引导交叉视图注意力（GCA）**：
    *   **代理查询（Proxy Query）**：不再使用受损渲染图的特征作为 Query，而是根据几何对应关系，直接从“干净”的参考视图特征图中采样对应的特征作为“代理查询”。
    *   **局部窗口限制**：将交叉注意力的检索范围限制在对应点周围的 $k \times k$ 局部窗口内。这能有效抑制长程的语义误匹配，确保特征提取在物理几何上是准确的。
*   **自适应融合机制**：设计了一个可学习的门控模块，动态平衡几何引导特征与全局语义特征的比例，增强了模型在几何对应失效（如遮挡区）时的鲁棒性。
*   **算法流程**：3DGS 渲染 $\to$ 深度估计与几何对齐 $\to$ GCA 模块进行特征修复 $\to$ 扩散模型输出高质量图像 $\to$ 将修复图作为伪标签重新监督 3DGS 优化。

### 3. 实验设计
*   **数据集**：
    *   **DL3DV-Benchmark**：包含 123 个场景用于训练（约 10 万对图像），12 个场景用于测试。
    *   **Mip-NeRF360**：用于验证在无界场景下的泛化能力。
*   **Benchmark 与对比方法**：
    *   **任务**：渲染伪影去除（Artifact Removal）和稀疏视图重建（3/6/9 视图）。
    *   **对比对象**：包括原始 3DGS、FSGS（正则化方法）、DIFIX3D+（基于图像扩散的 SOTA 方法）等。
*   **评价指标**：PSNR、SSIM、LPIPS、FID。

### 4. 资源与算力
*   **硬件**：使用了 **1 张 NVIDIA A100 GPU**。
*   **训练细节**：在 $576 \times 1024$ 分辨率下训练了 10 万次迭代，学习率为 $2 \times 10^{-5}$，Batch Size 为 1。
*   **推理效率**：在 A100 上处理一张 $1237 \times 822$ 的图像约需 **1.2 秒**，峰值显存占用为 **21.13 GB**。

### 5. 实验数量与充分性
*   **实验规模**：论文进行了多维度的评估，包括在两个主流数据集上的定量对比、针对不同稀疏度（3/6/9 视图）的鲁棒性测试。
*   **消融实验**：非常充分。作者分别验证了代理查询的有效性、局部窗口大小的影响（$k=1,3,5,7$）、以及与传统极线注意力（Epipolar Attention）的对比。
*   **客观性**：通过区域级 PSNR 分析（将图像分为高误差区和低误差区）深入探讨了“查询污染”的影响，证明了该方法在修复严重受损区域的同时，不会像基准方法那样破坏原本正确的区域。

### 6. 主要结论与发现
*   **解决污染**：通过引入几何代理查询，GeoQuery 成功打破了“受损查询导致错误检索”的恶性循环。
*   **性能领先**：在伪影去除任务上，比 SOTA 方法 DIFIX3D+ 提升了 **1.09 dB (PSNR)** 并显著降低了 FID。
*   **极端稀疏下的鲁棒性**：在极具挑战性的 **3 视图** 场景下，GeoQuery 表现出极强的稳定性，有效防止了基准方法中常见的几何坍塌现象。

### 7. 优点（亮点）
*   **物理与生成的结合**：巧妙地将显式几何约束（深度/位姿）嵌入到隐式生成模型（扩散模型）中，兼具了两者的长处。
*   **局部性约束**：引入局部窗口注意力不仅提高了匹配精度，还将计算复杂度从 $O(N^2)$ 降低到 $O(Nk^2)$，提升了处理高分辨率图像的效率。
*   **即插即用**：该框架可以无缝集成到现有的基于扩散的 3D 重建流水线中。

### 8. 不足与局限
*   **对几何质量的依赖**：方法依赖于预先估计的深度图。在无纹理区域或高反光表面，深度估计可能失效，从而导致几何引导失效。
*   **极端视点差异**：当目标视图与参考视图之间存在巨大遮挡或视点跨度过大时，由于缺乏重叠区域，模型将退化为纯生成模式，此时质量取决于扩散模型的幻觉能力。
*   **计算开销**：虽然比全局注意力快，但 1.2 秒/帧的修复速度对于实时交互式应用仍有提升空间。

（完）
