---
title: Z-Order Transformer for Feed-Forward Gaussian Splatting
title_zh: 用于前馈高斯泼溅的 Z-Order Transformer
authors: "Can Wang, Lei Liu, Wei Jiang, Dong Xu"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13465v1"
tags: ["query:ff3dr"]
score: 10.0
evidence: 用于3D重建的前馈高斯泼溅
tldr: 针对传统3D高斯泼溅（3DGS）优化速度慢及现有前馈方法渲染质量差、冗余度高的问题，本文提出了一种基于Z-order Transformer的前馈架构。该方法通过Z-order策略将无序高斯点集转化为空间相干序列，利用稀疏注意力机制捕捉空间语义关系，并能自适应抑制冗余，实现了高效、高质量的单次前馈3D重建。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.13465v1/fig-001.webp\", \"caption\": \"Table 1. Quantitative Comparison with Varying Numbers of Views. We highlight the best results in red and the second-best in yellow.\", \"page\": 6, \"index\": 1, \"width\": 916, \"height\": 380}, {\"url\": \"assets/figures/arxiv/2605.13465v1/fig-002.webp\", \"caption\": \"Figure 2. Gaussian representations in feed-forward GS.\", \"page\": 2, \"index\": 2, \"width\": 422, \"height\": 526}, {\"url\": \"assets/figures/arxiv/2605.13465v1/fig-003.webp\", \"caption\": \"Figure 5. Visual Comparison. Our method achieves better performance in capturing sharp edges and intricate details.\", \"page\": 7, \"index\": 3, \"width\": 1002, \"height\": 391}, {\"url\": \"assets/figures/arxiv/2605.13465v1/fig-004.webp\", \"caption\": \"Figure 3. Framework. Given multi-view images, our method first utilizes a transformer encoder and a depth head to generate depth maps, which are then projected into a 3D point map based on the camera. Next, global features are extracted from the transformer encoder, and geometry features are derived from the depth head. These features, along with the pixel color and point map, are processed through our ZFormer blocks to generate Z-order-based Gaussian representations. Finally, the Gaussian representations are passed to the Gaussian head, which generates multi-level GS for further rendering.\", \"page\": 3, \"index\": 4, \"width\": 1010, \"height\": 506}, {\"url\": \"assets/figures/arxiv/2605.13465v1/fig-005.webp\", \"caption\": \"Figure 6. Visual Ablation Study. Results of ablations on different components of our method.\", \"page\": 8, \"index\": 5, \"width\": 1012, \"height\": 210}, {\"url\": \"assets/figures/arxiv/2605.13465v1/fig-006.webp\", \"caption\": \"Figure 7. Ablation Study on Layer Selection. We use two Z-order blocks in our framework to prevent degradation while achieving a lower number of GS primitives.\", \"page\": 8, \"index\": 6, \"width\": 409, \"height\": 469}, {\"url\": \"assets/figures/arxiv/2605.13465v1/fig-007.webp\", \"caption\": \"Figure 4. ZFormer Block. The ZFormer block begins by serializing and ordering the 3D points, features, and pixel colors using Z-order. The serialized data is then passed through a sparse attention mechanism, including group attention, and top-K attention. After the attention steps, Z-order pooling is applied to further aggregate the features.\", \"page\": 4, \"index\": 7, \"width\": 452, \"height\": 386}]"
motivation: 传统3DGS依赖缓慢的迭代优化，而现有前馈方法在处理高斯基元冗余和渲染质量方面存在局限。
method: 引入Z-order策略将高斯点组织为空间相干序列，并结合Transformer的稀疏注意力机制进行上下文建模与基元压缩。
result: 实验证明该方法能以更少的高斯基元实现快速且高质量的新视角合成。
conclusion: Z-order Transformer有效地平衡了前馈3DGS的计算效率与重建精度，为实时3D场景生成提供了新方案。
---

## 摘要
近期 3D 高斯泼溅（3DGS）的进展在逼真新视角合成方面取得了显著进步。然而，传统的 3DGS 依赖于缓慢的迭代优化过程，这限制了其在需要实时结果的场景中的应用。为了克服这一瓶颈，近期的前馈方法旨在直接从图像中预测高斯属性，但它们往往在处理高斯基元的冗余和渲染质量方面面临困难。在这项工作中，我们介绍了一种专门为前馈高斯泼溅设计的基于 Transformer 的架构。我们的核心见解是，通过 Z-order 策略将无结构的高斯集合组织成空间相干序列，可以利用稀疏注意力机制有效捕捉高斯之间的空间和语义关系。此外，我们结合了这种 Z-order 策略，在保留关键结构细节的同时自适应地抑制冗余。这使得 Transformer 能够在单次前馈过程中高效地建模上下文、压缩高斯基元并预测高斯属性。全面的实验表明，我们的方法能够以更少的高斯基元实现快速且高质量的新视角合成。

## Abstract
Recent advances in 3D Gaussian Splatting (3DGS) have enabled significant progress in photorealistic novel view synthesis. However, traditional 3DGS relies on a slow, iterative optimization process, which limits its use in scenarios demanding real-time results. To overcome this bottleneck, recent feed-forward methods aim to predict Gaussian attributes directly from images, but they often struggle with the redundancy of Gaussian primitives and rendering quality. In this work, we introduce a transformer-based architecture specifically designed for feed-forward Gaussian Splatting. Our key insight is that spatial and semantic relationships among Gaussians can be effectively captured through a sparse attention mechanism, enabled by a Z-order strategy that organizes the unstructured Gaussian set into a spatially coherent sequence. Furthermore, we incorporate this Z-order strategy to adaptively suppress redundancy while preserving critical structural details. This allows the transformer to efficiently model context, compress Gaussian primitives, and predict Gaussian attributes in a single forward pass. Comprehensive experiments demonstrate that our method achieves fast and high-quality novel view synthesis with fewer Gaussian primitives.

---

## 论文详细总结（自动生成）

这是一份关于论文《Z-Order Transformer for Feed-Forward Gaussian Splatting》的结构化深入分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的 3D 高斯泼溅（3DGS）依赖于针对每个场景的迭代优化，过程缓慢且缺乏泛化能力。现有的前馈（Feed-forward）方法虽然能直接预测高斯参数，但面临两个主要挑战：
    1.  **冗余性**：基于像素的方法（如 PixelSplat）会产生海量高斯基元，导致内存和渲染开销巨大。
    2.  **质量与效率的权衡**：基于体素的方法（如 AnySplat）虽能减少冗余，但固定的体素网格会引入量化误差，导致细节模糊，且在处理稀疏区域时效率低下。
*   **研究动机**：提出一种能够高效组织无结构高斯点集、捕捉空间语义关系并自适应压缩冗余的前馈架构，以实现快速、高质量且轻量化的 3D 重建。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：引入 **Z-order（Morton 曲线）策略**，将 3D 空间中的点映射为一维序列。这种映射能保持空间局部性（即 3D 空间邻近的点在序列中也邻近），从而允许使用高效的 Transformer 机制处理 3D 数据。
*   **关键技术细节**：
    *   **Z-order 序列化**：将多视图投影生成的 3D 点云通过位交错（Bit-interleaving）编码为 Z-order 码并排序，将无序点集转化为空间相干的序列。
    *   **ZFormer Block（核心模块）**：
        *   **Group Attention**：在序列的局部块内进行注意力计算，捕捉局部空间细节。
        *   **Top-K Attention**：利用注意力权重选择最相关的块进行交互，降低全局计算复杂度。
        *   **Z-order Pooling**：通过对 Z-order 编码进行位移操作，将邻近的高斯点聚类并聚合，实现基元的有效压缩。
    *   **深度与特征提取**：采用 DINOv2 作为骨干网络，结合 DPT 头部预测深度图，并提取几何与全局特征。
    *   **最大覆盖视角选择算法**：在推理阶段，针对密集捕获的视图，利用 Z-order 覆盖率贪婪地选择最具代表性的视图，减少冗余计算。

### 3. 实验设计
*   **数据集**：
    *   **RealEstate10K**：室内外漫游场景。
    *   **DL3DV**：大规模 3D 视觉场景。
    *   **ACID**：用于跨数据集泛化性评估。
*   **Benchmark 与对比方法**：
    *   **优化类方法**：3DGS, MipSplatting。
    *   **前馈类方法**：DepthSplat, AnySplat, PixelSplat, MVSplat, NoPoSplat 等。
*   **评估指标**：PSNR（峰值信噪比）、SSIM（结构相似性）、LPIPS（感知相似度）、推理时间及高斯基元数量。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 张 NVIDIA A100 GPU**。
*   **训练细节**：
    *   迭代次数：100K 次。
    *   Batch Size：2。
    *   训练时长：约 **2 天**。
    *   优化器：AdamW，并对深度估计分支和 Transformer 分支设置了不同的学习率。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了 2、4、8、12 等不同输入视角下的定量对比。
    *   进行了跨数据集（RealEstate10K/DL3DV → ACID）的泛化性测试。
    *   **消融实验**：涵盖了 Z-order 模块必要性、稀疏注意力机制、SH 参数初始化策略、深度图联合训练以及 Z-order 层数选择等 6 组核心实验。
*   **充分性评价**：实验设计非常充分。不仅对比了最新的前馈方法，还通过消融实验验证了每个设计组件的有效性。对比维度涵盖了质量、速度和内存占用（基元数量），评估客观且全面。

### 6. 主要结论与发现
*   **性能领先**：在所有测试数据集上，该方法在 PSNR 和 SSIM 等指标上均优于现有的前馈方法（如 DepthSplat 和 AnySplat）。
*   **极高效率**：推理速度比传统 3DGS 快约 **1000 倍**。
*   **高压缩比**：在保持更高渲染质量的前提下，使用的高斯基元数量仅为同类方法的 **1/2 到 1/3**。
*   **泛化性强**：在未见过的 ACID 数据集上表现出优异的零样本泛化能力。

### 7. 优点（亮点）
*   **创新的序列化方案**：巧妙地将 Z-order 曲线引入 3DGS，解决了无结构点集难以用 Transformer 高效处理的难题。
*   **稀疏注意力设计**：结合 Group 和 Top-K 注意力，在捕捉长程依赖的同时保持了计算的高效性。
*   **自适应压缩**：Z-order Pooling 提供了一种比体素化更灵活、误差更小的基元聚合方式。
*   **实用性强**：提出的视角选择算法解决了实际应用中密集视图导致的推理冗余问题。

### 8. 不足与局限
*   **超高分辨率限制**：在处理超过 1K 分辨率的数据集时，受限于内存约束和模型复杂度，可能难以完美捕捉极细微的细节。
*   **压缩层数瓶颈**：实验发现增加 Z-order 块虽然能进一步减少高斯点数，但超过 2 层后会导致明显的性能下降（细节丢失）。
*   **深度依赖**：模型性能仍部分取决于初始深度估计的准确性，尽管联合训练有所缓解，但在极端遮挡场景下可能存在挑战。

（完）
