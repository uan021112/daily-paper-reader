---
title: "PointForward: Feedforward Driving Reconstruction through Point-Aligned Representations"
title_zh: "PointForward: 基于点对齐表示的前馈驾驶场景重建"
authors: "Cheng Chi, Xianqi Wang, Hongcheng Luo, Mingfei Tu, Gangwei Xu, Zehan Zhang, Bing Wang, Guang Chen, Hangjun Ye, Sida Peng, Xin Yang, Haiyang Sun"
date: 2026-05-12
pdf: "https://arxiv.org/pdf/2605.11594v1"
tags: ["query:ff3dr"]
score: 10.0
evidence: 通过点对齐表示进行前馈驾驶场景重建
tldr: PointForward是一个针对自动驾驶场景的高保真前馈重建框架。针对现有基于像素对齐的3D高斯泼溅方法存在的多视图不一致和动态建模缺乏一致性等问题，该方法通过在世界空间初始化稀疏3D查询并进行时空融合，实现了点对齐的表示。同时，利用场景图和3D边界框显式管理动态实例，显著提升了重建质量和时空一致性，在多个大规模基准测试中达到领先水平。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.11594v1/fig-001.webp\", \"caption\": \"Table 1: Quantitative results on Waymo. We compare image quality and geometry metrics against both per-scene optimization and feedforward methods. PSNR, SSIM, and D-RMSE are reported.\", \"page\": 7, \"index\": 1, \"width\": 826, \"height\": 791}, {\"url\": \"assets/figures/arxiv/2605.11594v1/fig-002.webp\", \"caption\": \"Figure 2: Overview of PointForward. We initialize sparse 3D queries in world space and project them onto multi-view image planes to aggregate features and geometric cues. A spatial-temporal fusion module produces coherent point-aligned representations, while scene-graph-based dynamic modeling ensures instance-level motion consistency for rendering dynamic driving scenes.\", \"page\": 4, \"index\": 2, \"width\": 826, \"height\": 468}, {\"url\": \"assets/figures/arxiv/2605.11594v1/fig-003.webp\", \"caption\": \"Figure 5: Qualitative extrapolated-view results on Waymo. The camera is left shifted 3 meters.\", \"page\": 9, \"index\": 3, \"width\": 829, \"height\": 598}]"
motivation: 现有的前馈3D重建方法因像素对齐范式导致多视图不一致，且在处理动态实例时缺乏显式的跨视图对应关系。
method: 提出点对齐表示法，通过在世界空间初始化3D查询并结合场景图与3D边界框，实现显式的跨视图一致性和动态实例运动传播。
result: 实验结果表明，PointForward在大规模自动驾驶基准测试上取得了最先进的重建性能。
conclusion: 该研究证明了点对齐表示和显式场景图管理在提升前馈驾驶场景重建质量和一致性方面的有效性。
---

## 摘要
高保真驾驶场景重建对自动驾驶至关重要。虽然最近的前馈 3D 高斯泼溅 (3DGS) 方法实现了快速重建，但其逐像素高斯预测范式通常面临多视图不一致和分层伪影的问题。此外，现有方法通常通过密集流预测来建模动态实例，这缺乏显式的跨视图对应关系和实例级一致性。在本文中，我们提出了 PointForward，这是一个通过点对齐表示实现的前馈驾驶重建框架。与像素对齐方法不同，我们在世界空间中初始化稀疏 3D 查询，并通过时空融合将多视图图像信息聚合到这些查询上，从而在单次前馈过程中强制执行显式的跨视图一致性。为了处理场景动态，我们引入了场景图，在重建过程中显式地组织移动实例。通过利用 3D 边界框，我们的方法实现了实例级运动传播和时间一致的动态表示。大量实验表明，PointForward 在大规模驾驶基准测试中达到了最先进的性能。代码将在论文发表后公开。

## Abstract
High-fidelity reconstruction of driving scenes is crucial for autonomous driving. While recent feedforward 3D Gaussian Splatting (3DGS) methods enable fast reconstruction, their per-pixel Gaussian prediction paradigm often suffers from multi-view inconsistency and layering artifacts. Moreover, existing methods often model dynamic instances via dense flow prediction, which lacks explicit cross-view correspondence and instance-level consistency. In this paper, we propose PointForward, a feedforward driving reconstruction framework through point-aligned representations. Unlike pixel-aligned methods, we initialize sparse 3D queries in world space and aggregate multi-view image information via spatial-temporal fusion onto these queries, enforcing explicit cross-view consistency in a single feedforward pass. To handle scene dynamics, we introduce scene graphs that explicitly organize moving instances during reconstruction. By leveraging 3D bounding boxes, our method enables instance-level motion propagation and temporally consistent dynamic representations. Extensive experiments demonstrate that PointForward achieves state-of-the-art performance on large-scale driving benchmarks. The code will be available upon the publication of the paper.

---

## 论文详细总结（自动生成）

这是一份关于论文《PointForward: Feedforward Driving Reconstruction through Point-Aligned Representations》的结构化深入总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在保证速度的前提下，实现高保真、多视图一致且动态协调的自动驾驶场景重建。
*   **研究背景**：
    *   早期的 NeRF 或 3DGS 方法依赖于单场景优化，耗时数小时，无法满足大规模部署需求。
    *   现有的**前馈（Feedforward）**方法多采用“像素对齐（Pixel-aligned）”范式，即从每个像素独立预测高斯基元。这导致同一物理点在不同视角下预测的 3D 位置存在微小偏差，产生分层、重影伪影。
    *   **动态建模缺陷**：现有方法多依赖 2D 掩码和密集流（Dense Flow）预测，缺乏实例级的 3D 运动一致性。

### 2. 方法论：点对齐表示与场景图
PointForward 的核心思想是从“像素预测高斯”转向“空间查询聚合特征”。
*   **点对齐表示（Point-Aligned Representation）**：
    *   在世界空间中初始化一组稀疏的 **3D 查询（Queries）**，而非依赖像素。
    *   将这些 3D 查询投影到所有输入视图中，聚合图像特征和几何线索。
    *   通过**时空融合（Spatial-Temporal Fusion）**模块，将多视图信息整合到每个查询中，确保全局一致性。
*   **基于场景图的动态建模**：
    *   利用 3D 边界框（Bounding Boxes）将场景分解为静态背景和动态实例。
    *   **规范化空间（Canonical Space）**：将动态实例的查询转换到其自身的局部坐标系中，实现跨时间的运动传播。
    *   **寿命建模（Lifespan Modeling）**：为每个高斯预测一个寿命参数 $\sigma$，用于处理动态物体的可见性变化。
*   **渲染流程**：
    *   将更新后的查询解码为 3D 高斯参数（位置、颜色特征、旋转、缩放、不透明度）。
    *   使用轻量级 UNet 将渲染的高维特征图转换为最终的 RGB 图像。

### 3. 实验设计
*   **数据集**：
    *   **Waymo Open Dataset**：主要实验平台，包含 798 个训练序列和 202 个验证序列。
    *   **nuScenes**：用于验证零样本（Zero-shot）泛化能力和微调性能。
*   **基准指标**：PSNR（峰值信噪比）、SSIM（结构相似性）、D-RMSE（深度均方根误差）以及外推视角下的 FID。
*   **对比方法**：
    *   **单场景优化类**：EmerNeRF, 3DGS, PVG, DeformableGS 等。
    *   **前馈类（SOTA）**：STORM, DGGT, AnySplat, GS-LRM 等。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA H20 GPU**。
*   **训练细节**：
    *   训练迭代次数：45K 次。
    *   全局 Batch Size：8。
    *   学习率：$5 \times 10^{-4}$，采用 AdamW 优化器和余弦退火调度。
    *   训练时长：文中未明确给出具体小时数，但提到前馈推理时间在亚秒级。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 Waymo 上进行了全图像和动态区域的定量对比。
    *   在 nuScenes 上进行了跨数据集的零样本泛化和微调实验。
    *   进行了**外推视角（Extrapolated View）**实验（相机平移 1m 到 6m），验证几何稳定性。
    *   **消融实验**：验证了时空融合、深度感知融合、寿命参数以及 LiDAR 深度依赖性的影响。
*   **评价**：实验设计非常充分且客观。通过引入外推视角和跨数据集测试，有力证明了“点对齐”比“像素对齐”具有更强的几何鲁棒性。

### 6. 主要结论与发现
*   **性能领先**：在 Waymo 上，PointForward 的 PSNR 达到 28.48，比之前的 SOTA 方法 DGGT 高出 1.1 dB；在动态区域提升更显著（+2.2 dB）。
*   **几何一致性**：点对齐表示能有效消除重影和分层伪影，尤其在视角大幅度变化时表现稳定。
*   **低依赖性**：即使不使用 LiDAR 深度，仅靠单目深度估计模型，该方法依然能超越现有的前馈方法。

### 7. 优点与亮点
*   **范式创新**：将自动驾驶检测中的“稀疏查询（Sparse Query）”思想引入重建领域，解决了像素对齐的不一致性痛点。
*   **动态一致性**：通过场景图和规范化空间，实现了真正的实例级运动建模，而非破碎的像素流。
*   **泛化能力强**：在 nuScenes 的零样本测试中表现优异，证明了模型学习到了通用的几何先验。

### 8. 不足与局限
*   **非刚性运动限制**：目前的动态建模主要针对 3D 边界框定义的刚性物体（如车辆），对于行人等高度非刚性变形的物体处理效果较弱。
*   **依赖外部输入**：模型需要 3D 边界框作为先验（虽然可以由检测器提供），这在完全端到端的无监督场景下可能存在限制。
*   **计算开销**：虽然是前馈模型，但初始化大量 3D 查询并进行多视图投影聚合，在查询数量极高时可能会增加显存压力。

（完）
