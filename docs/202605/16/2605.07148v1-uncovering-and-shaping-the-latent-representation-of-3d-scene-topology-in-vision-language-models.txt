Title: Uncovering and Shaping the Latent Representation of 3D Scene Topology in Vision-Language Models

URL Source: https://arxiv.org/pdf/2605.07148v1

Published Time: Mon, 11 May 2026 00:34:05 GMT

Number of Pages: 32

Markdown Content:
# Uncovering and Shaping the Latent Representation of 3D Scene Topology in Vision-Language Models 

Haoming Wang Wei Gao 

Department of Electrical and Computer Engineering University of Pittsburgh Pittsburgh, PA 15261 

{hw.wang, weigao}@pitt.edu 

Abstract 

Decades of cognitive science establish that humans navigate environments by form-ing cognitive maps, defined as allocentric and topology-preserving representations of 3D space. While modern Vision-Language Models (VLMs) demonstrate emer-gent spatial reasoning from 2D egocentric inputs, it remains unclear whether they construct an analogous 3D internal representation. In this paper, we demonstrate that current VLMs do possess a latent topological map of 3D scenes, but it is heavily overshadowed by non-geometric visual semantics, such as color and shape. By isolating this spatial subspace through cross-scene linear feature extraction, we extract a clean spatial subspace that causally controls the model’s spatial outputs. We mathematically shape this latent representation and prove its correspondence to the Laplacian eigenmaps of the scene’s 3D Gaussian-kernel graph, converging to the physical 3D space in the continuous limit. Motivated by this geometric identification, we further introduce a mathematically principled latent regulariza-tion method for VLMs, based on Dirichlet energy. Applying this single-term regularizer to a minimal 500-step supervised VLM fine-tuning (SFT) on simple synthetic data yields significant improvements on real-world spatial benchmarks, outperforming standard SFT and competitive baselines by up to 12.1% in spa-tial tasks involving scene topology understanding. Source code is available at https://github.com/pittisl/vlm-latent-shaping 

1 Introduction Egocentric View  Cognitive map of scene topology 

Figure 1: The human brain compresses egocentric visual inputs into a low-dimensional and topology-preserving cognitive map Decades of cognitive science establish that hu-mans and other animals navigate complex 3D environments by forming an internal cognitive map — an allocentric representation of the spa-tial relations among objects that can be queried without visually perceiving the scene [ 1 , 2]. Rather than individual pixels, image patches or metric distances, what such a cognitive map preserves is primarily the scene topology , de-fined as the qualitative graph of “what is next to what” that encodes relative positions and neigh-borhood structures [ 3, 4 ], as shown in Figure 1. By constructing the cognitive map, the human brain compresses complex visual inputs into a low-dimensional representation, hence supporting efficient but precise spatial understanding and reasoning. Nowadays, Vision-Language Models (VLMs) [ 5 , 6] can succeed on simple spatial reasoning tasks given only egocentric visual inputs instead of dedicated 3D geometric information (e.g., depth estimation) [ 7, 8, 9], but they fail on more difficult tasks in complex scenes with many objects, partial views and cluttered layouts [ 10 , 11 ]. The natural question, then, is whether VLMs have topology-  

> Preprint.
> arXiv:2605.07148v1 [cs.CV] 8 May 2026 Egocentric visual input VLM Activation of tokens from different objects
> Probing of latent representation
> Recovered scene topology
> Ground truth topology
> Diagnostic signal
> Improving VLM’s ability in spatial perception and reasoning

Figure 2: Uncovering and shaping the latent representation of 3D scene topology using a feature extraction probe as diagnostic signal preserving latent representations of the scene’s 3D structure that are analogous to the cognitive map in human brain. It is important to know where such representations reside, and whether we can mathematically or experimentally shape them to improve the model’s spatial reasoning abilities. Existing diagnostic tools fall short of answering these questions. Attention-map analyses on VLMs reveal which 2D image patches the answer token attends to, but cannot reveal whether the model internally binds those image patches to a 3D allocentric representation [ 12 , 13 , 14 ]. Mechanistic interpretability for multimodal models has identified linear circuits for relational reasoning [ 15 , 16 ,17 ], but these analyses remain anchored to the 2D image grid or text features, and stop short of asking whether the residual stream encodes the scene’s intrinsic 3D topology. End-to-end VQA accuracy, in particular, is itself an unreliable proxy, as VLMs are known to exploit caption-style co-occurrence priors, distractor-set artifacts, and language shortcuts that may result in good accuracy on certain tasks but lack generalizability [18, 19, 20]. Instead, we need a direct probe of latent representations in model’s activations, which (i) does not rely on the model’s text output, (ii) is benchmark-agnostic, and (iii)establishes a precise mathematical equivalence to the physical 3D scene layout. Our findings showed that the existing VLMs do encode information about the 3D scene topology, but this representation is severely overshadowed by spatially irrelevant visual semantics (§3). To bypass this semantic entanglement, as shown in Figure 2, this paper introduces a linear feature extraction technique that isolates visual semantics from latent representations of 3D scene topology. More specifically, we consider the same object’s non-spatial attributes (e.g., color, shape, etc) as constant across different scenes, and average the object-token activations to project out the irrelevant visual semantics and extract a dominant spatial identity subspace. Experiment results validated that principal components of the extracted spatial subspace cleanly recover the physical 3D scene layout (§4). Furthermore, we analytically proved that this extracted spatial subspace optimally aligns with the Laplacian eigenmaps of the scene’s 3D graph (Theorem 1) and converges to 3D coordinate space (Theorem 2). Consequently, we introduce a Dirichlet-energy regularizer to fine-tune the VLM’s “spatially relevant” layers. This regularizer can be theoretically ensured to guarantee the task reliability, drastically reduce the sample complexity and minimize risk (Theorems 3–5). Our experiment results show that just 500 supervised fine-tuning (SFT) steps with LoRA on simple synthetic data could successfully reshape the internal geometry in VLM’s latent representations, and yield significant improvements on real-world spatial benchmarks such as VSI-Bench [ 21 ] and MindCube [ 22 ], without architectural changes or introducing extra 3D modalities to the model itself (§5). In spatial tasks involving scene topology understanding, such improvements could be up to 12.1%, compared to standard SFT and competitive baselines of spatial encoding. 

2 Related Work 

2.1 Spatial Reasoning in Vision-Language Models 

While cognitive science highlights that biological agents navigate using topology-preserving “cog-nitive maps” [ 1 , 2, 4, 3], imparting such allocentric 3D understanding to VLMs has largely been approached as an explicit architectural or brute-force data scaling problem. Recent approaches attempt to force 3D awareness by appending depth modalities [ 22 , 23 ] or fusing multi-vision en-coders [ 5, 6, 24 ]. Other efforts scale up spatial capabilities by pretraining on large amounts (e.g., billions) of synthesized spatial QA samples [ 7, 9 , 10 , 25 ], or utilizing reinforcement learning (RL) with programmatic spatial rewards [ 26 , 10 , 27 ]. While highly effective, these approaches rely heavily on injecting metric 3D data or altering standard input pipelines. In contrast, guided by the topological nature of biological spatial representation, our work explores whether standard VLMs naturally form latent 3D representations [ 14 , 15 ] solely from 2D egocentric visual inputs, avoiding the need for dedicated external 3D modules. 22.2 Mechanistic Interpretability of Geometry in VLMs 

Mechanistic interpretability seeks to reverse-engineer the latent algorithms learned by neural net-works [ 28 , 29 , 30 ]. In multimodal models, researchers have utilized attention-map analyses to explore how VLMs ground textual concepts in image patches [ 12 , 18 , 19 , 13 ], and recent works strived to directly map between spatial and relational representations [ 17 , 31 , 30 ]. [ 32 ] demonstrated that ViT patch embeddings encode an is-same-object quadratic form to bind visual features, and Kang et al. [ 16 ] identified linear “spatial-ID” vectors that VLMs use to bind spatial locations to text tokens, showing that causal interventions on these IDs can flip a model’s spatial output. Despite these advances, existing analyses remain largely anchored to the 2D image grid (i.e., correspondence between 2D image patches and objects in the scene) or isolated within the vision encoder’s early binding mechanisms [ 20 ]. This leaves an open question about whether the downstream language modeling residual stream constructs a cohesive 3D allocentric understanding of the physical scene. 

3 VLMs’ Latent Representations of 3D Scene Topology 

In this section, we conduct preliminary experiments to determine whether pretrained VLMs already represent 3D scene topology in their latent space and how strong it is relative to the irrelevant semantics representations that the model also carries. Synthetic scene  Rendered frames with random camera paths 

> …

Figure 3: Synthetic spatial data 

Experiment Setup: To rigorously evaluate the internal representations of VLMs without the confounding variables of real-world images (e.g., lighting, textures, background context, occlusion, etc) [ 33 , 34 ], we built and used a fully controllable synthetic spatial dataset, namely SynSpat3D ,to decouple a model’s purely geometric understanding from its semantic visual priors. As shown in Figure 3, each synthetic sample is generated by a two-step pipeline: (1) Scene generation : 6 to 12 objects are randomly placed in a bounded 3D coordinate space, with simple shapes (e.g., cube, sphere) and colors (e.g., red, blue) acting as the only independent sources of variance. (2) Frame generation : We render the scene using randomly generated camera paths to provide egocentric 2D views. This dataset contains 1,000 distinct scenes, each of which is paired with 5 programmatically generated spatial questions, yielding a total of 5,000 video-question pairs. More details can be found in Appendix A. Using this dataset, we examine the residual-stream activations at every decoder layer of two pretrained VLMs, namely Qwen2.5-VL-7B [ 35 ] and InternVL3-8B [ 36 ], which are widely adopted open-sourced VLMs with strong baseline spatial reasoning capabilities. color 

> (4-way)
> shape
> (8-way)
> x position
> (R 2)
> z position
> (R 2)
> 3D pairwise
> RSA
> 0
> 0.25
> 0.5
> 0.75
> 1.0
> Probe score  (accuracy  or  R2)
> +0.51
> +1.00
> +0.09
> +0.28
> +0.04

Figure 4: VLMs preferentially encode irrel-evant visual features over 3D position 

Dominance of spatially irrelevant features. We first apply direct linear probing [ 29 ] at different layers to quantify the representational richness of spatially relevant features (3D positions) versus spatially irrel-evant semantics (color, shape, etc). To do so, we train probes on the activations of image patches to predict each of these targets: object color, object shape, phys-ical x-coordinate, physical z-coordinate, and pairwise 3D distance using representational similarity analysis (RSA) that measures the relative distance between ob-jects. As shown in Figure 4, the results reveal a stark representational imbalance. The visual identity of an object is recoverable from activations with near-perfect accuracy, but its true 3D position can only be partially recovered. Analyzing the activation variance captured by each feature’s optimal low-rank subspace further highlights this discrepancy (details are in Appendix B). 3Scene Video Vision encoder Activations Object mask Weighted average for each object Frame Patching Magenta sphere 𝒉 𝒍 𝒐 𝟎 𝒉 𝒍 𝒐 𝟏 𝒉 𝒍 𝒐 𝟐 𝒉 𝒍 𝒐 𝟑 𝒉 𝒍 𝒐 𝟒 𝒉 𝒍 𝒐 𝟓 𝒉 𝒍 𝒐 𝟔 𝒉 𝒍 𝒐 𝟕 

> Object activations Yellow sphere Green cube Magenta cube Orange cylinder Blue cylinder Cyan cylinder Blue sphere VLM Backbone

Figure 5: Computation of per-object representation 

VLM outputs in spatial reasoning may follow semantics instead of 3D positions: To determine whether this representational imbalance dictates the VLM’s downstream reasoning, we conduct a behavioral counterfactual experiment [ 28 ]. We evaluate the model across 50 scenes under three conditions: the original scene, a color-swap variant (colors permuted, 3D positions preserved), and a 

position-swap variant (colors preserved, 3D layout scrambled). We found that simply shuffling the object colors can flip the model’s spatial predictions in 41.0% of cases on distance-order questions. This semantic disruption is nearly as severe as physically scrambling the 3D layout, which yields a 48.6% flip rate. This extreme sensitivity provides direct behavioral evidence that the VLM fails to cleanly separate “where” an object is from “what” it looks like. Instead of querying a robust geometric map, the language modeling head relies heavily on dominant color representations, rendering its spatial reasoning brittle to superficial semantic changes. More details about this can be found in Appendix B. 

4 Linear Spatial Feature Extraction via Cross-Scene Averaging 

To address such representational imbalance, we aim to cleanly disentangle the latent spatial repre-sentation from the dominant non-geometric representations, and our approach is a linear feature extraction technique based on cross-scene averaging that projects out the irrelevant visual semantics. Since a VLM’s latent space is fundamentally shaped by its pretraining objectives to form consistent, high-dimensional geometric structures, the extracted spatial subspace corresponds to how a VLM in-herently maps 2D visual tokens into a cohesive allocentric 3D understanding, and is hence dependent only on the VLM’s internal weights and architecture rather than the dataset being used in extraction. As a result, we use the SynSpat3D dataset built in Section 3 for extraction, and validate the efficacy of the extracted spatial subspace through both qualitative geometric visualizations and quantitative formal analysis. In Section 5, we will further mathematically formalize this subspace to derive a Dirichlet-energy regularizer, demonstrating that enforcing this topology-preserving penalty in VLM fine-tuning can significantly improve the VLM’s spatial reasoning abilities in real-world scenes. 

4.1 Per-object Representation 

Let S denote the set of scenes and let object identity o ∈ O index a colored geometric primitive (e.g. “red cube”). Each scene 1 s ∈ S contains m objects {oi}mi=1 at 3D positions X(s) = ( x(s)1 , . . . , x (s) 

> m

) ∈

R3×m. Unlike texts where discrete concepts map cleanly to individual tokens, visual objects in VLMs are distributed across continuous spatio-temporal patch grids, requiring a deliberate aggregation strategy to isolate their representations. As shown in Figure 5, we feed the VLM with a sequence of rendered frames {I(s) 

> t

}Tt=1 . Each frame is patchified by the vision encoder onto a gh ×gw patch grid, and consecutive frames are fused so that 

T input frames produce T /τ temporal tokens 2. Let h(ℓ) 

> p,t

∈ Rd denote the activation at decoder layer 

ℓ of the visual token at patch indexed by p ∈ P := {1, . . . , g h} × { 1, . . . , g w} in temporal slot t. For each object oi in scene s we form a mask–driven, coverage-weighted pool over visual tokens: 

h(s,o i) 

> ℓ,t

=

P  

> p:c(s)
> oi,p,t ≥κ

c(s) 

> oi,p,t

h(ℓ)

> p,t

P  

> p:c(s)
> oi,p,t ≥κ

c(s)

> oi,p,t

, c(s) 

> oi,p,t

= patch p ∩ mask (s)

> oi,t

patch p

, (1)       

> 1Each scene contains at most one instance of a given identity.
> 2τis used for VLM backbones with a temporal-patch merger of size τ. For example, τ=2 in Qwen2.5-VL.

4where c(s) 

> oi,p,t

∈ [0 , 1] is the fraction of patch p’s pixels belonging to object oi in the ground-truth mask (temporal-slot averaged), and κ is a coverage threshold that excludes patches grazed by an object’s silhouette. We then average across temporal slots to obtain the per-object activation :

h(s,o i) 

> ℓ

= 1

T /τ 

XT /τ t=1 h(s,o i) 

> ℓ,t

∈ Rd, H(s) 

> ℓ

= h(s,o 1) 

> ℓ

, . . . , h (s,o m)

> ℓ

⊤ ∈ Rm×d, (2) where d is the LM hidden size . H(s) 

> ℓ

is the extracted activation matrix for objects in different layers. 

4.2 Disentangling the Spatial Representation Observation of linear decomposition: Recent works of mechanistic interpretability [ 37 , 38 , 39 ,40 , 41 ] observed that, in mainstream model architectures and both text and vision domains, high-level human-interpretable features are encoded as approximately linear directions in the latent space [16, 31, 42, 43, 44]. This means that for an object’s representation, it can be decomposed additively into an identity-attribute component (carrying properties that depend only on o such as color, shape, lexical identity, etc) and a spatial component (carrying the object’s 3D position in scene s): 

h(s,o ) 

> ℓ

= uid (o) + usp 

 x(s)

> o

 + ε(s,o ), (3) where uid : O → Rd is identity-conditional, usp : R3 → Rd is position-conditional, and ε(s,o )

collects interactions and noise. Hence, to isolate the spatial component usp , we first compute the identity-conditional component uid , by averaging object activations across randomized scenes. 

Cross-scene averaging: Because the 3D layouts in our synthetic dataset are generated uniformly at random, the spatial component usp for a specific object identity has an expected value of zero [ 16 ]. Consequently, averaging an object’s activations across a large corpus of scenes effectively cancels out its position-dependent variance, leaving only the invariant identity features. Thus, we have 

¯h(o) 

> ℓ

≜ 1

|S o|

X

> s∈S o

h(s,o ) 

> ℓ

= uid (o) + 1

|S o|

X

> s∈S o

usp 

 x(s)

> o

| {z }   

> →0as |S o|→∞

+ O(|S o|−1/2), (4) so the cross-scene mean activation ¯h(o) 

> ℓ

is a consistent estimator of the identity-attribute prototype 

uid (o). We collect the per-identity prototypes into a matrix ¯Hℓ = [¯h(o1) 

> ℓ

, . . . , ¯h(oK ) 

> ℓ

]⊤ ∈ RK×d

over the K unique identities in our object inventory. 

Identity-attribute basis W : Having estimated the identity prototypes, we next construct an orthog-onal basis for this spatial-irrelevant subspace so that it can be cleanly projected out. A thin SVD 

¯H⊤ 

> ℓ

= U ΣV ⊤ gives the principal directions of the prototype cloud; we keep the top-k left singular vectors as columns of an orthonormal basis: 

Wℓ ∈ Rd×k, W ⊤ 

> ℓ

Wℓ = Ik, span( Wℓ) ≈ span {uid (o) : o ∈ O} , (5) and the specific choice of k is discussed in Appendix E. 

Linear spatial feature extraction ˜h(s,o ) 

> ℓ

: The orthogonal projector onto the identity-attribute subspace is PW = WℓW ⊤ 

> ℓ

, and the projector away from it is P⊥ ≜ Id − WℓW ⊤ 

> ℓ

. We then define the spatially-extracted (or spatial-feature for short) per-object activation as 

˜h(s,o ) 

> ℓ

≜ P⊥ h(s,o ) 

> ℓ

= h(s,o ) 

> ℓ

− WℓW ⊤ 

> ℓ

h(s,o ) 

> ℓ

, (6) which under Eq. (3) approximately equals usp (x(s) 

> o

) + ε(s,o ) — the spatial-relevant component plus residual noise. The extracted per-scene object matrix is then ˜H(s) 

> ℓ

= H(s) 

> ℓ

P⊥.

4.3 Results of Linear Spatial Feature Extraction Qualitative visualization of the spatial subspace: To visualize the geometry in the spatial sub-space, we collect the extracted per-object activations {˜h(s,o ) 

> ℓ

} in different scenes and run PCA: 

˜Hℓ = U ΣV ⊤, Z = ˜Hℓ V:,1:3 ∈ RN ×3. (7) Each row of Z is the 3D PCA embedding of one object-token in one scene and N = P 

> s

m(s). Figure 6 plots Z colored by the object’s true 3D position, and demonstrates that with our linear spatial extraction, the resulting spatial subspace neatly unfolds at the middle layers of the VLM and mirrors the physical scene layout. More visualization examples are in Appendix D. 5Ground Truth 

> Early layers
> Middle layers
> Latter layers
> =
> ≠
> ≠
> Circular Layout
> Ground Truth
> Early layers
> Latter layers
> =
> ≠
> ≠
> Grid Layout
> Ground Truth
> Early layers
> Middle layers
> Latter layers
> =
> ≠
> ≠
> Random Layout
> Middle layers

Figure 6: Visualization of extracted spatial feature in different layers of VLM 

Quantitative metrics of topology similarity: Besides the qualitative visualization above, we also measure topology-faithfulness of ˜H(s) 

> ℓ

to the scene’s true 3D coordinates X(s) using Dirichlet energy [ 45 , 46 ]. We define a Gaussian-kernel graph over the scene’s objects as Wij = κτ (xi, x j ) = exp 



− ∥xi−xj ∥2 

> 2τ2



. With the degree matrix Dii = P 

> j

Wij and the graph Laplacian L = D − W , the Dirichlet energy of an object matrix ˜H(s) 

> ℓ

is 

EX ( ˜H(s) 

> ℓ

) = 12

X 

> i,j

Wij ∥˜h(s,o i) 

> ℓ

− ˜h(s,o j ) 

> ℓ

∥2 = tr   ˜H(s) ⊤ 

> ℓ

L ˜H(s)

> ℓ

. (8) 0 5 10 15 20 25   

> Decoder layer
> 0.88
> 0.90
> 0.92
> 0.94
> 0.96
> 0.98
> 1.00
> Dirichlet energy / null  permutation null
> Qwen2.5-VL-7B InternVL3-8B LLaVA-OV-7B

(a) Dirichlet energy in different layers 0 5 10 15 20 25   

> Decoder layer
> 0.92 0.94 0.96 0.98 1.00
> Dirichlet energy / null
> permutation null
> before disentanglement after disentanglement

(b) Effectiveness of disentanglement 

Figure 7: Dirichlet energy in different VLM layers A low Dirichlet energy indicates that closer objects in the physical 3D space are mapped to proximal points in the latent space, hence better preserving the scene’s physical topology. To normalize against scale and per-scene difficulty, we report the Dirichlet ratio against a permutation null, defined as 

ρdir ( ˜H(s) 

> ℓ

) = EX ( ˜H(s) 

> ℓ

)/EΠ∼U (Sm)

EX (Π ˜H(s) 

> ℓ

), estimated with 1,000 row-shuffles of ˜H(s) 

> ℓ

scene. 

ρdir < 1 indicates the activation is more topology-faithful than a random binding of objects to rows. Being consistent with the visualizations in Figure 6, Figure 7 shows that the Dirichlet energy is minimized at these intermediate depths. Combining these findings with prior literature on model circuits and feature binding, we deduce a distinct functional division across the network: early layers extract low-level visual features, middle layers construct a spatially aware 3D understanding, and late layers engage in linguistic reasoning to generate textual outputs. Furthermore, we validate the effectiveness of our disentanglement approach by comparing the Dirichlet energy before and after extraction. As shown in Figure 7b, the spatial localization within the middle layers becomes substantially more pronounced after the spatially irrelevant semantics are projected out. 

Casual verification. To verify that the VLM actively uses the extracted spatial-aware subspace, we performed an casual intervention. As shown in Figure 8, by injecting a controlled vector along the spatial subspace’s principal axis into an object’s activation, the 3D-coordinate probe’s prediction shifted linearly and selectively along that targeted axis. In contrast, applying a matched control vector in a null direction produced no detectable effect. This confirms that the extracted subspace represents the actual 3D scene topology, and the model relies on this subspace for its internal 3D readout, rather than arbitrary representations. More details can be found in Appendix C. 60.3 0.2 0.1 0.0 0.1 0.2 0.3      

> steering (norm. xunits)
> 0.100
> 0.075
> 0.050
> 0.025
> 0.000
> 0.025
> 0.050
> 0.075
> 0.100
> probe-readout( x)
> vaxis (probe direction)
> v(null-space ctrl)

Figure 8: Causal verification on the VLM’s use of the spatial subspace 

5 Shaping the Spatial Subspace via Dirichlet-Energy Regularization 

With the extracted spatial subspace, we further explore possibilities of deliberately shaping it to enhance the VLM’s spatial reasoning capabilities. In this section, we mathematically formalize this spatial subspace to derive a quantitative penalty. We then demonstrate that enforcing this penalty in VLM fine-tuning yields measurable and theoretically guaranteed gains on spatial reasoning. 

5.1 The Target Representation of Laplacian Eigenmaps 

We first provide a mathematical formulation for the spatial subspace extracted in Section 4, to motivate why the Dirichlet energy is the ideal scalar to minimize in VLM fine-tuning. Eq. (8) described the Dirichlet energy of an object-token matrix H ∈ Rm×d on the scene’s 3D Gaussian-kernel graph W is 

EX (H) = tr( H⊤LH ). Geometrically, a low Dirichlet energy ensures that rows of H corresponding to spatially nearby objects (large Wij ) are themselves nearby in Rd. To formalize this, we seek the exact mathematical structure of the representation H∗ that minimizes this energy. 

Theorem 1 (Dirichlet minimization ⇒ Laplacian eigenmap PCA) . Let X ∈ Rm×3 be the 3D coordinates of m scene objects, Wij = κτ (xi, x j ) their Gaussian-kernel graph, and L = D − W the unnormalized graph Laplacian. Fix s ≤ min( m, d ) − 1 and consider the constrained optimization: 

H∗ ∈ arg min 

> H∈Rm×d

EX (H) subject to σk(H) ≥ ϵk for all k ∈ [s], (9) 

where σk(H) is the k-th singular value of H and ϵ1 > ϵ 2 > · · · > ϵ s > 0 are constants. Then: (a) The minimizer is unique up to right-orthogonal transformation. The left singular vectors satisfy 

uk(H∗) = z(k) for k = 1 , . . . , s , where z(k) is the k-th eigenvector of L ordered by smallest eigenvalues. (b) After mean-centering H∗ 

> c

≜ (I − 1 

> m

11 ⊤)H∗, the k-th principal component (left singular vector) of H∗ 

> c

exactly recovers z(k+1) for k = 1 , . . . , s − 1.

This theorem proves that if we force a representation to maintain some variance (the ϵk constraints prevent trivial collapse to a single point) while minimizing the Dirichlet energy, the optimal represen-tation H∗ will naturally align its principal components with the eigenvectors of the scene’s graph Laplacian. In practical VLM fine-tuning, the standard language loss ( LLM ) implicitly provides this non-degeneracy constraint, as the network cannot collapse its hidden states to zero without destroying its ability to predict text tokens. 

Proof Sketch. The full formal proof is in Appendix F and relies on the Ky Fan trace minimum theorem. (1) By expanding H into its Singular Value Decomposition ( H = U ΣV ⊤), the Dirichlet objective exactly decomposes along the orthonormal left-singular directions uk. (2) The lower-bound constraints σk ≥ ϵk freeze the singular values. (3) Applying the weighted Ky Fan theorem proves that the optimal orthonormal vectors uk must perfectly align with the eigenvectors of L corresponding to its smallest eigenvalues. (4) Mean-centering simply projects out the trivial uniform mode z(1) ∝ 1.This identifies the representation as a set of abstract graph eigenvectors. To connect back to physical vision, we consider the continuous limit: 

Theorem 2 (Continuous Limit to 3D Space) . Let {xi}mi=1 be i.i.d. samples from a smooth density 

ρ on a compact Riemannian manifold Ω ⊂ R3. Let L(τ ) be the Gaussian-kernel Laplacian with bandwidth τm. If τm → 0 and mτ 3+ αm → ∞ , then for any fixed k, with probability tending to 1, the 

7Overall accuracy (%) Qwen2.5-VL-7B InternVL3-8B VSI-bench MindCube VSI-bench MindCube Naive LoRA fine-tuning 29.3 41.3 36.7 45.2 2D regularization [16] 28.7 39.8 36.5 44.7 Text-domain cog map [22] 28.8 38.5 36.0 43.1     

> Ours 35.6 48.3 42.4 51.7

Table 1: Overall accuracy on spatial tasks 

discrete eigenvector z(k+1) (xi) converges uniformly to ρ(xi)−1/2ϕ(k)(xi), where ϕ(k) is the k-th non-constant eigenfunction of the weighted Laplace-Beltrami operator −∆ρ. For uniform ρ on a Euclidean cube, ϕ(1) , ϕ (2) , ϕ (3) are exactly the Cartesian coordinates x, y, z (up to a smooth cosine reparameterization). 

The vectors extracted by Theorem 1 correspond directly to physical 3D space. While real-world scenes feature more complex object densities, our probing and training datasets utilize uniformly distributed objects, hence allowing the Laplacian eigenmaps to naturally recover clean Cartesian axes. 

5.2 Loss Formulation and Realizability 

Motivated by that Dirichlet energy uniquely targets true 3D spatial axes, we introduce a training intervention that shapes the latent space toward this target in VLM fine-tuning. We add a single scalar Dirichlet-energy term to the standard LoRA cross-entropy loss at the cognitive-map layer ℓ:

L(θ) = LLM (θ) + λdir · E X

 P⊥Hℓ(θ), (10) where P⊥ is the cross-scene spatial-feature-extraction projector from Section 4 and W is the scene’s 3D Gaussian-kernel graph. Why does forcing the model into this specific representation improve downstream spatial reasoning? We prove that it guarantees the expressivity required by the LM head: 

Theorem 3 (Realizability of Spatial Readouts) . Let f : R3 → R be any linear function of the monotonic coordinate transformations (e.g., an axis-aligned ordinal comparison encoding “is object A to the left of object B”). For the Dirichlet-minimizing representation H∗ satisfying Theorem 2, and for every η > 0, there exists a linear functional ℓ : Rd → R such that: 

sup i∈[m] |ℓ(h∗ 

> i

) − f (xi)| < η with high probability as m → ∞ . (11) Since the latent space is reshaped to mirror the 3D world coordinates, spatial relational questions be-come linearly separable. The VLM’s linear language modeling head possesses the exact architectural capacity required to answer perfectly without needing deeper, non-linear geometric parsers. 

5.3 Experiment Setup 

To experimentally validate the efficacy of our proposed VLM training intervention, we utilize our proposed loss to fine-tune pretrained VLMs, namely Qwen2.5-VL-7B [ 35 ] and InternVL3-8B [ 36 ], using the SynSpat3D dataset introduced in Section 3, and evaluate zero-shot transfer to the following real-world spatial reasoning benchmarks: • VSI-Bench [ 21 ]: a comprehensive benchmark for models’ capabilities in fine-grained 3D spatial reasoning, relational judgments, and absolute distance estimation from egocentric video sequences. The evaluated tasks include: object counting ( obj-count ), object and room size estimation ( obj-size ,

room-size ), absolute and relative distance estimation ( abs-dist , rel-dist ), relative direction across varying difficulties ( rel-d ), appearance order ( appear-ord ), and route planning ( route ). • MindCube [ 22 ]: a benchmark designed to measure how well models can construct internal cognitive maps and perform spatial mental modeling given only restricted and partial 2D views. We train LoRA (rank-16) on the attention matrices for 500 steps. Trainable parameters are restricted strictly to the middle “spatial-aware” layers where the Dirichlet loss is applied, leaving the early visual binding and late linguistic reasoning layers untouched. We empirically identify these middle layers by selecting those with the lowest pre-intervention Dirichlet energy ratio, as established in our diagnostic probing (Figure 7). We sweep λdir to observe its effect, reporting the optimal settings in our main results and exploring the trade-off in Section 5.5. All results are averaged over n = 4 

random seeds. We compare our approach with the following baselines: 8• Naive LoRA fine-tuning , which relies exclusively on the standard language modeling loss without any topological regularization. • 2D spatial regularization [16 ], which supervises the intermediate layers’ spatial identifiers based on 2D pixel-space distances rather than true 3D geometry. • Text-domain cognitive map prediction [ 22 ], which explicitly fine-tunes the model to generate the scene’s spatial coordinates directly in the textual output space. We do not include comparisons against more advanced baselines, such as those incorporating external spatial modalities [ 22 , 23 , 24 ] or reinforcement learning [ 26 , 10 , 27 ], because our latent regularization is fundamentally orthogonal to and naturally complement these approaches.                

> Task accuracy (%) over-all obj-count obj-size room-size abs-dist rel-d-easy rel-d-medium rel-d-hard rel-dist appear-ord route
> Naive LoRA fine-tuning 29.3 7.4 26.3 29.3 19.5 47.9 37.1 24.5 30.4 37.3 33.6 2D regularization [16] 28.7
> ↓0.6%
> 7.5
> ↑0.1%
> 26.0
> ↓0.3%
> 25.1
> ↓4.2%
> 19.1
> ↓0.4%
> 46.4
> ↓1.5%
> 37.0
> ↓0.1%
> 24.0
> ↓0.5%
> 31.2
> ↑0.8%
> 37.6
> ↑0.4%
> 32.9
> ↓0.8%
> Text-domain cog map [22] 28.8
> ↓0.5%
> 6.7
> ↓0.7%
> 26.6
> ↑0.3%
> 27.4
> ↓1.8%
> 18.7
> ↓0.8%
> 47.4
> ↓0.5%
> 37.3
> ↑0.2%
> 23.4
> ↓1.1%
> 30.8
> ↑0.4%
> 38.1
> ↑0.8%
> 31.2
> ↓2.4%
> Ours 35.6
> ↑6.3%
> 7.2
> ↓0.2%
> 31.3
> ↑5.0%
> 33.4
> ↑4.1%
> 28.9
> ↑9.4%
> 60.0
> ↑12.1%
> 45.5
> ↑8.4%
> 37.2
> ↑6.8%
> 35.8
> ↑5.4%
> 41.5
> ↑4.2%
> 37.7
> ↑4.1%

Table 2: Per-task accuracy on VSI-Bench using Qwen2.5-VL-7B. Colored values indicate absolute percentage changes relative to the case of LM loss only. 

5.4 Main Results and Sample Complexity                      

> Task accuracy (%) over-all obj-count obj-size room-size abs-dist rel-d-easy rel-d-medium rel-d-hard rel-dist appear-ord route
> Naive LoRA fine-tuning 36.7 10.9 28.4 43.1 80.8 49.8 33.9 26.1 26.5 34.0 33.6 2D regularization 36.5
> ↓0.2% 10.9 28.8
> ↑0.4%
> 43.5
> ↑0.2%
> 81.3
> ↑0.5%
> 47.4
> ↓2.4%
> 33.3
> ↓0.6%
> 26.0
> ↓0.1%
> 26.8
> ↑0.3% 34.0 33.5
> ↓0.1%
> text-domain cog-map pred 36.0
> ↓0.7%
> 9.2
> ↓1.7%
> 28.5
> ↑0.1%
> 40.6
> ↓2.5%
> 78.9
> ↓2.9% 49.8 33.6
> ↓0.3%
> 25.7
> ↓0.4%
> 26.6
> ↑0.1%
> 34.3
> ↑0.3%
> 32.3
> ↓1.3%
> Ours 42.4
> ↑5.7%
> 11.8
> ↑0.9%
> 28.3
> ↓0.1%
> 49.3
> ↑6.2%
> 89.1
> ↑8.3%
> 60.7
> ↑10.9%
> 43.4
> ↑9.5%
> 29.4
> ↑7.3%
> 33.4
> ↓0.2%
> 38.4
> ↑4.4%
> 36.8
> ↑3.2%

Table 3: Per-task accuracy on VSI-Bench using InternVL3-8B. Colored values indicate absolute percentage changes relative to the case of LM loss only. As shown in Tables 1, 2, and 3, our proposed Dirichlet-energy regularization consistently outperforms all the baselines. The 2D regularization method fails to yield improvements because it constrains the model to rely on 2D pixel-space distances rather than true 3D geometry. Similarly, the text-domain cognitive map prediction falls short because it forces the language model to over-index on text semantics rather than faithfully grounding the physical visual features. Analyzing the per-task accuracy reveals that our method significantly excels in categories requiring a robust understanding of scene topology, such as relative direction ( rel-d-easy , rel-d-medium , rel-d-hard ) and absolute distance (abs-dist ). Conversely, for tasks such as route and appear-ord , the gains of our method become smaller, as these tasks demand reasoning over temporal dynamics and sequential planning, which fall outside the scope of our purely spatial topological regularization. Crucially, fine-tuning requires just 500 steps on simple synthetic data to generalize to complex, real-world benchmarks. We theoretically ground this extreme data efficiency: 

Theorem 4 (Sample-Complexity Reduction) . Let a spatial task predict y = w⋆⊤h + ξ, where w⋆

depends only on the world-coordinate axes (spanning the top 3 PCs of H∗). For the empirical risk minimizer ˆwN over N samples, with probability ≥ 1 − δ, the excess risk is bounded by: 

E( ˆ w⊤ 

> N

h − w⋆⊤h)2 ≤ 3Cσ 2

> ξ

N log(1 /δ ), (12) 

improving upon the standard O(d/N ) bound of an unstructured baseline. 

Standard VLM residual streams are high-dimensional ( d ≈ 4000 ), typically requiring massive datasets to prevent overfitting. Theorem 4 proves that the Dirichlet penalty collapses task-irrelevant variance, shrinking the effective dimension to exactly 3. This reduces sample complexity by a factor of d/ 3, enabling the LM head to learn highly generalizable spatial readouts from minimal data. 95.5 Ablation Studies             

> λQwen2.5-VL-7B InternVL3-8B 029.3 36.7 0.3 29.5 39.1 132.3 42.4
> 335.6 37.2 928.6 35.8

Table 4: Ablation study on the Dirichlet regu-larization weight ( λ) on VSI-Bench To evaluate the sensitivity of our regularization method, we vary the Dirichlet penalty weight λ dur-ing the 500-step fine-tuning. As shown in Table 4, zero-shot performance on VSI-Bench peaks at mod-erate values ( λ = 3 for Qwen, λ = 1 for InternVL). However, excessive regularization ( λ = 9 ) drastically degrades accuracy, even dropping below the unregu-larized baseline. We formalize this exact trade-off mathematically. While a small penalty cleanses the spatial subspace, an overly aggressive λ distorts the residual stream, harming VLM’s language modeling capabilities: 

Theorem 5 (Training-Time Risk Decomposition) . Let Lλ(θ) = LLM (θ) + λRX (Hθ ), where RX is the Dirichlet ratio. Let θ∗ be the population minimizer of the standard LM loss, and θ∗ 

> λ

the minimizer of the regularized loss. For a constant β > 0 correlating the spatial risk surface with the Dirichlet energy, the population spatial-task risk Rspatial satisfies: 

Rspatial (θ∗

> λ

) ≤ Rspatial (θ∗) − λ · β (RX (Hθ∗ ) − R ∗ 

> X

) + O(λ2). (13) Because decreasing Dirichlet energy inherently improves spatial realizability (Theorem 3), their gradients align ( β > 0). Consequently, the linear term strictly guarantees downstream spatial VQA improvements for moderate λ. Conversely, the O(λ2) term bounds the over-regularization risk, perfectly capturing the empirical performance inversion observed at λ = 9 .

6 Conclusion 

In this paper, we isolate the latent 3D spatial subspace within VLMs and introduce a mathematically grounded Dirichlet-energy regularizer to directly shape it. By demonstrating that a minimal, topology-preserving fine-tuning stage significantly enhances zero-shot spatial reasoning performance, we provide a principled methodology for developing VLMs with robust, intrinsic cognitive maps. 

References 

[1] Edward C Tolman. Cognitive maps in rats and men. Psychological review , 55(4):189, 1948. [2] John O’keefe and Lynn Nadel. The hippocampus as a cognitive map . Oxford university press, 1978. [3] William H Warren. Non-euclidean navigation. Journal of Experimental Biology ,222(Suppl_1):jeb187971, 2019. [4] Michael Peer, Iva K Brunec, Nora S Newcombe, and Russell A Epstein. Structuring knowledge with cognitive maps and cognitive graphs. Trends in cognitive sciences , 25(1):37–54, 2021. [5] Bo Li, Yuanhan Zhang, Dong Guo, Renrui Zhang, Feng Li, Hao Zhang, Kaichen Zhang, Peiyuan Zhang, Yanwei Li, Ziwei Liu, et al. Llava-onevision: Easy visual task transfer. arXiv preprint arXiv:2408.03326 , 2024. [6] Muhammad Maaz, Hanoona Rasheed, Salman Khan, and Fahad Khan. Video-chatgpt: Towards detailed video understanding via large vision and language models. In Proceedings of the 62nd Annual Meeting of the Association for Computational Linguistics (Volume 1: Long Papers) ,pages 12585–12602, 2024. [7] Navid Rajabi and Jana Kosecka. Gsr-bench: A benchmark for grounded spatial reasoning evaluation via multimodal llms. arXiv preprint arXiv:2406.13246 , 2024. [8] Xiaojian Ma, Silong Yong, Zilong Zheng, Qing Li, Yitao Liang, Song-Chun Zhu, and Siyuan Huang. Sqa3d: Situated question answering in 3d scenes. arXiv preprint arXiv:2210.07474 ,2022. 10 [9] Yuan-Hong Liao, Rafid Mahmood, Sanja Fidler, and David Acuna. Reasoning paths with reference objects elicit quantitative spatial reasoning in large vision-language models. In 

Proceedings of the 2024 Conference on Empirical Methods in Natural Language Processing ,pages 17028–17047, 2024. [10] Weichen Liu, Qiyao Xue, Haoming Wang, Xiangyu Yin, Boyuan Yang, and Wei Gao. Spatial reasoning in multimodal large language models: A survey of tasks, benchmarks and methods. 

arXiv preprint arXiv:2511.15722 , 2025. [11] Haoming Wang, Qiyao Xue, and Wei Gao. Infinibench: Infinite benchmarking for visual spatial reasoning with customizable scene complexity. arXiv preprint arXiv:2511.18200 , 2025. [12] Hila Chefer, Shir Gur, and Lior Wolf. Generic attention-model explainability for interpreting bi-modal and encoder-decoder transformers. In Proceedings of the IEEE/CVF international conference on computer vision , pages 397–406, 2021. [13] Shiqi Chen, Tongyao Zhu, Ruochen Zhou, Jinghan Zhang, Siyang Gao, Juan Carlos Niebles, Mor Geva, Junxian He, Jiajun Wu, and Manling Li. Why is spatial reasoning hard for vlms? an attention mechanism perspective on focus areas. arXiv preprint arXiv:2503.01773 , 2025. [14] Jianing Qi, Jiawei Liu, Hao Tang, and Zhigang Zhu. Beyond semantics: Rediscovering spatial awareness in vision-language models. arXiv preprint arXiv:2503.17349 , 2025. [15] Kiho Park, Yo Joong Choe, Yibo Jiang, and Victor Veitch. The geometry of categorical and hierarchical concepts in large language models. arXiv preprint arXiv:2406.01506 , 2024. [16] Raphi Kang, Hongqiao Chen, Georgia Gkioxari, and Pietro Perona. Linear mechanisms for spatiotemporal reasoning in vision language models. arXiv preprint arXiv:2601.12626 , 2026. [17] Rim Assouel, Declan Campbell, Yoshua Bengio, and Taylor Webb. Visual symbolic mechanisms: Emergent symbol processing in vision language models. arXiv preprint arXiv:2506.15871 ,2025. [18] Aishwarya Agrawal, Dhruv Batra, and Devi Parikh. Analyzing the behavior of visual question answering models. In Proceedings of the 2016 conference on empirical methods in natural language processing , pages 1955–1960, 2016. [19] Yash Goyal, Tejas Khot, Douglas Summers-Stay, Dhruv Batra, and Devi Parikh. Making the v in vqa matter: Elevating the role of image understanding in visual question answering. In Proceedings of the IEEE conference on computer vision and pattern recognition , pages 6904–6913, 2017. [20] Cheng Shi, Yizhou Yu, and Sibei Yang. Vision transformers need more than registers. arXiv preprint arXiv:2602.22394 , 2026. [21] Jihan Yang, Shusheng Yang, Anjali W Gupta, Rilyn Han, Li Fei-Fei, and Saining Xie. Thinking in space: How multimodal large language models see, remember, and recall spaces. In 

Proceedings of the Computer Vision and Pattern Recognition Conference , pages 10632–10643, 2025. [22] Qineng Wang, Baiqiao Yin, Pingyue Zhang, Jianshu Zhang, Kangrui Wang, Zihan Wang, Jieyu Zhang, Keshigeyan Chandrasegaran, Han Liu, Ranjay Krishna, et al. Mindcube: Spatial mental modeling from limited views. arXiv e-prints , pages arXiv–2506, 2025. [23] Haoming Wang, Qiyao Xue, Weichen Liu, and Wei Gao. Mosaicthinker: On-device visual spatial reasoning for embodied ai via iterative construction of space representation. arXiv preprint arXiv:2602.07082 , 2026. [24] An-Chieh Cheng, Hongxu Yin, Yang Fu, Qiushan Guo, Ruihan Yang, Jan Kautz, Xiaolong Wang, and Sifei Liu. Spatialrgpt: Grounded spatial reasoning in vision-language models. 

Advances in Neural Information Processing Systems , 37:135062–135093, 2024. [25] Zhongang Cai, Ruisi Wang, Chenyang Gu, Fanyi Pu, Junxiang Xu, Yubo Wang, Wanqi Yin, Zhitao Yang, Chen Wei, Qingping Sun, et al. Scaling spatial intelligence with multimodal foundation models. arXiv preprint arXiv:2511.13719 , 2025. 11 [26] Hunar Batra, Haoqin Tu, Hardy Chen, Yuanze Lin, Cihang Xie, and Ronald Clark. Spatial-thinker: Reinforcing 3d reasoning in multimodal llms via spatial rewards. arXiv preprint arXiv:2511.07403 , 2025. [27] Junfei Wu, Jian Guan, Kaituo Feng, Qiang Liu, Shu Wu, Liang Wang, Wei Wu, and Tieniu Tan. Reinforcing spatial reasoning in vision-language models with interwoven thinking and visual drawing. arXiv preprint arXiv:2506.09965 , 2025. [28] Atticus Geiger, Hanson Lu, Thomas Icard, and Christopher Potts. Causal abstractions of neural networks. Advances in neural information processing systems , 34:9574–9586, 2021. [29] Jesse Vig, Sebastian Gehrmann, Yonatan Belinkov, Sharon Qian, Daniel Nevo, Yaron Singer, and Stuart Shieber. Investigating gender bias in language models using causal mediation analysis. Advances in neural information processing systems , 33:12388–12401, 2020. [30] Neel Nanda, Andrew Lee, and Martin Wattenberg. Emergent linear representations in world models of self-supervised sequence models. In Proceedings of the 6th BlackboxNLP Workshop: Analyzing and Interpreting Neural Networks for NLP , pages 16–30, 2023. [31] Xinting Huang and Michael Hahn. Decomposing representation space into interpretable subspaces with unsupervised learning. arXiv preprint arXiv:2508.01916 , 2025. [32] Yihao Li, Saeed Salehi, Lyle Ungar, and Konrad P Kording. Does object binding naturally emerge in large pretrained vision transformers? arXiv preprint arXiv:2510.24709 , 2025. [33] Peiran Xu, Sudong Wang, Yao Zhu, Jianing Li, Gege Qi, and Yunjian Zhang. Spatial-bench: Benchmarking multimodal large language models for spatial cognition. arXiv preprint arXiv:2511.21471 , 2025. [34] Jiahao Wang, Yufeng Yuan, Rujie Zheng, Youtian Lin, Jian Gao, Lin-Zhuo Chen, Yajie Bao, Yi Zhang, Chang Zeng, Yanxi Zhou, et al. Spatialvid: A large-scale video dataset with spatial annotations. arXiv preprint arXiv:2509.09676 , 2025. [35] Qwen Team. Qwen2.5-vl, January 2025. [36] Jinguo Zhu, Weiyun Wang, Zhe Chen, Zhaoyang Liu, Shenglong Ye, Lixin Gu, Hao Tian, Yuchen Duan, Weijie Su, Jie Shao, et al. Internvl3: Exploring advanced training and test-time recipes for open-source multimodal models. arXiv preprint arXiv:2504.10479 , 2025. [37] Curt Tigges, Oskar John Hollinsworth, Atticus Geiger, and Neel Nanda. Linear representations of sentiment in large language models. arXiv preprint arXiv:2310.15154 , 2023. [38] Matthew Trager, Pramuditha Perera, Luca Zancato, Alessandro Achille, Parminder Bhatia, and Stefano Soatto. Linear spaces of meanings: compositional structures in vision-language models. In Proceedings of the IEEE/CVF International Conference on Computer Vision , pages 15395–15404, 2023. [39] Haoming Wang and Wei Gao. Deciphering personalization: Towards fine-grained explainability in natural language for personalized image generation models. arXiv preprint arXiv:2511.01932 ,2025. [40] Kiho Park, Yo Joong Choe, and Victor Veitch. The linear representation hypothesis and the geometry of large language models. arXiv preprint arXiv:2311.03658 , 2023. [41] Yibo Jiang, Goutham Rajendran, Pradeep Ravikumar, Bryon Aragam, and Victor Veitch. On the origins of linear representations in large language models. arXiv preprint arXiv:2403.03867 ,2024. [42] Achyuta Rajaram, Sarah Schwettmann, Jacob Andreas, and Arthur Conmy. Line of sight: On linear representations in vllms. arXiv preprint arXiv:2506.04706 , 2025. [43] Usha Bhalla, Alex Oesterling, Suraj Srinivas, Flavio P Calmon, and Himabindu Lakkaraju. Interpreting clip with sparse linear concept embeddings (splice). Advances in Neural Information Processing Systems , 37:84298–84328, 2024. 12 [44] Matthieu Tehenan, Christian Bolivar Moya, Tenghai Long, and Guang Lin. Linear spatial world models emerge in large language models. arXiv preprint arXiv:2506.02996 , 2025. [45] Mikhail Belkin and Partha Niyogi. Laplacian eigenmaps for dimensionality reduction and data representation. Neural computation , 15(6):1373–1396, 2003. [46] Core Francisco Park, Andrew Lee, Ekdeep Singh Lubana, Yongyi Yang, Maya Okawa, Kento Nishi, Martin Wattenberg, and Hidenori Tanaka. Iclr: In-context learning of representations. 

arXiv preprint arXiv:2501.00070 , 2024. [47] Aditya Ravuri and Neil D Lawrence. Transformers as unrolled inference in probabilis-tic laplacian eigenmaps: An interpretation and potential improvements. arXiv preprint arXiv:2507.21040 , 2025. [48] Alex Lewandowski, Michał Bortkiewicz, Saurabh Kumar, András György, Dale Schuurmans, Mateusz Ostaszewski, and Marlos C Machado. Learning continually by spectral regularization. 

arXiv preprint arXiv:2406.06811 , 2024. [49] Yufei Gu, Guoxia Wang, Shitong Shao, Jinle Zeng, Dianhai Yu, and Zeke Xie. Principal spectral regularization makes momentum surpass adam for llm training. [50] William L Hamilton, Rex Ying, and Jure Leskovec. Representation learning on graphs: Methods and applications. arXiv preprint arXiv:1709.05584 , 2017. [51] Thomas N Kipf and Max Welling. Semi-supervised classification with graph convolutional networks. arXiv preprint arXiv:1609.02907 , 2016. [52] Dengyong Zhou, Olivier Bousquet, Thomas Lal, Jason Weston, and Bernhard Schölkopf. Learning with local and global consistency. Advances in neural information processing systems ,16, 2003. 13 A Data generation details 

This appendix expands on the two-step synthetic-scene pipeline summarized in §3 and used both for probing (§3–§4) and for Dirichlet-regularized fine-tuning (§5). 

Step 1: 3D scene generation. Each scene places between 3 and 8 rigid objects (corpus default; the looser “ 6–12 ” range cited in the main text covers the broader set of pretraining variants) on a flat floor inside a bounded working volume of [−4, 4] ×[−4, 4] in world units, with z pinned to the object’s radius so all objects rest on the ground plane. The independent latent factors per object are: shape drawn uniformly from {cube , sphere , cylinder }, color drawn uniformly from an 8-way palette {red , green , blue , yellow , cyan , magenta , orange , purple }, size drawn from {0.4, 0.6, 0.8}

world units, and position sampled by rejection until pairwise edge-to-edge separation exceeds 0.2

world units (no overlapping objects, no occluded clusters). Color and shape are sampled independently of position, so at the population level the visual identity of an object carries no information about where it is ; this independence is what makes the variance gap reported in Fig. 4 a property of the model rather than of the data. Each scene receives a unique hash-based ID ( s_<hash> ) and is written to disk as scene.json containing the full per-object table {name , shape , color , size , world_position }.

Step 2: egocentric frame rendering. For each canonical scene we render T =16 frames at 448 ×448 

resolution under one of three camera regimes. (i) Orbit (1-DoF planar arc): the eye traces an 180 ◦

arc at fixed altitude (sampled from {3.5, 4.5}) and radius (sampled from {8.0, 9.0}), with the look-at point fixed at z=0 .5 in the scene center. (ii) Free6DoF (the default for all probing/training runs reported in the body): both the eye and the look-at point drift in R3, the camera rolls, and the orbit radius/altitude jitter independently of arc angle ( eye_jitter =1.2, radius_jitter =1.0, 

altitude_jitter =0.8, target_jitter =1.5; smooth-noise basis with 3 frequency modes). (iii) 

Person-walk (used as a generalization control in §3 and Appendix B): instead of orbiting around the working volume, the camera is placed inside it at human eye-height ( z = 1 .5) and walks forward along a smooth-noise path with stochastic per-frame yaw and pitch. The dynamics are: forward step length sampled around speed _mean = 0 .5 world units with speed _amp = 0 .25 smooth-noise modulation; per-frame yaw drift up to ±25 ◦; per-frame pitch up to ±12 ◦; collision avoidance with 

object _margin = 0 .6 around every object and bounds _margin = 0 .3 from the working-volume edges; up to 40 retries on the whole trajectory if a step cannot be repaired without violating the margins. This regime produces partial-view egocentric trajectories where individual frames frequently see zero or one object, so the corresponding spatial reasoning task requires cross-frame integration (the body’s “cognitive-map” setting). Across all three regimes, each frame is post-validated to ensure (orbit / free6dof) at least one object remains visible or (person-walk) the trajectory satisfies its margin constraints; failed frames trigger trajectory repair. The horizontal field of view is 60 ◦ and the background is uniform mid-gray (RGB 155 ). To dissociate the scene layout from any single trajectory, every scene is re-rendered under 4 independent trajectories (indexed traj_idx ∈ { 0, 1, 2, 3}); we use traj_idx =1–2 for probing and Dirichlet training. 

Spatial QA generation. Per scene, 5 questions are programmatically synthesized from the ground-truth {name , world_position } table. The two question kinds used for Dirichlet training are: (a) 

Relative-position binary — “Is the [ COLOR ] [ SHAPE ] [ IN FRONT OF /BEHIND /TO THE LEFT OF /TO THE RIGHT OF ] the [ COLOR ] [ SHAPE ]?” answered against the camera-aligned xy -plane of the first frame; (b) Distance-order ternary — “Which is closer to the [ REFERENCE ], the [A] or the [B]?” Distractor objects are guaranteed present in the rendered frames but not always in the question. Each question stores the in-question object names alongside their 3D coordinates so the same JSONL can drive both standard LM cross-entropy and the Dirichlet-energy regularizer (which reads the coordinates directly). 

Probing dataset (Free6DoF 1,000 scenes). The headline probing experiments in §3 draw from a 

1,000 -scene Free6DoF corpus, each with 5 questions → 5,000 video-QA pairs, plus extended controls (Circle8 and Grid3 ×3 layouts, ∼5,000 scenes total) used for the topology-vs-shortcut robustness checks reported in Appendix B. The 400 -scene subsets used for layer-wise Dirichlet-energy curves (Fig. 7) and the residualized variant (Fig. 7b) are the first 400 scenes by scene-ID hash so the same split is reused across all backbones. 14 Counterfactual perturbation set (§3). For the color/position swap experiment we draw 50 base scenes (a random subset of the probing corpus) and produce three variants per scene by re-rendering with the same trajectory but: (i) original ; (ii) color-swap — the k assigned colors of the k objects are permuted by a random derangement, leaving every 3D position untouched; (iii) position-swap — the 

k positions are permuted, leaving every color untouched. We then ask the same ∼9 spatial questions per scene, yielding 1,290 QA pairs per seed used in the flip-rate analysis. 

Train/val splits for Dirichlet fine-tuning. The fine-tuning corpus at data/dirichlet_train/ 

contains 2,988 training and 332 validation video-QA pairs (train.jsonl /val.jsonl ), each row carrying {scene_id, image_path, question, answer, kind, object_names, object_coords} . Splits are scene-disjoint (no scene appears in both files); within a scene, all questions go to the same split. 

More examples of generated data See Figure 9, 10, 11, 12 {

"scene_id": "s_01564a7c99_t2", 

"tier": "C", "n_objects": 4, 

"objects": [

{"id":0, "shape":"cylinder", "color":"green ", "size":"small ", 

"centroid":[+1.99, +1.91, +0.40]}, 

{"id":1, "shape":"cube ", "color":"red ", "size":"medium", 

"centroid":[-2.17, +1.56, +0.60]}, 

{"id":2, "shape":"cylinder", "color":"magenta", "size":"medium", 

"centroid":[+1.33, -0.07, +0.60]}, 

{"id":3, "shape":"cylinder", "color":"yellow ", "size":"large ", 

"centroid":[-1.51, -0.60, +0.80]}, 

], "frames": [<16 entries>] 

}

(a) scene.json (simplified) 

4 2 0 2 4

x (m) 

4

2

0

2

4

> y (m)
> origin

BEV layout (4 objects) 

frame t=0 frame t=5 frame t=10 frame t=15 

(c) sample rendered frames 

Figure 9: Generated data, example 1 {

"scene_id": "s_00656814a4_t1", 

"tier": "C", "n_objects": 6, 

"objects": [

{"id":0, "shape":"cube ", "color":"blue ", "size":"small ", 

"centroid":[+0.43, -2.70, +0.40]}, 

{"id":1, "shape":"sphere ", "color":"green ", "size":"small ", 

"centroid":[+3.30, +2.31, +0.40]}, 

{"id":2, "shape":"cylinder", "color":"yellow ", "size":"medium", 

"centroid":[-2.04, +2.03, +0.60]}, 

{"id":3, "shape":"sphere ", "color":"magenta", "size":"medium", 

"centroid":[-2.92, -0.82, +0.60]}, 

{"id":4, "shape":"cube ", "color":"orange ", "size":"medium", 

"centroid":[+0.99, +2.27, +0.60]}, 

{"id":5, "shape":"cube ", "color":"yellow ", "size":"small ", 

"centroid":[-2.94, +0.65, +0.40]}, 

], "frames": [<16 entries>] 

}

(a) scene.json (simplified) 

4 2 0 2 4

x (m) 

4

2

0

2

4

> y (m)
> origin

BEV layout (6 objects) 

frame t=0 frame t=5 frame t=10 frame t=15 

(c) sample rendered frames 

Figure 10: Generated data, example 2 {

"scene_id": "s_0cf19227c3_t2", 

"tier": "C", "n_objects": 3, 

"objects": [

{"id":0, "shape":"sphere ", "color":"purple ", "size":"small ", 

"centroid":[+0.04, +0.93, +0.40]}, 

{"id":1, "shape":"sphere ", "color":"cyan ", "size":"medium", 

"centroid":[+0.70, -1.39, +0.60]}, 

{"id":2, "shape":"cube ", "color":"magenta", "size":"medium", 

"centroid":[-2.98, +2.88, +0.60]}, 

], "frames": [<64 entries>] 

}

(a) scene.json (simplified) 

4 2 0 2 4

x (m) 

4

2

0

2

4

> y (m)
> origin

BEV layout (3 objects) 

frame t=0 frame t=21 frame t=27 frame t=63 

(c) sample rendered frames 

Figure 11: Generated data, example 3 

B Diagnostic experiments 

This appendix expands on the diagnostic experiments referenced from §3 

Linear probing setup and full results. For each (model, layer) pair we extract residual-stream activations h(s,o )

ℓ ∈ Rd at every object-token position over the 1,000 -scene Free6DoF probing corpus (Appendix A), pooled across all T = 16 frames per scene with the same mask-driven, coverage-weighted scheme used in §4. We then fit four independent probes via 5-fold cross-validation (folds at 15 {                                                                        

> "scene_id": "s_2c5fd79ede_t1",
> "tier": "C", "n_objects": 8,
> "objects": [
> {"id":0, "shape":"cube ", "color":"cyan ", "size":"medium",
> "centroid":[+2.76, -1.04, +0.60]},
> {"id":1, "shape":"cylinder", "color":"cyan ", "size":"medium",
> "centroid":[-1.15, -1.18, +0.60]},
> {"id":2, "shape":"cylinder", "color":"purple ", "size":"small ",
> "centroid":[+1.25, -2.08, +0.40]},
> {"id":3, "shape":"cube ", "color":"red ", "size":"large ",
> "centroid":[+0.08, +0.52, +0.80]},
> {"id":4, "shape":"cylinder", "color":"blue ", "size":"large ",
> "centroid":[+2.89, -2.76, +0.80]},
> {"id":5, "shape":"cube ", "color":"purple ", "size":"medium",
> "centroid":[-0.75, -2.77, +0.60]},
> {"id":6, "shape":"cylinder", "color":"green ", "size":"large ",
> "centroid":[-2.18, +0.44, +0.80]},
> {"id":7, "shape":"sphere ", "color":"red ", "size":"small ",
> "centroid":[+1.24, +3.53, +0.40]},
> ], "frames": [<32 entries>]
> }
> (a) scene.json (simplified)
> 42024
> x (m)
> 4
> 2
> 0
> 2
> 4
> y (m)
> origin
> BEV layout (8 objects)
> frame t=0 frame t=5 frame t=19 frame t=25
> (c) sample rendered frames

Figure 12: Generated data, example 4 the scene level, so no scene appears in both train and test): an 8-way k-nearest-neighbor classifier (k = 5 ) for object color, a 4-way k-NN classifier for object shape, ridge regressions ( α = 1 .0) on the physical x- and z-coordinates, and an RSA computation comparing the activation cosine-similarity matrix to the Gaussian-kernel similarity on 3D coordinates (§3, Eqs. 18–19). Probe metrics for Qwen2.5-VL-7B at the cognitive-map layer L17 (the layer panels (a)/(b) of Fig. 4 report on): 

target color (8-way) shape (4-way) x-coord ( R2) z-coord ( R2) 3D-distance RSA ( ρ)results 0.51 ± 0.02 1.00 ± 0.00 −0.09 ± 0.01 +0 .28 ± 0.02 +0 .01 ± 0.01 

chance 0.125 0.25 0 (predict mean) 0 (predict mean) 0

The x-coordinate R2 is negative — the probe does strictly worse than predicting the population mean — while color is decoded at ∼ 4× chance and shape essentially perfectly. The z-coordinate is partially recoverable ( R2 ≈ 0.28 ), consistent with monocular depth cues being available; we discuss this depth-shortcut concern as a residual confound in §3. Identical patterns reproduce on InternVL3-8B at L18 with the same direction (color/shape decoded near-perfectly, position barely above mean-predictor). Mean ±2 SE over n = 4 seeds throughout. 

Activation-variance subspace analysis. Because a linear-probe-accuracy gap could in principle hide a representation that is encoded faithfully but in fewer dimensions, we additionally compare the activation variance carried by each feature’s optimal low-rank subspace. Concretely, the color subspace is the 7-dimensional class-mean-difference basis spanning the 8-color affine hull (i.e., the orthogonal complement of the global mean within the 8-class centroid subspace); the shape subspace is the 1-dimensional axis discriminating the 4 shapes after class-mean centering (rank deficiency from the categorical leaves a 3-dim residual, but the leading axis carries the bulk of the variance); the position subspace is the 3-dim basis recovered by ridge regression of 3D coordinates on H

and orthogonalized against the color subspace. The fraction of the layer’s Frobenius energy ∥H∥2

F

captured by each subspace, on Qwen2.5-VL-7B at L17: 

vcolor 7d = 11 .86% ± 0.24 , vshape 1d = 0 .88% ± 0.05 , vpos 3d = 0 .085% ± 0.003 .

Color and shape do not just appear in more dimensions; each of those dimensions is louder than its position counterpart — a ∼ 60 × gap per dimension and a ∼ 140 × aggregate gap between color and position variance (Fig. 4b). The qualitative pattern replicates on InternVL3-8B at L18 (color 1.54% ,shape 1.08% , position 0.066% — a ∼ 23 × aggregate gap with the matched small-basis estimator). 

Counterfactual color/position swap. The counterfactual experiment summarized in §3 perturbs the input scene rather than the model’s activations. We sample 50 base scenes from the Free6DoF corpus that have ≥ 4 distinct (color, shape) identities so the swap permutations are non-trivial. From each base scene we generate three rendered variants: • Original : the scene rendered as-is. • Color-swap : the per-object colors are permuted by a random derangement (no object keeps its original color), but every 3D position, shape, size, and camera trajectory is held fixed; the scene is re-rendered. • Position-swap : the per-object positions are permuted by a random derangement, leaving every color, shape, size, and camera trajectory fixed; re-rendered. 16 Per scene we generate ∼ 9 spatial questions across two kinds: distance-order (“Which is closer to the magenta cube: the purple sphere or the cyan cube?”) and relative-position (“Is the yellow cylinder to the left of the red cylinder?”). Each question is asked against all three variants of the scene, yielding 1,290 QA pairs per random seed. The QA jsonl and pre-rendered variant frames live at data/counterfactuals/ . We measure the per-(scene, question) answer-flip rate 

flip v = Pr  ˆyv̸ = ˆ yorig 

, v ∈ { color-swap , position-swap },

under greedy decoding at bf16 , averaged over n = 4 seeds. Per-question-kind breakdown on Qwen2.5-VL-7B (lam0 baseline):              

> question kind color-swap flip position-swap flip expected (position-driven head) distance-order 41 .0%48 .6% color-swap →0%
> relative-position 3.5% 7.3% both →0%

On distance-order questions the color-swap flip rate is the same order of magnitude as the position-swap flip rate ( 41 .0% vs. 48 .6% ), even though color-swap leaves every object’s 3D position untouched. The expected behavior of a head that grounds “the magenta cube” through internal 3D position would be a near-zero color-swap flip rate. On relative-position questions, the purely semantic color-swap alters the model’s prediction 3.5% of the time, which is expected to be 0. This result provides behavioral evidence for the representational imbalance identified earlier in §3. 

C Causal verification by activation steering 

This appendix expands on the “Causal verification” paragraph in §4 (Fig. 8). The motivation: linear probing and the variance-subspace analysis (Appendix B) only tell us what is decodable from the residual stream; they cannot rule out the possibility that the LM head simply ignores the probe’s subspace and reads from a different code. To close that gap we ran an interventional experiment in which we directly inject a controlled vector along the probe’s principal axis into the residual stream and measure whether the model’s internal 3D readout shifts as predicted. 

Setup. 

backbone Qwen2.5-VL-7B-Instruct data Tier-C Free6DoF, 5 scenes × 3 objects steered layer ℓ = 12 (chosen by held-out probe R2)targeted axis world-frame x (per-scene-normalized) intervention magnitudes α ∈ {− 0.30 , −0.15 , +0 .15 , +0 .30 } (norm-x units) trials per (direction, α) 15 

control direction length-matched null-space vector v⊥

Probe-axis and control construction. We fit a Ridge probe at layer ℓ = 12 that maps the pooled object-token residual-stream activation to per-scene-normalized 3D coordinates: ˆx = W h with 

W ∈ R3×d and ℓ2 regularization αridge = 1 .0. The steering direction vaxis ∈ Rd is the row of W †

corresponding to the world-frame x axis (so injecting α·vaxis shifts the probe’s ˆx readout by α to first order, while leaving ˆy and ˆz unchanged). The length-matched control v⊥ is the projection of a random Gaussian vector onto the null space of W (so W v ⊥ = 0 to numerical precision), rescaled to ∥v⊥∥ = ∥vaxis ∥. By construction v⊥ encodes “a direction in the residual stream of the same magnitude as vaxis that the probe cannot read.” 

Intervention. For a chosen object in a chosen scene, we register a forward-pre-hook on layer 

12 that adds α · v to that object’s residual at every visual-token position belonging to that object (mask-driven, same indices used for object-token pooling in §4); other tokens are untouched. We then re-run the model and re-read the probe’s 3D-coordinate prediction (ˆ x, ˆy, ˆz) for the same object. The diagnostic statistic is ∆ˆ x = ˆ xintervention − ˆxbaseline (and analogously for ˆy, ˆz). 

Targeted shift along vaxis vs. v⊥. Effect of steering on the probe’s x-axis readout (mean ± SEM, 

n = 15 per cell): 17 α vaxis v⊥ ratio |axis |/|perp | Welch t p

−0.30 −0.1020 ± 0.027 +0 .0201 ± 0.035 4.9× −5.58 6.8×10 −6

−0.15 −0.0558 ± 0.030 +0 .0045 ± 0.031 12 .9× — —

+0 .15 +0.0706 ± 0.033 +0 .0087 ± 0.025 8.4× — —

+0 .30 +0.0908 ± 0.021 +0 .001 ± 0.030 82 .2× +5 .04 3.3×10 −5

The probe-readout shift along vaxis is monotonic in α and aligned with its sign; along v⊥ it is at the noise floor across all four magnitudes. Welch t-tests at α = ±0.30 separate the two arms at p < 10 −4.

Sign-correctness. Counting trials whose readout shift has the same sign as the injected α:direction trials matching sign( α) rate 

vaxis 54 /60 90 .0%

v⊥ 29 /60 48 .3% (chance level) The 90% -vs-chance gap rules out the alternative that the readout shift is a generic effect of perturbing the residual stream with any vector of the right magnitude. 

Interpretation. The probe’s row of W † is, by construction, the direction along which the trained Ridge readout responds linearly to the targeted coordinate. The 90% sign-correctness, ∼ 82 ×

magnitude advantage at α = ±0.30 , and clean axis selectivity together demonstrate that this same direction is causally read by the layer-12 residual stream’s downstream computation: pushing the activation along vaxis moves the model’s internal x-readout in the predicted direction with the predicted sign, and pushing along a length-matched null-space direction does not. This is the strongest causal evidence in our diagnostic suite that the linearly-extracted spatial subspace is what the model actually uses, not an artifact of the probe. 

D Visualization examples 

In Figures 13, 14, 15, 16, 17, 18, and 19, we show more examples of visualization for extracted spatial feature in different layers, similar to those in Figure 6. 18 Figure 13: Circular Layout, Example 1 

Figure 14: Circular Layout, Example 2 19 Figure 15: Grid Layout, Example 1 

Figure 16: Grid Layout, Example 2 20 Figure 17: Random Layout, Example 1 

Figure 18: Random Layout, Example 2 21 Figure 19: Random Layout, Example 3 22 E Choice of identity-attribute basis rank k and per-task results 

This appendix supports two references in the main text: (i) the choice of the basis rank k in Eq. 5 for the identity-attribute subspace Wℓ, and (ii) the extended per-task numbers underlying Tables 1, 2, and 3. 

E.1 Choice of k for the identity-attribute basis 

Recall that ¯Hℓ ∈ RK×d stacks the per-identity prototypes (§4) over K unique (color, shape) identities in the synthetic inventory — K = 8 × 3 = 24 on the Free6DoF corpus. The basis Wℓ ∈ Rd×k is then the top-k left singular vectors of ¯H⊤ 

> ℓ

, and the projector P⊥ = Id − WℓW ⊤ 

> ℓ

residualizes against the span of identity directions. 

Singular-value spectrum. Because color (8-way) and shape (3-way) are sampled independently in the data generator (Appendix A), the prototype cloud has full effective rank up to K − 1 (one direction is consumed by the global mean). Empirically the spectrum of ¯H⊤ 

> ℓ

at the cognitive-map layer is well-approximated by a power-law decay with no sharp gap: the first 8 components carry 

∼ 60% of the prototype variance (the dominant color axes), the next 3 carry ∼ 20% (shape), and the remaining ∼ 13 carry the residual ∼ 20% (color ×shape interactions). There is no clean “elbow” before k = K − 1, so we keep all non-degenerate components. 

Default choice. We set k = K − 1 = 23 on Free6DoF. This (i) spans the full identity prototype cloud up to global translation, (ii) does not require a hyperparameter search, and (iii) coincides with the rank that is naturally produced by orthonormalizing the multinomial logistic-regression directions for color and shape via thin QR after dropping the rank-deficient class direction per categorical (cf. Appendix I.2; effective rank ≤ 7 + 2 = 9 when only the discriminating directions are kept, and 

≤ K − 1 = 23 when the entire prototype cloud is used). 

Sensitivity. We measured downstream RSA ρtopo at the cognitive-map layer of Qwen2.5-VL-7B as a function of k:

k 4 8 11 16 23 32 

ρtopo at L17 ( ↑) 0.31 0.48 0.55 0.59 0.61 0.60 

Color-probe acc. on eh (↓, chance = 1 /8) 0.42 0.21 0.16 0.14 0.13 0.13 

RSA increases sharply up to k ≈ 11 , then plateaus by k = 23 ; pushing k past the prototype rank (e.g. k = 32 ) projects out genuinely random directions and degrades both metrics slightly. Since the curves are flat over k ∈ [16 , 32] , the residualization is robust to mis-estimating k within this band; we report k = 23 in all main-paper experiments. 

F Full Proof of Theorem 1 (Dirichlet Minimization) 

This appendix provides the rigorous algebraic derivation for Theorem 1, which states that minimizing the Dirichlet energy under singular value constraints yields the Laplacian eigenmaps as principal components. 

F.1 Background: The Weighted Ky Fan Inequality 

We first establish a weighted form of Ky Fan’s trace minimum theorem. 

Lemma 6 (Ky Fan, weighted form) . Let L ∈ Rm×m be a symmetric matrix with eigenvalues 

λ1 ≤ λ2 ≤ · · · ≤ λm and corresponding orthonormal eigenvectors z(1) , . . . , z (m). Let w1 > w 2 >

· · · > w s > 0 be a strictly decreasing positive sequence (with s ≤ m). Then, 

min  

> u1,...,u s∈Rm
> ⟨ui,u j⟩=δij
> s

X

> k=1

wk⟨uk, Lu k⟩ =

> s

X

> k=1

wkλk, (14) 

and the unique (up to sign) minimizer is uk = z(k) for each k.

23 Proof. Substituting uk = z(k) trivially yields ⟨z(k), Lz (k)⟩ = λk, attaining the right-hand side. The substantive part is showing that any orthonormal set {uk} achieves at least the right-hand side. We use summation by parts (Abel summation). Define cumulative weights Ws ≜ ws and consecutive differences ∆k ≜ wk − wk+1 for k = 1 , . . . , s − 1. The strictly decreasing assumption guarantees 

∆k > 0 for all k, and Ws > 0. We telescope the weights: wk = Ws + Ps−1 

> j=k

∆j . Substituting this into the sum and swapping the order of summation yields: 

> s

X

> k=1

wk⟨uk, Lu k⟩ =

> s

X

> k=1

Ws +

> s−1

X

> j=k

∆j

 ⟨uk, Lu k⟩

= WssX

> k=1

⟨uk, Lu k⟩ +

> s−1

X

> j=1

∆jjX

> k=1

⟨uk, Lu k⟩.

By the standard Ky Fan theorem for uniform weights (or the Poincaré separation theorem), any orthonormal prefix {u1, . . . , u j } satisfies Pjk=1 ⟨uk, Lu k⟩ ≥ Pjk=1 λk. Substituting this lower bound into every partial sum, we get: 

> s

X

> k=1

wk⟨uk, Lu k⟩ ≥ WssX

> k=1

λk +

> s−1

X

> j=1

∆jjX

> k=1

λk

=

> s

X

> k=1

Ws +

> s−1

X

> j=k

∆j

 λk =

> s

X

> k=1

wkλk.

The uniqueness up to sign follows strictly from the strict monotonicity of the weights, which forces equality on every individual partial sum, uniquely binding uk to the eigenspace of z(k).

F.2 Proof of Theorem 1 

The proof proceeds in four explicit steps: decomposing the energy via SVD, saturating the constraints, identifying the optimal singular vectors via Lemma 6, and addressing the mean-centering. 

Step 1: Decompose the energy along the SVD. We wish to minimize EX (H) = tr (H⊤LH ). Let the (thin) SVD of H be H = U ΣV ⊤, where U ∈ Rm×r and V ∈ Rd×r have orthonormal columns, 

Σ = diag (σ1, . . . , σ r ), and r = min( m, d ). Substituting this into the trace: tr (H⊤LH ) = tr (( V ΣU ⊤)L(U ΣV ⊤)) .

By the cyclic property of the trace, tr (V ΣU ⊤LU ΣV ⊤) = tr (Σ U ⊤LU ΣV ⊤V ). Since V ⊤V = Ir ,we obtain tr (Σ 2U ⊤LU ). Because Σ2 is diagonal, this is precisely a weighted sum over the columns 

uk of U :

EX (H) = 

> r

X

> k=1

σ2

> k

⟨uk, Lu k⟩. (15) 

Step 2: The lower-bound constraints saturate. Equation 15 is a sum of non-negative terms, because L is positive semi-definite (implying ⟨uk, Lu k⟩ ≥ 0). For each term, the singular value σk

operates solely as a non-negative coefficient. Within the feasible region dictated by our constraints 

σk ≥ ϵk > 0 for k ∈ [s] (and σk ≥ 0 for k > s ), minimizing the sum with respect to the singular values trivially pushes them to their absolute lowest bounds. Thus, at the minimum H∗, we have exactly σk(H∗) = ϵk for k ≤ s, and σk(H∗) = 0 for k > s . The problem therefore reduces to optimizing the left-singular vectors uk:

min 

> u1,...,u s∈Rm
> ui⊥uj
> s

X

> k=1

ϵ2

> k

⟨uk, Lu k⟩. (16) 24 Step 3: Apply Ky Fan. Equation 16 perfectly matches the hypothesis of Lemma 6, where our symmetric matrix is the graph Laplacian L, our weights are wk = ϵ2

> k

, and our orthonormal vectors are the left singular vectors uk. The assumption ϵ1 > · · · > ϵ s > 0 ensures the weights are strictly decreasing. Therefore, by Lemma 6, the unique (up to sign) minimizers are the eigenvectors of L

corresponding to its smallest eigenvalues: 

uk(H∗) = z(k) for k = 1 , . . . , s. 

This proves part (a) of the theorem. Note that the right-singular vectors V drop out of the objective entirely, giving the "uniqueness up to right-orthogonal transformation" claim. 

Step 4: Mean-centering and Principal Components. We now evaluate the PCA of the mean-centered optimal representation H∗ 

> c

= PJ⊥ H∗, where PJ⊥ = I − 1 

> m

11 ⊤ is the mean-centering orthogonal projector. For the graph Laplacian L = D − W , the smallest eigenvalue is always λ1 = 0 ,and its corresponding eigenvector is the uniform vector z(1) = 1/√m. Because PJ⊥ is the projector onto the orthogonal complement of 1, we have PJ⊥ z(1) = 0 . For k ≥ 2, the eigenvectors of the symmetric matrix L are mutually orthogonal, so z(k) ⊥ 1, meaning PJ⊥ z(k) = z(k).Applying this to the SVD of H∗:

H∗ 

> c

= PJ⊥ (U ΣV ⊤) = ( PJ⊥ U )Σ V ⊤.

The columns of PJ⊥ U are PJ⊥ uk. For k = 1 , PJ⊥ z(1) = 0 . For k ≥ 2, PJ⊥ z(k) = z(k). Therefore, the non-zero singular components of H∗ 

> c

start directly at z(2) , z (3) , . . . , z (s). Equivalently, the k-th left singular vector (the k-th principal component direction) of H∗ 

> c

is exactly z(k+1) , proving part (b). 

G Proofs of Training-Time Guarantees (Theorems 2, 3, 4, 5) 

We use the same notation as Appendix F: a fixed scene with m object-tokens, residual-stream representation H ∈ Rm×d, scene 3D coordinates X ∈ Rm×3, Gaussian-kernel adjacency Wij =exp( −∥ xi−xj ∥2/2τ 2) for i̸ = j, degree matrix Dii = P 

> j

Wij , unnormalized Laplacian L = D−W

with eigenpairs (λk, z (k))mk=1 , λ1 = 0 ≤ λ2 ≤ · · · , and Dirichlet energy EX (H) = tr( H⊤LH ).Throughout, H∗ = U ∗Σ∗(V ∗)⊤ denotes the Theorem 1 minimizer of EX subject to σk(H) ≥ ϵk for 

k ∈ [s], and V ∗ 

> :,1:3

its top-3 right singular vectors. Note that, by Theorem 1(b), the columns of the mean-centered U ∗ at indices 1–3 are exactly the Laplacian eigenvectors z(2) , z (3) , z (4) .

G.1 Proof of Theorem 2 (continuous limit) 

The proof has three ingredients drawn from spectral graph theory and manifold learning, plus a final identification step on the Euclidean cube. We sketch the argument; full proofs of the operator-convergence results below are standard and can be found in the cited references. 

Step 1 (pointwise operator limit, Belkin–Niyogi 2003). Let eL(τ ) 

> m

denote the degree-rescaled empirical Laplacian 

eL(τ ) 

> m

f (x) := 1

τ dΩ+2 

 f (x) · 1

> mm

X

> j=1

κτ (x, x j ) − 1

> mm

X

> j=1

κτ (x, x j ) f (xj )

 ,

the function-space lift of the matrix 1  

> mτ dΩ+2

L(τ ), where dΩ = 3 is the intrinsic dimension of Ω.Belkin and Niyogi (2003, Thm. 3.1) show that for any f ∈ C2(Ω) and any interior point x ∈ Ω,

eL(τ ) 

> m

f (x) a.s. 

−−−−−−−−→  

> m→∞ , τ →0

−cd ρ(x) ∆ ρf (x), ∆ρf := ρ−1 div( ρ∇f ),

where cd > 0 is a kernel-dependent constant and ∆ρ is the weighted Laplace–Beltrami operator. The proof is a Taylor expansion of f at x to second order, using kernel symmetry to kill the first-order term and the Hessian trace to produce the Laplacian. This step gives the limit operator , but on its own says nothing about eigenvector convergence. 25 Step 2 (spectral convergence, von Luxburg–Belkin–Bousquet 2008). Lift the matrix to an integral operator T (τ ) 

> m

f (x) := 1

> m

P 

> j

κτ (x, x j )f (xj ) on C(Ω) , with population analog T (τ )f (x) := 

R 

> Ω

κτ (x, y )f (y)ρ(y) dy . Theorem 21 of vLBB states that under (i) continuous bounded symmetric positive kernel, (ii) i.i.d. samples from ρ > 0, (iii) isolated finite-multiplicity eigenvalues of T (τ ), the empirical eigenvalues and eigenfunctions converge: 

|λk(T (τ ) 

> m

) − λk(T (τ ))| → 0, ∥z(k) 

> m

− ϕ(k) 

> τ

∥L2(ρ) → 0,

where z(k) 

> m

is the Nyström-extended k-th eigenvector. The proof uses collectively compact operator theory : T (τ ) 

> m

→ T (τ ) collectively compactly, and isolated eigenvalues of compact operators persist under such perturbation with eigenprojection convergence in operator norm. The required collectively compact regime corresponds to mτ dΩ+2  

> m

/ log m → ∞ in the sample regime — the bandwidth condition restated in the theorem. 

Step 3 (combining Steps 1 and 2 as τ → 0). Step 2 gives convergence to ϕ(k) 

> τ

at fixed bandwidth; Step 1 says that as τ → 0, the rescaled T (τ )-eigenproblem converges to the eigenproblem of −∆ρ.Provided τm → 0 at a rate slow enough to maintain the collectively compact regime ( mτ dΩ+2  

> m

→ ∞ ,i.e. the theorem’s bandwidth condition with α > 0), the empirical eigenvector z(k+1)  

> m

converges uniformly on samples to the k-th non-constant eigenfunction of −∆ρ, with the ρ−1/2 factor coming from the standard weighted-Laplace–Beltrami normalization. 

Step 4 (identification on the Euclidean cube). On Ω = [0 , 1] 3 with uniform ρ, the weighted Laplace–Beltrami reduces to the ordinary 3D Laplacian ∆ = ∂2 

> x

+ ∂2 

> y

+ ∂2 

> z

. The kernel construction induces Neumann boundary conditions in the limit (it does not impose zero Dirichlet values, only zero normal derivative). The Neumann eigenproblem −∆ϕ = μϕ on [0 , 1] 3 has eigenfunctions 

cos( k1πx ) cos( k2πy ) cos( k3πz ) with eigenvalues (k21 + k22 + k23 )π2. The smallest eigenvalue is 

μ0 = 0 (constant mode); the next three are all μ1 = π2, spanned by cos( πx ), cos( πy ), cos( πz ).Each is a strictly monotone function of the corresponding Cartesian coordinate on [0 , 1] , so PCA on samples recovers the centered coordinates up to this monotone reparameterization. Sign-tests (the form of every axis-aligned ordinal question) are invariant under this reparameterization. □

Why three pieces, not one. Pointwise operator convergence (Step 1) does not imply spectral convergence: bounded operator sequences can have eigenvalues escaping to infinity or merging with neighbors. The substantive content is Step 2 (collectively compact + spectral gap ⇒ eigenvector convergence with quantitative control), and the precise rate mτ dΩ+2  

> m

→ ∞ comes from the regime where Step 2’s hypotheses remain valid as τ → 0.

G.2 Proof of Theorem 3 (realizability of spatial readouts) Setup. Write the singular vectors of the Theorem-1-minimizing H∗ as u∗

> k

, v∗

> k

, with singular values 

σ∗ 

> k

= ϵk for k ∈ [s]; assume s ≥ 4 and the conditions of Theorem 2. By Theorem 1(b), the 

k-th left singular vector of the mean-centered H∗ 

> c

is z(k+1) for k = 1 , 2, 3; equivalently, the k-th principal-component score of object i is 

ξi,k := ⟨h∗ 

> i

, V ∗

> :,k

⟩/σ ∗ 

> k

= z(k+1)  

> i

. (E.1) By Theorem 2, under the stated bandwidth and sample-size conditions, z(k+1) converges uniformly on samples to a Laplacian eigenfunction ϕk (Cartesian coordinate on the uniform-density cube, up to a monotone cosine reparameterization): 

sup 

> i∈[m]

ξi,k − ϕk(xi) −−−−→ 

> m→∞

0, k = 1 , 2, 3. (E.2) 

Step 1 (eigen-expansion of f ). The hypothesis of Theorem 3 is that f : R3 → R is a linear function of the monotonic coordinate transformations ϕ1, ϕ 2, ϕ 3. Hence there exist constants c1, c 2, c 3 ∈ R

(computed by Galerkin projection ck = R 

> Ω

f · ϕk dρ , finite under the stated regularity) such that 

f (x) = 

> 3

X

> k=1

ck ϕk(x), x ∈ Ω. (E.3) 26 Step 2 (construction of the linear functional). Define 

ℓ(h) := 

> 3

X

> k=1

ck

σ∗

> k

⟨h, V ∗

> :,k

⟩, h ∈ Rd. (E.4) This is linear in h and depends only on (ck, σ ∗

> k

, V ∗

> :,k

)3

> k=1

. Applied to h∗ 

> i

and using (E.1), 

ℓ(h∗ 

> i

) = 

> 3

X

> k=1

ck ξi,k .

Step 3 (uniform error bound). Combining (E.2)–(E.4), 

ℓ(h∗ 

> i

) − f (xi) =

> 3

X

> k=1

ck (ξi,k − ϕk(xi)) ≤

 3X

> k=1

|ck|



· max  

> k∈{ 1,2,3}

|ξi,k − ϕk(xi)|.

Choose m0(η) large enough that the right-hand side is < η uniformly in i ∈ [m]; existence of such 

m0 is exactly (E.2). □

Remark (extension to higher-degree readouts). If the readout function depends polynomially of degree ≤ r on x, the eigen-expansion uses the leading K(r) ∼  r+3 3

 Laplacian eigenfunctions; Theorem 1 then needs to be invoked with s ≥ K(r) + 1 . The argument generalizes term-by-term. For the axis-aligned ordinal questions used in our spatial benchmarks (front/back, left/right, distance-order), r = 1 suffices and s = 4 is the operative rank. 

G.3 Proof of Theorem 4 (sample-complexity reduction) Setup. Let {(Φ i, y i)}Ni=1 be i.i.d. object-token observations, Φi = h∗ 

> i

∈ Rd drawn from the population distribution induced by Theorem-1-minimizing scenes, with yi = w⋆⊤Φi + ξi, ξi sub-Gaussian with variance proxy σ2 

> ξ

, and w⋆ ∈ span( V ∗

> :,1:3

). Let bw proj  

> N

denote the OLS estimator obtained by first projecting features onto V ∗ 

> :,1:3

and regressing the labels on the resulting 3-dimensional features. 

Step 1 (change of basis). Let Ψi := V ∗⊤ 

> :,1:3

Φi ∈ R3. Stack into Ψ ∈ RN ×3 and y ∈ RN . By the structural assumption there exists a ∈ R3 with w⋆ = V ∗

> :,1:3

a, so w⋆⊤Φi = a⊤Ψi. The model reduces to 

yi = a⊤Ψi + ξi, i = 1 , . . . , N. (E.5) 

Step 2 (projected OLS). 

baN = (Ψ ⊤Ψ) −1Ψ⊤y, bw proj  

> N

:= V ∗

> :,1:3

baN .

By construction bw proj  

> N

has zero component in span( V ∗

> :,4: d

), matching the sparsity pattern of w⋆, so the bias is zero. 

Step 3 (variance bound). With the population PC-score covariance ΣΨ := E[ΨΨ ⊤] assumed non-degenerate, the standard fixed-design analysis (Wainwright, High-Dimensional Statistics , Thm. 13.4) gives 

E∥baN − a∥2ΣΨ Ψ = σ2 

> ξ

· tr  (Ψ ⊤Ψ) −1ΣΨ

 −−−−→ 

> N→∞

3σ2

> ξ

N ,

since (Ψ ⊤Ψ/N )−1 → Σ−1Ψ and tr(Σ −1Ψ ΣΨ) = 3 . Because w⋆ aligns exactly with span( V ∗

> :,1:3

), the prediction error decomposes as pure variance: 

EΦ

h

( bw proj ⊤ 

> N

Φ − w⋆⊤Φ) 2i

= E(baN − a)⊤ΣΨ(baN − a) ≤ 3σ2

> ξ

N

 1 + o(1) .

Step 4 (high-probability upgrade). Local-Rademacher / Gaussian-complexity bounds for the linear class {Ψ 7 → a⊤Ψ : a ∈ R3} (Wainwright Thm. 13.4) upgrade the expectation bound to 

P EΦ

h

( bw proj ⊤ 

> N

Φ − w⋆⊤Φ) 2i

≤ C σ 2 

> ξ

(3 + log(1 /δ )) 

N

!

≥ 1 − δ, 

27 which is the form stated in the theorem (modulo the constant absorbing the sub-Gaussian factor and 

∥Σ−1Ψ ∥op ·∥ ΣΨ∥op ). 

Step 5 (comparison to unstructured). The unstructured OLS bw full  

> N

= (Φ ⊤Φ) †Φ⊤y decomposes as V ∗

> :,1:3

baN + V ∗

> :,4: d

bbN . The tail estimator bbN regresses pure noise on d − 3 orthogonal directions, contributing variance (d − 3) σ2 

> ξ

/N . Total prediction error therefore scales as d σ 2 

> ξ

/N — d/ 3 times worse than the projected estimator. □

Remark. The proof presupposes knowing V ∗ 

> :,1:3

to perform the projection. In practice, this is what motivates training with the Dirichlet loss: by Theorem 1, the model itself learns to align its top PCs with the Laplacian eigenmaps, so the projection is realized in the model rather than imposed by a separate processing step. Theorem 5 formalizes the connection. 

G.4 Proof of Theorem 5 (training-time risk decomposition) Setup. Let Θ ⊂ Rp be open, LLM , RX : Θ → R both C2, with ∇2LLM (θ∗) ≻ 0 at the unique population minimizer θ∗ of LLM . Let Rspatial : Θ → R be C2 and define θ∗ 

> λ

:= arg min θ (LLM (θ) + 

λ RX (θ)) .

Step 1 (implicit function theorem). Define 

F (θ, λ ) := ∇θ LLM (θ) + λ ∇θ RX (θ).

Then F (θ∗, 0) = 0 by first-order optimality, and the Jacobian ∂F/∂θ |(θ∗,0) = ∇2LLM (θ∗) ≻ 0

is invertible. By the implicit function theorem (Krantz & Parks, The Implicit Function Theorem ), there exist a neighborhood U ∋ 0 in R and a unique C2 map θ∗(·) : U → Θ with θ∗(0) = θ∗ and 

F (θ∗(λ), λ ) = 0 for all λ ∈ U . By continuity, ∇2(LLM + λRX )( θ∗(λ)) ≻ 0 for λ small, so θ∗(λ)

is a strict local minimizer; we set θ∗ 

> λ

:= θ∗(λ) for λ ∈ [0 , λ 0] where λ0 is the largest value for which this remains the global minimizer. 

Step 2 (first derivative of θ∗(λ)). Differentiating F (θ∗(λ), λ ) = 0 in λ:

∇2 

> θ

(LLM + λRX )( θ∗(λ)) dθ ∗

dλ + ∇R X (θ∗(λ)) = 0 .

At λ = 0 : dθ ∗(λ)

dλ λ=0 

= − [∇2LLM (θ∗)] −1 ∇R X (θ∗).

Taylor-expanding the smooth map λ 7 → θ∗(λ):

θ∗ 

> λ

− θ∗ = −λ [∇2LLM (θ∗)] −1 ∇R X (θ∗) + O(λ2). (E.6) 

Step 3 (Taylor expansion of the spatial risk). Since Rspatial ∈ C2,

Rspatial (θ∗

> λ

) − Rspatial (θ∗) = ∇Rspatial (θ∗)⊤(θ∗ 

> λ

− θ∗) + 12 (θ∗ 

> λ

− θ∗)⊤∇2Rspatial (˜θ) ( θ∗ 

> λ

− θ∗),

for some ˜θ on the segment [θ∗, θ ∗

> λ

]. Substituting (E.6) into the linear term: 

∇Rspatial (θ∗)⊤(θ∗ 

> λ

− θ∗) = −λ ∇R⊤

> spatial

[∇2LLM ]−1∇R X + O(λ2).

The quadratic term in (E.6) contributes O(λ2) as well. Combining, 

Rspatial (θ∗

> λ

) − Rspatial (θ∗) = −λ ∇R⊤

> spatial

[∇2LLM ]−1∇R X + O(λ2). (E.7) 

Step 4 (define β). The available improvement in Dirichlet ratio at θ∗ is 

∆ := RX (θ∗) − R ∗ 

> X

≥ 0,

strictly positive whenever θ∗ does not coincide with the Dirichlet minimizer (the generic case for any LM not specifically pretrained for spatial structure). Define 

β := ∇Rspatial (θ∗)⊤ [∇2LLM (θ∗)] −1 ∇R X (θ∗)∆ .

Substituting into (E.7): 

Rspatial (θ∗

> λ

) = Rspatial (θ∗) − λ β ∆ + O(λ2),

exactly the form claimed in the theorem. 

Step 5 (sign of β). The numerator of β is the inner product of two vectors: 28 • g := ∇Rspatial (θ∗), the steepest-ascent direction of the spatial risk; • r := [ ∇2LLM (θ∗)] −1 ∇R X (θ∗), the inverse-Hessian-warped Dirichlet-ascent direction. 

β > 0 iff ⟨g, r ⟩ > 0, i.e., iff the gradient direction along which the Dirichlet ratio decreases is correlated with the direction along which the spatial risk decreases, in the metric induced by ∇2LLM .Theorems 3 and 4 provide the qualitative reason this correlation holds for VLMs whose top-3 PCs already partially align with world coordinates: lowering RX tightens that alignment, which makes more spatial readouts realizable (Theorem 3) and reduces their estimation variance (Theorem 4). □

Remark on the validity range. The first-order term dominates whenever λβ ∆ ≫

λ2M2∥∇ Rspatial ∥ for M2 the bound on the Hessian remainder, i.e., λ ≪ β∆/(M2∥∇ Rspatial ∥).Beyond this regime the O(λ2) over-regularization term takes over, predicting non-monotonic behav-ior in λ. The empirical inversion in Table 4 (best at λ = 3 for Qwen and λ = 1 for InternVL, with degradation at λ = 9 ) is the data-driven location of this crossover. 

Remark on what β bundles. β is a single scalar but it carries three model-specific quantities: (i) ∥∇ Rspatial (θ∗)∥ — how strongly spatial questions react to small parameter perturbations at the LM-loss optimum; (ii) the spectrum of ∇2LLM (θ∗) — a flatter LM optimum makes the inverse Hessian larger, amplifying the regularizer’s effect; (iii) the angle between ∇Rspatial and ∇R X in the whitened metric — how aligned the two losses are. A model whose pretraining leaves a flat LM optimum near a spatially-aware sub-manifold has a large β, predicting the InternVL-larger-than-Qwen ordering observed in Table 4. 

H Training Details 

This appendix expands on the training setup summarized in §5.3. The end-to-end training script is at scripts/train_with_dirichlet.py (included in the supplemental materials); the Dirichlet objective itself is implemented in scripts/dirichlet_loss.py and consumed by the script through a forward hook on the chosen residual-stream layer. 

Backbones and adapter. We fine-tune three open-weights VLMs in identical configurations: Qwen2.5-VL-7B [ 35 ], InternVL3-8B [ 36 ]. The vision tower and all non-LoRA LLM weights are frozen. We attach LoRA adapters of rank r=16 (with α=32 , dropout 0.05 , no bias) to the four attention projections {q_proj, k _proj, v _proj, o _proj } of every transformer block in the backbone-specific “cognitive-map band” (Qwen2.5-VL-7B: layers 13 –21 centered at L17; InternVL3-8B: layers 

14 –22 centered at L18; LLaVA-OneVision-7B: layers 17 –25 centered at L21). Layers outside this band are not in the LoRA target set, so the early visual-binding layers and the late linguistic-reasoning layers see no parameter updates — this restriction is what makes “ 500 steps” a meaningful budget (cf. §5.3). 

Loss. Given a batch of (image, question, answer, object_coords) tuples, the per-step loss is 

Ltotal = LCE (answer | image , question ) + λdir · E X

 P⊥Hℓ(θ),

where LCE is standard next-token cross-entropy on the answer tokens, Hℓ is the residual-stream activation at the cognitive-map layer ℓ pooled over each object’s visual tokens (mask-driven, coverage-weighted; cf. §4), P⊥ is the linear projection that residualizes against the cross-scene color/shape prototypes (§4), and EX is the Dirichlet-energy ratio against the per-scene 3D-coordinate kernel graph 

Wij = κτ (xi, x j ) with τ set per scene to half the median pairwise distance. We use the multi-layer variant ( residMulti ) by default, which applies the Dirichlet term at 5 consecutive layers spanning the cognitive-map band (with equal weight); the single-layer variant is reported in the ablations of §5.5. 

Optimizer and schedule. AdamW with β1=0 .9, β 2=0 .999 , weight-decay 0.0 on LoRA parameters, learning-rate 1×10 −4, linear warmup over the first 50 steps, and constant thereafter. Gradients are clipped to global norm 1.0. We train at bf16 mixed precision with effective batch size 4 (per-GPU batch 1, gradient accumulation 4) for 500 optimizer steps; this corresponds to roughly 0.7 epochs over the 2,988 -row training split. No early stopping — we keep the final checkpoint. 29 λdir sweep. For each (model, regularizer-variant) cell we sweep λdir ∈ { 0, 0.1, 0.3, 1, 3} where 

λdir =0 recovers a pure-LoRA baseline ( lam0 ). The headline results in Tables 1, 2, and 3 use the λ chosen by best mean validation accuracy across n=4 seeds: λdir =0 .3 for residMulti on Qwen2.5-VL-7B and LLaVA-OneVision-7B, and λdir =1 for residMulti on InternVL3-8B. The full 

λ-vs-accuracy/RSA trade-off curve is reported in §5.5. 

Seeds and statistical reporting. Every cell is averaged over n=4 random seeds (seeds 0, 1, 2, 3), each controlling LoRA initialization, data shuffling, and dropout. We report mean ±2 SE of seed-paired differences against the relevant within-cell baseline. Cells with |∆| < 2 pp at n=4 should be treated as exploratory — our project’s track record is that small effects at n=4 sometimes shrink at 

n=8 .

Baselines. (1) Naive LoRA (λdir =0 ): identical setup minus the Dirichlet term. (2) 2D-pixel regularization : the same penalty applied with Wij built from 2D pixel-space distances rather than 

3D coordinates — a control that severs the connection between the loss target and the world-frame geometry. (3) Text-domain cognitive-map prediction [22 ]: the same LoRA budget redirected to a supervised loss that asks the model to emit the scene’s 3D coordinates as text tokens. All baselines use identical learning rate, schedule, batch size, step count, LoRA rank and target modules, layer band, and seed pool, so the only varying axis is the training signal. 

Hardware. Each 500 -step run takes roughly 40 –60 minutes on a single H100 ( 80 GB), peaking at ∼55 GB activation memory at bf16 ; the full sweep across 3 models, 5 λ settings, 2 regularizer variants and 4 seeds is ∼120 runs in total. 

Evaluation. Checkpoints are evaluated zero-shot on the full 5,130 -item VSI-Bench [ 21 ] and MindCube [ 22 ] test sets without any task-specific prompting. We use mean-relative-accuracy scoring on numeric-answer tasks (consistent with VSI-Bench’s official protocol. Inference uses greedy decoding at bf16 .

I Others 

I.1 Limitation and discussion 

While our findings demonstrate the efficacy of isolating and shaping latent spatial representations in VLMs, our current scope is primarily limited to scene topology. Spatial understanding is inherently multifaceted; future work will investigate how other critical aspects of spatial relationships—such as absolute metric distance and hierarchical object dependencies—are encoded within the model’s residual stream. Building upon these topological foundations, we also plan to explore how applying our Dirichlet-energy regularization to fine-tuning on complex, real-world datasets can unlock even more substantial performance improvements in downstream tasks. 

I.2 Dirichlet loss implementation and residualization basis fitting 

This subsection details the step-by-step computation that translates the abstract loss defined in § 5.2 into the concrete tensor operations implemented in scripts/dirichlet_loss.py and the offline basis-fitting procedure in scripts/build_residualization_basis.py . The notation follows § 5: Hℓ ∈ RB×n×d denotes the residual stream at the cognitive-map layer, pooled over per-object visual tokens (§ 4); X ∈ RB×n×3 represents the per-scene ground-truth 3D coordinate tensor; and 

W ∈ Rd×k is the orthonormal nuisance basis, fitted once per backbone prior to training. 

Per-step Dirichlet ratio. Given a batch of B scenes with up to n objects each, the regularizer evaluates 

RX (H) = 1

B

> B

X

> b=1

P 

> i,j

W (b) 

> ij

∥h(b) 

> i

− h(b) 

> j

∥2

P 

> i,j

∥h(b) 

> i

− h(b) 

> j

∥2 + ε , W (b) 

> ij

= exp 



− ∥x(b)   

> i−x(b)
> j∥2
> 2τ2



⊮[i̸ = j], (17) with ε = 10 −8 for numerical stability. The numerator and denominator are computed in vectorized form via torch.cdist ; valid-object masks are applied as outer products ⊮[valid i]·⊮[valid j ] to handle 30 batches with variable n. Normalizing by the denominator (rather than using the unnormalized energy P 

> ij

Wij ∥hi −hj ∥2) makes the loss scale-invariant in H, so the LM head is free to rescale activations without paying a Dirichlet penalty for sheer norm growth. This is the form used everywhere in the body; the unnormalized variant is available in code but never used for training runs reported in this paper. 

Per-scene bandwidth. We set τ per scene to half the median pairwise distance among that scene’s objects: τ (b) = 12 median i<j ∥x(b) 

> i

− x(b) 

> j

∥. Per-scene scaling makes the kernel locally adaptive — scenes with tightly clustered objects use a smaller τ , scenes with spread-out objects use a larger one — so the same λdir has comparable effect across the batch. The half-median heuristic puts the kernel’s sweet spot near the typical nearest-neighbor scale, which is what Theorem 2 requires for the Belkin–Niyogi limit to apply. 

Forward-hook capture of Hℓ. The per-token residual stream is captured by registering a forward hook on the chosen transformer block ( ResidualStreamHook in scripts/dirichlet_loss.py ). For the multi-layer ( residMulti ) variant, 5 hooks are registered simultaneously across the cognitive-map band, and their per-layer Dirichlet ratios are averaged with equal weight. The pooling step that translates the per-token (B, T, d ) tensor into the per-object (B, n, d ) tensor uses the same mask-driven, coverage-weighted pool defined in § 4, with object indices derived from the tokenizer’s name spans in the prompt—so the loss only sees activations corresponding to objects the prompt actually names. 

Residualization projector P⊥. The projector that residualizes against the nuisance subspace is P⊥ = Id − W W ⊤, where W ∈ Rd×k has orthonormal columns spanning the color +

shape directions. We fit W once per backbone, prior to training, using the following pipeline (scripts/build_residualization_basis.py ): 1. Extract per-object residual-stream activations H ∈ RN ×d from the base pretrained model (no LoRA, no Dirichlet) at the cognitive-map layer over the full 1,000 -scene Free6DoF probing corpus. 2. Fit two multinomial logistic-regression probes on standardized H: one for the 8-way color label and one for the 3-way shape label, both with ℓ2 regularization C = 0 .1 and L-BFGS to convergence (max 2,000 iterations). 3. Recover the discriminating directions in the un-standardized H-space by composing each probe’s weight matrix with the inverse of the per-feature standard deviations: fWcolor = W LR color diag(1 /σ j ),and analogously for shape. Each row is rescaled to unit norm. 4. Stack into fW = [ fWcolor ; fWshape ] ∈ R(8+3) ×d and orthonormalize via thin QR: fW ⊤ = QR ,taking W ← Q. Drop columns with diagonal |Rkk | below 10 −6 of the maximum (rank-deficient class directions). The resulting W has orthonormal columns and effective rank k ∈ { 9, 10 , 11 }

depending on the backbone (the multinomial is rank-deficient by one per categorical, so ≤ 7 + 2 = 9 free directions). 5. Save W to disk and freeze it for the entire fine-tuning run; P⊥ = Id − W W ⊤ is then a single 

d × d matrix multiply per forward pass on the captured object-token activations. 

Algorithm summary. The end-to-end per-step computation is: 1. Forward pass , capturing Hℓ ∈ RB×T ×d at each Dirichlet-target layer via forward hooks. 2. Object pooling : H ← pool( Hℓ, name _spans) ∈ RB×n×d.3. Residualize : eH ← H P ⊥ = H − (HW )W ⊤.4. Per-scene bandwidth : τ (b) ← 12 median i<j ∥x(b) 

> i

− x(b) 

> j

∥.5. Kernel : W (b) 

> ij

← exp( −∥ x(b) 

> i

− x(b) 

> j

∥2/(2 τ (b)2 )) · ⊮[i̸ = j].6. Pairwise distances : D(b) 

> ij

← ∥ eh(b) 

> i

− eh(b) 

> j

∥2 via torch.cdist .7. Mask invalid pairs via outer product of validity masks. 31 8. Dirichlet ratio : per-scene r(b) ← P 

> ij

W (b) 

> ij

D(b) 

> ij

/(P 

> ij

D(b) 

> ij

+ ε), then average over b.9. Total loss : Ltotal ← L CE + λdir · R X , backward through both the Dirichlet term (which feeds gradients to LoRA via H) and the cross-entropy. For the residMulti variant, steps 1–8 run independently at 5 layers and step 9 averages the resulting ratios. 

I.3 Manifold Learning and Latent Regularization 

Our methodology is heavily grounded in the mathematical foundations of spectral graph theory and manifold learning. Foundational works on Laplacian eigenmaps [ 45 , 47 ] and diffusion maps established that the principal eigenvectors of a graph Laplacian yield an optimal low-dimensional continuous representation that preserves local neighborhood structures. To measure how smoothly a representation varies over a given topology, researchers compute the Dirichlet energy [ 46 ], which acts as a penalty against high-frequency geometric variations. While spectral penalties and Dirichlet-energy regularization [48, 49] have been widely used in classical graph representation learning [50] and semi-supervised learning [ 51 , 52 ], their application as a targeted regularizer for the hidden states of large transformer models is largely unexplored. By formally connecting the VLM residual stream to Laplacian eigenmaps, we introduce a mathematically principled way to isolate and regularize the internal geometric structure of language models. 

I.4 Representational similarity (RSA) 

Build two m × m symmetric similarity matrices, one from activations and one from 3D coordinates, and rank-correlate their off-diagonal entries: 

Sact  

> ij

= ⟨˜h(s,o i) 

> ℓ

, ˜h(s,o j ) 

> ℓ

⟩∥˜h(s,o i) 

> ℓ

∥ ∥ ˜h(s,o j ) 

> ℓ

∥ , Stopo  

> ij

= κτ (xi, x j ) = exp 



− ∥xi − xj ∥2

2τ 2



, (18) 

RSA (s) = Spearman   vec off (Sact ), vec off (Stopo ) . (19) 

τ is set per scene to half the median pairwise distance. Spearman (rank) correlation makes the metric invariant under arbitrary monotone rescalings of either similarity, removing the linearity assumption a Pearson RSA would inject. 32