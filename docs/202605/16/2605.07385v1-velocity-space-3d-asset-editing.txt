Title: Velocity-Space 3D Asset Editing

URL Source: https://arxiv.org/pdf/2605.07385v1

Published Time: Mon, 11 May 2026 01:00:39 GMT

Number of Pages: 17

Markdown Content:
# Velocity-Space 3D Asset Editing 

Hao Liu 1,3 Yuxuan Lin 1 Jingfeng Guo 2 Ruihang Chu 1 Junjie Wang 1

Ruotong Li 3 Yujiu Yang 1∗

> 1

Tsinghua University 2South China University of Technology 3Peng Cheng Laboratory 

{h-l25, linyx23, rhchu}@mails.tsinghua.edu.cn drjfguo@gmail.com, {wangjunjie, yang.yujiu}@sz.tsinghua.edu.cn, lirt@pcl.ac.cn 

## Abstract 

Editing a 3D asset locally, modifying a target region while preserving the rest, is a fundamental requirement of native 3D editing. Existing methods enforce locality through mechanisms external to the generator, such as manual 3D masks, post-hoc voxel merging, or 2D multi-view lifting. None of them intervene where the corruption actually originates: inside the ODE sampler. For a rectified-flow generator to achieve faithful local editing, its velocity field should be strong over the target editing region while vanishing on preserved content. Yet a single velocity field can hardly satisfy both requirements simultaneously, leading to three prob-lems: (i) identity leakage that keeps the edit signal non-zero on preserved regions; (ii) no dedicated edit-amplification channel, so strengthening the edit inevitably perturbs identity; and (iii) an identity drag at the geometry and material stages, where a global condition pulls every token toward the target. We propose VS3D 

(Velocity-Space 3D Asset editing ), an inversion-free, training-free, and mask-free framework that addresses each problem with a targeted intervention inside the sampler. VS3D integrates three complementary modules, each corresponding to a specific stage of the editing pipeline. Reconstruction-Anchored Source Injection (RASI) absorbs identity leakage by turning the unconditional embedding into a per-step, asset-specific anchor calibrated through source reconstruction. Partial-Mean Guidance (PMG) amplifies the edit signal by contrasting high- and low-quality subsample estimates of the velocity difference, active only where a consistent edit exists. Twin-Agreement Residual injection (TAR) lets the sampler decide token by token what to preserve at the geometry and material stages. Experiments on diverse 3D assets show that VS3D outperforms state-of-the-art native-3D and 2D-lifted editors, demonstrating that a purely velocity-space approach can serve as a general-purpose editing paradigm for pretrained 3D generators. 

## 1 Introduction 

Precise local editing of 3D assets is essential for content creation, game development, and embodied simulation, yet it poses a fundamental dual requirement on any pretrained generator: the model must apply a strong, targeted modification to the region the user specifies while leaving every other region strictly unchanged. The dominant family of native 3D generators [ 7 , 16 – 19 ] now builds on rectified-flow DiTs [ 4 , 3], where generation is an ODE whose velocity field is learned by conditional flow matching. Editing with such a model therefore reduces to controlling its velocity field : the velocity update must carry a non-trivial edit signal on the target region and vanish on the rest. How to achieve this control on a frozen 3D rectified-flow DiT, without resorting to external masks or additional training, remains an open problem. 

> ∗Corresponding author. Preprint.
> arXiv:2605.07385v1 [cs.GR] 8 May 2026

Existing 3D editors do not ask why the velocity field of the pretrained ODE fails to stay silent on non-edited regions; they accept it and outsource identity to a mechanism bolted outside the generator. Three routes recur: (i) mask-based editors [ 8– 10 , 12 ] ask the user or an external tool to supply a binary 3D region that pins every non-edited voxel to the source, tedious to draw on a voxel grid, brittle for natural edits whose support fades gradually, and prone to seam artefacts wherever a hard binary boundary meets smooth geometry; (ii) post-hoc mergers [14 ] try to recover identity by connected-component stitching that can neither distinguish genuine edits from leakage nor suppress boundary artefacts at the merge seam; and (iii) multi-view / video editors [ 15 , 11 ] enforce identity in 2D and lift the result back through a reconstructor, yet edits that alter topology or volumetric occupancy introduce multi-view inconsistencies that accumulate across views. All three share a deeper limitation: because the velocity field of a rectified-flow ODE is the sole carrier of both identity and edit signals , the most direct path to simultaneous identity preservation and edit amplification is to control that velocity field from within; none of the existing routes attempt this, and instead impose identity from outside the ODE without ever examining the ODE itself. Among inversion-free velocity-space editing formulations, FlowEdit [ 2] couples a source and a target branch through shared noise so that their differential velocity v∆ directly drives the edit offset without inverting the source into noise. We adopt this coupling and examine the ODE itself to understand why identity still drifts. Our analysis reveals three problems. (P1) Two-channel identity leakage. 

Even when the two conditions locally agree, v∆ does not vanish on preserved content: it carries a 

CFG-asymmetry residual , structurally non-zero because effective editing requires a much stronger target guidance than source guidance, and an attention-mediated condition residual , because the image-feature embedding is a single global token whose perturbation propagates via dense cross-attention into every voxel token. Euler integration accumulates both residuals and visibly deforms non-edited regions (closed-form derivation in §3.2). (P2) Absence of a dedicated edit-amplification channel. Identity preservation and edit amplification compete for the same degrees of freedom within the velocity field. Because few-sample inference is unavoidable for costly 3D DiTs, the Monte-Carlo estimate of v∆ is dominated by estimation variance rather than the consistent edit signal, leaving the edit region under-expressed even when the leakage has been suppressed. (P3) Global-condition override. The downstream geometry and material stages refine shape and appearance on the coordinate occupancy produced by the sparse-occupancy stage, each conditioned on a single global image-feature token of the target image that pulls every token toward the target appearance. Without a counterbalancing mechanism, this global drag overwrites the source identity on preserved tokens, causing geometry distortion and texture drift. We address each problem with a targeted intervention inside the ODE, and the three solutions share a common insight: identity preservation and edit amplification should both be derived from the velocity field itself, not imposed from outside . For P1 , Reconstruction-Anchored Source Injection (RASI) turns the unconditional embedding into a per-step, asset-specific handle optimised through a source-reconstruction probe, absorbing both leakage channels at once so that v∆ collapses toward zero on non-edited voxels without any external region indication. For P2 , Partial-Mean Guidance (PMG) contrasts a high-quality and a low-quality subsample estimate of the edit direction and extrapolates away from the noisier one; the amplification is automatically gated by v∆ itself, active only where a consistent edit signal survives and inert where RASI has already suppressed the velocity. For P3 ,Twin-Agreement Residual injection (TAR) runs a second sparse-DiT pass on the same coordinate occupancy, but conditioned on the source image, and measures per-token agreement; tokens on which the two passes agree are blended back toward the source encoding, while the edit region is left untouched. We refer to the resulting system as VS3D , an inversion-free, training-free, mask-free native-3D editor that achieves high-fidelity local edits with strong identity preservation entirely inside the ODE, without resorting to external tools. 

Contributions. Our main contributions are as follows: • We present Reconstruction-Anchored Source Injection (RASI) to eliminate the two-channel identity leakage (CFG-asymmetry and attention-mediated condition residual), en-suring v∆ collapses toward zero on non-edited regions without any external mask (§3.2). • We introduce Partial-Mean Guidance (PMG) to overcome the conflict between edit amplification and identity preservation within the same velocity field, boosting the consistent edit signal while remaining inert on preserved regions (§3.3). 2• We propose Twin-Agreement Residual injection (TAR) to suppress the global identity drag at the geometry and material stages that otherwise distorts preserved geometry and texture (§3.4). • Combining the above, we build VS3D , a purely velocity-space, inversion-free, training-free, and mask-free native-3D editing framework that outperforms state-of-the-art native-3D and 2D-lifted editors on both edit fidelity and non-edit-region preservation (§4). 

## 2 Background 

Native structured-latent 3D generator. We adopt TRELLIS 2.0 [ 7] as a frozen backbone: a native 3D generator whose generation process unfolds in three stages with three independent DiTs, all conditioned on a DINOv3-L [ 5] embedding of the input image. The three stages are (i) sparse-structure generation , (ii) geometry generation , and (iii) material generation . Stage 1 operates on a 

dense low-resolution latent zss ∈ RC×R×R×R (R=64 in our 1024 3-output setting) and is decoded into a binary occupancy grid whose active coordinates V ⊂ Z3 define the voxel support of the subsequent stages. Stages 2–3 are sparse DiTs on V whose token-wise latents we refer to as structured latents 

(SLATs) – the geometry SLAT and the material SLAT (PBR channels: albedo, roughness, metallic, normal) – decoded by two Sparse Compression VAEs (SC-VAEs) at 16 × spatial compression; the material SC-VAE is conditioned on the geometry VAE’s subdivision structure during upsampling [ 7], aligning geometry and PBR fields on the shared O-Voxel support. 

Rectified flow. Each stage of TRELLIS 2.0 is a rectified flow [ 4 ] trained under the conditional flow-matching objective [ 3]. For a clean latent x0 and Gaussian noise ε ∼ N (0 , I), the forward process is the straight-line interpolation 

x(t) = (1 − t) x0 + t ε, t ∈ [0 , 1] , (1) whose constant time derivative ε − x0 is the regression target of the velocity network vθ (x, t, c ),where c denotes the image-conditioning signal. Setting vθ (x, t, c ) ≈ ε − x0 and eliminating ε via (1) gives the implicit clean-sample estimate 

ˆx0(x, t, c ) = x − t v θ (x, t, c ). (2) 

Inversion-free velocity-space editing. Given the clean source latent xsrc and conditions csrc , c tgt ,FlowEdit [ 2] transports xsrc to an edited latent without ever inverting it into a noise latent. It maintains an edit state zedit  

> t

initialised at zedit 1 =xsrc and integrated from t=1 down to t=0 along a discrete schedule 1= t0>t 1> · · · >t T =0 with step size ∆t=tk+1 − tk. At each step the two branches are coupled by a shared noise draw ε ∼ N (0 , I) on the interpolation of (1), 

zsrc  

> t

= (1 − t) xsrc + t ε, ztgt  

> t

= zedit  

> t

+  zsrc  

> t

− xsrc 

, (3) so that ztgt  

> t

− zsrc  

> t

equals the running edit offset zedit  

> t

− xsrc at every t. Each branch is then sampled under its own condition and its own classifier-free guidance weight, 

˜vθ (zt, t, c, ϕ ; ω) = (1 + ω) vθ (zt, t, c ) − ω v θ (zt, t, ϕ ), (4) where ω is the guidance weight and ϕ is an unconditional embedding (by default the network’s built-in null ϕ0). Since ztgt  

> t

− zsrc  

> t

tracks the edit offset itself, the direction along which this offset should evolve is exactly the difference of the two branches’ guided velocities: 

v∆(zedit  

> t

, t ) = ˜ vθ (ztgt  

> t

, t, c tgt , ϕ ; ωtgt ) − ˜vθ (zsrc  

> t

, t, c src , ϕ ; ωsrc ). (5) The edit state is advanced by one Euler step zedit  

> tk+1

= zedit  

> tk

+ ∆ t v ∆(zedit  

> tk

, t k), where v∆ is estimated as a Monte-Carlo average over S independent noise realisations of the coupling (3) , with S kept small on a 3D rectified-flow DiT due to per-step cost. Integrating v∆ thus transports xsrc to an edited latent directly in velocity space, without ever inverting the source into a noise latent. 

## 3 VS3D 

3.1 Overview 

VS3D addresses mask-free local editing with three modules acting on the ODE, each targeting a distinct failure mode (Fig. 1). The editing problem naturally splits into two regimes. The dense 

3Add a closed umbrella          

> Source Image/Condition
> Render
> Source Voxel
> Voxelize
> Edited Voxel
> 2D Edit
> RASI
> optimize & cache for in non-edited region
> ×Nmax Cached
> Target Image/Condition =Source Condition Source Condition
> =Target Condition
> ×Nmax
> FlowEdit with per-step injection and edit-signal amplification
> PMG
> decode
> Geometry & Material Slat Residual injection Twin-agreement detection
> Source Asset Edited Asset
> TAR
> Slat Keep Map

Figure 1: Overview of the VS3D pipeline. A source 3D asset is rendered and 2D-edited to obtain the condition. Stage 1 operates on the dense occupancy latent: RASI (§3.2) optimises a per-step 

ϕt to suppress v∆ on non-edited regions, and PMG (§3.3) amplifies the edit signal via subsample extrapolation. Stages 2–3 handle sparse geometry and material SLATs: TAR (§3.4) computes a token-wise pkeep map (blue = preserve, red = edit) and injects source residuals accordingly to produce the final edited asset. Stage-1 occupancy latent admits FlowEdit’s coupling (3) , so editing reduces to shaping v∆; however, two leakage channels—CFG asymmetry and the global-condition gap (§3.2)—keep v∆ structurally non-zero on preserved content. The sparse Stage-2/3 geometry and material SLATs have mismatched active coordinates between source and target, making velocity-space coupling unavailable; a single global DINOv3 embedding further spreads the target condition into every token, so the challenge becomes which tokens to leave alone (§3.4). Our three modules resolve these issues in sequence: (i) Reconstruction-Anchored Source Injection 

(RASI, §3.2) closes Stage-1 identity leakage by turning the null embedding into a per-step, asset-specific ϕtk fitted via a source-reconstruction probe, absorbing both leakage channels simultaneously. 

(ii) Partial-Mean Guidance (PMG, §3.3) amplifies the edit signal by extrapolating from a noisier subsample mean of v∆ toward a cleaner one; the amplification is automatically gated by v∆ itself, active only where a consistent edit exists. (iii) Twin-Agreement Residual Injection (TAR, §3.4) lets the Stage-2/3 sparse-DiT sampler decide token by token what to preserve and what to regenerate, handling additions, removals, and replacements uniformly. 

Problem formulation. Given a source 3D asset Asrc with geometry and material, and a pair of reference images (Isrc , I tgt ) depicting its current and desired appearance, we seek an edited asset that realises the edit in the implied region while leaving the rest intact, under two hard constraints: • Training-free : no model or LoRA is fine-tuned; all parameters of vθ are frozen. • Mask-free : neither manual masks nor external segmentation networks are used to delineate the edit region. 

3.2 Reconstruction-Anchored Source Injection for Identity-Leakage Closure 

The velocity difference v∆ should vanish on non-edited voxels, yet off-the-shelf FlowEdit leaves a persistent residual that Euler integration accumulates into identity drift. We trace this residual to two structural channels and close both by turning the unconditional embedding ϕ into a per-step, asset-specific calibration handle optimised through a source-reconstruction probe. 

Problem diagnosis. To isolate the first leakage channel, consider forcing ctgt = csrc : the two branches of (3) then share the same condition and should ideally give v∆ = 0 , yet substituting 

ctgt = csrc into (5) and expanding (4) yields 

v∆ ctgt =csrc 

= ( ωtgt − ωsrc )  vθ (zsrc  

> t

, t, c src ) − vθ (zsrc  

> t

, t, ϕ ) . (6) 4Effective editing adopts ωtgt ≫ ωsrc for the target condition to take effect, so the prefactor in (6) is structurally non-zero even in this degenerate regime: asymmetric CFG weights alone are already enough to keep v∆ away from zero, independent of any condition gap. Second, local agreement on a non-edited region does not imply global agreement: since DINOv3 encodes each image into a single global embedding, any difference in the edited region perturbs csrc , c tgt as a whole, and dense cross-attention then propagates this gap into every voxel token, so vθ (·, t, c src )̸ = vθ (·, t, c tgt ) even on non-edited voxels. Euler integration accumulates both residuals into zedit  

> t

along the non-edited support, and mask-free editing cannot remove them after the fact. 

Absorbing leakage via ϕ. Among the symbols in (6) , vθ is frozen, csrc , c tgt carry the edit semantics, and ωtgt , ω src set the edit strength. The unconditional embedding ϕ is the natural absorption site for both leakage channels : optimising ϕ per timestep encodes asset-specific reconstruction information without interfering with the condition or the guidance schedule [ 1]. We design a source-reconstruction probe: at each active step tk, we replace ctgt with csrc on the target branch while keeping its guidance weight at ωtgt , and ask whether a single Euler step of v∆ can bring zedit  

> tk

back to 

xsrc . Switching only the condition but not the CFG weight reproduces the exact guidance schedule used at editing time, so both leakage channels, including the (ωtgt − ωsrc ) prefactor, are present in the probe and available to be absorbed by ϕ. Concretely, we solve 

ϕtk = arg min  

> ϕ

zedit  

> tk

+ ∆ t ˜vθ (ztgt  

> tk

, t k, c src , ϕ ; ωtgt ) − ˜vθ (zsrc  

> tk

, t k, c src , ϕ ; ωsrc ) − xsrc 

> 22

, (7) where zsrc  

> tk

, z tgt  

> tk

are formed via the coupling (3) , both branches are conditioned on csrc , and each branch retains its real-editing guidance weight. Minimising the deviation from xsrc forces ϕtk to absorb both leakage channels into a single correction tied to this asset and this timestep. We then advance zedit one Euler step along the probe ODE and cache ϕtk ; optimisation hyper-parameters are reported in §A.1. During editing (§3.3), we evaluate v∆ with ctgt on the target branch and csrc on the source branch, substituting the cached ϕt for ϕ0 in both CFG calls. Because the probe has pinned the csrc -only behaviour of v∆ to self-reconstruction, the real update retains only the ctgt -driven edit signal: on non-edited voxels v∆ collapses toward zero without any mask, turning identity preservation into a property of the velocity field itself. 

3.3 Partial-Mean Guidance for Edit-Signal Amplification 

RASI secures identity on non-edited voxels, but the same optimisation may inadvertently suppress 

v∆ on edited ones as well, weakening the edit signal. We need an amplification handle that is automatically gated by the edit region—active where v∆ is large, silent where RASI has driven it toward zero—so that edit strength and identity preservation are decoupled without any mask. We obtain such a handle from the Monte-Carlo structure of v∆ itself, at no extra forward cost, by extrapolating between a full-sample mean and a noisier partial-sample mean. 

Why a separate amplifier is needed. Because the RASI objective (7) asks one Euler step of v∆

to reconstruct xsrc , the optimised ϕt may absorb v∆ on edited voxels too, leaving the surviving edit signal as whatever the optimiser could not capture. 

Extrapolation between subsample means. Recall that v∆ (5) is estimated via the shared noise 

ε in the coupling (3) . Each independent draw ε(s) produces a velocity difference v(s)∆ , which we decompose as v(s)∆ = μ + η(s) where μ is the conditional expectation (the true edit signal) and 

η(s) is a zero-mean noise term. The S-sample mean ˆμS = 1

> S

PSs=1 v(s)∆ and any partial mean ˆμL =

> 1
> L

PLs=1 v(s)∆ with 1≤L<S are both unbiased for μ but with variances σ2 

> η

/S and σ2 

> η

/L respectively. Extrapolating away from the noisier estimate amplifies the direction in which the full-sample estimate is more confident: 

ˆμS + w (ˆ μS − ˆμL) = (1 + w) ˆ μS − w ˆμL, w ∈ R≥0. (8) 

The gated behaviour emerges automatically. On non-edited voxels RASI has driven μ toward zero, so ˆμS and ˆμL both hover near zero and their gap has no consistent direction; the extrapolation is 5negligible and identity is not perturbed. On edited voxels μ is large, ˆμS tracks it more faithfully than 

ˆμL, and (8) lengthens exactly the edit-aligned component. The full Stage-1 update at active step tk

becomes 

zedit  

> tk+1

= zedit  

> tk

+ ∆ t  ˆμS + w (ˆ μS − ˆμL) , (9) where every v(s)∆ already carries the injected ϕt from RASI. 

3.4 Twin-Agreement Residual Injection for Sparse-Stage Preservation 

Stage 1 commits the edited coordinates Ctgt but does not specify the Stage-2 geometry and Stage-3 material SLATs. Naively running the sparse sampler under ctgt drifts non-edited tokens because the global DINOv3 embedding propagates edit-region perturbation via dense cross-attention. We resolve this by letting the sampler itself identify which tokens are edit-sensitive: a twin forward under csrc on the same scaffold and noise reveals, per token, whether the output changes; tokens that agree across the twin are pulled back toward the source encoding, while disagreeing tokens are left untouched. 

Why FlowEdit coupling is unavailable. The coupling (3) requires both ODE branches to share the same token space, yet Ctgt and Csrc generally differ in voxel support after an add or remove edit, making direct coupling impossible. Even when supports overlap, coordinate coincidence does not imply content preservation , which is why the geometry-based merge of Nano3D [14] fails. 

Twin-forward disagreement map. The key insight is that tokens belonging to the non-edited region are semantically independent of the conditioning image: swapping ctgt for csrc on the same scaffold and noise should leave them unchanged, whereas edit-sensitive tokens will diverge. This lets the sampler itself draw the preserve/edit boundary without any external mask. Let Sstage  

> θ

denote the Stage-2 or Stage-3 sparse-DiT sampler. On the target scaffold Ctgt we run twin forwards with identical seeded noise ε, schedule, and CFG weight—only the image embedding differs: 

ztgt = Sstage 

> θ

 Ctgt ; ε, c tgt 

, ztwin src = Sstage 

> θ

 Ctgt ; ε, c src 

. (10) Per-token disagreement di = ∥ztgt [i] − ztwin src [i]∥2 is mapped to a preserve-confidence via robust quantile clipping: pflow [i] = 1 − clip  (di − qα(d)) /(qβ (d) − qα(d)) , 0, 1 with (α, β )=(0 .05 , 0.95) .Tokens producing nearly identical features under both conditions receive pflow ≈ 1 and are safe to preserve. 

Residual injection. Let zenc src be the SC-VAE encoding of the source asset on Csrc and I = Ctgt ∩C src 

the integer-voxel intersection. On I we form a norm-clipped residual ri = clip τ (zenc src [i] − ztgt [i]) 

and blend: 

z[i] = ztgt [i] + λ p flow [i] 1[pflow [i] ≥ ϑ] ri (i ∈ I ), z[i] = ztgt [i] (i ∈ C tgt \ I ), (11) with scale λ ∈ (0 , 1] and threshold ϑ ∈ [0 , 1) . Edit-only tokens ( i ∈ C tgt \ I ) stay at the target branch, protecting fresh geometry or material; on intersection tokens the indicator zeroes the update wherever the twin disagrees, while agreeing tokens are softly retracted toward the source encoding with strength governed by pflow .We apply (10) – (11) once per SLAT (geometry and material). Since the main target forward is bit-identical to the ctgt twin, TAR adds exactly one extra sparse-DiT forward per stage . The full pipeline (RASI + PMG on Stage 1, TAR on Stages 2–3) is given in Algorithm 1 (Appendix). 

## 4 Experiments 

We defer benchmark construction, metrics, and all implementation details to Appendix A.1. All results below use a single, fixed set of hyper-parameters shared across every edit in the benchmark. 

4.1 Baselines Baselines. We compare against six methods grouped by whether they require a user-supplied 3D mask. Mask-based: TRELLIS 2.0 [ 7] (mask-conditioned native repaint), VoxHammer [ 8]6Table 1: Quantitative comparison on our Objaverse-derived editing benchmark. All metrics are averaged over the same set of edits. The Mask-free column marks methods that do not require a user-supplied 3D mask ( ✓) versus those that do ( ×). Best per column is in bold .                                                        

> Method Mask-free PSNR ↑SSIM ↑LPIPS ↓DINO-I ↑CLIP-T ↑CD ↓
> TRELLIS 2.0 [7] ×17.30 0.793 0.288 0.720 0.278 0.089 VoxHammer [8] ×18.12 0.786 0.155 0.809 0.283 0.024
> Nano3D [14] ✓19.04 0.801 0.242 0.715 0.285 0.096 Instant3dit [11] ×14.96 0.684 0.229 0.609 0.267 0.053 Edit360 [15] ✓16.74 0.830 0.299 0.501 0.245 0.173 VecSet-Edit [10] ×20.40 0.829 0.199 0.763 0.212 0.051
> Ours ✓22.51 0.867 0.145 0.841 0.294 0.034

(inversion + KV-replacement), Instant3dit [11 ] (feed-forward multi-view inpainting, 2D-lift), and 

VecSet-Edit [ 10 ] (training-free editing on TripoSG). All mask-based methods receive the same human-annotated mask. Mask-free: Nano3D [14 ] (FlowEdit + connectivity-based Voxel/SLat-Merge) and Edit360 [15 ] (video-diffusion 2D-edit propagation around a 360 ◦ trajectory). For fair comparison, we re-implement VoxHammer and Nano3D, both originally built on TRELLIS 1.0, on the same TRELLIS 2.0 backbone. 

4.2 Main comparison Quantitative comparison. Table 1 summarises the results on our benchmark. Our method ranks first on the majority of metrics, achieving the best identity preservation (PSNR, SSIM, LPIPS) while simultaneously attaining the highest edit fidelity (DINO-I, CLIP-T). 

Qualitative comparison. As shown in Fig. 2, our method produces clean edits while faithfully preserving the geometry and texture of non-edited regions. For elementary operations such as add 

and remove , our framework already achieves high-fidelity results with precise identity preservation. More importantly, it generalises seamlessly to compound edits ( add & remove simultaneously), where existing baselines largely fail. For deformation -style edits that resemble replacement, our method also delivers competitive results, maintaining structural coherence throughout the transformation. Additional large-scale qualitative results are provided in Appendix A.4. 

4.3 Ablation studies 

We ablate the proposed modules by progressively adding them on top of the base FlowEdit pipeline. Fig. 3 shows two representative cases (rows 1–2 and rows 3–4). For each case, the first column displays the source asset (front and side views) together with the editing pair; subsequent columns incrementally activate each module: FlowEdit alone, +RASI, +RASI+PMG, and +RASI+PMG+TAR (full model). FlowEdit alone suffers from severe voxel-occupancy drift on non-edited regions. Adding RASI anchors the source trajectory and visibly corrects this drift—the moose’s hat shape and position shift back toward the source, and the robot’s torso largely recovers its original geometry—yet the edited region now lacks sufficient editing strength. Introducing PMG amplifies the edit signal: the blue hat and the red Christmas hat both emerge clearly, while changes in non-edited areas remain well suppressed. At this stage, however, fine-grained details (e.g. material and texture) of the non-edited region still deviate noticeably from the source, because Stages 2–3 are generated via forward sampling without explicit source-information injection. Finally, TAR injects source-asset details into these later stages: the moose’s trousers and body colour return to normal, and surface textures on the robot’s torso are faithfully restored. We provide additional visualisations of the geometry and material keep-SLAT maps in Appendix A.5, further demonstrating the effectiveness of TAR in identifying and preserving non-edited tokens. 

## 5 Discussion 

VS3D achieves strong identity preservation in non-edit regions: RASI anchors the source trajectory to suppress occupancy drift, while PMG amplifies the edit signal only where needed. The resulting 7Figure 2: Qualitative comparison. Our method is the only mask-free approach that jointly preserves the non-edited region and produces high-quality edits. Red boxes highlight the edited regions for easier visual inspection and are not part of the model input. In the Mask row, black regions indicate the user-provided editing mask required by mask-based methods. geometry in preserved regions closely matches the original asset, demonstrating that purely ODE-level interventions can deliver high-fidelity shape retention without any explicit mask. In the SLAT decoding stage, TAR injects source-encoded residuals to recover material and texture details. However, because no hard mask isolates the edit boundary, texture in non-edit regions can only be made close to ,rather than identical to , the original. This is an inherent characteristic of mask-free methods: without explicit spatial partitioning, pixel-level texture consistency remains out of reach. Nevertheless, the perceptual difference is minor and acceptable for most practical editing scenarios. 8Figure 3: Ablation study. Each module is progressively added and resolves a distinct failure mode. Furthermore, VS3D operates entirely through residual injection on frozen DiTs; consequently, the editing quality is upper-bounded by the generative capacity of the TRELLIS backbone itself. Any failure mode of the base model propagates into the edited output unchanged. Despite this limitation, VS3D demonstrates that high-quality shape and texture preservation is achievable through ODE-space modifications alone, without masks, inversion, or fine-tuning. We hope this perspective offers new insights and inspiration for future mask-free 3D editing methods. 

## 6 Conclusion 

We presented VS3D, a training-free, mask-free framework for high-fidelity local 3D asset editing that operates entirely within the velocity space of a frozen rectified-flow generator. By composing three lightweight ODE-level interventions—RASI for trajectory anchoring, PMG for selective edit amplification, and TAR for texture detail recovery—VS3D achieves strong identity preservation and faithful editing in a single forward pass, without masks, inversion, or fine-tuning. Experiments on diverse object categories and edit types validate its effectiveness, and we hope the velocity-space perspective inspires further exploration of mask-free 3D editing paradigms. 9References 

[1] R. Mokady, A. Hertz, K. Aberman, Y. Pritch, and D. Cohen-Or. Null-text inversion for editing real images using guided diffusion models. In IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) , pages 6038–6047, 2023. [2] V. Kulikov, M. Kleiner, I. Huberman-Spiegelglas, and T. Michaeli. FlowEdit: Inversion-free text-based editing using pre-trained flow models. arXiv preprint arXiv:2412.08629 , 2024. [3] Y. Lipman, R. T. Q. Chen, H. Ben-Hamu, M. Nickel, and M. Le. Flow matching for generative modeling. In The Eleventh International Conference on Learning Representations (ICLR) , 2023. [4] X. Liu, C. Gong, and Q. Liu. Flow straight and fast: Learning to generate and transfer data with rectified flow. In The Eleventh International Conference on Learning Representations (ICLR) , 2023. [5] O. Siméoni, H. V. Vo, M. Seitzer, F. Baldassarre, M. Oquab, C. Jose, V. Khalidov, M. Szafraniec, S. E. Yi, M. Ramamonjisoa, F. Massa, D. Haziza, L. Wehrstedt, J. Wang, T. Darcet, T. Moutakanni, L. Sentana, C. Roberts, A. Vedaldi, J. Tolan, J. Brandt, C. Couprie, J. Mairal, H. Jégou, P. Labatut, and P. Bojanowski. DINOv3. arXiv preprint arXiv:2508.10104 , 2025. [6] J. Xiang, Z. Lv, S. Xu, Y. Deng, R. Wang, B. Zhang, D. Chen, X. Tong, and J. Yang. Structured 3D latents for scalable and versatile 3D generation. In IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) , pages 21469–21480, 2025. [7] J. Xiang, X. Chen, S. Xu, R. Wang, Z. Lv, Y. Deng, H. Zhu, Y. Dong, H. Zhao, N. J. Yuan, and J. Yang. Native and compact structured latents for 3D generation. arXiv preprint arXiv:2512.14692 , 2025. [8] L. Li, Z. Huang, H. Feng, G. Zhuang, R. Chen, C. Guo, and L. Sheng. VoxHammer: Training-free precise and coherent 3D editing in native 3D space. arXiv preprint arXiv:2508.19247 , 2025. [9] S. Hu, Y. Wei, F. Zha, Y. Guo, and J. Zhang. Easy3E: Feed-forward 3D asset editing via rectified voxel flow. arXiv preprint arXiv:2602.21499 , 2026. [10] T.-F. Hsiao, B.-K. Ruan, Y.-L. Liu, and H.-H. Shuai. VecSet-Edit: Unleashing pre-trained LRM for mesh editing from single image. arXiv preprint arXiv:2602.04349 , 2026. [11] A. Barda, M. Gadelha, V. G. Kim, N. Aigerman, A. H. Bermano, and T. Groueix. Instant3dit: Multiview inpainting for fast editing of 3D objects. In IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) , pages 16273–16282, 2025. [12] Y. Chi, X. Li, Z. Huang, and J. M. Rehg. Vinedresser3D: Agentic text-guided 3D editing. arXiv preprint arXiv:2602.19542 , 2026. [13] I. Gat, D. Cohen-Bar, G. Levy, E. Richardson, and D. Cohen-Or. ShapeUP: Scalable image-conditioned 3D editing. arXiv preprint arXiv:2602.05676 , 2026. [14] J. Ye, S. Xie, R. Zhao, Z. Wang, H. Yan, W. Zu, L. Ma, and J. Zhu. Nano3D: A training-free approach for efficient 3D editing without masks. arXiv preprint arXiv:2510.15019 , 2025. [15] J. Huang, X. Hu, S. Shi, Z. Tian, and L. Jiang. Edit360: 2D image edits to 3D assets from any angle. arXiv preprint arXiv:2506.10507 , 2025. [16] Z. Lai, Y. Zhao, H. Liu, Z. Zhao, Q. Lin, H. Shi, X. Yang, M. Yang, S. Yang, Y. Feng, S. Zhang, X. Huang, D. Luo, F. Yang, F. Yang, L. Wang, S. Liu, Y. Tang, Y. Cai, Z. He, T. Liu, Y. Liu, J. Jiang, Linus, J. Huang, and C. Guo. Hunyuan3D 2.5: Towards high-fidelity 3D assets generation with ultimate details. arXiv preprint arXiv:2506.16504 , 2025. [17] Z. Li, Y. Wang, H. Zheng, Y. Luo, and B. Wen. Sparc3D: Sparse representation and construction for high-resolution 3D shapes modeling. arXiv preprint arXiv:2505.14521 , 2025. [18] S. Wu, Y. Lin, F. Zhang, Y. Zeng, Y. Yang, Y. Bao, J. Qian, S. Zhu, X. Cao, P. Torr, and Y. Yao. Direct3D-S2: Gigascale 3D generation made easy with spatial sparse attention. arXiv preprint arXiv:2505.17412 , 2025. [19] Z. Lai, Y. Zhao, Z. Zhao, H. Liu, Q. Lin, J. Huang, C. Guo, and X. Yue. LATTICE: Democratize high-fidelity 3D generation at scale. arXiv preprint arXiv:2512.03052 , 2025. [20] M. Deitke, D. Schwenk, J. Salvador, L. Weihs, O. Michel, E. VanderBilt, L. Schmidt, K. Ehsani, A. Kem-bhavi, and A. Farhadi. Objaverse: A universe of annotated 3D objects. In IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) , pages 13142–13153, 2023. 

10 [21] R. Zhang, P. Isola, A. A. Efros, E. Shechtman, and O. Wang. The unreasonable effectiveness of deep features as a perceptual metric. In IEEE/CVF Conference on Computer Vision and Pattern Recognition (CVPR) , pages 586–595, 2018. [22] M. Oquab, T. Darcet, T. Moutakanni, H. V. Vo, M. Szafraniec, V. Khalidov, P. Fernandez, D. Haziza, F. Massa, A. El-Nouby, M. Assran, N. Ballas, W. Galuba, R. Howes, P. Huang, S. Li, I. Misra, M. Rabbat, V. Sharma, G. Synnaeve, H. Xu, H. Jégou, J. Mairal, P. Labatut, A. Joulin, and P. Bojanowski. DINOv2: Learning robust visual features without supervision. Transactions on Machine Learning Research (TMLR) ,2024. [23] A. Radford, J. W. Kim, C. Hallacy, A. Ramesh, G. Goh, S. Agarwal, G. Sastry, A. Askell, P. Mishkin, J. Clark, G. Krueger, and I. Sutskever. Learning transferable visual models from natural language supervision. In International Conference on Machine Learning (ICML) , pages 8748–8763, 2021. 

## A Technical appendices and supplementary material 

A.1 Experimental setup Benchmark. We evaluate on a benchmark drawn from Objaverse [ 20 ] covering diverse local-edit operations with human-written edit instructions and a single target-view image per case. 

Metrics. We report six metrics that jointly cover the three failure modes observed in 3D editing: 

(i) PSNR ↑, (ii) SSIM ↑, and (iii) LPIPS ↓ [ 21 ], measuring pixel-, structural-, and perceptual-level fidelity of the rendered views against the reference target-view renderings; (iv) DINO-I ↑, measuring multi-view semantic alignment to the target-view image via a DINOv2 [ 22 ] backbone; (v) CLIP-T↑ [ 23 ], measuring alignment between rendered views and the textual edit instruction; (vi) CD ↓

(Chamfer Distance), measuring 3D geometric identity preservation against the source mesh on points sampled outside the human-annotated edit region. PSNR/SSIM/LPIPS/DINO-I/CLIP-T are averaged over 16 canonical views at 512 2.

Implementation details. Stage-1 (RASI + PMG). We use T =25 discretised timesteps, an active window governed by nmax =12 and nmin =0 , and a noise-sample budget of S=5 . PMG uses (w, L ) = (1 .2, 2) . RASI uses K=3 inner steps with inner-loop learning rate 10 −5 and early-stop threshold 

10 −5. The CFG weights are ωsrc =1 .5 and ωtgt =9 .0 over the CFG-active interval t ∈ [0 .6, 1.0] . Stage-2 / Stage-3 (TAR). Both TAR stages run on the 1024 -token pipeline with its native sparse-DiT sampler defaults. The agreement field pkeep is computed with robust quantiles (α, β ) = (0 .05 , 0.95) . The shape-TAR and texture-TAR injections both use λ=0 .5, norm-clip τ =10 , and agreement threshold 

ϑ=0 .7, with the residual anchored to the target branch (i.e. ri = clip τ (zenc src [i] − ztgt [i]) ). The src-encoded latents zenc src are obtained by running the frozen SC-VAE encoders on the source GLB at 

1024 3; both Stage-2 and Stage-3 twins reuse the main-pipeline target forward for ztgt .

A.2 Runtime comparison 

Table 2 reports the wall-clock time for editing a single asset on a single NVIDIA RTX 4090 GPU. VS3D completes the full three-stage pipeline in 57 s without any optimisation or fine-tuning. Table 2: Per-asset inference time (seconds) on a single NVIDIA RTX 4090 GPU. Method Mask-free Time (s) VoxHammer ✗ 394 Instant3dit ✗ 46.2 Nano3D ✓ 143 Edit360 ✓ 495 

VS3D (Ours) ✓ 57 

11 A.3 Full algorithm pseudocode Algorithm 1 VS3D: three-stage editing pipeline (RASI + PMG + TAR) 

Require: source latent xsrc ; source mesh/asset for SC-VAE encoding; conditions csrc , c tgt ; active timesteps Tact ; CFG weights ωsrc , ω tgt ; noise samples S; PMG (w, L ); RASI (K, η 0, τ es ); TAR 

(λ, τ, ϑ ). 

> 1:

▷ Stage 1: Occupancy editing (RASI + PMG) 

▷ Phase 1: RASI calibration  

> 2:

zedit ← xsrc  

> 3:

for t ∈ T act in descending order do  

> 4:

initialise ϕt ← ϕ0; set ηt by linear anneal  

> 5:

for K inner steps do  

> 6:

form zsrc  

> t

, z tgt  

> t

via (3); evaluate Lrec (ϕt) of (7)  

> 7:

ϕt ← Adam( ϕt, ∇ϕt Lrec , η t); break if Lrec < τ es  

> 8:

end for  

> 9:

advance zedit by one Euler step on the source-reconstruction ODE; cache ϕt 

> 10:

end for 

▷ Phase 2: PMG editing  

> 11:

zedit ← xsrc  

> 12:

for t ∈ T act in descending order do  

> 13:

for s = 1 , . . . , S do  

> 14:

sample ε(s) ∼ N (0 , I); form zsrc  

> t

, z tgt  

> t

via (3)  

> 15:

compute ˜vsrc , ˜vtgt with CFG using cached ϕt; set v(s)∆ = ˜ vtgt − ˜vsrc  

> 16:

end for  

> 17:

ˆμS ← 1

> S

PSs=1 v(s)∆ ; ˆμL ← 1

> L

PLs=1 v(s)∆ 

> 18:

u ← ˆμS + w (ˆ μS − ˆμL) ▷ PMG contrast  

> 19:

zedit ← zedit + ∆ t u  

> 20:

end for  

> 21:

decode zedit to edited occupancy Ctgt  

> 22:

▷ Stage 2: Geometry SLAT (TAR)  

> 23:

zgeo tgt ← S geo  

> θ

(Ctgt ; ε, c tgt ) ▷ native sparse-DiT forward  

> 24:

zgeo ,twin src ← S geo  

> θ

(Ctgt ; ε, c src ) ▷ condition-swapped twin  

> 25:

compute per-token agreement pgeo keep via robust quantile clipping on ∥zgeo tgt [i] − zgeo ,twin src [i]∥2 

> 26:

zgeo ,enc src ← SC -VAE geo (source asset on Csrc ) 

> 27:

blend via (11) with (λ, τ, ϑ ) on I = Ctgt ∩ C src to obtain zgeo  

> 28:

▷ Stage 3: Material SLAT (TAR)  

> 29:

zmat tgt ← S mat  

> θ

(Ctgt , z geo ; ε′, c tgt ) ▷ geometry-conditioned  

> 30:

zmat ,twin src ← S mat  

> θ

(Ctgt , z geo ; ε′, c src ) 

> 31:

compute per-token agreement pmat keep ; encode zmat ,enc src  

> 32:

blend via (11) to obtain zmat  

> 33:

return (Ctgt , z geo , z mat ) ▷ decode to mesh via Flexicubes + texture 

A.4 Large-scale qualitative gallery 

To further demonstrate the generality and robustness of VS3D, we present an extended gallery of editing results across a wide variety of 3D assets and edit instructions. These examples span diverse local-edit operations as well as mixed-operation edits, covering diverse object categories including characters, animals, vehicles, and everyday objects. The results confirm that VS3D consistently produces high-fidelity edits while preserving non-edited regions, even under challenging compositional instructions. 12 A.5 TAR agreement-field visualisation 

We visualise the per-token agreement fields computed by TAR across diverse editing cases. For each asset, we show four images: the source rendering, the target rendering, the geometry keep-SLAT map, and the material keep-SLAT map. Warm-coloured regions indicate large discrepancy between the twin branches (edited region); cool-coloured regions indicate high agreement (preserved region where TAR injects source residuals). 13 Source Image Target Image Geo. Keep-SLAT Map Mat. Keep-SLAT Map 

Replace the large dark blue pig snout with a smaller pink snout, keep everything else unchanged. 

Add a small handbag to the character’s hand, keep everything else unchanged. 

Add a closed red-and-white striped umbrella to the character’s hand, keep everything else unchanged. 

Add a pair of black sunglasses on the character’s face, keep everything else unchanged. 

Add a red Christmas hat on top of the robot’s head, keep everything else unchanged. 

Figure 4: TAR keep-SLAT map visualisation (Part 1). Each row: source rendering, edited rendering, geometry keep-SLAT map, and material keep-SLAT map. Warm colours denote the edited region (high twin discrepancy); cool colours denote the preserved region where TAR injects source residuals. 14 Source Image Target Image Geo. Keep-SLAT Map Mat. Keep-SLAT Map 

Add a big gold chain necklace around the character’s neck, keep everything else unchanged. 

Remove the pink bow on the rabbit’s ear, add gloves on its hands and socks on its feet, keep everything else unchanged. 

Remove the belt and the white cape from the character, and add side-swept bangs on the character’s head, keep everything else unchanged. 

Add a scarf tied around the neck of the guitar, keep everything else unchanged. 

Figure 5: TAR keep-SLAT map visualisation (Part 2). Each row: source rendering, edited rendering, geometry keep-SLAT map, and material keep-SLAT map. Warm colours denote the edited region (high twin discrepancy); cool colours denote the preserved region where TAR injects source residuals. 15 Figure 6: Extended editing gallery (Part 1). Large-scale qualitative results of VS3D across diverse assets and edit operations. Each group shows the source asset and the edited result under the corresponding text instruction. 16 Figure 7: Extended editing gallery (Part 2). Additional large-scale results demonstrating VS3D’s robustness on mixed-operation edits and complex compositional instructions. 17