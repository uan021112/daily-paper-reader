---
title: "Map2World: Segment Map Conditioned Text to 3D World Generation"
title_zh: Map2World：基于分割图条件的文本到三维世界生成
authors: "Jaeyoung Chung, Suyoung Lee, Jianfeng Xiang, Jiaolong Yang, Kyoung Mu Lee"
date: 2026-05-01
pdf: "https://arxiv.org/pdf/2605.00781v1"
tags: ["query:3dgen"]
score: 9.5
evidence: 基于分割图和文本提示的3D世界生成
tldr: Map2World是一个基于分割图引导的文本到3D世界生成框架，旨在解决现有方法在生成大规模场景时受限于网格布局且物体比例不一致的问题。该方法允许用户通过任意形状和比例的分割图定义场景布局，并结合细节增强网络提升生成质量。通过利用资产生成器的先验知识，Map2World在保持全局结构一致性的同时，实现了跨领域的鲁棒生成，显著提升了用户可控性和内容连贯性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.00781v1/fig-001.webp\", \"caption\": \"Fig. 4: Qualitative comparison with SynCity in a grid-type segmentation map. Syncity only supports generating assets on square-type tiles, and it lacks contextual continuity between adjacent tiles. Our model fills the entire world with appropriate objects in a seamless manner.\", \"page\": 11, \"index\": 1, \"width\": 726, \"height\": 336}, {\"url\": \"assets/figures/arxiv/2605.00781v1/fig-002.webp\", \"caption\": \"Fig. 7: Qualitative comparison of rendered scenes on design choices for the detail enhancer. Best viewed when zoomed in.\", \"page\": 15, \"index\": 2, \"width\": 726, \"height\": 213}, {\"url\": \"assets/figures/arxiv/2605.00781v1/fig-003.webp\", \"caption\": \"Fig. 3: Qualitative results conditioned by arbitrary shaped segment maps with userdefined text prompts. Our model creates the 3D world corresponds to the input map regardless of its size and shape. We note that Syncity cannot generate the scene from such a complicated segment map.\", \"page\": 10, \"index\": 3, \"width\": 723, \"height\": 482}, {\"url\": \"assets/figures/arxiv/2605.00781v1/fig-004.webp\", \"caption\": \"Fig. 5: Samples in various scales generated by the same prompt but different seeds.\", \"page\": 14, \"index\": 4, \"width\": 710, \"height\": 212}, {\"url\": \"assets/figures/arxiv/2605.00781v1/fig-005.webp\", \"caption\": \"Fig. 6: Ablation on spectral-domain parameterization. This parameterization stabilizes the optimization process, enabling rapid convergence to the target within a few steps using a high learning rate.\", \"page\": 14, \"index\": 5, \"width\": 723, \"height\": 194}, {\"url\": \"assets/figures/arxiv/2605.00781v1/fig-006.webp\", \"caption\": \"Table 1: Evaluation using the proposed World Quality (WQ) metric where WQ = 0.15S + 0.45W + 0.25C + 0.15R.\", \"page\": 13, \"index\": 6, \"width\": 675, \"height\": 386}, {\"url\": \"assets/figures/arxiv/2605.00781v1/fig-007.webp\", \"caption\": \"Fig. 2: Visualization of the overall generation pipeline. First, we estimate the structured latent for a large world conditioned by a segmentation map and a text prompt for each segment using latent fusion (top). Then, we further upscale the resolution of the generated scene using the detail enhancer. Detail enhancer includes an MLP layer to incorporate condition latent and noise, as well as a flow Transformer (bottom).\", \"page\": 5, \"index\": 7, \"width\": 717, \"height\": 399}]"
motivation: 现有的3D世界生成方法受限于固定的网格布局，且在生成大规模环境时难以保持物体比例的一致性。
method: 提出Map2World框架，利用用户定义的分割图引导生成，并结合细节增强网络和资产生成器先验来提升场景的精细度与一致性。
result: 实验表明，该方法在用户可控性、比例一致性和内容连贯性方面显著优于现有技术，支持生成更复杂的3D世界。
conclusion: Map2World为生成具有全局一致性和高灵活性的复杂3D场景提供了一种有效的解决方案，具有强大的泛化能力。
---

## 摘要
三维世界生成对于沉浸式内容创作或自动驾驶仿真等应用至关重要。近期三维世界生成的进展已取得显著成果；然而，这些方法受限于网格布局，且在整个世界范围内存在物体尺度不一致的问题。在这项工作中，我们提出了一种名为 Map2World 的新型框架，首次实现了基于用户定义的任意形状和尺度的分割图（segment maps）的三维世界生成，确保了广阔环境中的全局尺度一致性和灵活性。为了进一步提升质量，我们提出了一个细节增强网络，用于生成世界的精细细节。该细节增强器通过整合全局结构信息，在不损害整体场景连贯性的情况下，实现了细粒度细节的添加。我们设计的整个流水线利用了来自资产生成器的强大先验，即使在场景生成训练数据有限的情况下，也能在不同领域实现鲁棒的泛化。大量实验表明，我们的方法在用户可控性、尺度一致性和内容连贯性方面显著优于现有方法，使用户能够在更复杂的条件下生成三维世界。

## Abstract
3D world generation is essential for applications such as immersive content creation or autonomous driving simulation. Recent advances in 3D world generation have shown promising results; however, these methods are constrained by grid layouts and suffer from inconsistencies in object scale throughout the entire world. In this work, we introduce a novel framework, Map2World, that first enables 3D world generation conditioned on user-defined segment maps of arbitrary shapes and scales, ensuring global-scale consistency and flexibility across expansive environments. To further enhance the quality, we propose a detail enhancer network that generates fine details of the world. The detail enhancer enables the addition of fine-grained details without compromising overall scene coherence by incorporating global structure information. We design the entire pipeline to leverage strong priors from asset generators, achieving robust generalization across diverse domains, even under limited training data for scene generation. Extensive experiments demonstrate that our method significantly outperforms existing approaches in user-controllability, scale consistency, and content coherence, enabling users to generate 3D worlds under more complex conditions.

---

## 论文详细总结（自动生成）

以下是对论文《Map2World: Segment Map Conditioned Text to 3D World Generation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何生成大规模、高画质且具有全局一致性的3D世界。
*   **研究动机**：
    *   **数据匮乏**：高质量的世界级3D数据集极难获取，导致现有模型多局限于特定领域（如室内或驾驶场景）。
    *   **布局僵化**：现有方法（如SynCity）通常采用网格化（Grid-based）布局，无法处理现实中不规则形状的区域边界。
    *   **不一致性**：模块化拼接资产的方法往往导致物体比例失调、边界连接处断裂或上下文不连贯。
*   **整体含义**：Map2World 通过引入用户定义的**分割图（Segment Map）**作为约束，结合强大的3D资产生成器先验，实现了任意形状、大规模且细节丰富的3D世界生成。

### 2. 方法论：核心思想与关键技术
Map2World 基于 TRELLIS（一种先进的3D资产生成器）构建，主要包含以下三个核心阶段：

*   **3D潜空间中的空间扩展与融合（Latent Fusion）**：
    *   **多窗口扩散策略**：将大场景划分为多个重叠的3D立方体窗口，在去噪过程中通过高斯核对重叠区域的预测速度场进行加权融合，确保跨边界的无缝连接。
    *   **分割图引导**：引入用户定义的语义分割图 $M_k$。在去噪每一步，根据分割图对不同区域应用不同的文本提示词，并通过随时间步 $t$ 动态调整的高斯平滑掩码确保区域过渡自然。
    *   **尺度感知初始化优化**：针对初始噪声进行频谱域（3D FFT）参数化优化，使生成的稀疏结构符合预期的物体尺度和地面约束。
*   **细节增强网络（Detail Enhancer）**：
    *   **核心思想**：学习从粗糙潜张量到精细潜张量的映射。将一个大立方体潜张量拆分为8个小立方体，训练一个轻量级 MLP 层（$F_\theta$）配合冻结的 Transformer 预测更精细的结构。
    *   **上下文感知**：增强器不仅输入当前块的粗糙信息，还引入相邻已生成块的潜信息，保证全局一致性。
*   **解码器微调（SLAT Decoder Fine-tuning）**：
    *   针对局部场景块（而非完整物体）对 3DGS 解码器进行微调，以提升几何边缘的锐利度和纹理质量。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：从 Objaverse 中筛选出 43 个高质量场景（基于 NuiScene 标签），提取 17,500 个立方体对用于训练细节增强器。
    *   **测试集**：使用 NuiScene43 的 35 个网格模型生成文本描述进行评估。
*   **Benchmark 与评价指标**：
    *   **定量指标**：PSNR、LPIPS、FID（基于 Inceptionv3, DINOv2, CLIP）。
    *   **新指标 (World Quality, WQ)**：综合了清晰度 (S)、世界完整性 (W)、连贯性 (C) 和现实感 (R) 的加权得分，由 GPT-4o 评估。
*   **对比方法**：
    *   **SynCity**：同样基于 TRELLIS 的网格化生成方法。
    *   **GaussianCube**：结构化显式辐射场生成模型。

### 4. 资源与算力
*   **GPU 型号与数量**：每个网络（几何流 Transformer $G_S$、纹理流 Transformer $G_L$、解码器 $D_L$）均在 **1 台 NVIDIA A100 80GB GPU** 上进行微调。
*   **训练时长**：每个网络约耗时 **60 小时**。
*   **参数量**：微调的参数（主要是 MLP 层）仅占总参数量的约 **4%**（类似于 LoRA 的效率）。

### 5. 实验数量与充分性
*   **实验规模**：
    *   进行了大规模的定性展示，涵盖了从规则网格到极其复杂的自由形状分割图。
    *   针对细节增强器的设计（架构选择、是否使用 CFG、解码器微调的作用）进行了详细的消融实验。
    *   通过 50 个随机种子进行了 CLIP-Score 热图分析，验证了生成内容与分割图标签的语义对齐度。
*   **充分性评价**：实验设计较为充分，不仅有视觉对比，还引入了多维度的定量指标和消融分析。对比实验涵盖了当前最相关的基准，客观地展示了其在处理复杂边界和大规模连贯性上的优势。

### 6. 主要结论与发现
*   **灵活性**：Map2World 能够遵循任意形状的分割图，打破了传统 3D 生成对网格布局的依赖。
*   **连贯性**：潜空间融合策略有效解决了资产拼接时的断裂感，实现了大尺度物体的跨区域生成。
*   **质量提升**：细节增强网络在不改变整体结构的前提下，显著提升了场景的视觉丰富度和纹理清晰度。
*   **泛化性**：通过微调少量参数并利用资产生成器先验，模型在有限的场景数据下表现出极强的跨领域泛化能力。

### 7. 优点（亮点）
*   **用户可控性极高**：支持像素级的语义控制，用户可以通过画图定义世界布局。
*   **训练效率高**：采用参数高效的微调方案，无需从头训练庞大的世界生成模型。
*   **无缝扩展**：支持任意分辨率的逐步生成，且能保持全局尺度的一致性。

### 8. 不足与局限
*   **位置编码限制**：由于底层 TRELLIS 使用绝对位置编码，在合并多个小块时可能会导致解码后的 3D 结构发生轻微偏移或变化。
*   **数据偏差**：训练数据主要来自 Objaverse，虽然经过筛选，但仍以相对简单的网格为主，对于极其复杂的真实世界纹理（如照片级真实感）仍有提升空间。
*   **推理开销**：自回归式的细节增强虽然效果好，但在生成超大规模世界时，计算耗时会随块数增加。

（完）
