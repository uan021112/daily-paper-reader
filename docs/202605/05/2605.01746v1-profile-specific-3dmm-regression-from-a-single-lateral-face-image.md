---
title: Profile-Specific 3DMM Regression from a Single Lateral Face Image
title_zh: 基于单张侧面人脸图像的侧脸特定 3DMM 回归
authors: "Taiki Kanaya, Hideo Saito"
date: 2026-05-03
pdf: "https://arxiv.org/pdf/2605.01746v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 从侧面人脸图像进行单图3D人脸重建
tldr: 本研究针对正畸临床中侧位X射线辐射风险，提出从单张侧面RGB图像进行3D面部重建的方法。通过构建ProfileSynth合成数据集，利用扩散模型生成带几何约束的侧位图像，并结合可见性感知下颌线正则化的FLAME回归模型，解决了极端侧位视角下遮挡严重、特征稀缺的难题，为非侵入式头影测量分析奠定了基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.01746v1/fig-001.webp\", \"caption\": \"Table 5. ProfileSynth generation setup.\", \"page\": 11, \"index\": 1, \"width\": 479, \"height\": 597}, {\"url\": \"assets/figures/arxiv/2605.01746v1/fig-002.webp\", \"caption\": \"Figure 1. ProfileSynth generation pipeline. We sample FLAME parameters at extreme yaw, render geometry cues (depth, normals, silhouettes, landmarks), and generate photorealistic profile RGB images using ControlNet conditioned on rendered depth and normals.\", \"page\": 3, \"index\": 2, \"width\": 829, \"height\": 374}, {\"url\": \"assets/figures/arxiv/2605.01746v1/fig-003.webp\", \"caption\": \"Figure 4. Qualitative comparison on the NoW dataset [29]. We show two images of the same subject: neutral (top) and expressive (bottom). All baselines are run with their official implementations and default settings. NoW provides a single subject-level 3D scan as ground truth (GT) even for expressive inputs, so expression-aware methods may deviate around the mouth. Visually, our method yields the cleanest and most consistent chin-to-neck profile contour.\", \"page\": 8, \"index\": 3, \"width\": 829, \"height\": 270}, {\"url\": \"assets/figures/arxiv/2605.01746v1/fig-004.webp\", \"caption\": \"Figure 3. Qualitative comparison on ProfileSynth. We show reconstructions from single extreme-profile images (yaw ≈ 90◦). All predicted meshes are rigidly aligned to the ground-truth (GT) mesh and rendered from the same canonical profile view. Our method better preserves the facial outline and jawline and produces shapes closer to GT than existing baselines.\", \"page\": 8, \"index\": 4, \"width\": 829, \"height\": 270}, {\"url\": \"assets/figures/arxiv/2605.01746v1/fig-005.webp\", \"caption\": \"Figure 2. Overview of the proposed Profile3DMM regressor. An ImageNet-pretrained ResNet-50 extracts global features, an MLP head predicts FLAME parameters, and the FLAME decoder outputs the reconstructed mesh and landmarks for supervision and evaluation.\", \"page\": 4, \"index\": 5, \"width\": 832, \"height\": 207}]"
motivation: 传统的侧位头影测量依赖有辐射的X射线，且现有3D面部重建方法多针对正脸，难以处理侧脸严重的遮挡和几何特征提取。
method: 提出ProfileSynth数据集，利用扩散模型结合深度和法线图生成合成侧位图，并开发了带有可见性感知下颌线正则化的FLAME回归基准模型。
result: 成功构建了专门针对极端侧位视角的3DMM回归框架，能够从单张侧面图像中有效恢复面部几何结构。
conclusion: 该研究填补了侧位3D面部重建的空白，为实现更安全、非侵入式的临床头影测量分析提供了可行的技术路径。
---

## 摘要
单张图像 3D 人脸重建是计算机视觉中的核心问题，在正畸学中的头影测量关键点分析等临床应用中具有重要意义。传统上，这种分析依赖于侧位 X 射线成像；然而，由于辐射问题，频繁的 X 射线照射并不切实际。虽然最近的研究探索了从侧面 RGB 图像中检测关键点作为替代方案，但现有方法通常依赖于眼睛、嘴巴、耳朵和边界轮廓等 2D 特征，未能充分利用跨越面部轮廓和下颌线的底层 3D 面部几何结构，而这对于准确诊断至关重要。同时，尽管正视图 3D 人脸重建已取得显著进展，但大多数基于学习的 3D 可变形模型 (3DMM) 回归器是在外观线索丰富的近正视图图像上开发和基准测试的。在极端侧视图（偏航角约 90 度）中，人脸的大部分被遮挡，可用信号以边界线索为主，这使得准确的 3D 重建具有挑战性。在本文中，我们通过几何条件的合成数据和针对单张侧面图像的简单侧脸特定 FLAME 回归基准来弥补这一差距。我们介绍了 ProfileSynth，这是一个通过在极端偏航范围内采样 FLAME 形状和姿态参数，并使用以深度图和法向图为条件的扩散模型生成逼真侧脸图像而创建的数据集。我们进一步研究了一个具有可见性感知下颌线正则化的侧脸特定基准。我们的框架为“侧脸 x 3DMM”重建提供了一个实用的基准，并为从侧面 RGB 图像进行更准确、无创的头影测量分析奠定了有希望的基础。

## Abstract
Single-image 3D face reconstruction is a core problem in computer vision, with important clinical applications such as cephalometric landmark analysis in orthodontics. Traditionally, this analysis relies on lateral X-ray imaging; however, frequent X-ray exposure is impractical due to radiation concerns. While recent research has explored detecting landmarks from lateral RGB images as an alternative, existing methods typically rely on 2D features such as the eyes, mouth, ears, and boundary silhouettes, failing to fully exploit the underlying 3D facial geometry spanning the facial profile and jawline, which is essential for accurate diagnosis. Meanwhile, although 3D face reconstruction from frontal views has seen significant progress, most learning-based 3D morphable model (3DMM) regressors are developed and benchmarked on near-frontal images, where appearance cues are abundant. In extreme profile views (yaw $\approx 90^\circ$), much of the face is occluded, and the available signal is dominated by boundary cues, making accurate 3D reconstruction challenging. In this paper, we bridge this gap with geometry-conditioned synthetic data and a simple profile-specific FLAME regression baseline for single lateral images. We introduce ProfileSynth, a dataset created by sampling FLAME shape and pose parameters in extreme yaw ranges and generating photorealistic profile images using a diffusion model conditioned on depth and normal maps. We further study a profile-specific baseline with visibility-aware jawline regularization. Our framework provides a practical baseline for "profile $\times$ 3DMM" reconstruction and a promising foundation for more accurate, non-invasive cephalometric analysis from lateral RGB images.

---

## 论文详细总结（自动生成）

这篇论文针对极端侧位人脸图像的 3D 重建问题提出了一个专门的框架，旨在为正畸临床中的无创头影测量分析提供技术支持。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：如何从单张极端侧位（偏航角约 90°）的 RGB 图像中准确回归 3D 人脸模型（FLAME）。
*   **研究动机**：
    *   **临床需求**：正畸治疗中的头影测量分析传统上依赖侧位 X 射线，但频繁辐射对患者（尤其是青少年）有害。
    *   **技术瓶颈**：现有的 3DMM 重建方法大多针对近正脸设计，在极端侧位下，由于严重的自遮挡和内部特征缺失，模型往往无法准确捕捉面部轮廓和下颌线几何结构。
    *   **数据缺失**：缺乏具有精确 3D 标注的极端侧位人脸数据集。

### 2. 方法论
论文提出了一个包含数据生成、模型架构和损失函数的完整流程：
*   **ProfileSynth 合成数据集**：
    *   **核心思想**：利用扩散模型生成带有精确几何标注的合成数据。
    *   **流程**：采样 FLAME 参数（偏航角 85°-95°） $\rightarrow$ 渲染深度图、法向图、轮廓和关键点 $\rightarrow$ 使用 **ControlNet**（以深度和法向为条件）生成逼真的侧脸 RGB 图像。
*   **Profile3DMM 回归器**：
    *   **架构**：采用在 ImageNet 上预训练的 **ResNet-50** 作为骨干网络提取特征，后接一个两层 MLP 头部，直接回归 FLAME 的形状参数（$\beta$）和姿态参数（$\theta$）。
    *   **预处理**：将图像缩放至 512x512，并进行水平翻转归一化，使所有侧脸朝向一致。
*   **可见性感知下颌线正则化（Visibility-Aware Loss）**：
    *   **核心思想**：在极端侧位，只有部分下颌线可见。通过光栅化技术确定可见顶点，仅对可见区域施加约束，避免不可见区域的噪声干扰。
    *   **损失函数**：由参数损失（$L_{param}$）、3D 静态关键点损失（$L_{lm3d}$）和可见性感知下颌线顶点损失（$L_{jaw}$）加权组成。

### 3. 实验设计
*   **数据集**：
    *   **ProfileSynth**：100,000 个样本（8:1:1 划分训练/验证/测试）。
    *   **NoW profile subset**：从 NoW 数据集中手动筛选的 26 张真实侧位图像。
    *   **临床照片**：2,277 张经过验证的真实临床侧位照片（用于补充评估）。
*   **对比方法（Baselines）**：DECA, EMOCAv2, MICA, Pixel3DMM, 3DDFA V2, SynergyNet。
*   **评估指标**：
    *   **3D 误差**：可见区域顶点误差（$E_{vis}$）、下颌线误差（$E_{jaw,vis}$）。
    *   **轮廓指标**：轮廓交并比（IoU）、边界 Chamfer 距离。
    *   **对齐方式**：采用刚性对齐（Rigid Alignment）以公平比较形状恢复能力。

### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX 4090** GPU。
*   **训练细节**：
    *   训练 30 个 epoch。
    *   使用 AdamW 优化器，Batch Size 为 64。
    *   启用了自动混合精度（AMP）训练。
    *   初始学习率：Backbone 为 $1 \times 10^{-5}$，MLP 头部为 $1 \times 10^{-4}$。

### 5. 实验数量与充分性
*   **实验规模**：在 10,000 个合成测试样本上进行了大规模定量评估，结果具有统计学意义。
*   **消融实验**：针对 3D 关键点损失、下颌线损失和参数损失进行了消融研究，验证了各组件的贡献。
*   **真实感验证**：对合成图像的几何-外观一致性进行了“Sanity Check”，证明了 ControlNet 生成的图像与原始几何标签高度匹配。
*   **客观性**：通过在合成数据、公开数据集（NoW）和临床真实数据上的多维度测试，实验设计较为全面且客观。

### 6. 主要结论与发现
*   **性能领先**：在 ProfileSynth 测试集上，该方法在可见区域误差和轮廓一致性上显著优于 DECA 和 MICA 等强基准（例如，下颌线误差降低了约 4 倍）。
*   **关键点作用**：消融实验表明，稀疏的 3D 关键点监督在极端遮挡下提供了关键的几何锚点。
*   **真实域迁移**：尽管在合成数据上训练，但在真实临床照片的轮廓拟合测试中，该方法依然表现出比通用模型更好的鲁棒性和准确性。

### 7. 优点（亮点）
*   **针对性强**：填补了 3DMM 在极端侧位视角下研究的空白，具有明确的临床应用前景。
*   **数据生成策略创新**：巧妙利用 ControlNet 解决了侧位 3D 标注数据稀缺的问题，且保证了标签与图像的高度一致性。
*   **可见性感知**：引入可见性掩码来指导损失函数，符合侧位成像的物理特性，提高了训练的稳定性。

### 8. 不足与局限
*   **表情限制**：目前仅限于**中性表情**（Neutral Expression），未考虑说话或张嘴等动态变化。
*   **相机模型固定**：合成数据使用了固定的透视相机参数，可能与真实拍摄环境存在偏差。
*   **真实域差距**：虽然在临床照片上表现良好，但合成图像与真实图像之间仍存在外观分布差异（Domain Gap）。
*   **样本量限制**：在 NoW 这种公开真实数据集上的侧位样本极少（仅 26 张），导致真实域的定量评估不够充分。

（完）
