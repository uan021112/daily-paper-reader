---
title: Rethinking Dense Optical Flow without Test-Time Scaling
title_zh: 重新思考无需测试时缩放的稠密光流
authors: "Praroop Chanda, Suryansh Kumar"
date: 2026-05-08
pdf: "https://arxiv.org/pdf/2605.08000v1"
tags: ["query:ff3dr"]
score: 6.5
evidence: 利用基础模型的几何先验，通过单次前向传递估计稠密光流
tldr: 本研究挑战了密集光流估计中依赖测试时迭代优化的传统范式。作者提出一种无需迭代细化的单次前向传播框架，通过整合DINO-v2的语义先验和单目深度模型的几何先验，利用全局匹配实现高效光流估计。该方法在保持计算效率的同时，在Sintel等基准测试上展现了卓越的跨数据集泛化能力，证明了强大的基础模型先验可以替代昂贵的测试时计算缩放。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.08000v1/fig-001.webp\", \"caption\": \"Figure 2. Qualitative comparison on Sintel (Final). This benchmark contains severe motion blur, illumination variation, and large displacements. Despite operating without iterative refinement, our method preserves sharp motion boundaries and produces accurate flow estimates, performing comparably to—and in some cases better than—refinement-based approaches.\", \"page\": 8, \"index\": 1, \"width\": 1006, \"height\": 374}, {\"url\": \"assets/figures/arxiv/2605.08000v1/fig-002.webp\", \"caption\": \"Table 1. Cross-dataset generalization after training on Chairs and Things. No target-domain fine-tuning is applied. Lower is better.\", \"page\": 6, \"index\": 2, \"width\": 1022, \"height\": 226}, {\"url\": \"assets/figures/arxiv/2605.08000v1/fig-003.webp\", \"caption\": \"Figure 1. Overview. The conventional CNN-based feature encoder is replaced with DINOv2 [27] to provide semantically rich, largescale self-supervised visual features, while original transformer-based feature interaction, global matching, and flow propagation modules remain unchanged. In addition, monocular depth estimates from Depth Anything V2 [43] are introduced as a geometric prior to improve feature conditioning and correspondence estimation.\", \"page\": 5, \"index\": 3, \"width\": 829, \"height\": 400}]"
motivation: 针对当前光流算法过度依赖复杂架构和高计算成本的迭代优化问题，探索利用基础模型先验实现高效推理的可能性。
method: 提出一种融合冻结DINO-v2语义特征与单目深度几何线索的框架，通过全局匹配在单次前向传播中估计密集对应关系。
result: 在Sintel Final数据集上达到2.81 EPE，在无需迭代的情况下显著优于SEA-RAFT、RAFT和GMFlow等主流模型。
conclusion: 强大的视觉基础模型先验能够有效替代测试时的迭代细化，为开发高效且泛化能力强的光流系统提供了新路径。
---

## 摘要
近期稠密光流的进展主要由日益复杂的架构和用于测试时缩放的多步细化所驱动。虽然这些方法在基准测试中取得了强劲性能，但它们在推理过程中也需要大量的计算。这提出了一个根本性问题：扩展测试时计算是提高稠密光流准确性的唯一途径吗？我们认为并非如此。相反，现代基础模型中编码的强大视觉语义和几何先验可以减少甚至消除测试时对计算昂贵的迭代细化的需求。在本文中，我们提出了一个在单次前向传递中估计稠密光流的框架，利用预训练的基础表示，同时避免了迭代细化和额外的推理时间计算，从而为测试时缩放提供了一种替代方案。我们的方法从冻结的 DINO-v2 骨干网络中提取视觉语义特征，并将其与来自单目深度基础模型的几何线索相结合。我们将这些互补的先验融合到一个统一的表示中，并应用全局匹配公式来估计稠密对应关系，而无需循环更新或测试时优化。尽管避免了迭代细化，我们的方法在具有挑战性的基准测试中仍实现了强大的跨数据集泛化能力。在 Sintel Final 数据集上，我们在无需细化的情况下获得了 2.81 的 EPE，在同等训练条件下显著优于最先进的 (SOTA) SEA-RAFT，并优于相同设置下的 RAFT、GMFlow（无细化）以及最近的 FlowSeek。这些结果表明，强大的基础先验可以替代测试时缩放，为重度依赖细化的流水线提供了一种计算高效的替代方案。

## Abstract
Recent progress in dense optical flow has been driven by increasingly complex architectures and multi-step refinement for test-time scaling. While these approaches achieve strong benchmark performance, they also require substantial computation during inference. This raises a fundamental question: Is scaling test-time computation the only way to improve dense optical flow accuracy? We argue that it is not. Instead, powerful visual semantic and geometric priors encoded in modern foundation models can reduce, if not overcome, the need for computationally expensive iterative refinement at test-time. In this paper, we present a framework that estimates dense optical flow in a single forward pass, leveraging pretrained foundation representations, while avoiding iterative refinement and additional inference-time computation, thus offering an alternative to test-time scaling. Our method extracts visual semantic features from a frozen DINO-v2 backbone and combines them with geometric cues from a monocular depth foundation model. We fuse these complementary priors into a unified representation and apply a global matching formulation to estimate dense correspondences without recurrent updates or test-time optimization. Despite avoiding iterative refinement, our approach achieves strong cross-dataset generalization across challenging benchmarks. On Sintel Final, we obtain 2.81 EPE without refinement, significantly improving over state-of-the-art (SOTA) SEA-RAFT under comparable training conditions and outperforming RAFT, GMFlow (without refinement), and recent FlowSeek in the same setting. These results suggest that strong foundation priors can substitute for test-time scaling, offering a computationally efficient alternative to refinement-heavy pipelines.