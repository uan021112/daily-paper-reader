---
title: "FeatureFox: Sample-Efficient Panoptic Graph Segmentation for Machining Feature Recognition in B-Rep 3D-CAD Models"
title_zh: FeatureFox：用于 B-Rep 3D-CAD 模型加工特征识别的样本高效全景图分割
authors: "Bertram Fuchs, Altay Kacan, Aaron Haag, Oliver Lohse"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26770v1"
tags: ["query:partseg"]
score: 9.0
evidence: 3D-CAD模型中加工特征识别的全景分割
tldr: FeatureFox 是一种针对 B-Rep 3D-CAD 模型的高效全景图分割方法，旨在解决传统深度学习方法在加工特征识别（AFR）中数据需求量大且计算复杂的问题。该方法通过边缘分类器定位边界并利用连通分量恢复实例，在极少样本下即可实现高精度的特征分割与分类，显著提升了 CAD/CAM 自动化的效率与实用性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26770v1/fig-001.webp\", \"caption\": \"Fig. 4. Sample efficiency on MFInstSeg (excluding stock): PQ versus trainingset size for AAGNet and FeatureFox. FeatureFox dominates in the low-data regime and reaches > 90% PQ with ∼ 250 training parts, whereas AAGNet requires ∼ 5,000; AAGNet is only marginally better on the full training set.\", \"page\": 5, \"index\": 1, \"width\": 524, \"height\": 355}, {\"url\": \"assets/figures/arxiv/2604.26770v1/fig-002.webp\", \"caption\": \"Fig. 3. FeatureFox pipeline. Starting from a B-Rep 3D-CAD model, we construct a face-adjacency graph and attributes, propagate vertex information to edges (1), and localize the feature with a calibrated edge boundary classifier for instance grouping (2). We then compute connected-component attributes (3) and recognize the class of each predicted instance with a semantic model (4). Pink: Attribute calculation. Teal: Model training. Gray: Intermediate representation.\", \"page\": 5, \"index\": 2, \"width\": 816, \"height\": 277}]"
motivation: 现有的基于学习的 AFR 方法通常过于复杂、依赖海量标注数据，且无法同时高效处理实例分组和语义标注。
method: 该方法利用增强的边缘属性进行二元分类以确定特征边界，通过修剪面邻接图提取连通分量，并对聚合后的子图属性进行实例级分类。
result: 在 MFInstSeg 数据集上，FeatureFox 仅需 250 个训练样本即可达到 0.9 以上的 PQ 分数，且训练速度极快，在小样本情况下表现优于 AAGNet。
conclusion: FeatureFox 证明了通过结合局部几何特征与图分割技术，可以在极低的数据需求下实现工业级的加工特征识别，具有极强的实际应用潜力。
---

## 摘要
B-Rep 3D-CAD 模型上的自动特征识别 (AFR) 是 CAD/CAM 自动化的核心，但大多数基于学习的方法非常复杂、耗费数据，且通常将实例分组和语义标注分开评估。我们提出了 FeatureFox，这是一种全景 AFR 流水线，可输出带有语义标签的加工实例：通过在增强的边属性上使用校准的二元边分类器来定位特征边界，将实例恢复为修剪后的面邻接图中的连通分量，并利用每实例分类器从聚合的子图属性中预测加工类别。我们在 MFInstSeg 数据集上使用全景质量 (PQ) 指标进行评估，该指标综合衡量了实例分离和语义正确性。FeatureFox 的样本和计算效率显著优于深度学习基准 AAGNet，仅需约 250 个训练零件即可达到 PQ > 0.9，而 AAGNet 则需要约 5,000 个，且在完整 MFInstSeg 数据集上的训练在 GPU 上仅需数秒。在完整训练集上，AAGNet 在 PQ 指标上略微超过 FeatureFox，而 FeatureFox 在特征级识别和定位精度上仍保持微弱领先。最后，利用其低数据需求特性，我们在 270 个手动标注的工业 CAD 零件上训练了 FeatureFox，并展示了其对未见过的真实工业零件的定性泛化能力，证明了其实际应用价值。

## Abstract
Automatic feature recognition (AFR) on B-Rep 3D-CAD models is central to CAD/CAM automation, yet most learning-based methods are complex, data-hungry, and evaluate instance grouping and semantic labeling separately. We present FeatureFox, a panoptic AFR pipeline that outputs machining instances with semantic labels: a calibrated binary edge classifier on enriched edge attributes localizes feature boundaries, instances are recovered as connected components in a pruned face-adjacency graph, and a per-instance classifier predicts the machining class from aggregated subgraph attributes. We evaluate on MFInstSeg using Panoptic Quality (PQ), which jointly scores instance separation and semantic correctness. FeatureFox is substantially more sample- and compute-efficient than the deep baseline AAGNet, reaching $\mathrm{PQ}>0.9$ with $\sim250$ training parts versus $\sim5{,}000$ for AAGNet, and training on the full MFInstSeg set takes seconds on a GPU. On the full training set, AAGNet surpasses FeatureFox marginally in PQ, while FeatureFox remains slightly ahead in feature-level recognition and localization accuracy. Finally, leveraging its low data requirement, we train FeatureFox on $270$ manually labeled industrial CAD parts and show qualitative generalization to an unseen real industrial part, indicating practical real-world applicability.

---

## 论文详细总结（自动生成）

以下是对论文《FeatureFox: Sample-Efficient Panoptic Graph Segmentation for Machining Feature Recognition in B-Rep 3D-CAD Models》的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：自动特征识别（AFR）是连接 CAD 设计与 CAM 制造的关键环节。现有的 AFR 方法主要分为规则驱动（鲁棒性差、维护成本高）和深度学习驱动（如 AAGNet）。
*   **核心问题**：目前的深度学习 AFR 模型通常过于复杂、计算量大，且极度依赖海量标注数据（通常需要数千个零件）。然而，在工业实际应用中，高质量的标注 CAD 数据非常稀缺且昂贵。
*   **整体含义**：本文旨在提出一种**样本高效（Sample-Efficient）**且**可解释**的全景图分割流水线，在极少标注数据的情况下实现高精度的加工特征实例分割与语义分类。

### 2. 方法论：FeatureFox 流水线
FeatureFox 将 AFR 建模为 B-Rep 面邻接图上的**全景分割（Panoptic Segmentation）**任务，分为四个关键步骤：
*   **增强边缘属性提取**：在 B-Rep 面邻接图的基础上，将面属性（UV 网格采样）传播至边缘，结合边缘自身的几何属性（二面角、凹凸性、曲率等）形成增强特征向量。
*   **校准的边缘边界分类**：使用 **XGBoost** 训练一个二元分类器，预测每条边是否属于特征边界。引入**等值回归（Isotonic Calibration）**对概率进行校准，以确保决策阈值的稳定性。
*   **实例恢复**：根据预测结果修剪面邻接图，移除被识别为边界的边，通过寻找图中的**连通分量（Connected Components）**来提取独立的加工特征实例。
*   **实例级语义分类**：对每个提取出的子图进行属性聚合（如面数量、对数特征长度、曲率分布直方图等），再使用另一个 XGBoost 分类器预测该实例的加工类别（如孔、槽、口袋等）。

### 3. 实验设计
*   **数据集**：主要使用公开的 **MFInstSeg** 数据集（包含约 6.2 万个 STEP 文件）。此外，还使用了 270 个手动标注的真实工业零件进行泛化性验证。
*   **基准模型（Benchmark）**：对比了当前最先进的深度学习模型 **AAGNet** 和 **MFTReNet**。
*   **评估指标**：引入了计算机视觉中的**全景质量（PQ, Panoptic Quality）**指标，统一评估实例定位的准确性（IoU）和语义分类的正确性。

### 4. 资源与算力
*   **硬件环境**：使用单张 **NVIDIA RTX 6000 Ada** GPU。
*   **训练时长**：
    *   **FeatureFox**：在完整数据集上训练仅需 **8 秒**，GPU 利用率 100%。
    *   **AAGNet**：训练 100 个 epoch 需要 **38 分钟**，且 GPU 利用率较低（约 18%）。
*   **效率对比**：FeatureFox 的训练速度比深度学习基准快了两个数量级。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在不同训练样本规模（50 到 4.35 万个样本）下进行了多次对比实验。
    *   针对每个实验设置了 3 个随机种子以确保结果的客观性。
    *   进行了面级性能对比、特征级性能对比以及针对工业零件的定性消融实验。
*   **充分性评价**：实验设计较为充分，特别是在**样本效率曲线**的对比上非常直观，证明了其在小样本下的绝对优势。

### 6. 主要结论与发现
*   **样本效率极高**：FeatureFox 仅需 **250 个**训练零件即可达到 PQ > 0.9 的精度，而 AAGNet 达到同等水平需要约 **5,000 个**零件。
*   **性能表现**：在全量数据下，AAGNet 的 PQ 略高于 FeatureFox（约高 1%），但在特征级识别准确率上，FeatureFox 依然保持微弱领先。
*   **工业泛化性**：在仅使用 270 个工业样本训练后，模型能成功识别 NIST 标准工业零件中的复杂特征，证明了其实际部署的可行性。

### 7. 优点与亮点
*   **轻量且快速**：基于树模型（XGBoost）而非复杂的 GNN，训练和推理极快，适合在算力受限的本地工业环境部署。
*   **全景评估**：首次在 AFR 领域倡导使用 PQ 指标，解决了以往将实例分割和语义标注分开评价导致的评估不完整问题。
*   **低数据依赖**：打破了深度学习方法对大数据集的依赖，为工业界稀缺标注数据的场景提供了解决方案。

### 8. 不足与局限
*   **非端到端风险**：采用分级流水线设计，边缘边界预测的错误会直接传播到后续的语义分类阶段。
*   **复杂拓扑处理**：目前无法处理由非连通面组成的单个加工特征（例如被其他特征切断的特征实例）。
*   **特征覆盖**：对于某些极其罕见的加工特征（如多级孔），由于训练样本极少，模型仍存在误判风险。

（完）
