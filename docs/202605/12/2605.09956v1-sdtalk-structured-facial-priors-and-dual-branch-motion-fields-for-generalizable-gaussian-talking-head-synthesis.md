---
title: "SDTalk: Structured Facial Priors and Dual-Branch Motion Fields for Generalizable Gaussian Talking Head Synthesis"
title_zh: SDTalk：用于泛化高斯说话人头像合成的结构化面部先验与双分支运动场
authors: "Peng Jia, Zhen Xiao, Jia Li, Xueliang Liu, Zhenzhen Hu, Lingyun Yu"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.09956v1"
tags: ["query:3dgen"]
score: 9.0
evidence: 从单张图像进行单次3D高斯泼溅重建
tldr: 针对现有说话人头部合成方法依赖特定身份模型且泛化性差的问题，本文提出SDTalk框架。该框架基于3D高斯泼溅（3DGS）技术，通过引入结构化面部先验和双分支运动场，实现了仅需单张参考图即可生成高质量、实时且具备强泛化能力的说话人视频。SDTalk在保持高推理效率的同时，显著提升了面部细节的保真度和口型同步效果，为通用型数字人合成提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.09956v1/fig-001.webp\", \"caption\": \"Figure 3: Qualitative comparison of talking head synthesis across different methods on the MEAD [19] dataset.\", \"page\": 4, \"index\": 1, \"width\": 460, \"height\": 461}, {\"url\": \"assets/figures/arxiv/2605.09956v1/fig-002.webp\", \"caption\": \"Figure 2: Qualitative comparison across different methods. “Real3DP” and “NeRFFS” refer to Real3DPortrait [16] and NeRFFaceSpeech [17], respectively. Key regions are highlighted by boxes.\", \"page\": 3, \"index\": 2, \"width\": 468, \"height\": 657}, {\"url\": \"assets/figures/arxiv/2605.09956v1/fig-003.webp\", \"caption\": \"Figure 1: Overview of SDTalk. Our approach consists of two main modules: a reconstruction module and a motion field module. Given a source image Is and its corresponding audio, the reconstruction module leverages a learned structured facial prior to obtain the Gaussian head. The motion field then predicts point-wise deformations conditioned on the audio features fa. Finally, a rasterizer and a neural renderer synthesizes the final talking head Ir . During inference, the reconstruction module is executed only once, after which audio-driven animation is achieved solely through the motion field module.\", \"page\": 2, \"index\": 3, \"width\": 987, \"height\": 337}]"
motivation: 现有的说话人头部合成方法通常需要针对特定身份进行训练或微调，限制了其在未知身份上的泛化能力和实时应用。
method: 提出一种基于3DGS的两阶段框架，结合结构化面部先验进行单图重建，并利用双分支运动场分别建模粗粒度和细粒度的面部动态。
result: 实验结果表明，SDTalk在视觉质量、口型同步准确度以及推理效率方面均显著优于现有的先进方法。
conclusion: 该研究通过结合结构化先验与双分支运动建模，成功实现了无需微调的高质量、通用化实时说话人头部合成。
---

## 摘要
高质量、实时的说话人头像合成仍然是计算机视觉中的一个基本挑战。现有的基于重建和渲染的方法通常依赖于特定身份的模型，限制了跨身份的泛化能力。为了解决这个问题，我们提出了 SDTalk，这是一个基于单样本 3D 高斯泼溅（3DGS）的框架，无需个性化训练或微调即可泛化到未见过的身份。我们的框架包含两个模块，采用两阶段训练策略。在第一阶段，我们将结构化面部先验整合到重建模块中，并分别预测可见区域和遮挡区域的 3DGS 参数，从而实现从单张图像进行完整的头部重建。在第二阶段，我们引入了双分支运动场来建模粗粒度和细粒度的面部动态，提高了细节保真度和口型同步性。实验表明，SDTalk 在视觉质量和推理效率方面均优于现有方法。

## Abstract
High-quality, real-time talking head synthesis remains a fundamental challenge in computer vision. Existing reconstruction- and rendering-based methods typically rely on identity-specific models, limiting cross-identity generalization. To address this issue, we propose SDTalk, a one-shot 3D Gaussian Splatting (3DGS)-based framework that generalizes to unseen identities without personalized training or fine-tuning. Our framework comprises two modules with a two-stage training strategy. In the first stage, we incorporate structured facial priors into the reconstruction module and separately predict 3DGS parameters for visible and occluded regions, enabling complete head reconstruction from a single image. In the second stage, we introduce a dual-branch motion field to model coarse and fine facial dynamics, improving detail fidelity and lip synchronization. Experiments demonstrate that SDTalk surpasses existing methods in both visual quality and inference efficiency.

---

## 论文详细总结（自动生成）

以下是对论文《SDTalk: Structured Facial Priors and Dual-Branch Motion Fields for Generalizable Gaussian Talking Head Synthesis》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何在无需针对特定人物进行个性化微调（One-shot）的情况下，实现高质量、实时且具备强泛化能力的说话人头像合成。
*   **研究背景**：
    *   **生成式方法**（如GAN、扩散模型）：泛化性强但图像质量有限，难以保持身份特征，且效率较低。
    *   **重建式方法**（如NeRF、3DGS）：图像质量高，但通常需要针对每个身份进行数小时的训练或微调，无法直接应用于未知身份。
    *   **动机**：利用3D高斯泼溅（3DGS）的显式表示优势，结合面部结构先验，消除对特定身份微调的依赖。

### 2. 方法论
SDTalk 采用基于 3DGS 的两阶段训练框架，核心由**重建模块**和**运动场模块**组成：
*   **结构化面部先验重建**：
    *   **可见分支（Visible Branch）**：利用双重提升（Dual-lifting）方法，从单张图像中提取局部和全局特征，预测可见区域的高斯参数（位置、缩放、旋转、不透明度、颜色）。
    *   **补全分支（Completion Branch）**：针对单图缺失的内部结构（如牙齿、口腔），引入 FLAME 3D 变形模型作为几何先验，预测遮挡区域的高斯参数，从而构建完整的头部模型。
*   **双分支运动场（Dual-Branch Motion Fields）**：
    *   **粗粒度分支**：捕捉全局面部运动趋势。
    *   **细粒度分支**：建模局部高频细节（如口型微动）。
    *   **技术细节**：使用 HuBERT 提取音频特征，结合三平面哈希编码（Tri-plane Hash Encoder）对高斯点位置进行编码，通过 MLP 预测每个点的变形偏移量（$\Delta\mu, \Delta s, \Delta r, \Delta \alpha$）。
*   **训练策略**：第一阶段训练重建模块（使用 L1、LPIPS 和提升距离损失）；第二阶段冻结重建模块，专门优化运动场（加入专门的口型损失 $L_{lip}$）。

### 3. 实验设计
*   **数据集**：
    *   **HDTF**：高分辨率音频-视频数据集，用于训练和跨身份评估。
    *   **MEAD**：多视角情感数据集，用于验证跨数据集的泛化能力和不同头部姿态下的鲁棒性。
*   **对比基准（Baselines）**：
    *   **零样本/单样本方法**：Wav2Lip, EAT, Real3DPortrait, NeRFFaceSpeech。
    *   **预训练与适配方法**：MimicTalk（需要微调）。
*   **评价指标**：图像质量（PSNR, SSIM, LPIPS）、口型同步性（LMD, Sync-C）以及推理速度（FPS）。

### 4. 资源与算力
*   **硬件**：单张 **NVIDIA RTX 4090** GPU。
*   **训练时长**：整个训练过程大约耗时 **32 GPU 小时**。
*   **训练参数**：两个阶段各进行 200,000 次迭代，学习率为 $1.0 \times 10^{-4}$。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流数据集上进行了广泛测试，并遵循严格的跨身份协议（测试集身份在训练中未出现）。
*   **消融实验**：针对“补全分支（CB）”和“细粒度运动场（FM）”进行了消融研究，证明了各组件对恢复口腔细节和提升口型同步性的必要性。
*   **客观性**：通过定量指标、定性视觉对比以及不同姿态下的性能分析，实验设计较为全面、客观，能够反映模型在真实场景下的泛化能力。

### 6. 主要结论与发现
*   **泛化性突破**：SDTalk 实现了无需微调的单样本合成，且在图像质量指标（PSNR/SSIM）上显著优于需要微调的方法（如 MimicTalk）。
*   **效率优势**：推理速度达到 **45 FPS**，远超对比的 NeRF 或扩散模型方法，满足实时应用需求。
*   **结构完整性**：引入 FLAME 先验成功解决了单图重建中“牙齿缺失”或“口腔塌陷”的常见问题。

### 7. 优点（亮点）
*   **结构化先验的巧妙结合**：将参数化模型（FLAME）的稳定性与 3DGS 的渲染表现力结合，解决了单图信息不足的问题。
*   **分层运动建模**：双分支设计有效分离了全局动态与局部细节，提升了口型同步的自然度。
*   **端到端推理**：推理时重建模块仅需运行一次，后续仅靠运动场驱动，极大地优化了计算开销。

### 8. 不足与局限
*   **极端姿态下的退化**：虽然比现有方法更鲁棒，但在极端大角度头部转动时，合成质量仍会出现一定程度的下降。
*   **背景处理**：论文提到移除了背景区域，这意味着该方法目前主要聚焦于头部本身，完整的场景融合可能需要额外处理。
*   **依赖预训练音频模型**：性能在一定程度上受限于 HuBERT 特征的质量。

（完）
