---
title: "Pixal3D: Pixel-Aligned 3D Generation from Images"
title_zh: Pixal3D：基于图像的像素对齐 3D 生成
authors: "Dong-Yang Li, Wang Zhao, Yuxin Chen, Wenbo Hu, Meng-Hao Guo, Fang-Lue Zhang, Ying Shan, Shi-Min Hu"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10922v1"
tags: ["query:3dgen"]
score: 10.0
evidence: 用于从图像创建高保真3D资产的像素对齐3D生成范式
tldr: 针对现有图像转3D生成模型在像素级保真度上的不足，本文提出Pixal3D。该方法摒弃了传统的规范空间生成模式，采用像素对齐的3D生成范式，通过像素反投影机制将多尺度图像特征显式提升至3D特征体积，建立了明确的2D-3D对应关系。Pixal3D不仅显著提升了生成资产的保真度，还支持多视图输入和复杂场景合成，为高质量3D内容创作提供了新路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.10922v1/fig-001.webp\", \"caption\": \"Fig. 5. Qualitative comparison of single-view 3D generation on in-the-wild images.\", \"page\": 6, \"index\": 1, \"width\": 1022, \"height\": 1251}, {\"url\": \"assets/figures/arxiv/2605.10922v1/fig-002.webp\", \"caption\": \"Fig. 6. Qualitative comparison of multi-view 3D generation on Toys4K.\", \"page\": 7, \"index\": 2, \"width\": 976, \"height\": 1269}, {\"url\": \"assets/figures/arxiv/2605.10922v1/fig-003.webp\", \"caption\": \"Fig. 2. Overview of the Pixal3D framework. The framework consists of three key components: (1) Pixel-Aligned Structured Latent Representation Learning (top-right), which uses a VAE to compress pixel-aligned sparse SDF into efficient sparse latents; (2) an Image Back-Projection-based Conditioner (top-left) that explicitly lifts 2D image features into 3D feature volumes; and (3) a two-stage generative process (Structure Generation and Structured Latents Generation) conditioned on these volumes to predict coarse structure and detailed latents, respectively. Finally, the generated latents are decoded into a high-fidelity mesh.\", \"page\": 4, \"index\": 3, \"width\": 1010, \"height\": 362}, {\"url\": \"assets/figures/arxiv/2605.10922v1/fig-004.webp\", \"caption\": \"Fig. 4. Qualitative comparisons of single view 3D generation.\", \"page\": 5, \"index\": 4, \"width\": 1022, \"height\": 372}]"
motivation: 现有3D生成模型因在规范空间生成且依赖注意力机制注入图像信息，导致2D与3D之间缺乏明确的像素级对应关系，限制了生成结果的保真度。
method: 提出一种像素对齐的生成范式，通过像素反投影方案将多尺度图像特征显式映射到3D特征体积中，实现与输入视图一致的3D生成。
result: 实验表明Pixal3D在保持高生成质量的同时，显著提升了像素级保真度，达到了接近重建水平的精度，并能有效扩展至多视图和场景合成。
conclusion: Pixal3D首次实现了大规模的3D原生像素对齐生成，为从单视图或多视图图像中创建高保真3D物体和场景开辟了新方向。
---

## 摘要
近期 3D 生成模型的进展迅速提升了图像到 3D 合成的质量，实现了更高分辨率的几何结构和更逼真的外观。然而，衡量生成的 3D 资产与输入图像在像素级忠实度的“保真度”（fidelity）仍然是一个核心瓶颈。我们认为这源于隐式的 2D-3D 对应问题：大多数原生 3D 生成器在规范空间（canonical space）中合成形状，并通过注意力机制注入图像线索，导致像素到 3D 的关联变得模糊。为了解决这一问题，我们从 3D 重建中汲取灵感，提出了 Pixal3D，这是一种用于从图像创建高保真 3D 资产的像素对齐 3D 生成范式。Pixal3D 并非在规范姿态下生成，而是直接以与输入视图一致的像素对齐方式生成 3D。为实现这一点，我们引入了一种像素反向投影条件方案，显式地将多尺度图像特征提升到 3D 特征体积中，从而建立了明确的像素到 3D 对应关系。我们展示了 Pixal3D 不仅具有可扩展性且能够生成高质量的 3D 资产，还显著提高了保真度，接近重建算法的水平。此外，Pixal3D 通过聚合跨视图的反向投影特征体积，自然地扩展到多视图生成。最后，我们展示了像素对齐生成对场景合成的益处，并提出了一个模块化流水线，可以从图像中生成高保真、物体分离的 3D 场景。Pixal3D 首次在大规模上展示了原生 3D 像素对齐生成，并为从单视图或多视图图像进行物体或场景的高保真 3D 生成提供了一种启发性的新途径。项目主页：https://ldyang694.github.io/projects/pixal3d/

## Abstract
Recent advances in 3D generative models have rapidly improved image-to-3D synthesis quality, enabling higher-resolution geometry and more realistic appearance. Yet fidelity, which measures pixel-level faithfulness of the generated 3D asset to the input image, still remains a central bottleneck. We argue this stems from an implicit 2D-3D correspondence issue: most 3D-native generators synthesize shape in canonical space and inject image cues via attention, leaving pixel-to-3D associations ambiguous. To tackle this issue, we draw inspiration from 3D reconstruction and propose Pixal3D, a pixel-aligned 3D generation paradigm for high-fidelity 3D asset creation from images. Instead of generating in a canonical pose, Pixal3D directly generates 3D in a pixel-aligned way, consistent with the input view. To enable this, we introduce a pixel back-projection conditioning scheme that explicitly lifts multi-scale image features into a 3D feature volume, establishing direct pixel-to-3D correspondence without ambiguity. We show that Pixal3D is not only scalable and capable of producing high-quality 3D assets, but also substantially improves fidelity, approaching the fidelity level of reconstruction. Furthermore, Pixal3D naturally extends to multi-view generation by aggregating back-projected feature volumes across views. Finally, we show pixel-aligned generation benefits scene synthesis, and present a modular pipeline that produces high-fidelity, object-separated 3D scenes from images. Pixal3D for the first time demonstrates 3D-native pixel-aligned generation at scale, and provides a new inspiring way towards high-fidelity 3D generation of object or scene from single or multi-view images. Project page: https://ldyang694.github.io/projects/pixal3d/

---

## 论文详细总结（自动生成）

这是一份关于论文《Pixal3D: Pixel-Aligned 3D Generation from Images》的结构化深度总结：

### 1. 论文的核心问题与整体含义（研究动机和背景）
*   **核心问题**：现有的图像转 3D 生成模型在**保真度（Fidelity）**上存在严重瓶颈。生成的 3D 模型往往只能在语义上“接近”输入图像，但在像素级细节和几何对齐上表现欠佳。
*   **研究背景**：
    *   **生成模型的问题**：大多数原生 3D 生成器（如 TRELLIS, Direct3D）在“规范空间”（Canonical Space）中生成形状，通过交叉注意力（Cross-Attention）注入图像特征。这种方式导致 2D 像素与 3D 空间之间的对应关系是隐式且模糊的，模型难以精确还原局部细节。
    *   **重建模型的启示**：传统的 3D 重建（如单视图深度估计、点图预测）通常是“像素对齐”的，具有明确的几何约束。
*   **整体含义**：Pixal3D 旨在结合“重建的几何严谨性”与“生成的创造性能力”，通过像素对齐的生成范式解决保真度难题。

### 2. 论文提出的方法论
*   **核心思想**：摒弃规范空间生成，直接在**相机坐标系（输入视图视角）**下生成 3D 资产。
*   **关键技术细节**：
    *   **像素反向投影条件方案（Back-projection Conditioning）**：取代交叉注意力机制。将 2D 图像特征（由 DINOv2 提取）沿相机射线反向投影到 3D 体积中。每个 3D 体素直接获取其对应像素的特征，建立显式的 2D-3D 几何对应。
    *   **多尺度特征提升**：利用特征上采样模型（NAF）将 DINOv2 的粗粒度特征提升至高分辨率，以保留和传播图像中的微小结构细节。
    *   **两阶段生成流程**：
        1.  **结构生成**：预测粗略的占用网格。
        2.  **结构化潜在表示生成**：在稀疏体素潜在空间中进行去噪，最后通过 VAE 解码器生成高保真 SDF 和网格。
    *   **多视图扩展**：通过对多个视图的反向投影特征体积进行简单平均聚合，实现多视图一致性生成。
    *   **场景生成流水线**：结合 SAM3（分割）、Qwen-image-edit（补全）和 MoGe（全局深度估计），将多个像素对齐的物体组合成完整的 3D 场景。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：Objaverse 的子集 TRELLIS-500K。
    *   **测试集**：Toys4K（用于定量评估几何精度）、150 张互联网/AI 生成的野外图像（用于定性评估和用户研究）。
*   **Benchmark 与对比方法**：
    *   **对比对象**：TRELLIS, TripoSG, Hunyuan3D-2.1, Direct3D-S2。
    *   **评估指标**：IoU、PSNR、SSIM、LPIPS（图像一致性）；法线误差（Mean/Median/Mean_B）；语义一致性指标（ULIP2, Uni3D）；以及用户偏好评分。

### 4. 资源与算力
*   **训练细节**：
    *   **稀疏 DiT 训练**：采用由粗到精的策略，分辨率从 256 逐步提升至 1024，总计迭代约 420k 次。
    *   **密集 DiT 训练**：训练 500k 次迭代。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号和数量（如 A100 数量），但从其训练迭代次数和处理 1024 分辨率的规模来看，该模型需要相当规模的工业级算力支持。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **定量实验**：在 Toys4K 全量数据集上进行了详尽的法线和几何对比。
    *   **定性实验**：展示了大量单视图、多视图及复杂场景的生成结果。
    *   **消融实验**：验证了“反向投影条件”相对于“交叉注意力”的优越性，以及“多尺度特征上采样”对细节还原的作用。
*   **充分性评价**：实验设计较为全面，涵盖了从物体到场景、从合成到真实图像的多个维度。通过引入法线图对比（Normal Rendering），客观地量化了保真度这一主观指标，实验结果具有较强的说服力。

### 6. 论文的主要结论与发现
*   **像素对齐是关键**：直接在相机空间生成并使用显式几何投影，能显著消除 2D-3D 对应的歧义。
*   **保真度飞跃**：Pixal3D 在保真度指标上大幅领先现有模型，达到了接近 3D 重建算法的精确度，同时保持了生成模型对遮挡区域的补全能力。
*   **可扩展性**：该范式可以无缝扩展到多视图输入，且视图越多，生成的确定性（重建感）越强。

### 7. 优点（亮点）
*   **范式创新**：首次在大规模 3D 原生生成中验证了像素对齐范式的可行性。
*   **高保真度**：解决了 3D 生成中常见的“长得像但对不上”的问题，细节还原度极高（如键盘布局、花瓣数量）。
*   **模块化场景生成**：通过像素对齐简化了多物体对齐问题，避免了复杂的 7-DoF 位姿估计。

### 8. 不足与局限
*   **噪声敏感性**：对输入图像的分割边缘噪声较为敏感，可能导致生成的几何边缘出现伪影。
*   **位姿依赖**：多视图模式下仍依赖已知的相机参数。
*   **补全风险**：场景生成依赖 2D 修复（Inpainting），在处理复杂遮挡时，2D 补全的错误会直接遗传给 3D 结果。
*   **纹理缺失**：目前主要关注几何生成，尚未深度整合材质和纹理贴图的同步生成。

（完）
