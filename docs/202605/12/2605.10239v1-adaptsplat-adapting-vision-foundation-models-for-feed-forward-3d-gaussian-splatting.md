---
title: "AdaptSplat: Adapting Vision Foundation Models for Feed-Forward 3D Gaussian Splatting"
title_zh: AdaptSplat：适配视觉基础模型用于前馈 3D 高斯泼溅
authors: "Mingwei Xing, Xinliang Wang, Yifeng Shi"
date: 2026-05-11
pdf: "https://arxiv.org/pdf/2605.10239v1"
tags: ["query:ff3dr"]
score: 10.0
evidence: 用于前馈3D高斯泼溅的适配器设计
tldr: AdaptSplat 提出了一种轻量级适配器方案，用于增强前馈 3D 高斯泼溅（3DGS）的性能。针对现有方法在跨域泛化和高频几何细节恢复上的不足，该研究通过在视觉大模型中引入仅 1.5M 参数的频率保持适配器（FPA），从浅层特征中提取方向感知的结构先验，有效补偿了深层特征的平滑效应。实验证明，该方法在多个基准测试中达到了最先进的重建水平，并具有出色的泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.10239v1/fig-001.webp\", \"caption\": \"Table 3: Quantitative results on DL3DV at 280× 512 resolution.\", \"page\": 7, \"index\": 1, \"width\": 829, \"height\": 280}, {\"url\": \"assets/figures/arxiv/2605.10239v1/fig-002.webp\", \"caption\": \"Figure 3: Qualitative comparison on DL3DV. AdaptSplat yields superior high-frequency fidelity and sharper geometric boundaries.\", \"page\": 7, \"index\": 2, \"width\": 700, \"height\": 306}, {\"url\": \"assets/figures/arxiv/2605.10239v1/fig-003.webp\", \"caption\": \"Table 4: Zero-shot generalization on Tanks & Temples and Mip-NeRF360. Model trained on DL3DV, tested on unseen datasets without fine-tuning.\", \"page\": 8, \"index\": 3, \"width\": 822, \"height\": 245}, {\"url\": \"assets/figures/arxiv/2605.10239v1/fig-004.webp\", \"caption\": \"Figure 2: Overview of AdaptSplat. Based on the generic feature extraction-interaction-decoding pipeline, AdaptSplat introduces a lightweight Frequency-Preserving Adapter (FPA, 1.5M parameters). FPA explicitly extracts high-frequency structural priors to combat the network’s spectral bias. These priors are then injected into the Multi-view Transformer as frequency-guided positional encodings (PE) and into the DPT decoder via multi-scale adaptive residual modulation, significantly sharpening the 3D Gaussian primitives.\", \"page\": 4, \"index\": 4, \"width\": 693, \"height\": 390}, {\"url\": \"assets/figures/arxiv/2605.10239v1/fig-005.webp\", \"caption\": \"Figure 4: Attention maps Visualization. Without FPA, attention is diffuse; with FPA, attention focuses on structural edges and feature boundaries are sharp.\", \"page\": 9, \"index\": 5, \"width\": 573, \"height\": 235}]"
motivation: 现有前馈 3DGS 方法受限于 3D 训练数据规模和深度网络的低通滤波效应，难以处理跨域泛化和高频几何细节。
method: 提出频率保持适配器（FPA），通过高频位置编码和自适应残差调制，将视觉大模型浅层特征中的结构先验集成到通用流水线中。
result: 在多个标准基准测试上实现了最先进的前馈重建性能，并展现出稳定的跨领域泛化能力。
conclusion: 简单的轻量级适配器设计足以显著提升 3DGS 的几何保真度和泛化性，无需复杂的架构工程。
---

## 摘要
本研究探索了一种用于前馈 3D 高斯泼溅（3DGS）的简单且强大的轻量级适配器设计。现有方法通常在“图像特征提取 -> 多视图交互 -> 特征解码”这一通用流水线之上，应用复杂的、针对特定架构的设计。然而，受限于 3D 训练数据的规模瓶颈以及深度网络的低通滤波效应，这些方法在跨域泛化和高频几何保真度方面仍表现不足。为解决这些问题，我们提出了 AdaptSplat，它证明了无需复杂的组件工程，仅在通用架构中引入一个参数量仅为 1.5M 的适配器，就足以实现卓越的性能。具体而言，我们设计了一个轻量级的频率保持适配器（FPA），它从强大的视觉基础模型主干网络的浅层特征中提取方向感知的高频结构先验，并通过高频位置编码和自适应残差调制将其无缝集成到通用流水线中。这有效地补偿了深度特征中由于过度平滑引起的高频衰减，提高了高斯基元在复杂表面和锐利边界上的拟合精度。大量的实验表明，AdaptSplat 在多个标准基准测试中实现了最先进的前馈重建性能，并具有稳定的跨域泛化能力。代码可在 https://github.com/xmw666/AdaptSplat 获取。

## Abstract
This work explores a simple yet powerful lightweight adapter design for feed-forward 3D Gaussian Splatting (3DGS). Existing methods typically apply complex, architecture-specific designs on top of the generic pipeline of image feature extraction $\rightarrow$ multi-view interaction $\rightarrow$ feature decoding. However, constrained by the scale bottleneck of 3D training data and the low-pass filtering effect of deep networks, these methods still fall short in cross-domain generalization and high-frequency geometric fidelity. To address these problems, we propose AdaptSplat, which demonstrates that without complex component engineering, introducing a single adapter of only 1.5M parameters into the generic architecture is sufficient to achieve superior performance. Specifically, we design a lightweight Frequency-Preserving Adapter (FPA) that extracts direction-aware high-frequency structural priors from the shallow features of a powerful vision foundation model backbone, and seamlessly integrates them into the generic pipeline via high-frequency positional encodings and adaptive residual modulation. This effectively compensates for the high-frequency attenuation caused by over-smoothing in deep features, improving the fitting accuracy of Gaussian primitives on complex surfaces and sharp boundaries. Extensive experiments demonstrate that AdaptSplat achieves state-of-the-art feed-forward reconstruction performance on multiple standard benchmarks, with stable generalization across domains. Code available at: https://github.com/xmw666/AdaptSplat.

---

## 论文详细总结（自动生成）

这是一份关于论文《AdaptSplat: Adapting Vision Foundation Models for Feed-Forward 3D Gaussian Splatting》的结构化深入分析总结：

### 1. 核心问题与整体含义（研究动机和背景）
该论文聚焦于**前馈 3D 高斯泼溅（Feed-Forward 3DGS）**领域。
*   **核心挑战**：现有的前馈 3DGS 方法通常依赖复杂的架构设计（如极线 Transformer、成本体积等）来提升性能，但面临三大瓶颈：
    1.  **泛化能力差**：过度设计的模型在未见过的场景中表现不佳。
    2.  **视觉基础模型（VFM）利用不足**：现有方法多将 VFM 冻结作为特征提取器，切断了其对 3D 几何约束的适应性。
    3.  **光谱偏差（Spectral Bias）**：深度神经网络具有“低通滤波”效应，导致高频细节（如物体边缘、复杂纹理）被平滑，产生各向同性的“球状”退化，无法精确拟合复杂表面。
*   **研究目标**：通过一种极简的适配器设计，在不改变通用流水线的前提下，激活 VFM 的泛化先验并修复高频几何细节。

### 2. 方法论：核心思想与关键技术
AdaptSplat 提出了一个**极简适配范式**，核心是一个仅 **1.5M 参数**的**频率保持适配器（FPA）**。
*   **核心思想**：利用离散小波变换（DWT）从 VFM 的浅层特征中提取方向感知的高频结构先验，并将其注入深度解码过程，以对抗深度网络的平滑效应。
*   **关键技术细节**：
    *   **频率保持适配器 (FPA)**：使用 Haar 小波将图像分解为 LL、LH、HL、HH 四个子带。LH 和 HL 子带捕捉水平和垂直方向的高频能量，为网络提供方向性的结构张量。
    *   **高频引导注意力位置编码**：将 FPA 提取的高频信号作为位置编码（PE）注入 Multi-view Transformer 的 Query 和 Key 空间。这引导注意力机制优先在结构相似的区域进行聚合，保持边缘锐度。
    *   **自适应多尺度残差调制**：在 DPT 解码器的上采样阶段，利用 FPA 生成的动态空间掩码对特征进行残差调制，补偿双线性插值带来的二次高频衰减。
    *   **损失函数**：除了常规的 MSE 和 LPIPS，引入了**焦频率损失（Focal Frequency Loss, FFL）**，在频率域进一步约束预测结果。

### 3. 实验设计
*   **数据集**：
    *   **训练与评估**：DL3DV（大规模多样化场景）和 RealEstate10K（RE10K，室内外漫游）。
    *   **零样本泛化测试**：Tanks & Temples（复杂户外）和 Mip-NeRF360（无边界真实场景）。
*   **基准方法（Benchmarks）**：对比了 PixelSplat、MVSplat、DepthSplat、YoNoSplat、Long-LRM、MVP 等当前最先进的前馈重建模型。
*   **评估指标**：PSNR、SSIM、LPIPS 以及反映高斯各向异性的 FA 指标。

### 4. 资源与算力
论文明确说明了实验环境：
*   **硬件**：使用了 **32 台 NVIDIA H200 GPU**。
*   **训练时长**：采用三阶段训练策略。
    *   第一阶段：100k 迭代（约 4 天）。
    *   第二阶段：50k 迭代（约 4 天）。
    *   第三阶段：30k 迭代（约 3 天）。
*   **总计**：单次完整训练流程约需 11 天（在 32 卡规模下）。

### 5. 实验数量与充分性
*   **实验规模**：论文在四个主流数据集上进行了详尽测试，涵盖了从低分辨率（256px）到高分辨率（960px）的多种配置。
*   **消融实验**：针对 VFM 的作用、FPA 模块、FFL 损失、多尺度调制等进行了逐项验证，并对比了不同高频提取策略（如傅里叶变换、Sobel 算子等）。
*   **客观性与公平性**：通过在相同配置下重新训练 Long-LRM 和 MVP 等竞争对手，确保了对比的基准一致。引入 FA 指标定量分析高斯基元的形状退化，增强了实验的说服力。

### 6. 主要结论与发现
*   **极简胜过复杂**：无需复杂的 3D 归纳偏置，仅靠 1.5M 参数的适配器即可超越拥有数亿参数的复杂模型。
*   **频率补偿至关重要**：显式引入高频方向先验能有效打破高斯基元的“各向同性退化”，显著提升边缘保真度。
*   **VFM 潜力巨大**：解冻 VFM 并配合轻量级适配，能显著增强模型在野外场景（Zero-shot）的泛化表现。

### 7. 优点
*   **架构优雅**：回归通用流水线，通过“即插即用”的适配器解决核心痛点，降低了系统复杂度。
*   **性能卓越**：在 RE10K 和 DL3DV 上均刷新了 SOTA 记录，尤其在处理透明物体、重叠边缘等高频区域表现惊人。
*   **效率高**：Tiny 版本在保持高性能的同时，参数量和显存占用均优于同类模型，推理速度快。

### 8. 不足与局限
*   **算力门槛高**：虽然适配器轻量，但训练过程依赖 32 张 H200 显卡且耗时较长，普通研究者难以复现全量训练。
*   **深度依赖 VFM**：性能高度依赖于 DINOv3 等基础模型的预训练质量，若基础模型存在偏差，适配器可能难以完全纠正。
*   **长序列处理**：虽然在多视图下表现稳定，但在极长轨迹或超大规模场景下的显存管理仍面临挑战。

（完）
