---
title: "FeatureFox: Sample-Efficient Panoptic Graph Segmentation for Machining Feature Recognition in B-Rep 3D-CAD Models"
title_zh: FeatureFox：用于 B-Rep 3D-CAD 模型加工特征识别的样本高效型全景图分割
authors: "Bertram Fuchs, Altay Kacan, Aaron Haag, Oliver Lohse"
date: 2026-04-29
pdf: "https://arxiv.org/pdf/2604.26770v1"
tags: ["query:partseg"]
score: 9.0
evidence: 3D-CAD模型中加工特征识别的全景分割
tldr: FeatureFox 是一种针对 B-Rep 3D-CAD 模型的高效全景图分割方法，旨在解决自动特征识别（AFR）中数据需求量大和模型复杂的问题。该方法通过边缘分类器定位特征边界，并利用修剪后的面邻接图恢复加工实例及其语义标签。实验表明，FeatureFox 在样本效率和计算速度上显著优于深度学习基线 AAGNet，仅需少量训练样本即可达到高精度，并具备良好的工业泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.26770v1/fig-001.webp\", \"caption\": \"Fig. 4. Sample efficiency on MFInstSeg (excluding stock): PQ versus trainingset size for AAGNet and FeatureFox. FeatureFox dominates in the low-data regime and reaches > 90% PQ with ∼ 250 training parts, whereas AAGNet requires ∼ 5,000; AAGNet is only marginally better on the full training set.\", \"page\": 5, \"index\": 1, \"width\": 524, \"height\": 355}, {\"url\": \"assets/figures/arxiv/2604.26770v1/fig-002.webp\", \"caption\": \"Fig. 3. FeatureFox pipeline. Starting from a B-Rep 3D-CAD model, we construct a face-adjacency graph and attributes, propagate vertex information to edges (1), and localize the feature with a calibrated edge boundary classifier for instance grouping (2). We then compute connected-component attributes (3) and recognize the class of each predicted instance with a semantic model (4). Pink: Attribute calculation. Teal: Model training. Gray: Intermediate representation.\", \"page\": 5, \"index\": 2, \"width\": 816, \"height\": 277}]"
motivation: 现有的基于学习的自动特征识别方法通常过于复杂、依赖大量数据，且未能将实例分组与语义标注有效结合。
method: 提出一种全景 AFR 流水线，通过增强边缘属性的二元分类器识别边界，并结合面邻接图的连通分量与子图属性分类器实现特征识别。
result: 在 MFInstSeg 数据集上，FeatureFox 仅需 250 个样本即可达到 0.9 以上的 PQ 分数，且训练速度极快，在特征定位精度上略领先于 AAGNet。
conclusion: FeatureFox 证明了通过高效的图分割策略可以在极低的数据需求下实现高精度的工业 CAD 特征识别，具有极强的实际应用价值。
---

## 摘要
B-Rep 3D-CAD 模型上的自动特征识别 (AFR) 是 CAD/CAM 自动化的核心，然而大多数基于学习的方法往往过于复杂、依赖大量数据，且将实例分组与语义标注分开评估。我们提出了 FeatureFox，这是一种全景 AFR 流水线，可输出带有语义标签的加工实例：通过在丰富的边缘属性上使用校准的二元边缘分类器来定位特征边界，在修剪后的面邻接图中将实例恢复为连通分量，并利用每实例分类器从聚合的子图属性中预测加工类别。我们在 MFInstSeg 数据集上使用全景质量 (PQ) 指标进行评估，该指标综合评分了实例分离和语义正确性。FeatureFox 的样本和计算效率显著优于深度学习基准 AAGNet，仅需约 250 个训练零件即可达到 PQ > 0.9，而 AAGNet 则需要约 5,000 个，且在完整 MFInstSeg 集上的训练在 GPU 上仅需数秒。在完整训练集上，AAGNet 在 PQ 指标上略胜 FeatureFox，而 FeatureFox 在特征级识别和定位精度方面仍保持微弱领先。最后，利用其低数据需求特性，我们在 270 个手动标注的工业 CAD 零件上训练了 FeatureFox，并展示了其对未见过的真实工业零件的定性泛化能力，表明了其在现实世界中的实用性。

## Abstract
Automatic feature recognition (AFR) on B-Rep 3D-CAD models is central to CAD/CAM automation, yet most learning-based methods are complex, data-hungry, and evaluate instance grouping and semantic labeling separately. We present FeatureFox, a panoptic AFR pipeline that outputs machining instances with semantic labels: a calibrated binary edge classifier on enriched edge attributes localizes feature boundaries, instances are recovered as connected components in a pruned face-adjacency graph, and a per-instance classifier predicts the machining class from aggregated subgraph attributes. We evaluate on MFInstSeg using Panoptic Quality (PQ), which jointly scores instance separation and semantic correctness. FeatureFox is substantially more sample- and compute-efficient than the deep baseline AAGNet, reaching $\mathrm{PQ}>0.9$ with $\sim250$ training parts versus $\sim5{,}000$ for AAGNet, and training on the full MFInstSeg set takes seconds on a GPU. On the full training set, AAGNet surpasses FeatureFox marginally in PQ, while FeatureFox remains slightly ahead in feature-level recognition and localization accuracy. Finally, leveraging its low data requirement, we train FeatureFox on $270$ manually labeled industrial CAD parts and show qualitative generalization to an unseen real industrial part, indicating practical real-world applicability.

---

## 论文详细总结（自动生成）

### 论文总结：FeatureFox - 用于 B-Rep 3D-CAD 模型加工特征识别的样本高效型全景图分割

#### 1. 核心问题与整体含义（研究动机和背景）
自动特征识别（AFR）是连接 CAD 设计与 CAM 制造（如刀具路径规划）的关键环节。尽管深度学习在 B-Rep（边界表示法）模型上取得了进展，但现有方法（如 AAGNet）存在三大痛点：
*   **数据饥渴**：通常需要数千甚至上万个标注零件才能达到高精度，而工业界标注数据极其稀缺且昂贵。
*   **任务割裂**：往往将语义分割（是什么）和实例分割（是哪一个）分开处理，缺乏统一的评估标准。
*   **计算复杂**：深度图神经网络（GNN）训练耗时长，难以在资源受限的工业现场快速部署。
**FeatureFox** 的目标是提出一种**样本高效、计算轻量且可解释**的全景分割流水线，在极少数据下实现高精度的加工特征识别。

#### 2. 方法论：核心思想与关键技术
FeatureFox 将 AFR 建模为**全景图分割（Panoptic Graph Segmentation）**任务，采用分层决策架构：
*   **图表示**：将 B-Rep 模型转化为面邻接图（Face-Adjacency Graph），面为节点，共享边缘为图的边。
*   **增强边缘属性**：不仅提取边缘本身的几何特征（如曲率、长度），还通过信息传播聚合相邻面的属性（如二面角、凹凸性、面积比等）。
*   **特征边界定位（实例分割）**：使用校准的 **XGBoost 二元分类器**预测每条边是否为特征边界。通过等渗回归（Isotonic Calibration）校准概率，并利用阈值修剪图，将剩下的连通分量识别为独立的加工特征实例。
*   **实例语义识别**：对每个识别出的实例子图进行属性聚合（如面/边数量、特征尺度、曲率分布直方图等），再通过另一个 **XGBoost 分类器**预测其语义类别（如孔、槽、口袋等）。

#### 3. 实验设计
*   **数据集**：主要使用公开的 **MFInstSeg** 数据集（包含约 6.2 万个 STEP 文件）。此外，还使用了 270 个手动标注的真实工业零件和 NIST 公开 CAD 模型进行泛化性验证。
*   **评估指标**：引入了计算机视觉中的**全景质量（PQ, Panoptic Quality）**，统一衡量实例定位精度和语义分类正确性。
*   **对比基准（Benchmark）**：主要对比了当前最先进的深度学习模型 **AAGNet** 和 **MFTReNet**。

#### 4. 资源与算力
*   **硬件**：使用单张 **NVIDIA RTX 6000 Ada** GPU。
*   **训练时长**：
    *   **FeatureFox**：在完整数据集上训练仅需 **8 秒**（GPU 利用率 100%）。
    *   **AAGNet**：训练 100 个 epoch 需要 **38 分钟**。
*   **效率对比**：FeatureFox 的训练速度比深度学习基准快了约 285 倍。

#### 5. 实验数量与充分性
*   **实验规模**：进行了多组对比实验，包括全量数据对比、样本效率消融实验（从 50 个到 4.3 万个样本的不同梯度）以及工业零件的定性验证。
*   **充分性与公平性**：
    *   实验涵盖了从合成数据到真实工业数据的跨度。
    *   通过 3 个随机种子取平均值来保证结果的稳定性。
    *   对比实验中使用了相同的超参数设置，确保了对比的客观性。

#### 6. 主要结论与发现
*   **极高的样本效率**：FeatureFox 仅需 **250 个**训练样本即可达到 PQ > 0.9 的精度，而 AAGNet 达到同等水平需要约 **5,000 个**样本。
*   **性能表现**：在全量数据下，AAGNet 的 PQ 略高（约高 1%），但在特征级完全识别率和定位精度上，FeatureFox 依然保持微弱领先。
*   **工业泛化性**：在仅 270 个工业样本训练下，模型能成功识别 NIST 复杂零件中的孔、槽和口袋，证明了其实际应用潜力。

#### 7. 优点（亮点）
*   **轻量且快速**：摒弃了复杂的深度 GNN，采用基于树的集成学习，极大地降低了算力门槛。
*   **全景视角**：首次在 AFR 领域倡导使用 PQ 指标，将实例和语义任务有机统一。
*   **可解释性强**：分层架构（先找边界，再定类别）符合人类工程师的直觉，且特征工程透明。

#### 8. 不足与局限
*   **非端到端风险**：由于是分步流水线，第一步的边界预测错误会直接传播到第二步的语义分类。
*   **复杂拓扑处理**：目前难以处理非连通的特征实例（即一个加工特征由 B-Rep 中不相邻的面组成）。
*   **对几何特征的依赖**：高度依赖人工设计的特征工程，对于某些极其模糊或非标准的几何边界，鲁棒性可能不如端到端的深度学习模型。

（完）
