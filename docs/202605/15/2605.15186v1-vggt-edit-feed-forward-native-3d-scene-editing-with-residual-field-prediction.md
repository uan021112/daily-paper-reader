---
title: "VGGT-Edit: Feed-forward Native 3D Scene Editing with Residual Field Prediction"
title_zh: VGGT-Edit：基于残差场预测的前馈原生 3D 场景编辑
authors: "Kaixin Zhu, Yiwen Tang, Yifan Yang, Renrui Zhang, Bohan Zeng, Ziyu Guo, Ruichuan An, Zhou Liu, Qizhi Chen, Delin Qu, Jaehong Yoon, Wentao Zhang"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.15186v1"
tags: ["query:ff3dr"]
score: 10.0
evidence: 前馈原生3D场景编辑与重建
tldr: VGGT-Edit是一个前馈式原生3D场景编辑框架，旨在解决传统2D提升方法导致的纹理模糊和几何不一致问题。该方法通过深度同步文本注入和残差变换头，直接预测3D几何位移，实现了高效且一致的文本驱动场景编辑。此外，研究者还构建了DeltaScene数据集以支持高质量训练，实验证明该方法在细节保留和推理速度上均优于现有基准。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.15186v1/fig-001.webp\", \"caption\": \"Figure 4: Overview of the VGGT-Edit Model Architecture. Our model features a synchronized prompt injection mechanism and a residual transformation head, enabling native 3D scene editing through a single forward pass.\", \"page\": 6, \"index\": 1, \"width\": 748, \"height\": 352}, {\"url\": \"assets/figures/arxiv/2605.15186v1/fig-002.webp\", \"caption\": \"Figure 6: Generalization to Unseen Instructions.\", \"page\": 11, \"index\": 2, \"width\": 598, \"height\": 377}, {\"url\": \"assets/figures/arxiv/2605.15186v1/fig-003.webp\", \"caption\": \"Figure 1: Comparison of 3D Editing Methods.\", \"page\": 2, \"index\": 3, \"width\": 738, \"height\": 347}, {\"url\": \"assets/figures/arxiv/2605.15186v1/fig-004.webp\", \"caption\": \"Figure 5: Qualitative Comparison on Diverse 3D Editing Tasks. We evaluate VGGT-Edit against state-of-the-art baselines across various scene-level operations, including object addition, removal, and transformation. Our method produces sharp, instruction-accurate results while maintaining superior multi-view consistency compared to 2D-lifting approaches.\", \"page\": 10, \"index\": 4, \"width\": 683, \"height\": 404}, {\"url\": \"assets/figures/arxiv/2605.15186v1/fig-005.webp\", \"caption\": \"Figure 2: The DeltaScene Data Pipeline. Our automated framework leverages LLMs and VLMs to generate diverse 3D editing pairs through a multi-stage consensus filtering process.\", \"page\": 4, \"index\": 5, \"width\": 736, \"height\": 307}, {\"url\": \"assets/figures/arxiv/2605.15186v1/fig-006.webp\", \"caption\": \"Figure 3: Overview of the DeltaScene Dataset Details.\", \"page\": 5, \"index\": 6, \"width\": 767, \"height\": 199}]"
motivation: 现有的3D编辑方法多依赖2D视图编辑后再提升至3D，导致空间一致性差且纹理模糊，难以满足交互式应用需求。
method: 提出深度同步文本注入以对齐语义引导，并利用残差变换头直接预测3D几何位移，同时结合多项目标函数进行监督。
result: 实验表明VGGT-Edit在物体细节清晰度、多视图一致性以及推理速度方面均显著优于基于2D提升的基准模型。
conclusion: 该研究通过原生3D编辑路径和高质量数据集，为实现快速、高保真且空间一致的交互式3D场景编辑提供了有效方案。
---

## 摘要
高质量 3D 场景重建近期已向可泛化的前馈架构演进，实现了在单次前向传递中生成复杂环境。然而，尽管这些模型在静态场景感知方面表现强劲，但在响应动态人类指令方面仍存在局限，这限制了它们在交互式应用中的使用。现有的编辑方法通常依赖于 2D 提升（2D-lifting）策略，即独立编辑单个视图，然后将其提升回 3D 空间。这种间接流水线往往导致纹理模糊和几何不一致，因为 2D 编辑器缺乏跨视角保持结构所需的空间感知。为了解决这些局限性，我们提出了 VGGT-Edit，这是一个用于文本条件原生 3D 场景编辑的前馈框架。VGGT-Edit 引入了深度同步文本注入，以将语义引导与骨干网络的空间位姿对齐，确保稳定的指令落地。该语义信号随后由残差变换头处理，直接预测 3D 几何位移以使场景变形，同时保持背景稳定性。为了确保高保真结果，我们使用多项目标函数对框架进行监督，以强制执行几何准确性和跨视图一致性。我们还构建了 DeltaScene 数据集，这是一个通过自动化流水线生成的大规模数据集，并采用 3D 一致性过滤以确保真值质量。实验表明，VGGT-Edit 显著优于 2D 提升基线，产生了更清晰的物体细节、更强的多视图一致性以及近乎瞬时的推理速度。

## Abstract
High-quality 3D scene reconstruction has recently advanced toward generalizable feed-forward architectures, enabling the generation of complex environments in a single forward pass. However, despite their strong performance in static scene perception, these models remain limited in responding to dynamic human instructions, which restricts their use in interactive applications. Existing editing methods typically rely on a 2D-lifting strategy, where individual views are edited independently and then lifted back into 3D space. This indirect pipeline often leads to blurry textures and inconsistent geometry, as 2D editors lack the spatial awareness required to preserve structure across viewpoints. To address these limitations, we propose VGGT-Edit, a feed-forward framework for text-conditioned native 3D scene editing. VGGT-Edit introduces depth-synchronized text injection to align semantic guidance with the backbone's spatial poses, ensuring stable instruction grounding. This semantic signal is then processed by a residual transformation head, which directly predicts 3D geometric displacements to deform the scene while preserving background stability. To ensure high-fidelity results, we supervise the framework with a multi-term objective function that enforces geometric accuracy and cross-view consistency. We also construct the DeltaScene Dataset, a large-scale dataset generated through an automated pipeline with 3D agreement filtering to ensure ground-truth quality. Experiments show that VGGT-Edit substantially outperforms 2D-lifting baselines, producing sharper object details, stronger multi-view consistency, and near-instant inference speed.

---

## 论文详细总结（自动生成）

这篇论文介绍了 **VGGT-Edit**，一个旨在实现高效、一致且原生的 3D 场景编辑的前馈框架。以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **研究背景**：当前的 3D 场景重建已进入“前馈（Feed-forward）”时代（如 VGGT、$\pi^3$），能够单次推理生成复杂环境。然而，3D 场景的**交互式编辑**仍然滞后。
*   **核心问题**：现有的 3D 编辑方法多采用“2D 提升（2D-lifting）”策略，即先用 2D 编辑器修改各视角图像，再重新重建。这种方式存在两个致命缺陷：
    1.  **不一致性**：2D 编辑器缺乏空间感知，导致不同视角间的几何和纹理无法对齐，产生模糊或伪影。
    2.  **效率低下**：通常涉及耗时的多视图扩散采样或逐场景优化。
*   **研究动机**：开发一种能够直接在 3D 几何场中操作、响应文本指令、且具备近乎瞬时推理速度的原生 3D 编辑模型。

### 2. 方法论
VGGT-Edit 的核心思想是将编辑任务建模为**原生 3D 残差场预测**。
*   **冻结的几何先验**：基于预训练的 $\pi^3$ 重建模型作为骨干网络，提取多视图特征并提供基础几何结构。
*   **深度同步文本注入（Depth-Synchronized Text Injection）**：
    *   将文本指令（OpenCLIP 编码）注入到 Transformer 解码器的特定层中，这些层与骨干网络的位姿调制阶段对齐。
    *   这种同步确保了语义引导能在几何形状形成的相同特征深度进行融合。
*   **视角感知重要性权重（View-Aware Importance Weighting）**：
    *   引入轻量级 MLP，根据遮挡情况、边界比例和置信度为不同视角分配权重，优先处理观察清晰的视角，减少噪声。
*   **残差变换头（Residual Transformation Head）**：
    *   不重新学习整个场景，而是预测一个密集的 3D 位移场（$\Delta P$）。
    *   最终几何输出为：$P_{edit} = P_{base} + \Delta P \odot M$（$M$ 为编辑掩码）。这种设计能有效保持背景区域的稳定性。
*   **多项目标函数**：包括掩码尺度对齐损失、编辑重建损失、背景保持损失、法线一致性损失以及相机空间投影一致性损失。

### 3. 实验设计
*   **数据集：DeltaScene**
    *   构建了一个包含约 **100,000 对**高质量 3D 编辑对的大规模数据集。
    *   涵盖增加（Add）、删除（Delete）、修改（Modify）、移动（Move）四种原子操作。
    *   数据源自 Replica、ScanNet 和 ScanNet++，通过自动化流水线生成，并经过 3D 一致性过滤。
*   **对比基准（Baselines）**：
    *   **优化类方法**：GaussCtrl, EditSplat。
    *   **2D 提升/前馈类方法**：Omni-3DEdit, Edit3r, 以及适配后的 NoPoSplat。
*   **评估指标**：CLIP Score（语义对齐）、C-FID/C-KID（跨视图一致性）、推理耗时。

### 4. 资源与算力
*   **硬件**：使用 **8 张 NVIDIA A100 GPU** 进行训练。
*   **训练细节**：迭代 10k 次，Batch Size 为 16，使用 Adam 优化器。
*   **推理效率**：单场景编辑仅需约 **5 秒**（相比之下，优化类方法需 300-600 秒，2D 提升类方法需 10-115 秒）。

### 5. 实验数量与充分性
*   **实验规模**：在 500 个经过人工验证的高质量测试案例上进行了定量评估。
*   **消融实验**：针对深度同步注意、视角权重、残差头分别做了消融，验证了各模块对几何稳定性和语义对齐的贡献。
*   **泛化性测试**：展示了模型对训练集中未出现的指令（如“顺时针旋转 90 度”）的零样本泛化能力。
*   **评价**：实验设计全面，对比了不同技术路径的代表性方法，定量与定性结果互补，论证较为客观公平。

### 6. 主要结论与发现
*   **原生 3D 编辑优于 2D 提升**：直接在 3D 空间预测位移能显著提升多视图一致性，C-FID 降至 122.4（行业领先）。
*   **残差学习是关键**：通过预测残差而非全量几何，模型能更好地保留背景细节，避免了全局结构的漂移。
*   **速度优势巨大**：实现了近乎瞬时的前馈编辑，比传统方法快 2 到 120 倍，具备实时交互潜力。

### 7. 优点
*   **架构创新**：深度同步注入和残差场预测的设计非常巧妙，解决了 3D 编辑中的“背景漂移”和“视角冲突”问题。
*   **数据贡献**：DeltaScene 数据集的发布填补了大规模、高质量 3D 编辑数据的空白。
*   **实用性强**：极高的推理速度使其非常适合机器人操作和空间计算等实时应用场景。

### 8. 不足与局限
*   **依赖骨干网络**：编辑质量受限于冻结的重建模型（$\pi^3$）的上限，若原始重建存在缺陷，编辑效果也会受损。
*   **掩码依赖**：虽然有自动化流水线，但在实际应用中可能仍需要精确的物体分割掩码来辅助残差预测。
*   **场景限制**：目前主要集中在室内静态场景，对于大规模室外环境或剧烈动态变化的场景处理能力尚待验证。

（完）
