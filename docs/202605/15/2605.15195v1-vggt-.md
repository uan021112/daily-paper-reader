---
title: VGGT-$Ω$
title_zh: VGGT-$Ω$
authors: "Jianyuan Wang, Minghao Chen, Shangzhan Zhang, Nikita Karaev, Johannes Schönberger, Patrick Labatut, Piotr Bojanowski, David Novotny, Andrea Vedaldi, Christian Rupprecht"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.15195v1"
tags: ["query:ff3dr"]
score: 10.0
evidence: 前馈重建模型在竞争中优于基于优化的方法
tldr: "本研究提出VGGT-Ω，旨在通过扩展模型和数据规模提升前馈重建模型的性能。针对前代模型计算开销大的问题，引入了寄存器注意力机制和简化的预测头，显著降低了显存占用约70%。通过在15倍规模的监督数据及海量无标签视频上进行自监督训练，该模型在静态和动态场景重建中均取得领先，并证明了重建任务作为空间理解代理任务的潜力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.15195v1/fig-001.webp\", \"caption\": \"Table 3. Performance on the LIBERO benchmark. We freeze our pretrained model, feed the scene tokens as additional input to OpenVLA-OFT, and report the success rate (SR) (higher is better). The clear gains validate the effectiveness of our scene tokens in aggregating spatial information.\", \"page\": 11, \"index\": 1, \"width\": 487, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2605.15195v1/fig-002.webp\", \"caption\": \"Figure 6. Qualitative Comparison to Depth Anything 3. Left: a snow lift sequence over a snow-covered field, where the repetitive terrain misleads DA3 into estimating little to no camera motion. Our method recovers the correct camera trajectory. Right: a drone sequence in which the camera rolls while flying toward a tower. Under this strong viewpoint change, DA3 produces severe ghosting and duplicated tower structures, whereas our reconstruction remains globally consistent.\", \"page\": 10, \"index\": 2, \"width\": 1034, \"height\": 285}, {\"url\": \"assets/figures/arxiv/2605.15195v1/fig-003.webp\", \"caption\": \"Figure 2. Architecture Overview. VGGT-Ω appends camera and scene tokens (registers) to image tokens, and then alternates between global attention (or register attention) and frame attention layers. We replace the redundant dense heads of VGGT with training-only losses.\", \"page\": 3, \"index\": 3, \"width\": 975, \"height\": 436}, {\"url\": \"assets/figures/arxiv/2605.15195v1/fig-004.webp\", \"caption\": \"Table 1. Camera Pose Estimation across static and dynamic benchmarks. The metric AUC is higher-is-better. Feed-forward models (DA3, PI3, VGGT) are robust across datasets, but still lag behind the dynamic optimization-based method MegaSaM at strict thresholds on Sintel (e.g., AUC@3◦ of 16.2 vs. 22.5). In contrast, dynamic optimization methods (MegaSaM, MonST3R) degrade on wide-baseline scenes (e.g., AUC@30◦ of 38.1 vs. 86.4 on ETH3D). Our method, however, substantially advances the state of the art across all scenarios, e.g., improving AUC@3◦ from 22.5 to 40.0 on Sintel, with a 77% relative improvement.\", \"page\": 8, \"index\": 4, \"width\": 1036, \"height\": 209}, {\"url\": \"assets/figures/arxiv/2605.15195v1/fig-005.webp\", \"caption\": \"Table 2. Depth Estimation across static and dynamic benchmarks. The metric δ1.25 denotes the percentage of predicted depths within a factor of the ground truth (higher is better), and AbsRel is the mean absolute relative error (lower is better).\", \"page\": 8, \"index\": 5, \"width\": 1036, \"height\": 244}, {\"url\": \"assets/figures/arxiv/2605.15195v1/fig-006.webp\", \"caption\": \"Figure 4. Qualitative Results. VGGT-Ω handles both static and dynamic content, as evidenced by the overlaid traffic flow and the tennis player’s trajectory. It also generalizes to hard scenes, e.g., underwater coral reefs. Each example uses 64, 4, 9, 16, and 32 input frames.\", \"page\": 5, \"index\": 6, \"width\": 1036, \"height\": 691}]"
motivation: 旨在验证前馈重建模型在扩大模型和数据规模时的可扩展性，并解决现有模型训练效率低下的问题。
method: 通过引入寄存器注意力机制限制帧间信息交换，并简化架构以移除高分辨率卷积层，从而大幅提升训练效率。
result: "在多个基准测试中表现优异，其中在Sintel数据集上的相机估计准确率比前代最优模型提升了77%。"
conclusion: 实验证明重建任务是实现空间理解的强大且可扩展的代理任务，且学习到的场景表示能有效增强视觉-语言-动作模型。
---

## 摘要
最近的前馈重建模型（如 VGGT）已被证明在与传统的基于优化的重建器竞争中具有优势，同时还能提供对其他任务有用的几何感知特征。在本文中，我们展示了这些模型的质量随模型和数据规模的可预测扩展性。我们通过引入 VGGT-$Ω$ 来实现这一点，它显著提高了静态和动态场景的重建精度、效率和能力。为了能够以前所未有的规模训练该模型，我们引入了提高训练效率的架构改进、支持动态场景的高质量数据标注流水线以及一种自监督学习协议。我们通过使用带有多任务监督的单个密集预测头并移除昂贵的高分辨率卷积层，简化了 VGGT 的架构。我们还使用寄存器（registers）将场景信息聚合为紧凑表示，并引入了寄存器注意力（register attention），将帧间信息交换限制在这些寄存器中，部分取代了全局注意力。通过这种方式，在训练期间，VGGT-$Ω$ 仅消耗其前代模型约 30% 的 GPU 显存，使我们能够使用比之前工作多 15 倍的有监督数据进行训练，并利用海量的无标注视频数据。VGGT-$Ω$ 在多个基准测试的静态和动态场景重建中均取得了优异结果，例如，在 Sintel 上的相机估计精度比之前的最佳结果提高了 77%。我们还展示了学习到的寄存器可以改进视觉-语言-动作模型并支持与语言的对齐，这表明重建可以作为空间理解的一种强大且可扩展的代理任务。项目主页：http://vggt-omega.github.io/

## Abstract
Recent feed-forward reconstruction models, such as VGGT, have proven competitive with traditional optimization-based reconstructors while also providing geometry-aware features useful for other tasks. Here, we show that the quality of these models scales predictably with model and data size. We do so by introducing VGGT-$Ω$, which substantially improves reconstruction accuracy, efficiency, and capabilities for both static and dynamic scenes. To enable training this model at an unprecedented scale, we introduce architectural changes that improve training efficiency, a high-quality data annotation pipeline that supports dynamic scenes, and a self-supervised learning protocol. We simplify VGGT's architecture by using a single dense prediction head with multi-task supervision and removing the expensive high-resolution convolutional layers. We also use registers to aggregate scene information into a compact representation and introduce register attention, which restricts inter-frame information exchange to these registers, in part replacing global attention. In this way, during training, VGGT-$Ω$ uses only about 30% of the GPU memory of its predecessor, allowing us to train with 15x more supervised data than prior work and to leverage vast amounts of unlabeled video data. VGGT-$Ω$ achieves strong results for reconstruction of static and dynamic scenes across multiple benchmarks, for example, improving over the previous best camera estimation accuracy on Sintel by 77%. We also show that the learned registers can improve vision-language-action models and support alignment with language, suggesting that reconstruction can be a powerful and scalable proxy task for spatial understanding. Project Page: http://vggt-omega.github.io/

---

## 论文详细总结（自动生成）

### VGGT-Ω：前馈 3D 重建模型的可扩展性研究总结

#### 1. 论文的核心问题与研究动机
*   **核心问题**：探讨前馈重建模型（Feed-forward reconstruction models）在扩大模型参数和训练数据规模时，其性能是否遵循类似于大语言模型的“规模法则”（Scaling Laws）。
*   **研究动机**：虽然 VGGT 等模型在重建任务上表现优异，但其训练效率低、显存占用高，限制了向更大规模扩展的可能性。此外，如何利用海量无标注视频和处理动态场景也是当前 3D 视觉领域的重大挑战。

#### 2. 论文提出的方法论
*   **核心思想**：通过架构简化与引入“寄存器（Registers）”机制，在降低计算开销的同时，利用大规模标注流水线和自监督协议提升模型性能。
*   **关键技术细节**：
    *   **寄存器注意力（Register Attention）**：在部分全局注意力层中，限制帧间信息交换仅在寄存器（Scene Tokens）之间进行，而非所有图像 Token，显著降低了计算复杂度（FLOPs 降低约 23%）。
    *   **显存优化架构**：移除昂贵的高分辨率卷积层，改用轻量级的 MLP 配合 Pixel Shuffle 操作进行深度预测；将多头预测简化为单头预测，但保留多任务 Loss 监督。
    *   **自监督协议**：引入教师-学生（Teacher-Student）动量更新机制，通过特征匹配和回归损失在 1800 万个无标注视频上进行预训练。
    *   **数据标注流水线**：利用 VLM 预过滤、动态物体掩码、COLMAP 验证及几何后过滤，构建了包含 400 万个高质量序列的训练集（规模为前作的 15 倍）。

#### 3. 实验设计
*   **数据集/场景**：
    *   **静态场景**：7 Scenes, NRGBD, ETH3D。
    *   **动态场景**：DyCheck, Sintel, TUM-Dynamic。
*   **Benchmark（基准）**：相机位姿估计（AUC@3°, AUC@30°）和深度估计（AbsRel, $\delta_{1.25}$）。
*   **对比方法**：前馈模型（Depth Anything 3, PI3, VGGT）以及基于优化的动态重建方法（MegaSaM, MonST3R）。

#### 4. 资源与算力
*   **硬件资源**：使用了 **128 块 96GB H100 GPU**。
*   **训练细节**：采用 bfloat16 混合精度、梯度检查点（Gradient Checkpointing）和 FSDP 技术。总计训练 240K 次迭代（包含监督与自监督阶段）。

#### 5. 实验数量与充分性
*   **实验规模**：论文评估了从 0.2B 到 10B 参数量的模型变体，以及从 2K 到 2M 序列的数据规模，验证了性能随规模的可预测增长。
*   **消融实验**：针对寄存器注意力、多任务学习、自监督协议、标注质量等核心组件进行了详尽的对比实验。
*   **充分性评价**：实验覆盖了从水下、航拍到室内外的多种极端场景，对比基准涵盖了当前最先进的各类模型，实验设计客观、公平且具有极强的说服力。

#### 6. 主要结论与发现
*   **规模效应**：重建精度随模型容量和数据规模的增加呈现出明显的幂律增长趋势。
*   **效率突破**：VGGT-Ω 在训练时仅消耗前代模型 30% 的显存，推理速度提升了 20-25%。
*   **语义涌现**：学习到的寄存器（Scene Tokens）不仅能辅助几何重建，还能作为通用的空间特征改进视觉-语言-动作（VLA）模型，并能实现与自然语言的零射对齐。
*   **性能领先**：在 Sintel 等动态基准上，相机估计精度比此前最优方法提升了 77%。

#### 7. 优点
*   **架构简洁高效**：通过寄存器机制巧妙解决了 Transformer 处理长视频序列时的二次方复杂度问题。
*   **动态场景鲁棒性**：无需显式的运动建模即可处理复杂的非刚性运动。
*   **通用表示能力**：证明了重建任务可以作为空间理解的强大代理任务，其特征具有跨模态迁移的潜力。

#### 8. 不足与局限
*   **极端环境限制**：在极度运动模糊、剧烈焦距变化或相机镜头高度扭曲的情况下，性能仍有提升空间。
*   **数据标注偏差**：尽管有严格过滤，但合成数据中的“虚假背景”或真实数据中的传感器噪声（如 ScanNet++）仍可能导致模型产生特定的预测伪影。
*   **算力门槛高**：10B 规模模型的训练对算力资源要求极高，限制了中小型研究团队的复现。

（完）
