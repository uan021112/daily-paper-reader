---
title: "SegviGen: Repurposing 3D Generative Model for Part Segmentation"
title_zh: SegviGen：将 3D 生成模型重新利用于部件分割
authors: "Lin Li, Haoran Feng, Zehuan Huang, Haohua Chen, Wenbo Nie, Shaohua Hou, Keqing Fan, Pan Hu, Sheng Wang, Buyu Li, Lu Sheng"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.16869v1"
tags: ["query:partseg"]
score: 10.0
evidence: 将3D生成模型重新用于3D部件分割
tldr: "SegviGen 提出了一种将预训练 3D 生成模型重新用于 3D 部件分割的新框架。针对现有方法依赖大量标注数据或 2D 提升导致的不一致性问题，该方法利用生成模型的结构化先验，通过预测几何对齐重建中的部件指示颜色来实现分割。实验证明，该方法在仅使用 0.32% 标注数据的情况下，显著提升了交互式和全分割性能，展示了 3D 生成先验在有限监督下的强大迁移能力。"
selection_source: fresh_fetch
motivation: 现有的 3D 分割方法要么受限于 2D-3D 提升带来的跨视图不一致性，要么需要海量的 3D 标注数据和训练资源。
method: SegviGen 利用预训练 3D 生成模型的结构化先验，通过在几何对齐的重建体素上预测部件指示颜色来诱导分割。
result: "在仅使用 0.32% 标注数据的情况下，该方法在交互式分割和全分割任务上分别比现有技术提升了 40% 和 15%。"
conclusion: 预训练的 3D 生成先验可以有效地迁移到 3D 部件分割任务中，在极低监督下实现卓越的性能。
---

## 摘要
我们介绍了 SegviGen，这是一个将原生 3D 生成模型重新利用于 3D 部件分割的框架。现有的流程要么通过蒸馏或多视图掩码聚合将强大的 2D 先验提升到 3D，这通常会受到跨视图不一致和边界模糊的影响；要么探索原生 3D 判别式分割，这通常需要大规模标注的 3D 数据和大量的训练资源。相比之下，SegviGen 利用预训练 3D 生成模型中编码的结构化先验，通过独特的部件着色来引导分割，建立了一个新颖且高效的部件分割框架。具体而言，SegviGen 对 3D 资产进行编码，并在几何对齐重建的激活体素上预测指示部件的颜色。它在一个统一的框架中支持交互式部件分割、全分割以及带有 2D 引导的全分割。大量实验表明，SegviGen 在交互式部件分割上比之前的最优方法提高了 40%，在全分割上提高了 15%，而仅使用了 0.32% 的标注训练数据。这表明预训练的 3D 生成先验可以有效地迁移到 3D 部件分割中，从而在有限的监督下实现强大的性能。参见我们的项目页面：https://fenghora.github.io/SegviGen-Page/。

## Abstract
We introduce SegviGen, a framework that repurposes native 3D generative models for 3D part segmentation. Existing pipelines either lift strong 2D priors into 3D via distillation or multi-view mask aggregation, often suffering from cross-view inconsistency and blurred boundaries, or explore native 3D discriminative segmentation, which typically requires large-scale annotated 3D data and substantial training resources. In contrast, SegviGen leverages the structured priors encoded in pretrained 3D generative model to induce segmentation through distinctive part colorization, establishing a novel and efficient framework for part segmentation. Specifically, SegviGen encodes a 3D asset and predicts part-indicative colors on active voxels of a geometry-aligned reconstruction. It supports interactive part segmentation, full segmentation, and full segmentation with 2D guidance in a unified framework. Extensive experiments show that SegviGen improves over the prior state of the art by 40% on interactive part segmentation and by 15% on full segmentation, while using only 0.32% of the labeled training data. It demonstrates that pretrained 3D generative priors transfer effectively to 3D part segmentation, enabling strong performance with limited supervision. See our project page at https://fenghora.github.io/SegviGen-Page/.

---

## 论文详细总结（自动生成）

以下是对论文《SegviGen: Repurposing 3D Generative Model for Part Segmentation》的结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：3D 部件分割是空间智能和内容创作的核心。现有方法存在两大瓶颈：
    1.  **2D-to-3D 提升法**：利用 2D 分割先验（如 SAM）投影至 3D，但常受限于跨视图不一致性和边界模糊。
    2.  **原生 3D 判别法**：直接在 3D 空间预测，但极度依赖大规模、高质量且标注一致的 3D 数据，训练成本高昂。
*   **核心思想**：论文提出 **SegviGen**，主张将预训练的 **3D 生成模型**（已内化丰富的几何与纹理结构先验）重新利用于分割任务。通过将分割建模为“部件着色”任务，在极少量标注数据下实现高精度分割。

### 2. 方法论
*   **核心框架**：基于结构化 3D 生成模型（如 Trellis.2），将分割任务转化为条件化的颜色预测问题。
*   **关键技术细节**：
    *   **任务重构**：不增加额外的分割通道，而是直接利用生成模型的颜色属性。
        *   *交互式分割*：给定点击点，目标部件涂白，其余涂黑。
        *   *全分割*：为不同部件分配随机调色板中的颜色。
        *   *2D 引导分割*：结合 2D 分割图作为额外条件。
    *   **模型架构**：使用预训练的 Sparse Compression VAE 将 3D 资产编码为潜在空间（Latent），并利用基于 DiT（Diffusion Transformer）的流匹配（Flow-Matching）生成器进行去噪。
    *   **条件注入**：通过任务嵌入（Task Embedding）区分不同模式，并利用交叉注意力机制注入用户点击点（Point Tokens）或 2D 引导图特征。
*   **流程**：输入 3D 几何 $\rightarrow$ VAE 编码 $\rightarrow$ 结合任务指令与提示 $\rightarrow$ 多任务流变换器预测部件颜色 $\rightarrow$ 获得分割结果。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：PartVerse（约 1.2 万个物体，9.1 万个部件标注）。
    *   **评估集**：PartObjaverse-Tiny（200 个物体）和 PartNeXT 子集（300 个物体）。
*   **Benchmark 与对比方法**：
    *   **交互式分割**：对比 Point-SAM、P3-SAM。
    *   **全分割**：对比 Find3D、SAMPart3D、PartField、P3-SAM。
*   **评价指标**：不同点击次数下的 IoU（IoU@N）以及全分割的总 IoU。

### 4. 资源与算力
*   **硬件**：使用了 **8 台 NVIDIA A800 GPU**。
*   **训练时长**：模型训练仅需 **8 小时**。
*   **推理效率**：默认采用 12 步推理，在精度与速度间取得平衡。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了交互式分割（1-10 次点击）、无引导全分割、有 2D 引导全分割等多种场景。
*   **消融实验**：针对点嵌入机制（显式坐标编码 vs. 标签语义嵌入）和采样步数（1 到 25 步）进行了详细对比。
*   **充分性评价**：实验设计较为全面，对比了当前最先进的（SOTA）原生 3D 和 2D 提升方法。特别强调了**数据效率**：仅使用 0.32% 的标注数据即实现了大幅领先，证明了其客观优越性。

### 6. 主要结论与发现
*   **性能飞跃**：在交互式分割上 IoU 提升了 **40%**，全分割提升了 **15%**。
*   **数据高效性**：证明了 3D 生成先验具有极强的迁移能力，能够弥补 3D 标注数据的匮乏。
*   **边界清晰度**：相比于 2D 提升方法，SegviGen 产生的部件边界更加锐利且符合几何逻辑。
*   **多功能性**：一个统一框架即可处理交互式、自动全分割及受控分割任务。

### 7. 优点
*   **创新视角**：首次系统性地将 3D 生成模型重新利用于部件分割，避开了判别式模型对海量数据的依赖。
*   **高保真度**：生成的分割结果具有极高的几何一致性和清晰的边界。
*   **实用性强**：支持 2D 引导模式，允许用户通过简单的 2D 图像控制 3D 分割的粒度，非常适合工业应用。
*   **训练高效**：相比从头训练大型分割模型，其微调成本极低。

### 8. 不足与局限
*   **模型依赖**：性能高度依赖于底层 3D 生成模型（如 Trellis.2）的质量，若生成模型无法处理某些拓扑，分割也会受限。
*   **颜色歧义**：全分割依赖随机调色板，虽然通过多组采样缓解，但在极端复杂场景下可能存在颜色分配不均的问题。
*   **评估规模**：虽然在两个基准上表现优异，但评估集（Tiny/Subset）规模相对较小，在大规模开放场景下的鲁棒性仍待进一步验证。
*   **推理延迟**：作为生成式模型，其推理速度（多步去噪）虽已优化，但仍慢于单次前向传播的纯判别式模型。

（完）
