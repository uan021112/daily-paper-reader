---
title: Masked BRep Autoencoder via Hierarchical Graph Transformer
title_zh: 基于分层图 Transformer 的掩码 BRep 自动编码器
authors: "Yifei Li, Kang Wu, Wenming Wu, Xiaoming Fu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14927v1"
tags: ["query:partseg"]
score: 9.0
evidence: 用于CAD模型分割和零件分类的自监督学习
tldr: 本研究提出了一种基于分层图Transformer的掩码BRep自编码器，旨在从无标注的CAD模型中学习通用的边界表示（BRep）特征。通过在大规模无标签数据集上进行自监督预训练，该模型利用掩码重建机制和融合全局与局部信息的架构，显著提升了在零件分类、分割及加工特征识别等下游任务中的表现，尤其在小样本场景下展现出极强的泛化能力。
selection_source: fresh_fetch
motivation: 针对CAD模型标注数据稀缺的问题，探索如何通过自监督学习从大规模无标注BRep数据中提取有效的几何与拓扑特征。
method: 提出一种结合掩码图自编码器与分层图Transformer的架构，通过跨尺度互注意力机制和图神经网络融合全局依赖与局部拓扑信息。
result: 在零件分类、建模分割和加工特征识别等多个任务上取得了优异性能，且在极少标注数据的情况下表现显著优于现有方法。
conclusion: 该模型证明了自监督预训练在BRep表示学习中的有效性，为CAD下游任务提供了一个具有强泛化能力的通用基础模型。
---

## 摘要
我们提出了一种新颖的自监督学习框架，该框架能自动从输入的计算机辅助设计（CAD）模型中学习表示，用于下游任务，包括零件分类、建模分割和加工特征识别。为了训练我们的网络，我们构建了一个大规模的、无标签的边界表示（BRep）模型数据集。我们算法的成功依赖于两个关键组件。第一个是掩码图自动编码器，它通过重建随机掩码的 BRep 几何形状和属性来进行表示学习，从而增强泛化能力。第二个是分层图 Transformer 架构，它通过跨尺度互注意力模块（用于建模远程几何依赖关系）和图神经网络模块（用于聚合局部拓扑信息）优雅地融合了全局和局部学习。在训练完自动编码器后，我们将其解码器替换为针对特定任务的网络，并使用少量有标签数据进行训练，以完成下游任务。我们在各种任务上进行了实验，即使在有标签数据较少的情况下也取得了高性能，证明了我们模型的实用性和泛化性。与其他方法相比，在相同训练数据量的情况下，我们的模型在下游任务上的表现明显更好，尤其是在训练数据非常有限的情况下。

## Abstract
We introduce a novel self-supervised learning framework that automatically learns representations from input computer-aided design (CAD) models for downstream tasks, including part classification, modeling segmentation, and machining feature recognition. To train our network, we construct a large-scale, unlabeled dataset of boundary representation (BRep) models. The success of our algorithm relies on two keycomponents. The first is a masked graph autoencoder that reconstructs randomly masked geometries and attributes of BReps for representation learning to enhance the generalization. The second is a hierarchical graph Transformer architecture that elegantly fuses global and local learning by a cross-scale mutual attention block to model long-range geometric dependencies and a graph neural network block to aggregate local topological information. After training the autoencoder, we replace its decoder with a task-specific network trained on a small amount of labeled data for downstream tasks. We conduct experiments on various tasks and achieve high performance, even with a small amount of labeled data, demonstrating the practicality and generalizability of our model. Compared to other methods, our model performs significantly better on downstream tasks with the same amount of training data, particularly when the training data is very limited.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **Masked BRep Autoencoder** 的自监督学习框架，旨在解决 CAD 模型（边界表示，BRep）在深度学习任务中面临的标注数据稀缺和结构复杂性挑战。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：CAD 模型（BRep 格式）是工业制造的核心，但高质量的标注数据集极难获取（涉及商业机密）。现有的深度学习方法要么依赖大量标注，要么在处理 BRep 复杂的“离散拓扑+连续几何”结构时，难以平衡全局长程依赖与局部细节特征。
*   **研究动机**：
    *   **数据稀缺性**：企业不愿公开带标注的 CAD 模型。
    *   **尺度差异大**：CAD 模型中既有巨大的平面，也有极小的圆角，单一尺度的特征提取效果差。
    *   **数据冗余**：密集的几何采样在平坦区域会产生大量冗余信息，干扰 Transformer 的注意力机制。

### 2. 方法论
该方法的核心是结合了**掩码自编码器（MAE）**逻辑与**分层图 Transformer** 架构。
*   **核心思想**：通过在大规模无标注 BRep 数据集上随机遮蔽（Masking）70% 的几何与属性信息，强制网络根据剩余的上下文重建缺失部分，从而学习通用的特征表示。
*   **关键技术细节**：
    1.  **几何属性邻接图（gAAG）**：将 BRep 转化为图结构，面为节点，边为图的边。
    2.  **双分辨率采样**：对每个面进行 $3 \times 3$（全局语义引导）和 $13 \times 13$（精细局部细节）两种分辨率的 UV 采样，以应对尺度变化。
    3.  **分层图 Transformer 编码器**：
        *   **跨尺度互注意力（CSMA）模块**：利用低分辨率特征作为“锚点”去查询高分辨率特征，捕捉长程几何依赖，同时避免被冗余细节淹没。
        *   **局部拓扑消息传递（MPNN）**：在全局注意力之后，通过图神经网络聚合邻域拓扑信息。
    4.  **两阶段解码器**：先通过图解码器重建图空间特征，再通过 BRep 解码器（利用 FoldingNet 分支）还原显式的 3D 坐标、法线和属性。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：整合了 MFInstSeg、MFCAD++、CADSynth、Fusion 360 Gallery 和 SolidLetter 的训练集，共计约 **19.8 万个无标注模型**。
    *   **下游任务**：加工特征识别（25类）、建模操作分割（8类）、零件分类（26类）。
*   **Benchmark 与对比方法**：
    *   对比了 **AAGNet、UVNet、BRepFormer、BRep-BERT、BRepMAE** 等最先进的 BRep 学习模型。
    *   特别设置了**极低监督比例**（如 0.1%、0.5%、1% 到 100%）来测试小样本学习能力。
*   **隔离协议**：严格执行数据隔离，确保测试集在预训练和微调阶段均不可见。

### 4. 资源与算力
*   **硬件**：单台服务器，配备 **1块 NVIDIA A800 GPU**。
*   **训练细节**：
    *   预训练：100 个 epoch，使用 AdamW 优化器，余弦退火学习率。
    *   微调：200 个 epoch。
    *   **推理效率**：下游任务推理延迟约为 10.42 ms/模型，吞吐量达 96.01 models/s。

### 5. 实验数量与充分性
*   **实验规模**：论文在 3 个不同的下游任务、5 个主流数据集上进行了详尽测试。
*   **消融实验**：针对双分辨率设计、掩码必要性、全局-局部顺序、冻结编码器效果、掩码比例等进行了 7 组以上的对比实验。
*   **充分性评价**：实验设计非常充分且客观。通过跨数据集泛化实验（如在不包含 SolidLetter 数据的预训练模型上测试 SolidLetter 分类）证明了模型的鲁棒性。

### 6. 主要结论与发现
*   **自监督优势显著**：在仅有 0.1% 标注数据时，该方法在加工特征识别上的准确率（88.75%）远超从头训练的 Transformer（33.43%）。
*   **分层架构的必要性**：双分辨率采样和 CSMA 模块能有效解耦宏观拓扑与微观几何，解决了标准 Transformer 在处理 CAD 数据时的过拟合和冗余问题。
*   **特征通用性**：即使冻结预训练编码器仅训练线性分类头，模型依然能取得极高的性能，说明预训练确实提取到了任务无关的本质几何特征。

### 7. 优点（亮点）
*   **小样本能力极强**：在标注数据极度匮乏的工业场景下具有极高的实用价值。
*   **架构创新**：CSMA 模块优雅地解决了 BRep 模型中面片大小悬殊导致的特征提取难题。
*   **两阶段解码设计**：通过中间图特征监督，使高比例掩码（70%）下的重建训练更加稳定。

### 8. 不足与局限
*   **计算开销**：双分辨率采样虽然提升了精度，但也增加了数据预处理的计算和存储负担。
*   **扩展性限制**：对于包含数千个零件的大型工业装配体，由于内存限制，目前的节点序列长度可能面临瓶颈。
*   **几何覆盖**：虽然支持 NURBS，但实验主要集中在常见的机械加工特征上，对于极其复杂的自由曲面表现尚待进一步验证。

（完）
