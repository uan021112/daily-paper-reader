---
title: In-Field 3D Wheat Head Instance Segmentation From TLS Point Clouds Using Deep Learning Without Manual Labels
title_zh: 基于深度学习且无需人工标注的 TLS 点云田间三维麦穗实例分割
authors: "Tomislav Medic, Liangliang Nan"
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14309v1"
tags: ["query:partseg"]
score: 9.0
evidence: LiDAR点云的3D实例分割
tldr: 本研究针对农田环境下小麦穗的3D实例分割难题，提出了一种无需人工标注的深度学习框架。该方法利用地面激光扫描（TLS）点云，通过3D-2D多视图投影结合Grounded SAM生成伪标签，随后训练3D全景分割网络。实验证明该方法在复杂场景下表现优异，优于基于高斯泼溅的现有方案，为植物表型分析提供了高效、低成本的自动化解决方案。
selection_source: fresh_fetch
motivation: 针对农田复杂场景下小麦穗3D点云人工标注极其困难且耗时的问题，探索无需手动标签的自动化分割方法。
method: 提出两阶段流程：首先利用多视图投影和Grounded SAM生成初始伪标签，随后以此训练监督式3D全景分割神经网络。
result: 实验结果显示该方法优于现有的Wheat3DGS方案，证明了TLS点云在无需人工标注的情况下实现高质量分割的可行性。
conclusion: 该研究展示了TLS结合深度学习在植物表型分析中的潜力，且具备良好的任务迁移能力和低成本应用前景。
---

## 摘要
激光扫描（LiDAR）点云的三维实例分割在许多遥感相关领域仍是一项挑战。成功的解决方案通常依赖于监督式深度学习和人工标注，因此主要关注那些可以通过视觉检查和点云人工标注进行清晰界定的对象。然而，对于场景更为复杂且拥挤的任务，如农业中的田间植物表型分析，此类方法往往并不可行。在本研究中，我们直接利用地面激光扫描（TLS）点云来解决田间麦穗实例分割任务。为了解决这一问题并规避对人工标注的需求，我们提出了一种新型的两阶段流水线。为了获取初始的三维实例建议，第一阶段采用了三维到二维的多视图投影、用于零样本二维以对象为中心分割的 Grounded SAM 流水线以及多视图标签融合。第二阶段将这些初始建议作为带噪声的伪标签，用于训练一个监督式三维全景式分割神经网络。我们的结果证明了所提方法的可行性，并显示出相对于 Wheat3DGS 的性能提升。Wheat3DGS 是近期一种基于多视图 RGB 图像和三维高斯泼溅（3D Gaussian Splatting）、无需人工三维标注的田间麦穗实例分割替代方案。这表明 TLS 是一种具有竞争力的传感替代方案。此外，结果表明所提流水线的两个阶段均能在无需人工标注的情况下提供可用的三维实例分割，这预示着该方法在其他类似的基于 TLS 的点云分割任务中具有良好的、低成本的可迁移性。

## Abstract
3D instance segmentation for laser scanning (LiDAR) point clouds remains a challenge in many remote sensing-related domains. Successful solutions typically rely on supervised deep learning and manual annotations, and consequently focus on objects that can be well delineated through visual inspection and manual labeling of point clouds. However, for tasks with more complex and cluttered scenes, such as in-field plant phenotyping in agriculture, such approaches are often infeasible. In this study, we tackle the task of in-field wheat head instance segmentation directly from terrestrial laser scanning (TLS) point clouds. To address the problem and circumvent the need for manual annotations, we propose a novel two-stage pipeline. To obtain the initial 3D instance proposals, the first stage uses 3D-to-2D multi-view projections, the Grounded SAM pipeline for zero-shot 2D object-centric segmentation, and multi-view label fusion. The second stage uses these initial proposals as noisy pseudo-labels to train a supervised 3D panoptic-style segmentation neural network. Our results demonstrate the feasibility of the proposed approach and show performance improvementsrelative to Wheat3DGS, a recent alternative solution for in-field wheat head instance segmentation without manual 3D annotations based on multi-view RGB images and 3D Gaussian Splatting, showcasing TLS as a competitive sensing alternative. Moreover, the results show that both stages of the proposed pipeline can deliver usable 3D instance segmentation without manual annotations, indicating promising, low-effort transferability to other comparable TLS-based point cloud segmentation tasks.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种针对田间小麦穗进行三维实例分割的创新方法，其核心亮点在于完全无需人工标注点云数据。以下是对该论文的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在复杂的田间环境下，如何高效、准确地对小麦穗进行三维实例分割。
*   **研究背景**：
    *   **标注困境**：三维点云的人工标注极其耗时且困难，尤其是在植被茂密、遮挡严重的农田中，人类几乎无法准确手动勾勒每一个麦穗。
    *   **现有技术局限**：现有的监督学习方法依赖大量标注；而基于图像（如3D Gaussian Splatting）的方法在几何精度上可能不如激光雷达（LiDAR）。
    *   **研究动机**：利用地面激光扫描（TLS）的高精度几何信息，结合二维视觉大模型的零样本（Zero-shot）能力，实现无需人工标签的自动化表型分析。

### 2. 方法论
论文提出了一个**两阶段（Two-stage）**的处理流程：

*   **第一阶段：基于多视图投影的零样本分割（Stage-I）**
    *   **3D到2D投影**：将TLS扫描的原始点云投影为球形强度图和距离图（类似全景图）。
    *   **2D实例分割**：利用 **Grounded SAM** 流水线（结合了 Grounding DINO 目标检测和 SAM2 分割模型），根据文本提示（如“wheat”）自动生成二维掩码。
    *   **多视图融合**：将二维掩码重新投影回三维空间，利用基于图的聚类技术（HCS算法）和有向包围盒（OBB）进行跨视图的实例关联与融合，生成初步的三维实例建议。
*   **第二阶段：知识蒸馏与三维网络训练（Stage-II）**
    *   **伪标签生成**：将第一阶段生成的初步结果作为“带噪声的伪标签”。
    *   **3D全景分割网络**：使用 Minkowski Engine 构建 U-Net 骨干网络，训练一个直接处理三维点云的神经网络。该网络包含语义分割头和两个实例聚类头（PointGroup风格和判别嵌入风格）。
    *   **核心思想**：通过神经网络学习麦穗的判别性三维特征，从而纠正第一阶段因遮挡或视角不足导致的漏检，提高鲁棒性。

### 3. 实验设计
*   **数据集与场景**：使用了一个公开的田间小麦数据集，包含7个样方（Plots），涵盖42个遗传品种以确保多样性。
*   **硬件设备**：使用 FARO Focus 3D S 120 激光扫描仪获取高分辨率点云。
*   **Benchmark（基准）**：对比了 **Wheat3DGS**（目前该领域的先进方案，基于多视图RGB图像和3D高斯泼溅）。
*   **评估指标**：由于无法制作密集真值，研究者手动标注了麦穗的中心点作为稀疏参考，计算 F1 分数、精确率（Precision）、召回率（Recall）和计数误差。

### 4. 资源与算力
*   **GPU 型号**：NVIDIA GeForce RTX 3090 Ti (24 GB VRAM)。
*   **训练时长**：第二阶段的网络训练耗时约 **12 小时**。
*   **软件框架**：基于 Minkowski Engine 和 PyTorch。

### 5. 实验数量与充分性
*   **实验规模**：将7个样方划分为训练集（5个）、验证集（1个）和测试集（1个）。
*   **对比实验**：分别评估了 Stage-I、Stage-II 以及两者融合（I+II）的效果，并与 Wheat3DGS 进行了横向对比。
*   **充分性评价**：实验设计较为客观，通过空间分割（Spatial Split）验证了模型的泛化能力。虽然样本样方数量有限，但考虑到麦穗实例总数达到数千个，且涵盖了42个品种，实验具有一定的代表性。

### 6. 主要结论与发现
*   **性能超越**：所提方法在 F1 分数上显著优于基于图像的 Wheat3DGS 方案（F1 从 0.22 提升至 0.69-0.72）。
*   **TLS 的优势**：证明了 TLS 点云在复杂农田场景下是比纯图像更具竞争力的传感手段。
*   **两阶段的必要性**：Stage-II 能够通过学习三维特征找回 Stage-I 因视角受限而丢失的实例；而将两个阶段的结果融合（I+II）能获得最高的召回率（0.76）。
*   **零样本可行性**：成功证明了可以利用二维视觉基础模型（Foundation Models）的知识来引导三维点云任务，无需任何手动三维标注。

### 7. 优点
*   **无需标注**：极大地降低了农业表型分析的门槛和成本。
*   **端到端流程**：从原始扫描到最终实例分割，流程完整且通用，易于迁移到其他作物。
*   **跨模态融合**：巧妙地将二维图像处理的成熟技术与三维点云的几何优势结合。

### 8. 不足与局限
*   **欠分割问题**：在麦穗极度密集的区域（5个以上麦穗簇拥），模型容易将其识别为一个实例（欠分割率约 7.5%-9.5%）。
*   **异常样本敏感**：对于形状奇特（如极度弯曲或水平生长）的麦穗，Stage-II 的泛化能力仍有提升空间。
*   **评估局限**：由于缺乏密集的逐点真值（Ground Truth），评估仅基于中心点匹配，无法精确衡量分割边缘的准确度。
*   **硬件成本**：TLS 设备昂贵，且在大型农田中的作业效率（Throughput）仍面临挑战。

（完）
