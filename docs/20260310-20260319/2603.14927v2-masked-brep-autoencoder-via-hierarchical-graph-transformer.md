---
title: Masked BRep Autoencoder via Hierarchical Graph Transformer
title_zh: 基于分层图 Transformer 的掩码 BRep 自动编码器
authors: "Yifei Li, Kang Wu, Wenming Wu, Xiao-Ming Fu"
date: 2026-03-16
pdf: "https://arxiv.org/pdf/2603.14927v2"
tags: ["query:partseg"]
score: 10.0
evidence: 用于 CAD 模型分割和部件分类的自监督学习
tldr: 本研究提出了一种基于分层图Transformer的掩码BRep自编码器，旨在通过自监督学习从大规模无标注CAD模型中提取特征。该框架结合了掩码图自编码器和融合全局与局部信息的层次化架构，能有效捕捉长程几何依赖和局部拓扑。实验证明，该模型在零件分类、分割和加工特征识别等任务中表现优异，尤其在标注数据稀缺的情况下具有显著的泛化能力。
selection_source: fresh_fetch
motivation: 针对CAD模型标注数据获取困难的问题，探索如何通过自监督学习从大规模无标注BRep数据中学习通用的几何表示。
method: 提出一种分层图Transformer架构，利用掩码自编码机制重建BRep的几何与属性，并通过跨尺度互注意力机制融合全局与局部特征。
result: 在零件分类、建模分割和加工特征识别等多个下游任务中取得了高性能，且在小样本学习场景下表现显著优于现有方法。
conclusion: 该模型通过自监督预训练有效提升了BRep表示学习的泛化性，为CAD下游任务提供了一个高效且实用的深度学习框架。
---

## 摘要
我们提出了一种新颖的自监督学习框架，该框架能自动从输入的计算机辅助设计 (CAD) 模型中学习表示，用于下游任务，包括零件分类、建模分割和加工特征识别。为了训练我们的网络，我们构建了一个大规模的、无标签的边界表示 (BRep) 模型数据集。我们算法的成功依赖于两个关键组件。第一个是掩码图自动编码器，它通过重建随机掩码的 BRep 几何形状和属性来进行表示学习，从而增强泛化能力。第二个是分层图 Transformer 架构，它通过跨尺度互注意力模块（用于建模长程几何依赖关系）和图神经网络模块（用于聚合局部拓扑信息）优雅地融合了全局和局部学习。在训练完自动编码器后，我们将其解码器替换为针对特定任务的网络，并使用少量有标签数据进行下游任务训练。我们在各种任务上进行了实验，即使在有标签数据较少的情况下也取得了高性能，证明了我们模型的实用性和泛化性。与其他方法相比，在相同训练数据量的情况下，我们的模型在下游任务上的表现显著更好，尤其是在训练数据非常有限的情况下。

## Abstract
We introduce a novel self-supervised learning framework that automatically learns representations from input computer-aided design (CAD) models for downstream tasks, including part classification, modeling segmentation, and machining feature recognition. To train our network, we construct a large-scale, unlabeled dataset of boundary representation (BRep) models. The success of our algorithm relies on two keycomponents. The first is a masked graph autoencoder that reconstructs randomly masked geometries and attributes of BReps for representation learning to enhance the generalization. The second is a hierarchical graph Transformer architecture that elegantly fuses global and local learning by a cross-scale mutual attention block to model long-range geometric dependencies and a graph neural network block to aggregate local topological information. After training the autoencoder, we replace its decoder with a task-specific network trained on a small amount of labeled data for downstream tasks. We conduct experiments on various tasks and achieve high performance, even with a small amount of labeled data, demonstrating the practicality and generalizability of our model. Compared to other methods, our model performs significantly better on downstream tasks with the same amount of training data, particularly when the training data is very limited.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Masked BRep Autoencoder** 的自监督学习框架，旨在解决 CAD 模型（边界表示，BRep）在深度学习任务中面临的标注数据稀缺和几何拓扑复杂性问题。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：CAD 模型（BRep 格式）是工业制造的核心，但高质量的标注数据集极难获取（涉及商业机密）。现有的深度学习方法要么依赖大量标注，要么在处理 BRep 时仅关注局部拓扑（如 GNN），忽略了长程几何依赖。
*   **研究动机**：现有的 Transformer 架构在处理 BRep 时，往往采用单一尺度的注意力机制，难以应对 CAD 模型中极端的尺度变化（如巨大的平面与微小的圆角）以及高密度采样带来的数据冗余。

### 2. 方法论
该方法的核心是结合了**掩码自编码器（MAE）**和**分层图 Transformer** 的自监督框架。
*   **核心思想**：通过在大规模无标注 BRep 数据集上随机掩码 70% 的几何和属性信息，强制网络根据剩余的上下文重建原始模型，从而学习通用的几何表示。
*   **关键技术细节**：
    *   **双分辨率几何提取**：对每个面进行 UV 参数化采样，提取 3×3（全局语义引导）和 13×13（精细局部细节）两种分辨率的网格特征。
    *   **分层图 Transformer 编码器**：
        *   **跨尺度互注意力（CSMA）模块**：利用低分辨率特征作为查询（Query），从高分辨率特征中提取关键细节，有效过滤冗余并捕捉长程依赖。
        *   **局部拓扑消息传递（MPNN）**：在全局注意力之后，通过图神经网络聚合邻域拓扑信息，确保几何连接的精确性。
    *   **两阶段解码器**：先通过图解码器重建中间特征，再通过 BRep 解码器（利用 FoldingNet 分支）还原显式的 3D 坐标、法线、修剪指示器及离散属性。

### 3. 实验设计
*   **数据集**：
    *   **预训练**：整合了 MFInstSeg、MFCAD++、CADSynth、Fusion 360 Gallery 和 SolidLetter 的训练集，共计 **198,113 个无标注模型**。
    *   **下游任务**：加工特征识别（25类）、建模操作分割（8类）、零件分类（26类）。
*   **对比方法**：包括全监督的 AAGNet、BRT、BRepFormer，以及自监督的 BRep-BERT、BRepMAE 等。
*   **Benchmark 协议**：严格的数据隔离，确保测试集在预训练和微调阶段均不可见。

### 4. 资源与算力
*   **硬件**：使用单台配备 **NVIDIA A800 GPU** 的服务器。
*   **训练时长**：预训练进行 100 个 epoch（Batch size 128），下游任务微调 200 个 epoch。
*   **模型规模**：预训练阶段参数量为 8.15M，FLOPs 为 20.98G；下游任务阶段参数量降至 6.09M。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个主流 CAD 数据集上进行了详尽测试。
*   **充分性**：
    *   涵盖了从 **0.1% 极低标注比例**到 100% 全监督比例的对比。
    *   进行了多维度的**消融实验**，验证了双分辨率设计、掩码策略、模块顺序（先全局后局部）以及特征冻结后的迁移能力。
    *   实验设计客观公平，通过跨数据集泛化实验（Ours*）证明了模型并非单纯记忆数据。

### 6. 主要结论与发现
*   **小样本性能卓越**：在仅有 0.1% 标注数据的情况下，该模型在 MFInstSeg 上的准确率达到 88.75%，远超从零训练的 Transformer（33.43%）。
*   **分层架构的优势**：相比扁平化的 Transformer，分层 CSMA 模块能更好地处理 CAD 模型的尺度差异，避免了 GNN 的过度平滑问题。
*   **输入级掩码更有效**：直接掩码原始几何输入比掩码潜在特征（Latent Features）更能迫使模型学习深层语义。

### 7. 优点与亮点
*   **创新的 CSMA 模块**：通过非对称的信息瓶颈设计，优雅地解决了 CAD 模型中大平面与小特征共存导致的计算冗余问题。
*   **两阶段解码策略**：引入中间图特征监督，稳定了复杂的 BRep 重建过程。
*   **极强的泛化性**：即使在完全未见过的几何领域（跨数据集测试），性能依然稳健。

### 8. 不足与局限
*   **计算开销**：双分辨率采样虽然提升了精度，但也增加了数据预处理的计算和存储负担。
*   **装配体限制**：目前主要针对单个零件模型，在处理包含成千上万个面的大规模工业装配体时，可能会遇到内存瓶颈和序列长度限制。
*   **应用范围**：尚未在 BRep 生成任务（如自动建模）中进行验证。

（完）
