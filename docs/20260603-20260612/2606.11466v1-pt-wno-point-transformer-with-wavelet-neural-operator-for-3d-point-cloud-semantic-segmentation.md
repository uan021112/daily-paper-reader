---
title: "PT-WNO: Point Transformer with Wavelet Neural Operator for 3D Point Cloud Semantic Segmentation"
title_zh: PT-WNO：结合小波神经算子的点变换器用于3D点云语义分割
authors: "Nhut Le, Maryam Rahnemoonfar"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.11466v1"
tags: ["query:partseg"]
score: 9.0
evidence: 结合全局上下文的点云语义分割
tldr: 点云语义分割需兼顾局部细节与全局场景理解。现有Transformer网络通过跳跃连接传递语境信息，但全局感知仍不足。为此，本文提出PT-WNO，在点云Transformer骨干网络的跳跃连接旁集成可学习的小波神经算子分支，将点特征投影到密集网格上进行多尺度全局频谱特征提取，并通过轻量适配器融合回网络。在S3DIS、DALES等数据集上超过基线，证明了全局特征增强的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.11466v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1824, \"height\": 924, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11466v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1833, \"height\": 357, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11466v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1695, \"height\": 1242, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11466v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1679, \"height\": 1231, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.11466v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 828, \"height\": 474, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11466v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1846, \"height\": 207, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11466v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1845, \"height\": 339, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11466v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1841, \"height\": 200, \"label\": \"Table\"}]"
motivation: Transformer网络依赖跳跃连接传递全局语境，但缺乏显式的全局特征提取模块，限制场景级理解。
method: 在点云Transformer的编码-解码跳跃连接旁，插入共享的小波神经算子分支，对网格化点特征进行可学习的多尺度频谱分解与重建，并通过适配器融合。
result: "在S3DIS Area 5上mIoU达71.59%（+1.03），DALES上达81.05%（+1.47），ScanNet v2上达76.19%。"
conclusion: PT-WNO通过可学习全局特征模块有效增强点云分割的场景理解，简单集成即可提升性能。
---

## 摘要
点云语义分割需要既能捕捉细粒度局部几何结构又能理解广阔全局场景结构的架构。基于变换器的网络通过专注于详细的局部特征聚合展现了强大的性能；然而，全局上下文主要通过编码器-解码器阶段的跳跃连接传递，我们认为这对于完整的场景理解是不够的。我们假设，通过可学习的全局特征提取模块增强跳跃连接，可以使网络在深入到局部细节之前获取场景级知识，从而产生更丰富且更具上下文基础的表示。为此，我们提出了点变换器与小波神经算子（PT-WNO），它在点云变换器骨干网络的跳跃连接旁集成了一个共享的小波神经网络算子（WNO）分支。在每个编码器-解码器过渡处，点特征被投影到密集的3D体积网格上，WNO通过可学习的小波分解与重构捕获多尺度全局谱上下文。这些全局特征通过轻量级适配器融合回网络，补充而非替代现有的跳跃连接。在四个大规模3D点云基准上的实验证明了PT-WNO的有效性。在S3DIS（Area 5）上，PT-WNO达到了71.59%的mIoU，比Point Transformer v3（PTv3）基线高出+1.03个百分点。在DALES上，它达到了81.05%的mIoU（比基线高+1.47）。在ScanNet v2上，PT-WNO获得了76.19%的mIoU，与基线（76.36%）保持竞争力。

## Abstract
Point cloud semantic segmentation requires architectures that capture both fine-grained local geometry and broad global scene structure. Transformer-based networks have demonstrated strong performance by focusing on detailed local feature aggregation; however, global context is conveyed primarily through skip connections across encoder-decoder stages, which we argue is insufficient for full scene understanding. We hypothesize that augmenting skip connections with a learnable global feature extraction module allows the network to acquire scene-level knowledge before descending into local detail, leading to richer and more contextually grounded representations. To this end, we propose Point Transformer with Wavelet Neural Operato (PT-WNO), which integrates a shared Wavelet Neural Operator (WNO) branch alongside the skip connections of a point cloud transformer backbone. At each encoder-decoder transition, point features are projected onto a dense 3D volumetric grid where the WNO captures multi-scale global spectral context through learnable wavelet decomposition and reconstruction. These global features are fused back into the network via lightweight adapters, complementing rather than replacing the existing skip connections. Experiments on four large-scale 3D point cloud benchmarks demonstrate the effectiveness of PT-WNO. On S3DIS (Area 5), PT-WNO achieves 71.59% mIoU, outperforming the Point Transformer v3 (PTv3) baseline by +1.03 points. On DALES it achieves 81.05% mIoU (+1.47 over the baseline). On ScanNet~v2, PT-WNO obtains 76.19% mIoU, remaining competitive with the baseline (76.36%).

---

## 论文详细总结（自动生成）

# 论文总结：PT-WNO: Point Transformer with Wavelet Neural Operator for 3D Point Cloud Semantic Segmentation

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：现有基于Transformer的点云语义分割网络（如PTv3）虽然通过序列化空间填充曲线和窗口注意力实现了高效的局部特征聚合，但全局上下文主要依赖编码器-解码器之间的跳跃连接来隐式传递。这种设计缺乏显式的场景级全局建模机制，导致在处理大尺度室内外场景（如房间布局、走廊关系、大型物体间远距离依赖）时能力不足。
- **整体含义**：作者认为，仅靠堆叠局部注意力和层级池化无法充分捕获全场景结构，需要引入一个可学习的全局特征提取模块，在局部细化之前先获取场景级知识，从而产生更丰富、上下文更清晰的表示。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：在串行化点云Transformer骨干网络的跳跃连接旁，集成一个共享的**小波神经算子（WNO）分支**。该分支将中间层点特征投影到规则3D网格上，通过可学习的小波分解与重构进行多尺度全局谱上下文聚合，然后将增强后的特征通过轻量适配器融合回主干网络，**补充而非替代**原有的跳跃连接。
- **关键技术细节**：
    1. **网格投影**：在每个编码器阶段，点特征通过MLP投影到共享通道空间（维度D），然后根据归一化坐标映射到固定分辨率（64³）的3D网格，落入同一体素的特征通过均值池化聚合，空缺体素用零填充。
    2. **相对位置编码**：在体素化之前，为每个点计算相对于该阶段中心点的归一化位移和半径，并通过小MLP生成位置编码，与点特征相加，以保留几何关系。
    3. **小波神经算子（WNO）**：核心操作是将网格张量视为连续函数的离散化，通过**可学习的深度可分步长卷积（分析）和转置卷积（合成）** 替代传统固定小波滤波器组，实现分级多尺度分解与重建。公式为：
        \[
        \mathbf{V}^{(s)} = \mathcal{K}_\theta(\tilde{\mathbf{U}}^{(s)}) = \text{IDWT}(R_\theta \odot \text{DWT}(\tilde{\mathbf{U}}^{(s)})) + B(\tilde{\mathbf{U}}^{(s)})
        \]
        其中DWT/IDWT为可学习操作，\(R_\theta\)为1×1×1卷积进行子带系数混合，\(B\)为局部跳跃连接以保留高频细节。
    4. **网格到点提升**：算子输出后，每个点根据其体素索引查找对应全局特征，通过MLP投影回骨干通道维度，再经最大池化或广播适应不同分辨率。
    5. **多阶段融合**：全局特征与局部特征通过拼接+线性投影+LayerNorm+GELU融合，替换原特征，并通过U-Net跳跃连接传播到解码器。
    6. **损失函数**：交叉熵损失 + Lovász-Softmax损失，端到端训练。

## 3. 实验设计

- **数据集与场景**：
    - **S3DIS (Area 5)**：13类室内场景，体素分辨率0.02m，评价mIoU、mAcc、OA。
    - **ScanNet v2 (validation)**：20类密集网格重建室内场景。
    - **DALES**：8类室外航空LiDAR数据集，覆盖10km²，体素分辨率0.05m，移除颜色增强以测试纯几何推理。
- **基准（Benchmark）**：标准3D语义分割性能评价，主要报告mIoU。
- **对比方法**：
    - 引用已有公开结果：PointNet++、SPG、ConvPoint、KPConv、MinkUNet、SPT、PTv3等。
    - **核心对比**：复现的PTv3基线（与PT-WNO在相同硬件、超参数、随机种子下从零训练），确保差异仅来自WNO分支。
    - 消融实验：移除RGB颜色特征后的表现。

## 4. 资源与算力

- **文中未明确说明**所使用的GPU型号、数量或训练时长。仅提到优化器为AdamW（学习率6e-3，权重衰减0.05），学习率调度为一周期余弦，训练3000个epoch。WNO分支网格分辨率为64³，通道数D=64。未提供推理时间或FLOPs对比。

## 5. 实验数量与充分性

- **实验数量**：
    - 主实验：在3个数据集（S3DIS、ScanNet v2、DALES）上进行性能对比。
    - 消融实验：1组（S3DIS上移除颜色特征）。
    - 定性结果：S3DIS和DALES的可视化展示（2组示例）。
- **充分性评价**：
    - **优点**：严格控制了基线复现条件，保证了比较的公平性；覆盖了室内（S3DIS、ScanNet）、室外航空（DALES）多种场景，验证了泛化性；消融实验证明了纯几何推理的有效性。
    - **不足**：消融实验仅涉及颜色移除一个变量，缺少对网格分辨率、WNO层数、融合位置、算子规模等的系统探索；未在更多数据集（如SemanticKITTI、Waymo）上验证；缺少与现有全局建模方法（如非局部模块、状态空间模型）的直接对比。

## 6. 主要结论与发现

- **PT-WNO在S3DIS Area 5上mIoU达71.59%**，比复现的PTv3基线（70.56%）提升+1.03个百分点；在DALES上mIoU达81.05%，提升+1.47个百分点；在ScanNet v2上mIoU为76.19%，略低于基线（76.36%，下降0.17），但仍在运行方差范围内。
- **收益来源**：WNO分支对几何结构明显、边界尖锐的类别（如柱子、门、沙发）提升显著；对依赖密集纹理的扫描场景（ScanNet）帮助有限甚至略有下降。
- **颜色移除实验**：去除颜色后S3DIS mIoU为70.04%，与完整颜色基线（70.56%）非常接近，证明WNO分支主要学习纯几何线索，对颜色不敏感，适用于无RGB的LiDAR数据。

## 7. 优点

- **创新性**：首次将小波神经算子（WNO）系统集成到点云Transformer中，作为显式全局特征提取模块，而非隐式依赖堆叠注意力。
- **方法设计合理**：采用可学习分解替代固定小波，适应3D点云特征；通过残差融合保持高频细节；共享算子分支参数增量小（+3.5%参数量）。
- **实验严谨**：严格执行基线复现协议，控制所有变量，使性能增益可归因于提出的模块；在多个数据集上验证，覆盖不同场景类型和传感器模态。
- **应用价值**：为大规模户外LiDAR场景（如城市级数字孪生、自动驾驶）提供了一种有效的全局上下文增强方案，且对颜色信息不敏感。

## 8. 不足与局限

- **性能不一致**：在ScanNet v2上未获得提升甚至略降，表明该方法可能不适用于所有场景类型（如密集、纹理丰富的室内扫描），缺乏更细致的适配或条件选择。
- **消融不完整**：仅做了颜色移除一组消融，缺少对网格分辨率、WNO层数、融合位置、算子通道数等超参数的系统分析，无法全面理解各组件贡献。
- **计算资源未公开**：未报告训练时间、推理速度、GPU型号/数量等，难以评估实际部署成本。
- **对比方法局限**：仅与PTv3基线对比，未与其他的全局上下文模型（如非局部网络、Mamba/SSM变体）直接比较，说服力有待加强。
- **缺乏理论分析**：为何小波域比傅里叶域更适合点云？为何可学习变换优于固定小波？这些问题未进行理论或可视化解释。
- **潜在偏差**：所有实验在特定超参数下进行，未讨论对超参数的敏感性；基线复现可能与官方最佳结果存在差距（PTv3原论文S3DIS Area 5 mIoU为74.70%，而本文化复现为70.56%）。

（完）
