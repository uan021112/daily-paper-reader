---
title: "When Wider Views Fail: Stress-Testing Feed-Forward 3D Reconstruction"
title_zh: 当更宽的视角失效：对前馈三维重建的压力测试
authors: "Daisy Li, Kyle Gao, Quanyun Wu, Hanna Chomko, John S. Zelek, Jonathan Li"
date: 2026-09-21
pdf: "https://arxiv.org/pdf/2609.24839v1"
tags: ["query:feed-forward"]
score: 9.0
evidence: 在视角分布偏移下压力测试前馈3D重建模型
tldr: 前馈式3D重建模型可从稀疏图像高效估计几何，但预训练特性使其面对训练分布之外的输入时可能失效。本文把视角变化视为可控分布偏移，在固定输入图像数量下改变视角角跨度，对多个前馈重建模型进行压力测试。结果显示，视角跨度增大时性能显著退化，宽跨度还会导致表面覆盖不完整并生成观测图像不支持的几何。该工作揭示了超越传统重建不完整的失效模式，强调需在挑战几何先验的分布偏移下评估此类模型。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.24839v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1791, \"height\": 612, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.24839v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1794, \"height\": 597, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.24839v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 892, \"height\": 968, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.24839v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 893, \"height\": 969, \"label\": \"Table\"}]"
motivation: 前馈3D重建模型依赖预训练先验，在稀疏视图下高效，但面对训练分布外输入时可靠性不明，需识别其失效模式。
method: 将视角变化作为受控分布偏移，在固定输入图像预算下改变稀疏图像的视角角跨度，对多个前馈重建模型做压力测试。
result: 随视角跨度增大，多个模型性能显著下降；宽跨度不仅造成表面覆盖不完整，还生成观测图像不支持的几何。
conclusion: 视角变化会引发超出传统重建不完整的失效模式，说明应在挑战预训练几何先验的分布偏移下评估前馈重建模型。
---

## 摘要
前馈三维重建模型能够从稀疏图像中高效地估计几何结构，但其预训练特性使其容易受到训练数据之外的分布偏移的影响。识别这些失效模式，对于理解此类模型何时能够在无约束成像场景中可靠部署至关重要。我们将视角变化作为一种可控的分布偏移加以研究：在保持输入预算固定的前提下，改变稀疏图像输入的角度跨度。在多个前馈重建模型中，我们观察到随着视角跨度增大，性能出现显著退化；大跨度既会导致表面覆盖不完整，也会产生不被观测图像支持的几何结构。这些结果表明，视角变化会引发超出传统重建不完整性之外的失效模式，凸显了在挑战其已学几何先验的分布偏移下评估预训练前馈模型的必要性。

## Abstract
Feed-forward 3D reconstruction models enable efficient geometry estimation from sparse images, but their pretrained nature can make them vulnerable to distribution shifts beyond their training data. Identifying these failure modes is important for understanding when such models can be reliably deployed in unconstrained imaging settings. We investigate viewpoint variation as a controlled distribution shift by varying the angular span of sparse image inputs while keeping the input budget fixed. Across multiple feed-forward reconstruction models, we observe substantial degradation as viewpoint span increases, with wide spans producing both incomplete surface coverage and geometry unsupported by the observed imagery. These results reveal that viewpoint variation can induce failure modes beyond conventional reconstruction incompleteness, highlighting the need to evaluate pretrained feed-forward models under distribution shifts that challenge their learned geometric priors.

---

## 论文详细总结（自动生成）

> 说明：以下总结基于所提供的论文元数据与摘要。由于提取文本未包含正文、实验表格与图示内容，涉及数据集、模型清单、算力与具体实验组数等信息只能标注为“未明确说明”，不做推测性补全。

## 1. 核心问题与整体含义
- **研究背景**：前馈式三维重建模型能够从稀疏图像中高效估计几何结构，在效率上具有优势。
- **核心问题**：这类模型通常依赖预训练先验，因此面对训练分布之外的输入时可能脆弱；识别其失效模式，是判断其能否在无约束成像场景中可靠部署的关键。
- **研究切入**：论文将“视角变化”视为一种可控的分布偏移，在固定输入图像预算的前提下，改变稀疏图像输入的角度跨度。
- **整体含义**：宽视角跨度并不必然带来更稳健的重建，反而可能诱发超出传统“重建不完整”的失效模式，例如生成不被观测图像支持的几何结构。这提示评估前馈三维重建模型时，需要主动挑战其已学到的几何先验。

## 2. 方法论
- **核心思想**：
  - 不改变输入图像数量或预算，只改变稀疏视角之间的角跨度。
  - 通过这种控制变量方式，将视角分布变化构造为可分析的分布偏移。
  - 对多个前馈三维重建模型进行压力测试，观察其性能与失效形式。
- **关键技术细节**：
  - 摘要中明确提到“保持输入预算固定，改变稀疏图像输入的角度跨度”。
  - 评估关注两类现象：
    1. 表面覆盖是否完整；
    2. 生成的几何是否被观测图像支持。
  - 具体模型名称、网络结构、损失函数、公式或算法流程未在提供文本中给出。
- **可概括流程**：
  - 选择多个前馈三维重建模型；
  - 固定每例输入图像数量；
  - 设置不同视角角跨度；
  - 执行重建；
  - 比较性能退化、表面覆盖完整性和几何支持性。

## 3. 实验设计
- **数据集 / 场景**：未明确说明。提供文本未列出使用的数据集、场景类型或采集条件。
- **Benchmark**：未明确说明。摘要未给出标准 benchmark 名称或评估协议细节。
- **对比方法**：
  - 论文对“多个前馈重建模型”进行压力测试。
  - 但具体对比了哪些模型、是否包含非前馈方法或优化式方法，未在提供文本中说明。
- **实验变量**：
  - 自变量：稀疏图像输入的视角角跨度。
  - 控制变量：输入图像预算固定。
- **评估指标**：
  - 性能退化程度；
  - 表面覆盖完整性；
  - 生成几何是否被观测图像支持。
  - 具体数值指标、误差定义与统计方式未提供。
- **图表信息**：元数据表明论文包含 2 张图和 2 张表，但图表内容未给出，无法进一步分析。

## 4. 资源与算力
- 提供文本未提及 GPU 型号、数量、训练时长、推理开销或显存需求。
- 无法判断实验是以训练为主还是以预训练模型推理评估为主。
- 因此，算力资源与计算成本在本总结中无法总结，属于信息缺失项。

## 5. 实验数量与充分性
- **实验数量**：
  - 从提供文本无法确定具体实验组数。
  - 可确认的是：涉及多个前馈重建模型，并在不同视角角跨度下进行比较。
  - 元数据仅显示 2 图 2 表，但无法据此推断完整实验规模。
- **充分性判断**：
  - 优点：固定输入预算、改变视角跨度，属于较清晰的控制变量设计，有利于隔离视角分布偏移的影响。
  - 局限：缺少数据集数量、模型数量、跨度设置、统计显著性和误差分析等信息，难以全面判断实验充分性。
- **客观性与公平性**：
  - 若所有模型在相同输入预算和相同评估协议下比较，则内部公平性较好。
  - 但提供文本未说明模型选择标准、数据划分、评估细节和是否存在调参差异，因此公平性无法完全确认。

## 6. 主要结论与发现
- 随着视角跨度增大，多个前馈三维重建模型出现显著性能退化。
- 宽视角跨度不仅导致表面覆盖不完整，还会产生“不被观测图像支持”的几何结构。
- 这说明视角变化会引发超出传统重建不完整性之外的失效模式，可能包括几何幻觉或先验驱动的错误补全。
- 论文强调：应在挑战预训练几何先验的分布偏移下，评估前馈三维重建模型，而不能仅依赖常规稀疏视图设定。

## 7. 优点
- **问题重要**：聚焦前馈三维重建在真实无约束场景中的可靠性，具有部署意义。
- **控制变量清晰**：固定输入图像预算，仅改变视角角跨度，便于分析视角分布偏移的独立影响。
- **失效模式有新意**：不仅关注重建不完整，还指出观测不支持的几何生成，揭示更危险的失败形式。
- **评估视角有启发**：提示社区应把分布偏移纳入前馈重建模型的压力测试与基准设计。

## 8. 不足与局限
- **正文信息缺失导致复现困难**：提供文本未给出具体模型、数据集、指标、公式和训练/推理配置。
- **实验覆盖不明**：不清楚是否覆盖室内、室外、对象中心、大场景、合成与真实数据等多种条件。
- **对比范围有限**：仅说明测试多个前馈重建模型，未说明是否与优化式、多视图立体或其他鲁棒方法比较。
- **统计与偏差风险**：未提供显著性检验、误差棒、失败案例筛选标准，结论稳健性无法完全评估。
- **定义不够明确**：何谓“宽跨度”、跨度如何分级、输入预算具体是多少，提供文本未说明。
- **应用限制**：结果提示在无约束成像中，使用宽基线或大视角跨度稀疏输入时需谨慎；预训练几何先验可能不可靠，甚至产生看似合理但无观测支持的几何。

（完）
