---
title: "SURE-Map: Self-Correcting Streaming Geometric Foundation Model"
title_zh: SURE-Map：自校正流式几何基础模型
authors: "Mingkai Liu, Hao Zhao, Xingxing Zuo"
date: 2026-09-14
pdf: "https://arxiv.org/pdf/2609.15795v1"
tags: ["query:feed-forward"]
score: 8.0
evidence: 面向重建的流式几何基础模型与自校正机制
tldr: 流式几何基础模型替代SLAM，但每帧预测上下文有限，易受动态物体和弱纹理影响，局部误差累积导致几何畸变与长程尺度漂移。SURE-Map提出自校正框架：显式建模跨视图几何不确定性，判断联合位姿与深度是否产生一致像素对应；并引入多时间尺度自校正，快帧推理保效率，稀疏关键帧窗口定期重标定尺度。在KITTI等长程基准上降低ATE-RMSE，结合回环进一步优化。为在线前馈重建树立新SOTA。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.15795v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1883, \"height\": 995, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.15795v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1730, \"height\": 1113, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.15795v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 805, \"height\": 1102, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.15795v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 882, \"height\": 919, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.15795v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 847, \"height\": 659, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.15795v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 813, \"height\": 331, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15795v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1841, \"height\": 947, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15795v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 801, \"height\": 356, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15795v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 902, \"height\": 267, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.15795v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 832, \"height\": 197, \"label\": \"Table\"}]"
motivation: 流式预测上下文有限，动态物体与弱纹理易致局部误差累积，引发几何畸变和长程尺度漂移，需要模型不仅预测还能自校正。
method: 显式建模跨视图几何不确定性，度量联合位姿与深度是否产生一致像素对应；多时间尺度自校正：连续帧快推理，关键帧窗口定期重标定尺度。
result: 在KITTI、Oxford Spires和VBR长程基准上，在线前馈重建的ATE-RMSE分别从24.00、5.11、31.37 m降至17.24、4.74、28.58 m，结合回环细化后进一步降至15.17、4.63、22.12 m。
conclusion: SURE-Map通过自校正机制显著缓解长程漂移，为在线前馈几何重建提供新SOTA，并可与回环细化协同。
---

## 摘要
流式几何基础模型正逐渐成为 SLAM 系统的一种极具吸引力的替代方案。然而，这种流式特性带来了一个根本问题：每次预测都基于有限的上下文，因而容易受到动态物体和弱纹理的影响。微小的局部误差会累积成严重的几何畸变和长时程尺度漂移。我们认为，可靠的流式重建要求几何基础模型不仅具备预测能力，还应具备自校正能力。我们提出 SURE-Map，一个基于两项互补原则构建的自校正框架。首先，我们显式建模跨视角几何不确定性。与传统深度或点置信度主要反映单视角预测可靠性不同，我们的不确定性直接衡量联合预测的位姿与深度是否产生几何一致的跨视角像素对应关系。其次，由于仅靠局部校正无法消除缓慢累积的尺度误差，我们引入多时间尺度自校正：快速的连续帧推理保持流式效率，而稀疏的关键帧窗口推理提供更长程的几何证据，以周期性地重新校准近期轨迹的尺度。SURE-Map 在长时程基准上的在线前馈重建中确立了新的最先进性能，将 KITTI 上的 ATE-RMSE 从 24.00 降至 17.24 m，Oxford Spires 上从 5.11 降至 4.74 m，VBR 上从 31.37 降至 28.58 m；当结合回环闭合优化后，进一步改善至 15.17、4.63 和 22.12 m。项目页面：https://mingkai-liu.github.io/projects/sure-map/。

## Abstract
Streaming geometric foundation models are emerging as a compelling alternative to SLAM systems. Yet this streaming nature introduces a fundamental issue: each prediction is made from limited context, which is vulnerable to dynamic objects and weak textures. Small local errors accumulate into severe geometric distortion and long-horizon scale drift. We argue that reliable streaming reconstruction requires geometric foundation models to be not only predictive, but also self-correcting. We introduce SURE-Map, a self-correcting framework built upon two complementary principles. First, we explicitly model cross-view geometric uncertainty. Unlike conventional depth or point confidence, which primarily reflects the reliability of individual-view prediction, our uncertainty directly measures whether the jointly predicted pose and depth induce geometrically consistent cross-view pixel correspondences. Second, because local correction alone cannot eliminate slowly accumulating scale errors, we introduce multi-timescale self-correction: fast consecutive-frame inference preserves streaming efficiency, while sparse keyframe-window inference provides longer-range geometric evidence to periodically recalibrate the scale of recent trajectories. SURE-Map establishes new state-of-the-art performance for online feed-forward reconstruction across long-horizon benchmarks, reducing ATE-RMSE from 24.00 to 17.24 m on KITTI, 5.11 to 4.74 m on Oxford Spires, and 31.37 to 28.58 m on VBR, with further improvements to 15.17, 4.63, and 22.12 m when incorporating loop-closure refinement. Project page: https://mingkai-liu.github.io/projects/sure-map/.

---

## 论文详细总结（自动生成）

# SURE-Map 论文中文总结

## 1. 核心问题与整体含义

- **研究背景**：前馈式几何基础模型（如 DUSt3R、VGGT 等）通过直接回归相机位姿与稠密几何，绕过传统 SLAM 中显式特征匹配、三角化与后端优化，成为 3D 重建的新范式。将此类模型扩展到**流式/在线重建**是自然趋势。
- **核心矛盾**：流式推理必须因果、低延迟、有界内存，每次预测只能依赖有限的近期上下文；而离线模型可以访问完整序列或未来帧，因而具备全局约束能力。
- **问题链条**：
  - 有限上下文 → 对动态物体、弱纹理、几何退化、重复结构等模糊证据脆弱；
  - 局部模糊 → 隐式数据关联出错，产生局部位姿误差与稠密点云噪声；
  - 局部误差累积 → 长时程出现严重的几何畸变与**段间尺度漂移**（scale drift）。
- **核心论点**：可靠的流式重建要求几何基础模型不仅"会预测"，还要"能自校正"。
- **整体含义**：SURE-Map 提出一个自校正框架，在不牺牲流式效率的前提下，同时提升稠密几何质量与长时程轨迹一致性，为在线前馈重建树立新 SOTA。

## 2. 方法论

### 2.1 核心思想
SURE-Map 基于两个互补原则：
1. **跨视图几何不确定性**：不评估单视角深度是否可靠，而是评估"联合预测的位姿与深度是否诱导出几何一致的跨视图像素对应"。
2. **多时间尺度自校正**：用快速连续帧推理保持效率，用稀疏关键帧窗口的全注意力推理提供长程几何证据，周期性重标定近期轨迹尺度。

### 2.2 关键技术细节

**（a）跨视图几何不确定性建模**
- 给定相邻帧 $(I_{i-1}, I_i)$ 的预测位姿与深度，解析地推导后向光流 $f_{i\to i-1}(u)$（式 2）：将当前帧像素反投影为 3D 点，变换到上一帧坐标系，再投影回图像平面。
- 在流式骨干上增加不确定性头 $h_\psi$，输入为两帧几何 token $(G_{i-1}, G_i)$，输出水平/垂直对数方差图 $S_i=[S_i^u;S_i^v]$，对应像素级对角协方差 $\Sigma_i(u)=\mathrm{diag}(e^{S_i^u}, e^{S_i^v})$。
- 用位姿-深度诱导光流与 GT 光流的残差 $\epsilon_i$ 做 NLL 监督（式 4）：Mahalanobis 项让大残差对应高不确定性，log-determinant 项防止平凡地输出大不确定性。

**（b）不确定性的两个应用**
- **稠密点过滤**：将协方差转为标量 $\sigma_i^{geo}(u)=\sqrt{\mathrm{tr}(\Sigma_i(u))}$，仅反投影低不确定性且深度有效的像素，剔除噪声点。
- **不确定性加权局部平移优化**：
  - 构造点到平面残差 $r_i(u;t)$（式 6）；
  - 通过一阶 Jacobian 将 $\Sigma_i(u)$ 传播到残差方差 $\sigma_{r,i}^2$（式 7）；
  - 固定旋转、仅优化相对平移，求解加权最小二乘（式 8），用少量 Gauss-Newton 迭代求解。

**（c）多时间尺度自校正**
- **关键帧窗口推理**：维护固定大小滑动窗口 $\mathcal{K}$，按固定步长并依据光流阈值插入关键帧；周期性对窗口内帧做全注意力联合推理，得到更一致的深度与位姿。
- **逆深度对齐**：将全注意力深度与流式深度做逆深度拟合，求尺度因子 $\eta$（式 10）。
- **段尺度估计**：比较关键帧窗口相对平移长度与流式相对平移长度（经 $\eta$ 归一），取中位数得到尺度线索 $\phi$（式 11）。
- **多时间尺度融合**：保留预测旋转，仅更新平移，将尺度重标定平移 $t_i^{scale}$ 与局部 GN 平移 $t_i^{GN}$ 按超参 $\mu$ 加权融合（式 12–13）。

## 3. 实验设计

- **训练数据**：仅训练跨视图几何不确定性头，使用 **TartanAir** 的 GT 光流与有效性掩码做残差监督；骨干网络（LingBot-Map）冻结。
- **评估数据集 / 场景**：
  - 长时程轨迹：**KITTI**（11 个序列，0.4–5.1 km）、**Oxford Spires**、**VBR**；
  - 稠密重建：**Neural RGB-D**（杂乱室内）、**7-Scenes**（干净室内）。
- **Benchmark 指标**：
  - 轨迹：Sim(3) 对齐后的 **ATE-RMSE（m）**；
  - 重建：Accuracy、Completeness、Chamfer Distance、**F1（0.05 m 阈值）**。
- **对比方法**：
  - 优化中心：MASt3R-SLAM、VGGT-SLAM 2.0、COLMAP、MASt3R-SfM、DPVO、DROID-SLAM；
  - 离线前馈：VGGT-Long、FastVGGT、LoGeR、Scal3R；
  - 流式前馈：CUT3R、TTT3R、Stream3R、StreamVGGT、InfiniteVGGT、LongStream、LingBot-Map、HorizonStream、Wint3R 等。
- **可选模块**：回环闭合（LC）使用缓存 DINOv2 早期层特征检索重访帧对，作为 PGO 约束。

## 4. 资源与算力

- **明确提到**：不确定性头训练 20k 次迭代，使用 8–24 帧片段，分辨率 518×392，AdamW 优化器，学习率 $10^{-4}$。
- **运行开销**：在同一流式设定下，每帧比 LingBot-Map 多 15–30 ms；KITTI 序列 04 吞吐从 11.68 FPS 降至 9.17 FPS。
- **未明确说明**：GPU 型号、数量、总训练时长、显存占用等算力细节文中均未给出。这一点属于可复现性上的信息缺口。

## 5. 实验数量与充分性

- **轨迹实验**：KITTI 逐序列（表 I，11 个序列）+ 三数据集平均（表 II），对比约 20 种方法。
- **重建实验**：NRGBD 与 7-Scenes 两个数据集（表 III），对比约 9 种流式方法。
- **消融实验**：
  - 位姿估计消融（表 IV）：去掉 LC、去不确定性加权优化、去尺度重标定，逐步验证各模块贡献；
  - 点云过滤消融（表 V）：SURE-Map vs. 不过滤 vs. 深度置信度过滤，并在两个数据集上验证；
  - 定性分析：图 3（不确定性 vs. 深度置信度）、图 4（长轨迹对比）、图 5（过滤可视化）。
- **充分性评价**：
  - **较充分**：覆盖长时程室外（KITTI、VBR、Oxford）与室内稠密重建（NRGBD、7-Scenes），消融链条清晰，指标口径与对比方法基本统一。
  - **公平性**：与 LingBot-Map/HorizonStream 等同为流式前馈，且 SURE-Map 建立在冻结的 LingBot-Map 骨干之上，提升可归因于方法本身；但训练数据仅 TartanAir，与其他基线训练数据规模是否对等未完全讨论。
  - **潜在偏置**：所有消融均在 LingBot-Map 骨干上完成，结论对骨干的可迁移性未验证。

## 6. 主要结论与发现

- **轨迹精度 SOTA**：
  - 平均 ATE-RMSE：KITTI 24.00→**17.24 m**，Oxford 5.11→**4.74 m**，VBR 31.37→**28.58 m**；
  - 加回环后进一步降至 **15.17 / 4.63 / 22.12 m**。
- **重建质量提升**：NRGBD F1 65.10→**66.20**，7-Scenes 81.77→**81.93**，Accuracy 也有改善。
- **不确定性有效性**：跨视图不确定性比骨干深度置信度更能定位动态/模糊区域，过滤后精度提升而完整性基本保持；深度置信度过滤反而丢弃过多有效点。
- **消融结论**：尺度重标定主要抑制累积漂移，不确定性加权优化提供互补的局部位姿修正，两者叠加效果最佳。
- **效率保持**：虽引入轻微延迟，但仍保持流式推理的实时性与低延迟特征。

## 7. 优点

- **问题定义清晰**：把"流式上下文受限 → 局部误差 → 段间尺度漂移"的因果链条讲清楚，动机有说服力。
- **不确定性建模新颖**：从"单视角预测可靠性"转向"跨视图对应一致性"，用位姿-深度诱导光流残差做 NLL 监督，形式简洁且有物理意义。
- **多时间尺度设计巧妙**：快慢双尺度解耦了效率与长程一致性，避免引入传统 SLAM 的完整后端优化开销。
- **工程上务实**：
  - 只训练轻量不确定性头，骨干冻结，训练成本低；
  - 与回环闭合可无缝协同，具备模块化扩展性。
- **实验体系完整**：长时程轨迹 + 稠密重建 + 过滤消融 + 运行时分析，定性与定量结合，图表支撑充分。
- **与基线可比性强**：在同一流式协议下对比，并单独报告"w/o LC"与"w/ LC"，透明度较高。

## 8. 不足与局限

- **算力信息缺失**：未报告 GPU 型号、数量与训练时长，不利于复现与成本评估。
- **骨干依赖**：全部实验基于 LingBot-Map 骨干，未验证对其他流式几何基础模型（如 Stream3R、HorizonStream）的通用性。
- **训练数据单一**：不确定性头仅在 TartanAir（合成）上训练，存在从合成到真实的域差距风险；真实场景下的泛化性未单独分析。
- **开销不可忽略**：每帧增加 15–30 ms，FPS 下降约 21%，在超长序列或算力受限设备上可能成为瓶颈。
- **回环模块非本文核心**：LC 依赖缓存 DINOv2 特征，其检索质量与代价未被深入讨论，与主方法的耦合边界不够清晰。
- **尺度重标定的假设**：方法依赖"稠密深度局部尺度比位姿更稳定"这一经验观察，在极端场景（大范围纹理缺失、快速旋转）下该假设是否成立缺乏分析。
- **评估范围**：主要集中在 KITTI/Oxford/VBR/NRGBD/7-Scenes，缺少动态物体密集、极端光照等更具挑战性的场景评测。
- **旋转固定策略**：局部优化固定旋转以避免累积旋转误差，但可能牺牲旋转精度，论文未给出旋转误差的单独分析。

（完）
