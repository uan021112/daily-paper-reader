1
VS-Splat: Voxel-Selective feed-forward Gaussian
Splatting for end-to-end 3D object reconstruction
from sparse-views
Yunsu Jeong*, Hyuk Heo*, Youngsang Kwak, Jaehwa Kwak, and Il Yong Chun, Member, IEEE
Abstract—Feed-forward
Gaussian
splatting
models
have
demonstrated remarkable effectiveness in reconstructing three-
dimensional (3D) objects from a few two-dimensional (2D)
images, even if they are unseen. As existing methods typically
predict Gaussian primitives uniformly across the 3D space, most
primitives are placed in non-object regions. This may hinder the
representation of fine object details. This paper proposes a Voxel-
Selective Gaussian Splatting model (VS-Splat), a new end-to-end
feed-forward Gaussian splatting framework that predicts many
primitives only within selected voxels that are likely to belong
to an object, without 3D structural supervision. To achieve this,
we propose a new learnable voxel selection approach that iden-
tifies object-centric voxels only with 2D rendering supervision.
Our sparse-view rendering experiments with three benchmark
datasets show that proposed VS-Splat outperforms several state-
of-the-art methods. We further demonstrate its effectiveness as a
backbone for an existing densification method and show that an
optional extension improves its robustness to inaccurate camera
pose estimates.
Index Terms—Feed forward Gaussian splatting, object recon-
struction, 3D Gaussian splatting
I. INTRODUCTION
A
wide range of industries including games, virtual reality,
augmented reality, medical imaging, and film production
increasingly require high-quality three-dimensional (3D) data
[1]–[5]. While two-dimensional (2D) images are readily avail-
able at scale, high-quality 3D data remain relatively scarce
due to the difficulty and cost of reconstruction. In particular,
3D object reconstruction has been extensively studied as a
fundamental task for downstream multimedia applications.
Recent advances in Neural Radiance Fields (NeRF) [6] and 3D
Gaussian Splatting (3DGS) [7] have significantly improved 3D
*These authors contributed equally. The work was supported in part by
NRF Grants RS-2026-25480389 and RS2023-00213455 funded by MSIT,
IITP Grants RS-2026-25615334 (AI Star Fellowship Support Program) and
RS-2019-II190421 (AI Graduate School Support Program (Sungkyunkwan
University)) funded by MSIT, the BK21 FOUR Project, the Digital Thera-
peutics Development and Demonstration Support Program Grant H0601-24-
1023 funded by MSIT and NIPA, and KIAT Grant RS-2024-00418086 (HRD
Program for Industrial Innovation) funded by MOTIE. (Corresponding author:
Il Yong Chun.)
Yunsu Jeong is with the Department of Electrical and Computer Engineer-
ing (ECE), Sungkyunkwan University (SKKU), Suwon 16419, South Korea
(e-mail: intyeger@g.skku.edu).
Hyuk Heo is with the Department of Display Convergence Engineering
(DCE), SKKU, Suwon 16419, South Korea (e-mail:mongyong2@g.skku.edu).
Youngsang Kwak and Jaehwa Kwak are with AiM Future, Seoul 06804,
South Korea (email: {youngsang.kwak, jaehwa.kwak}@aimfuture.ai)
Il Yong Chun is with the Departments of ECE, Artificial Intelligence,
Advanced Display Engineering, Semiconductor Convergence Engineering, and
DCE, SKKU, Suwon 16419, South Korea (e-mail: iychun@skku.edu).
(a) Existing
methods
(b) VS-Splat (ours)
Fig. 1. Overview of the proposed framework vs. existing E2E feed-forward
object reconstruction approaches in generalizable Gaussian splatting under
a sparse-view setup. (a) Existing methods predict a fixed, small number of
Gaussian primitives at every pixel or voxel, regardless of object occupancy. (b)
The proposed VS-Splat framework first selects voxels that are likely to belong
to an object, and then predicts many Gaussian primitives at each selected
voxel, all within a single end-to-end feed-forward pass. Its training does not
require 3D structural supervision.
object reconstruction by learning effective 3D representations
from multi-view images of an isolated object [8] or an object
segmented from a scene [9]. Although 3DGS enables real-
time rendering compared to the computationally expensive
volumetric rendering of NeRF, 3DGS [7] and its variants [8],
[9] still require iterative optimization and typically rely on
densely captured multi-view images and iterative optimization
to reconstruct a single 3D object.
To overcome these limitations, a new class of generalizable
feed-forward Gaussian splatting (GS) models has emerged.
Such models leverage a 3D prior learned from large-scale
multi-view data, enabling the direct generation of Gaussian
representations from a single image or a few sparse-view im-
ages via a single forward pass, without per-object optimization.
These feed-forward GS approaches typically adopt either
a pixel-aligned strategy [10] that places Gaussian primitives
within 2D pixel grid cells, or a voxel-aligned strategy [11],
[12] that places them within 3D voxel grid cells. By placing
Gaussian primitives within each pixel or voxel, the mapping
from 2D image(s) to Gaussian representations becomes more
explicit and efficient. However, as spatial resolution of the
underlying grid increases, the number of grid cells grows
quadratically in the pixel-aligned strategy and cubically in
the voxel-aligned strategy, thereby restricting each cell to
contain only one or two Gaussian primitives for efficient
arXiv:2609.12343v1  [cs.CV]  11 Sep 2026


2
novel view synthesis. This limitation becomes particularly
inefficient for 3D objects surrounded by large empty regions,
as many Gaussian primitives are allocated to uninformative
empty regions rather than to an object itself.
Another stream of generalizable GS explores 3D geometry-
aware GS research investigates 3D geometry-aware GS mod-
els trained with a large-scale 3D geometry dataset. These
geometry-aware methods [13], [14] adopt a multi-stage
pipeline: a first network estimates the 3D object geometry
and predicts the positions of Gaussian primitives, followed by
a separate network that predicts their parameters. Yet, these
methods are not end-to-end (E2E) with respect to rendering
fidelity. As a result, they are inherently prone to error propa-
gation, since each stage is trained independently – either with
supervision from 3D geometries (in the first network) or from
2D images (in the second network).
We propose a Voxel-Selective Gaussian Splatting model,
called VS-Splat, a new E2E feed-forward GS framework
for novel view synthesis from a few 2D views without 3D
structural supervision. VS-Splat is built around a central E2E
design principle: rendering supervision determines not only
the parameters of Gaussian primitives but also their spatial
allocation. Accordingly, VS-Splat predicts many Gaussian
primitives only in selected voxels likely to belong to an object.
Fig. 1 highlights this distinction between VS-Splat and existing
E2E feed-forward GS approaches for sparse-view 3D object
reconstruction.
Specifically, the proposed VS-Splat framework employs a
coarse-to-fine (C2F) approach strategically allocates Gaussian
primitives to object-centric regions. The coarse stage of VS-
Splat estimates the coarse voxelized structure of an object, and
predicts a single coarse Gaussian primitive at each voxel. Next,
we select voxels that are likely to belong to an object using the
coarse, global structural feature. We achieve this without any
3D structural supervision, by casting 3D structure estimation
of an object as a voxel-wise binary classification task, where
the outputs consist of a confidence score map and a voxel
selection mask identifying target voxels for dense Gaussian
primitive placement. For E2E learning, we propose a new
learnable voxel selection approach that integrates the resulting
selection confidence scores into the GS rendering process. To
enhance the representation of object details, the fine stage
of VS-Splat predicts many additional fine-grained Gaussian
primitives near selected coarse ones. We propose a network
that enriches coarse structural feature by incorporating fine
local details at the selected voxels.
Our contributions are summarized as follows:
• We propose a new E2E feed-forward Gaussian splatting
model VS-Splat that selects voxels likely to belong to
an object and then predicts many fine-grained Gaussian
primitives at those voxels.
• We propose a new learnable voxel selection approach that
circumvents explicit 3D structure estimation, relying on
2D rendering supervision for training.
• To predict many fine-grained Gaussian primitives, we
propose a selective feature refinement network that refines
coarse, global features by integrating fine, local geometric
features, at selected object-centric voxels.
• Our sparse-view rendering experiments with the three
benchmark datasets, large-scale GObjaverse [15], Google
Scanned Objects (GSO) [16], and Common Objects in 3D
(CO3D) [17], show that VS-Splat outperforms existing
state-of-the-art (SOTA) methods.
• We further demonstrate the extensibility of VS-Splat in
two settings. It consistently improves rendering perfor-
mance when used in place of the original backbone of an
existing densification method [18]. We also introduce an
optional extension that improves its robustness to noisy
camera pose estimates on in-the-wild data.
II. RELATED WORKS
A. Feed-forward GS methods
Recent feed-forward GS methods reconstruct a 3D represen-
tation of a scene or an object by predicting Gaussian primitives
in 3D space from one or more input images.
1) Scene reconstruction methods:
Scene reconstruction
methods mainly adopt a pixel-aligned strategy [19]–[22], in
which Gaussian predictions are indexed by a regular 2D grid
associated with each input view. In methods with known
camera parameters, Gaussian centers are typically obtained by
predicting a depth or depth distribution at each grid location
and locating the centers along the corresponding camera rays
[19]–[21]. For methods that do not assume known camera
parameters, Gaussian centers may instead be directly regressed
from pixel-wise features [22].
2) Object reconstruction methods: Object reconstruction
methods broadly adopt three Gaussian prediction strategies:
pixel-aligned [10], [23], [24], voxel-aligned [11], [12], [18],
and geometry-aware [13], [25]. The pixel- and voxel-aligned
strategies are illustrated in Fig. 1(a).
Pixel-aligned methods organize Gaussian predictions on a
dense 2D grid associated with each input view and predict one
or a few primitives at every grid location. Because each grid
location provides only a fixed number of primitive prediction
slots, a dense grid is used to provide sufficient representa-
tional capacity for fine image details. Such dense prediction
remains computationally feasible because the number of grid
locations grows quadratically with grid resolution. Depending
on the method, Gaussian centers are either constrained to the
corresponding camera rays [10], [24] or directly regressed in
3D [23].
Voxel-aligned methods instead define a regular 3D voxel
grid and predict one or a few Gaussian primitives within
every voxel. Since the number of voxels grows cubically with
grid resolution, increasing the spatial resolution is substantially
more expensive than for a 2D pixel grid. Consequently, voxel-
aligned methods typically operate on a lower grid resolution,
and their uniform allocation also assigns primitive prediction
slots to empty voxels. As illustrated in Fig. 1(a), both pixel-
and voxel-aligned methods initially allocate a fixed number of
primitives over their respective grids, including background
pixels or empty voxels.
Geometry-aware methods first estimate an explicit 3D struc-
ture, such as a point cloud or occupied anchors, using 3D


3
Fig. 2. Overall architecture of proposed VS-Splat. It consists of three stages: coarse, voxel selection, and fine.
supervision and then decode Gaussian primitives from the es-
timated structure using rendering-based supervision [13], [25].
Because their Gaussian prediction depends on an intermediate
3D representation learned with explicit 3D supervision, these
methods are not E2E with respect to rendering supervision
alone (thus falling outside the scope of the E2E comparison
in Fig. 1(a)). In contrast, the aforementioned pixel- and
voxel-aligned methods are trained E2E with rendering-based
supervision.
In the sparse-view setting, LGM [23] and GS-LRM [24]
adopt pixel-aligned prediction, LaRa [11] and Generative
Densification (GenDen) [18] adopt voxel-aligned prediction,
while GeoLRM [13] adopts geometry-aware prediction.
B. C2F approaches in 3D reconstruction
C2F approaches have been adopted in various forms across
3D reconstruction. Earlier methods typically realize C2F by
progressively refining coarse representations into finer ones.
Point-based methods (e.g., point clouds) realize C2F by pro-
gressively increasing point density in selected regions [26].
Volume-based methods, which represent 3D space using voxel
grids storing color and opacity, realize C2F by progressively
increasing grid resolution [27]. Neural implicit representation
methods realize C2F by progressively refining ray sampling
around object surfaces through hierarchical sampling [6], [28]–
[30].
1) C2F approaches in feed-forward GS methods for 3D ob-
ject reconstruction: Recent feed-forward GS methods for 3D
object reconstruction have explored different C2F strategies for
refining Gaussian representations. LaRa predicts two Gaussian
primitives at each voxel throughout a fixed voxel grid and se-
lectively refines the view-dependent appearance of primitives
whose opacity exceeds a predefined threshold, without gen-
erating additional primitives [11]. For object reconstruction,
GenDen augments LaRa with an add-on densification module
that selects a subset of LaRa-generated Gaussian primitives
based on their view-space positional gradients and generates
additional fine Gaussian primitives around them [18].
Unlike LaRa and GenDen, which select already predicted
Gaussian primitives for appearance refinement or densification,
respectively, the proposed VS-Splat performs selection at the
voxel level before fine Gaussian generation. Specifically, VS-
Splat predicts one coarse Gaussian primitive at each voxel and
estimates a voxel-wise selection mask from the coarse volu-
metric features to identify voxels likely to belong to an object.
It then generates multiple fine Gaussian primitives only within
the selected voxels. Thus, the coarse representation determines
where new fine Gaussian primitives are instantiated, rather
than which existing primitives should be refined or densified.
2) Selection strategies for Gaussian refinement and alloca-
tion: Several feed-forward GS methods employ different cri-
teria to select Gaussian primitives or spatial regions for refine-
ment, densification, or adaptive primitive allocation. LaRa uses
opacity as its selection criterion and applies its fine appearance
decoder only to primitives whose opacity exceeds a predefined
threshold [11]. GenDen first selects Gaussian primitives with
large view-space positional gradients for densification and sub-
sequently uses learned confidence scores to determine which
generated primitives undergo further densification [18]. For
scene-level generalizable novel view synthesis, SplatWeaver
uses predicted routing scores to select a cardinality expert for
each pixel grid cell, thereby determining the number of Gaus-
sian primitives generated at that cell [22]. In supervising this
routing process, it uses pseudo-labels obtained by ranking 2D
image pixels according to their frequency energy, in addition
to the rendering objective. These discrete selection operations
are non-differentiable, and GenDen and SplatWeaver employ a
straight-through estimator (STE) [31] to train their score-based
selection modules.
The proposed VS-Splat learns voxel selection in an E2E
learnable manner and allocates fine Gaussian primitives only
within voxels likely to belong to an object. Its selection module
is trained solely through the rendering objective, without
pseudo-labels or other auxiliary supervision.


4
III. METHODS
We propose the VS-Splat framework that takes N 2D
images from different views {in : n = 1, . . . , N} and their
corresponding camera pose parameters (as input), and gener-
ates 3D Gaussian representations aligned to selected voxels
(as output). Throughout this study, we focus on a sparse-
view setup, i.e., {in} are a few sparse-view images. Fig. 2
illustrates the overall architecture of VS-Splat that adopts a
C2F approach:
• The coarse stage extracts feature maps from sparse-view
images, aggregates them into a unified feature volume
that captures coarse, global structure of an object. From
this feature volume, we predict a coarse Gaussian primi-
tive at each voxel across the entire volumetric space. See
Fig. 2: Coarse stage.
• Next, we select voxels that are likely to belong to an
object by a new learnable voxel selection approach. We
refer to the coarse Gaussian primitives corresponding to
the selected voxels as “skeletal” Gaussian primitives. See
Fig. 2: Voxel selection stage.
• The fine stage predicts many fine-grained Gaussian prim-
itives near a “skeletal” primitive at each selected voxel,
via a new selective feature refinement module that refines
coarse, global features by integrating fine, local geometric
features, at object-centric voxels. See Fig. 2: Fine stage.
In training, we propose to use 2D supervision in both coarse
and fine renderings. See the bottom part of Fig. 2. In addition
to the overall design, Fig. 2 illustrates the detailed workflow
of each stage.
A. Backgrounds
3DGS represents a 3D asset with a set of K 3D Gaussian
primitives {Gk : k = 1, . . . , K} [7]. Each Gaussian primitive
Gk is parameterized by 1) a mean vector µk ∈R3 representing
its 3D position, 2) a covariance matrix Σk ∈R3×3 represent-
ing its 3D shape that can be decomposed into a scale vector
sk ∈R3 and a rotation quaternion rk ∈R4, 3) an opacity
ok ∈[0, 1], and 4) spherical harmonics coefficients hk ∈R3S
representing view-dependent color, where S is the number
of basis functions per color channel (red, green, and blue).
These primitives are passed through the Gaussian splatting
rasterization [7], producing 2D images rendered from arbitrary
viewpoints.
The voxel-aligned feed-forward Gaussian splatting models
predict Gaussian primitives aligned to a voxel grid from
sparse-view images [11], [12], so that K becomes the number
of voxels in a voxel grid. All views share the predefined voxel
grid within the normalized 3D space [−0.5, 0.5]3, centered
at the origin, where the voxel grid uses the center-coordinate
system. For the kth voxel, instead of directly predicting the
position of its Gaussian primitive, µk, the methods predict
its positional offset ∆k ∈[−r, r]3, where r ∈R>0 is the
maximum displacement, and add the predicted offset to its co-
ordinate, for k = 1, . . . , K. This strategy can ensure that each
primitive is placed within the corresponding voxel. Similar to
3DGS, the primitives are rendered through rasterization.
B. The coarse stage
The proposed coarse stage consists of the following three
core modules: 1) view-specific volumetric feature extraction,
2) multi-view feature aggregation & compression, and 3)
coarse primitive decoder.
1) View-specific volumetric feature extraction.: We first
obtain a volumetric feature from a 2D input image at each
view in, for n = 1, . . . , N, by following the approach in
[11], [12]. As its first step, 2D feature maps are extracted
from each view image, using a shared feature extractor. For
each view n, its feature map is then modulated using the
adaptive layer normalization (AdaLN) scheme in [32] with
a Pl¨ucker embedding map pn from its corresponding camera
pose parameters [33].
This produces a set of pose-aware feature maps {fn :
n = 1, . . . , N}. Then, these modulated feature maps are
backprojected into a predefined voxel grid to produce a view-
specific feature volume vn ∈RH×W ×D×C, for n = 1, . . . , N,
where H, W, and D are the height, width, and depth of
the voxel grid, respectively, and C denotes the number of
volumetric feature maps at each view.
2) Multi-view feature aggregation & compression.: Next,
we propose a lightweight module that aggregates volumetric
features from all the input views and transforms it into a
compact volumetric representation. Rather than the computa-
tionally expensive transformer architecture [11], [12], [34], we
use a lightweight variant of 3D U-Net [35] after aggregating
feature volumes {vn : n = 1, . . . , N}.
We
form
an
initial
unified
feature
volume
v0
∈
RH×W ×D×2C as follows:
v0 = Mean(v1, . . . , vN) c Var(v1, . . . , vN),
(1)
where
c
denotes channel-wise feature concatenation, and
Mean(·) and Var(·) denote the voxel-wise mean and variance
operators across views, respectively. By using the variance
operator, we capture voxel-wise deviations in features across
views.
To use a lightweight variant of 3D U-Net – with one
encoding block and one decoding block – for reduced mem-
ory and computational cost, we compress v0 in (1) into a
feature volume with a smaller number of volumetric feature
maps ˜C using a 3D convolutional compressor, denoted by
C : RH×W ×D×2C →RH×W ×D× ˜
C, where ˜C ≪2C. The
lightweight 3D U-Net variant Ulight(·) further transforms the
compressed feature volume into a unified volumetric represen-
tation, denoted ˜v ∈RH×W ×D× ˜
C. We formulate this process
as follows:
˜v = Ulight(C(v0)).
(2)
3) Coarse primitive decoder.: The final step in the coarse
stage is to decode voxel-wise features in ˜v of (2) to
coarse Gaussian primitives. We use a fully-connected de-
coder Dcrs(·) to decode a feature vector at each voxel
˜vh,w,d
∈
R ˜
C
into
a
Gaussian
primitive
Gcrs
h,w,d
=
{µcrs
h,w,d, scrs
h,w,d, rcrs
h,w,d, ocrs
h,w,d, hcrs
h,w,d}:
Gcrs
h,w,d = Dcrs(˜vh,w,d),
(3)


5
for h = 1, . . . H, w = 1, . . . , W, and d = 1, . . . , D. As
described in the background section, we determine µcrs
h,w,d
by adding a predicted offset ∆crs
h,w,d to the coordinate of
the (h, w, d)th voxel, ∀h, w, d. Note that we predict a single
Gaussian primitive per voxel.
C. The voxel selection stage
It is widely known in Gaussian splatting that to achieve
high-quality renderings, one needs to position Gaussian prim-
itives near an object [7], [36]. This insight motivates the
proposed framework design: we first identify voxels that are
likely to belong to an object and then predict many Gaussian
primitives within those selected voxels. In this section, we pro-
pose a new learnable voxel selection approach that identifies
voxels likely to belong to an object, without explicit supervised
learning using a large-scale 3D geometry dataset [13].
1) Technical motivation of the proposed voxel selection
approach.: A straightforward voxel selection approach is to
treat a voxel as belonging to an object to an object if the
opacity of its coarse Gaussian, ocrs
h,w,d in (3), exceeds a pre-
defined threshold. However, manually tuning the threshold is
challenging. More importantly, the opacity alone may not be a
reliable indicator in determining important Gaussian primitives
[37], which, in our framework, corresponds to identifying
voxels that are likely to belong to an object.
To circumvent 3D structural supervision in training, we es-
timate voxel-wise confidence scores only with 2D supervision
(i.e., comparing rendered 2D images compared against ground-
truth images) using coarse primitives in (3). In inference, we
select voxels with high confidence scores, where one does
not need to tune a threshold. We conjecture that selecting
important coarse Gaussian primitives – those associated with
high confidence scores – effectively identifies voxels that are
likely to belong to an object.
2) Prediction of voxel-wise confidence scores for object
membership.: We propose a learnable module that predicts
voxel-wise confidence scores for object membership, σ ∈
[0, 1]H×W ×D. The score map guides the selection of voxels
likely to belong to an object, where fine Gaussian primitives
are subsequently placed.
The proposed confidence score predictor consists of a fully-
connected predictor P : R ˜
C →R and a Gumbel-Softmax
operator [38]. First, for each feature vector ˜vh,w,d of ˜v in (2),
we predict a confidence logit, i.e., P(˜vh,w,d), ∀h, w, d. Next,
we model the binary decision for each voxel using a Gumbel-
Sigmoid estimator. To provide a differentiable approximation
of discrete binary choices, we apply a variant of the Gumbel-
Softmax distribution, known as the Gumbel-Sigmoid [39], to
confidence logit for each voxel as follows:
σh,w,d =
1
1 + exp(−(P(˜vh,w,d) + g0 −g1)/τ),
(4)
for h = 1, . . . H, w = 1, . . . , W, and d = 1, . . . D, where g0
and g1 denote independent and identically distributed samples
drawn from the Gumbel(0, 1) distribution [40], and τ denotes
the temperature. Note that Gumbel-Sigmoid-based predictor in
(4) stochastically pushes the score toward 0 or 1 (especially
when τ is low), so σh,w,d ∈[0, 1], ∀h, w, d.
Fig. 3. Visualizations of selected voxels with high confidence scores by the
proposed learnable voxel selection approach.
To adjust each primitive’s contribution in rendered 2D
image, we propose to refine the opacity of every coarse
Gaussian primitive using its confidence score σh,w,d in (4):
˜ocrs
h,w,d = σh,w,d · ocrs
h,w,d,
(5)
for h = 1, . . . , H, w = 1, . . . W, and d = 1, . . . , D. In
training, the proposed scheme (5) helps maintain uninterrupted
gradient propagation across all voxels. Using 2D supervision
with the proposed refinement (5), we can assign high con-
fidence to coarse primitives that contribute to high-quality
rendering.
3) Voxel selection.: Now, we select the voxels that are
likely to belong to an object if the confidence score in (5)
exceeds 0.5, where we write the skeletal Gaussian primitives
by defining the set of the corresponding selected indices, as
follows:
{Gcrs
h,w,d : (h, w, d) ∈I},
I = {(h, w, d) : σh,w,d > 0.5},
(6)
We note that the hard thresholding value of 0.5 does not
require tuning, as it is consistently used across prior Gumbel-
Sigmoid-based works [39], [41]. The threshold 0.5 is a bal-
anced point in the binary decision space.
Ultimately, the proposed approach (4)–(6) combined with
the widely known insight in Gaussian primitive positioning
(see the beginning of “The voxel selection stage” section) can
promote the selection of voxels that are likely to belong to an
object. Fig. 3 supports our conjecture that selected voxels, i.e.,
coarse Gaussians, with the proposed high confidence scores
can capture the 3D structure of an object.
We observed that the proposed voxel selection approach
(4)–(6) gives higher rendering accuracy, compared to variants
of the voxel selection approach motivated by the existing se-
lection methods [11], [18]. See Appendix A.I for comparisons
with alternative voxel selection approaches.
D. The fine stage
The proposed fine stage consists of the following three
core modules: 1) pose-aware projection at selected voxels,
2) selective feature refinement module, and 3) fine primitive
decoder & skeletal primitive adjustor.
1) Pose-Aware Projection (PAP) at selected voxels.: The
feature volume ˜v of (2) in the coarse stage is constructed
by backprojecting 2D feature maps from different views to
the center of each voxel across the entire volumetric space.
This projection method ignores actual surface geometry and
assumes that 2D features can be reliably mapped to the
voxel center via camera rays, which can lead to ambiguous


6
or inconsistent feature assignment, particularly in occluded
or background regions. Moreover, associating all features
with voxel centers discards sub-voxel geometric precision,
potentially limiting the representation of fine-grained details.
To resolve the aforementioned limitations, we aim to effec-
tively extract multi-view contextual information only for each
selected voxel that are likely to belong to an object, where
we refer to this process as Pose-Aware Projection (PAP) at
selected voxels.
We first project positions of skeletal Gaussians in (6),
{µcrs
h,w,d
: (h, w, d) ∈I}, onto each of N pose-aware
feature maps {fn : n = 1, . . . , N} (obtained in the coarse
stage), and sample the features at the corresponding projected
locations. This results in projected feature volumes tailored
to each view, i.e., PAP-derived feature volumes, denoted by
{¯v(h,w,d),1, . . . , ¯v(h,w,d),N
: (h, w, d) ∈I}. By aligning
selected 3D skeletal primitives with 2D feature maps from
different camera views, we can allow the network to associate
each selected voxel with rich, pose-aligned feature, which
would be useful for accurate fine-grained rendering.
We propose to employ inverse distance weighting to ag-
gregate these PAP-derived feature volumes from different
views in a voxel-wise manner, giving higher importance to
those captured from viewpoints closer to the voxel’s primitive
location:
ˆvh,w,d =
PN
n=1 z−1
(h,w,d),n · ¯v(h,w,d),n
PN
n′=1 z−1
(h,w,d),n′
,
(h, w, d) ∈I, (7)
where z(h,w,d),n denotes the distance between µcrs
h,w,d and the
nth view camera position. For example, suppose that there are
a voxel and three camera views. The view closest to the voxel
will have the greatest influence, while features from farther
views receive lower weights and contribute less.
2) Selective Feature Refinement (SFR) module.: While ˜v
in (2) serves as a globally aggregated coarse representation of
the entire 3D object, {ˆvh,w,d : (h, w, d) ∈I} in (7) focuses
on enriching selected object-centric voxels with fine-grained,
pose-aware features derived from 2D views. With this coarse-
to-fine feature hierarchy, we can capture both global structure
and detailed local geometry.
We propose a new module that refines the coarse, global
structure at object-centric voxels, {˜vh,w,d : (h, w, d) ∈I}, by
integrating fine, view-specific details from the corresponding
pose-aware features, {ˆvh,w,d : (h, w, d) ∈I}. To provide
more accurate spatial location information to each feature
{˜vh,w,d, ˆvh,w,d}, we apply positional encoding to the corre-
sponding skeletal primitive position, µcrs
h,w,d, ∀(h, w, d) ∈I.
We refer to this process as Selective Feature Refinement
(SFR).
We write its overall process that produces refined object-
centric representation {f +
h,w,d : (h, w, d) ∈I} by:
f +
h,w,d = R(˜vh,w,d c ˆvh,w,d c γ(µcrs
h,w,d)),
(h, w, d) ∈I,
(8)
where R(·) denotes SFR module, ˜vh,w,d and ˆvh,w,d are
given in (2) and (7), respectively, and γ(·) is the NeRF
positional encoding function [6]. Specifically, we concatenate
˜vh,w,d, ˆvh,w,d, and γ(µcrs
h,w,d) in a channel-wise manner at
each selected voxel (h, w, d) ∈I, and feed the concatenated
results into SFR module. We then fuse concatenated feature
via a fully-connected layer and then process fused feature
via R residual convolutional blocks [42] with two sparse 3D
convolution layers. To further promote intricate details, we
once more concatenate ˆvh,w,d with proposed fused feature,
and pass the result through the final sparse 3D convolutional
layer. See the detailed architecture in the Appendix A.II.
3) Fine primitive decoder & skeletal primitive adjustor.:
Finally, we generate fine-grained primitives and adjust the
skeletal primitives for fine rendering.
We first generate fine-grained primitives “near” each skeletal
primitive, where we define a primitive as being near a skeletal
primitive if it lies within a 0.5-voxel radius from the skeletal
one. We use a fully-connected decoder Dfin(·) to decode
refined feature at each selected voxel f +
h,w,d into M fine-
grained Gaussian primitives:
{Gfin
(h,w,d),m : m = 1, . . . , M} = Dfin(f +
h,w,d),
(9)
where Gfin
(h,w,d),m denotes the mth fine-grained Gaussian gen-
erated near the (h, w, d)th skeletal primitive. Specifically,
we determine the position of each fine-grained primitive, as
similarly in (3): µfin
(h,w,d),m = µcrs
h,w,d + ∆fin
(h,w,d),m, where
∆fin
(h,w,d),m denotes the predicted offset, for (h, w, d) ∈I and
m = 1, . . . , M. This ensures that fine-grained primitives are
near a skeletal primitive at each selected voxel.
To seamlessly integrate skeletal primitives (6) and fine-
grained primitives (9), we adjust the appearance parameters –
specifically, opacity and spherical harmonics coefficients – of
skeletal primitives while preserving their structure. As skeletal
primitives represent an object with fewer elements, they tend
to exhibit higher opacities compared to fine-grained primitives,
which may hinder seamlessly integration. We thus, adjust the
appearance parameters of skeletal primitives.
We use a fully-connected adjustor A(·) to predict the
opacity scaling factor αh,w,d and new spherical harmonics
coefficients hnew
h,w,d from each refined feature f +
h,w,d:
(αh,w,d, hnew
h,w,d) = A(f +
h,w,d),
(h, w, d) ∈I.
(10)
We then update the opacity of skeletal primitives by
oupd
h,w,d = Sigmoid(αh,w,d) · ˜ocrs
h,w,d,
(h, w, d) ∈I,
(11)
where ˜ocrs
h,w,d is in (5) and Sigmoid(·) : R →[0, 1] denotes
the sigmoid function.
Finally, we obtain final rendered images using both find-
grained and adjusted skeletal primitives. During inference, we
bypass coarse rendering.
E. Loss function
We propose to train VS-Splat in an E2E manner using only
rendering-based supervision from ground-truth 2D images
{in : n = 1, . . . , N + N ′} corresponding to N input view-
points and N ′ novel viewpoints, without explicit 3D structural
supervision. We render the coarse and fine Gaussian represen-
tations at the corresponding viewpoints, producing the image
sets {ˆicrs
n : n = 1, . . . , N +N ′} and {ˆifin
n : n = 1, . . . , N +N ′}
respectively. We followed the recent sparse-view setup [11],


7
[23], N = N ′ = 4. We propose the overall E2E training loss
as follows:
L := Ei
1
N + N′
N+N ′
X
n=1
X
lev∈{crs,fin}
LMSE(in,ˆilev
n )+λ·LSSIM(in,ˆilev
n ),
(12)
where Ei denotes the expectation over training object in-
stances, and LMSE(·, ·) and LSSIM(·, ·) denote the mean squared
error (MSE) and the structural similarity (SSIM) loss, respec-
tively. We set the balancing parameter λ to 0.5, following [11].
F. Extension for handling noisy camera poses
For each spatial feature (e.g., a token embedding) in an
image feature map, a Pl¨ucker embedding represents the as-
sociated camera ray as a six-dimensional vector. This vector
comprises the normalized ray direction d ∈R3 and the ray
moment m = o × d, where o ∈R3 denotes the camera
center and × denotes the cross product. For each view n,
the resulting Pl¨ucker embedding map pn is transformed into
scale and shift parameters that AdaLN [32] uses to modulate
the image features and produce a pose-aware feature map (see
Section III-B1).
However, camera poses estimated for in-the-wild datasets
such as CO3D [17] may contain noise. Because each pose
determines all camera rays in the corresponding view, these
pose errors perturb the Pl¨ucker embeddings across the entire
feature map. The base VS-Splat model, trained only with
accurate camera poses, is not exposed to such perturbations
and thus does not learn to compensate for them. Consequently,
AdaLN modulates the image features using scale and shift
parameters derived from the perturbed embeddings, potentially
yielding pose-aware feature maps that encode inaccurate cam-
era geometry and are inconsistent across views.
To address this issue, we introduce a SHARE-based ex-
tension [43] that predicts Pl¨ucker embedding maps directly
from the 2D feature maps, rather than constructing them
from potentially noisy camera pose estimates. Specifically,
the extension predicts Pl¨ucker embedding maps {ˆpn : n =
1, . . . , N} using a two-stage transformer architecture. In the
first stage, a matching transformer applies windowed self-
attention within each view and cross-attention across views to
establish local feature correspondences between views. In the
second stage, a lightweight transformer jointly processes the
correspondence-aware features from all views to capture their
global camera configuration. A regression head then maps each
spatial token to a six-dimensional Pl¨ucker vector, producing a
Pl¨ucker embedding map for each view.
We train the Pl¨ucker embedding map predictor by super-
vising its predictions with ground-truth Pl¨ucker embedding
maps computed from the ground-truth camera poses of input
views. We propose the overall training loss for this extension
as follows:
Lext := L + Ei
1
N
N
X
n=1
LMSE([p(i)]n, [ˆp(i)]n),
(13)
where L is defined in (12), and [p(i)]n and [ˆp(i)]n denote
the ground-truth and predicted Pl¨ucker embedding maps, re-
spectively, for the nth input view of training object instance
i. Implementation details of the extension are provided in
Appendix A.III.
IV. RESULTS AND DISCUSSION
A. Experimental setups
We compared proposed VS-Splat with several SOTA feed-
forward novel-view synthesis methods using a few sparse-view
images, by following the sparse-view rendering setup in [11],
[18], N = 4. We compared VS-Splat with three categories
of SOTA feed-forward methods in 3D object reconstruction:
1) GS-based methods, including LaRa [11], GS-LRM [24],
GeoLRM [13], and LGM [23]; 2) NeRF-based methods,
including MuRF [45] and MVSNeRF [44]; and 3) the novel
view synthesis method without constructing a renderable 3D
representation, LVSM [46]. The first two categories learn
intermediate geometric representations.
We followed the most comprehensive experimental setup
[11], [18] using the three benchmark datasets, large-scale
GObjaverse [15], Google Scanned Objects (GSO) [16], and
Common Objects in 3D (CO3D) [17]. As evaluation metrics,
we used peak signal-to-noise ratio (PSNR), structural simi-
larity index (SSIM) [47], and learned perceptual image patch
similarity (LPIPS) [48] as evaluation metrics. PSNR measures
pixel-level rendering fidelity, SSIM evaluates structural simi-
larities including luminance and contrast, and LPIPS assesses
perceptual similarity correlated with human visual perception.
B. Comparisons of feed-forward novel-view synthesis methods
for 3D object reconstruction
1) Comparisons between different GS and NeRF-based
feed-forward methods: Fig. 4 and Tables I–II show that
proposed VS-Splat outperforms the existing six SOTA GS- and
NeRF-based methods. Fig. 4(a) shows that VS-Splat produces
sharper renderings with finer details compared to SOTA meth-
ods. Fig. 4(b) and 4(c) demonstrate superior generalization
capability of VS-Splat, consistently achieving significantly
better rendering quality compared to SOTA methods across
different datasets. In particular, Fig. 4(c) shows that VS-Splat
generalizes well to the real-world CO3D dataset, produces
both better overall structure and finer details than other meth-
ods. Tables I–II show that VS-Splat outperforms the SOTA
methods across on all three benchmark datasets.
Now, we compare proposed VS-Splat particularly with the
two SOTA GS methods, LaRa and GeoLRM. By focusing
primitive generation on regions of interest, VS-Splat achieves
both accelerated inference and improved results compared
to LaRa. In particular, VS-Splat can achieve up to 1.3×
faster inference speed compared to LaRa; see inference speed
comparisons with LaRa in Appendix A.IV). While LaRa
1We obtained the VS-Splat results on GObjaverse and GSO using models
trained with the base loss in (12), whereas we obtained the CO3D results
using the proposed extension trained with the loss in (13). We re-evaluated
GeoLRM using its publicly released weights. GeoLRM could not be evaluated
on GObjaverse, as its publicly available weights were trained on the full
dataset. We followed the experimental setup in [18].
2Since GS-LRM and LVSM do not provide publicly released weights and
their papers do not include results on GObjaverse and CO3D, we only report
their GSO results. We followed the experimental setup in [24].


8
TABLE I
QUANTITATIVE COMPARISONS OF FEED-FORWARD NOVEL-VIEW SYNTHESIS METHODS FOR 3D OBJECT RECONSTRUCTION.1
Methods
GObjaverse
GSO
CO3D
PSNR↑SSIM↑LPIPS↓
PSNR↑SSIM↑LPIPS↓
PSNR↑SSIM↑LPIPS↓
MVSNeRF [44]
14.48
0.896
0.186
15.21
0.912
0.154
12.94
0.841
0.241
MuRF [45]
14.05
0.877
0.302
12.89
0.885
0.280
11.60
0.815
0.393
LGM [23]
19.67
0.867
0.158
23.67
0.917
0.064
13.81
0.739
0.414
GeoLRM [13]
-
-
-
25.06
0.947
0.068
16.49
0.829
0.284
LaRa [11]
27.51
0.938
0.093
29.81
0.959
0.071
21.18
0.862
0.216
Ours
28.77
0.947
0.080
30.84
0.962
0.064
21.62
0.864
0.211
Lara + GenDen [18]
28.58
0.945
0.080
31.06
0.966
0.058
21.72
0.865
0.209
Ours + GenDen
29.10
0.949
0.075
31.50
0.968
0.057
22.29
0.867
0.207
LGM
GeoLRM
LaRa
LaRa
Ours
Ours
Ground-truth
Ground-truth
(a) GObjaverse
(b) GSO
(c) CO3D
Fig. 4. Qualitative comparisons of feed-forward novel-view synthesis methods with different 3D object reconstruction datasets.
TABLE II
QUANTITATIVE COMPARISONS OF FEED-FORWARD NOVEL-VIEW
SYNTHESIS METHODS WITH GSO (CROSS-DOMAIN SETUP).2
Methods
GPUs PSNR↑SSIM↑LPIPS↓Time↓
Non Gaussian Splatting
LVSM (enc–dec)
8
26.48
0.901
0.065
0.78
LVSM (enc–dec)
64
29.32
0.933
0.052
0.78
LVSM (dec only)
8
27.04
0.910
0.055
2.56
LVSM (dec only)
64
32.36
0.962
0.028
2.56
Gaussian Splatting
GS-LRM
64
30.52
0.952
0.050
0.46
Ours
4
31.14
0.965
0.061
0.21
Ours+ GenDen
4
31.91
0.969
0.055
0.27
generates two Gaussian primitives at every voxel, VS-Splat
selects on average about 10% of the voxels and generates
17 primitives (one skeletal and 16 fine-grained) per selected
voxel. In addition, Fig. 4 and Table I show that VS-Splat
outperforms GeoLRM, a multi-stage method that requires 3D
geometry supervision and is prone to error propagation. These
results demonstrate that proposed E2E training facilitate high-
quality rendering. See Appendix A.V for comparisons of VS-
Splat with and without the extension for handling noisy camera
poses.
2) Comparisons of augmented feed-forward GS models for
3D object reconstruction: GenDen is an add-on to LaRa
for 3D object reconstruction, further densifying the Gaussian
primitives generated by the LaRa backbone [18]. Table I shows
that employing the proposed VS-Splat as the backbone for
GenDen (“Ours + GenDen”; see details in Appendix A.VI)
consistently outperforms using LaRa as the backbone (“LaRa
+ GenDen”) [18] across all datasets. This result indicates that
improvements in a backbone network consistently translate


9
TABLE III
ABLATION RESULTS FOR VS-SPLAT (GOBJAVERSE DATASET).
VS-Splat variants
Metrics
Coarse stage
Voxel selection
stage
Fine stage:
PAP
Fine stage:
SFR
M in (9)
PSNR↑
SSIM↑
LPIPS↓
(a)
#
×
×
×
N/A
27.80
0.938
0.096
(b)
#
#
×
×
N/A
27.81
0.938
0.095
(c)
#
#
×
#
16
28.17
0.942
0.088
(h)
#
#
#
#
16
28.40
0.944
0.084
TABLE IV
QUANTITATIVE COMPARISON OF VS-SPLAT ACROSS DIFFERENT M VALUES (GOBJAVERSE DATASET).
VS-Splat variants
Metrics
Coarse stage
Voxel selection
stage
Fine stage:
PAP
Fine stage:
SFR
M in (9)
PSNR↑
SSIM↑
LPIPS↓
(d)
#
#
#
#
1
28.28
0.942
0.087
(e)
#
#
#
#
2
28.27
0.943
0.087
(f)
#
#
#
#
4
28.34
0.943
0.085
(g)
#
#
#
#
8
28.34
0.944
0.085
(h)
#
#
#
#
16
28.40
0.944
0.084
into performance gains when coupled with the add-on model.
3) Comparisons with LVSM without constructing a ren-
derable 3D representation: Table II shows that, in general,
both proposed VS-Splat and its GenDen-augmented variant
outperform LVSM, while using substantially fewer GPU re-
sources during training and enabling significantly faster view
synthesis. While slightly underperforming the best-performing
LVSM variant, “LVSM (dec only)” trained with a large batch
size (i.e., using 64 GPUs), the augmented VS-Splat variant
achieved approximately 10× faster novel view synthesis and
consumes far fewer computational resources during training.
This is because LVSM performs a full network pass for
each target view, whereas VS-Splat an explicit 3D Gaussian
representation only once per object and renders all target views
in real-time.
C. Ablation study
To evaluate the contribution of each stage, we conducted an
ablation study of VS-Splat with the GObjaverse dataset. We
ran all experiments with learned models using 30 epochs. We
investigated the following VS-Splat variants:
• (a) only the coarse stage: using coarse primitives in every
voxel;
• (b) the combination of the coarse and voxel selection
stages: using skeletal primitives;
• (c) the combination of the coarse, voxel selection, and
fine stages, but excluding the PAP derived features in
(7);
• (d)–(g) the full model with M = 1, 2, 4, 8 fine-grained
primitives per skeletal primitive; and
• (h) the full model with M = 16 fine-grained primitives
per skeletal primitive (our default model).
We did not evaluate the full pipeline with the SFR module
removed, as this setup is not particularly informative. Without
the SFR module, we simpy use different forms of coarse
Gaussian representations, conceptually corresponding to the
setup (b).
1) Comparisons of rendering perfomance across VS-Splat
stages: This section investigates how different combinations
of stages and fine-stage designs affect the performance of VS-
Splat. Comparing (a) and (b) in Table III shows that using only
selected coarse primitives, i.e., skeletal primitives, can achieve
rendering quality comparable to using all coarse primitives.
This result implies that the proposed learnable voxel selection
approach effectively identifies object-centric voxels that can
contribute to high-quality renderings. Comparing (b) and (h)
in Table III shows that introducing the fine stage with many
fine-grained primitives leads to a significant improvement in
rendering performance compared to the variant without the fine
stage. The result suggests that fine-grained primitives serve as
an effective representation for modeling object details.
2) Comparisons between different design choices in the fine
stage: Next, we study different design choices in the fine stage
in terms of the rendering quality. Comparing (c) and (h) in Ta-
ble III shows that using PAP-derived features in (2) noticeably
improves rendering quality. Comparing the variants from (d) to
(h) in Table IV shows that progressively increasing the number
of fine-grained primitives leads to improved rendering fidelity.
See Appendix A.VII for qualitative comparisons of these fine-
stage design choices.
V. CONCLUSION
In 3D reconstruction and rendering, it is impractical to
collect 2D images from many views. Yet, it is extremely
challenging to reconstruct a 3D object from a few sparse-view
2D images. The proposed VS-Splat, a novel E2E feed-forward
GS model, can achieve high-quality renderings from sparse-
view images. Our key idea is to select voxels that are likely to
belong to an object and generate many fine-grained Gaussian
primitives at those voxels. To achieve this, we proposed a new
learnable voxel selection approach that effectively identifies
object-centric voxels without 3D supervision, and the SFR net-
work that integrates coarse and fine-grained features. Beyond
its standalone performance, VS-Splat serves as an effective
backbone for an existing densification method [18] and can


10
be extended to improve its robustness to noisy camera pose
estimates.
REFERENCES
[1] G. Stan, D. Wofk, E. Aflalo, S.-Y. Tseng, Z. Cai, M. Paulitsch, and
V. Lal, “LDM3D-VR: Latent Diffusion Model for 3D VR,” in NeurIPS
Workshop on Diffusion Models, 2023.
[2] Y. S. Jeong, H. B. Yoo, and I. Y. Chun, “DX2CT: Diffusion model for
3D CT reconstruction from bi or mono-planar 2D X-ray(s),” in Proc.
IEEE ICASSP, 2025.
[3] C. Cao, J. Ren, H. Fu, S. L¨u, B. Zhao, and X. Zhang, “Mo-
bile3DScanner: Online high-quality 3D reconstruction on Mobile de-
vices,” in Proc. IEEE/CVF CVPR, 2024, pp. 14 234–14 243.
[4] Y. Zhou, M. Wang, H. Zhang, G. Wang, and H. Yu, “Scale-GS: Scale-
aware 3D Gaussian splatting for immersive content engineering,” IEEE
Transactions on Multimedia, vol. 26, pp. 8912–8924, 2024.
[5] C.-H. Lin, J. Gao, L. Tang, P. Towghi, N. Zeng, and F. Sanja, “Magic3D:
High-resolution text-to-3D content creation,” in Proc. IEEE/CVF CVPR,
2023, pp. 300–310.
[6] B. Mildenhall, P. P. Srinivasan, M. Tancik, J. T. Barron, R. Ramamoorthi,
and R. Ng, “NeRF: Representing scenes as Neural Radiance Fields for
view synthesis,” in Proc. ECCV, 2020, pp. 99–106.
[7] B. Kerbl, G. Kopanas, T. Leimk¨uhler, and G. Drettakis, “3D Gaussian
splatting for real-time radiance field rendering,” ACM Transactions on
Graphics, vol. 42, no. 139, 2023.
[8] A. Gu´edon and V. Lepetit, “SuGaR: Surface-aligned Gaussian splatting
for efficient 3D mesh Reconstruction and high-quality mesh rendering,”
in Proc. IEEE/CVF CVPR, 2024, pp. 5354–5363.
[9] J. Cen, J. Fang, H. Chen, W. Zhang, K. Lin, and X. Wang, “Segment
anything in 3D with NeRFs,” in Proc. IEEE/CVF CVPR, 2023, pp.
12 039–12 048.
[10] S. Szymanowicz, C. Rupprecht, and V. Andrea, “Splatter Image: Ultra-
fast single-view 3D reconstruction,” in Proc. IEEE/CVF CVPR, 2024,
pp. 10 208–10 217.
[11] A. Chen, H. Xu, S. Esposito, S. Tang, and A. Geiger, “LaRa: Efficient
Large-baseline Radiance fields,” in Proc. ECCV, 2024, pp. 338–355.
[12] Y. Shen, K. Zhou, H. Wang, Y. Yang, and T. Shao, “High-fidelity
3D object generation from single image with RGBN-volume Gaussian
reconstruction model,” in Proc. IEEE/CVF CVPR, 2025, pp. 21 558–
21 569.
[13] C. Zhang, H. Song, Y. Wei, Y. Chen, J. Lu, and Y. Tang, “GeoLRM:
Geometry-aware Large Reconstruction Model for high-quality 3D Gaus-
sian generation,” in Proc. NeurIPS, 2024, pp. 55 761–55 784.
[14] D. Shi, W. Wang, D. Y. Chen, Z. Zhang, J.-W. Bian, B. Zhuang, and
C. Shen, “Revisiting depth representations for feed-forward 3D Gaussian
splatting,” in Proc. 3DV, 2026.
[15] Q. Zuo, X. Gu, Y. Dong, Z. Zhao, W. Yuan, L. Qiu, L. Bo, and Z. Dong,
“High-fidelity 3d textured shapes generation by sparse encoding and
adversarial decoding,” in Proc. ECCV, 2024.
[16] L. Downs, A. Francis, N. Koenig, B. Kinman, R. Hickman, K. Reymann,
T. B. McHugh, and V. Vanhoucke, “Google Scanned Objects: A high-
quality dataset of 3D scanned household items,” in Proc. IEEE ICRA,
2022, pp. 2553–2560.
[17] J. Reizenstein, R. Shapovalov, P. Henzler, L. Sbordone, P. Labatut,
and D. Novotny, “Common Objects in 3D: Large-scale learning and
evaluation of real-life 3D category reconstruction,” in Proc. IEEE/CVF
CVPR, 2021, pp. 10 901–10 911.
[18] S. Nam, X. Sun, G. Kang, Y. Lee, S. Oh, and E. Park, “Generative Den-
sification: Learning to densify Gaussians for high-fidelity generalizable
3D reconstruction,” in Proc. IEEE/CVF CVPR, 2025, pp. 26 683–26 693.
[19] D. Charatan, S. L. Li, A. Tagliasacchi, and V. Sitzmann, “pixelSplat:
3D Gaussian splats from image pairs for scalable generalizable 3D
reconstruction,” in Proc. IEEE/CVF CVPR, 2024, pp. 19 457–19 467.
[20] Y. Chen, H. Xu, C. Zheng, B. Zhuang, M. Pollefeys, A. Geiger, T.-
J. Cham, and J. Cai, “MVSplat: Efficient 3D Gaussian splatting from
sparse Multi-View images,” in Proc. ECCV, 2024, pp. 370–386.
[21] H. Xu, S. Peng, F. Wang, H. Blum, D. Barath, A. Geiger, and M. Polle-
feys, “DepthSplat: Connecting Gaussian splatting and depth,” in Proc.
IEEE/CVF CVPR, 2025, pp. 16 453–16 463.
[22] Y. Wan, F. Li, M. Shao, and W. Zuo, “SplatWeaver: Learning to
allocate Gaussian primitives for generalizable novel view synthesis,”
arXiv preprint arXiv:2605.07287, 2026.
[23] J. Tang, Z. Chen, X. Chen, T. Wang, G. Zeng, and Z. Liu, “LGM: Large
multi-view Gaussian Model for high-resolution 3D content creation,” in
Proc. ECCV, 2024, p. 465–484.
[24] K. Zhang, S. Bi, H. Tan, Y. Xiangli, N. Zhao, K. Sunkavalli, and Z. Xu,
“GS-LRM: Large Reconstruction Model for 3D Gaussian splatting,” in
Proc. ECCV, 2024, pp. 1–19.
[25] Z.-X. Zou, Z. Yu, Y.-C. Guo, Y. Li, D. Liang, Y.-P. Cao, and S.-H. Zhang,
“Triplane meets Gaussian splatting: Fast and generalizable single-view
3D reconstruction with transformers,” in Proc. IEEE/CVF CVPR, 2024,
pp. 10 324–10 335.
[26] X. Wang, S. Liu, G. Cao, and X. Wei, “Cascaded refinement network for
point cloud completion,” in Proc. IEEE/CVF CVPR, 2020, pp. 790–799.
[27] J. Sun, Y. Xie, L. Chen, X. Zhou, and H. Bao, “NeuralRecon: Real-time
coherent 3D reconstruction from monocular video,” in Proc. IEEE/CVF
CVPR, 2021, pp. 13 598–13 607.
[28] K. Deng, A. Liu, J.-Y. Zhu, and D. Ramanan, “Depth-supervised NeRF:
Fewer views and faster training for free,” in Proc. IEEE/CVF CVPR,
2022, pp. 12 882–12 891.
[29] H. B. Yoo, H. M. Han, S. S. Hwang, and I. Y. Chun, “Improving neural
radiance fields using near-surface sampling with point cloud generation,”
Neural Processing Letters, vol. 56, no. 214, 2024.
[30] R. Li, H. Gao, M. Tancik, and A. Kanazawa, “NerfAcc: Efficient
sampling Accelerates NeRFs,” in Proc. IEEE/CVF ICCV, 2023, pp.
18 537–18 546.
[31] Y. Bengio, N. L´eonard, and A. Courville, “Estimating or propagating
gradients through stochastic neurons for conditional computation,” arXiv
preprint arXiv:1308.3432, 2013.
[32] W. Peebles and S. Xie, “Scalable diffusion models with transformers,”
in Proc. IEEE/CVF ICCV, 2023, p. 4195–4205.
[33] V. Sitzmann, S. Rezchikov, W. T. Freeman, J. B. Tenenbaum, and
F. Durand, “Light field networks: Neural scene representations with
single-evaluation rendering,” in Proc. NeuRIPS, 2021, pp. 8505–8527.
[34] A. Vaswani, N. Shazeer, N. Parmar, J. Uszkoreit, L. Jones, A. N. Gomez,
L. u. Kaiser, and I. Polosukhin, “Attention is all you need,” in Proc.
NeurIPS, 2017, p. 5998–6008.
[35]
¨O. C¸ ic¸ek, A. Abdulkadir, S. S. Lienkamp, T. Brox, and O. Ronneberger,
“3D U-Net: Learning dense volumetric segmentation from sparse anno-
tation,” in Proc. MICCAI, 2016, pp. 424–432.
[36] B. Huang, Z. Yu, A. Chen, A. Geiger, and S. Gao, “2D Gaussian
splatting for geometrically accurate radiance fields,” in Proc. ACM
SIGGRAPH, 2024, pp. 1–11.
[37] Z. Fan, K. Wang, K. Wen, Z. Zhu, D. Xu, and Z. Wang, “LightGaussian:
Unbounded 3D Gaussian compression with 15x reduction and 200+
FPS,” in Proc. NeuRIPS, 2024, pp. 140 138–140 158.
[38] E. Jang, S. Gu, and B. Poole, “Categorical reparameterization with
Gumbel-Softmax,” in Proc. ICLR, 2017.
[39] Z. Zhang, T. Song, Y. Lee, L. Yang, C. Peng, R. Chellappa, and
D. Fan, “LP-3DGS: Learning to Prune 3D Gaussian splatting,” in Proc.
NeuRIPS, 2024, pp. 122 434–122 457.
[40] E. J. Gumbel, Statistical Theory of Extreme Values and Some Practical
Applications: A Series of Lectures.
U.S. Government Printing Office,
1954, no. 33.
[41] W. Wydma´nski and M. ´Smieja, “GFSNetwork: Differentiable fea-
ture
selection
via
Gumbel-Sigmoid
relaxation,”
arXiv
preprint
arXiv:2503.13304, 2025.
[42] K. He, X. Zhang, S. Ren, and J. Sun, “Deep residual learning for image
recognition,” in Proc. IEEE CVPR, 2016, pp. 770–778.
[43] Y. Na, T. Kim, and S.-E. Yoon, “SHARE: Shape-Ray Estimation for
camera pose-free multi-view 3D reconstruction,” in Proc. IEEE ICIP,
2025, pp. 1–5.
[44] A. Chen, Z. Xu, F. Zhao, X. Zhang, F. Xiang, J. Yu, and H. Su, “MVS-
NeRF: Fast generalizable radiance field reconstruction from Multi-View
Stereo,” in Proc. IEEE/CVF ICCV, 2021, pp. 14 124–14 133.
[45] H. Xu, A. Chen, Y. Chen, C. Sakaridis, Y. Zhang, M. Pollefeys,
A. Geiger, and F. Yu, “MuRF: Multi-baseline Radiance Fields,” in Proc.
IEEE/CVF CVPR, 2024, pp. 20 041–20 050.
[46] H. Jin, H. Jiang, H. Tan, K. Zhang, S. Bi, T. Zhang, F. Luan, N. Snavely,
and Z. Xu, “LVSM: A Large View Synthesis Model with minimal 3D
inductive bias,” in Proc. ICLR, 2025.
[47] Z. Wang, A. Bovik, H. Sheikh, and E. Simoncelli, “Image quality assess-
ment: From error visibility to structural similarity,” IEEE Transactions
on Image Processing, vol. 13, no. 4, pp. 600–612, 2004.
[48] R. Zhang, P. Isola, A. A. Efros, E. Shechtman, and O. Wang, “The
unreasonable effectiveness of deep features as a perceptual metric,” in
Proc. IEEE CVPR, 2018, pp. 586–595.


11
VS-Splat: Voxel-Selective feed-forward Gaussian
Splatting for end-to-end 3D object reconstruction from
sparse-views (Appendix)
This appendix consists of seven sections:
• Section A.I compares different voxel selection variants in
VS-Splat,
• Section A.II describes the architecture of the Selective
Feature Refinement (SFR) module,
• Section A.III describes detailed experimental setup,
• Section A.IV compares inference speed with LaRa [1],
• Section A.V presents comparisons of VS-Splat with and
without the extension in Section III-F for handling noisy
camera poses,
• Section A.VI presents the implementation details and
qualitative results for VS-Splat augmented with GenDen
[2],
• Section A.VII presents qualitative comparisons of fine-
stage design choices in VS-Splat.
A.I. COMPARISONS BETWEEN DIFFERENT VOXEL SELEC-
TION VARIANTS IN PROPOSED VS-SPLAT
This section compares different voxel selection variants
inspired by existing selection methods, in the proposed VS-
Splat framework. We evaluated them in terms of rendering
quality and the average number of selected voxels with the
CO3D dataset. CO3D is a real-world dataset with estimated
camera poses, so it is challenging to accurately select object-
centric voxels. We ran all experiments with learned models
with 30 epochs.
We modified the voxel selection variant of VS-Splat with
the following selection schemes:
• (a) opacity-based thresholding (OBT), using the prede-
fined threshold of 0.005 as in [1];
• (b) predicting confidence scores and applying STE to
enable gradient flow to the scores, as in [2];
• (c) replacing the Gumbel-Sigmoid in (4) with the deter-
ministic Sigmoid function; and
• (d) our proposed VS-Splat that predicts confidence scores
via Gumbel-Sigmoid and uses them to refine opacity
values.
Note that in (a), we carefully tuned the opacity threshold
value (by evaluating inference performances) and observed
that 0.005 yielded the best rendering performance among the
candidates {0, 0.0025, 0.005, 0.0075, 0.01, 0.02, 0.05, 0.1}. In-
terestingly, excessive generation of fine-grained primitives in
non-object (background) regions degraded rendering quality.
We observed this from experiments with the opacity threshold
of 0 that corresponds to the setup of generating many fine-
grained primitives across all voxels.
Comparing (a) and (d) in Table A.I demonstrates that the
proposed learnable voxel selection approach outperforms the
simple OBT scheme across all metrics, while using approx-
imately 15% fewer voxels compared to OBT. The corre-
sponding visualizations in Fig. A.1 show that VS-Splat is
TABLE A.I
COMPARISONS BETWEEN DIFFERENT VOXEL SELECTION VARIANTS IN
THE PROPOSED VS-SPLAT FRAMEWORK (THE CO3D DATASET).
Variants
PSNR↑SSIM↑LPIPS↓# of voxels
(a) OBT
21.21
0.859
0.223
22,867
(b) STE
14.71
0.814
0.317
121
(c) Sigmoid
18.01
0.835
0.288
2,223
(d) Ours
21.31
0.863
0.213
19,587
(a) OBT
(b) STE
(c) Sigmoid
(d) Ours
Ground-truth
Fig. A.1.
Visualizations of selected voxels using different voxel selection
variants.
significantly better at selecting object-centric voxels compared
to OBT. In particular, Fig. A.1(a) implies that the simple
OBT scheme is unreliable in selecting object-centric voxels,
corresponding to the argument in [3]. Note that in the OBT
scheme, one needs to manually tune a predefined threshold.
Fig. A.1(b) and Table A.I(b) show that simply applying
STE to the confidence scores results in selecting an extremely
limited number of voxels. In contrast, our approach enables
significantly more appropriate voxel selection and higher-
quality renderings over the STE scheme. This implies that
integrating predicted voxel-selection scores into the rendering
process (as proposed) is effective in selecting object-centric
voxels. Compare (b) and (d) in Fig. A.1 and Table A.I.
Fig. A.1(c) and Table A.I (c) show that using the standard
Sigmoid function instead of Gumbel-Sigmoid in (4) fails in
properly selecting object-centric voxels, leading to significant
rendering quality drops. Comparing (c) and (d) in Fig. A.1
and Table A.I demonstrates that the binary approximation in a
stochastic way via Gumbel-Sigmoid encourages the network
select object-voxels in an appropriate manner. Fig. A.1 and
Table A.I show that the proposed selection approach better
identifies voxels that are likely to belong to an object, com-
pared to the alternative variants derived from existing selection
methods.
A.II. ARCHITECTURE OF SFR MODULE
This section describes the Selective Feature Refinement
(SFR) module proposed in (8). To operate exclusively on
selected voxels, we build the SFR module with sparse 3D
convolutional layers that compute 3D convolution only at
non-empty locations [4], [5]. The SFR module consists of
fully-connected layer, R residual convolutional blocks with
sparse convolution, and a sparse 3D convolutional layer. Each
residual convolutional block consists of two sequential sparse


12
Fig. A.2.
Architecture of the proposed SFR module. “Sparse 3D conv.”
denotes a sparse 3D convolutional layer, and “Residual conv. block” denotes
a residual convolutional block. The symbol c indicates channel-wise con-
catenation. See further details in Section A.III-B.
3D convolutional layers – each preceded by normalization and
Swish activation [6] – and has a skip connection.
For each selected voxel (h, w, d) ∈I in (6), the SFR takes
the unified coarse feature ˜vh,w,d in (2), the fine detail feature
ˆvh,w,d in (7), and the position-encoded skeletal primitive
position γ(µcrs
h,w,d). The SFR module then concatenates these
inputs in a channel-wise manner and ultimately generate the
refined object-centric feature f +
h,w,d in (8); see Section III-D2.
Figure A.2 illustrates the architecture of the proposed SFR
module.
A.III. EXPERIMENTAL SETUPS
A. Datasets
Across all the datasets, we used multi-view images of each
object with a resolution of 512×512. In inference, we input 2D
images from four different views, and evaluated each model
with rendered 2D images from four novel views. We trained
and evaluated VS-Splat with the large-scale GObjaverse [7]
dataset, following the preprocessing of [1], which comprises
238,297 training and 26,478 evaluation objects. To assess the
cross-domain generalization capability, we evaluated VS-Splat
with a subset of the Google Scanned Objects (GSO) [8] and
the Common Objects in 3D (CO3D) [9]. GSO comprises
1,030 objects following the preprocessing of [1], and CO3D
comprises 139 objects (47 hydrants and 92 teddy bears)
following the preprocessing of [2].
B. Implementation details
We adopted the Gaussian representation in 3DGS [10]. We
set the voxel grid resolution to H = W = D = 48, and
generated M = 16 fine-grained primitives for each selected
voxel. We linearly annealed the Gumbel-Sigmoid temperature
τ from 1.0 to 0.1 over the first 50,000 iterations in training,
and fixed it as 0.1 for the remaining iterations in training, and
inference.
In extracting 2D feature maps from each input 2D images
(see Section III-B1), we employed DINOv1 [11], pre-trained
with the ImageNet-1K dataset [12]. The 3D convolutional
compressor C (see Section III-B2) reduces the channel dimen-
sion from 2C = 1,536 to ˜C = 128. The SFR module in (8)
contains R = 2 residual convolutional blocks and produces
TABLE A.II
INFERENCE TIME COMPARISON BETWEEN LARA AND PROPOSED
VS-SPLAT WITH THREE DIFFERENT DATASETS (IN SECONDS).
Methods
GObjaverse
GSO
CO3D
LaRa
0.146
0.151
0.156
Ours
0.114
0.139
0.138
features with a channel dimension of 512. In total, VS-Splat
consists of approximately 128 million trainable parameters.
For
the
extension
in
Section
III-F
to
handle
noisy
camera poses, we followed the official implementation of
SHARE [13], except for a modification to its input module.
Specifically, we used three 1 × 1 2D convolutional layers
to reduce the channel dimension of the feature maps iN
n=1
extracted in Section III-B1 from 768 to 128, matching the input
dimension of SHARE. Each convolutional layer is preceded
by a normalization layer and a Swish activation [6]. Due to
GPU memory constraints, we set M = 8 for this extension.
We trained VS-Splat and its extension for 50 epochs with
four NVIDIA A100 80GB GPUs, using a batch size of 4 per
GPU. We used the AdamW optimizer with a learning rate
of 4 × 10−4 and applied cosine learning rate scheduling. We
implemented all experiments in Pytorch 2.3.1 [14], and fixed
the random seed to 42.
C. Details of experimental setups for Table II
We report only the GSO results of GS-LRM [15] and LVSM
[16], since neither method releases public weights nor reports
results on GObjaverse and CO3D. For the proposed VS-Splat,
we used DINOv3 pre-trained with LVD-1689M [17] as the 2D
feature extractor.
Following the GS-LRM and LVSM setup, we selected four
2D images from different views, and evaluated our models
using rendered 2D images from randomly selected 10 novel
views. For input view selection, we followed the process in
[2]. We clustered camera viewpoints into four groups using the
classical K-means algorithm and selected the cluster centers
as input views to ensure an even distribution around the object.
We measured inference speed as the wall-clock time (in
seconds) required to process four input images and render 10
novel views.
.
A.IV. INFERENCE SPEED COMPARISONS WITH LARA
We compared the inference speed of proposed VS-Splat
particularly with that of LaRa [1], using the GObjaverse, GSO,
and CO3D datasets. For each model, we measured the time
taken to process four input images and to generate rendered
images for four input viewpoints and four novel viewpoints.
Table A.II shows that VS-Splat is faster than LaRa across all
the benchmark datasets, achieving about a 1.3× speedup with
the GObjaverse dataset. See related discussion of these results
in Section IV-B1.
1“Ours” and “Ours + GenDen” denote VS-Splat and GenDen-augmented
VS-Splat, respectively, both trained with the base loss in (12). “Oursext”
and “Oursext + GenDen” denote the corresponding models incorporating the
extension in Section III-F, trained with the extended loss in (13).


13
TABLE A.III
QUANTITATIVE COMPARISONS OF VS-SPLAT AND ITS
GENDEN-AUGMENTED VARIANT WITH THE EXTENSION FOR HANDLING
NOISY CAMERA POSES (CO3D DATASET).1
Variants
PSNR↑SSIM↑LPIPS↓
Ours
21.31
0.863
0.213
Oursext
21.62
0.864
0.211
Ours + GenDen
22.09
0.865
0.208
Oursext + GenDen
22.29
0.867
0.207
Ground-truth
Ours
Oursext
Ours + GenDen
Oursext + GenDen
Fig. A.3.
Additional qualitative comparisons between VS-Splat and its
extension in Section III-F for handling noisy camera poses (CO3D dataset).
A.V. COMPARISONS BETWEEN WITH AND WITHOUT THE
EXTENSION FOR HANDLING NOISY CAMERA POSES IN VS-
SPLAT
This section compares VS-Splat with and without the ex-
tension in Section III-F for handling noisy camera poses on
the CO3D dataset [9].
A comparison of the results in Tables I and A.III shows
that VS-Splat outperforms the six existing GS- and NeRF-
based methods on the CO3D dataset [9]. This advantage
persists when GenDen is applied, as GenDen-augmented VS-
Splat (“Ours + GenDen” in Table A.III) outperforms “LaRa
+ GenDen” in Table I. However, the performance gains are
smaller on CO3D than on GObjaverse and GSO [7], [8],
likely because CO3D provides less accurate camera pose
estimates, whereas GObjaverse and GSO provide accurate
camera poses. Table A.III further shows that the extended
variants (“Oursext” and “Oursext + GenDen”) outperform their
respective counterparts without the extension (“Ours” and
“Ours + GenDen”). These results demonstrate that VS-Splat
can be effectively extended to improve its robustness to noisy
camera poses.
Fig. A.3 presents qualitative comparisons of renderings
produced by VS-Splat and GenDen-augmented VS-Splat, both
with and without the extension described in Section III-F,
on the CO3D dataset [9]. Consistent with the quantitative
results in Table A.III, the extended models (“Oursext” and
“Oursext + GenDen”) produce fewer artifacts and higher-
quality renderings than their respective counterparts without
the extension (“Ours” and “Ours + GenDen”).
LaRa + GenDen
Ours + GenDen
Ground-truth
Fig. A.4.
Qualitative comparisons between VS-Splat and LaRa, both aug-
mented with GenDen (GObjaverse dataset).
A.VI. AUGMENTING VS-SPLAT WITH AN ADD-ON MODEL
This section describes the implementation details for in-
tegrating the GenDen add-on model [2] into VS-Splat, and
provides qualitative comparisons of rendered images obtained
using VS-Splat and LaRa as backbone models within GenDen.
A. Implementation details
When using VS-Splat as the backbone, we set M = 8
rather than M = 16, due to GPU memory constraints. All
other implementation details are the same as those in Sec-
tion A.III-B. We followed the official GenDen implementation,
replacing the original LaRa backbone with VS-Splat and
adjusting input feature dimension of GenDen to match VS-
Splat’s output feature dimension. We jointly trained the VS-
Splat–GenDen model from scratch for 50 epochs and evaluated
its performance.
B. Qualitative results
Fig. A.4 provides qualitative comparisons of rendered im-
ages using VS-Splat and LaRa as backbones for GenDen.
In line with the quantitative results reported in Table I,
Fig. A.4 demonstrates that GenDen achieves better rendering
quality when built on VS-Splat rather than LaRa. This result
demonstrates that VS-Splat can serve as an effective backbone
for the GenDen densification method.
A.VII. QUALITATIVE COMPARISONS OF FINE-STAGE DE-
SIGN CHOICES
Fig. A.5 presents qualitative comparisons that complement
the quantitative results in Tables III and IV. A comparison of
panels Fig. A.5(c) and Fig. A.5(h) shows that incorporating
the PAP-derived features {ˆvh,w,d : (h, w, d) ∈I} in (7)
improves rendering quality, with Fig. A.5(h) reconstructing
fine local details more accurately than Fig. A.5(c). This result
indicates that the PAP-derived features provide useful fine-
grained information for high-fidelity rendering.
Fig. A.5(d)–(h) further shows that increasing the number of
fine-grained Gaussian primitives (M) progressively improves
the clarity of fine details, consistent with the quantitative
results in Table IV.


14
Ground-truth
(a) Coarse primitives
(b) Skeletal primitives
(c) W/o PAP driven
features (M = 16)
(d) Full model (M = 1)
(f) Full model (M = 4)
(h) Full model
(M = 16; default)
Fig. A.5. Qualitative comparisons between different VS-Splat variants (GObjaverse dataset). We described the VS-Splat variants (a)-(h) in Section IV-C.
REFERENCES
[1] A. Chen, H. Xu, S. Esposito, S. Tang, and A. Geiger, “LaRa: Efficient
Large-baseline Radiance fields,” in Proc. ECCV, 2024, pp. 338–355.
[2] S. Nam, X. Sun, G. Kang, Y. Lee, S. Oh, and E. Park, “Generative Den-
sification: Learning to densify Gaussians for high-fidelity generalizable
3D reconstruction,” in Proc. IEEE/CVF CVPR, 2025, pp. 26 683–26 693.
[3] Z. Fan, K. Wang, K. Wen, Z. Zhu, D. Xu, and Z. Wang, “LightGaussian:
Unbounded 3D Gaussian compression with 15x reduction and 200+
FPS,” in Proc. NeuRIPS, 2024, pp. 140 138–140 158.
[4] B. Liu, M. Wang, H. Foroosh, M. Tappen, and M. Pensky, “Sparse
convolutional neural networks,” in Pro. IEEE CVPR, 2015, p. 806–814.
[5] B. Graham, M. Engelcke, and L. van der Maaten, “3D semantic
segmantation with submanifold sparse convolutional networks,” in Proc.
IEEE/CVF CVPR, 2018, pp. 9224–9232.
[6] P. Ramachandran, B. Zoph, and Q. V. Le, “Searching for activation
functions,” arXiv preprint arXiv:1710.05941, 2017.
[7] Q. Zuo, X. Gu, Y. Dong, Z. Zhao, W. Yuan, L. Qiu, L. Bo, and Z. Dong,
“High-fidelity 3d textured shapes generation by sparse encoding and
adversarial decoding,” in Proc. ECCV, 2024.
[8] L. Downs, A. Francis, N. Koenig, B. Kinman, R. Hickman, K. Reymann,
T. B. McHugh, and V. Vanhoucke, “Google Scanned Objects: A high-
quality dataset of 3D scanned household items,” in Proc. IEEE ICRA,
2022, pp. 2553–2560.
[9] J. Reizenstein, R. Shapovalov, P. Henzler, L. Sbordone, P. Labatut,
and D. Novotny, “Common Objects in 3D: Large-scale learning and
evaluation of real-life 3D category reconstruction,” in Proc. IEEE/CVF
CVPR, 2021, pp. 10 901–10 911.
[10] B. Kerbl, G. Kopanas, T. Leimk¨uhler, and G. Drettakis, “3D Gaussian
splatting for real-time radiance field rendering,” ACM Transactions on
Graphics, vol. 42, no. 139, 2023.
[11] M. Caron, H. Touvron, I. Misra, H. J´egou, J. Mairal, P. Bojanowski, and
A. Joulin, “Emerging properties in self-supervised vision transformers,”
in Proc. IEEE/CVF ICCV, 2021, pp. 9650–9660.
[12] J. Deng, W. Dong, R. Socher, L.-J. Li, K. Li, and L. Fei-Fei, “ImageNet:
A large-scale hierarchical image database,” in Proc. IEEE CVPR, 2009,
pp. 248–255.
[13] Y. Na, T. Kim, and S.-E. Yoon, “SHARE: Shape-Ray Estimation for
camera pose-free multi-view 3D reconstruction,” in Proc. IEEE ICIP,
2025, pp. 1–5.
[14] A. Paszke, S. Gross, F. Massa, A. Lerer, J. Bradbury, G. Chanan,
T. Killeen, Z. Lin, N. Gimelshein, L. Antiga, A. Desmaison, A. K¨opf,
E. Yang, Z. DeVito, M. Raison, A. Tejani, S. Chilamkurthy, B. Steiner,
L. Fang, J. Bai, and S. Chintala, “PyTorch: an imperative style, high-
performance deep learning library,” in Proc. NeuRIPS, 2019, pp. 8026–
8037.
[15] K. Zhang, S. Bi, H. Tan, Y. Xiangli, N. Zhao, K. Sunkavalli, and Z. Xu,
“GS-LRM: Large Reconstruction Model for 3D Gaussian splatting,” in
Proc. ECCV, 2024, pp. 1–19.
[16] H. Jin, H. Jiang, H. Tan, K. Zhang, S. Bi, T. Zhang, F. Luan, N. Snavely,
and Z. Xu, “LVSM: A Large View Synthesis Model with minimal 3D
inductive bias,” in Proc. ICLR, 2025.
[17] O. Sim´eoni, H. V. Vo, M. Seitzer, F. Baldassarre, M. Oquab, C. Jose,
V. Khalidov, M. Szafraniec, S. Yi, M. Ramamonjisoa, F. Massa, D. Haz-
iza, L. Wehrstedt, J. Wang, T. Darcet, T. Moutakanni, L. Sentana,
C. Roberts, A. Vedaldi, J. Tolan, J. Brandt, C. Couprie, J. Mairal,
H. J´egou, P. Labatut, and P. Bojanowski, “DINOv3,” arXiv preprint
arXiv:2508.10104, 2025.
