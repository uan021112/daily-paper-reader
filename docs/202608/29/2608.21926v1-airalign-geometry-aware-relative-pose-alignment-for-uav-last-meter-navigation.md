---
title: "AirAlign: Geometry-Aware Relative Pose Alignment for UAV Last-Meter Navigation"
title_zh: AirAlign：面向无人机最后阶段导航的几何感知相对位姿对齐
authors: "Jinyi Zhou, Shuo Feng, Yufei Wu, Piji Li"
date: 2026-08-22
pdf: "https://arxiv.org/pdf/2608.21926v1"
tags: ["query:feed-forward"]
score: 8.0
evidence: 使用预训练视觉几何重建模型作为主干进行相对位姿对齐
tldr: 无人机低空导航在最后进近阶段需要高精度姿态对齐，但视角和外观剧烈变化带来挑战。本文提出AirAlign框架，利用预训练几何重建模型提取几何感知特征，并通过场景不重叠的数据划分进行交叉验证与模型选择，推理时集成多个模型提升鲁棒性。在PairUAV挑战赛上验证了有效性，消融实验证明各组件均有贡献。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.21926v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1612, \"height\": 963, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.21926v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 871, \"height\": 495, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.21926v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 895, \"height\": 625, \"label\": \"Table\"}]"
motivation: 最后一米导航中，无人机面临极端视角和外观变化，使得图像对相对姿态对齐极为困难。
method: 采用预训练视觉几何重建模型作为骨干，结合场景分离的交叉验证与模型选择，推理时对多个模型输出取平均。
result: 在PairUAV挑战赛数据集上取得准确且鲁棒的姿态对齐效果，消融实验确认各设计均有效。
conclusion: 提出几何感知的RGB姿态对齐框架，为无人机末端导航提供可靠方案，并验证集成策略的有效性。
---

## 摘要
现代低空环境中的无人机导航在最终接近阶段需要更精确的位姿对齐，以便获取目标信息或执行操作，这使得“最后阶段”导航变得越来越重要。然而，严重的视点和外观变化使这项任务具有挑战性。为了解决这一问题，我们提出了AirAlign，一个用于无人机RGB图像对相对位姿对齐的框架。AirAlign使用预训练的视觉几何重建模型作为主干，从源-目标图像对中提取几何感知特征。此外，为了更好地利用有限的训练数据，我们将训练集划分为多个场景不相交的折（folds），用于未见场景的交叉验证和模型选择。在推理过程中，所选模型的预测被平均，形成整个框架的集成输出。在ACMMM 2026多媒体无人机研讨会上的PairUAV挑战赛中的实验证明了我们方法的有效性和鲁棒性，同时全面的消融研究验证了每个组件的贡献。

## Abstract
Unmanned aerial vehicle (UAV) navigation in modern low-altitude environments requires more accurate pose alignment in the final approach stage for target information acquisition or manipulation, making "last-meter" navigation increasingly important. However, severe viewpoint and appearance variations make this task challenging. To tackle this problem, we propose AirAlign, a framework for RGB-only image-pair relative pose alignment for UAVs. AirAlign uses a pretrained visual geometry reconstruction model as the backbone to extract geometry-aware features from source-target image pairs. In addition, to better utilize the limited training data, we split the training set into multiple scene-disjoint folds for unseen cross-validation and model selection. During inference, the predictions of the selected models are averaged to form the ensemble output of the overall framework. Experiments on the PairUAV challenge at the ACMMM 2026 Workshop on UAVs in Multimedia demonstrate the effectiveness and robustness of our method, while comprehensive ablation studies validate the contribution of each component.

---

## 论文详细总结（自动生成）

## 中文总结

### 论文核心问题与整体含义（研究动机和背景）

- **任务定义**：本文研究无人机（UAV）"最后一米导航"（last-meter navigation）中的图像对相对位姿对齐问题，即从源—目标RGB图像对中预测相对平移量和相对航向角。
- **核心挑战**：
  - GNSS等传统定位手段在近距离精细位姿估计中精度不足；
  - 源图像和目标图像存在显著的视角与外观差异（不同高度、不同航向），建筑物外观和空间布局变化剧烈；
  - 设定仅依赖RGB图像，无深度传感器或辅助模态，难以显式捕获三维几何关系。
- **与现有工作的区别**：现有研究（如跨视角定位、图像检索）主要聚焦于地点级匹配，无法提供最终进近阶段所需的几何级相对位姿信息。本文直接针对图像对的相对位姿回归问题，补充了这一研究空白。

### 论文提出的方法论

**核心思想**：利用预训练的视觉几何重建模型提取几何感知特征，配合场景分离的交叉验证训练策略和多模型集成推理，实现高效、鲁棒的RGB图像对相对位姿预测。

**关键技术细节**：

1. **几何感知特征提取**：
   - 采用π³模型作为骨干网络，包含DINOv2编码器、交替的视图级与全局自注意力层，以及两个专用解码器（相机位姿解码器和3D点图解码器）；
   - 源-目标图像对经骨干网络得到隐藏几何特征 H_s、H_t，再经由解码器生成相机位姿特征 c_s、c_t 和点图特征 p_s、p_t；
   - 通过特征拼接得到 pair-level 表示：f_cam = [c_s; c_t]，f_pts = [p_s; p_t]。

2. **双任务预测头**：
   - **航向预测头**：仅使用相机位姿特征 f_cam，输出为二维单位方向向量（经L2归一化），避免直接回归角度的训练复杂度；
   - **平移预测头**：拼接 f_cam 和 f_pts 两种特征，经过MLP后使用 sigmoid 函数映射到归一化范围。

3. **训练策略**：
   - 将训练集按场景不重叠原则划分为多个折（fold），轮流作为验证集进行模型选择和 checkpoint 筛选；
   - 训练时冻结 π³ 骨干网络，仅优化预测头；
   - 损失函数由四部分组成：
     - 航向的 smooth L1 损失；
     - 相对角度误差损失（辅助项，使用360°循环差值的归一化误差）；
     - 平移的 smooth L1 损失；
     - 相对距离误差损失（辅助项，使用去归一化后预测值与真实值的相对误差）；
   - 总体损失：L = L_head + λ_ang·L_rel-ang + L_trans + λ_dist·L_rel-dist。

4. **推理集成**：将多个训练好的模型预测结果进行平均——航向角通过平均二维方向向量后使用 atan2 恢复；平移量直接对各模型去归一化后的预测取平均。

### 实验设计

- **数据集/场景**：使用 PairUAV 挑战赛（ACMMM 2026 UAVs in Multimedia Workshop）提供的训练集和测试集；
- **Benchmark**：PairUAV 挑战赛官方排行榜，评估指标包括距离相对误差和角度相对误差，最终分数为两者平均值；
- **对比方法**：
  - 官方 baseline（分数 0.633957，远差于所有参赛队伍）；
  - 排行榜前5名队伍（light、liang、ze_rong、g2502795c、Team）以及其他参赛队伍；
- **消融实验**：设计了3组消融研究，分别考察折数、辅助损失和输入特征设计的影响（详见下一节）。

### 资源与算力

- 论文正文中**未明确说明**使用的GPU型号、数量及训练总时长；
- 仅提供了以下训练相关参数：输入分辨率 448×448、batch size 2048、每轮训练 550 epochs、AdamW 优化器、初始学习率 2×10⁻³。

### 实验数量与充分性

- **实验总数**：除主实验结果外，共设计3组消融实验，且每组包含2–3个子配置，整体实验规模较小；
- **有效性评估**：
  - 消融实验覆盖了方法的关键设计维度（折数、损失函数、特征输入），对每个组件的贡献给出了量化验证；
  - 结果显示：5折集成最优；辅助相对损失对性能有明显贡献；航向预测仅需相机位姿特征，平移预测则需要两种特征组合。
- **不足**：
  - 仅在单一数据集（PairUAV）上验证，缺乏跨数据集泛化实验；
  - 未与其他发表的重建后回归方法（如直接用MASt3R、DUSt3R等）进行对比；
  - 排行榜对比为比赛形式的间接比较，非受控条件下的严格对照实验。

### 论文的主要结论与发现

- AirAlign 在 PairUAV 挑战赛测试集上取得最终分数 0.002790（距离相对误差 0.003175、角度相对误差 0.002405），显著优于官方 baseline（0.633957），位列排行榜第6名；
- 预训练视觉几何重建模型的特征对于图像对相对位姿预测具有显著价值；
- 场景分离的交叉验证与多模型集成能有效提升预测精度与稳定性；
- 不同预测任务需要不同级别的几何特征——航向预测重点依赖相机位姿特征，平移预测则需要相机位姿特征与点图特征的联合输入；
- 辅助相对误差损失能够有效提升模型对相对量预测的精度。

### 优点

1. **问题定位精准**：聚焦于"最后阶段"导航中常被忽视的相对位姿对齐问题，弥补了地点级检索与精细位姿估计之间的空白；
2. **利用预训练模型优势**：采用冻结的视觉几何重建骨干网络，以少量数据即可获得良好的几何感知表征，训练成本低；
3. **巧妙的数据划分策略**：场景不相交的交叉验证解决了挑战赛训练数据有限的问题，在模型选择的同时实现了多模型集成；
4. **轻量且有效**：仅依赖RGB输入，推理时无需深度传感器，适合机载部署；
5. **消融实验设计清晰**：每组消融都有明确的结论，验证了各设计模块的贡献与必要性。

### 不足与局限

1. **实验覆盖有限**：仅在 PairUAV 单一数据集上验证，缺乏在多个数据集或多场景类型上的泛化评估；
2. **算力信息缺失**：未报告GPU型号、数量及训练时间，难以为他人复现提供参考；
3. **对比方法不足**：未与端到端位姿回归、传统特征匹配+SfM、其他几何重建模型（如DUSt3R/MASt3R）等方法进行公平对比；
4. **模型集成带来的推理开销**：虽然骨干网络是共享的，但多个模型（3–7个）的集成推理会增加整体计算负担，论文未讨论其在实时飞行场景中的可行性；
5. **平移量归一化依赖数据范围**：归一化参数（t_min=-132, t_max=132）直接沿用官方 baseline，可能不适用于不同飞行高度或距离分布的新场景，存在域偏移风险。

（完）
