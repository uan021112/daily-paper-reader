---
title: "Depth Anything V4: Dynamic 4D Scene Reconstruction via Riemannian Flow Matching on 4D Gaussian Splatting"
title_zh: 深度万物V4：基于4D高斯泼溅的黎曼流匹配动态4D场景重建
authors: "Jiaming Fan, Jian Lu, Jinling Jia, Chenbin Zhang"
date: 2026-08-18
pdf: "https://arxiv.org/pdf/2608.18388v2"
tags: ["query:feed-forward"]
score: 9.0
evidence: 在四维高斯泼溅上应用黎曼流匹配进行动态四维场景重建，是几何三维视觉模型的显著改进
tldr: 从单目视频重建动态4D场景是具挑战性任务。本文提出Depth Anything V4（DAV4），将黎曼流匹配（RFM）应用于4D高斯溅射参数，在尺度、旋转、不透明度等非欧几里得流形上定义概率路径，确保中间状态有效。对照实验显示，相比同架构的确定性MLP基线，RFM使F-score从0.762提升至0.806（+0.044），且无需人工深度标签。DAV4在动态重建和新视图合成上超越先前模型，预训练成本可摊销于大规模部署。
source: arxiv
selection_source: fresh_fetch
motivation: 4D高斯参数位于非欧几里得空间，现有回归或优化方法易产生无效状态，缺乏流形感知的生成式建模，且过度依赖人工深度标签。
method: 提出DAV4，将黎曼流匹配应用于4D高斯溅射的尺度、旋转和不透明度参数，在流形上定义概率路径，并结合测试时优化与大规模预训练。
result: 隔离实验表明RFM贡献F-score提升+0.044（0.806 vs 0.762），优于先前模型，无需人工深度标签；预训练360GPU小时可摊销于超万场景。
conclusion: DAV4验证了流形感知的概率生成方法在动态重建中的有效性，为无监督大规模4D场景重建提供了新思路。
---

## 摘要
我们提出了深度万物V4（DAV4），一种从单目视频进行动态4D场景重建的框架。我们的关键贡献是将黎曼流匹配（RFM）应用于4D高斯泼溅参数，直接在非欧几里得流形（尺度、旋转、不透明度）上定义概率路径，确保所有中间状态都是有效的。通过受控实验，我们将RFM的贡献与测试时优化（TTO）和预训练分离开来。使用相同数据、架构和TTO的确定性MLP基线实现了0.762的F分数；RFM实现了0.806——+0.044的提升是RFM的独立贡献。我们提供了修正的计算成本分析：预训练需要360个GPU小时，大规模部署（超过10,000个场景）可摊销。不确定性通过负高斯对数似然和期望校准误差进行量化。DAV4在动态重建和新视角合成方面优于先前的深度万物模型和逐场景4D-GS，同时不使用人工标注的深度标签作为训练损失。

## Abstract
We present Depth Anything V4 (DAV4), a framework for dynamic 4D scene reconstruction from monocular video. Our key contribution is the application of Riemannian Flow Matching (RFM) to 4D Gaussian Splatting parameters, defining probability paths directly on non-Euclidean manifolds (scale, rotation, opacity), ensuring all intermediate states are valid. Through controlled experiments, we isolate RFM's contribution from test-time optimization (TTO) and pre-training. A deterministic MLP baseline with the same data, architecture, and TTO achieves F-score 0.762; RFM achieves 0.806 - the +0.044 gain is RFM's isolated contribution. We provide corrected computational cost analysis: pre-training is 360 GPU-hours, amortizing for large-scale deployment (over 10,000 scenes). Uncertainty is quantified via Negative Gaussian Log-Likelihood and Expected Calibration Error. DAV4 outperforms prior Depth Anything models and per-scene 4D-GS on dynamic reconstruction and novel-view synthesis, while using no human-annotated depth labels as training losses.

---

## 论文详细总结（自动生成）

# 《Depth Anything V4: 基于4D高斯泼溅的黎曼流匹配动态4D场景重建》论文总结

## 1. 论文的核心问题与整体含义
- **研究动机**：从单目视频重建动态4D场景是计算机视觉/图形学中的挑战性任务。现有方法要么采用确定性回归或逐场景优化，要么过度依赖人工标注的深度标签，难以在非欧几里得空间中保证高斯泼溅参数（尺度、旋转、不透明度）的中间状态有效性。
- **核心问题**：4D高斯参数位于非欧几里得流形上，直接使用欧氏空间中的概率建模或回归会产生无效状态，缺乏流形感知的生成式建模；同时许多方法在训练时需要人工深度监督，限制了可扩展性。
- **整体含义**：论文提出 Depth Anything V4（DAV4），首次将黎曼流匹配（Riemannian Flow Matching, RFM）应用于4D高斯泼溅参数，在流形上直接定义概率路径，实现无需人工深度标签的动态场景重建，并为无监督大规模4D重建提供了新的生成式建模范式。

## 2. 论文提出的方法论
- **核心思想**：将动态4D场景重建建模为流形上的概率生成问题，利用黎曼流匹配在高斯泼溅的尺度、旋转和不透明度等非欧几里得参数空间上定义概率路径，使所有中间状态均落在有效流形上。
- **关键技术细节**：
  - 使用 **4D高斯泼溅（4D Gaussian Splatting）** 作为场景表示，每个高斯包含位置、尺度（正实数流形）、旋转（四元数/单位四元数流形）、不透明度（（0,1）区间）等参数。
  - 在 **黎曼流形** 上定义插值和概率路径，而非欧氏线性插值，确保生成的中间高斯参数始终有效。
  - 采用 **黎曼流匹配（RFM）** 训练生成模型，学习从噪声分布到目标参数分布的映射，替代确定性回归。
  - 结合 **测试时优化（Test-Time Optimization, TTO）** 与 **大规模预训练**，在推理阶段对特定场景进一步优化。
  - 训练损失不包含人工标注的深度标签，仅使用自监督/重建损失驱动（文中未详列损失项，强调“no human-annotated depth labels”）。
- **公式/算法说明**：论文虽未给出具体公式，但核心流程可概括为：定义流形上的条件概率路径 → 学习速度场/向量场 → 通过流匹配目标训练生成模型 → 推理时从先验采样并沿流形上的ODE积分，得到有效的4D高斯参数。

## 3. 实验设计
- **数据集与场景**：文中未列出具体数据集名称（如RealEstate10K、DNeRF或自采数据），仅说明使用**单目视频**进行训练和评估；预训练覆盖大规模场景（超过10,000个场景用于摊销计算）。
- **Benchmark**：动态重建质量采用 **F-score** 作为主要指标（越高越好）；新视角合成质量未给出具体指标名称，但声称优于对比方法；不确定性采用 **负高斯对数似然（Negative Gaussian Log-Likelihood）** 和 **期望校准误差（Expected Calibration Error, ECE）** 量化。
- **对比方法**：
  - 先前深度万物模型（Depth Anything 系列）。
  - 逐场景优化的 4D-GS（per-scene 4D-GS）。
  - 确定性MLP基线（同数据、同架构、同TTO，用于隔离RFM贡献）。

## 4. 资源与算力
- 论文明确给出了修正后的计算成本：**预训练需要360 GPU小时**。
- 未说明GPU型号（如A100/H100）及集群规模。
- 指出该成本在大规模部署（超过10,000个场景）时可以摊销，即单场景平均成本很低。
- 测试时优化（TTO）的额外算力未明确。

## 5. 实验数量与充分性
- **受控实验**：将RFM的贡献与TTO和预训练完全分离，设置同架构确定性MLP基线（F-score 0.762）对比RFM（0.806），该对比公平且干净。
- **消融内容**：主要消融是“有无RFM”，其他组件（TTO、预训练）保持一致。
- **指标覆盖**：除F-score外，还包括不确定性量化（NLL、ECE）和对比实验（先前深度万物模型、4D-GS）。
- **充分性评价**：实验设计在隔离单一变量（RFM）上较为严谨，但数据集多样性、场景类型、与更多SOTA方法的对比不够详细；缺乏视觉质量（PSNR/SSIM/LPIPS）等常见渲染指标报告，可能不够全面。

## 6. 论文的主要结论与发现
- RFM在4D高斯泼溅参数上建模，相比确定性回归，独立贡献 **+0.044 F-score**（0.806 vs 0.762）。
- DAV4在动态重建和新视角合成上优于先前深度万物模型和逐场景4D-GS。
- 不使用人工标注深度标签，证明了基于流形感知的生成式方法能摆脱深度监督，实现无监督/自监督动态重建。
- 预训练成本可摊销，适合大规模部署。

## 7. 优点
- **方法创新**：将黎曼流匹配引入4D场景重建，解决非欧几里得参数空间的有效性问题，具有理论新颖性。
- **实验隔离**：通过相同数据/架构/TTO的基线，清晰分离出RFM的贡献，归因可信。
- **无深度标签**：减少了对人工标注的依赖，有利于数据扩展。
- **不确定性量化**：提供NLL和ECE，使模型具有可靠的不确定性估计。
- **实用性**：预训练-微调范式有利于大规模应用。

## 8. 不足与局限
- **信息缺失**：文本中未列出具体的训练数据集、视频来源、场景类别，无法判断泛化范围。
- **指标有限**：只报告F-score，缺乏PSNR、SSIM、LPIPS等新视角合成常用指标，对比不够完整。
- **对比方法不足**：仅对比“先前深度万物”和“4D-GS”，未与更多近期动态重建方法（如动态NeRF、其他4D高斯方法）比较。
- **算力细节模糊**：未说明GPU型号、batch size、训练轮数等，360 GPU小时的具体配置不明。
- **论文状态**：arXiv页面显示该版本已撤回（withdrawn），可能意味着结果或方法存在争议、不可复现或已被替换，需谨慎参考。
- **应用限制**：从单目视频重建动态4D本身具有病态性，模型对动态遮挡、大运动、复杂拓扑等鲁棒性未讨论。

（完）
