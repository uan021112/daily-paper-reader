---
title: A Cross-Model VLM-Judge Protocol for Single-Image 3D Mesh Quality (and Why Cheap Proxies Fall Short)
title_zh: 单图像3D网格质量的跨模型VLM评判协议（以及为何廉价代理不够好）
authors: "Ali Asaria, Tony Salomone, Deep Gandhi"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.18451v1"
tags: ["query:dgen"]
score: 8.0
evidence: 评估单图像到3D网格的质量
tldr: 单图转3D网格生成进展迅速，但缺乏自动评估方法。本文提出基于VLM的评估协议：固定24视角渲染、两个独立VLM裁判、强制位置偏差校正。实验表明，两个裁判一致性高（kappa=0.66），而廉价代理（CLIP相似性、几何有效性）不可靠：CLIP为随机水平，几何有效性呈双峰分布，仅在缺陷明显时有效。因此推荐VLM协议作为可靠评估标准，不建议使用几何/CLIP代理作为优化目标。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.18451v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1448, \"height\": 617, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.18451v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 820, \"height\": 730, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.18451v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1208, \"height\": 390, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.18451v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1399, \"height\": 318, \"label\": \"Table\"}]"
motivation: 现有廉价自动代理（CLIP相似性和几何有效性）无法可靠反映单图3D网格的感知质量。
method: 提出固定24视图渲染、两个独立VLM裁判及位置偏差校正的评估协议。
result: 两个VLM裁判一致性高（kappa=0.66），廉价代理表现差：CLIP随机，几何有效性双峰且仅在缺陷明显时有效。
conclusion: 推荐VLM协议作为可靠评估器，不建议使用几何/CLIP代理作为优化目标。
---

## 摘要
单图像到3D生成器正在快速改进，但目前尚无公认的、无需人工的方法来判断一个生成的网格是否比另一个更好。实践者通常依赖廉价的自动代理（渲染空间CLIP相似度和网格几何有效性统计），但这些代理在多大程度上跟踪感知质量尚未确定。我们做出两项贡献。首先，我们提出并验证了一个可复现的VLM评判评估协议：一个固定的24视角无头渲染装置，两个独立的视觉语言评判家族，以及一个强制的位置偏差校正，该校正查询两种展示顺序并仅保留顺序一致的判断。两个评判家族之间具有实质一致性（Cohen's kappa = 0.66），远高于随机一致性基线。其次，使用此协议作为参考，我们证明廉价代理不能替代它。几何有效性平均而言仅是一个弱信号（因为，我们显示，它是双峰的），并且低于我们预先注册的目标，而渲染CLIP处于随机水平。一个学习的Bradley-Terry头部收敛到一个单一流形统计量（给渲染CLIP赋予负权重），并且与仅几何的模型完全匹配，因此学习特征权重毫无所得。代理也是双峰的：在具有可见几何缺陷的对比中显著高于随机水平，而在模糊对比中处于随机水平，这与几何有效性仅在缺陷视觉显著时跟踪评判者的情况一致。因此，我们推荐VLM评判协议作为在测试条件下（Google Scanned Objects上的两个前馈生成器，带有面丢弃退化方案）可靠、可复现的评估器，并建议不要将几何/CLIP代理作为优化目标。

## Abstract
Single-image-to-3D generators are improving quickly, but there is no agreed, human-free way to tell whether one generated mesh is better than another. Practitioners commonly rely on cheap automatic proxies (render-space CLIP similarity and mesh geometry-validity statistics), yet how well these track perceived quality is unestablished. We make two contributions. First, we propose and validate a reproducible VLM-judge evaluation protocol: a fixed 24-view headless render rig, two independent vision-language judge families, and a mandatory position-bias correction that queries both presentation orders and keeps only order-consistent verdicts. The two judge families agree substantially with each other (Cohen's kappa = 0.66), well above the chance-agreement floor. Second, using this protocol as the reference, we show the cheap proxies do not substitute for it. Geometry validity is only a weak signal on average (because, as we show, it is bimodal) and stays below our pre-registered target, while render-CLIP is at chance. A learned Bradley-Terry head collapses onto a single manifoldness statistic (giving render-CLIP a negative weight) and matches geometry-only exactly, so learning the feature weights buys nothing. The proxy is also bimodal: it is significantly above chance on contrasts with visible geometric defects but at chance on ambiguous contrasts, consistent with geometry validity tracking the judge only when the defect is visually salient. We therefore recommend the VLM-judge protocol as a reliable, reproducible evaluator under the conditions tested (two feed-forward generators on Google Scanned Objects, with a face-drop degradation regime) and advise against geometry/CLIP proxies as optimization targets.

---

## 论文详细总结（自动生成）

# 论文中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：单图像到3D网格生成技术发展迅速，但缺乏公认的、无需人工干预的自动评估方法。实践者常使用廉价代理（如渲染空间CLIP相似度和网格几何有效性统计）来评估生成网格的质量，但这些代理在多大程度上与人类感知质量一致尚未得到系统验证。
- **研究动机**：为单图3D网格质量提供可靠、可复现、无人类标签的评估协议，并检验廉价代理能否替代该协议。
- **整体含义**：若廉价代理不可靠，则基于它们的排名、优化或奖励信号会误导3D生成器的对齐工作。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：使用两个独立的视觉语言模型（VLM）作为裁判，在固定多视角渲染上进行比较，并强制校正位置偏差，以此作为质量参考；然后将廉价代理与此参考对比。
- **关键技术细节**：
  - **固定渲染装置**：将所有网格归一化到单位包围盒，使用无头离屏光栅化器从固定24视角（转台式）渲染，确保一致输入。
  - **VLM裁判家族**：两个独立开源VLM：裁判X（Qwen2.5-VL-7B-Instruct，用作预言机）和裁判Y（InternVL3-8B，用作验证裁判）。两者不同，可报告交叉模型一致性作为可靠性指标。
  - **位置偏差校正**：对每一对网格，先后以两种顺序（A,B和B,A）询问裁判，仅当两次判断一致时才保留该判决；不一致的判为位置偏差并丢弃。实验显示约26%的原始判决不一致，校正确有必要。
  - **廉价代理**：为每个网格提取五个特征：水密性、流形性、无自交、法向一致性、渲染CLIP相似度（使用OpenCLIP ViT-B-32）。基于这些特征构建：
    - 几何有效性得分（几何特征组合，如五项特征的无权重组合？实际文中是分别使用五个特征，但几何得分具体如何？从表1看“geometry-only”是基于五项特征？但更准确是：几何有效性通常指watertightness, manifoldness, non-self-intersection, normal consistency之和？文中未明确定义“geometry-only”的计算方式，但从结果看是所有几何特征无权重组合？）。从学习和图来看，几何得分是五个几何特征（不包含CLIP）的简单组合？实际上表1提到“Geometry-only”是“geometry validity”，而“render-CLIP only”是单独的。固定复合是固定权重的组合。学习头是通过逻辑回归学习权重。具体细节：文中说“From these we form: (i) a geometry-only score, (ii) a render-CLIP-only score, (iii) a fixed-weight composite, and (iv) a learned pairwise Bradley–Terry head”。几何得分可能简单平均或取逻辑与？但论文未详细说明几何得分公式，仅作为代理使用，重点在其表现。
    - **学习Bradley-Terry头**：在裁判X标签上训练逻辑回归模型 P(a ≻ b) = σ(w^T(φ(a)-φ(b)))，其中φ是五维特征向量。在独立裁判Y上评估。

## 3. 实验设计：数据集、基准、对比方法
- **数据集**：Google Scanned Objects（公开，CC-BY 4.0，3069个对象目录）。随机采样N=60个对象用于主要评估。
- **基准**：以两个VLM裁判之间的跨模型一致性作为可靠性基准（预期高），以0.5的随机一致性作为基线。
- **对比方法**：
  - **生成方法**：两个单图转3D前馈生成器作为黑盒：Stable Fast 3D和TripoSR。每个对象生成4个候选：两个生成器的原生输出，以及每个生成器的面丢弃退化变体（故意移除一些面）。这样形成质量谱系。
  - **被评估的代理**：
    - 几何有效性得分（geometry-only）
    - 渲染CLIP相似度（render-CLIP only）
    - 固定权重复合（fixed composite）
    - 学习的Bradley-Terry头（learned head）
  - **评估指标**：代理与验证裁判Y在成对比对上的准确率（Agreement），使用Wilson 95%置信区间，Cohen's kappa，以及双峰检验（χ²或z检验）。

## 4. 资源与算力
- **算力**：研究使用了约 **3.4 H100-hours**，分布在14个已完成的作业（一个运行被丢弃）上，使用单个H100级GPU（主要是H100-SXM5）。论文未详细说明每个作业的具体时长或总训练时间。

## 5. 实验数量与充分性
- **实验数量**：
  - 主要评估使用N=60个对象，共生成4×60=240个网格，形成60个对象×每个对象6对=360对，但由于位置校正后最终得到262个位置一致对。
  - 对于学习头，严格按对象划分训练集和测试集（训练对象对和测试对象对，测试集有98对）。
  - 进行了多维度分析：全数据集准确性、按子组（跨生成器、同一生成器退化等）分层准确性、校准曲线（按奖励差距分层）、以及学习头部权重分析。
- **充分性与公平性**：
  - **充分**：实验覆盖了两种代表性生成器、一种常见退化模式、60个对象，统计方法严格（cluster bootstrap、Wilson CI、Cohen's kappa），考虑了对象内聚类。
  - **不足**：
    - 仅测试了Google Scanned Objects一个数据集，且对象类别有限。
    - 退化仅使用面丢弃，未覆盖薄结构、透明度、多对象等自然失败模式。
    - N=60的对象数量较小，子组分析（n=43/50/119/50）为探索性，未进行多重比较校正，单细胞结果应视为假设生成性质。
    - 未进行人工验证（human spot-check），因此“可靠性”仅指跨模型一致性，而非与人类评判的契合度。
  - **公平性**：学习头在训练时使用裁判X标签，在测试时对裁判Y评估，避免了过拟合；规则基代理无训练，直接在所有对评估。但注意学习头可能过拟合到裁判X的偏见。

## 6. 论文的主要结论与发现
- **VLM裁判协议可靠**：两个独立VLM裁判在120对双重标记对上的准确率为0.83（Cohen's kappa=0.66，实质一致性），远高于随机基线（0.51）。位置偏差校正至关重要（原始一致性可能低至0.333，校正后0.714）。
- **廉价代理不可替换**：
  - 几何有效性：全数据集准确率0.62（95% CI [0.55,0.69]），显著高于随机但远低于预设目标≥0.75。
  - 渲染CLIP：准确率0.48（CI [0.42,0.54]），与随机无显著差异，不含可用质量信号。
  - 学习Bradley-Terry头：所有权重几乎全集中在流形性（2.16），渲染CLIP权重为负（-0.11），结果与纯几何得分完全相同，学习无收益。
- **几何代理具有危险的双峰性**：
  - 在可见几何缺陷的对比中（跨生成器：0.91；TripoSR内退化：0.80），准确率显著高于随机。
  - 在模糊对比中（跨生成器混合：0.53），准确率接近随机。
  - 两组差异显著（z=5.06, p<0.0001）。这意味着代理在“容易”的对比中表现良好，但在实际需要精细判断的模糊情况下失效，容易误导用户。
- **校准提示**：几何代理在奖励差距大时准确率略高（0.67）比差距小时（0.57），但差异不显著。

## 7. 优点
- 提出可复现的VLM评判协议，包含固定渲染、双裁判、位置偏差校正，减少了单模型偏差和顺序效应。
- 直接测量了常用代理与VLM裁判的一致性，揭示了它们的不可靠性和双峰性，为社区提供实证警告。
- 使用了严格的统计方法（cluster bootstrap、Cohen's kappa），考虑对象内相关性。
- 通过交叉模型验证增强可靠性，而非仅依赖单个VLM。
- 对学习头进行分析，展示了特征集本身的局限性（学习会退化到单一几何指标）。

## 8. 不足与局限
- **VLM裁判作为真理**：论文将VLM裁判（特别是裁判X）视为地面真相，但VLM本身可能不完美。虽然通过交叉模型一致性缓解，但无法保证与人类评判一致。作者承认“the judge is the oracle”。
- **狭窄的实验场景**：仅测试了两个前馈生成器（Stable Fast 3D, TripoSR）、一个物体数据集（Google Scanned Objects）、一种退化模式（面丢弃）。自然失败模式（薄结构、透明、多对象）未考虑。结果可能无法泛化到其他生成器（如扩散式生成器或多视图方法）或不同数据集。
- **一个子组存在混淆**：在Stable Fast 3D内退化对比中，裁判自身对完整vs退化网格的判断也处于随机水平（0.40），因为面丢弃对开放壳体几乎不可见。因此该子组不能用于衡量代理失败，论文已排除此单元格的失败主张。
- **探索性分析未校正多重比较**：四个子组分析（n=43/50/119/50）被明确声明为探索性，未应用形式化多重比较校正，单细胞方向性读数应视为假设生成性质。
- **未进行人工验证**：缺乏人类评判作为金标准，因此论文的“可靠性”停留在跨模型一致性层面，而非与感知质量的一致性。
- **计算资源相对有限**：仅使用单GPU运行约3.4 H100小时，但考虑到任务规模（60个对象的渲染+推理）合理。

（完）
