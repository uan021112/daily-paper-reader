---
title: Unpaired RGB-Thermal Gaussian-Splatting Using Visual Geometric Transformers
title_zh: 无配对RGB-热成像高斯泼溅：使用视觉几何变换器
authors: "Jean Cordonnier, Chenghao Xu, Olga Fink, Malcolm Mielle"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.05491v1"
tags: ["query:ffdr"]
score: 8.0
evidence: 利用VGGT 3D前馈Transformer进行位姿估计和多模态高斯泼溅
tldr: 针对现有RGB-热红外多模态新视图合成依赖配对校准或立体系统的问题，提出一种无配对框架。利用VGGT三维前馈变压器独立估计各模态相机位姿，通过Procrustes算法和跨模态特征匹配实现联合对齐。在此基础上，提出多模态3D高斯泼溅方法直接学习无配对图像。实验在多种场景下实现有竞争力的热视图合成并保持RGB保真度，同时引入基准评测框架评估模态一致性和交叉模态连贯性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05491v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1605, \"height\": 467, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05491v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1799, \"height\": 389, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05491v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1445, \"height\": 1131, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05491v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 901, \"height\": 430, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.05491v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1351, \"height\": 279, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05491v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 562, \"height\": 230, \"label\": \"Table\"}]"
motivation: 现有RGB-热红外新视图合成依赖精确配对的图像对或立体装置，限制了可扩展性和实际部署。
method: 使用VGGT独立估计位姿，Procrustes对齐结合跨模态匹配，再以多模态3D高斯泼溅进行无配对学习。
result: 在多种场景下热视图合成性能有竞争力，同时保持RGB保真度，且重建具有跨模态一致性。
conclusion: 提出了无配对RGB-热红外NVS框架和基准，推动了多模态3D重建的实用化与评估标准化。
---

## 摘要
结合RGB和热成像的多模态新视角合成（NVS）能够利用视觉和热信息实现精确的3D场景重建。然而，现有方法通常依赖于精确校准的RGB-热成像图像对或立体设置，限制了可扩展性和实际部署。为了解决这一问题，我们提出了一种无配对RGB-热成像NVS框架，利用VGGT（一种3D前馈transformer架构）独立估计每种模态的相机位姿。然后，通过跨模态特征匹配器使用Procrustes算法对齐位姿集合，从而在无需配对校准的情况下实现联合配准。基于此对齐，我们进一步提出了一种多模态3D高斯泼溅方法，直接从无配对的RGB和热成像图像中学习。在多种场景上的实验表明，我们的方法在热成像视角合成中取得了具有竞争力的性能，同时保持了RGB保真度。此外，我们表明现有的重建方法可以产生缺乏跨模态一致性的模态特定重建。因此，我们引入了一个基准测试框架，以严格评估每个模态的图像合成以及重建场景的多模态一致性。

## Abstract
Multi-modal novel view synthesis (NVS) combining RGB and thermal imagery enables precise 3D scene reconstruction with visual and thermal information. However, existing methods typically rely on precisely calibrated RGB-thermal image pairs or stereo setups, limiting scalability and practical deployment. To address this, we introduce a framework for unpaired RGB-thermal NVS that leverages VGGT, a 3D feed-forward transformer architecture, to independently estimate camera poses for each modality. The pose sets are then aligned using the Procrustes algorithm with a cross-modal feature matcher, enabling joint registration without paired calibration. Building on this alignment, we further propose a multi-modal 3D Gaussian Splatting approach that learns directly from unpaired RGB and thermal images. Experiments on diverse scenes demonstrate that our method achieves competitive performance in thermal view synthesis while maintaining RGB fidelity. Moreover, we show that existing reconstruction approaches can produce modality-specific reconstructions that lack cross-modal consistency. We thus introduce a benchmarking framework to rigorously evaluate both per-modality image synthesis and the multi-modal coherence of reconstructed scenes.

---

## 论文详细总结（自动生成）

# 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题**：现有 RGB-热成像多模态新视角合成（NVS）方法依赖精确配对的 RGB-T 图像对或立体装置，需要进行精确的跨模态标定，导致系统成本高、部署灵活性差，难以扩展到不同传感器独立采集的场景（如多机器人建图）。
- **背景**：热成像图像纹理弱、存在鬼影效应，传统 SfM（如 COLMAP）难以从中稳定估计相机位姿；而已有方法（如 ThermoNeRF、Thermal Gaussian）依赖于将 RGB 位姿直接迁移给热图像，限制了数据获取方式。
- **整体含义**：本文首次提出一种无需配对、无需立体标定的 RGB-T NVS 框架，允许两种模态的图像独立采集，然后自动对齐并联合重建，大幅提升了多模态三维重建的实用性和可扩展性。

# 2. 论文提出的方法论

- **核心思想**：将 RGB 和热成像视为两个独立的图像集，分别用 VGGT（视觉几何基础Transformer）估计每张图像的相机位姿（内外参），再通过跨模态特征匹配（XoFTR）建立对应点，利用 Procrustes 算法计算相似变换（平移、旋转、缩放），将热成像位姿对齐到 RGB 坐标系，最后用改进的 3D 高斯泼溅（3DGS）同时学习 RGB 和热场。
- **关键技术细节**：
  1. **独立位姿估计**：对热成像，将单通道复制为三通道输入 VGGT（无需微调），VGGT 直接输出相机内参、外参、深度图、点云。
  2. **跨模态对齐**：
     - 使用 XoFTR 匹配 RGB 与热图像的特征点，MAGSAC 过滤外点，保留 >150（单模态）/ >12（跨模态）对应关系的图像对。
     - 利用 VGGT 输出的深度置信度进一步筛选（置信度 < 50% 分位数的点被剔除），得到 3D 对应点云：\( P_{RGB} \) 和 \( P_{T} \)。
     - 采用修剪 Procrustes 算法（类似修剪 ICP，但用 Procrustes 代替 ICP 迭代）：
       \[
       \min_{s,R,t} \sum_{i=1}^N \| p_{i,RGB} - (s R p_{i,T} + t) \|^2
       \]
       每次迭代丢弃 10% 残差最大的点，直至收敛，得到相似变换矩阵 \( T_{Sim(3)} \)。
     - 将热成像位姿 \( T_T \) 变换到 RGB 坐标系：\( T_T^* = T_T \, T_{Sim(3)}^{-1} \)。
  3. **多模态 3D 高斯泼溅**：
     - 每个高斯体同时包含 RGB 颜色 c 和热颜色 t（均含球谐系数），共享几何参数（位置、协方差、不透明度）。
     - 训练时每个 epoch 随机打乱 RGB 和热图像顺序，每次随机渲染一对图像（无空间对应约束），损失函数为：
       \[
       \mathcal{L}_{\text{total}} = \mathcal{L}_{\text{SSIM}}(Temp_{pred}, Temp_{gt}) + \mathcal{L}_{\text{SSIM}}(RGB_{pred}, RGB_{gt})
       \]
       其中 \( \mathcal{L}_{\text{SSIM}}(X,Y) = (1-\lambda)\cdot MAE + \lambda\cdot (1-\text{SSIM}) \)。
- **算法流程**：图 1 展示了整体流程：VGGT 独立估计 → XoFTR 匹配 → Procrustes 对齐 → 3DGS 联合训练。

# 3. 实验设计

- **数据集**：使用 ThermalGaussian [6] 和 ThermoScenes [2] 中的 9 个场景（Building A Spring、DimSum、Truck、Kettle、INR Building 等），每个场景包含配对的 RGB 和热图像。论文人为构造无配对数据：从每一对中随机采样 RGB 或热一张，保证总数相等且无配对对应。
- **基准（Benchmark）**：本文自己引入了新的评估框架，区分 **inter-modal**（用 RGB 位姿评估热合成 / 用热位姿评估 RGB 合成）和 **intra-modal**（各模态用自己位姿评估）。这是主要基准。
- **对比方法**：ThermalGaussian（T-GS），这是理想配对情况下的 SOTA 方法。对比了 T-GS 分别用 RGB 位姿和热位姿训练的结果（T-GS RGB / T-GS T）。
- **指标**：PSNR、SSIM、LPIPS（RGB 仅）、温度 MAE；位姿评估用 ATE 和 RPE（先缩放至单位球内）。
- **消融实验**：手动对齐（已知初始位姿 + ICP）与本文 Procrustes 对齐的对比（图 3），展示未对齐会学到两个独立的表示。

# 4. 资源与算力

- **未明确说明**：论文中仅提到“Our GD implementation uses the same default parameters as nerfacto”以及默认参数设置，未提及 GPU 型号、数量、训练时长等具体算力信息。

# 5. 实验数量与充分性

- **实验数量**：9 个不同场景，覆盖室内外、不同纹理和热特征；每个场景有 RGB 和热测试集；进行了一次主实验（表 I、II）和位姿误差分析（表 III），以及一个手动对齐对比（图 3）。
- **充分性**：实验设计较为合理，但规模偏小（仅 9 个场景），且排除了 XoFTR 匹配失败的低纹理场景，可能存在选择偏差。对比方法只有 T-GS 一种（理想配对情况），缺乏与其他无配对方法或传统 SfM 方法的对比。消融实验仅对比了替代对齐策略，未对 VGGT 本身或特征匹配策略进行深入消融。总体而言，实验能支撑核心结论，但充分性有提升空间。

# 6. 论文的主要结论与发现

- 使用未微调的 VGGT 可以直接从热成像中估计合理的相机位姿，展示了 transformer 的跨模态泛化能力。
- 提出的无配对框架在有挑战的场景下能接近理想配对方法 T-GS 的性能，特别是在热视角合成方面（如用热位姿合成时 PSNR 达到 25.595，优于 T-GS T 的 25.107）。
- 位姿对齐仍然存在误差（平均 ATE 位置 0.08m，旋转 0.07°），导致 inter-modal 指标（如热从 RGB 位姿合成）下降明显（PSNR 从 25.595 降至 21.315）。
- 如果不对齐，模型会学到两个独立的 3D 表示（图 3），导致高 intra-modal 指标但跨模态不一致，因此必须同时报告 inter-modal 和 intra-modal 指标。

# 7. 优点

- **无配对灵活性**：首次实现无需配对或立体标定的 RGB-T NVS，降低数据采集成本。
- **方法高效**：利用 VGGT 一次性估计所有位姿，无需传统 SfM 的迭代优化；使用现成特征匹配器 XoFTR，简化跨模态对应。
- **评估框架创新**：首次提出 inter-modal 与 intra-modal 双维度评估，揭示了以往被忽视的跨模态一致性问题，为后续研究提供了更严格的评价标准。
- **跨模态泛化能力验证**：证明 VGGT 在没有微调的情况下也能处理热成像，暗示 3D 前馈 Transformer 的广阔适用性。

# 8. 不足与局限

- **匹配依赖性**：依赖 XoFTR 的跨模态匹配质量，对低纹理、极端热变化场景（如纯黑体或过热区域）鲁棒性不足，论文主动排除了这类场景（仅保留 9 个），“ cherry-picking ”嫌疑。
- **对齐精度有限**：Procrustes 对齐后位姿仍有误差（ATE 位置可达 0.16m），导致 inter-modal 热合成质量下降明显，实际应用中可能无法接受。
- **对比基线的局限性**：仅与理想配对方法 T-GS 对比，未与其他无配对基线（如分别训练两个单模态模型然后对齐域）对比，也未能与使用 COLMAP 的热 SfM 结果对比（因热图像难以用 COLMAP）。
- **数据集规模小**：9 个场景不足以全面验证泛化能力，尤其缺少动态环境或多视角大场景。
- **算力与训练细节缺失**：未报告 GPU 型号、训练时间、内存消耗等，可重复性稍差。
- **VGGT 与热模态的适配问题**：仅将单通道复制为三通道，未针对热图像特性（如非线性辐射、低对比度）设计任何预处理，可能影响位姿精度。
- **缺乏真实无配对测试**：使用配对不同构造的无配对，是否等同于真实独立采集数据仍有差异。

（完）
