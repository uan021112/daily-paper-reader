Title: Learned Radius Estimation for UDF-Based Point Cloud Reconstruction

URL Source: https://arxiv.org/pdf/2606.18787v1

Published Time: Thu, 18 Jun 2026 00:41:33 GMT

Number of Pages: 2

Markdown Content:
# Learned Radius Estimation for UDF-Based Point Cloud Reconstruction 

Eito Ogawa 

Graduate School of FSE Waseda University 

Tokyo, Japan ogawaeito@fuji.waseda.jp 

Hiroshi Watanabe 

Graduate School of FSE Waseda University 

Tokyo, Japan hiroshi.watanabe@waseda.jp 

Abstract —Surface reconstruction from point clouds is im-portant for consumer-grade 3D capture, including AR/VR and indoor scanning. Local-patch Unsigned Distance Field (UDF) methods are lightweight and generalizable, but their accuracy depends on the support radius, traditionally fixed or selected by a one-dimensional curvature heuristic that cannot capture heterogeneous local geometry. We propose a learned per-query radius selector that predicts a continuous support radius and plugs into a frozen LoSF-UDF backbone. The selector is trained using off-grid target radii obtained by parabolic interpolation of cached UDF error curves. Experiments show improved fine-scale reconstruction accuracy, with ScanNet F1@0.005 improving from 0.645 to 0.691 over a curvature-based baseline. 

Index Terms —3D reconstruction, unsigned distance field, point cloud, adaptive radius selection, consumer electronics 

I. I NTRODUCTION 

With the widespread use of smartphones, AR/VR devices, and home robots, efficient 3D reconstruction for consumer-grade capture has become increasingly important. Point clouds captured by such consumer devices typically exhibit non-uniform sampling density and local sparsity, requiring re-construction methods that robustly handle both closed and open surfaces. Unsigned Distance Fields (UDFs) [1] avoid in-side/outside classification, and LoSF-UDF [2] estimates UDF values locally from point-cloud patches in a lightweight man-ner. Our previous method, GeoLA [3], adapts the local support radius via a curvature heuristic. However, a scalar curvature descriptor cannot capture heterogeneous local geometry. In this paper, we propose a learned per-query radius selector that replaces this heuristic with a data-driven mapping. The selector is trained using interpolated target radii derived from local parabolic interpolation of cached UDF error curves, providing off-grid supervision beyond the discrete candidate set. It plugs into the frozen LoSF-UDF backbone without retraining while keeping the overall pipeline compact. II. R ELATED WORK 

Implicit neural representations such as signed distance and occupancy fields require inside/outside classification, limiting them to closed surfaces. UDFs avoid this by representing the unsigned distance to the nearest surface, supporting both closed and open surfaces. LoSF-UDF [2] estimates the UDF LoSF 

> UDF
> Mesh
> Extraction
> DCUDF

## 𝑞  𝑅  𝑞  Ƹ 𝑟 

> selector

Fig. 1. Overview of the proposed framework. For each query point, a parent patch with radius R is extracted, from which the selector predicts an adaptive support radius ˆr. The selected patch with radius ˆr is fed into the frozen LoSF-UDF backbone to estimate the UDF value, which is then used for mesh extraction with DCUDF [4]. 

locally from a neighborhood patch around each query point, rather than compressing the point cloud into a global latent representation. This local design yields a lightweight network that generalizes across shapes. However, the patch radius is fixed across the dataset: an overly large radius causes multi-surface contamination at fine details, while an overly small one provides insufficient local context for UDF estimation. III. P ROPOSED METHOD 

Our framework consists of (i) an offline procedure that derives interpolated target radii from cached UDF error curves, and (ii) a lightweight selector that regresses the per-query radius from the parent patch and plugs into the frozen LoSF-UDF backbone without retraining. 

A. Target Radius Estimation via Local Parabolic Interpolation 

For each query q, we construct K local patches at candidate radii {r1, . . . , r K } and pass them through the frozen LoSF-UDF backbone, obtaining a per-query error vector e ∈ RK

against the ground truth. From the discrete argmin k∗ =arg min k ek, we fit a parabola through (ek∗−1, e k∗ , e k∗+1 ) and take its vertex as the interpolated target radius r∗

δ = 12 · ek∗−1 − ek∗+1 

ek∗−1 − 2ek∗ + ek∗+1 

, r∗ = rk∗ + δ · ∆r, (1) where ∆r is the candidate spacing. On the boundary or in non-convex neighborhoods, we fall back to rk∗ . This provides an off-grid regression target instead of a discrete candidate label. These cached target radii are used only during training; no test-time ground truth is required.                                                         

> arXiv:2606.18787v1 [cs.CV] 17 Jun 2026 TABLE I QUANTITATIVE RESULTS
> Cars [5] Fashion [6] ScanNet [7] Method CD ↓F1 0.005 ↑F1 0.01 ↑NC ↑CD ↓F1 0.005 ↑F1 0.01 ↑NC ↑CD ↓F1 0.005 ↑F1 0.01 ↑NC ↑
> LoSF-UDF [2] 0.794 0.506 0.865 0.594 0.442 0.646 0.941 0.951 0.715 0.651 0.948 0.876
> GeoLA [3] 0.732 0.571 0.893 0.808 0.422 0.657 0.945 0.946 0.795 0.645 0.899 0.824 Ours 0.705 0.581 0.896 0.822 0.404 0.681 0.946 0.942 0.692 0.691 0.945 0.855

B. Selector Architecture and Loss 

The parent-patch points P ∈ RN ×3 are encoded by a masked ResNet-PointNet into a 256-dimensional feature ϕ,concatenated with the point count log(1 + N ) and a radial density histogram h ∈ R6. A three-layer MLP with sigmoid output yields a continuous radius ratio ρ ∈ [ρmin , ρ max ], giving the final radius ˆr = ρ · Rparent . We train with a confidence-weighted normalized L1 loss: 

L = w · |ˆr − r∗|

rmax − rmin 

, (2) where w = max e − min e emphasizes samples whose UDF error is sensitive to the radius choice. At inference, a single selector forward pass yields ˆr, independent of K used during training. IV. E XPERIMENTS 

We evaluate surface reconstruction accuracy on ScanNet [7], ShapeNet-Cars [5], and DeepFashion3D [6], using 100 ran-domly sampled test shapes from each dataset. For selector training, we use 40 cars from ShapeNet-Cars and 40 garments from DeepFashion3D, disjoint from the test shapes. ScanNet is held out entirely from training, providing a cross-domain evaluation of generalization to real indoor scans. We use 

K = 7 candidate radii in {0.012 , 0.014 , . . . , 0.024 } with parent radius Rparent = 0 .024 , corresponding to ρ ∈ [0 .5, 1.0] ,and adopt the released pretrained LoSF-UDF [2] backbone, kept frozen during selector training. Final meshes are extracted from the predicted UDF field using DCUDF [4]. We report bidirectional L1 Chamfer Distance (CD, ×10 2), F-score at thresholds 0.005 and 0.01 , and Normal Consistency (NC). Table I compares our method against the curvature-based GeoLA [3] and the fixed-radius LoSF-UDF [2]. Our method achieves the best CD and F1@0.005 across all three datasets, including the held-out ScanNet, demonstrating that the learned selector generalizes from only 80 CAD/garment training shapes to real indoor scans. Over GeoLA on ScanNet, F1@0.005 improves from 0.645 to 0.691 and CD from 0.795 to 0.692; improvements over GeoLA are also observed on ShapeNet-Cars and DeepFashion3D. While the fixed-radius LoSF-UDF retains higher F1@0.01 and NC for ScanNet (0.948 vs. 0.945, 0.876 vs. 0.855), the consistent advantage of our method on the strict-distance metrics (CD and F1@0.005) confirms the benefit of learning radius selection from local patch features rather than from a single curvature scalar. Fig. 2 shows ScanNet reconstructions: the curvature-based GeoLA tends to oversmooth or drop local detail in regions with non-uniform sampling such as wall-floor boundaries and GT LoSF -UDF 

GeoLA Ours  

> Fig. 2. Reconstruction images on ScanNet. From left to right and top to bottom: GT, LoSF-UDF, GeoLA, and ours. Red boxes highlight regions where ours preserves fine boundaries and local geometric details.

occlusion edges, whereas the proposed selector preserves the local structure through its learned density-aware features. V. C ONCLUSION 

We proposed a learned per-query radius selector trained using interpolated target radii derived from parabolic interpo-lation. Trained on only 80 CAD/garment meshes, our method generalizes to real indoor scans (ScanNet), outperforming the curvature-based prior GeoLA on strict-distance metrics across three datasets. The full pipeline (backbone + selector) totals 4.7 MB of fp32 parameters and requires no backbone retrain-ing, making it a compact plug-in component for consumer-grade 3D capture. Future work includes robustness to noise and missing regions, and extension to dynamic scenes. REFERENCES [1] J. Chibane et al. , “Neural Unsigned Distance Fields for Implicit Function Learning,” in Adv. Neural Inf. Process. Syst. (NeurIPS) , 2020. [2] J. Hu et al. , “A Lightweight UDF Learning Framework for 3D Recon-struction Based on Local Shape Functions,” in Proc. IEEE/CVF Conf. Comput. Vis. Pattern Recognit. (CVPR) , 2025, pp. 1297–1307. [3] E. Ogawa et al. , “Accurate and Efficient Surface Reconstruction from Point Clouds via Geometry-Aware Local Adaptation,” in Proc. IIEEJ Int. Conf. Image Electronics and Visual Computing (IEVC) , 2026. [4] F. Hou et al. , “Robust Zero Level-Set Extraction from Unsigned Distance Fields Based on Double Covering,” ACM Trans. Graph. , vol. 42, no. 6, Art. 245, 2023. [5] A. X. Chang et al. , “ShapeNet: An Information-Rich 3D Model Repos-itory,” arXiv:1512.03012 , 2015. [6] H. Zhu et al. , “Deep Fashion3D: A Dataset and Benchmark for 3D Garment Reconstruction from Single Images,” in Proc. Eur. Conf. Comput. Vis. (ECCV) , 2020. [7] A. Dai et al. , “ScanNet: Richly-Annotated 3D Reconstructions of Indoor Scenes,” in Proc. IEEE Conf. Comput. Vis. Pattern Recognit. (CVPR) ,2017, pp. 2432–2443.